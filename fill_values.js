// ============================================================
//  fill_values.js
//
//  CommunityDragon 의 bin.json 에서 실제 수치를 읽어
//  custom_values.js 의 "?" 자리를 채운다.
//
//  사용법:
//      node fill_values.js            <- 미리보기. 파일을 만들지도 고치지도 않는다.
//      node fill_values.js --write    <- public/custom_values.new.js 생성
//
//  PRESERVE 에 적힌 챔피언은 기존 내용을 그대로 둔다.
// ============================================================

const fs = require('fs');
const path = require('path');
const { loadStringTable, getPassiveTooltip } = require('./stringtable');
const { getBin, cacheStats } = require('./bincache');
// 구분선 아래 회색 글씨. build_champion_data.js 와 **같은 모듈**을 써야 자리 수가 안 어긋난다.
//   문장은 build 가 "<슬롯>_rules" 로 찍고, 그 안의 {pN} 값을 여기서 채운다.
//   ★ 회색 글씨를 desc **뒤에** 이어 붙인다. build 쪽도 뒤에 붙이므로 번호가 같아진다.
const { belowLineMap, textOfSpellObj } = require('./belowline');

const WRITE = process.argv.includes('--write');

// 챔피언 블록을 통째로 안 건드릴 목록.
//   ★ 2026-08-08 비웠다. 가렌·갈리오는 {pN} 체계가 생기기 전 손으로 쓴 옛 형식이라
//     다른 챔피언과 구조가 달랐다 (pN 이 아예 없음). 이제 CD 에서 똑같이 받아온다.
//     손으로 쓴 v1/v2 는 extractVV() 가 원문 그대로 물려주므로 여기 안 적어도 안 날아간다.
const PRESERVE = [];

// @ShieldDuration.1@ / @f2.0@ 처럼 이름 끝에 붙는 ".숫자" 를 어떻게 볼 것인가.
//   true  = 소수점 자릿수로 본다 (1.53 -> ".1" 이면 1.5)
//   false = 그냥 떼어내고 무시한다 (원래 값 그대로)
//   ★ 배열 인덱스로 보는 해석도 있는데, 그러면 ".0" 이
//     "DataValues 0번은 쓰레기" 규칙과 정면으로 부딪힌다. 그래서 자릿수로 본다.
//     결과는 마지막에 목록으로 찍히니 인게임 툴팁과 대조할 것.
const DOT_AS_PRECISION = true;

const CD = 'https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/ko_kr/v1';
const BIN = 'https://raw.communitydragon.org/latest/game/data/characters';

const PUBLIC_DIR = path.join(__dirname, 'public');
const SRC_VALUES = path.join(PUBLIC_DIR, 'custom_values.js');
const OUT_VALUES = path.join(PUBLIC_DIR, 'custom_values.new.js');

const DELAY = 150;

// 미리보기에서 스킬 값을 한 줄씩 다 찍어 볼 챔피언.
//   눈으로 검산할 때만 쓴다. 평소엔 비워 두면 요약만 나온다.
//   예:  const PREVIEW = ['Aatrox', 'Ahri', 'MonkeyKing', 'Locke'];
const PREVIEW = [];

// ------------------------------------------------------------
// 손으로 확인한 값. 자동 추출보다 우선한다.
//
//   키는 미리보기에 찍히는 "챔피언 스킬 / 이름" 을 그대로 복사해 쓰면 된다.
//   영문 alias 로 써도 된다:  "MissFortune W / LoveTapRefund"
//
//   ★ 이 표에 적어 두면 재실행해도 안 날아간다.
//     자동 추출이 틀리거나 못 찾은 값을 확인할 때마다 여기에 쌓을 것.
//     (챔피언 통째로 지키려면 위의 PRESERVE 를 쓴다)
//
//   표에 있는데 한 번도 안 쓰인 키는 마지막에 경고로 알려준다. 오타 잡기용.
// ------------------------------------------------------------
const MANUAL = {
  '미스 포츈 W / LoveTapRefund': '2',
  '올라프 Q / TooltipCDRefund': '2.5',
  '사일러스 R / PerTargetCooldown': '200',
  '카타리나 E / DaggerCooldownReduction': '12 / 11 / 10 / 9 / 8',
  '애쉬 E / ChargeCooldown': '5',
  // ★ bin 5랭크 값이 9999(무제한 표식)라 화면에 "3.25 / … / 9999초" 로 나가고 있었다 (2026-08-24).
  //   롤위키 blurb "At max rank, channel duration may last indefinitely"
  '아우렐리온 솔 Q / MaxChannelDuration': '3.25 (5랭크는 무제한)',
  // ★ 자동 추출이 "최대 마나의 100% x (…)" 라는 식을 그대로 내보내던 자리 (2026-08-24).
  //   트린다미어의 자원은 분노(최대 100)다 — 접으면 롤위키 Maximum Heal 과 정확히 일치:
  //   30+0.5x100 ~ 70+2.3x100 = 80~300, 주문력 30% + 1.2%x100 = 150%.
  //   ★ 문장이 길다는 지적으로 "(최대: …)" 를 통째로 각주로 접었다 — 템플릿의 {p4} 자리에
  //     이 각주 HTML 이 그대로 들어간다 (KEEP_TEXT Tryndamere Q 와 한 쌍, 홑따옴표만 쓸 것)
  '트린다미어 Q / MaximumHeal': "<span class='custom-footnote'>[1]<span class='custom-footnote-content'><div style='font-size:12px; color:#fff; white-space:nowrap;'>최대: 80 / 135 / 190 / 245 / 300 (+ 주문력의 150%)</div></span></span>",
  // ★ 클라 회색 글씨가 "보호막은 200초에 걸쳐 점차 사라집니다" 로 뜬다 — 라이엇이 초당 감소량(200)을
  //   지속시간 자리에 꽂은 오타다. 본문 "최대 2.5초까지 감소하는 보호막" 이 진짜 값 (kenchR.PNG, 2026-08-23)
  '탐 켄치 R / ShieldDecayPerSecond': '2.5',

  // 2026-08-08 인게임 확인분.
  //   벨베스 E / 세트 W / 신 짜오 E / 진 E 는 숫자만으로 안 끝난다.
  //   (공속 비례분·투지 계수 등이 문장 쪽에 같이 들어가야 함) — 확인결과.md 2번 참고
  '말파이트 W / f1': '10 / 15 / 20 / 25 / 30',
  '말파이트 W / f2': '30 / 45 / 60 / 75 / 90',
  '벨베스 Q / f1': '16 / 15 / 14 / 13 / 12',
  '신드라 W / f2': '1.5',

  // 2026-08-09 검산목록 A 대조분 (롤위키 + 인게임 스샷).
  //   미스 포츈: 활보 쿨 감소는 2초. 자동값 240 은 자릿수부터 말이 안 됐다
  //     (활보 기본 쿨이 12초). P 는 W 값을 교차 참조하는 자리라 같이 넣는다.
  //   타릭 P: 자동값이 궁극기 쿨(160/140/120)을 끌어와 껍데기를 만들고 있었다.
  //     실제는 스킬 가속에 따라 1~2초. "스킬 쿨타임으로 추론" 휴리스틱 오답 사례.
  //   우디르 Q: 문장 자리는 번개 6회 '총합'이다. 인게임 툴팁(우디르Q.PNG) 1레벨이
  //     "여섯 번 일으켜 최대 체력의 9.0%", 회색 줄이 "각 번개 = 1.5% ~ 3.0% + 0.006%".
  //     회당 값과 AP 계수에 각각 x6 을 실은 값. 근본 원인은 guessPart 의 x100 배율
  //     누락이라 그쪽이 고쳐지면 이 줄은 빼도 된다.
  // 2026-08-11 인게임 확인분 (위키 대조 6건 중 2건이 우리 오답이었다).
  //   누누 W: bin 의 NoImpactSnowballDamage(최대 x0.333)를 최소 피해로 쓰고 있었는데
  //     그건 "안 맞았을 때" 값이다. 인게임 툴팁도 60 으로 찍히지만 실제로 때려보면 36 —
  //     최소 = 최대의 20% 가 맞다(양 위키도 36/45/54/63/72). bin 에 0.2 가 없어서 손으로 넣는다.
  //   키아나 Q: 두 번째 대상은 TremorDamage(x0.6)가 아니라 FalloffMod(x0.75)다.
  //     bin 에 EnchantedFalloff/VanillaFalloff 라는 별도 계산식이 있는데 그쪽을 못 잡았다.
  '누누와 윌럼프 W / NoImpactSnowballDamage': '36 / 45 / 54 / 63 / 72 (+ 주문력의 30%)',
  '키아나 Q / TremorDamage': '52.5 / 75 / 97.5 / 120 / 142.5 (+ 추가 공격력의 63.75%)',

  '미스 포츈 P / Spell.MissFortuneViciousStrikes:LoveTapRefund': '2',
  '타릭 P / CDR': '1 ~ 2 (스킬 가속에 따라)',
  //   ★ 2026-08-11: 계수를 0.036% -> 3.6% 로 고쳤다. StatBy* 조각이 mult 를 무시하던
  //     버그(아래 partToText 주석 참고) 때문에 100배 작게 적혀 있었다.
  //     lolwiki 우디르 Q 각성 번개가 "+0.6% per 100 AP" (회당) 이고 6회 총합이라 3.6%.
  '우디르 Q / EmpoweredLightningBonusMax': '9 ~ 18% (레벨에 따라) (+ 주문력의 3.6%)',

  // 2026-08-09 폴백 해제 후 화면에 드러난 자리들.
  //   스몰더: BuffCounterByNamedDataValue 조각이 "0.25 (중첩당)" 이라는 분수를 그대로 낸다.
  //     위키 대조로 Q 25% / W 55% / E 8% 확인 (E 는 치명타 확률에 따라 12.8% 까지).
  //     ★ (중첩당) 자리를 일괄로 x100 하면 안 된다 — 드레이븐 P 골드 '1 (중첩당)',
  //       스웨인 P 체력 '15 (중첩당)' 처럼 절대값인 자리와 '2.5% (+ 0.5 (중첩당))' 처럼
  //       이미 % 단위인 자리가 섞여 있다. 곱셈 배율 규칙과 같은 함정이다.
  //     문장 쪽은 "용 훈련 중첩의 {pN}에 해당하는" 으로 같이 손봤다.
  '스몰더 P / Passive_QDamageIncrease': '25%',
  '스몰더 P / Passive_WDamageIncrease': '55%',
  '스몰더 P / EBonusDamage': '8%',
  '스몰더 Q / spell.SmolderP:Passive_QDamageIncrease': '25%',
  '스몰더 W / spell.SmolderP:Passive_WDamageIncrease': '55%',
  '스몰더 E / spell.SmolderP:EBonusDamage': '8%',

  //   카이사 Q: 계산식이 전개가 안 돼 "((...) x 5) x 0.25" 라는 날것이 화면에 찍혔다.
  //     첫 발 100% + 나머지 5발 x 25% = 기본값 x 2.25. 위키 총합표와 일치.
  '카이사 Q / MaxDamageDisplay': '90 / 123.75 / 157.5 / 191.25 / 225 (+ 추가 공격력의 123.75% + 주문력의 45%)',

  //   가렌 E: f1 은 본체 계산식 NumberOfStrikes 이고 그 속은 NamedDataValue "NumTicks"
  //     = [7, 7, 7, 7, 7, 7] 하나뿐이다. 이름이 안 맞아서 못 찾았을 뿐 값은 본체에 있었다.
  //     ★ 위키 "7 spins + 1 per 25% bonus attack speed" — 숫자만 넣으면 공속 비례분이
  //       사라지므로 문장에 그 문구를 같이 넣었다 (벨베스 E 와 같은 성격).
  //     랭크별 피해 4/7/10/13/16 (+40~52% AD) 도 위키와 일치해 교차 확인됨.
  '가렌 E / f1': '7',

  // 2026-08-09 인게임 확인분 (확인요청-0809.md).
  //   ★ 셋 다 "본체 한정 규칙" 때문에 못 채우던 자리다. 규칙을 계열까지 푸는 실험을
  //     해봤더니 신 짜오가 30/35/40/45/50 이라는 틀린 값을 물어왔다 (실제 38/46/…).
  //     계열 등급도 못 믿는다 — 완화는 철회했고 전부 손으로 넣는다.
  //
  //   세트 W: 투지 최대치가 "최대 체력의 50%" 이고 피해가 "투지의 25% (+ 추가 AD 0.25%)"
  //     라서 최대 피해 = 최대 체력의 12.5% (+ 추가 공격력의 0.125%) 로 환산된다.
  //     체력에 비례하는 값이라 랭크별 고정 숫자가 아니다. bin 후보 80/125/170/215/260 은 오답.
  '세트 W / f1': '80 / 100 / 120 / 140 / 160 + 최대 체력의 12.5% (+ 추가 공격력의 0.125%)',

  //   신 짜오 E: 문장에 "{p5}% ( )" 라고 빈 괄호가 노출돼 있어서 문장 쪽도 같이 고쳤다.
  '신 짜오 E / f1': '38 / 46 / 54 / 62 / 70% (+ 주문력의 10%) (+ 영구적 공격 속도 5%당 1%)',

  //   벨베스 E: 문장에 이미 "공격 속도에 비례해 증가" 문구가 있어 숫자만 넣고 문구를 구체화.
  '벨베스 E / f2.0': '6',

  //   벨베스 E 최대 피해: "x 2" 곱셈이 전개가 안 돼 문장에 껍데기가 노출됐다.
  //     (최소값 x 2 가 정확히 위키 최대값과 일치해 교차 확인됨)
  '벨베스 E / MaxDamagePerStrikeTooltip': '20 / 24 / 28 / 32 / 36 (+ 총 공격력의 24%)',

  //   진 E: mStat 10 항이 안 풀려 "+ (?)" 가 붙어 폴백이 걸려 있었다.
  //     그 항은 인게임 툴팁에 아예 표시되지 않고(그래서 역산도 불가), 실측 충전 시간이
  //     24 / 21.5 / 19 / 16.5 / 14 로 이 값과 일치한다. 표시 안 되는 항이면 빼는 게 맞다.
  '진 E / AmmoRechargeRateTooltip': '24 / 21.5 / 19 / 16.5 / 14',

  // 2026-08-09 2차. 마지막 폴백 4스킬. 전부 bin 에서 못 가져오는 자리다.
  //   바드 P: 슬롯은 있는데 value 가 비어 있다. 위키로 확인 — 레벨이 아니라 '고대의 종' 수에 연동.
  '바드 P / f5': '8 ~ 4 (고대의 종에 따라)',
  '바드 P / f4': '1 ~ 9 (고대의 종에 따라)',

  //   아우렐리온 솔 P: mEffectAmount 자체가 없다. 인게임 툴팁에도 계수가 안 나온다.
  //     ★ 영문 위키 "15% Stardust" 는 검증 실패(별가루 100개에서 반지름 +17.4%).
  //       나무위키 "0.379 별가루%" 가 맞고, 이건 '면적' 증가율이다:
  //       특이점 외곽 √(275² + 16.93²·S) 에서 16.93²/275² = 0.00379,
  //       내부 8.46²/137.5² = 0.003786 으로 세 자리가 전부 일치한다.
  //     문장의 "% 증가합니다" 에서 % 를 뺐다 — 별의 비행은 유닛이라 % 가 틀린 말이었다.
  '아우렐리온 솔 P / f2.1': '별가루 1개당 7.5',
  '아우렐리온 솔 P / f3.1': '별가루 1개당 면적 0.379%',
  '아우렐리온 솔 P / f4.1': '별가루 1개당 면적 0.379%',

  //   자야 P: 값은 본체에 다 있는데 f 번호가 배열 인덱스와 대응이 안 된다.
  //     f12 -> 인덱스 0(PFeatherDuration=6), f14 -> 인덱스 5(PStacksPerCast=3),
  //     f16 -> 인덱스 7~3(PDamageFalloffMin/Mid/Max=0.35/0.45/0.55).
  //     규칙성이 없어서 재사용 가능한 해법을 못 찾았다. 인게임 확인값으로 직접 넣는다.
  '자야 P / f14': '3',
  '자야 P / f16*100': '35 ~ 55 (레벨에 따라)',
  '자야 P / f12': '6',

  //   퀸 P: mEffectAmount 가 없다. 위키 "7 – 2.56 (based on critical strike chance)".
  //     레벨이 아니라 치명타 확률에 연동된다.
  '퀸 P / f1': '7 ~ 2.56 (치명타 확률에 따라)',

  //   아우렐리온 솔 P 나머지 두 자리 — 값이 아니라 서식 통일이다.
  //     한 문장 안에서 "별가루 1개당 7.5" 와 "0.031 (중첩당)%" 가 섞여 튀었다.
  //     처형 기준값 0.026 은 나무위키 "별가루 100개당 2.6%p" 로 검증됨 (0.026 x 100 = 2.6).
  //     빛의 숨결 0.031 은 bin 값 그대로고 표기만 바꾼 것이다.
  '아우렐리온 솔 P / QPassiveScaling': '별가루 1개당 최대 체력의 0.031%',
  '아우렐리온 솔 P / EPassiveScalingExecute': '별가루 1개당 0.026%p',

  //   스몰더 Q 225중첩 화상: "최대 체력의 추가 공격력의 0.025%…" 로 조사가 겹쳐 깨져 있었다.
  //     문장을 "대상 최대 체력의 {p10}%" 로 바꾸고 값에서 단위를 뺐다.
  '스몰더 Q / Tier3_Burn': '(+0.025 추가 공격력)(+0.005 용 훈련 중첩)',
};

const manualUsed = new Set();

// ------------------------------------------------------------
// 레벨 보간 범위 표기.
//   ★ 양 끝이 같으면 범위가 아니라 고정값이다. "6 ~ 6% (레벨에 따라)" 처럼
//     나가면 성장하지 않는 값을 성장하는 것처럼 읽히게 만든다.
//     (스웨인 P 회복량, 아이번 P 이동 속도, 렉사이 R 진동 피해 3자리 해당)
//   보간 조각 네 군데가 같은 문장을 각자 만들고 있어서 여기로 모았다.
// ------------------------------------------------------------
const levelRange = (a, b) => (String(a) === String(b) ? `${a}` : `${a} ~ ${b} (레벨에 따라)`);

// ------------------------------------------------------------
// mStat 번호 -> 한글 스탯 이름
//   ★ 인게임 툴팁으로 실제 확인한 것: mStat 2 = 공격력(아트록스 Q),
//     mStat 4 = 공격 속도(카타리나 R — 툴팁 "+0.5 추가 공격 속도" 와
//     0.16 x 3.125 = 0.5 가 일치), mStat 18 = 생명력 흡수(암베사 R "+0.5 생명력 흡수"),
//     mStat 29 = 물리 관통력(파이크 R "+1.5 물리 관통력"). 나머지는 아직 추정이다.
//     모르는 번호가 나오면 "?" 로 두고 마지막에 목록으로 알려준다.
//     ★ 3 은 원래 '공격 속도'로 적혀 있었으나 4가 공격 속도로 확정되어 뺐다.
//       추측으로 채우면 "?" 보다 나쁘다 — 쓰이는 자리가 있으면 목록에 뜰 것이다.
// ------------------------------------------------------------
const STAT_NAMES = {
    0: '주문력',
    1: '방어력',
    2: '총 공격력',
    4: '공격 속도',
    // ★ 5 = 스킬 가속. 2026-08-10 인게임 확인 (추정이었던 것을 확정으로 바꿈).
    //   타릭 P 가 유일한 사용처다. 인게임 툴팁엔 "재사용 대기시간이 1초 감소" 라고만
    //   적혀 있고 계수가 안 보이는데, **스킬 가속 100을 찍으면 2초로 바뀐다**
    //   (실제 규칙은 스킬 가속 10당 0.1초, 100에서 상한). 우리 값 `1 ~ 2` 가 맞다.
    5: '스킬 가속',
    18: '생명력 흡수',
    29: '물리 관통력',
    // ★ 6 과 8 은 원래 서로 반대로 적혀 있었다. 2026-08-08 인게임 확인으로 교환:
    //   닐라 R 이 "치명타 확률의 10%"(사이트는 마법 저항력이라고 했음),
    //   뽀삐 W 가 "마법 저항력의 12%"(사이트는 치명타 확률이라고 했음).
    //   정확히 맞교환이라 우연이 아니다. 21자리가 틀린 이름으로 나가고 있었다.
    6: '마법 저항력',
    8: '치명타 확률',
    // ★ 9 는 원래 '이동 속도'로 적혀 있었으나 2026-08-08에 '치명타 피해량'으로 정정.
    //   야스오 패시브 CurrentCritDamage 가 위키 기준 180%(치명타 피해량)인데
    //   "이동 속도의 100%"로 찍히고 있었다. 탈론 Q 인게임 설명("근접 공격 피해량은
    //   치명타 피해량 증가에 영향을 받습니다")도 같은 결론.
    //   케이틀린 P·루시안 R·미스 포츈 Q·샤코 Q 의 "치명타 확률 x (X - 1)" 형태도
    //   X 가 치명타 피해량일 때만 성립한다.
    9: '치명타 피해량',
    // ★ 7 = 이동 속도. 2026-08-08 추가. 표에 없어서 잔나 P·헤카림 P 가 통째로 죽고 있었다.
    //   헤카림 P BonusAD 가 결정적: mStat 7 / mStatFormula 2(추가) 에
    //   레벨 3·6·9·12·15·18 마다 +0.02 씩 붙는 0.12 -> 0.24 계수다.
    //   헤카림 패시브(전쟁의 길)가 정의상 "추가 이동 속도의 12~24% 만큼 추가 공격력"이라
    //   숫자까지 그대로 맞는다. 잔나 P(순풍)도 추가 이동 속도 비례로 같은 모양.
    7: '이동 속도',
    // ★ 11·13 은 아직 추정이지만 **확인할 필요가 없다.** 2026-08-10 전수 조사 결과:
    //   11 은 bin 전체에 `mStat 11` 이 **한 번도 안 나오고**, 13 은 bin 에 4곳
    //   (문도 Q·엘리스 Q·킨드레드 W·자헨 R) 있지만 **화면에 나가는 문장엔 0자리**다.
    //   사이트의 "추가 공격력" 290자리는 11번이 아니라 `mStat 2` + `mStatFormula 2`(추가)
    //   조합에서 나온 말이라 무관하다. 새 챔피언이 쓰기 시작하면 그때 확인할 것.
    11: '추가 공격력',
    12: '최대 체력',
    13: '추가 체력',
    // ★ 14 = 현재 체력. 2026-08-10 인게임 확인.
    //   자크 Q 가 "현재 체력의 8% (56) 소모" 로 뜨는데, 그때 자크의 현재 체력이 695였다.
    //   695 x 0.08 = 55.6 ≒ 56 이라 최대 체력이 아니라 **현재** 체력이 맞다
    //   (12 = 최대 체력과 구별된다). W·E 는 절반인 4% 다.
    14: '현재 체력',
};

const unknownStats = new Map();   // 번호 -> { count, where[] }
const unknownParts = new Map();   // 계산식 조각 종류 -> { count, keys, where[], solved }
const guessedList = [];           // 필드 모양으로 추측해서 푼 값 (눈으로 검산할 것)
const viaField = [];              // 최상위 / mAmmo 필드에서 건진 값 (새 경로라 검산 대상)
const dotList = [];               // ".숫자" 꼬리를 처리한 값 (해석이 확정 아님, 검산 대상)
const caseList = [];              // 대소문자를 무시하고 찾아낸 이름 (툴팁 철자 != bin 철자)
const caseSeen = new Set();
const zeroDrop = [];              // 참조 대상이 bin 에 없어 0으로 본 항 (조회 버그와 구분하려고 남긴다)
const hashHits = new Set();       // CD 가 이름을 못 푼 {해시} 자리를 해시로 찾아낸 곳
const crossHits = new Set();      // @spell.X:Y@ 교차 참조를 풀어낸 곳
const crossMiss = [];             // 교차 참조인데 못 푼 곳
const costFromText = [];          // CD 소모값 문구를 풀어서 채운 곳
const costTextFail = [];          // 소모값 문구를 못 푼 곳
const ieNotes = [];               // "무한의 대검 보유 시" 각주를 단 자리
// ============================================================
//  ★★ 우상단 값 손 표 (2026-08-14) — 롤위키·나무위키 대조로 확정한 자리
//
//  bin 에서 **자동으로 못 얻는** 값들이다. 근거를 반드시 같이 적을 것.
//    · 사거리      — `castRange` 가 25000(표시할 값 없음)이라 뺐는데 진짜 값이 있는 자리
//    · 투사체 속도 — 본체에 기본 공격 기본값(828.5 / 779.9)이 박혀 있어 진짜 값이 딴 데 있는 자리
//    · 돌진 속도   — 후보가 둘이라 자동으로 못 고른 자리
//
//  ★ 키는 `"<한글 챔피언명> <슬롯>"` 이다 (MANUAL 표와 같은 형식).
// ============================================================
const STAT_MANUAL = {
    // --- 사거리: bin `castRange` 가 25000 이라 뺀 자리 중 위키에 진짜 값이 있는 것 ---
    // 투사체 950: 온 계열이 100(스크립트 유도 미사일 자리값) — 롤위키 950 / 부착 가속 1650 (2026-08-24)
    '유미 Q': { '사거리': '850', '투사체 속도': '950 (부착 강화 시 1650)' },    // 사거리: 롤위키 850 · 나무위키 850 (bin 계열은 1150/1250 로 셋 다 달랐다)
    '유미 W': { '사거리': '700' },    // 롤위키 700 · 나무위키 700 · bin 계열 `YuumiWCast` 700 — 셋 일치
    '요네 E': { '사거리': '300 (귀환 돌진은 전역)', '시전시간': '0.225 (재사용)' },    // 사거리 나무위키 300 · bin `EDashRange` 300 · 위키 `Return dash and detonation damage radius` 가 Global (2026-08-24 효과 범위 → 사거리 괄호)
    '세라핀 R': { '사거리': '1200' }, // 나무위키 1200 · bin DataValue `RRange` 1200 — 일치
    '아트록스 E': { '사거리': '300' },  // 롤위키 `75 - 300` · bin DataValue `EMaxRange` 300
    '오른 W': { '사거리': '500' },      // 롤위키 `500 • 560` (bin 계열 override 2500 은 취약 효과 범위였다)
    '바드 Q': { '사거리': '850' },      // 롤위키 850 (bin 계열은 950·525 로 둘 다 달랐다)
    '헤카림 R': { '사거리': '1000' },   // 롤위키 `300 - 1000` · bin DataValue `MaxDashRange` 1000
    '제리 E': { '사거리': '300' },      // 롤위키 `cast range 300` · bin DataValue `MaxDistance` 300 — 일치
    '세트 W': { '사거리': '720' },      // 롤위키 `Range: -25 - 720` / `Circle Radius: 720`
    '자헨 Q': { '사거리': '200' },      // 나무위키 200 (기본 공격 175 + bin DataValue `QBonusRange` 25)
    '자헨 E': { '사거리': '350' },      // 나무위키 돌진 거리 350 · bin DataValue `DashDistance` 350 — 일치
    // --- 사거리: bin 값이 20000 컷 **아래**의 더미라 걸러지지 않던 자리 (2026-08-23 롤위키 전수 대조) ---
    // ★★ 투사체 속도 1000 — 2026-08-24 인게임 실측. **본체 missileSpeed 1450.4 가 틀값이었다.**
    //   스탑워치로 카드가 닿는 데 1.7초가 걸렸는데, 시전시간 0.25 를 더하면
    //   1000 → 1.70초 · 1300(나무위키) → 1.37초 · 1450.4 → 1.25초 라 롤위키가 정확히 맞는다.
    //   ★ 틀값 목록(1200/902/828.5/779.9/8700)에 없는 값도 틀릴 수 있다는 첫 사례다.
    //     TF 기본 공격은 1500 이라 "기본공격 값이 새어든 것" 도 아니었다
    '트위스티드 페이트 Q': { '사거리': '1450', '투사체 속도': '1000' },   // bin castRange 10000 (override 없음) · 롤위키 둘 다
    // ★★ 2026-08-24 정정 — 롤위키가 맞았다. 예전엔 "롤위키 500~850 은 폭을 더한 값" 이라 보고
    //   나무위키 300~600 을 골랐는데, **인게임에서 최대 충전 표시가 사이온 E(800) 와 거의 같았다.**
    //   롤위키 원문도 충전 시간별 표를 갖고 있다: {{pp|채널 시간|500;500;675;762.5;850|0~1초}}
    '사이온 Q': { '사거리': '500 ~ 850 (충전 시간에 따라)' }, // bin castRange 10000 (더미)

    // --- 투사체 속도: 본체 값이 기본 공격 기본값이라 진짜 값이 딴 데 있는 자리 ---
    //   ★ 롤위키 스킬 상자에 `SPEED:` 로 적혀 있다. 나무위키는 "투사체 속도" 로 적는다
    //   ★ 한 슬롯에 항목이 여럿이면 **반드시 한 줄로 합칠 것.** 키를 두 번 적으면
    //     JS 객체라 **뒤엣것만 남아 앞엣것이 조용히 사라진다** (진 R 사거리가 그랬다).
    //   진 R — 사거리는 롤위키 `RANGE: 3500`(= bin 계열 `JhinRShot`),
    //   ★ 투사체는 나무위키 3000 이었는데 2026-08-24 에 5000 으로 정정 —
    //     bin `JhinRShotMis` spec 5000 = 롤위키 5000 으로 근거 둘이 일치한다 (나무위키가 옛 값)
    '진 R': { '사거리': '3500', '투사체 속도': '5000' },
    '코르키 R': { '사거리': '1225 (세 번째 미사일 1500)', '투사체 속도': '2000' }, // 투사체 나무위키 2000 (본체 828.5) · 사거리 둘째 값은 롤위키 (문장: "세 번째 미사일은 매번 …")
    '카직스 W': { '투사체 속도': '1700' }, // 롤위키 `SPEED: 1700` (본체 828.5)
    //   미스 포츈 R — 사거리는 나무위키 1400(bin 계열 650 은 튕기는 탄 사거리),
    //   투사체 속도는 롤위키 `SPEED: 2000` (본체 779.9)
    '미스 포츈 R': { '사거리': '1400', '투사체 속도': '2000' },
    // 계열 미사일 객체가 여럿이라 이름 규칙으로 못 고르는 자리 (2026-08-23 롤위키 SPEED)
    '칼리스타 Q': { '투사체 속도': '2400' },  // 롤위키 2400 = bin `KalistaMysticShotMisTrue` (Missile 객체는 3000)
    '신드라 E': { '투사체 속도': '2500' },    // 롤위키 2500 (bin 계열은 1100~2000 이 섞여 있다)
    '탈론 R': { '투사체 속도': '2400' },      // 롤위키 2400 = bin `TalonRMisOne` (돌아오는 칼날 MisTwo 는 4000)
    '탈론 W': { '사거리': '900', '투사체 속도': '2571 (돌아올 때 3000)' }, // 사거리 롤위키·나무위키 900 (castRange 650) · 계열 보정이 잡은 3000 은 돌아오는 단검 (2026-08-24)
    '사미라 P': { '사거리': '650 ~ 950', '투사체 속도': '2800' }, // 2050 은 강화 공격 돌진 속도였다 — 2800 이 총알 (롤위키)
    '트리스타나 W': { '돌진 속도': '1100' },  // bin 1100 이 투사체 칸에 있었다 — 점프 속도라 돌진으로 (롤위키 툴팁)
    '브라이어 E': { '사거리': '400 ~ 600 (충전 시간에 따라)', '시전시간': '0.15 (발사)' }, // 롤위키 [400 to 600] 충전형 (bin·DD 는 400 하나) · 발사 0.15
    // --- 롤위키 값이 "숫자 하나" 가 아니라 build_wiki_stats.js 가 버린 자리 (2026-08-23, 손으로 옮김) ---
    //   효과 범위 `Global` 은 "전역" · 범위/충전 비례는 `A ~ B` · `None | 0.25` 는 뒤쪽(재사용/2타) 값
    // ★ 진짜로 "효과가 전역" 인 자리만 남긴다 (롤위키가 설명 없이 `Global` 만 적는다)
    '카서스 R': { '효과 범위': '전역' }, '소라카 R': { '효과 범위': '전역' }, '트위스티드 페이트 R': { '효과 범위': '전역' },
    // ★★ 스킬의 **일부만** 전역인 자리는 무엇이 전역인지 괄호로 밝힌다 (2026-08-24).
    //   위의 셋(카서스 R·소라카 R·트페 R)은 스킬 전체가 전역이라 설명이 필요 없지만,
    //   아래 셋은 문장을 읽어야 뜻이 갈린다 — 워윅 W 를 `4000 (패시브 감지는 전역)` 으로 적은 것과 같은 정신이다.
    '녹턴 R': { '효과 범위': '전역 (어둠)' },              // 문장 "전장을 어둠으로 뒤덮어" — 돌진은 사거리 2500/3250/4000 로 따로 있다
    '자야 E': { '효과 범위': '전역 (깃털 회수)' },          // 문장 "모든 깃털을 불러들여"
    '조이 W': { '효과 범위': '전역 (파편 떨어뜨리기)' },     // 문장 "적이 소환사 주문이나 아이템을 사용하면 주문 파편을 떨어뜨립니다"
                                                     //   (롤위키도 `Range for enemy champions to drop a Spell Shard`)

    // ══ "효과 범위 = 전역" 이 사실은 "재사용·회수 사거리가 전역" 이던 자리 (2026-08-24) ══
    //   ★★ 워윅 W 와 같은 함정이다 — 롤위키 `Global` 에 괄호 설명이 붙어 있는데 그걸 안 읽으면
    //     "스킬 효과가 맵 전체에 미친다" 로 읽힌다. 실제 뜻은 "재사용할 때 거리 제한이 없다" 다.
    //   ★ 효과 범위 칸에서 빼고 **사거리 괄호**로 옮긴다 — 라벨과 뜻을 맞춘다
    //     (사이온 E·칼리스타 R·케인 W 와 같은 처리). 효과 범위 제거는 아래 STAT_DROP 이 맡는다.
    '아칼리 E': { '사거리': '825 (재사용은 전역)' },                          // 위키 `Recast range`
    '제드 R': { '사거리': '625 (재사용은 전역)' },                            // 위키 `Recast and ability mimick range`
    '오로라 Q': { '사거리': '900 (표식 대상 재사용은 전역)' },                  // 위키 `Recast range for marked targets`
    '이렐리아 E': { '사거리': '850 (검이 모이는 거리는 전역)' },                // 위키 `Blade convergence range`
    '쓰레쉬 Q': { '사거리': '1075 (기절·끌어당김은 전역)' },                   // 위키 `For the stun and pulling` (+ 3000 Deathly Leap)
    //   ★ 쉔 Q 는 투사체 속도를 같이 들고 있다 — 한 줄로 합쳐야 안 사라진다
    '쉔 Q': { '사거리': '400 (영혼검 회수는 전역)', '투사체 속도': '2000 ~ 5000' }, // 위키 `Spirit Blade recall range`
    //   ★ 우르곳 R 은 효과 범위 자리를 '전역' 이 차지해서 **진짜 값 600(공포 반경)이 밀려나 있었다**
    '우르곳 R': { '사거리': '2500 (재사용은 전역)', '효과 범위': '600' },        // 위키 `Recast range` + `600 Fear radius`
    '갈리오 W': { '효과 범위': '175 ~ 350 (충전 시간에 따라)' },
    '애니비아 R': { '효과 범위': '200 ~ 400 (지속 시간에 따라)' },
    '케일 R': { '효과 범위': '675 / 675 / 775' },
    '에코 Q': { '효과 범위': '160 ~ 210' },
    '킨드레드 Q': { '효과 범위': '500 (+ 추가 공격 사거리)' },
    // ★ 누누 W 사거리는 안 적는다 (STAT_DROP). 굴린 거리 750~1750 은 사거리 개념이 아니다.
    //   대신 폭발 반경을 효과 범위로 — 롤위키 130/260/390 (눈덩이 크기별)
    '누누와 윌럼프 W': { '효과 범위': '130 ~ 390 (눈덩이 크기에 따라)' },
    // ★ 사이온 E — 롤위키 range 칸에 값이 **둘**이다: 800(포효 미사일) · 1350(밀려난 미니언의
    //   최대 이동 거리). 우리가 첫 값만 가져오고 있었다. 문장에도 "밀려난 유닛에게 부딪힌
    //   적들에게 동일하게 적용" 이라 적혀 있으니 실효 범위가 훨씬 길다 (2026-08-24 사용자 지적)
    '사이온 E': { '사거리': '800 (밀려난 유닛은 최대 1350까지)' },
    // ══ 롤위키 range/target range 칸에 값이 둘 이상인 자리 (2026-08-24, 사이온 E 지적에서 부류로 확장) ══
    //   52자리 중 **문장이 "다른 시전 모드의 사거리" 라고 뒷받침하는 8자리만** 넣었다.
    //   ★ 안 넣은 것: 비에고 E 8000(벽 따라 퍼지는 안개 — 사거리가 아니다) ·
    //     그레이브즈 R 1690(폭발 원뿔 포함 — 효과 범위 성격) · 리산드라 Q·라칸 E
    //     (우리 기준값이 위키 첫 값과 달라 괄호를 붙이면 되레 헷갈린다) · 신드라 E(뜻이 불분명)
    '케인 W': { '사거리': '700 (그림자 암살자 900)' },      // 문장: "그림자 암살자: … 이동하여 사거리를 늘릴 수 있습니다"
    '케인 R': { '사거리': '550 (그림자 암살자 750)' },      // 문장: "그림자 암살자: 이 스킬의 사거리 … 가 증가하며"
    '신 짜오 E': { '사거리': '650 (도전 대상 1100)' },      // 문장: "도전 받은 적을 대상으로는 이 스킬의 사거리가 증가합니다"
    '나르 Q': { '사거리': '1100 (돌아올 때 3000)' },        // 문장: "부메랑은 적 하나를 맞힌 다음 돌아오며" (탈론 W 와 같은 처리)
    '클레드 E': { '사거리': '550 (재사용 765)' },           // 문장: "재사용하면 같은 대상에게 다시 돌진합니다"
    '크산테 Q': { '사거리': '450 (강화 930)' },             // 문장: "2회 중첩되면 … 충격파를 발사합니다"
    // ★★ 릴리아 R 은 전역이다 (2026-08-24 인게임 확인). bin 본체 castRange 1600 은 시전 이펙트 반경이고,
    //   진짜 적용은 `LilliaRExpungeMissile` 의 **19500**(협곡 대각선보다 크다) 이다.
    //   문장도 "꿈가루가 묻은 적 챔피언을 **전부**" 라고 적는다. 롤위키 표기는 `Global (or 19500)`.
    '릴리아 R': { '사거리': '전역' },
    // ══ 최소~최대 사거리 (2026-08-24). 롤위키 range 칸이 Minimum/Maximum 두 값인 자리다.
    //   우리는 최대값만 적고 있어서 틀린 건 아니었지만, 조준 지점에 따라 달라지는 스킬이라 범위로 적는다.
    //   ★ 괄호 설명은 안 붙인다 — 암베사 P `175 ~ 350`·사미라 P `650 ~ 950` 과 같은 꼴이다
    // ★★ 오로라 W — override 700 은 돌진 거리가 아니다 (2026-08-24 인게임 확인).
    //   "E(825)의 절반쯤" 이라는 관찰에 맞춰 bin DataValue 를 열어 보니 답이 있었다:
    //     JumpDistance 300(일반) · WallCheatDistance 450(지형 넘을 때) · DashBonusSpeed 350(이미 돌진 속도로 쓰는 값)
    //   **롤위키 300/450 과 정확히 일치한다.** 문장도 "지정한 방향으로 뛰어오릅니다" 라 대상 지정이 없다.
    //   ★ 돌진 거리를 사거리 칸에 적는 건 이 저장소 관례다 (자헨 E 350 · 헤카림 R 1000 · 볼리베어 R 700).
    //   ★ override 700 · castRadius 1000 의 정체는 확인 못 했다 — 돌진 거리는 아니라는 것만 확실하다
    // ══ 2026-08-24 인게임 확인으로 풀린 넷 ══
    //   ★ 에코 R — 위키 0.5. 우리 0.25 는 CD v1 값이었다 (인게임에 시전시간이 실제로 있다)
    '에코 R': { '시전시간': '0.5' },
    '오로라 W': { '사거리': '300 (지형 넘으면 450)' },
    '애니비아 W': { '사거리': '100 ~ 1000' },   // 롤위키 100(최소) ~ 1000(벽 중심 최대)
    '올라프 Q': { '사거리': '425 ~ 1000' },     // 롤위키 425(최소, 추정) ~ 1000(최대)
    '라이즈 R': { '사거리': '1000 ~ 3000' },    // 롤위키 1000(최소) ~ 3000(최대)
    '사일러스 Q': { '사거리': '50 ~ 775' },     // 롤위키 50(최소) ~ 775(최대)
    '우르곳 E': { '사거리': '450 ~ 475' },      // 롤위키 450(최소) ~ 475(최대)
    // ★★ 워윅 W — 사거리 칸을 빼고 효과 범위 하나로 (2026-08-24 인게임 확인 + bin 대조).
    //   `WarwickW` 는 castRange 3000 · override 4000 · castRadius 2200 으로 셋이 다른데,
    //   **액티브는 대상을 지정하지 않고 "가장 가까이에 있는 적 챔피언" 을 자기 주변에서 찾는다.**
    //   그래서 4000 의 정체는 사거리가 아니라 검색 반경이다 (롤위키도 `effect radius` 칸에
    //   `Search radius for target` 이라고 적는다). 사거리·효과 범위에 4000 이 두 번 뜨던 것을 정리했다.
    //   ★ 패시브 감지가 전역인 건 맞다 — 롤위키 `range = Global` 의 괄호가 `Passive detection of
    //     wounded enemies` 다. **`Global` 만 보고 액티브 사거리로 읽지 말 것.**
    '워윅 W': { '효과 범위': '4000 (패시브 감지는 전역)' },
    '암베사 P': { '사거리': '175 ~ 350' },
    '올라프 E': { '시전시간': '0.25 ~ 0.175' },
    '스카너 W': { '시전시간': '0.3' },
    '이렐리아 W': { '시전시간': '0.25 (재사용)' },
    '판테온 E': { '시전시간': '0.25 (재사용)' }, '럼블 R': { '시전시간': '0.58 (발사)' },
    '룰루 P': { '투사체 속도': '900 ~ 2600 (가속)' },   // 픽스 투사체가 가속한다 — 롤위키 "가속도 5200, 0.33초에 최대"
    '잔나 Q': { '투사체 속도': '880 ~ 1408' },
    '시비르 W': { '투사체 속도': '1000 (튕김)' }, '아크샨 P': { '투사체 속도': '5000' },
    '그레이브즈 P': { '투사체 속도': '3000' },
    '모데카이저 Q': { '스킬 폭': '90 ~ 150' },
    // 그웬 R 바늘은 날아가면서 폭이 좁아진다 (롤위키 "0.5초당 -140"). 숫자만 두면 오타처럼 읽힌다
    '그웬 R': { '스킬 폭': '240 ~ 30 (거리에 따라)' },
    // 공격 속도로 줄어드는 시전시간 — 롤위키 공식 (2026-08-23). bin castTime 은 기본값 하나뿐이다
    // ★★ 사거리는 **한 줄에 같이 적어야 한다** — 키를 두 번 적으면 뒤엣것만 남아 시전시간이 조용히 사라진다.
    //   Q3(회오리)은 평타 Q 와 사거리가 완전히 다른데 우리는 첫 값만 쓰고 있었다 (롤위키 range 칸 둘째 값).
    // ★★ 공속에 비례해 줄어드는 시전시간 — **롤위키가 공식을 주는 자리만** 넣는다 (2026-08-24).
    //   ★ bin 으로 계산하면 안 된다: `mAttackCastTime / mAttackTotalTime / as_base` 로 재면
    //     자야 Q 가 0.269 가 나오는데 **위키 공식은 0.25 에서 시작한다.** 스킬 고유 값이지
    //     기본 공격 준비 동작 그 자체가 아니라는 뜻이라, 계산법을 못 믿는다.
    //   ★ 그래서 케일 Q·미스 포츈 Q·비에고 Q·스몰더 Q 는 **안 넣었다** — 위키가
    //     "Attack Windup Time"·"Basic Attack Timer" 라고만 하고 숫자를 안 준다.
    '시비르 Q': { '시전시간': '0.25 ~ 0.1 (추가 공격 속도 100%당 50% 감소, 최대 60%)' },   // 롤위키 formula: 0.25*(1-(0.5 per 100% bonus AS))
    // ★ bin mCastTime 1.5 는 정체 불명 값이다 — 문장 p8(충전시간 clamp 1.2~0.6)·롤위키 cast time
    //   (1.2 에서 공속 비례 감소, pp 표)과 안 맞는다. 문장과 같은 clamp 양끝으로 적는다 (2026-08-24)
    '카이사 E': { '시전시간': '1.2 ~ 0.6 (공격 속도에 따라)' },
    // 투사체 1200(회오리): 본체 wrapper legacy 1500 은 근접 찌르기(Q1·Q2, 투사체 없음)까지 뭉뚱그린 틀값이고,
    //   진짜 투사체는 Q3 회오리뿐 — bin `YasuoQ3Mis` spec 1200 = 롤위키 `Whirlwind missile speed` 1200 (2026-08-24)
    '야스오 Q': { '사거리': '475 (회오리 1150)', '시전시간': '0.35 ~ 0.175 (추가 공격 속도 24%당 0.035초 감소, 최대 50%)', '투사체 속도': '1200 (회오리)' },
    // 요네 Q 는 롤위키 range 칸에 값이 셋이다 — 450(찌르기) · 450(3타 돌진 거리) · 1050(회오리 전체)
    '요네 Q': { '사거리': '450 (회오리 1050)', '시전시간': '0.35 ~ 0.175 (추가 공격 속도 24%당 0.035초 감소, 최대 50%)', '스킬 폭': '80 (회오리 160)' },   // 폭: bin mLineWidth 15 는 자리값. 롤위키 찌르기 80·회오리 160, 인게임 확인 (2026-08-24)
    '요네 W': { '시전시간': '0.5 ~ 0.19 (추가 공격 속도 1.68%당 1% 감소, 최대 62.5%)' },
    '애쉬 W': { '투사체 속도': '2000' },      // 롤위키·나무위키 2000 (계열 `VolleyAttack` 1500 은 다른 값)
    '하이머딩거 W': { '투사체 속도': '1200' }, // 나무위키 1200 (계열 750 은 다른 값)
    // --- ★★ 본체 legacy `missileSpeed` 가 틀값 목록(1200 등)에 없는데도 틀린 자리 (2026-08-24 전수) ---
    //   TF Q 와 같은 모양: `<본체>Missile` 객체의 실제 값과 롤위키 SPEED 가 **둘 다 일치**해서
    //   근거가 두 개다. 본체 legacy 는 패치 갱신이 안 된 옛 값으로 보인다 (spec-우선 규칙 주석 참고).
    '쓰레쉬 E': { '투사체 속도': '2000' },     // 본체 1100 / ThreshEMissile1 2000 = 롤위키 2000
    '우르곳 W': { '투사체 속도': '2500' },     // 본체 1600 / UrgotWMissile 2500 = 롤위키 2500
    '제드 W': { '투사체 속도': '2500' },       // 본체 1600 / ZedWMissile 2500 = 롤위키 2500
    '케이틀린 R': { '투사체 속도': '3200' },   // 본체 1500 / CaitlynRMissile 3200 = 롤위키 3200
    '트위치 W': { '투사체 속도': '1400' },     // 본체 1750 / TwitchVenomCaskMissile 1400 = 롤위키 1400
    '브랜드 E': { '투사체 속도': '900' },      // 본체 1800 / BrandEMissile 900 = 롤위키 900
    '브라움 Q': { '투사체 속도': '1700' },     // 본체 1100 / BraumQMissile 1700 = 롤위키 1700
    '바루스 Q': { '투사체 속도': '1900' },     // 본체 1500 / VarusQMissile 1900 = 롤위키 1900 (VarusQLaunch 1200 은 다른 값)
    '그레이브즈 R': { '투사체 속도': '2100' }, // 본체 1400 / GravesChargeShotShot 2100 = 롤위키 2100
    '다이애나 Q': { '투사체 속도': '1900 (바깥쪽 원호 2100)' }, // 본체 1300 / Inner 1900·Outer 2100 = 롤위키 1900·2100
    '오리아나 Q': { '투사체 속도': '1400' },   // 본체(Command) 1200 틀값 / 공 객체 OrianaIzuna 1400 = 롤위키 1400
    '오리아나 E': { '투사체 속도': '1850' },   // 본체(Command) 1200 틀값 / 공 객체 OrianaRedact 1850 = 롤위키 1850
    '브라움 R': { '투사체 속도': '1400' },     // 본체 wrapper 1200 틀값 / BraumRMissile 1400 = 롤위키 1400
    '애니비아 Q': { '투사체 속도': '950' },
    // --- 스킬 폭: 인게임 확인으로 롤위키가 맞은 자리 (2026-08-24 밤, 사용자 확인) ---
    //   bin mLineWidth(x2)가 판정 폭이 아닌 자리들 — 바루스 R 450x2=900 은 덩굴 퍼짐, 럭스 R 190x2=380,
    //   사미라 E 50x2=100 은 돌진 이펙트 폭. 위키 폭이 인게임과 일치했다
    '바루스 R': { '스킬 폭': '240' },
    '럭스 R': { '스킬 폭': '200' },
    '사미라 E': { '스킬 폭': '300' },
    //   같은 밤 2차 확인 (사용자: "다 위키가 맞는듯") — 세주아니 W·제라스 Q·유미 R 은 bin x2 가 크고,
    //   암베사 R·제리 Q 는 mLineWidth 15(자리값)라 30 으로 나가던 자리
    '세주아니 W': { '스킬 폭': '150' },
    '제라스 Q': { '스킬 폭': '145' },
    '암베사 R': { '스킬 폭': '130' },
    '제리 Q': { '스킬 폭': '80' },
    '유미 R': { '스킬 폭': '450' },    // 본체 wrapper(FlashFrost) legacy 850 / 진짜 미사일 FlashFrostSpell spec 950 = 롤위키 950
    // 본체 legacy 가 틀값(1200)인데 계열 객체도 없는 자리 — 롤위키 값을 쓴다 (속도 40/47 검증된 소스)
    '벨코즈 W': { '투사체 속도': '1700' },     // 본체 1200 틀값 · 롤위키 1700 (Ripple missile speed)
    '벨코즈 Q': { '투사체 속도': '1300' },     // 본체 1200 틀값 · 롤위키 1300 (Primary missile speed)
    '아리 E': { '투사체 속도': '1550' },       // 본체 1200 틀값 · 롤위키 1550
    // --- 사거리: `castRangeDisplayOverride` 가 있는데도 틀린 자리 (나무위키·롤위키 둘 다 다르다) ---
    // ★ 시전시간은 공속 비례 — 롤위키 formula: 0.25-(0.07 per 100% bonus AS), 최대 60% 감소 (2026-08-24)
    '자야 Q': { '사거리': '1100', '시전시간': '0.25 ~ 0.1 (추가 공격 속도 100%당 0.07초 감소, 최대 60%)' }, // 사거리: override 400 / castRange 1100 · 롤위키·나무위키 1100
    // 돌진 1350: 자기 몸을 날리는 돌진기다 — 본체 legacy 1500 은 투사체 칸에 잘못 들어가 있었다. 롤위키 `1350 Dash speed` (2026-08-24)
    '자크 E': { '사거리': '1200 ~ 1800 (충전 시간에 따라)', '돌진 속도': '1350' }, // override 300 · 롤위키 1200~1800 · 나무위키 "1레벨 1200 최대 레벨 1800"
    // override 가 위키 둘(롤위키·나무위키 일치)과 다른 자리 — 둘이 같으면 위키를 따른다 (2026-08-23)
    '오른 E': { '사거리': '800' },       // override 450 · 롤위키 800 · 나무위키 800
    // 말파이트 R 돌진: 롤위키 `1500 + 100% movement speed`. 본체 legacy 700 은 기절 이펙트 값이라 투사체 칸에서 뺐다 (2026-08-24)
    '말파이트 R': { '돌진 속도': '1500 (+ 이동 속도의 100%)' },
    // 탈론 Q 는 대상에게 뛰어드는 돌진기다 — 본체 spec 2000 을 돌진 칸으로 옮긴다 (롤위키는 dash 1400 "estimated" 라 bin 을 따름, 2026-08-24)
    '탈론 Q': { '돌진 속도': '2000' },
    '밀리오 W': { '사거리': '650 (재사용 3000)' },  // override 350 · castRange 650 · 롤위키·나무위키 650 · 재사용으로 온기가 따라갈 아군을 바꾼다(위키 Targeted unit recast range)
    '볼리베어 R': { '사거리': '700' },   // override 550 · castRange 700 · 롤위키 700 · 나무위키 "도약 거리 700"
    // ★ 칼리스타 R 은 두 단계라 사거리가 둘이다 (2026-08-24 인게임 확인 + bin 대조).
    //   R1(창에 꽂힌 아군을 끌어당김) = 본체 `KalistaRx` castRange·override 1000 — 인게임에서 E(1000) 와 같았다
    //   R2(그 아군을 던짐)          = `KalistaRAllyStun` castRange 1200 — 위키 둘이 적은 값이 이쪽이었다
    //   **위키가 틀린 게 아니라 다른 걸 재고 있었다.** 시전 사거리는 1000 이 맞다
    '칼리스타 R': { '사거리': '1000 (던지는 거리 1200)' },
    '초가스 W': { '사거리': '650' },     // castRange 300 · 롤위키 650 · 나무위키 650 (전방 60도 원뿔)

    // --- 돌진 속도: 후보가 둘이라 자동으로 건너뛴 자리 ---
    //   ★ 크산테 W — bin `DashSpeedBase` 는 1400 인데 **롤위키·나무위키·bin `missileSpeed`
    //     셋이 1500** 이다. 1400 만 예외라 1500 을 쓴다 (각성 상태는 `DashSpeedAO` 1800).
    //     bin 의 `KnockbackSpeed`(800)·`VictimDashMod`(1.3)는 **끌려가는 적**의 속도라
    //     우리가 쓰는 "돌진 속도"(크산테 본인)와 다른 값이다 — 롤위키의 "knockback speed" 가 이것이다
    '크산테 W': { '돌진 속도': '1500' },
    '람머스 R': { '돌진 속도': '900 ~ 2000', '사거리': '800' },   // 롤위키 min 900 / max 2000 · 사거리 800
    // ★ "고정분 + 이동 속도 %" 꼴이 있다. 본문 계수 표기(`(+ 주문력의 60%)`)와 같은 모양으로 적는다.
    //   bin 의 `DashSpeed` 는 **고정분만** 들고 있어서 그대로 쓰면 실제보다 느려 보인다.
    '라칸 E': { '돌진 속도': '1250 (+ 이동 속도의 80%)' },   // 롤위키 `1250 + 80% movement speed`
                                                            // (bin 은 1150 / 자야에게 1250. 나무위키의 1700 은 **W** 값이다)
    '키아나 E': { '돌진 속도': '600 (+ 이동 속도의 100%)' }, // 롤위키 `600 + 100% movement speed`
                                                            // (bin `DashSpeedMax` 1200 은 상한이다)
    '키아나 W': { '돌진 속도': '440 (+ 이동 속도의 100%)' }, // 롤위키 `440 + 100% movement speed`
    '브라이어 R': { '사거리': '12000 (2차 시전 돌진은 전역)', '돌진 속도': '2500 ~ 5000' }, // 돌진 나무위키 · 사거리 12000 은 bin·롤위키 일치(머리를 던지는 거리) · 위키 `Second cast dash range` 가 Global
    //   브라이어 Q — 롤위키 `600 - 900 (based on distance)`.
    //   bin 의 `DashSpeedBase` 600 + `ExtraDashSpeedBasedOnDistance` 300 = 900 과 정확히 맞는다
    '브라이어 Q': { '돌진 속도': '600 ~ 900 (거리에 따라)' },
    '벨베스 Q': { '돌진 속도': '850' },          // 나무위키 "돌진 속도: 850" (균열 상태 1500 은 안 적는다)
};

// ★ 화면에서 빼야 하는 자리. **투사체가 없는 스킬인데 값이 박혀 있는 것**이다.
//   `828.5` / `779.9` 는 라이엇 기본 공격 계열 기본값이라 본체에 남아 있다
//   (347.8 과 같은 성격인데 값이 챔피언마다 달라 값만으로는 못 걸러낸다).
// 액티브 keyCooldown 문장의 `@f1@` 이 bin 에 없어서 못 푸는 자리 — 나무위키로 확정한 값 (2026-08-23)
const CD_MANUAL = {
    '탈론 E': '2',    // 나무위키 "재사용 대기시간이 2초로 매우 짧은 대신" (벽당 160~60초 재사용 불가는 문장에 따로 있다)
    '베이가 W': '8',  // 나무위키 표 8 (극악 50중첩마다 10% 감소는 문장에 있다)
    // 인게임 확인 (2026-08-23, zeriQ.PNG / kenchR.PNG)
    '제리 Q': '1.52 (1레벨 기준, 공격 속도에 따라 감소)', // 클라 1.52초 = 1 ÷ 기본 공격 속도 0.658. 문장에 "공격 속도에 따라 감소" 가 있다
    '탐 켄치 R': '120 / 100 / 80',   // 클라 문장 `@CDCalc@{{ Item_Cooldown }}` 이 깨져 "113 (0초)" 로 뜬다. 툴팁 하단 표의 재사용 대기시간
    // 갱플 E 는 클라 문장에 재사용 대기시간이 빠져 있다 — 롤위키 static 0.5. 닐라 E `(재사용 0.5초)` 와 꼴을 맞춘다 (2026-08-24)
    '갱플랭크 E': '재충전 대기시간 17 / 16 / 15 / 14 / 13초 (재사용 0.5초) (최대: 3 / 3 / 4 / 4 / 5개)',
    // ★ 공격 속도로 줄어드는 쿨타임 — 클라 툴팁은 "단축할 수 있다" 고만 적고 수치를 안 준다. 롤위키 공식 (2026-08-23)
    // ★ 2차 폼이라 keyCooldown 문장 처리를 못 탄다 (2026-08-24). 클라 문장
    //   `재장전 대기시간 @TOOLTIPAmmoRecharge@초 (%i:cooldown% @Cooldown@초)` 를 손으로 푼 것 —
    //   bin RechargeTime [20,18,16,14,12,10,8] 1번부터 = 롤위키 recharge 18~10 과 일치, Cooldown 3.
    //   코르키 R `재장전 대기시간 20초 (재사용 2초)` 와 같은 꼴이다
    '클레드 Q2': '재장전 대기시간 18 / 16 / 14 / 12 / 10초 (재사용 3초)',
    // ★ 클라 문장(`재충전 대기시간 6초 (최대: 2개)`)에 재사용 대기시간이 빠져 있다 — 롤위키 cooldown 0.5.
    //   갱플랭크 E 와 같은 보충이다 (2026-08-24)
    '럼블 E': '재충전 대기시간 6초 (재사용 0.5초) (최대: 2개)',
    // ★ 클라 문장은 "재사용 대기시간 없음"(spell_cooldown_nocooldown) — 문장은 지키고 씨앗 재충전을
    //   괄호(→각주)로 보충한다. bin mAmmoRechargeTime [18,18,16,14,12,10,8] 1번부터 = 롤위키 18~10 (2026-08-24)
    '자이라 W': '없음 (씨앗 재충전 18 / 16 / 14 / 12 / 10초)',
    // ★ 토글이라 bin cooldownTime 이 전부 0 인데 keyCooldown 문장이 없어서 "0" 이 그대로 새어 나가고
    //   있었다. 징크스 Q(0.9)는 데이터에 재사용 쿨이 있어서 적는 것 — 신지드는 데이터에 없으니 '없음'.
    //   롤위키의 1초는 커뮤니티 실측이라 클라에 없는 숫자다 (2026-08-24)
    '신지드 Q': '없음',
    '야스오 Q': '4 ~ 1.33 (추가 공격 속도 1.67%당 1% 감소, 최대 67%)',
    '요네 Q': '4 ~ 1.33 (추가 공격 속도 1.67%당 1% 감소, 최대 67%)',
    '요네 W': '14 ~ 6 (추가 공격 속도 1.51%당 1% 감소, 최대 62.5%)',
};

// 소모값이 bin 스펠 객체에 없는 식으로 변하는 자리 — 인게임 확인값 (2026-08-23)
const COST_MANUAL = {
    // bin `mana` 는 40 고정인데 18레벨 툴팁이 23 이다 (롤위키 `40 → 23 레벨 비례`). 패시브 가교 쪽 계산으로 보이나 bin 에 없다
    '우디르 W': '40 ~ 23 (레벨에 따라)',
    '우디르 E': '40 ~ 23 (레벨에 따라)',
    '우디르 R': '40 ~ 23 (레벨에 따라)',
};

// ★ 롤위키 우상단 값 (build_wiki_stats.js → wiki_stats.json, 2026-08-23).
//   **bin 에 값이 없는 자리에만** 깔린다 — 우선순위 STAT_MANUAL > bin > 위키. STAT_DROP 은 여기도 막는다.
//   효과 범위(effect radius)는 bin 에 대응 필드가 없어 전부 위키다. 사거리 "전역" 도 위키 `Global`.
const WIKI_STATS = (() => {
    try { return JSON.parse(fs.readFileSync(path.join(__dirname, 'wiki_stats.json'), 'utf8')); }
    catch (e) { console.log('★ wiki_stats.json 이 없다 — node build_wiki_stats.js 를 먼저 돌릴 것'); return {}; }
})();
const wikiStatUsed = [];           // 위키 값으로 채운 자리
// ★ 순서는 "공간(사거리·효과 범위·스킬 폭) → 시간(시전시간·투사체·돌진)" 이다 (2026-08-23 결정)
const STAT_ORDER = ['사거리', '효과 범위', '스킬 폭', '시전시간', '투사체 속도', '돌진 속도'];

const STAT_DROP = {
    // 위키로 "투사체가 아니다" 를 확인한 자리
    '럼블 W': ['투사체 속도'],         // 나무위키: 자기 보호막. 투사체 아님
    '마스터 이 W': ['투사체 속도'],    // 나무위키: 자기 회복
    '레오나 W': ['투사체 속도'],       // 나무위키: 자기 방어력 + 광역 폭발
    '그라가스 W': ['투사체 속도'],     // 나무위키: 자기 강화
    '아우렐리온 솔 W': ['투사체 속도'], // 나무위키: 비행 스킬 ("비행 속도" 는 따로 있다)
    // 계열 객체조차 없고 자기 강화가 명백한 자리 (779.9 궁극기 묶음)
    '나서스 R': ['투사체 속도', '사거리'], '레넥톤 R': ['투사체 속도'], '아트록스 R': ['투사체 속도'],
    '세트 R': ['투사체 속도'], '케넨 R': ['투사체 속도'], '제리 R': ['투사체 속도', '사거리'],   // 제리 R 사거리 800 은 자기 중심 광역 (2026-08-24)
    // 828.5 묶음 중 자기 대상·주변 광역
    '문도 박사 R': ['투사체 속도'], '알리스타 R': ['투사체 속도'], '잔나 R': ['투사체 속도', '사거리'],   // 잔나 R 사거리 725 는 자기 중심 광역 (2026-08-24)
    '헤카림 W': ['투사체 속도'], '누누와 윌럼프 R': ['투사체 속도'],
    //   ★ 이블린 R — 롤위키가 `Effect Radius 500 / Angle 180°` 인 **원뿔형 광역**이라고
    //     못 박는다. 투사체가 아니다. bin 본체에 `missileSpeed 1300` · `mLineWidth 80` 이
    //     둘 다 박혀 있는데 **80 은 원뿔 반경 500 과도 안 맞는 값**이라 같이 뺀다
    '이블린 R': ['투사체 속도', '스킬 폭'],
    // 2026-08-23 롤위키 전수 대조에서 걸린 사거리 더미
    '누누와 윌럼프 W': ['사거리'],   // override 7500 — 굴러가는 최대 거리지 사거리가 아니다 (롤위키 750~1750 은 충전 비례)
    // ★★ 돌진기인데 본체 legacy missileSpeed 가 투사체 칸에 실려 있던 자리 (2026-08-24 전수).
    //   투사체가 없는 스킬이라 칸 자체가 성립하지 않는다 — 돌진 속도는 따로 있거나(코르키 W 650 ·
    //   우르곳 E 1200 · 오로라 W 350 · 키아나 W 440) STAT_MANUAL 로 옮겼다(말파이트 R · 자크 E · 탈론 Q).
    //   비 R 은 돌진 속도가 가속식(롤위키 800 initial, ramps up)이라 숫자 하나로 못 실어 칸 없이 둔다
    '말파이트 R': ['투사체 속도'],   // legacy 700 = 기절 이펙트 계열 값
    '바이 R': ['투사체 속도'],       // legacy 1400 (ViRMissile 2000 은 이펙트)
    '코르키 W': ['투사체 속도'],     // legacy 700 — 돌진 속도 650 이 따로 있다
    '자크 E': ['투사체 속도'],       // legacy 1500 → 돌진 속도 1350 (STAT_MANUAL)
    '우르곳 E': ['투사체 속도'],     // legacy 1500(UrgotEDash) — 돌진 속도 1200 이 따로 있다
    '오로라 W': ['투사체 속도'],     // legacy 1800 — 자기 도약, 돌진 속도 350 이 따로 있다
    '탈론 Q': ['투사체 속도'],       // spec 2000 → 돌진 속도로 옮김 (STAT_MANUAL)
    '자헨 R': ['투사체 속도'],       // legacy 8700 틀값 — 돌진 속도 2800 이 따로 있다
    '브라이어 Q': ['투사체 속도'],   // legacy 1500 — 물어뜯기 돌진, 돌진 속도 600~900 이 따로 있다
    '마오카이 W': ['투사체 속도'],   // legacy 1500 — 자기 몸을 날리는 돌진, 돌진 속도 1300(위키)이 따로 있다
    // ★★ 사거리검토_20260824.md 65자리 중 "기존 정책과 같은 부류" 45자리 반영 (2026-08-24 밤).
    //   ① 자기 버프·패시브·감지 반경·전역/최대이동 더미 — 8/24 에 뺀 46자리와 같은 부류.
    //   ② 자기(또는 구체) 중심 광역 — selfRadiusDup 정책과 같은 부류인데 사거리 값이 위키
    //     효과 범위와 몇십 단위로 달라 자동 규칙에 안 걸렸던 자리. 효과 범위 칸은 그대로 남는다.
    //   원뿔·방향형(카밀 W·카시 R·코르키 E·미포 R·판테온 E·스웨인 Q·요네 W·그웬 Q·카사딘 E 등)은
    //   "닿는 거리" 로서 사거리가 유의미해 **일부러 남겼다** — 사거리검토 파일의 권고 표 참고.
    // ① 자기 버프·패시브·감지·더미
    '아크샨 W': ['사거리'],      // 5500 = 악당 감지 반경 (렝가 R 과 같은 부류)
    '드레이븐 W': ['사거리'],    // 자기 버프
    '나르 W': ['사거리'],        // 미니 폼은 패시브 (기본 지속 효과 문장)
    '코그모 W': ['사거리'],      // 평타 강화 — 문장이 "사거리가 ◆ 증가" (트위치 R 부류)
    '미스 포츈 W': ['사거리'],   // 자기 버프
    '오공 Q': ['사거리'],        // 평타 강화 — 문장이 "사거리가 ◆ 증가" (트위치 R 부류)
    '올라프 W': ['사거리'],      // 자기 버프
    '파이크 W': ['사거리'],      // 자기 은신·이속
    '라칸 R': ['사거리'],        // 자기 이속 버프 — 150 은 접촉 판정 반경
    '렝가 R': ['사거리'],        // 2500/3000/3500 = 가장 가까운 적 감지 반경 (위키 Sight radius)
    '트린다미어 R': ['사거리'],  // 자기 버프
    '베인 W': ['사거리'],        // 패시브
    '바이 W': ['사거리'],        // 패시브
    '티모 E': ['사거리'],        // 패시브 (독 묻히기)
    '질리언 W': ['사거리'],      // 자기 쿨 감소
    '카르마 R': ['사거리'],      // 자기 강화 (만트라)
    '카서스 R': ['사거리'],      // 전역 — 효과 범위 '전역' 만 남긴다 (10000 은 더미)
    '사이온 R': ['사거리'],      // 7500 = 돌진 최대 이동 거리 (누누 W 7500 과 같은 부류)
    '아리 W': ['사거리'],        // 700 = 여우불 자동 조준 검색 반경 (워윅 W 부류)
    '칼리스타 E': ['사거리'],    // 대상 지정 없음 — 창이 박힌 적 전부 (효과 범위 1100 이 판정)
    '다이애나 W': ['사거리'],    // 자기 보호막 + 궤도 구체 (800 은 내부값)
    '에코 R': ['사거리'],        // 4초 전 위치로 귀환 — 지점을 지정하지 않는다
    '자야 R': ['사거리'],        // 자기 도약 (원뿔 1060 은 효과 범위에 있다)
    // ② 자기(구체) 중심 광역 — 효과 범위 칸이 판정 범위
    '알리스타 Q': ['사거리'], '아무무 W': ['사거리'], '헤카림 Q': ['사거리'],
    '일라오이 R': ['사거리'], '자르반 4세 W': ['사거리'], '잭스 R': ['사거리'],
    '제이스 W': ['사거리'], '케넨 W': ['사거리'], '킨드레드 R': ['사거리'],
    '릴리아 Q': ['사거리'], '리산드라 W': ['사거리'], '노틸러스 E': ['사거리'],
    '니코 R': ['사거리'], '닐라 R': ['사거리'], '오리아나 R': ['사거리'],
    '레넥톤 Q': ['사거리'], '리븐 W': ['사거리'], '샤코 R': ['사거리'],
    '사이온 W': ['사거리'], '스카너 W': ['사거리'], '소나 E': ['사거리'],
    '제드 E': ['사거리'], '나르 R': ['사거리'],   // 제리 R 은 위 828.5 묶음 줄에 합쳤다
    // ★★ 원뿔·방향형 정책 (2026-08-24 밤, 사용자 확정): **원뿔 길이는 사거리 하나로만 적는다.**
    //   위키의 effect radius 는 원뿔에서 "부채꼴의 반지름" = 닿는 거리라 우리 사거리와 같은 뜻이다
    //   (코르키 E `690 Cone radius` · 판테온 E `525 Cone radius` 로 위키 괄호가 명시).
    //   둘을 나란히 두면 워윅 W "4000 두 번" 꼴이 되고, 카밀 W 는 위키가 `325(안쪽)/650(바깥쪽)`
    //   두 값이라 첫 숫자(안쪽 반경)만 잘려 오는 함정까지 있었다. 폭은 angle 칸(각도)이 담당하는데
    //   우리도 위키도 각도를 안 적는다 — "최대 사거리 지점의 폭" 은 숙제로 남김 (CLAUDE.md).
    //   ★ 카사딘 E 만 예외로 효과 범위(600)를 남긴다 — 우리 사거리 400 과 크게 어긋나는 유일한
    //     자리라 인게임 확인 전까지 두 값을 다 보여 준다 (인게임확인_20260824_전수점검.md)
    '카밀 W': ['효과 범위'], '카시오페아 R': ['효과 범위'], '코르키 E': ['효과 범위'],
    '미스 포츈 R': ['효과 범위'], '판테온 E': ['효과 범위'], '스웨인 Q': ['효과 범위'],
    '요네 W': ['효과 범위'], '그웬 Q': ['효과 범위'],
    // ★ 위에서 사거리 괄호로 옮긴 자리들 — 위키가 채우는 '전역' 을 효과 범위에서 지운다
    '아칼리 E': ['효과 범위'],
    '제드 R': ['효과 범위'],
    '오로라 Q': ['효과 범위'],
    '쉔 Q': ['효과 범위'],
    '이렐리아 E': ['효과 범위'],
    '요네 E': ['효과 범위'],
    '쓰레쉬 Q': ['효과 범위'],
    '브라이어 R': ['효과 범위'],
    // ★★ 조이 W 는 사거리 개념이 없다 (2026-08-24). bin override 가
    //   `[3000, 3000, 4500, 6000, 3000, 3000, 3000]` 이고 DD 가 그 1~5칸을 잘라
    //   `3000 / 4500 / 6000 / 3000 / 3000` 을 주는데, **랭크 3에서 6000이었다가 4에서 3000으로
    //   되돌아오는** 모양이라 랭크 스케일일 수 없다 (같은 스킬의 TotalBaseDamage 는 5~65 로 정상 증가).
    //   **롤위키에는 `range` 칸 자체가 없다** — 주문 파편은 밟아서 줍는 것이라 사거리가 없는 게 맞다.
    //   주운 주문을 쓸 때는 그 주문의 사거리를 따른다 (bin 에 ZoeW_SummonerFlash 425 · Heal 850 …).
    '조이 W': ['사거리'],
    // ★★ 징크스 Q 시전시간 — 우리 0.25 는 `JinxQAttack`(로켓으로 **공격할 때**) 값이고
    //   스킬 자체(미니건↔로켓 폼 전환)는 즉발이다. 롤위키도 `cast time = none` 이고
    //   `static = 0.9` 로 적는데, 그 0.9 는 **폼 전환 최소 쿨타임**이라 이미 쿨타임 칸에 들어가 있다
    //   (bin `JinxQ` cooldown 0.9). bin `JinxQ` castTime 0.485 는 폼 전환 애니메이션으로 보인다.
    '징크스 Q': ['시전시간', '사거리'],   // 사거리 600 도 폼 전환이라 시전 사거리가 아니다 (2026-08-24)
    // ══ "다음 기본 공격을 강화하는" 스킬 22자리 — 사거리 칸이 성립하지 않는다 (2026-08-24) ══
    //   ★★ 대상이나 위치를 지정하지 않고 **자기 자신에게 거는** 스킬들이다. 그런데 bin 본체에
    //     castRange 가 남아 있어서(레오나 Q 100 · 나서스 Q 255 · 가렌 Q 300 …) 그걸 사거리로 적고 있었다.
    //   ★ 롤위키는 이 스킬들에 `range`·`target range` 칸을 **아예 안 쓴다** — 그게 판별 기준이었다.
    //   ★ 초가스 E·트위치 R 과 같은 부류다 (기본 공격 강화 스킬엔 시전 사거리가 없다).
    '가렌 Q': ['사거리'],                  // bin 300
    '나서스 Q': ['사거리'],                 // bin 255
    '다리우스 W': ['사거리'],                // bin 300
    '드레이븐 Q': ['사거리'],                // bin 300
    '레넥톤 W': ['사거리'],                 // bin 300
    '레오나 Q': ['사거리'],                 // bin 100
    '렝가 Q': ['사거리'],                  // bin 450
    '볼리베어 Q': ['사거리'],                // bin 300
    '신 짜오 Q': ['사거리'],                // bin 375
    '자헨 Q': ['사거리'],                  // bin 200
    '잭스 W': ['사거리'],                  // bin 300
    '카밀 Q': ['사거리'],                  // bin 325
    '트런들 Q': ['사거리'],                 // bin 300
    '트위스티드 페이트 W': ['사거리'],           // bin 200
    '피오라 E': ['사거리'],                 // bin 425
    '피즈 W': ['사거리'],                  // bin 600
    '헤카림 E': ['사거리'],                 // bin 300
    '케일 E': ['사거리'],                  // bin 550
    '말파이트 W': ['사거리'],                // bin 400
    '바이 E': ['사거리'],                  // bin 400
    '제이스 R': ['사거리'],                 // bin 600
    // ══ 같은 부류 9자리 — 자기 버프·태세·폼 전환이라 시전 사거리가 없다 (2026-08-24) ══
    //   ★ 위 22자리와 판별 기준이 같다: **롤위키가 `range`·`target range` 칸을 안 쓴다.**
    //   ★ 쉔 Q 는 뺐다 — "기의 검을 불러냅니다" 라 **위치를 지정하는 진짜 사거리**다.
    '우디르 Q': ['사거리'],               // `UdyrQ` cr600 · radius 20 — 발톱 태세는 자기 버프다 (튕기는 미사일은 `UdyrQBounceMis` cr25000)
    '우디르 E': ['사거리'],               // `UdyrE` cr600 — 쇄도 태세도 자기 버프. 돌진은 평타에 붙는다
    '쉔 W': ['사거리'],                 // ★ bin 에 400 이 **아예 없다** (`ShenW` 는 radius 35000 뿐) — 출처 불명
    '애쉬 Q': ['사거리'],                // ★ bin 에 400 이 **아예 없다** (`AsheQAttack` 은 cr25000 더미) — 출처 불명
    '노틸러스 W': ['사거리'],              // `NautilusWDoT` ov350 — 보호막 + 평타 강화라 시전 사거리가 아니다
    '일라오이 W': ['사거리'],              // `IllaoiW` cr350/ov400 · radius 350 — "다음 기본 공격 시 대상에게 돌진" 이라 평타 대상이 곧 목표다
    '자야 W': ['사거리'],                // `XayahW` cr1000 = radius 1000 — 칼날 폭풍은 자기 버프다
    '렉사이 W': ['사거리'],               // `RekSaiW` cr1500/ov1650 · radius 100 — 매복/돌출 전환이라 시전 사거리가 없다
    '바루스 W': ['사거리'],               // `VarusW` cr10000(더미)/ov750 · radius 210 — 역병은 평타에 붙는 지속 효과다
    // ══ 자기 버프·변신·태세·정화 15자리 — 시전 사거리가 없다 (2026-08-24) ══
    //   ★ 위 31자리와 판별 기준이 같다 (롤위키가 `range`·`target range` 칸을 안 쓴다) +
    //     문장이 "자신을/자신에게" 로 시작하는 자기 대상 스킬이다.
    '갱플랭크 W': ['사거리'],                // 귤을 먹어 방해 효과 제거 — 자기 정화
    '나피리 W': ['사거리'],                 // 사냥 준비 · 무리 소환 — 자기 버프
    '닐라 W': ['사거리'],                  // 자신을 안개로 감싸 유체화 — 자기 버프
    '람머스 Q': ['사거리'],                 // 공 모양으로 몸을 말아 — 자기 버프
    '람머스 W': ['사거리'],                 // 방어 태세 — 자기 버프
    '로크 W': ['사거리'],                  // 자신의 영혼을 불태워 — 자기 버프
    '멜 W': ['사거리'],                   // 자신을 방어막으로 감싸 — 자기 버프
    '블리츠크랭크 E': ['사거리'],              // 주먹에 힘을 모아 다음 공격 강화 — 위 22자리와 같은 부류
    '아펠리오스 W': ['사거리'],               // 주 무기와 보조 무기를 교체 — 자기 전환
    '올라프 R': ['사거리'],                 // 자신에게 걸린 방해 효과를 제거 — 자기 정화
    '잭스 E': ['사거리'],                  // 방어 태세에 들어가 기본 공격을 회피 — 자기 버프
    '케넨 E': ['사거리'],                  // 번개 구체로 변신해 유체화 — 자기 버프
    '케인 E': ['사거리'],                  // 유체화 상태로 지형 통과 — 자기 버프
    '트린다미어 Q': ['사거리'],               // 분노를 소모해 체력 회복 — 자기 회복
    '워윅 W': ['사거리'],        // 자기 주변 검색이라 사거리 개념이 없다 — 효과 범위 4000 만 남긴다 (위 STAT_MANUAL)
    // ★★ 둘 다 **기본 공격을 강화하는 스킬**이라 "사거리" 칸이 성립하지 않는다 (2026-08-24 정정).
    //   ★ 초가스 E — 650 을 넣었다가 되돌렸다. bin 을 보면 본체 `VorpalSpikes` 는
    //     castRange 40 · **DataValue `AttackRangeIncrease` 50**(공격 사거리를 50 늘린다) 이고,
    //     **650 은 `VorpalSpikesMissle` 이 날아가는 거리**다 — 투사체 속도 1475 와 짝이지 사거리가 아니다.
    //   ★ 트위치 R — 문장이 이미 "공격 사거리가 {p2}, 공격력이 {p3} 증가" 라고 적는다 (p2 = 300).
    //     위키 `1100 (+100% bonus attack range)` 도 **관통 화살이 날아가는 거리**지 시전 사거리가 아니다.
    //     우리가 쓰던 1200 은 E(`TwitchExpunge`) 와 같은 값이었다.
    '초가스 E': ['사거리'],
    '트위치 R': ['사거리'],
    // 위키 `speed`/`width` 가 투사체가 아닌 것을 적은 자리 (wiki_stats.json 폴백을 막는다)
    '유나라 Q': ['투사체 속도'],     // 위키 10000 = "즉시" 더미
    '클레드 P': ['투사체 속도'],     // 위키 400 = 스칼이 달려오는 속도
    '렐 R': ['투사체 속도', '사거리'], // 위키 300 = 끌어당기는 속도 · 사거리 200 은 자기 중심 광역 (2026-08-24)
    '그웬 W': ['투사체 속도'],       // 위키 2000 = 안개 퍼지는 속도
    '쓰레쉬 R': ['스킬 폭'],         // 위키 30 = 벽 두께
    // 2026-08-24 재점검 (위키 툴팁 전수) — 라벨과 뜻이 다른 값
    '크산테 W': ['투사체 속도'],     // bin 1500 = 돌진 속도와 같은 값 (투사체가 아니다)
    '람머스 R': ['투사체 속도'],     // bin 900 = 돌진 최저 속도 (돌진 속도 900~2000 에 이미 있다)
    '아크샨 Q': ['효과 범위'],       // 위키 400 = 부메랑 시야 반경
    '나피리 R': ['효과 범위'],       // 위키 2100 = 시야 반경
    '스몰더 R': ['효과 범위'],       // 위키 1000 = 파도 시야 반경
    '야스오 W': ['효과 범위'],       // 위키 300 = 시야 반경
    '크산테 R': ['투사체 속도'],     // 위키 2000 = 밀쳐내는 속도 (투사체가 아니다)
    '트리스타나 W': ['투사체 속도'], // bin 1100 = 점프(돌진) 속도 — 돌진 속도로 옮긴다 (아래 STAT_MANUAL)
};

// ★ 패시브 쿨타임 각주 — **인게임 툴팁 값과 실제 동작이 다른 자리** (2026-08-14).
//   우리는 인게임 툴팁을 재현하는 사이트라 **툴팁 값을 그대로 쓰되**, 각주로 실제를 알려준다.
//   지금은 니코 하나다. 나무위키가 `"클라이언트 설명에는 이유는 모르겠지만 6초로 나와 있다"`
//   고 적어 뒀고, 인게임에서 6초로 표시되는 것도 확인했다 (실제 재사용 대기시간은 2초).
//   ★ 각주 HTML 은 **홑따옴표만** 쓸 것 — 값이 큰따옴표 문자열이라 `"` 가 들어가면
//     이스케이프되고 `add_level_graphs.js` 의 줄 파싱 정규식이 통째로 깨진다.
const PASSIVE_CD_NOTE = {
    '니코': { real: '2초', why: '인게임 툴팁 표시값' },
};
const passiveCdNoteMade = [];

// ★ 우상단 값의 긴 괄호를 각주 [n] 로 접는다 (2026-08-24, 야스오 Q 지적).
//   `쿨타임 4 ~ 1.33 (추가 공격 속도 1.67%당 1% 감소, 최대 67%)` 가 한 줄에 다 나가면 너무 길다 —
//   괄호를 hover 각주로 빼서 `쿨타임 4 ~ 1.33 [1]` 로 줄인다. 쿨타임·소모값·stats 에만 적용한다 (문장 pN 은 제외).
//   ★ 남기는 괄호 셋: ① `+` 로 시작(계수 — 올라프 E 체력 소모) ② `…에 따라` 로 끝(값의 정체 —
//     레벨/충전/거리) ③ 7자 미만(눈으로 읽는 게 빠르다 — `(가속)`·`(재사용)`·`(튕김)`).
//   ★ 홑따옴표만 쓴다 — 값이 큰따옴표 문자열이라 `"` 가 들어가면 add_level_graphs 의 줄 파싱이 깨진다.
// ★★ `ctr` 은 **스킬 하나** 안에서 이어지는 각주 번호다 (2026-08-24).
//   없이 쓰면 값마다 [1] 부터 다시 세어, 한 스킬에 각주가 둘 이상일 때
//   화면에 [1] 이 여러 번 뜬다 — 야스오 Q 는 쿨타임·사거리·시전시간 셋이 전부 [1] 이었다.
//   번호 순서는 화면 순서와 같다 (쿨타임 → 소모값 → 우상단 값. index.html 의 champ-skill-meta).
function foldParenNote(val, ctr) {
    if (typeof val !== 'string' || val.includes('custom-footnote')) return val;
    let no = 0;
    return val.replace(/\s*\(([^()]+)\)/g, (m, inner) => {
        const t = inner.trim();
        // `재사용 N초` 는 길이와 무관하게 인라인 — 아무무 Q `(재사용 3초)` 는 6자라 남는데
        // 닐라 E `(재사용 0.5초)` 만 8자라 각주가 되면 재충전형끼리 들쭉날쭉해진다
        if (t.startsWith('+') || t.startsWith('재사용') || /에 따라$/.test(t) || t.length < 7) return m;
        no++;
        const shown = ctr ? ++ctr.n : no;
        return `<span class='custom-footnote'>[${shown}]<span class='custom-footnote-content'>` +
            `<div style='font-size:12px; color:#fff; white-space:nowrap;'>${t}</div></span></span>`;
    });
}

function passiveCdFootnote(no, shown, real, why) {
    return `<span class='custom-footnote'>[${no}]` +
        `<span class='custom-footnote-content'>` +
        `<div style='font-size:11px; color:#fff; margin-bottom:5px; white-space:nowrap;'>실제 재사용 대기시간</div>` +
        `<div style='font-size:12px; color:#ff9900; font-weight:bold;'>${real}</div>` +
        `<div style='font-size:10px; color:#9aa4af; margin-top:5px; white-space:nowrap;'>${shown}초는 ${why}입니다</div>` +
        `</span></span>`;
}

// ★★ 레벨 비례 **기본 피해량**만 정수로 접는다 (2026-08-14).
//   카타리나 P 가 `68.185 ~ 240.015 (레벨에 따라)` 로 나가서 읽기 불편했다 -> `68 ~ 240`.
//
//   ★★ 전역 반올림(`tidy` 에서 "소수 3자리면 정수로")을 시도했다가 **되돌렸다.**
//     두 가지가 망가진다:
//       ① **랭크별 배열이 뒤죽박죽이 된다** — 아리 Q 가
//          `105 / 118.125 / 131.25 / 144.375 / 157.5%` -> `105 / 118 / 131.25 / 144 / 157.5%`
//          로 **일부 칸만 정수**가 됐다. 한 줄 안에서 자릿수가 들쭉날쭉해 더 나쁘다
//       ② **데미지가 아닌 자리까지 바뀐다** — 브랜드 P 마나 회복, 쉔 P 쿨감,
//          신 짜오 W 미니언 배율, 그레이브즈 P **계수**(`총 공격력의 …%`)
//     그래서 자리를 표로 못 박는다. 키는 `"<한글 챔피언명> <슬롯> / <계산식 이름>"`.
//   ★ 값 **맨 앞의 `A ~ B`** 만 접는다. 뒤따르는 계수(`(+ 주문력의 70 ~ 100 …%)`)는 그대로 둔다
const ROUND_BASE_DAMAGE = new Set([
    '카타리나 P / TotalDamage',
]);
const roundBaseUsed = new Set();

// "68.185 ~ 240.015 (레벨에 따라) (+ …)" -> "68 ~ 240 (레벨에 따라) (+ …)"
function roundLeadingRange(val) {
    return String(val).replace(/^(\d+(?:\.\d+)?)\s*~\s*(\d+(?:\.\d+)?)(?=\s*\(레벨에 따라\))/,
        (all, a, b) => `${Math.round(parseFloat(a))} ~ ${Math.round(parseFloat(b))}`);
}

const statManualUsed = new Set();  // 안 쓰인 키는 오타 경고로 잡는다
const passiveCdMade = [];         // 패시브 쿨타임을 채운 곳 (bin 의 keyCooldown)
const passiveCdFail = [];         // keyCooldown 은 있는데 값을 못 푼 곳
const activeCdMade = [];          // QWER 의 keyCooldown 문장으로 쿨타임을 바꾼 곳 (2026-08-23)
const activeCdFail = [];          // QWER keyCooldown 이 있는데 못 푼 곳
const costSentence = [];          // 마나 스킬인데 소모값 문장을 그대로 쓴 곳 (2026-08-23)
// DD partype (마나/기력/…) — 소모값 문장의 @AbilityResourceName@ 을 푸는 데 쓴다
const ddPartype = (() => {
    try {
        const f = fs.readdirSync(path.join(__dirname, '.cache')).filter(x => /^dd_championFull_/.test(x)).sort().pop();
        const d = JSON.parse(fs.readFileSync(path.join(__dirname, '.cache', f), 'utf8')).data;
        const m = {};
        // ★ 여기 c.id 는 DD 의 영문 키고, 아래 루프의 c.id 는 CD 의 **숫자 id** 다. 둘 다 넣는다.
        for (const c of Object.values(d)) { m[c.id] = c.partype || ''; m[String(c.key)] = c.partype || ''; }
        return m;
    } catch (e) { return {}; }
})();
const dashMulti = [];             // 돌진 속도 후보가 둘 이상이라 건너뛴 곳
const missileSelfDropped = [];    // 자기 대상 스킬이라 투사체 속도를 뺀 곳
const missileFamilyFix = [];      // 본체 틀값을 계열 미사일 객체 값으로 바꾼 곳 (2026-08-23)
const missileFamilyMulti = [];    // 계열 후보가 여럿이라 못 고른 곳
const widthSelfDropped = [];
const widthNoDouble = [];   // 위키 폭 = mLineWidth 라 2배를 안 한 자리 (2026-08-24)      // 자기 대상 스킬이라 스킬 폭을 뺀 곳
let spellIndex = {};              // 지금 처리 중인 챔피언의 스펠 객체 색인 (buildSpellIndex)

// 툴팁 철자와 bin 철자가 어긋난 자리를 기록한다.
function noteCase(asked, actual) {
    const k = `${asked}|${actual}`;
    if (caseSeen.has(k)) return;
    caseSeen.add(k);
    if (caseList.length < 60) caseList.push(`${asked}  ->  ${actual}   (${ctx})`);
}

// DataValue 를 이름으로 찾는다. 정확히 일치가 우선, 없으면 대소문자를 무시한다.
//   ★ 계산식 안에서 다른 DataValue 를 참조할 때도 철자가 어긋난다.
//     AoeDamagePercent -> AoEDamagePercent, BonusLifesteal -> BonusLifeSteal 같은 식.
//     조각 하나가 null 이 되면 계산식이 통째로 죽으므로 여기가 제일 아프다.
// 폼이 두 개인 챔피언의 "두 번째 폼" 스킬.
//   ★ build_champion_data.js 의 FORM2 와 반드시 같아야 한다.
//     어긋나면 문장은 있는데 값이 없거나 그 반대가 된다.
//   ★ `label1` = **첫 번째 폼** 이름. 두 번째 폼 박스엔 원래부터 라벨이 있었는데
//     본체 쪽엔 없어서 "이게 어느 폼 설명이냐" 를 알 수 없었다 (2026-08-10 추가).
//     `build_champion_data.js` 의 FORM2 와 **항상 같아야 한다.**
const FORM2 = {
    Nidalee: { label: '쿠거 형태',   label1: '인간 형태',   Q: 'Takedown',         W: 'Pounce',           E: 'Swipe' },
    Elise:   { label: '거미 형태',   label1: '인간 형태',   Q: 'EliseSpiderQCast', W: 'EliseSpiderW',     E: 'EliseSpiderE' },
    Jayce:   { label: '대포 형태',   label1: '망치 형태',   Q: 'JayceShockBlast',  W: 'JayceHyperCharge', E: 'JayceAccelerationGate', R: 'JayceStanceGtH' },
    Gnar:    { label: '메가 나르',   label1: '미니 나르',   Q: 'GnarBigQ',         W: 'GnarBigW',         E: 'GnarBigE' },
    //   ★ '굴 파기' 였는데 인게임 표기가 '매복 상태' 라 맞췄다. 기본 QWE 문장은
    //     원래부터 "돌출 상태:" 로 시작한다 — 그래서 label1 도 '돌출 상태' 다
    RekSai:  { label: '매복 상태',   label1: '돌출 상태',   Q: 'RekSaiQBurrowed',  W: 'RekSaiWBurrowed',  E: 'RekSaiEBurrowed' },
    Kled:    { label: '스카를 하차', label1: '스카를 탑승', Q: 'KledRiderQ' },
};

function findSpellObj(bin, alias, objName) {
    const direct = bin[`Characters/${alias}/Spells/${objName}`];
    if (direct && direct.mSpell) return direct;
    for (const k of Object.keys(bin)) {
        if (k.endsWith('/' + objName) && bin[k] && bin[k].mSpell) return bin[k];
    }
    return null;
}

// 스킬 하나에 아이콘이 여러 개 달리는 자리 (재시전·취소·진화·1·2·3타 등).
//   bin 의 스펠 객체에 붙은 아이콘 파일 이름으로 찾는다.
//   ★ 규칙: 파일 이름이 챔피언 이름으로 시작하고, 그 뒤 첫 글자가 QWER 이고
//     바로 숫자가 따라와야 한다 (aatrox_q2, garen_e2, syndraw2 ...).
//     숫자 조건이 없으면 gragasexplosivecask 의 e, rivenwindscar 의 w 를
//     슬롯으로 오인한다.
//   ★ 다른 챔피언 에셋이 섞여 들어오므로(쓰레쉬에 Bowmaster_, 신드라에 Cryophoenix_)
//     챔피언 이름으로 시작하는 것만 받는다.
const ICON_BASE = 'https://raw.communitydragon.org/latest/game/';

// 스펠 객체에 안 붙어 있어서 위 규칙으로 못 찾는 아이콘. 파일이 실재하는 것만 적는다.
//   카이사 진화 아이콘이 그렇다 — kaisa_q2/w2/e2.png 는 있는데 스펠 객체엔 없다.
//   전부 파일 실재(200)와 롤위키 확인을 같이 거친 것만 적는다.
//   ★ 2026-08-09: 전수 방식으로 바꾸면서 대부분이 필요 없어졌다.
//     카이사·카르마·빅토르·카밀·크산테는 이제 bin 에서 자동으로 잡힌다.
//     크산테는 그대로 두면 `icons_ksante_q2`(수동)와 `ksante_q2`(자동)가
//     **같은 아이콘을 두 번** 넣는다 (nameOf 가 icons_ 를 벗겨서 이름이 갈린다).
//     리 신만 남는다 — 2타 스펠 객체가 1타와 같은 아이콘을 참조해서 bin 에 없다.
const EXTRA_ICONS = {
    // 2타 — 위키: "공명타·철갑·무쇠주먹은 각자 아이콘을 쓴다"
    LeeSin: { Q: ['leesinq2'], W: ['leesinw2'], E: ['leesine2'] },
    // ★ 인게임 확인 2026-08-10. 파일은 실재하고 화면에도 뜨는데 bin 스펠 객체엔 안 붙어
    //   있어서 자동으로 못 찾는다. (CD 디렉터리 목록으로 파일 실재 확인함)
    Locke: { W: ['locke_w2'] },        // W 재시전 — Q·R 은 인게임에 없다고 확인됨
    Sion:  { P: ['sion_passive2'] },   // P 좀비 형태
    //   ★ 키아나 Q 는 원소가 셋(얼음·바위·야생)인데 bin 엔 blue·green 만 붙어 있다.
    //     `qiyana_q2_red`(바위) 는 파일이 실재하는데 스펠 객체에 안 달려 있다 (화면 확인 2026-08-10)
    Qiyana: { Q: ['qiyana_q2_red'] },
    //   ★ 트페 W 는 카드가 셋인데 bin 엔 금색만 붙어 있다. 파랑·빨강도 파일은 실재한다
    TwistedFate: { W: ['cardmaster_blue', 'cardmaster_red'] },
    //   ★ 아펠리오스는 P·Q·R 이 전부 **무기 5종별로 효과가 갈린다.** 무기 아이콘은
    //     별도 캐릭터 bin 이라 자동으로 안 잡힌다. 파일명 규칙: `_m` = 주 무기,
    //     `_l` = 보조 무기, `_u` = 궁극기 강화. P·Q 는 주 무기 기준이라 `_m` 을 쓴다
    Aphelios: {
        P: ['calibrum_m', 'severum_m', 'gravitum_m', 'infernum_m', 'crescendum_m'],
        Q: ['calibrum_m', 'severum_m', 'gravitum_m', 'infernum_m', 'crescendum_m'],
    },
    //   ★ 유나라는 R(자기 초월) 을 켜면 W·E 가 다른 스킬로 바뀐다
    //     (심판의 궤적 -> 파멸의 궤적, 칸메이의 발자취 -> 닿지 않는 그림자).
    //     `Yunara_RW.dds` / `Yunara_RE.dds` 가 실재하는데 **스펠 객체가 아니라 버프 쪽에
    //     붙어 있어서** 자동 탐색(스펠 객체만 훑는다)에 안 걸린다. 2026-08-12 확인.
    //   Q 의 초월 파트는 전용 아이콘이 없어서(라이엇이 안 만들었다) 궁 아이콘을 쓴다.
    //     Q 는 `yunara_q2`(컬러, 사용 시) 가 스펠 객체에서 자동으로 잡히므로 여기 안 적는다.
    Yunara: { Q: ['yunara_r'], W: ['yunara_rw'], E: ['yunara_re'] },
};

// 기본 아이콘(Data Dragon 것)이 인게임과 다를 때 갈아끼운다.
//   ★ 벨베스 Q 가 유일한 사례다. 파일이 `bv_q_1`~`bv_q_15` 인데 **사분면 4칸의 비트값**이라
//     15 = 네 칸 다 켜진 그림이다. DD 가 주는 기본 아이콘은 하필 `bv_q_1`(한 칸만 켜짐)이라
//     인게임에서 실제로 보는 모습과 다르다. 나머지 14개는 "지금 몇 칸 찼나" 를 나타내는
//     상태 표시라 사이트에 다 넣을 값이 없다 — 꽉 찬 것 하나만 쓴다 (인게임 확인 2026-08-10).
const ICON_OVERRIDE = {
    Belveth: { Q: 'bv_q_15' },
    //   ★ 유나라 Q — DD 기본 아이콘은 `yunara_q`(흑백 구슬) 인데, 스킬 칸에서는
    //     컬러판(`yunara_q2`) 이 훨씬 알아보기 좋다. 설명 첫 파트("기본 지속 효과") 옆에는
    //     흑백을 그대로 두고 싶어서 아래 BODY_ICON 을 같이 쓴다 (2026-08-12).
    Yunara: { Q: 'yunara_q2' },
};

// 배열 템플릿의 **0번 파트** 옆에만 쓰는 아이콘. 스킬 칸(버튼·이름) 아이콘과 다르게 하고 싶을 때.
//   ICON_OVERRIDE 로 버튼을 컬러로 바꾸면 0번 파트까지 같이 컬러가 된다 — 둘 다
//   `values.img` 를 보기 때문이다. 여기 적으면 0번 파트만 따로 지정된다.
//   유나라 Q 가 유일한 사례: 버튼은 컬러(활성 상태), "기본 지속 효과" 옆은 흑백(비활성 상태).
const BODY_ICON = {
    Yunara: { Q: 'yunara_q' },
};

// ─── 2026-08-09: 전수 방식으로 교체 ─────────────────────────────────────
//   옛 방식은 스펠 객체마다 아이콘을 "하나만" 읽고, 파일명이 `<alias>[qwer][2-9]`
//   인 것만 받았다. 그래서 두 부류를 통째로 놓쳤다:
//     1. 본체 스펠이 아이콘을 **배열**로 들고 있는 경우
//        (GwenR = [Gwen_R, Gwen_R2, Gwen_R3] — 첫 개만 읽어 R2·R3 를 버렸다)
//     2. 파일명이 숫자 패턴이 아닌 경우
//        (Khazix_Q_red, Kayn_Q_Ass, HweiQE, Icons_Vex_Q02 — `0` 이 [2-9] 밖)
//   173명 전수 스캔으로 확인한 수: 기본 5개 외 아이콘이 **121명 / 335개** 있고,
//   옛 방식은 그중 72개만 잡았다. 새 방식은 슬롯을 확정할 수 있는 221개를 잡고,
//   나머지는 아래 ICON_SLOT 표로 채운다. 근거는 `아이콘위키조사.md` (173명 위키 대조).

// 노이즈 — 롤위키 대조로 확정한 네 부류.
//   ★ 구분 기준은 접미사 철자가 아니라 "플레이어가 누르는가" 다.
//     _Cancel/_Recast 는 실제 시전 동작이라 살리고(우르곳 W 수동 종료),
//     _Grey/_Off 는 같은 스킬의 비활성 표시라 버린다.
//   ★ 구분자가 없는 변종이 있다 — CaitlynPGrey(언더스코어 없음),
//     BrandP-Debuff(하이픈). 그래서 [-_]? 로 받는다.
const ICON_NOISE = /[-_]?(grey|gray|off|disabled|cooldown|no_charge|cannotcast|debuff|slow|sleep|charging)$/i;
//   접미사가 아니라 이름 중간에 오는 노이즈. 스킨별 아이콘·퀘스트 진행 표시·콜라보다.
//   (아펠리오스 ApheliosP_Skin01~40, 세나 Senna_Quest_Lucian, 제리 ZeriXEkko)
const ICON_NOISE2 = /(_skin\d|_quest_|xekko)/i;
//   구버전 이름 — 그 챔피언 것이 맞지만 지금 게임에 없는 스킬이다.
//   파일명이 alias 로 시작해서 자동 필터에 안 걸리므로 손으로 적는다.
//   (아리 "정기 흡수" 는 현재 "정기 조각" 이다 — CLAUDE.md 의 stringtable 경고와 같은 일)
const ICON_OLD = new Set([
    'ahri_souleater',            // 옛 패시브 "정기 흡수" (현재는 정기 조각)
    'thresh_e0',
    'missfortune_doubleup',      // 리메이크 전 Q 아이콘 (인게임 확인 2026-08-10)
]);
// ★ 인게임에서 직접 보고 "아니다" 로 판정한 것 (2026-08-10).
//   위키·파일명만으로는 진짜와 구별이 안 돼서 보류였던 자리들이다.
//   근거는 `아이콘확인목록.md` A 절에 항목별로 적혀 있다.
const ICON_REJECT = new Set([
    // 스킬 아이콘이 아니라 **적에게 걸리는 디버프/버프 표시**
    'kayle_q_shred',             // 케일 Q 방어력 감소 표시
    'sett_buff',                 // 세트 — 상대에게 갈 때 붙는 버프
    // **중첩·상태 표시**라 스킬 칸이 갈리는 게 아님
    'kayle_p_attackspeed',       // 케일 P 스택
    'senna_passivehaste',        // 세나 P 가속
    'oriannap2',                 // 오리아나 P
    'darius_passivebuff',        // 다리우스 P 녹서스의 힘
    // 회색(비활성) 아이콘. `_grey` 가 이름에 없어서 자동 필터에 안 걸린다
    'monkeykingcyclonecancel',   // 오공 R — 흑백 아이콘
    // 이스터에그·모드 전용
    'jax_fishing', 'fish_1', 'fish_2',   // 잭스 낚시
    // 인게임에 하위 아이콘이 아예 없는 자리
    'ryze_q_shield',
    'gravesbuckshot',
    'akali_q2',
    'mel_r2',                    // 처형 전용 표시
    'fiddlesticksw2', 'fiddlesticksr2',
    // ── 2026-08-10 2차 (화면을 직접 보고 판단. 가렌~블라디미르 60명 검수분) ──
    'graveshighnoon',            // 그레이브즈 R
    'rumble_junkyardtitan3',     // 럼블 P
    'rumble_electroharpoon_twin',// 럼블 E
    'lillia_icon_buff_speedup',  // 릴리아 Q
    'bard_meeps',                // 바드 P
    'varusw2',                   // 바루스 W
    'brandsear', 'brandpillarofflame', 'brandconflagration', 'brandpyroclasm',  // 브랜드 QWER
    'vladimir_tidesofblood',     // 블라디미르 E
    'briarwbuff',                // 브라이어 W — 세 아이콘 중 맨 오른쪽
    'draven_whirlingdeath_recall',   // 드레이븐 R
    // 르블랑 R — 미믹 Q·W·E 세 개만 쓴다. 궁 기본 설명엔 아이콘을 안 붙인다
    'leblancrr', 'leblancrwreturn',
    'syndra_wr1',                // 신드라 W — 잡는 상태. 던지는 쪽(wr2)만 쓴다
    // ── 2026-08-10 3차 (58스킬 화면 검수분) ──
    'gnar_e_cancel', 'viego_buff', 'senna_e2', 'senna_r2',
    'zerip', 'zeriqe', 'zeriqpassive', 'zeriqr', 'zeriqre',
    'jhin_r_shot', 'jhin_r_shot4', 'talonnoxiandiplomacy', 'taliyah_w2',
    'twitch_deadlyvenom', 'twitch_q_stealth', 'fiddlesticksp2', 'fiddlestickse2',
    // 사미라 R 7개 — 1차 위키 조사에서 이미 "내부 연출용" 으로 판정났던 자리다.
    //   전수 수집기로 바꾸면서 다시 들어왔던 것을 화면 확인으로 재확인해 뺀다
    'samirar2', 'samirar3', 'samirar4', 'samirar5', 'samirar6', 'samirar7', 'samirar8',
    'evelynn_ravage',            // 이블린 E — 셋 중 맨 오른쪽
    'corki_thepackage',          // 코르키 R — 둘 중 오른쪽
    'kayn_r1_slay', 'kayn_r2_primary',   // 케인 R — 남기는 건 r2_ass(암살자)·r2_slay(학살자)
    'hweiwashbrush',             // 흐웨이 W — 넷 중 첫 번째
    // ★ 케인 Q·E — 문장에 **한쪽 폼 설명만** 있다. Q 는 "다르킨 학살자" 만,
    //   E 는 "그림자 암살자" 만 나온다. 짝이 없는 아이콘은 붙일 자리가 없어서 뺀다
    //   (원문에 그 폼 설명이 아예 없는 것이라 우리가 지어낼 수는 없다)
    'kayn_q_ass', 'kayn_e_slay',
]);
//   기본 아이콘과 그림이 같은데 파일만 따로 있는 것. 넣으면 같은 아이콘이 두 번 나온다.
//   ★ 자동 판별이 안 된다 — 파일명이 기본과 달라서(rengarq vs rengar_q) 이름 비교로는
//     못 걸러지고, 그림을 봐야 안다. 롤위키 대조로 확인한 것만 적는다.
const ICON_DUP = new Set([
    'rengar_q',     // 기본 Q 와 동일 (위키: Q 하위는 강화판 하나뿐)
    'kled_q',       // 〃
    'qiyana_q1',    // 〃 (q2_green/q2_blue 가 진짜 하위)
    'syndraw2',     // W 가 적에게 거는 표시(SyndraWDebuff). syndra_w2 가 진짜 강화판
]);

// 파일명에 슬롯 글자가 없어 자동으로 못 정하는 것. 롤위키에서 확인한 슬롯을 적는다.
//   ★ 여기 없는 자리는 "확인 안 됨" 이 아니라 "노이즈로 판정" 이다.
//     (블리츠크랭크 RocketGrab = 기본 Q 중복, 잔나 HowlingGale_URF = URF 전용 등)
// 슬롯을 **강제로** 지정한다. 이름·객체명으로 자동 판별한 결과를 덮어쓴다.
//   ★ 르블랑 미믹(R 로 직전 스킬을 복제)이 유일한 사례다. 파일명이 `LeblancRQ` 라
//     Q 슬롯으로 잡히는데, 실제로는 **R 을 눌렀을 때 나가는 Q** 라서 R 밑에 있어야
//     뜻이 통한다 (화면 확인 2026-08-10). `LeblancWReturn`(W 자체의 귀환)은 W 에 남긴다.
// 중첩형 {{키_@인덱스@}} 를 푼다. **`build_champion_data.js` 의 NESTED_INDEX 와 항상 같아야 한다** —
//   어긋나면 문장의 {pN} 개수와 값의 pN 개수가 안 맞아 화면에 `{p2}` 가 그대로 찍힌다.
const NESTED_INDEX = {
    'game_spell_kayn_p_main': 0,      // 0 = 변신 전 (양쪽 폼 설명이 다 들어 있다)
    'spell_secondsight_tooltip': 1,   // 1 = 소환사의 협곡 (2 는 아레나)
};
const resolveNested = (text, strings) => {
    if (!text) return text;
    const tbl = (strings && strings.entries) ? strings.entries : strings;
    return String(text).replace(/\{\{\s*([A-Za-z0-9_]+?)_(?:@[^@]+@|\{p\d+\})\s*\}\}/g, (m0, prefix) => {
        const idx = NESTED_INDEX[prefix.toLowerCase()];
        if (idx === undefined) return m0;
        const hit = tbl && tbl[`${prefix.toLowerCase()}_${idx}`];
        return (typeof hit === 'string' && hit.trim()) ? hit : m0;
    });
};

const ICON_FORCE_SLOT = {
    Leblanc: { leblancrq: 'R', leblancrw: 'R', leblancre: 'R' },
    // ★ 아래 셋은 "궁이 기본 스킬을 강화하는" 구조라, 강화판 아이콘이 Q/W/E 가 아니라
    //   **궁 설명 옆에** 있어야 뜻이 통한다 (화면 확인 2026-08-10).
    Karma:        { karma_q2: 'R', karma_w2: 'R', karma_e2: 'R' },
    Khazix:       { khazix_q_red: 'R', khazix_w_red: 'R', khazix_e_red: 'R' },
    Heimerdinger: { heimerdinger_q2: 'R', heimerdinger_w2: 'R', heimerdinger_e2: 'R' },
};

// 한 슬롯 안의 아이콘 **순서**를 강제한다. 기본은 파일 이름순인데(`q2` -> `q3`),
//   르블랑 R 은 이름순이면 `re, rq, rw` = E·Q·W 가 돼서 문장(Q·W·E)과 어긋난다.
//   여기 적힌 것만 앞으로 당기고 나머지는 뒤에 이름순으로 붙는다.
const ICON_ORDER = {
    Leblanc: { R: ['leblancrq', 'leblancrw', 'leblancre'] },
    //   신드라 W 는 재사용(던지기) 다음에 진화가 와야 하는데 이름순이면 진화(w2)가 앞선다
    Syndra: { W: ['syndra_wr2', 'syndra_w2'] },
    // ★ 흐웨이는 **문장 순서와 파일 이름순이 서로 다르다.** 조합 스킬이라 짝이 어긋나면
    //   엉뚱한 그림이 붙는다. 문장에 나오는 차례대로 적는다 (화면 확인 2026-08-10).
    Hwei: {
        Q: ['hweiqq', 'hweiqw', 'hweiqe'],   // 파멸의 화염 · 절단의 번개 · 녹아내린 균열
        W: ['hweiwq', 'hweiww', 'hweiwe'],   // 쏜살같은 물살 · 반사의 웅덩이 · 요동치는 빛
        E: ['hweieq', 'hweiew', 'hweiee'],   // 암울한 형상 · 심연의 응시 · 파괴의 아귀
    },
    // 궁으로 옮긴 강화판들도 궁 문장에 나오는 차례(Q -> W -> E)로 맞춘다
    Karma:        { R: ['karma_q2', 'karma_w2', 'karma_e2'] },
    Khazix:       { R: ['khazix_q_red', 'khazix_w_red', 'khazix_e_red', 'khazix_r_red'] },
    Heimerdinger: { R: ['heimerdinger_q2', 'heimerdinger_w2', 'heimerdinger_e2'] },
    // 케인 Q·E 는 한쪽 폼 설명만 문장에 있다 (아래 ICON_REJECT 참고)
    Qiyana:      { Q: ['qiyana_q2_blue', 'qiyana_q2_red', 'qiyana_q2_green'] },  // 얼음 · 바위 · 야생
    TwistedFate: { W: ['cardmaster_blue', 'cardmaster_red', 'cardmaster_gold'] }, // 파랑 · 빨강 · 금색
    // 아펠리오스 무기 순서는 문장과 같게 — 만월총·절단검·중력포·화염포·반월검
    Aphelios: {
        P: ['calibrum_m', 'severum_m', 'gravitum_m', 'infernum_m', 'crescendum_m'],
        Q: ['calibrum_m', 'severum_m', 'gravitum_m', 'infernum_m', 'crescendum_m'],
        R: ['calibrum_u', 'severum_u', 'gravitum_u', 'infernum_u', 'crescendum_u'],
    },
};

const ICON_SLOT = {
    // 무기별 궁 변형 5종. 무기 이름이 곧 파일명이라 슬롯 글자가 없다
    Aphelios: { calibrum_u: 'R', severum_u: 'R', gravitum_u: 'R', infernum_u: 'R', crescendum_u: 'R' },
    // 점화 강화판 — 파일명이 스킬 영문명이다
    Brand: { brandsear: 'Q', brandpillarofflame: 'W', brandconflagration: 'E', brandpyroclasm: 'R' },
    Corki: { corki_thepackage: 'R' },
    Elise: { elisespiderq: 'Q', elisespiderw: 'W', elisespidere: 'E' },
    Graves: { graveshighnoon: 'R' },
    Hwei: { hweiwashbrush: 'W' },
    Katarina: { katarina_daggerspin: 'P' },
    Lillia: { lillia_icon_buff_speedup: 'Q' },
    Riven: { rivenwindscar: 'R' },
    Rumble: { rumble_junkyardtitan3: 'P' },
    TwistedFate: { cardmaster_gold: 'W', cardmaster_premonition: 'R' },
    Twitch: { twitch_deadlyvenom: 'P' },
    Vladimir: { vladimir_tidesofblood: 'E' },
    Zyra: { zyrapq: 'P' },
};

function findExtraIcons(bin, alias, binSpells) {
    const low = alias.toLowerCase();
    const rec = bin[`Characters/${alias}/CharacterRecords/Root`];
    if (!rec) return {};
    const fileOf = (o) => {
        const m = JSON.stringify(o).match(/"(ASSETS\/[^"]*Icons2D\/[^"]+\.dds)"/i);
        return m ? m[1] : null;
    };
    // 한 객체에 아이콘이 여러 개 박혀 있을 수 있다 (본체의 [기본, 강화] 배열)
    const filesOf = (o) => [...JSON.stringify(o).matchAll(/"(ASSETS\/[^"]*Icons2D\/[^"]+\.dds)"/gi)].map(m => m[1]);
    //   ★ 일부 챔피언은 파일 이름 앞에 icon_/icons_ 가 붙는다
    //     (icons_ksante_q2, icon_ambessa_q2, icons_smolder_q2).
    //     이걸 안 벗기면 "챔피언 이름으로 시작" 조건에 걸려 통째로 빠진다.
    //     URL 은 원래 경로를 쓰므로 여기서 벗겨도 안전하다.
    const nameOf = (f) => f.split('/').pop().toLowerCase().replace(/\.dds$/, '').replace(/^icons?_/, '');

    const main = (rec.spells || []).slice(0, 4);
    const passive = rec.mCharacterPassiveSpell;
    const SLOTS = ['Q', 'W', 'E', 'R'];

    // 기본 아이콘 = 본체 스펠·패시브가 들고 있는 **첫 번째** 아이콘.
    //   배열이면 [기본, 강화, ...] 순이라 0번만 기본이고 나머지는 전부 하위 아이콘이다.
    const baseIcon = {};
    const baseNames = [];
    [...main, passive].forEach((s, i) => {
        if (!s || !bin[s]) return;
        const f = fileOf(bin[s]);
        if (!f) return;
        const n = nameOf(f);
        baseNames.push(n);
        baseIcon[i < 4 ? SLOTS[i] : 'P'] = n;
    });
    const isBase = new Set(baseNames);

    // 두 번째 폼은 아래 박스로 따로 나가므로 아이콘 줄에서 뺀다.
    const f2 = FORM2[alias] || {};
    const f2Objs = new Set(SLOTS.map(k => f2[k]).filter(Boolean));
    const f2Icons = new Set([...f2Objs].map(n => {
        const o = findSpellObj(bin, alias, n);
        const f = o ? fileOf(o) : null;
        return f ? nameOf(f) : null;
    }).filter(Boolean));

    // 받아들일 파일명 접두사.
    //   ★ 챔피언 alias 와 파일명이 다른 경우가 많다 — 워윅 wolfman_, 제드 shadowninja_,
    //     샤코 jester_, 트페 cardmaster_, 벨베스 bv_, 나르 메가폼 gnarbig_.
    //     그래서 alias 만 보면 안 되고 **기본 아이콘의 접두사**도 같이 받는다.
    //     (구버전 아이콘인 wolfman_/shadowninja_ 는 기본 아이콘이 warwick_/zed_ 라서
    //      이 규칙만으로 자동으로 걸러진다 — 의도한 결과다)
    const prefixes = new Set([low]);
    baseNames.forEach(n => {
        const m = n.match(/^([a-z]{3,}?)(?=[_qwerp0-9]|$)/);
        if (m) prefixes.add(m[1]);
    });
    Object.keys(ICON_SLOT[alias] || {}).forEach(f => prefixes.add(f));
    const mine = (f) => [...prefixes].some(p => f.startsWith(p));

    const out = {};
    const seen = new Set();
    const force = ICON_FORCE_SLOT[alias] || {};
    const push = (slot0, file, path) => {
        const slot = force[file] || slot0;   // 강제 지정이 자동 판별을 이긴다
        if (!slot || seen.has(file)) return;
        if (isBase.has(file) || f2Icons.has(file)) return;
        if (file === baseIcon[slot]) return;
        seen.add(file);
        (out[slot] = out[slot] || []).push({ file, url: ICON_BASE + path.toLowerCase().replace(/\.dds$/, '.png') });
    };
    const usable = (file) =>
        mine(file) && !ICON_NOISE.test(file) && !ICON_NOISE2.test(file)
        && !ICON_OLD.has(file) && !ICON_DUP.has(file) && !ICON_REJECT.has(file);

    // ① 본체·패시브의 아이콘 배열 — **인덱스가 곧 슬롯**이라 이름을 안 봐도 된다.
    //    옛 방식이 통째로 놓치던 부류다 (그웬 R2·R3, 신드라 전 슬롯, 사이온 R2).
    [...main, passive].forEach((s, i) => {
        if (!s || !bin[s]) return;
        const slot = i < 4 ? SLOTS[i] : 'P';
        [...new Set(filesOf(bin[s]))].slice(1).forEach(p => {
            const file = nameOf(p);
            if (usable(file)) push(slot, file, p);
        });
    });

    // ② 별도 스펠 객체 — 슬롯을 이름에서 캐낸다.
    for (const k of Object.keys(bin)) {
        if (!k.startsWith(`Characters/${alias}/Spells/`) || !bin[k] || !bin[k].mSpell) continue;
        if (f2Objs.has(k.split('/').pop())) continue;
        const objName = k.split('/').pop();
        for (const p of [...new Set(filesOf(bin[k]))]) {
            const file = nameOf(p);
            if (seen.has(file) || isBase.has(file) || !usable(file)) continue;
            // 파일명에서 접두사를 떼고 남은 첫 글자가 슬롯이다.
            //   ★ 뒤에 영문이 이어지면 안 된다 — rivenwindscar 의 w, gragasexplosivecask 의 e 를
            //     슬롯으로 오인한다. 그래서 (?![a-z]) 를 붙인다.
            let slot = null;
            const pre = [...prefixes].filter(x => file.startsWith(x)).sort((a, b) => b.length - a.length)[0] || '';
            let m = file.slice(pre.length).replace(/^_/, '').match(/^([qwerp])(?![a-z])/);
            if (m) slot = m[1].toUpperCase();
            // 파일명으로 안 되면 스펠 객체 이름을 본다 (RengarWEmp -> W)
            if (!slot) {
                m = objName.replace(new RegExp('^' + alias, 'i'), '').match(/^_?([QWERP])(?![a-z])/);
                if (m) slot = m[1].toUpperCase();
            }
            // 그래도 안 되면 손으로 적어 둔 표
            if (!slot) slot = (ICON_SLOT[alias] || {})[file] || null;
            if (slot) push(slot, file, p);
        }
    }
    // 손으로 적어 둔 보완분
    const ex = EXTRA_ICONS[alias];
    if (ex) {
        for (const slot of Object.keys(ex)) {
            // ★ 중복 검사는 **슬롯 안에서만** 한다 (2026-08-10).
            //   예전엔 챔피언 전체로 봐서, 아펠리오스 P 와 Q 가 같은 무기 아이콘을
            //   쓰는데 **Q 쪽이 통째로 비어 버렸다.** 슬롯이 다르면 같은 그림을 써도 된다.
            const here = new Set((out[slot] || []).map(x => x.file));
            for (const file of ex[slot]) {
                if (here.has(file)) continue;
                here.add(file);
                seen.add(file);
                (out[slot] = out[slot] || []).push({
                    file,
                    url: `${ICON_BASE}assets/characters/${low}/hud/icons2d/${file}.png`,
                });
            }
        }
    }
    // 파일 이름 순으로 정렬해야 q2 -> q3 순서가 된다.
    //   ★ ICON_ORDER 에 적힌 슬롯은 그 순서를 먼저 따른다 (르블랑 R = Q·W·E 순).
    const ord = ICON_ORDER[alias] || {};
    for (const slot of Object.keys(out)) {
        out[slot].sort((a, b) => a.file.localeCompare(b.file));
        const want = ord[slot];
        if (!want) continue;
        const rank = (f) => { const i = want.indexOf(f); return i < 0 ? want.length : i; };
        out[slot].sort((a, b) => rank(a.file) - rank(b.file));
    }
    return out;
}

// 스펠 객체에 박혀 있는 아이콘 경로(ASSETS/....dds)를 CD 의 png URL 로 바꾼다.
const iconUrlOf = (obj) => {
    const m = JSON.stringify(obj).match(/"(ASSETS\/[^"]*Icons2D\/[^"]+\.dds)"/i);
    return m
        ? 'https://raw.communitydragon.org/latest/game/' + m[1].toLowerCase().replace(/\.dds$/, '.png')
        : null;
};

// 랭크 수가 표준(일반 5 / 궁 3)에서 벗어나는 챔피언.
//   ★ 자동 판별할 근거가 없다. bin 의 mSpell 엔 최대 레벨 필드가 없고,
//     CD v1 의 cooldownCoefficients / costCoefficients 는 챔피언과 무관하게
//     전부 길이 6 으로 패딩돼 있어서 랭크 수를 알려주지 않는다. 그래서 표로 박는다.
//   규칙: 1레벨에 스킬을 공짜로 주는 챔피언은 남는 포인트만큼 다른 스킬이 더 올라간다.
const MAX_RANK = {
    Udyr:    { Q: 6, W: 6, E: 6, R: 6 },   // 궁이 따로 없어 4개 전부 6랭크 (그래서 마스터 불가)
    Jayce:   { Q: 6, W: 6, E: 6, R: 1 },   // 1레벨에 R(변신) 공짜
    Nidalee: { Q: 5, W: 5, E: 5, R: 4 },   // 1레벨에 R 공짜
    Karma:   { Q: 5, W: 5, E: 5, R: 4 },   // 〃
    Elise:   { Q: 5, W: 5, E: 5, R: 4 },   // 〃
    Yuumi:   { Q: 6, W: 5, E: 5, R: 3 },   // 1레벨에 W 공짜라 Q 만 6랭크
};
// ★ Data Dragon 과 CommunityDragon 의 챔피언 ID 철자가 다른 경우.
//   app.js 는 **DD 의 `champ.id`** 로 customValues / customTemplates 를 조회한다.
//   그래서 파일에 적히는 키는 반드시 DD 철자여야 한다. CD 철자로 나가면 조회가
//   통째로 실패해서 **문장·수치를 다 버리고 DD 툴팁으로 폴백**한다
//   (피들스틱이 이 경우였다 — 화면에 `[스탯 비례]` 같은 미해결 토큰이 찍혔다).
//   ★ bin 경로·스펠 조회는 CD 철자(alias)를 그대로 써야 하므로 **출력 키에만** 적용할 것.
//   ★ `build_champion_data.js` 에도 같은 표가 있다. 어긋나면 템플릿과 값의 키가 갈린다.
const DD_ID = { FiddleSticks: 'Fiddlesticks' };
const ddId = (alias) => DD_ID[alias] || alias;

const rankOf = (alias, key) =>
    (MAX_RANK[alias] && MAX_RANK[alias][key]) || (key === 'R' ? 3 : 5);

// 라이엇 bin 프로퍼티 해시 (FNV-1a 32bit, 소문자 기준).
//   ★ CD 가 이름을 못 푼 자리는 {8자리16진수} 로 남는다. 툴팁이 부르는 이름을
//     이 해시로 바꾸면 그 자리를 찾아갈 수 있다.
//     검증: fnv1a('firsttierrangeincreasett') = d34fc902 이고
//     킨드레드 패시브의 계산식 키가 정확히 {d34fc902} 다.
const fnv1a = (s) => {
    let h = 0x811c9dc5;
    for (let i = 0; i < s.length; i++) { h ^= s.charCodeAt(i); h = Math.imul(h, 0x01000193) >>> 0; }
    return (h >>> 0).toString(16).padStart(8, '0');
};
const hashKey = (name) => `{${fnv1a(String(name).toLowerCase())}}`;

// 조각 안 필드 이름을 해시로만 남긴 자리가 있다. 위 FNV 로 전부 되찾았다:
//   {91d404a5} Level1DataValue                     {bbd778a2} InitialBonusPerLevelDataValue
//   {9823b29a} DataValueBreakpoints                {ae9b464d} AdditionalBonusAtThisLevelDataValue
//   {b0d8b2ac} BonusPerLevelAtAndAfterDataValue    {b2cd0eb0} BonusPerLevelDataValue
// 전부 ByCharLevelBreakpoints 의 "값 대신 DataValue 이름을 적는" 변종이다.
// 해시로도 원래 이름으로도 찾을 수 있게 해 둔다 (CD 가 나중에 풀어줄 수 있으므로).
const fld = (obj, name) => {
    if (!obj || typeof obj !== 'object') return undefined;
    const h = obj[hashKey(name)];
    return h !== undefined ? h : obj[name];
};

function findDataValue(spell, name) {
    if (name === undefined || name === null) return undefined;
    const list = spell.DataValues || [];
    const exact = list.find(d => d.name === name);
    if (exact) return exact;
    const hashed = list.find(d => d.name === hashKey(name));
    if (hashed) { hashHits.add(`${ctx}   (${name} -> ${hashKey(name)})`); return hashed; }
    const want = String(name).toLowerCase();
    const loose = list.find(d => String(d.name).toLowerCase() === want);
    if (loose) noteCase(name, loose.name);
    return loose;
}

// mSpellCalculations 항목을 이름으로 찾는다. 같은 이유로 대소문자 폴백을 둔다.
function findCalc(spell, name) {
    if (name === undefined || name === null) return undefined;
    const calcs = spell.mSpellCalculations || {};
    if (calcs[name] !== undefined) return calcs[name];
    if (calcs[hashKey(name)] !== undefined) {
        hashHits.add(`${ctx}   (${name} -> ${hashKey(name)})`);
        return calcs[hashKey(name)];
    }
    const want = String(name).toLowerCase();
    const key = Object.keys(calcs).find(k => k.toLowerCase() === want);
    if (key) { noteCase(name, key); return calcs[key]; }
    return undefined;
}

// 지금 어느 챔피언 어느 스킬 어느 자리를 처리 중인지. 미해결 원인 추적용.
let ctx = '';

// 지금 스킬의 쿨타임 (CD v1 기준 문자열, 예: "18 / 16 / 14 / 12 / 10").
// CooldownMultiplierCalculationPart 가 이 값을 쓴다.
// bin 의 cooldownTime 대신 v1 값을 쓰는 이유는 아래 case 주석 참고.
let currentCooldown = null;

// ------------------------------------------------------------

const sleep = (ms) => new Promise(r => setTimeout(r, ms));
const get = async (url) => {
    const res = await fetch(url);
    if (!res.ok) throw new Error(String(res.status));
    return res.json();
};

// 소수점 지저분한 것 정리 (0.30000000000000004 -> 0.3)
// ★ `tidy` 는 소수 3자리로만 다듬고 **반올림은 하지 않는다.**
//   전역 반올림을 넣었다가 되돌렸다 (2026-08-14). 아래 ROUND_BASE_DAMAGE 주석 참고.
const tidy = (n) => {
    let r = Math.round(n * 1000) / 1000;
    // ★ 레벨 보간이 만드는 float 꼬리를 정수로 스냅한다 (2026-08-24).
    //   그브 P `100.005`·브랜드 P `39.983`·쉔 P `7.995`·신 짜오 W `100.002` 가 화면에 그대로
    //   나가고 있었다 — 위키 원문은 넷 다 정수(100·40·8·100)다.
    //   ★ 조건 둘 다 필요하다: |차이| < 0.018 (0.98 같은 진짜 값은 지킴) · r >= 1 (0.005 같은
    //     소수가 0 으로 뭉개지는 것 방지). 현재 파일 전수에서 이 넷만 걸리는 것 확인함.
    if (r >= 1 && Math.abs(r - Math.round(r)) > 0 && Math.abs(r - Math.round(r)) < 0.018) r = Math.round(r);
    return String(r);
};

// 레벨별 값 배열 -> 표시 문자열.
//   bin 의 values 는 7칸이고 0번은 쓰레기다. 실제 랭크는 1번부터.
const levelsToText = (values, maxRank, mult) => {
    if (!Array.isArray(values)) return null;
    const picked = values.slice(1, 1 + maxRank).map(v => tidy(v * mult));
    if (!picked.length) return null;
    return picked.every(v => v === picked[0]) ? picked[0] : picked.join(' / ');
};

// 최상위 / mAmmo 필드용 배열 -> 표시 문자열.
//   ★ DataValues 와 달리 이쪽은 0번이 실제 1랭크다 (castRange, mana 와 같은 규칙).
const topLevelToText = (values, maxRank, mult) => {
    if (!Array.isArray(values) || !values.length) return null;
    if (typeof values[0] !== 'number') return null;
    const picked = values.slice(0, maxRank).map(v => tidy(v * mult));
    if (!picked.length) return null;
    return picked.every(v => v === picked[0]) ? picked[0] : picked.join(' / ');
};

// bin 의 필드 이름은 툴팁에 적힌 이름과 정확히 같지 않을 수 있다.
//   AmmoRechargeTime -> mAmmoRechargeTime 처럼 m 접두사가 붙거나 대소문자가 다르다.
//   그래서 (그대로 / m 붙여서 / 대소문자 무시) 순서로 찾는다.
function findField(obj, name) {
    if (!obj || typeof obj !== 'object' || Array.isArray(obj)) return undefined;
    if (obj[name] !== undefined) return obj[name];
    if (obj['m' + name] !== undefined) return obj['m' + name];
    const want = name.toLowerCase();
    for (const k in obj) {
        const kl = k.toLowerCase();
        if (kl === want || kl === 'm' + want) return obj[k];
    }
    return undefined;
}

// 필드에서 읽은 raw 값 -> 표시 문자열. 숫자도 배열도 아니면 실패로 본다.
//   rank1 = 0번이 "스킬 안 찍은 상태"라서 1번부터 읽어야 하는 필드인지.
const fieldToText = (raw, maxRank, mult, rank1) => {
    // ★ 최상위 필드가 배열이 아니라 { values: [...] } 로 한 겹 싸여 있는 경우가 있다.
    //   직스 P 가 부르는 Cooldown 이 이 모양이라 객체인 채로 그냥 실패하고 있었다.
    if (raw && typeof raw === 'object' && !Array.isArray(raw) && Array.isArray(raw.values)) raw = raw.values;
    if (typeof raw === 'number') return tidy(raw * mult);
    if (!Array.isArray(raw)) return null;
    // ★ 버릴 앞칸이 실제로 있을 때만 민다. 길이가 랭크 수와 같으면
    //   앞을 버리는 순간 마지막 랭크가 잘리므로 그냥 0번부터 읽는다.
    if (rank1 && raw.length > maxRank) return levelsToText(raw, maxRank, mult);
    return topLevelToText(raw, maxRank, mult);
};

// 위치는 스펠 최상위여도 인덱스 규칙은 DataValues 쪽(0번은 쓰레기)을 따르는 필드.
//   ★ 티모 R AmmoRechargeTime 이 "0 / 35 / 30" 으로 나온 게 증거다.
//     바이·자이라도 첫 값이 겹쳤다. 인게임에서 한 칸 민 쪽이 맞다고 확인함.
//     castRange, mana 는 0번부터가 맞으므로 여기 넣지 말 것.
const RANK1_FIELDS = /ammo/i;

// 툴팁이 부르는 이름 != bin 필드 이름인 자리. 소문자 키로 적는다.
//   Cost 는 스펠 안에 값이 없고 "이 스킬의 마나 소모값"을 가리키는 별명이다.
//   (카시오페아 Q: 처치하면 Q 코스트만큼 마나를 돌려받는다)
const FIELD_ALIAS = { cost: 'mana' };

// ".숫자" 꼬리를 소수점 자릿수로 적용한다.
const applyPrecision = (text, p) => {
    if (text === null || p === null) return text;
    if (!DOT_AS_PRECISION) return text;
    const out = String(text).replace(/-?\d+(?:\.\d+)?/g, (n) => {
        const f = parseFloat(n);
        return Number.isFinite(f) ? String(Number(f.toFixed(p))) : n;
    });
    if (dotList.length < 40) dotList.push(`${ctx} = ${out}   [.${p} -> 소수점 ${p}자리로 처리]`);
    return out;
};

// mStatFormula: 없음/0 = 총, 1 = 기본, 2 = 추가.
//   ★ 아트록스 Q(필드 없음)가 "총 공격력", 카타리나 R(2)이 "추가 공격력"·
//     "추가 공격 속도"인 것으로 확인했다. 지금까지 이 필드를 안 봐서
//     추가 계수를 총 계수로 잘못 적고 있었다.
const applyStatFormula = (name, formula) => {
    if (name === null) return null;
    // ★ 주문력에는 기본/추가 구분이 없다 (2026-08-12 위키 대조).
    //   챔피언 기본 스탯에 주문력이 아예 없어서 **총 AP = 추가 AP** 다. 그래서
    //   라이엇 툴팁도 lolwiki 도 나무위키도 전부 그냥 "주문력" 이라고 쓴다.
    //   bin 이 `mStatFormula: 2` 를 적어 둔 4자리(벨베스 W, 자크 E x2, 자크 R)가
    //   `추가 주문력의 150%` 로 나가고 있었다 — 값은 맞지만 롤에 없는 개념이라 헷갈린다.
    //   ★ 다른 스탯은 기본/추가 구분이 실제로 있으므로 여기만 예외다.
    if (name === '주문력') return name;
    if (formula === 1) return name.startsWith('기본 ') ? name : `기본 ${name.replace(/^(총|추가) /, '')}`;
    if (formula === 2) return name.startsWith('추가 ') ? name : `추가 ${name.replace(/^(총|기본) /, '')}`;
    return name;
};

// 순수 숫자(또는 랭크별 숫자 배열) 두 개를 곱해서 하나로 접는다.
//   접을 수 없으면 null 을 돌려 호출부가 "a x b" 를 그대로 쓰게 한다.
//   "1 / 1 / 1" 처럼 전부 같으면 한 칸으로 줄인다 (levelsToText 와 같은 규칙).
const foldProduct = (a, b) => {
    // x 1 은 아무 의미가 없다. 반대쪽을 그대로 돌려준다.
    //   (타릭 P "25 ~ 93 (레벨에 따라) x 1" 처럼 한쪽이 숫자가 아니어도 지울 수 있다)
    if (String(a).trim() === '1') return String(b);
    if (String(b).trim() === '1') return String(a);
    const parse = (t) => {
        const s = String(t).trim();
        if (!/^-?[\d.]+(\s*\/\s*-?[\d.]+)*$/.test(s)) return null;
        const arr = s.split('/').map(x => parseFloat(x));
        return arr.every(n => Number.isFinite(n)) ? arr : null;
    };
    const A = parse(a), B = parse(b);
    if (!A || !B) return null;
    if (A.length !== B.length && A.length !== 1 && B.length !== 1) return null;
    const n = Math.max(A.length, B.length);
    const out = [];
    for (let i = 0; i < n; i++) out.push(tidy(A[A.length === 1 ? 0 : i] * B[B.length === 1 ? 0 : i]));
    return out.every(x => x === out[0]) ? String(out[0]) : out.join(' / ');
};

// ------------------------------------------------------------
//  "A x M" 을 사람이 읽는 문장으로 바꾼다 (2026-08-12).
//
//  foldProduct / foldRankMultiplier 가 숫자 하나로 못 접은 배율이 화면에 그대로 찍히는데,
//  `x 1 + 0.3 x (치명타 피해량의 100% - 1)` 처럼 **무엇에 무엇이 곱해지는지 안 보인다.**
//
//  두 가지로 나눈다:
//   1. 배율이 숫자(랭크별 배열·범위 포함)면 → `A의 N%` 로 적는다. 라이엇 툴팁과 같은 어법이다
//      (모데카이저 Q `x 1.3 / 1.35 / ...` -> `의 130 / 135 / ...%`)
//   2. 그 외(스탯이 섞인 식)는 → 괄호로 묶어 `A x (M)` 으로만 만든다.
//      뜻을 바꾸진 못해도 **어디까지가 배율인지는 보이게** 한다
//
//  ★ 1 미만 배율은 `의 N%` 로 안 바꾼다. `A의 3.7%` 처럼 "깎는 값" 으로 읽혀야 하는데
//    원래 뜻이 그런지 자리마다 달라서 함부로 못 바꾼다 (블라디미르 P 가 그 경우).
// ------------------------------------------------------------
// ★ 게임의 **기본 치명타 피해량**. 배율 식에 든 `치명타 피해량의 100%` 를 숫자로 접는 데 쓴다.
//
//   bin 의 배율은 `1 + k x (치명타 피해량 - 1)` 꼴이라 그대로 찍으면 계산식이 화면에 남는다.
//   200% 는 나무위키 3자리로 교차 확인했다 (2026-08-12):
//     가렌 E   k=0.3 -> x1.3  (무한의 대검 x1.39)
//     미포 Q   k=0.5 -> x1.5  (무한의 대검 x1.65)
//     미포 R   k=0.3 -> x1.3  (무한의 대검 x1.39)
//   1 + k(C-1) 에 C=2.0 을 넣으면 셋 다 정확히 맞는다. C=1.75 면 하나도 안 맞는다.
//
//   ★ 라이엇이 기본 치명타 피해량을 바꾸면 **여기 한 줄만** 고치면 된다.
//     `추가 치명타 피해량`(아이템으로 얻는 초과분)은 일부러 안 접는다 — 사람마다 다르고,
//     문장에 남아 있는 편이 정보가 된다 (탈론 Q·케넨 W).
let CRIT_DAMAGE = 2.0;

// 무한의 대검(아이템 3031)이 올려 주는 치명타 피해량. 라이엇 아이템 데이터에서 확인했다
//   ("공격력 75 / 치명타 확률 25% / **치명타 피해량 30%**", 2026-08-12).
//   그래서 무한의 대검을 들면 치명타 피해량이 200% -> 230% 가 되고,
//   나무위키의 "무한의 대검 구비 시" 값과 정확히 맞는다 (가렌 E 139%, 미포 Q 165%).
const IE_CRIT_DAMAGE = 0.3;

// 이번 값을 만들면서 치명타 배율을 접었는가. 접었으면 무한의 대검 판본을 한 번 더 만들어 본다.
let critTouched = false;

// CRIT_DAMAGE 를 바꿔 값을 한 번 더 계산한다.
//   ★ 검산용 로그가 두 번 쌓이면 안 된다 — 미리보기 출력이 통째로 두 배가 되고
//     "이 자리를 어디서 건졌나" 목록이 거짓이 된다. 길이를 기억했다가 되돌린다.
function withCritDamage(c, fn) {
    const LOGS = [guessedList, viaField, dotList, caseList, zeroDrop, crossMiss];
    const SETS = [manualUsed, caseSeen, hashHits, crossHits];
    const lens = LOGS.map(a => a.length);
    const snaps = SETS.map(s => new Set(s));
    const prevC = CRIT_DAMAGE, prevT = critTouched;
    CRIT_DAMAGE = c;
    let out = null;
    try { out = fn(); } catch { out = null; }
    CRIT_DAMAGE = prevC;
    critTouched = prevT;
    LOGS.forEach((a, i) => { a.length = lens[i]; });
    SETS.forEach((s, i) => { s.clear(); snaps[i].forEach(x => s.add(x)); });
    return out;
}

// "무한의 대검 보유 시 …" 각주. 값이 실제로 달라질 때만 만든다.
//   ★ 홑따옴표만 쓴다. custom_values.js 는 값이 큰따옴표 문자열이라 " 가 들어가면
//     이스케이프되고, add_level_graphs.js 의 줄 파싱 정규식이 통째로 깨진다.
function critFootnote(no, base, ie) {
    // 꼬리의 "…의 N%" 만 달라지는 게 보통이다. 그러면 그 부분만 보여 준다
    const mB = String(base).match(/^(.*)의 ([\d./~ ]+)%$/);
    const mI = String(ie).match(/^(.*)의 ([\d./~ ]+)%$/);
    let shown = String(ie);
    if (mI && mB && mB[1] === mI[1]) shown = `${mI[2]}%`;          // 꼬리 배율만 다른 경우
    else if (mI && mI[1] === String(base)) shown = `${mI[2]}%`;    // 기본값엔 배율이 없던 경우
    return `<span class='custom-footnote'>[${no}]` +
        `<span class='custom-footnote-content'>` +
        `<div style='font-size:11px; color:#fff; margin-bottom:5px; white-space:nowrap;'>무한의 대검 보유 시</div>` +
        `<div style='font-size:12px; color:#ff9900; font-weight:bold;'>${shown}</div>` +
        `<div style='font-size:10px; color:#9aa4af; margin-top:5px; white-space:nowrap;'>치명타 피해량 200% → 230%</div>` +
        `</span></span>`;
}

// 배율 식을 숫자 하나로 계산해 본다. 못 하면 null.
//   `치명타 피해량의 N%` 만 상수로 바꿔 넣고, 남은 게 순수 사칙연산이면 계산한다.
const evalMultiplier = (expr) => {
    const src = String(expr);
    // ★ `/` 는 나눗셈이 아니라 **랭크 구분자**다. 하나라도 있으면 손대면 안 된다.
    //   `1 + 0.3 / 0.375 / 0.45 / 0.525 / 0.6`(람머스 W)를 나눗셈으로 계산해서
    //   `664.4%` 라는 엉터리가 나왔다. `6 / 7 / 8 / 9 / 10 - 1`(멜 Q)도 같은 사고였다.
    if (src.includes('/')) return null;
    // 치명타 피해량이 든 식만 손댄다. 다른 배율까지 접으면 영향 범위를 못 가늠한다.
    if (!src.includes('치명타 피해량')) return null;
    // `추가 치명타 피해량`(아이템으로 얻는 초과분)은 사람마다 달라서 상수로 못 바꾼다
    if (src.includes('추가 치명타 피해량')) return null;
    let s = src.replace(/치명타 피해량의 (-?[\d.]+)%/g,
        (m0, n) => `(${parseFloat(n) / 100}*${CRIT_DAMAGE})`);
    if (/[가-힣]/.test(s)) return null;             // 다른 스탯이 남아 있으면 못 접는다
    if (!/^[\d.+\-*()\sx]+$/.test(s)) return null;
    s = s.replace(/\sx\s/g, '*');
    let v;
    try { v = Function(`"use strict";return (${s});`)(); } catch { return null; }
    if (!Number.isFinite(v)) return null;
    critTouched = true;      // 무한의 대검 판본을 만들어 볼 자리라는 표시
    return String(tidy(v));
};

const NUMLIST = /^-?[\d.]+(\s*[~/]\s*-?[\d.]+)*$/;
// 괄호 짝이 맞는지. "(A) x (B)" 처럼 겉이 괄호로 시작·끝나도 한 덩어리가 아닐 수 있다.
const balanced = (s) => {
    let d = 0;
    for (const ch of s) {
        if (ch === '(') d++;
        else if (ch === ')' && --d < 0) return false;
    }
    return d === 0;
};
const timesText = (a, m) => {
    const mt = String(m).trim();
    // 뒤에 붙은 "(레벨에 따라)" 같은 꼬리는 떼고 숫자만 본다
    const tail = mt.match(/\s*\([^()]*\)$/);
    const head = (tail ? mt.slice(0, tail.index) : mt).trim();
    const suffix = tail ? tail[0] : '';

    // 치명타 배율처럼 상수만 넣으면 숫자가 되는 식은 먼저 접는다 (CRIT_DAMAGE 주석 참고)
    if (!NUMLIST.test(head)) {
        const ev = evalMultiplier(head);
        if (ev !== null) return timesText(a, ev + suffix);
    }

    if (NUMLIST.test(head)) {
        const nums = head.split(/[~/]/).map(x => parseFloat(x));
        // x 1 은 의미가 없다. 꼬리만 남긴다 (벨베스 P "x 1 (중첩당)")
        if (nums.every(n => n === 1)) return suffix ? `${a}${suffix}` : a;

        // ★ 양쪽이 순수 숫자면 그냥 곱한다. `의 N%` 로 적으면 안 된다 —
        //   `0.3 x 1.3` 이 `0.3의 130%` 가 되어 뒤쪽 계산이 통째로 어긋난다.
        //   기본값(치명타 피해량 200%)에서는 배율이 딱 1 이라 위 줄에 걸려 안 보였고,
        //   무한의 대검 판본(230%)을 만들면서 드러났다 (2026-08-12).
        const fp = foldProduct(a, head);
        if (fp !== null) return suffix ? `${fp}${suffix}` : fp;

        if (nums.length === 1 && !suffix) {
            // ① 왼쪽이 "스탯의 N%" 하나면 **계수를 직접 곱한다.**
            //    `주문력의 5% x 5` 를 `주문력의 5%의 500%` 로 적으면 오히려 더 안 읽힌다.
            //    그웬 Q(짤깍 5회)가 이 경우다 -> `주문력의 25%`
            const st = a.match(/^(.*의)\s*(-?[\d.]+(?:\s*\/\s*-?[\d.]+)*)%$/);
            if (st) return `${st[1]} ${st[2].replace(/-?[\d.]+/g, x => String(tidy(parseFloat(x) * nums[0])))}%`;

            // ② 왼쪽이 이미 "... x N" 으로 끝나면 배율끼리 먼저 접는다 (자크 R 의 이중 곱).
            //    바깥 괄호 한 겹은 벗기고 본다 — 앞 단계의 wrap() 이 씌워 둔 것이라
            //    `((A) x 0.5) x 3` 처럼 괄호로 끝나 정규식이 안 걸린다.
            const bare = /^\(.*\)$/.test(a) && balanced(a.slice(1, -1)) ? a.slice(1, -1) : a;
            const tm = bare.match(/^(.*)\sx\s(-?[\d.]+)$/);
            if (tm) {
                const k = tidy(parseFloat(tm[2]) * nums[0]);
                return timesText(tm[1], String(k));
            }
        }

        // ★ 범위 배율(`1 ~ 2`)은 양 끝을 각각 곱해 범위로 펼친다 (2026-08-12).
        //   `55 / 60 / 65 / 70 / 75%의 100 ~ 200%` 는 곱셈이 두 겹이라 읽히지 않는다.
        //   카이사 E 이동 속도가 이 자리다. 펼치면
        //   `55 / 60 / 65 / 70 / 75% ~ 110 / 120 / 130 / 140 / 150%` — 롤위키 표기와 같다.
        //   왼쪽이 순수 숫자 리스트일 때만 한다. 계수가 섞이면(`10 (+ 주문력의 50%)`)
        //   양 끝을 곱할 수 없어 아래 `의 N%` 로 물러난다.
        if (nums.length === 2 && /~/.test(head) && !suffix) {
            const lm = a.match(/^(-?[\d.]+(?:\s*\/\s*-?[\d.]+)*)(%?)$/);
            if (lm) {
                const scale = (k) => lm[1].replace(/-?[\d.]+/g, x => String(tidy(parseFloat(x) * k)));
                const lo = scale(nums[0]), hi = scale(nums[1]);
                if (lo !== hi) return `${lo}${lm[2]} ~ ${hi}${lm[2]}`;
            }
        }

        if (nums.every(n => n >= 1)) {
            const pct = head.replace(/-?[\d.]+/g, (x) => String(tidy(parseFloat(x) * 100)));
            return `${a}의 ${pct}%${suffix}`;
        }
    }
    // ★ `A x 스탯의 N%` 는 계수를 스탯 쪽으로 접는다 (2026-08-12).
    //   규칙 ① 의 좌우가 뒤집힌 꼴이다. 아이번 P 가 유일한 사례:
    //     `15 ~ 0.006 (레벨에 따라) x 기본 최대 체력의 1%`
    //       -> `기본 최대 체력의 15 ~ 0.006% (레벨에 따라)`
    //   계산은 원래부터 맞았고 표기만 깨져 있었다. 롤위키와 대조하면
    //   레벨1  630 x 15%    = 94.5  (위키 94.5)
    //   레벨18 2300 x 0.006% = 0.14  (위키 0.14) 로 양 끝이 맞는다.
    //   ★ 배율이 괄호식(`x (1.5 + ...)`)이면 안 걸린다 — 볼리베어 W·카타리나 R 등
    //     19자리는 이 규칙 밖이고 성격이 달라 그대로 둔다.
    //   ★ 배율 자리는 **스탯 이름 하나**여야 한다. `.+의` 로 느슨하게 잡으면
    //     `1 + 추가 공격 속도의 100%` 에서 `1 + 추가 공격 속도의` 까지 삼켜
    //     계수만 곱해진 엉터리가 나온다 (2026-08-12에 11자리를 이렇게 망가뜨렸다).
    //     한글로 시작해 한글·공백만 있는 이름으로 좁힌다.
    {
        const sm = mt.match(/^([가-힣][가-힣 ]*의)\s*(-?[\d.]+)%$/);
        if (sm) {
            const at = a.match(/\s*\([^()]*\)$/);
            const ah = (at ? a.slice(0, at.index) : a).trim();
            if (NUMLIST.test(ah)) {
                const k = parseFloat(sm[2]);
                const folded = ah.replace(/-?[\d.]+/g, x => String(tidy(parseFloat(x) * k)));
                return `${sm[1]} ${folded}%${at ? at[0] : ''}`;
            }
        }
    }

    // 덧셈·뺄셈이 든 배율은 괄호로 묶어야 어디까지가 배율인지 보인다
    return / [+-] /.test(mt) ? `${a} x (${mt})` : `${a} x ${mt}`;
};

// 랭크마다 다른 배율(미포 R "x 14 / 16 / 18", 흐웨이 Q "x 2 / 2.375 / ...")을 계산식 안으로 접는다.
//   랭크 i 는 배율 k_i 를 스칼라로 걸어 계산식을 돌린 뒤 그 결과에서 i번째 랭크 칸만 뽑는다(대각선).
//   그렇게 만든 랭크별 문자열 R개를 다시 합친다. 뼈대가 어긋나면 null 을 돌려 예전 "x ..." 표기를 남긴다.
//   ★ "~" 가 든 결과는 건너뛴다. 모데카이저 Q 의 "0 ~ 45 (레벨에 따라)" 처럼 범위 양 끝이
//     랭크마다 갈리면 "0 ~ 58.5 / 60.75 / ..." 라는 읽을 수 없는 표기가 되기 때문이다.
const foldRankMultiplier = (renderScaled, ranks) => {
    const NUM = /-?\d+(?:\.\d+)?/g;
    const GROUP = /-?\d+(?:\.\d+)?(?:\s*\/\s*-?\d+(?:\.\d+)?)+/g;
    const pickRank = (text, i) => String(text).replace(GROUP, (g) => {
        const parts = g.split("/").map(s => s.trim());
        return parts[Math.min(i, parts.length - 1)];
    });
    const rows = [];
    for (let i = 0; i < ranks.length; i++) {
        const raw = renderScaled(ranks[i]);
        if (raw === null || raw === undefined) return null;
        if (String(raw).indexOf("~") !== -1) return null;
        rows.push(pickRank(raw, i));
    }
    const skel = rows.map(r => r.replace(NUM, "#"));
    if (!skel.every(s => s === skel[0])) return null;
    const nums = rows.map(r => r.match(NUM) || []);
    if (!nums.every(n => n.length === nums[0].length)) return null;
    const merged = nums[0].map((_, p) => {
        const col = nums.map(n => n[p]);
        return col.every(v => v === col[0]) ? col[0] : col.join(" / ");
    });
    let k = 0;
    return rows[0].replace(NUM, () => merged[k++]);
};

// 값이 "점수" 가 아니라 "소수 비율(0~1)" 로 들어오는 스탯.
//   4 = 공격 속도, 8 = 치명타 확률, 9 = 치명타 피해량, 18 = 생명력 흡수.
//   이 넷은 계산식 배율(mDisplayAsPercent 의 x100 등)을 실으면 안 된다.
const FRACTION_STATS = new Set([4, 8, 9, 18]);

const statName = (id, hint, formula) => {
    // mStat 이 0(주문력)이면 JSON 에서 필드가 통째로 빠진다. undefined 는 0 으로 본다.
    if (id === undefined || id === null) id = 0;
    if (STAT_NAMES[id] !== undefined) return applyStatFormula(STAT_NAMES[id], formula);
    // 번호만으로는 무슨 스탯인지 알 수 없다. 어디서 나왔는지 같이 남겨서
    // 인게임 툴팁과 대조할 수 있게 한다. (추측으로 표를 채우면 "?" 보다 나쁘다)
    if (!unknownStats.has(id)) unknownStats.set(id, { count: 0, where: [] });
    const e = unknownStats.get(id);
    e.count++;
    // 계수 값을 같이 남긴다. 인게임 툴팁의 "OO의 80%" 와 숫자를 맞춰 보면
    // 이 번호가 무슨 스탯인지 바로 알 수 있다.
    const line = ctx + (hint ? `   (계수 ${hint})` : '');
    if (e.where.length < 6 && !e.where.includes(line)) e.where.push(line);
    return null;
};

// ------------------------------------------------------------
// 계산식 조각 하나를 문자열로
// ------------------------------------------------------------
function partToText(part, spell, maxRank, mult, depth = 0) {
    // ★ 깊이 8. 카타리나 R 이 6겹이라 4로는 안쪽 조각이 통째로 잘렸다.
    //   StatBySubPart > Sum > Product > Sum > Number 까지 내려간다.
    if (!part || depth > 8) return null;
    const type = part.__type || '';

    const dv = (name) => findDataValue(spell, name);

    switch (type) {
        case 'NamedDataValueCalculationPart': {
            const d = dv(part.mDataValue);
            if (d) {
                const t = levelsToText(d.values, maxRank, mult);
                if (t !== null) return t;
            }
            // ★ 여기까지 왔으면 그 항은 0이다. 두 경우가 있는데 결과는 같다.
            //     이름이 아예 없음        — 모드 전용 보너스 등
            //     이름은 있는데 값이 비었음 — 니달리 W 의 ModesBonusMaxTraps
            //   (?) 로 남기면 멀쩡한 값이 통째로 폴백돼 버리므로 0으로 보고 버린다.
            //   다만 조용히 버리면 조회 버그와 구분이 안 되니 목록에 남긴다.
            if (zeroDrop.length < 40) {
                zeroDrop.push(`${ctx}   (${part.mDataValue} — ${d ? '값이 비어 있음' : '이름이 없음'})`);
            }
            return '0';
        }

        case 'NumberCalculationPart':
            return tidy((part.mNumber || 0) * mult);

        // ★ 2026-08-11: 이 두 조각이 mult 를 통째로 무시하고 100 만 곱하고 있었다.
        //   calcToText 는 `mult * percent * selfMult` 를 내려보내는데(1264행),
        //   기본값 조각만 그걸 먹고 계수 조각은 못 먹어서 둘의 배율이 어긋났다.
        //   증상 두 가지가 같은 뿌리였다:
        //     1) mMultiplier 가 있는 계산식 — 기본값엔 배수가 걸리고 계수엔 안 걸림
        //        (아칼리 E ×0.3, 룰루 Q ×0.5, 릴리아 W ×3, 마스터 이 Q ×0.25)
        //     2) mDisplayAsPercent:true 인 체력 % 피해 — percent=100 을 계수가 못 받아
        //        `주문력의 0.03%` 처럼 100배 작게 나감 (렐 E, 판테온 W, 트런들 R ...)
        //   쉔 Q 처럼 base 가 이미 % 단위인(DisplayAsPercent 없는) 자리는 mult=1 이라 그대로다.
        //   ★ 단, 스탯 자체가 "소수(0~1)" 로 들어오는 것들은 mult 를 실으면 안 된다.
        //     공격 속도 0.5 = 50%, 치명타 확률 1.0 = 100% 처럼 이미 비율이라
        //     mDisplayAsPercent 의 x100 을 또 먹으면 100배로 튄다.
        //     (진 P `치명타 확률의 35%`, 야스오·요네 P `치명타 피해량의 100%`,
        //      닐라 Q/R, 세나 P 가 이 경우였다 — 전부 위키와 맞던 값이다)
        //     물리 관통력·이동 속도·체력·방어력 등은 "점수" 라 mult 대상이 맞다.
        case 'StatByNamedDataValueCalculationPart': {
            const d = dv(part.mDataValue);
            const m = FRACTION_STATS.has(part.mStat === undefined ? 0 : part.mStat) ? 1 : mult;
            const ratio = d ? levelsToText(d.values, maxRank, 100 * m) : null;
            const s = statName(part.mStat, ratio === null ? null : ratio + '%', part.mStatFormula);
            if (!d || s === null) return null;
            return `${s}의 ${ratio}%`;
        }

        case 'StatByCoefficientCalculationPart': {
            const m = FRACTION_STATS.has(part.mStat === undefined ? 0 : part.mStat) ? 1 : mult;
            const coef = tidy((part.mCoefficient || 0) * 100 * m);
            const s = statName(part.mStat, coef + '%', part.mStatFormula);
            if (s === null) return null;
            return `${s}의 ${coef}%`;
        }

        case 'ByCharLevelInterpolationCalculationPart': {
            const a = tidy((part.mStartValue || 0) * mult);
            const b = tidy((part.mEndValue || 0) * mult);
            return levelRange(a, b);
        }

        case 'ByCharLevelBreakpointsCalculationPart': {
            // ★ 이 조각은 세 가지 필드를 섞어 쓴다. 하나라도 빼먹으면 최댓값이 틀린다.
            //     mInitialBonusPerLevel      = 레벨당 증가분 (첫 구간)
            //     mBonusPerLevelAtAndAfter   = 이 레벨부터 레벨당 증가분이 바뀜
            //     mAdditionalBonusAtThisLevel= 이 레벨에서 한 번 뛰는 양
            //   예전엔 세 번째만 더해서 니달리 W(4~10)는 맞았지만
            //   레벨당 증가가 있는 스킬은 시작값 그대로 나왔다.
            const base = part.mLevel1Value || 0;
            const bps = [...(part.mBreakpoints || [])].sort((x, y) => (x.mLevel || 0) - (y.mLevel || 0));
            let v = base;
            let per = part.mInitialBonusPerLevel || 0;
            for (let lv = 2; lv <= 18; lv++) {
                const bp = bps.find(x => x.mLevel === lv);
                if (bp) {
                    // ★ 생략 = 0. bin 은 기본값인 필드를 통째로 빼고 적는다 (mStat 생략 = 주문력과 같은 규칙).
                    //   그래서 필드가 하나도 없는 Breakpoint 는 "이 레벨부터 성장 정지"라는 뜻이다.
                    //   생략을 "이전 값 유지"로 읽던 시절엔 멈춰야 할 레벨 뒤로도 계속 더해서
                    //   유미 R 이 1.3~1.9(실제 1.3~1.6), 아이번 HarvestDuration 이 -5초로 나왔다.
                    per = bp.mBonusPerLevelAtAndAfter !== undefined ? bp.mBonusPerLevelAtAndAfter : 0;
                    v += bp.mAdditionalBonusAtThisLevel || 0;
                }
                v += per;
            }
            return levelRange(tidy(base * mult), tidy(v * mult));
        }

        case 'BuffCounterByNamedDataValueCalculationPart': {
            const d = dv(part.mDataValue);
            return d ? `${levelsToText(d.values, maxRank, mult)} (중첩당)` : null;
        }

        case 'SumOfSubPartsCalculationPart': {
            const raw = (part.mSubparts || [])
                .map(p => partToText(p, spell, maxRank, mult, depth + 1));
            if (!raw.length || raw.every(p => p === null)) return null;
            // ★ 못 푼 조각을 조용히 버리면 "계수 빠진 반쪽 값"이 완성품처럼 보인다.
            //   calcToText 와 같은 규칙으로 (?) 를 남겨 눈에 띄게 한다.
            const parts = raw.map(p => p === null ? '(?)' : p);
            // 전부 단일 숫자면 그냥 더한다 (1 + 0.75 -> 1.75)
            if (parts.every(p => /^-?[\d.]+$/.test(p))) {
                return tidy(parts.reduce((a, b) => a + parseFloat(b), 0));
            }
            return joinTerms(parts);
        }

        case 'ClampSubPartsCalculationPart': {
            // clamp(하위 조각들의 합, mFloor, mCeiling).
            //   ★ 합계 안에 스탯 비례가 들어 있으면 실제 값이 게임 중에 변하므로
            //     고정 숫자로 쓸 수 없다. 그래서 가질 수 있는 범위(바닥~천장)를 보여준다.
            //   예전엔 이 타입을 몰라서 합계만 적었고, 그 결과 카이사 E 가
            //   "추가 공격 속도의 100% + 100%" 로 나왔다. 없는 상수를 더한 것처럼 보인다.
            //   ★ 한쪽 끝이 null 인 "열린 clamp" 가 있다. undefined 만 걸러내면
            //     null * mult = 0 이라 천장이 0 으로 찍힌다 — 카이사 E 충전시간이
            //     `0.6 ~ 0` 으로 나가 "0초까지 줄어든다" 로 읽혔다 (2026-08-12).
            const has = (v) => v !== undefined && v !== null;
            if (has(part.mFloor) && has(part.mCeiling)) {
                return `${tidy(part.mFloor * mult)} ~ ${tidy(part.mCeiling * mult)}`;
            }
            if (has(part.mFloor) || has(part.mCeiling)) {
                // 열린 쪽의 실질 끝값 = "스탯이 0일 때의 합".
                //   카이사 E: clamp(1.2 - 0.4 x 공격속도, 0.6, 없음) -> 공속 0 이면 1.2 다.
                //   순서는 열린 끝(스탯 0) -> 닫힌 끝. 롤위키 `1.2 – 0.6` 과 같은 방향이다.
                const open = sumAtZeroStats(part.mSubparts, spell, 0);
                if (open === null) return null;
                const closed = has(part.mFloor) ? part.mFloor : part.mCeiling;
                const a = tidy(open * mult), b = tidy(closed * mult);
                return a === b ? a : `${a} ~ ${b}`;
            }
            // 양쪽 다 없으면 clamp 의 뜻 자체가 안 잡힌다 (칼리스타 P). 손대지 않는다.
            return null;
        }

        case 'ProductOfSubPartsCalculationPart': {
            // ★ 배율(mult)은 "맨 숫자로 나오는 쪽" 에만 싣는다.
            //   롤위키 대조로 확정 — 두 경우가 정확히 반대였다:
            //     카타리나 R  위키 "16% bonus AD"  -> 0.16 쪽에 x100 이 필요하다
            //     다리우스 Q  위키 "+100~140% AD" -> 비율 쪽에 실으면 10000 이 된다
            //   비율 조각(스탯의 N%)은 자기가 이미 100 을 곱해 나오므로 건드리면 안 된다.
            let a = partToText(part.mPart1, spell, maxRank, 1, depth + 1);
            let b = partToText(part.mPart2, spell, maxRank, 1, depth + 1);
            if (!a || !b) return null;
            if (mult !== 1) {
                // ★ "맨 숫자" 이면서 "전부 1 미만" 일 때만 배율을 싣는다.
                //   1 미만이면 아직 분수라 x100 이 필요하고, 1 이상이면 이미 퍼센트 단위다.
                //   롤위키 대조로 셋 다 맞는 걸 확인했다:
                //     카타리나 R  0.16  -> 16%   (위키 "16% bonus AD")
                //     트위치  E  0.35  -> 35%   (위키 "+ 35% bonus AD")
                //     다리우스 Q  100/110/... 은 그대로 (위키 "+ 100~140% AD")
                const scalable = (t) => {
                    const s = String(t).trim();
                    if (!/^-?[\d.]+(\s*[~/]\s*-?[\d.]+)*$/.test(s)) return false;
                    return s.split(/[~/]/).every(x => Math.abs(parseFloat(x)) < 1);
                };
                if (scalable(a)) a = partToText(part.mPart1, spell, maxRank, mult, depth + 1);
                else if (scalable(b)) b = partToText(part.mPart2, spell, maxRank, mult, depth + 1);
                if (!a || !b) return null;
            }
            // ★ 덧셈이 든 조각은 괄호로 묶어야 한다.
            //   안 묶으면 "0.16 x 1 + 공격 속도의 312.5%" 가 되어
            //   0.16 x (1 + ...) 라는 원래 뜻과 달라진다.
            //   ★ 뺄셈도 같이 봐야 한다. joinTerms 가 음수 항을 " - " 로 적기 때문에
            //     ' + ' 만 보면 괄호를 놓쳐서 뜻이 달라진다.
            // ★ 2026-08-11: 양쪽이 순수 숫자면 곱해서 접는다.
            //   예전엔 "150 x 0.01", "6 x 2.5", "0.15 x 2", "x 1" 같은 날 계산식이
            //   그대로 화면에 찍혔다 (나르 R, 카타리나 R, 오리아나 P, 타릭 P).
            //   랭크별 배열끼리도 칸 수가 맞으면 칸끼리 곱한다. 한쪽이 값 하나면 전체에 곱한다.
            const folded = foldProduct(a, b);
            if (folded !== null) return folded;
            const wrap = (x) => / [+-] /.test(x) ? `(${x})` : x;
            return timesText(wrap(a), b);
        }

        case 'EffectValueCalculationPart': {
            const idx = (part.mEffectIndex || 1) - 1;
            const ea = (spell.mEffectAmount || [])[idx];
            return (ea && ea.value) ? levelsToText(ea.value, maxRank, mult) : null;
        }

        case 'StatBySubPartCalculationPart': {
            const s2 = statName(part.mStat, null, part.mStatFormula);
            const sub = partToText(part.mSubpart, spell, maxRank, 100, depth + 1);
            return (s2 !== null && sub !== null) ? `${s2}의 ${sub}%` : null;
        }

        case 'AbilityResourceByCoefficientCalculationPart': {
            // ★ 2026-08-11: mStatFormula 를 안 봐서 전부 "최대 마나" 로 나가고 있었다.
            //   라이즈 Q/W/E 가 mStatFormula=2(추가)인데 "최대 마나" 로 찍혀
            //   lolwiki `bonus mana` / 나무위키 `추가 마나` 와 어긋났다.
            //   (카사딘 R 은 이 조각을 안 쓰므로 영향 없음 — 거긴 실제로 최대 마나가 맞다)
            const res = part.mStatFormula === 2 ? '추가 마나'
                : part.mStatFormula === 1 ? '기본 마나'
                    : '최대 마나';
            return `${res}의 ${tidy((part.mCoefficient || 0) * 100)}%`;
        }

        case 'BuffCounterByCoefficientCalculationPart':
            return `${tidy((part.mCoefficient || 0) * mult)} (중첩당)`;

        case 'ByCharLevelFormulaCalculationPart': {
            // ★ 두 군데가 틀려서 이 분기는 지금까지 한 번도 값을 못 읽고 "0 ~ 0"만 냈다.
            //   1) 필드 이름이 mValues 가 아니라 values 다. 전수 조사 15자리 전부 values.
            //   2) 배열이 31칸이라 마지막 칸을 그대로 쓰면 안 된다.
            //      ※ "31칸 = 레벨 1~31" 로 읽었던 게 아래 2026-08-11 정정의 원인이다.
            const vals = findField(part, 'values');
            if (!Array.isArray(vals) || vals.length < 2) return null;
            // ★ 2026-08-11 정정: 배열은 31칸 = 레벨 0~30 이라 "인덱스 = 레벨" 이다.
            //   레벨1 = vals[1], 레벨18 = vals[18]. 예전엔 vals[0]/vals[17] 을 써서
            //   이 분기가 만드는 11자리 전부가 정확히 레벨 한 칸씩 낮게 나갔다.
            //   (노틸러스 P: idx0=8 idx1=14 idx17=110 idx18=116 idx30=188 — 위키 값이 14~116)
            //   위키 대조로 확인된 8자리: 노틸러스P 럭스P 레오나P 브라움P 카직스P
            //   그레이브즈P 카타리나P 렝가W. 그레이브즈·카타리나·렝가의 "0 ~" 시작값도
            //   전부 이것 때문이었다(레벨0 값이 0이라서). 별개 버그가 아니다.
            const a = tidy(vals[1] * mult);
            const b = tidy(vals[Math.min(18, vals.length - 1)] * mult);
            return levelRange(a, b);
        }

        case 'CooldownMultiplierCalculationPart': {
            // 필드가 __type 하나뿐이다. 용례가 전부 쿨타임 환급/충전 관련이라
            // (미스 포츈 W 사랑의 도장 환급, 애쉬 E 충전, 올라프 Q 환급, 사일러스 R 대상별 쿨타임)
            // 이 스킬의 쿨타임을 가리키는 것으로 본다.
            //   ★ 추론이다. guessedList 에 남기니 인게임 툴팁과 대조할 것.
            //   ★ bin 의 cooldownTime 을 쓰면 값이 한 칸씩 밀린 듯한 결과가 나왔다.
            //     (사일러스 R = 80/80/55, 카타리나 E = 12/12/11/10/9 처럼 첫 값이 겹침)
            //     그래서 화면에 실제로 찍는 값과 같은 CD v1 쿨타임을 쓴다.
            if (!currentCooldown || currentCooldown === '-') return null;
            const picked = currentCooldown.split('/').map(x => tidy(parseFloat(x.trim()) * mult));
            if (picked.some(x => !isFinite(parseFloat(x)))) return null;
            const out = picked.every(x => x === picked[0]) ? picked[0] : picked.join(' / ');
            if (guessedList.length < 40) guessedList.push(`${ctx} = ${out}   [스킬 쿨타임으로 추론]`);
            return out;
        }

        default: {
            // 타입 이름을 모르는 조각. ({ee18a47b} 처럼 CD 가 이름을 못 푼 것들)
            // 이름 대신 "어떤 필드를 갖고 있나"로 정체를 추측한다.
            const guessed = guessPart(part, spell, maxRank, mult, depth);

            if (!unknownParts.has(type)) {
                // 필드 이름이 해시라 뜻을 모를 때는 안에 든 실제 값을 봐야 한다.
                // 첫 등장 한 건의 내용을 통째로 남긴다.
                let sample;
                try {
                    sample = JSON.stringify(part, (k, v) =>
                        (Array.isArray(v) && v.length > 8) ? v.slice(0, 8).concat('...') : v);
                    if (sample.length > 600) sample = sample.slice(0, 600) + ' ...(잘림)';
                } catch (_) { sample = '(출력 실패)'; }
                unknownParts.set(type, {
                    count: 0, keys: Object.keys(part).join(', ') || '(필드 없음)',
                    where: [], solved: 0, sample
                });
            }
            const e = unknownParts.get(type);
            e.count++;
            if (e.where.length < 4 && !e.where.includes(ctx)) e.where.push(ctx);
            if (guessed !== null) {
                e.solved++;
                if (guessedList.length < 40) guessedList.push(`${ctx} = ${guessed}   [${type}]`);
            }
            return guessed;
        }
    }
}

// ------------------------------------------------------------
// 모르는 종류의 조각을 필드 모양으로 추측한다.
//   ★ 추측이므로 결과를 무조건 믿으면 안 된다.
//     푼 값은 guessedList 에 모아서 마지막에 전부 출력한다. 눈으로 검산할 것.
// ------------------------------------------------------------
// "OO의 80%" 를 만든다.
//   ★ 값 쪽이 이미 % 로 끝나면 붙이지 않는다. 계수 자리에 단일 숫자가 아니라
//     계산식 조각이 통째로 들어오는 경우가 있어서(애쉬 P) 그때 "100%%" 가 됐다.
const statRatio = (name, value) =>
    value === null ? null : `${name}의 ${value}${/%$/.test(String(value)) ? '' : '%'}`;

// 항을 이어붙인다. 음수 항이 "+ -1" 로 나오면 흉해서 "- 1" 로 바꾼다.
//   (루시안 R, 미스 포츈 Q·R, 제리 W, 샤코 Q 가 여기 걸렸다. 계산은 맞고 표시만 문제였다)
const joinTerms = (arr) =>
    arr.reduce((acc, t) => /^-/.test(t) ? `${acc} - ${t.slice(1)}` : `${acc} + ${t}`);

// 위와 같지만 맨 앞 항에도 부호를 붙인다. " (+ 주문력의 80%)" 처럼 쓰는 자리용.
const signedTerms = (arr) =>
    arr.map(t => /^-/.test(t) ? `- ${t.slice(1)}` : `+ ${t}`).join(' ');

// ------------------------------------------------------------
// 한쪽 끝이 열린 clamp 의 "열린 끝" 값을 구한다 (2026-08-12).
//   스탯 비례 항을 0 으로 두고 나머지를 숫자로 더한다.
//   ★ 랭크마다 값이 갈리면 물러난다(null). 범위 표기(`A ~ B`)와 랭크 표기(`a / b / c`)를
//     한 칸에 섞으면 읽을 수 없는 문자열이 된다.
// ------------------------------------------------------------
// 배열이 전부 같은 값이면 그 값을, 아니면 null.
//   skipFirst: DataValues 는 0번이 쓰레기라 1번부터 본다.
function foldSame(arr, skipFirst) {
    if (!Array.isArray(arr) || !arr.length) return null;
    const a = (skipFirst && arr.length > 1) ? arr.slice(1) : arr;
    return a.every(x => typeof x === 'number' && Math.abs(x - a[0]) < 1e-6) ? a[0] : null;
}

function numAtZeroStats(part, spell, depth) {
    if (!part || typeof part !== 'object' || depth > 6) return null;
    switch (part.__type) {
        // 스탯 비례 항은 0 으로 둔다 — 그게 "열린 끝" 의 정의다.
        case 'StatByCoefficientCalculationPart':
        case 'StatByNamedDataValueCalculationPart':
        case 'StatBySubPartCalculationPart':
            return 0;
        case 'NumberCalculationPart':
            return part.mNumber || 0;
        case 'EffectValueCalculationPart': {
            const ea = (spell.mEffectAmount || [])[(part.mEffectIndex || 1) - 1];
            return foldSame(ea && ea.value, false);
        }
        case 'NamedDataValueCalculationPart': {
            const d = findDataValue(spell, part.mDataValue);
            return d ? foldSame(d.values, true) : null;
        }
        case 'SumOfSubPartsCalculationPart':
            return sumAtZeroStats(part.mSubparts, spell, depth + 1);
        case 'ProductOfSubPartsCalculationPart': {
            const a = numAtZeroStats(part.mPart1, spell, depth + 1);
            const b = numAtZeroStats(part.mPart2, spell, depth + 1);
            return (a === null || b === null) ? null : a * b;
        }
        default:
            return null;
    }
}

function sumAtZeroStats(parts, spell, depth) {
    if (!Array.isArray(parts) || !parts.length) return null;
    let total = 0;
    for (const p of parts) {
        const v = numAtZeroStats(p, spell, depth);
        if (v === null) return null;
        total += v;
    }
    return total;
}

function guessPart(part, spell, maxRank, mult, depth) {
    if (!part || typeof part !== 'object') return null;

    // ── ByCharLevelBreakpoints 의 "값 대신 DataValue 이름" 변종 ──
    //   ★ 반드시 아래 "두 개짜리" 분기보다 먼저 봐야 한다. 이 모양도 문자열 두 개일 수 있는데
    //     두 번째가 레벨18 값이 아니라 "레벨당 증가분"이라 그대로 읽으면 거꾸로 나온다.
    //     (이렐리아 P 가 10 ~ 3 으로 나오고 있었다. 실제는 10 + 3x17 = 61)
    {
        const l1 = fld(part, 'Level1DataValue');
        if (typeof l1 === 'string') {
            // DataValue 이름 -> 숫자. DataValues 는 0번이 쓰레기라 1번을 쓴다.
            const nv = (name) => {
                if (typeof name !== 'string') return null;
                const d = findDataValue(spell, name);
                if (!d || !Array.isArray(d.values) || !d.values.length) return null;
                return d.values.length > 1 ? d.values[1] : d.values[0];
            };
            const base = nv(l1);
            if (base === null) return null;

            const perName = fld(part, 'BonusPerLevelDataValue');
            const initName = fld(part, 'InitialBonusPerLevelDataValue');
            let per = nv(perName !== undefined ? perName : initName) || 0;

            const raw = fld(part, 'DataValueBreakpoints');
            const bps = Array.isArray(raw)
                ? [...raw].sort((a, b) => ((a.level || a.mLevel || 0) - (b.level || b.mLevel || 0)))
                : [];

            let v = base;
            for (let lv = 2; lv <= 18; lv++) {
                const bp = bps.find(x => (x.level !== undefined ? x.level : x.mLevel) === lv);
                if (bp) {
                    // ★ 생략 = 0(성장 정지). 값 버전과 같은 규칙이다.
                    const after = fld(bp, 'BonusPerLevelAtAndAfterDataValue');
                    per = after !== undefined ? (nv(after) || 0) : 0;
                    const add = fld(bp, 'AdditionalBonusAtThisLevelDataValue');
                    if (add !== undefined) v += (nv(add) || 0);
                }
                v += per;
            }
            return levelRange(tidy(base * mult), tidy(v * mult));
        }
    }

    // 필드 두 개가 각각 "레벨1 값" / "레벨18 값" DataValue 의 이름인 모양.
    //   ({ee18a47b} 가 이 형태. 예: HealCapLevel1Value / HealCapLevel18Value)
    //   챔피언 레벨에 따라 두 값 사이를 보간하는 조각이다.
    //   JSON 키 순서가 곧 파일 순서라 앞이 레벨1, 뒤가 레벨18이다.
    {
        const fk = Object.keys(part).filter(k => k !== '__type');
        if (fk.length === 2 && fk.every(k => typeof part[k] === 'string')) {
            const a = (spell.DataValues || []).find(x => x.name === part[fk[0]]);
            const b = (spell.DataValues || []).find(x => x.name === part[fk[1]]);
            if (a && b) {
                const lo = levelsToText(a.values, 1, mult);
                const hi = levelsToText(b.values, 1, mult);
                if (lo !== null && hi !== null) return levelRange(lo, hi);
            }
            return null;
        }
    }

    // 다른 계산식을 이름으로 가리키는 모양. ({f3cbe7b2} 가 이 형태다)
    //   필드가 mSpellCalculationKey 하나뿐이라 정체가 뚜렷하다.
    if (part.mSpellCalculationKey !== undefined) {
        const ref = findCalc(spell, part.mSpellCalculationKey);
        return ref ? calcToText(ref, spell, maxRank, mult, depth + 1) : null;
    }

    // 이름 붙은 DataValue 를 가리키는 모양
    if (part.mDataValue !== undefined) {
        const d = findDataValue(spell, part.mDataValue);
        if (d) {
            if (part.mStat !== undefined) {
                const s = statName(part.mStat, null, part.mStatFormula);
                if (s !== null) return statRatio(s, levelsToText(d.values, maxRank, 100));
                return null;
            }
            return levelsToText(d.values, maxRank, mult);
        }
        return null;
    }

    // 스탯 * 계수 모양
    if (part.mCoefficient !== undefined && part.mStat !== undefined) {
        const s = statName(part.mStat, null, part.mStatFormula);
        return s === null ? null : statRatio(s, tidy(part.mCoefficient * 100));
    }

    // 상수
    if (part.mNumber !== undefined) return tidy(part.mNumber * mult);

    // 하위 조각을 품은 모양
    if (Array.isArray(part.mSubparts)) {
        const ps = part.mSubparts.map(p => partToText(p, spell, maxRank, mult, depth + 1)).filter(Boolean);
        if (!ps.length) return null;
        if (ps.every(p => /^-?[\d.]+$/.test(p))) return tidy(ps.reduce((a, b) => a + parseFloat(b), 0));
        return joinTerms(ps);
    }
    if (part.mPart1 && part.mPart2) {
        // 위 ProductOfSubParts 와 같은 이유로 배율을 안 싣는다.
        const a = partToText(part.mPart1, spell, maxRank, 1, depth + 1);
        const b = partToText(part.mPart2, spell, maxRank, 1, depth + 1);
        return (a && b) ? timesText(a, b) : null;
    }
    if (part.mSubpart) return partToText(part.mSubpart, spell, maxRank, mult, depth + 1);

    return null;
}

// mSpellCalculations 항목 하나 -> 문자열
function calcToText(calc, spell, maxRank, mult, depth = 0) {
    // 계산식이 다른 계산식을 참조하는 사슬. 위 partToText 상향과 짝을 맞춘다.
    if (!calc || depth > 6) return null;

    // 다른 계산식을 배율만 바꿔 재사용하는 형태
    if (calc.mModifiedGameCalculation) {
        const base = findCalc(spell, calc.mModifiedGameCalculation);
        const m = calc.mMultiplier ? partToText(calc.mMultiplier, spell, maxRank, 1, depth + 1) : null;
        // ★ 2026-08-11: 배율이 숫자 하나로 접히면 안쪽 계산식에 실어서 내려보낸다.
        //   예전엔 `${inner} x ${m}` 으로 날것을 붙여서 화면에 계산식이 그대로 찍혔다
        //   ("30 / 50 / 70 (+ 주문력의 10%) x 3"). 게다가 배율이 기본값에만 걸린 것처럼
        //   읽혀서 계수를 오해하기 쉬웠다. 내려보내면 기본값·계수가 같이 접힌다
        //   (그웬 R -> "90 / 150 / 210 (+ 주문력의 30%)" = lolwiki 와 일치).
        //   랭크별 배열처럼 숫자 하나로 못 접는 배율은 예전처럼 x 로 남긴다
        //   (모데카이저 Q 의 "x 1.3 / 1.35 / 1.4 / 1.45 / 1.5").
        // ★ 치명타 배율만은 기본값에 녹이지 않고 "…의 130%" 로 따로 보여 준다 (2026-08-12).
        //   녹이면 가렌 E 가 `5.2 / 9.1 / 13 / 16.9 / 20.8` 처럼 소수점투성이가 되고,
        //   나무위키 표기(×1.3)와도 모양이 달라져서 대조가 어렵다.
        //   판별은 **배율 조각 안에 치명타 피해량(mStat 9)이 있는지**로 한다 —
        //   partToText 를 거치고 나면 이미 `1.3` 이라 숫자만 봐서는 구분할 수 없다.
        const isCritMult = /"mStat"\s*:\s*9\b/.test(JSON.stringify(calc.mMultiplier || {}));
        if (!isCritMult && m !== null && /^-?[\d.]+$/.test(m)) {
            return calcToText(base, spell, maxRank, mult * parseFloat(m), depth + 1);
        }
        // 랭크별로 다른 배율("14 / 16 / 18")도 접어 본다. 실패하면 아래 예전 표기로 떨어진다.
        if (m !== null && /^-?[\d.]+(\s*\/\s*-?[\d.]+)+$/.test(m)) {
            const ranks = m.split('/').map(x => parseFloat(x.trim()));
            const folded = foldRankMultiplier(
                (k) => calcToText(base, spell, maxRank, mult * k, depth + 1), ranks);
            if (folded !== null) return folded;
        }
        const inner = calcToText(base, spell, maxRank, mult, depth + 1);
        if (!inner) return null;
        return m ? timesText(inner, m) : inner;
    }

    // 버프 유무로 계산식이 갈리는 형태 (카이사 Q MaxDamageDisplay).
    //   ★ 조건부 쪽은 이름이 {b2bd0d2f} 처럼 난독화돼 있어 못 쓴다.
    //     버프가 없는 기본 상태를 쓴다 — 툴팁 기본 표기와 같은 쪽이다.
    if (calc.mDefaultGameCalculation) {
        return calcToText(findCalc(spell, calc.mDefaultGameCalculation), spell, maxRank, mult, depth + 1);
    }

    const percent = calc.mDisplayAsPercent ? 100 : 1;

    // 계산식 전체에 곱해지는 배율 (예: 0.01)
    let selfMult = 1;
    let rawMult = null;
    if (calc.mMultiplier) {
        const mTxt = partToText(calc.mMultiplier, spell, maxRank, 1, depth + 1);
        if (mTxt !== null && /^-?[\d.]+$/.test(mTxt)) selfMult = parseFloat(mTxt);
        else if (mTxt !== null) rawMult = mTxt;   // 숫자 하나로 접을 수 없는 배율
    }

    const bodyOf = (m) => (calc.mFormulaParts || [])
        .map(p => partToText(p, spell, maxRank, m, depth + 1));
    let parts = bodyOf(mult * percent * selfMult);

    // ★ 숫자 하나로 못 접는 배율(랭크별 배열 등)을 어떻게 할 것인가.
    //   원래는 통째로 버렸다. 대부분은 그게 맞다 — 본체에 이미 피해량이 다 들어 있고
    //   배율은 "치명타 확률에 따라 증폭" 같은 별도 설명이라, 앞에 붙이면 문장만 길어진다.
    //   ★ 단 하나 예외: 본체가 순수 배율(예: clamp 의 "1 ~ 2")뿐이면 기본값이 통째로
    //     사라진다. 카이사 E 가 그랬다 — "100 ~ 200%" 로 나왔는데 실제는
    //     "기본 이동 속도 55~75% x (1 + 추가 공속, 최대 2배)" 다.
    //   그래서 "본체가 맨 숫자/범위 하나뿐일 때만" 배율을 곱셈 항으로 세운다.
    const isBareRange = (t) =>
        typeof t === 'string' && /^-?[\d.]+(\s*[~/]\s*-?[\d.]+)*$/.test(t.trim());
    let multText = null;
    if (rawMult !== null && parts.length === 1 && isBareRange(parts[0])) {
        multText = partToText(calc.mMultiplier, spell, maxRank, percent, depth + 1);
        parts = bodyOf(mult * selfMult);   // percent 는 multText 가 먹었다
    }

    if (!parts.length || parts.every(p => p === null)) return null;

    const base = parts[0];
    // ★ 기본값을 못 찾으면 실패로 본다.
    //   예전에는 "? (+ 주문력의 80%)" 같은 반쪽짜리를 돌려줬는데,
    //   그러면 성공으로 취급돼서 다음 후보 객체를 못 뒤졌다.
    if (base === null) return null;

    // 해석 못 한 계수 조각을 조용히 버리면 "계수 없는 반쪽 값"이 된다.
    // (?) 로 남겨서 미리보기에서 눈에 띄게 한다.
    const rest = parts.slice(1)
        .filter(p => p !== '0')          // 0인 항은 적어봐야 의미가 없다
        .map(p => p === null ? '(?)' : p);
    let out = base;
    // ★ 이미 % 로 끝나면 붙이지 않는다. 스탯 비례 조각은 guessPart 에서
    //   자기가 % 를 달고 나오기 때문에 여기서 또 붙이면 "30%%" 가 된다 (닐라 Q).
    if (calc.mDisplayAsPercent && !multText && !/%$/.test(out)) {
        // "(레벨에 따라)" 같은 꼬리표가 있으면 그 앞에 % 를 넣는다.
        //   ★ 아무 괄호나 잡으면 안 된다. 계산 항이 괄호로 끝나는 경우
        //     곱셈 한가운데에 % 가 끼어든다 ("0.6 x% (이동 속도의 100%)" — 샤코 Q).
        //     그래서 "~에 따라" 로 끝나는 진짜 꼬리표만 잡는다.
        const tail = out.match(/\s*\([^()]*에 따라\)$/);
        out = tail ? out.slice(0, tail.index) + '%' + tail[0] : out + '%';
    }
    // ★ 계수 항에도 % 를 붙여야 한다. 예전엔 기본값에만 붙여서
    //   우디르 Q 가 "20 / ... / 80% (+ 20 ~ 70 (레벨에 따라))" 로 나왔다.
    //   인게임은 뒤쪽도 "20% ~ 70%" 라 퍼센트다.
    //   ★ 숫자로만 된 항에만 붙인다. "주문력의 80%" 같은 스탯 비례 항은
    //     이미 자기 % 를 달고 나오므로 건드리면 "80%%" 가 된다.
    const asPercent = (t) => {
        if (!calc.mDisplayAsPercent || multText || /%/.test(t)) return t;
        const tail = t.match(/\s*\([^()]*에 따라\)$/);
        const head = tail ? t.slice(0, tail.index) : t;
        if (!/^-?[\d.]+(\s*[~/]\s*-?[\d.]+)*$/.test(head.trim())) return t;
        return tail ? head + '%' + tail[0] : head + '%';
    };
    if (rest.length) out += ` (${signedTerms(rest.map(asPercent))})`;
    if (multText) {
        const mt = (calc.mDisplayAsPercent && !/%$/.test(multText)) ? multText + '%' : multText;
        // 곱은 순서를 안 가리므로 표기 순서(mt 먼저)는 그대로 두고 뒷쪽만 다듬는다.
        //   카이사 E: "55 / ... / 75% x 1 ~ 2" -> "55 / ... / 75%의 100 ~ 200%"
        out = timesText(mt, out);
    }
    // 배율이 접히면서 이미 % 로 끝나는 항에 % 가 또 붙는 자리가 있다 (케이틀린 P).
    return String(out).replace(/%%+/g, '%');
}

// 플레이스홀더 이름 하나를 해결
function resolve(name, spell, maxRank, depth = 0) {
    // ★ @spell.<스펠이름>:<이름>@ — 다른 스펠의 값을 부르는 교차 참조.
    //   대소문자가 섞여 있다(@spell.GnarQ:...@ / @Spell.SonaQ:...@).
    //   자기 자신을 부르기도 한다(소나 P 의 @Spell.SonaPassive:...@).
    //   ★ 배율(*100)·자릿수(.1) 접미사는 안쪽 이름 끝에 붙어 있으므로
    //     떼지 말고 통째로 넘겨서 재귀 호출이 알아서 처리하게 둔다.
    const xm = String(name).trim().match(/^spell\.([A-Za-z0-9_]+)\s*:\s*(.+)$/i);
    if (xm) {
        if (depth > 3) return null;                       // 서로 부르는 경우 대비
        const t = spellIndex[xm[1].toLowerCase()];
        if (!t) {
            if (crossMiss.length < 40) crossMiss.push(`${ctx}   (스펠 객체 ${xm[1]} 없음)`);
            return null;
        }
        const v = resolve(xm[2].trim(), t.spell, t.maxRank, depth + 1);
        if (v === null && crossMiss.length < 40) crossMiss.push(`${ctx}   (${xm[1]} 안에서 ${xm[2]} 못 찾음)`);
        if (v !== null) crossHits.add(`${ctx} = ${v}`);
        return v;
    }

    // @Name*100@ / @Name*-100@ 형태에서 배율을 떼어낸다
    let mult = 1;
    let clean = String(name).trim();
    const m = clean.match(/^(.+?)\s*\*\s*(-?[\d.]+)$/);
    if (m) { clean = m[1].trim(); mult = parseFloat(m[2]); }

    // @ShieldDuration.1@ / @f2.0@ 처럼 이름 끝에 붙는 점+숫자를 떼어낸다.
    //   ★ 이걸 안 떼면 이름을 통째로 찾으려 해서 무조건 실패한다.
    //     f2.0 은 GENERIC_NAME 검사에도 안 걸려서 범용 이름 보호막까지 풀렸다.
    let precision = null;
    const dm = clean.match(/^(.+?)\.(\d+)$/);
    if (dm) { clean = dm[1].trim(); precision = parseInt(dm[2], 10); }

    const done = (v) => (v === null ? null : applyPrecision(v, precision));

    // 1) DataValues 에 직접 있나
    //    ★ 정확히 일치를 먼저 보고, 없으면 대소문자를 무시하고 한 번 더 본다.
    //      라이엇이 툴팁 철자와 bin 철자를 따로 관리해서 자주 어긋난다.
    //      HoTDuration -> HotDuration, Stunduration -> StunDuration,
    //      LifeStealPercent -> LifestealPercent 같은 식이다.
    const d = findDataValue(spell, clean);
    if (d) {
        // 문장에 이미 % 가 붙어 있으므로 값에는 붙이지 않는다 (50%% 방지)
        return done(levelsToText(d.values, maxRank, mult));
    }

    // 2) mSpellCalculations 에 있나 (여기도 같은 이유로 대소문자 폴백)
    const calc = findCalc(spell, clean);
    if (calc !== undefined) return done(calcToText(calc, spell, maxRank, mult));

    // 3) Effect2Amount 처럼 mEffectAmount 를 이름으로 부르는 경우
    const em = clean.match(/^Effect(\d+)Amount$/i);
    if (em && Array.isArray(spell.mEffectAmount)) {
        const ea = spell.mEffectAmount[parseInt(em[1]) - 1];
        if (ea && ea.value) {
            const t = levelsToText(ea.value, maxRank, mult);
            // 문장에 이미 % 가 붙어 있으므로 값에는 붙이지 않는다
            if (t !== null) return done(t);
        }
    }

    // 4) f1, f2 ... 도 같은 배열을 가리킨다
    const fm = clean.match(/^f(\d+)$/i);
    if (fm && Array.isArray(spell.mEffectAmount)) {
        const ea = spell.mEffectAmount[parseInt(fm[1]) - 1];
        if (ea && ea.value) {
            const t = levelsToText(ea.value, maxRank, mult);
            if (t !== null) return done(t);
        }
    }

    // 5) 탄약 값은 mSpell 바로 밑이 아니라 mAmmo 하위 블록에 따로 들어 있다.
    //    (AmmoRechargeTime, MaxAmmo ... — 바이 E, 벨코즈 W, 진 E 가 여기 걸렸다)
    const rank1 = RANK1_FIELDS.test(clean);
    const ammoTxt = fieldToText(findField(spell.mAmmo, clean), maxRank, mult, rank1);
    if (ammoTxt !== null) {
        if (viaField.length < 40) viaField.push(`${ctx} = ${ammoTxt}   [mAmmo 필드${rank1 ? ', 1번부터' : ''}]`);
        return done(ammoTxt);
    }

    // 6) 스펠 최상위 필드 (PerTargetCooldown, EnergyRefund ...)
    //    DataValues 도 계산식도 아니고 스펠 객체에 그냥 붙어 있는 값들이다.
    const aliased = FIELD_ALIAS[clean.toLowerCase()];
    const topRaw = findField(spell, clean) !== undefined
        ? findField(spell, clean)
        : (aliased ? findField(spell, aliased) : undefined);
    const topTxt = fieldToText(topRaw, maxRank, mult, rank1);
    if (topTxt !== null) {
        if (viaField.length < 40) viaField.push(`${ctx} = ${topTxt}   [최상위 필드${rank1 ? ', 1번부터' : ''}]`);
        return done(topTxt);
    }

    return null;
}

// ------------------------------------------------------------
// bin 에서 Q/W/E/R 스펠 객체를 찾는다.
//
//   ★ 예전에는 스킬 하나당 객체 하나만 봤다. 그런데 투사체·소환물·진화판의
//     수치는 본체가 아니라 딸린 객체에 들어 있어서 못 찾는 값이 많았다.
//     (카이사 진화, 크산테 변형, 자이라 씨앗, 하이머딩거 포탑 등)
//
//   그래서 키마다 "후보 목록"을 만든다. 앞에 있는 것부터 뒤진다:
//     1등급 본체  — CharacterRecord 의 spells 앞 4개 (Q,W,E,R 순서 보증)
//     2등급 계열  — 이름에 {alias}{key} 가 들어간 객체 (KaisaQMissile 등)
//     3등급 기타  — 같은 챔피언의 나머지 스펠 객체 전부 (최후의 수단)
//
//   본체를 항상 먼저 보므로 진화판 수치가 기본 수치 자리에 끼어들지 않는다.
// ------------------------------------------------------------
function getSpellsFromBin(bin, alias) {
    const keys = ['Q', 'W', 'E', 'R'];
    const out = { Q: [], W: [], E: [], R: [], P: [] };

    const rec = bin[`Characters/${alias}/CharacterRecords/Root`];
    if (!rec) return out;

    const used = new Set();

    // --- 1등급: 본체 ---
    (rec.spells || []).forEach((p, i) => {
        if (i >= 4) return;
        const obj = bin[p];
        if (obj && obj.mSpell) {
            out[keys[i]].push({ spell: obj.mSpell, tier: '본체', path: p });
            used.add(p);
        }
    });

    const passPath = rec.mCharacterPassiveSpell;
    const pass = bin[passPath];
    if (pass && pass.mSpell) {
        out.P.push({ spell: pass.mSpell, tier: '본체', path: passPath });
        used.add(passPath);
    }

    // --- 2등급 / 3등급 ---
    const prefix = `Characters/${alias}/Spells/`;
    const low = alias.toLowerCase();
    const leftovers = [];

    for (const p in bin) {
        if (!p.startsWith(prefix) || used.has(p)) continue;
        const obj = bin[p];
        if (!obj || !obj.mSpell) continue;

        const nameLow = String(obj.ObjectName || p).toLowerCase();
        let matched = false;
        for (const key of keys) {
            if (nameLow.includes(low + key.toLowerCase())) {
                out[key].push({ spell: obj.mSpell, tier: '계열', path: p });
                matched = true;
            }
        }
        if (!matched) leftovers.push({ spell: obj.mSpell, tier: '기타', path: p });
    }

    for (const key of keys) out[key].push(...leftovers);
    out.P.push(...leftovers);

    return out;
}

// @spell.<스펠이름>:<이름>@ 교차 참조를 풀기 위한 색인.
//   스펠 객체 이름(소문자) -> { spell, maxRank }
//   ★ maxRank 를 같이 들고 다녀야 한다. 패시브 문장이 Q 값을 부르는 경우가 있는데
//     P 의 maxRank(1)로 읽으면 1랭크 값만 나오고, 반대로 R 값을 5랭크로 읽으면
//     없는 4·5랭크가 지어내진다.
//   ★ 모를 때 기본값은 1 이다. levelsToText 는 값이 전부 같으면 하나로 줄이므로
//     랭크가 없는 값은 1 로 읽어도 맞고, 5 로 두면 없는 랭크가 부풀려진다.
function buildSpellIndex(bin, alias) {
    const idx = {};
    const putName = (nm, spell, maxRank) => {
        const k = String(nm || '').toLowerCase();
        if (!k || !spell || idx[k]) return;     // 먼저 등록된 쪽(더 확실한 쪽)을 지킨다
        idx[k] = { spell, maxRank };
    };
    const put = (path, spell, maxRank) =>
        putName(String(path || '').split('/').pop(), spell, maxRank);

    const rec = bin[`Characters/${alias}/CharacterRecords/Root`];
    if (!rec) return idx;

    // CharacterRecord 가 제일 확실하다. 옛날 이름(PowerBall=람머스 Q,
    // GlacialStorm=애니비아 R 등)도 여기서 제 랭크를 얻는다.
    //   ★ CD 가 경로 이름을 못 풀어 {해시}로 남기는 경우가 있다.
    //     스몰더 패시브가 `{c72a53d8}` 이라 SmolderP 라는 키가 아예 없었다.
    //     툴팁은 관례 이름으로 부르므로 그쪽으로도 같이 등록한다.
    //   ★ 랭크 수는 반드시 rankOf 로 얻어야 한다. 여기에 [5,5,5,3] 을 박아 두면
    //     교차 참조로 들어온 값만 표준 랭크로 잘린다. 제이스 Q 가 그랬다 —
    //     쿨타임은 6칸인데 피해량은 5칸이라 한 스킬 안에서 칸 수가 어긋났다.
    const keys = ['Q', 'W', 'E', 'R'];
    (rec.spells || []).forEach((p, i) => {
        if (i >= 4 || !bin[p] || !bin[p].mSpell) return;
        const r = rankOf(alias, keys[i]);
        put(p, bin[p].mSpell, r);
        putName(alias + keys[i], bin[p].mSpell, r);
    });
    const pp = rec.mCharacterPassiveSpell;
    if (bin[pp] && bin[pp].mSpell) {
        put(pp, bin[pp].mSpell, 1);
        putName(alias + 'P', bin[pp].mSpell, 1);
        putName(alias + 'Passive', bin[pp].mSpell, 1);
    }

    // ★ 두 번째 폼 스킬도 제 랭크로 등록한다. CharacterRecord 에 없어서 아래 이름 추론에
    //   맡기면 1랭크가 되고, 교차 참조로 들어온 값이 한 칸만 나온다.
    //   (제이스 Q2 피해량이 6랭크인데 "80" 하나로 나왔다)
    const f2 = FORM2[alias];
    if (f2) {
        for (const slot of ['Q', 'W', 'E', 'R']) {
            if (!f2[slot]) continue;
            const o = findSpellObj(bin, alias, f2[slot]);
            if (o) putName(f2[slot], o.mSpell, rankOf(alias, slot));
        }
    }

    // 나머지 스펠 객체(미사일·소환물·변신판 등)는 이름으로만 추론한다.
    const prefix = `Characters/${alias}/Spells/`;
    const low = alias.toLowerCase();
    for (const p in bin) {
        if (!p.startsWith(prefix)) continue;
        const o = bin[p];
        if (!o || !o.mSpell) continue;
        const nameLow = String(o.ObjectName || p).toLowerCase();
        let r = 1;
        if (nameLow.includes(low + 'r')) r = rankOf(alias, 'R');
        else {
            const k = ['q', 'w', 'e'].find(x => nameLow.includes(low + x));
            if (k) r = rankOf(alias, k.toUpperCase());
        }
        put(p, o.mSpell, r);
    }
    return idx;
}

// f1, f2, Effect3Amount 처럼 스킬 고유 이름이 아니라
// 거의 모든 스펠 객체에 들어 있는 범용 슬롯 이름.
//   ★ 이런 이름으로 pool 을 뒤지면 엉뚱한 객체에서 아무 값이나 물어온다.
//     (바드 W f1 = 19/0/0/0/0 같은 쓰레기가 이렇게 생겼다)
//     그래서 범용 이름은 본체에서만 찾는다.
const GENERIC_NAME = /^(f\d+|Effect\d+Amount)$/i;

// 최상위 / mAmmo 필드 중에도 거의 모든 스펠 객체에 들어 있는 이름이 있다.
//   위와 같은 이유로 이것들도 본체에서만 찾는다.
//   (안 그러면 미사일 객체의 cooldownTime 같은 게 딸려 들어온다)
const GENERIC_FIELDS = new Set([
    'cooldowntime', 'mana', 'castrange', 'castrangedisplayoverride',
    'missilespeed', 'castradius', 'castconeangle', 'castconedistance',
    'casttime', 'linewidth', 'ammorechargetime', 'maxammo', 'ammousedpercast', 'cost',
]);

const isGenericName = (clean) =>
    GENERIC_NAME.test(clean) || GENERIC_FIELDS.has(clean.toLowerCase());

// 후보 목록을 순서대로 뒤져서 처음 성공하는 값을 쓴다.
function resolveFromPool(name, pool, maxRank) {
    // ★ 배율(*100)뿐 아니라 ".숫자" 꼬리도 여기서 같이 떼야 한다.
    //   안 그러면 f2.0 이 범용 이름으로 안 잡혀서 pool 전체를 뒤진다.
    const clean = String(name).trim()
        .replace(/\s*\*\s*-?[\d.]+$/, '')
        .replace(/\.\d+$/, '')
        .trim();
    const generic = isGenericName(clean);

    for (const cand of pool) {
        // pool 은 본체가 항상 맨 앞이다. 범용 이름이면 거기서 끊는다.
        if (generic && cand.tier !== '본체') break;
        const v = resolve(name, cand.spell, maxRank);
        if (v !== null) return { val: v, tier: cand.tier };
    }
    return { val: null, tier: null };
}

// ------------------------------------------------------------
// 기존 파일에서 블록 잘라내기 (build_champion_data.js 와 같은 방식)
// ------------------------------------------------------------
function extractBlock(source, alias) {
    const m = new RegExp(`"${alias}"\\s*:\\s*\\{`).exec(source);
    if (!m) return null;
    let i = m.index + m[0].length - 1, depth = 0, inStr = null, esc = false;
    for (; i < source.length; i++) {
        const ch = source[i];
        if (inStr) {
            if (esc) { esc = false; continue; }
            if (ch === '\\') { esc = true; continue; }
            if (ch === inStr) inStr = null;
            continue;
        }
        if (ch === '"' || ch === "'" || ch === '`') { inStr = ch; continue; }
        if (ch === '{') depth++;
        else if (ch === '}') { depth--; if (!depth) return source.slice(m.index, i + 1); }
    }
    return null;
}

// 손으로 쓴 v1 / v2 를 기존 파일에서 원문 그대로 떠 온다.
//   ★ 재생성은 값을 전부 새로 찍으므로, 이게 없으면 손으로 만든 피해량 줄이 매번 날아간다.
//     (지금은 가렌·갈리오 6자리뿐이지만 v1·v2 작성은 앞으로 할 일이라 미리 막아 둔다)
//   ★ 값이 한 줄이 아닐 수 있다. 가렌 P 의 v1 은 drawGraph 호출이 6줄이다.
//     그래서 문자열로 파싱하지 않고 "그 속성이 차지하는 줄들"을 통째로 옮긴다.
function extractVV(source, alias) {
    const block = extractBlock(source, alias);
    if (!block) return {};
    const out = {};
    const lines = block.split('\n');
    let key = null;
    for (let i = 0; i < lines.length; i++) {
        // Q2 / W2 처럼 두 번째 폼 키도 받는다.
        const mk = lines[i].match(/^\s{8}"([PQWER]\d?)":\s*\{/);
        if (mk) { key = mk[1]; continue; }
        const mv = lines[i].match(/^\s{12}"(v1|v2)":/);
        if (!mv || !key) continue;
        const buf = [lines[i]];
        let j = i + 1;
        // 같은 들여쓰기의 다음 속성이나 스킬 블록 끝을 만나면 멈춘다.
        for (; j < lines.length; j++) {
            if (/^\s{12}"/.test(lines[j]) || /^\s{8}\}/.test(lines[j])) break;
            buf.push(lines[j]);
        }
        let text = buf.join('\n').replace(/\s+$/, '');
        if (!text.endsWith(',')) text += ',';   // 뒤에 항상 cooldown 이 오므로 쉼표가 필요하다
        (out[key] = out[key] || {})[mv[1]] = text;
        i = j - 1;
    }
    return out;
}

const q = (s) => '"' + String(s).replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\r?\n/g, '\\n') + '"';

// ------------------------------------------------------------

async function main() {
    console.log(WRITE ? '[모드] 파일 생성' : '[모드] 미리보기 — 아무 파일도 건드리지 않습니다\n');

    const oldValues = fs.existsSync(SRC_VALUES) ? fs.readFileSync(SRC_VALUES, 'utf8') : '';
    // ★ 파일 앞부분(drawGraph/drawSteps 헬퍼, 컬러 코드표)을 그대로 물려준다.
    //   ★★ 경계를 indexOf 로 찾으므로 **주석 안에라도** 같은 글자가 앞에 있으면
    //     거기서 잘려 헬퍼가 통째로 날아간다. 2026-08-12에 실제로 겪었다.
    //     그래서 줄 맨 앞에서 시작하는 선언만 찾는다 (주석은 // 로 시작하니 안 걸린다).
    const m = oldValues.match(/^const customValues/m);
    const prelude = m ? m.index : -1;
    const valuesPrelude = prelude === -1 ? '' : oldValues.slice(0, prelude);
    if (oldValues && prelude === -1) {
        console.log('[주의] 기존 custom_values.js 에서 값 선언을 못 찾았습니다 — 헬퍼가 빠진 채로 생성됩니다.');
    }

    // 패시브 문장은 CD v1 에 없어서 stringtable 에서 가져온다 (stringtable.js 주석 참고)
    const strings = await loadStringTable({ refresh: process.argv.includes('--refresh') });

    const summary = await get(`${CD}/champion-summary.json`);
    const seen = new Set();
    const champions = summary
        .filter(c => c.id > 0)
        .sort((a, b) => a.id - b.id)
        .filter(c => { if (seen.has(c.name)) return false; seen.add(c.name); return true; })
        .sort((a, b) => a.name.localeCompare(b.name, 'ko'));

    console.log(`챔피언 ${champions.length}명\n`);

    const entries = [];
    let total = 0, filled = 0;
    const binFails = [];
    const stillUnknown = [];
    const partial = [];
    const rescued = [];   // 본체가 아닌 딸린 객체에서 건져낸 값

    for (let n = 0; n < champions.length; n++) {
        const c = champions[n];
        const alias = c.alias;

        if (PRESERVE.includes(alias)) {
            const b = extractBlock(oldValues, alias);
            if (b) entries.push(`    ${b}, // ${c.name} (직접 작성)`);
            continue;
        }

        // 손으로 쓴 피해량 줄은 재생성해도 살린다.
        // 옛 파일이 CD 철자로 쓰여 있을 수 있어 양쪽 다 본다 (키를 바꾼 첫 실행 대비)
        const carried = Object.assign(extractVV(oldValues, alias), extractVV(oldValues, ddId(alias)));

        let v1, bin;
        try { v1 = await get(`${CD}/champions/${c.id}.json`); }
        catch (e) { binFails.push(`${c.name} v1(${e.message})`); await sleep(DELAY); continue; }

        try {
            const low = alias.toLowerCase();
            // ★ bin 은 로컬에 캐시한다 (7일). 표 한 줄 고치고 다시 돌리는 반복이
            //   1~2분에서 몇 초로 줄어든다. 패치 후에는 `--refresh` 를 붙일 것.
            bin = await getBin(`${BIN}/${low}/${low}.bin.json`, low);
        } catch (e) {
            binFails.push(`${c.name} bin(${e.message})`);
            await sleep(DELAY);
            continue;
        }

        const binSpells = getSpellsFromBin(bin, alias);
        spellIndex = buildSpellIndex(bin, alias);   // @spell.X:Y@ 교차 참조용
        const extraIcons = findExtraIcons(bin, alias, binSpells);   // 슬롯별 추가 아이콘
        // 이제 키는 항상 있고 값이 배열이다. 전부 비어 있으면 CharacterRecord 를 못 읽은 것.
        if (!Object.values(binSpells).some(arr => arr.length)) binFails.push(`${c.name} CharacterRecord 없음`);

        // ---- 패시브 ----
        // 문장은 stringtable 에서 온다. build_champion_data.js 와 같은 경로를 써야
        // 템플릿의 {pN} 번호와 여기서 매기는 pN 이 어긋나지 않는다.
        //   ★ 중첩형 {{키_@인덱스@}} 도 **build 와 똑같이** 풀어야 한다 (2026-08-10).
        //     안 풀면 여기서는 자리를 1개(@GameModeInteger@)로 세는데 템플릿엔
        //     풀린 문장의 자리가 4개 들어가서, 화면에 `{p2}` 같은 게 그대로 찍힌다.
        //     케인 P 가 실제로 그랬다. NESTED_INDEX 는 두 파일이 항상 같아야 한다.
        const passiveRaw = resolveNested(getPassiveTooltip(bin, alias, strings), strings);
        // 구분선 아래 회색 글씨. build 가 본문 뒤에 이어 붙여 번호를 매기므로 여기도 뒤에 붙인다.
        const belowMap = belowLineMap(bin, alias, strings);
        const passiveSrc = (passiveRaw || '') + (belowMap.P || '');
        const passiveNames = passiveSrc
            ? [...new Set([...String(passiveSrc)
                .replace(/@SpellModifierDescriptionAppend@/gi, '')
                .matchAll(/@([A-Za-z0-9_.*+\-/():]+?)@/g)].map(x => x[1].trim()))]
            : [];

        const previewLines = [];
        const lines = [];

        // ---- 패시브 쿨타임 (2026-08-14 추가) ----
        //   ★ 예전엔 무조건 "-" 로 박았다. "패시브엔 쿨타임이 없다" 는 전제가 틀렸다 —
        //     **173명 중 21명**의 패시브가 인게임에 쿨타임을 표시한다.
        //   찾아가는 길은 툴팁·회색 글씨와 같다:
        //     bin `mSpell.mClientData.mTooltipData.mLocKeys.keyCooldown` → stringtable(소문자)
        //   문장이 `"%i:cooldown% @PassiveCooldownModified@초"` 꼴이라
        //   본문과 **똑같은 @Placeholder@ 문법**이다. 기존 해석기를 그대로 쓴다.
        //   ★ 화면은 `쿨타임 ${cooldown}` 으로 찍으므로 QWER 과 맞춰 **숫자만** 남긴다
        //     (아이콘 토큰과 "초" 를 벗긴다).
        let passiveCd = '-';
        let passiveCdCalc = '';   // 계산식 이름. 줄 뒤 주석으로 나가 각주와 조인된다
        {
            const pObj = (binSpells.P || [])[0];
            const lk = pObj && pObj.spell && pObj.spell.mClientData
                && pObj.spell.mClientData.mTooltipData && pObj.spell.mClientData.mTooltipData.mLocKeys;
            const raw = lk && lk.keyCooldown ? strings[String(lk.keyCooldown).toLowerCase()] : null;
            if (raw) {
                // ★ `CooldownMultiplierCalculationPart` 는 `currentCooldown` 을 참조한다.
                //   패시브엔 랭크별 쿨타임이 없어서 그대로 두면 **직전 챔피언의 R 쿨타임**이
                //   섞여 들어온다. 갈리오 P 가 `CooldownMultiplier x PassiveCooldown` 이라
                //   이 자리가 곧 배율이고, 쿨감 0% 기준 배율은 **1** 이다.
                currentCooldown = '1';
                ctx = `${c.name} P / keyCooldown`;
                let ok = true;
                const filled = String(raw)
                    .replace(/%i:[a-zA-Z0-9_]+%/g, '')
                    .replace(/<[^>]+>/g, '')
                    .replace(/@([A-Za-z0-9_.*+\-/():]+?)@/g, (m0, nm) => {
                        // ★ 계산식 이름을 기억해 둔다. 줄 뒤 주석으로 내보내면
                        //   `add_level_graphs.js` 가 `level_curves.json` 과 조인해
                        //   레벨별 각주를 달 수 있다 (QWER 의 pN 자리와 같은 방식).
                        if (!passiveCdCalc) passiveCdCalc = nm.trim();
                        const r = resolveFromPool(nm.trim(), binSpells.P || [], 1);
                        if (!r || r.val === null) { ok = false; return m0; }
                        return r.val;
                    })
                    .replace(/초/g, '')
                    .replace(/\s+/g, ' ')
                    .trim();
                if (ok && filled && !/[@?]/.test(filled)) {
                    passiveCd = filled;
                    // 툴팁 값과 실제가 다른 자리에는 각주를 단다 (위 PASSIVE_CD_NOTE 주석 참고)
                    const note = PASSIVE_CD_NOTE[c.name];
                    if (note) {
                        passiveCd += passiveCdFootnote(1, filled, note.real, note.why);
                        passiveCdNoteMade.push(`${c.name}: 표시 ${filled} / 실제 ${note.real}`);
                    }
                    passiveCdMade.push(`${c.name} = ${filled}`);
                } else {
                    passiveCdFail.push(`${c.name} (${lk.keyCooldown}) = ${filled || raw}`);
                }
            }
        }

        // 패시브도 하위 아이콘을 가진다 (신드라 P2, 케인 암살자/그림자 패시브,
        //   카타리나 단검 회수, 피들스틱 P2 등 17자리). QWER 쪽과 달리 이 블록은
        //   따로 만들어져서 원래 icons 줄이 안 붙고 있었다 — 2026-08-09 추가.
        // ★ 패시브 stats 는 bin 에 없어서 전부 위키다 (아지르 P 700, 자크 P 700, 질리언 P 875 …). 2026-08-23
        const pStatBlock = (() => {
            const wk = (WIKI_STATS[DD_ID[alias] || alias] || {}).P || {};
            const man = STAT_MANUAL[`${c.name} P`] || {};
            const drop = STAT_DROP[`${c.name} P`] || [];
            if (STAT_MANUAL[`${c.name} P`] || STAT_DROP[`${c.name} P`]) statManualUsed.add(`${c.name} P`);
            const w = Object.assign({}, wk, man);   // 손표 > 위키
            const rows = STAT_ORDER.filter(k => w[k] !== undefined && !drop.includes(k))
                .map(k => { if (man[k] === undefined) wikiStatUsed.push(`${c.name} P ${k} = ${w[k]}`); return `                ${q(k)}: ${q(foldParenNote(w[k]))}`; });
            return rows.length ? `            "stats": {\n${rows.join(',\n')}\n            }` : null;
        })();
        const pIcons = extraIcons.P;
        const pIconLine = pIcons && pIcons.length
            ? `            "icons": [${pIcons.map(x => q(x.url)).join(', ')}],`
            : null;
        if (!passiveNames.length) {
            // 빈칸이 없어도 손으로 쓴 피해량 줄이나 아이콘이 있으면 한 줄짜리로 못 줄인다.
            //   ★ 쿨타임 계산식 이름을 주석으로 내보내야 하는 경우도 마찬가지다 —
            //     한 줄 형식에 `// 이름` 을 붙이면 **뒤의 "cost" 까지 주석이 먹어** 깨진다.
            if (pIconLine || passiveCdCalc
                || (carried.P && (carried.P.v1 !== undefined || carried.P.v2 !== undefined))) {
                lines.push(`        "P": {`);
                if (carried.P && carried.P.v1 !== undefined) lines.push(carried.P.v1);
                if (carried.P && carried.P.v2 !== undefined) lines.push(carried.P.v2);
                lines.push(`            "cooldown": ${q(passiveCd)},${passiveCdCalc ? ` // ${passiveCdCalc}` : ''}`);
                if (pIconLine) lines.push(pIconLine);
                lines.push(`            "cost": "-"${pStatBlock ? ',' : ''}`);
                if (pStatBlock) lines.push(pStatBlock);
                lines.push(`        },`);
            } else if (pStatBlock) {
                lines.push(`        "P": {`);
                lines.push(`            "cooldown": ${q(passiveCd)},`);
                lines.push(`            "cost": "-",`);
                lines.push(pStatBlock);
                lines.push(`        },`);
            } else {
                lines.push(`        "P": { "cooldown": ${q(passiveCd)}, "cost": "-" },`);
            }
        } else {
            lines.push(`        "P": {`);
            let noteNo = 0;   // 각주 번호는 스킬 안에서만 센다
            passiveNames.forEach((name, i) => {
                total++;
                ctx = `${c.name} P / ${name}`;
                const mKeyKo = `${c.name} P / ${name}`;
                const mKeyEn = `${alias} P / ${name}`;
                const manual = MANUAL[mKeyKo] !== undefined ? MANUAL[mKeyKo] : MANUAL[mKeyEn];

                let val, tier;
                critTouched = false;
                if (manual !== undefined) {
                    val = manual;
                    tier = '수동';
                    manualUsed.add(MANUAL[mKeyKo] !== undefined ? mKeyKo : mKeyEn);
                } else {
                    // ★ 패시브는 스킬 랭크가 없다. 레벨별 배열이 아니라 한 값이라 maxRank = 1.
                    //   챔피언 레벨에 따라 변하는 값은 보간 조각(guessPart)이 따로 처리한다.
                    ({ val, tier } = resolveFromPool(name, binSpells.P || [], 1));
                }

                let note = '';
                if (critTouched && val !== null) {
                    const ie = withCritDamage(CRIT_DAMAGE + IE_CRIT_DAMAGE,
                        () => resolveFromPool(name, binSpells.P || [], 1).val);
                    if (ie !== null && String(ie) !== String(val)) {
                        note = critFootnote(++noteNo, val, ie);
                        ieNotes.push(`${c.name} P / ${name}: ${val}  →  ${ie}`);
                    }
                }

                if (val === null) stillUnknown.push(`${c.name} P / ${name}`);
                else if (String(val).includes('(?)')) partial.push(`${c.name} P / ${name} = ${val}`);
                else {
                    filled++;
                    if (tier !== '본체' && tier !== '수동') rescued.push(`${c.name} P / ${name} = ${val}  [${tier}]`);
                }
                previewLines.push(`      P p${i + 1} (${name}) = ${val === null ? '?' : val}`);
                // 레벨 비례 기본 피해량만 정수로 접는다 (위 ROUND_BASE_DAMAGE 주석 참고)
                if (val !== null && ROUND_BASE_DAMAGE.has(`${c.name} P / ${name}`)) {
                    roundBaseUsed.add(`${c.name} P / ${name}`);
                    val = roundLeadingRange(val);
                }
                lines.push(`            "p${i + 1}": ${q((val === null ? '?' : val) + note)}, // ${name}`);
            });
            // 패시브는 원래 v1/v2 를 안 찍는다. 손으로 쓴 게 있을 때만 살려서 넣는다.
            if (carried.P && carried.P.v1 !== undefined) lines.push(carried.P.v1);
            if (carried.P && carried.P.v2 !== undefined) lines.push(carried.P.v2);
            lines.push(`            "cooldown": ${q(passiveCd)},${passiveCdCalc ? ` // ${passiveCdCalc}` : ''}`);
            if (pIconLine) lines.push(pIconLine);
            lines.push(`            "cost": "-"${pStatBlock ? ',' : ''}`);
            if (pStatBlock) lines.push(pStatBlock);
            lines.push(`        },`);
        }
        const seenKey = new Set();

        for (const s of (v1.spells || [])) {
            const key = String(s.spellKey || '').toUpperCase();
            if (!['Q', 'W', 'E', 'R'].includes(key) || seenKey.has(key)) continue;
            seenKey.add(key);

            // 구분선 아래 회색 글씨를 **뒤에** 이어 붙인다 (build 와 같은 순서).
            const desc = ((s.dynamicDescription || '') + (belowMap[key] || ''))
                .replace(/@SpellModifierDescriptionAppend@/gi, '');
            // ★ ':' 포함. build_champion_data.js 의 convertDescription 과 반드시 같아야 한다.
            //   여기가 어긋나면 문장의 {pN} 개수와 값의 pN 개수가 안 맞는다.
            const names = [...new Set([...desc.matchAll(/@([A-Za-z0-9_.*+\-/():]+?)@/g)].map(x => x[1].trim()))];

            const maxRank = rankOf(alias, key);
            const pool = binSpells[key] || [];
            const spell = pool.length ? pool[0].spell : null;   // 본체 (최상위 필드용)

            // 쿨타임을 먼저 구해 둔다. CooldownMultiplierCalculationPart 가 참조한다.
            const lvTop = (arr, r) => {
                if (!Array.isArray(arr)) return null;
                const p = arr.slice(0, r).map(tidy);
                return p.every(x => x === p[0]) ? p[0] : p.join(' / ');
            };
            currentCooldown = lvTop(s.cooldownCoefficients, maxRank) || '-';

            let noteNo = 0;   // 각주 번호는 스킬 안에서만 센다
            const pLines = names.map((name, i) => {
                total++;
                ctx = `${c.name} ${key} / ${name}`;

                // 손으로 확인한 값이 있으면 그걸 쓴다 (한글 이름 / 영문 alias 둘 다 허용)
                const mKeyKo = `${c.name} ${key} / ${name}`;
                const mKeyEn = `${alias} ${key} / ${name}`;
                const manual = MANUAL[mKeyKo] !== undefined ? MANUAL[mKeyKo] : MANUAL[mKeyEn];

                let val, tier;
                critTouched = false;
                if (manual !== undefined) {
                    val = manual;
                    tier = '수동';
                    manualUsed.add(MANUAL[mKeyKo] !== undefined ? mKeyKo : mKeyEn);
                } else {
                    ({ val, tier } = resolveFromPool(name, pool, maxRank));
                }

                // 치명타 배율이 든 값이면 무한의 대검 판본을 만들어 보고, 달라지면 각주를 단다
                let note = '';
                if (critTouched && val !== null) {
                    const ie = withCritDamage(CRIT_DAMAGE + IE_CRIT_DAMAGE,
                        () => resolveFromPool(name, pool, maxRank).val);
                    if (ie !== null && String(ie) !== String(val)) {
                        note = critFootnote(++noteNo, val, ie);
                        ieNotes.push(`${c.name} ${key} / ${name}: ${val}  →  ${ie}`);
                    }
                }

                if (val === null) stillUnknown.push(`${c.name} ${key} / ${name}`);
                else if (String(val).includes('(?)')) partial.push(`${c.name} ${key} / ${name} = ${val}`);
                else {
                    filled++;
                    if (tier !== '본체' && tier !== '수동') rescued.push(`${c.name} ${key} / ${name} = ${val}  [${tier}]`);
                }
                previewLines.push(`      ${key} p${i + 1} (${name}) = ${val === null ? '?' : val}` +
                    (tier === '수동' ? '   <- 수동 확인값'
                        : (tier && tier !== '본체' ? `   <- ${tier} 객체` : '')));
                // 레벨 비례 기본 피해량만 정수로 접는다 (위 ROUND_BASE_DAMAGE 주석 참고)
                if (val !== null && ROUND_BASE_DAMAGE.has(`${c.name} ${key} / ${name}`)) {
                    roundBaseUsed.add(`${c.name} ${key} / ${name}`);
                    val = roundLeadingRange(val);
                }
                return `            "p${i + 1}": ${q((val === null ? '?' : val) + note)}, // ${name}`;
            });

            // bin 의 최상위 필드 (castRange, mana, cooldownTime ...)
            // 이쪽은 DataValues 와 달리 0번부터 실제 1랭크다.
            const lv0 = (arr, r) => {
                if (!Array.isArray(arr) || !arr.length) return null;
                const p = arr.slice(0, r).map(tidy);
                return p.every(x => x === p[0]) ? p[0] : p.join(' / ');
            };
            // ★★ 사거리 배열은 **1번부터**가 랭크 1 이다 (2026-08-14 전수 확인).
            //   mana 는 0번부터가 맞는데(271자리 중 267자리) 사거리만 다르다.
            //   랭크별로 값이 변하는 19자리를 DD `rangeBurn` 과 대조하니
            //   **1번부터가 19/19(100%), 0번부터는 3/19** 였다.
            //   0번은 "스킬 안 찍은 상태" 라 쓰레기가 들어 있다 —
            //   레넥톤 Q 가 `[20, 325, 325, 325, 325, 325, 20]` 이라 1랭크가 **20** 으로 나갔다.
            //   대부분 스킬은 사거리가 랭크 불변이라(전부 같은 값) 여태 안 드러났다.
            const lv1 = (arr, r) => {
                if (!Array.isArray(arr) || arr.length < 2) return null;
                const p = arr.slice(1, r + 1).map(tidy);
                if (!p.length) return null;
                return p.every(x => x === p[0]) ? p[0] : p.join(' / ');
            };
            // ★ `castRangeDisplayOverride` 가 `-1` 이면 "덮어쓸 값 없음" 표식이다.
            //   그대로 쓰면 화면에 `사거리 -1` 이 나간다 (잔나 W 가 그랬다).
            //   DD 는 이걸 부호 없는 정수로 읽어 `4294967295` 를 내보내는데 그쪽이 더 나쁘다.
            //
            // ★★ 20000 이상은 "표시할 사거리 없음" 관용 더미다 (2026-08-14).
            //   **소환사의 협곡이 약 15000 유닛**이라 그보다 큰 값은 물리적으로 맵 전체다.
            //   37자리가 여기 걸린다 — 25000 이 34개 + 갱플랭크 R 30000 · 쉔 R 35000 ·
            //   헤카림 R 50000. 성격이 셋으로 갈리는데 **어느 쪽이든 숫자를 찍으면 안 된다**:
            //     · 진짜 전역기 (애쉬 R, 카직스 R)
            //     · 자기 중심 스킬 (아트록스 Q, 요네 E — 롤위키도 "Varied" 라고 적는다)
            //     · **사거리가 있는데 딴 데 있는 것** (유미 W = 계열 객체 `YuumiWCast` 의 700,
            //       아트록스 E = DataValue `EMaxRange` 의 300)
            //   ★ 마지막 부류를 되찾으려면 손으로 표를 만드는 수밖에 없다.
            //     이름 규칙이 없어서 자동 추출이 안 되고, **계열 객체를 자동으로 쓰면 오히려
            //     망가진다** — 럭스 R 은 override 3340 이 정답인데 계열에 이펙트 미사일
            //     `LuxRVfxMis=1175` 가 있고, 드레이븐 R(진짜 전역)은 계열이 `450` 이다
            //   ★ 3000~12000 은 건드리지 않는다. 갈리오 R `4000/4750/5500`,
            //     스웨인 W `5500~7500`, 케이틀린 R `3500` 처럼 진짜 값이 대부분이다
            //
            // ★★ 반대쪽 끝에도 더미가 있다 — `20` 이하는 "사거리 없음" 이다 (2026-08-14).
            //   **24자리**가 여기 걸리고 전부 **자기 대상 스킬**이다:
            //     `1` 8자리 — 다리우스 Q, 마스터 이 R, 베인 R, 알리스타 R, 카이사 E…
            //     `20` 16자리 — 마스터 이 W(명상) · 럼블 W(고철 방패) · 시비르 E(주문 방어막) ·
            //                   신지드 R(광기의 물약) · 트위치 Q(매복) · 티모 W(신속한 이동)…
            //   **DD `rangeBurn` 도 똑같이 1/20 이라 기계 대조로는 절대 안 걸린다.**
            //   (같은 라이엇 소스끼리는 "일관되게 쓸모없는 값" 을 서로 확인해 줄 뿐이다 —
            //    위 audit 절의 ★★★ 항목 참고)
            //   ★ 경계를 20 으로 둔 이유: 투사체 속도의 근접 더미와 같은 값이고,
            //     40(초가스 E) 위로는 진짜 사거리가 시작된다 — 레오나 Q 100, 신지드 E 125,
            //     닐라 W·라칸 R·말자하 W 150, 케넨 E 170, 렐 R·리븐 R·샤코 R 200.
            //     **40 은 애매해서 일부러 남겼다** (초가스 E 는 강화 평타다)
            const RANGE_GLOBAL = 20000;   // 이상이면 "맵 전체" (맵이 약 15000 유닛)
            const RANGE_SELF = 20;        // 이하면 "자기 자신"
            const okRange = (s) => {
                if (s === null || s === undefined) return false;
                const str = String(s);
                if (/^-/.test(str) || /^0( \/ 0)*$/.test(str)) return false;
                const first = parseFloat(str.split('/')[0]);
                if (!isFinite(first)) return true;
                return first > RANGE_SELF && first < RANGE_GLOBAL;
            };
            //   ★ override 가 무효일 때 **폴백해도 되는 경우와 아닌 경우가 갈린다** (2026-08-14):
            //     · 음수(`-1`) = "덮어쓸 값이 없다" → `castRange` 로 폴백하는 게 맞다 (잔나 W)
            //     · `20` 이하 = **"자기 자신"** → 폴백하면 안 된다. 라이엇이 "사거리 없음" 이라고
            //       **정해 준 것**이라 그 뜻을 살려야 한다. 다리우스 Q 가 `override 1 /
            //       castRange 270` 인데 폴백하면 `사거리 270` 이 나간다 — 롤위키는 이 스킬을
            //       `EFFECT RADIUS 460` 이라고 적지 사거리를 안 적는다. 270 은 아무 값도 아니다
            const ovRange = lv1(spell && spell.castRangeDisplayOverride, maxRank);
            const ovFirst = ovRange === null ? NaN : parseFloat(String(ovRange).split('/')[0]);
            const ovSaysNone = isFinite(ovFirst) && ovFirst >= 0 && ovFirst <= RANGE_SELF;
            const binRange = spell
                ? (okRange(ovRange) ? ovRange : (ovSaysNone ? null : lv1(spell.castRange, maxRank)))
                : null;
            const binMana = spell ? lv0(spell.mana, maxRank) : null;
            // mana 가 없고 manaUiOverride 에만 들어 있는 스펠이 있다 (킨드레드 E).
            //   ★ 단 이건 숫자만 있고 자원 이름이 없다. CD 소모값 문구를 먼저 보고
            //     그게 실패했을 때만 써야 한다. 먼저 쓰면 럼블이 "20 열기" 대신
            //     "20" 이 된다 (열기가 사라짐).
            const binManaUi = spell ? lv0(spell.manaUiOverride, maxRank) : null;
            // ★ 0.05 미만은 "시전시간 없음" 더미다 (2026-08-23). 브라움 E·피오라 W·루시안 R·
            //   킨드레드 Q·탈리야 R·헤카림 R·밀리오 E 0.01, 그라가스 W·잔나 R 0.001, 제라스 Q 0.005,
            //   닐라 W 0.013 — 롤위키는 전부 `CAST TIME: none` 이다. 화면에 `시전시간 0.01` 은 뜻이 없다
            const CAST_TIME_MIN = 0.05;
            const castTime = (spell && typeof spell.mCastTime === 'number' && spell.mCastTime >= CAST_TIME_MIN)
                ? tidy(spell.mCastTime) : null;
            // ★★ 투사체 속도는 **본체에서만** 읽는다 (2026-08-14).
            //   예전엔 pool 전체를 훑어 `> 0` 인 첫 값을 잡았는데, pool 은
            //   본체 → 이름이 겹치는 계열 → **기타(leftovers)** 3등급이고
            //   기타에는 `<챔프>BasicAttack`·`<챔프>CritAttack` 같은 **기본 공격 객체**가
            //   통째로 들어 있다 (getSpellsFromBin 이 leftovers 를 다섯 슬롯 전부에 복사한다).
            //   `missileSpeed` 는 이름으로 찾는 값이 아니라서, 등급을 내려가는 순간
            //   **아무 객체의 값이나 걸린다.** 전수로 재니 634자리 중 310자리가 그랬다:
            //     가렌 W = 347.8  ← GarenCritAttack (가렌 기본 공격 속도)
            //     갈리오 W·E·R = 1000  ← 기본 공격
            //     니코 R = 250000
            //   본체 값이 0 이면 그건 "이 스킬엔 투사체가 없다" 는 뜻이지 "값 없음" 이 아니다.
            //   ★ 20 이하는 근접 판정용 더미다. 본체에 20 이 박힌 자리가 70 개 있는데
            //     (아트록스 E·알리스타 Q·다리우스 R…) 전부 투사체가 없는 스킬이다.
            //   결과: 634 → 324자리. 남는 건 전부 진짜 투사체 스킬이다.
            //   ★ 본체 값이어도 걸러야 하는 더미가 두 종류 더 있다 (2026-08-14):
            //     · `1000000000` — "즉시 도달" 을 뜻한다. 스웨인 Q 가 유일한데
            //       화면에 `투사체 속도 1000000000` 이 그대로 찍히고 있었다
            //     · `347.8` — **라이엇 엔진의 기본 공격 기본값**이다. 값을 안 정한 스킬에
            //       그대로 남는다. 마스터 이 bin 에는 `BasicAttack` 객체가 **아예 없는데도**
            //       본체 `WujuStyle` 에 이 값이 박혀 있다 — 그래서 "기본 공격 속도와 같은가"
            //       로는 못 걸러내고 값 자체를 봐야 한다.
            //       6자리인데 **전부 투사체가 없는 스킬**이다 — 마스터 이 E(자기 버프) ·
            //       블리츠크랭크 R(자기 주변) · 신 짜오 R · 신지드 Q · 초가스 E(강화 평타) ·
            //       하이머딩거 R(자기 강화)
            //   ★ "그 챔피언 기본 공격 속도와 같으면 제외" 라는 규칙은 **쓰면 안 된다.**
            //     14자리가 걸리는데 드레이븐 E(도끼)·케넨 Q(표창)·샤코 E(단검)·엘리스 E(고치)
            //     처럼 **진짜 투사체**가 대부분이다. 원거리 챔피언은 스킬 투사체가 기본 공격과
            //     같은 속도인 게 자연스럽다
            //   ★ `943.8` 2자리도 뺐다 (2026-08-14) — **니달리 R · 엘리스 R 폼 변신**이다.
            //     투사체가 없는 게 확실하고, 소수점이 붙은 걸 보면 347.8 과 같은 계열 값이다
            const MISSILE_DUMMY = [347.8, 943.8];   // 라이엇 엔진 기본값 계열
            const MISSILE_MAX = 100000;             // 이보다 크면 "즉시 도달" 더미
            // ★★ 본체 안에서 legacy `missileSpeed` 와 `mMissileSpec.movementComponent.mSpeed` 가
            //   갈리면 **spec 쪽이 진짜다** (2026-08-24 전수 확인 — 갈리는 본체 20자리 전부에서
            //   롤위키 SPEED 가 spec 과 일치했다: 문도 Q 2000 · 이즈 W 1700 · 릴리아 E 1400 ·
            //   럭스 E 1200 · 라이즈 E 4000 · 세나 W 1200 / R 20000 · 스몰더 Q 1800 · 스웨인 E 1125 ·
            //   탐 켄치 Q 2800 · 벡스 Q/E/R · 자헨 W 2900 · 제리 W 2500 · 조이 E 1850 ·
            //   브라이어 R 2000 · 애쉬 R 1500, legacy 가 맞는 자리 0).
            //   legacy 는 옛 필드라 패치 때 갱신이 안 된 값으로 보인다.
            //   ★ "투사체가 있나" 판정은 legacy(bodyMs)로 그대로 하고 **숫자만** spec 으로 바꾼다.
            //     spec 이 MISSILE_MAX 이상이면 "즉시" 더미다 (아우렐리온 솔 E 2500000) — 칸을 뺀다.
            const bodyLegacy = (spell && typeof spell.missileSpeed === 'number') ? spell.missileSpeed : 0;
            const bodySpec = (() => {
                try {
                    const mc = spell && spell.mMissileSpec && spell.mMissileSpec.movementComponent;
                    const v = mc && ((typeof mc.mSpeed === 'number' && mc.mSpeed) || (typeof mc.mInitialSpeed === 'number' && mc.mInitialSpeed));
                    return (typeof v === 'number' && v > 20) ? v : null;
                } catch (e) { return null; }
            })();
            const bodyMs = bodyLegacy;
            const bodyBest = (bodyLegacy > 20 && bodySpec !== null && Math.abs(bodyLegacy - bodySpec) > 1) ? bodySpec : bodyLegacy;
            const msTidy = (bodyMs > 20 && bodyBest > 20) ? tidy(bodyBest) : null;
            let missileSpeed = (msTidy !== null
                && !MISSILE_DUMMY.some(d => Math.abs(parseFloat(msTidy) - d) < 0.05)
                && parseFloat(msTidy) < MISSILE_MAX) ? msTidy : null;

            // ★★ 본체 `missileSpeed` 가 **틀(기본값)** 인 자리는 진짜 값이 계열 미사일 객체에 있다 (2026-08-23).
            //   롤위키 `SPEED` 전수 대조에서 106자리가 어긋났는데, bin 을 되짚으니 본체 값이
            //   `1200` / `902` / `828.5` / `779.9` / `8700` 다섯 가지 **틀값**에 몰려 있었고
            //   (브랜드 Q 본체 1200 · 제드 Q 본체 902 · 요네 Q 본체 8700 …), 같은 챔피언의
            //   `<본체이름>Missile` / `<본체이름>Mis` 객체의 `mMissileSpec.movementComponent.mSpeed` 가
            //   롤위키와 일치했다 (브랜드 Q 1600 · 제드 Q 1700 · 케이틀린 R 3200 · 베이가 Q 2200 ·
            //   쓰레쉬 Q 1900 · 바루스 R 1500 · 에코 Q 1650 · 조이 Q 1200 — 단일 후보 47자리 중 40 일치).
            //   규칙:
            //     · 본체 값이 **틀값일 때만** 바꾼다. 본체가 0 인 자리는 건드리지 않는다
            //       ("투사체가 있나" 의 판정은 그대로 두고 **숫자만** 고친다 — 카밀 E·조이 R 같은
            //        돌진·순간이동 스킬에도 계열 미사일 객체가 있어서, 새로 채우면 오탐이 난다)
            //     · 후보가 하나면 그 값. 여럿이면 이름이 정확히 `<본체>Missile`·`<본체>Mis` 인 것.
            //       그래도 못 고르면 본체 값을 그대로 두고 `missileFamilyMulti` 로 찍는다
            //     · 칼리스타 Q(계열 3000 / 롤위키 2400)·신드라 E(계열 2000 / 롤위키 2500) 는
            //       STAT_MANUAL 로 덮는다 — 이름 규칙으로는 못 고르는 자리다
            const MISSILE_TEMPLATE = [1200, 902, 828.5, 779.9, 8700];
            // ★ 본체 spec 이 이미 고쳤으면(bodyBest !== bodyLegacy) 계열 탐색을 건너뛴다 —
            //   spec 은 본체 자신의 값이라 계열 후보보다 확실하다 (2026-08-24)
            if (missileSpeed !== null && bodyBest === bodyLegacy && MISSILE_TEMPLATE.some(d => Math.abs(bodyMs - d) < 0.05) && spell) {
                const bodyName = String((pool[0] && pool[0].path) || '').split('/').pop().toLowerCase();
                const fam = [];
                for (const cand of pool.slice(1)) {
                    const nm = String(cand.path || '').split('/').pop();
                    if (!bodyName || !nm.toLowerCase().includes(bodyName)) continue;
                    const mc = cand.spell && cand.spell.mMissileSpec && cand.spell.mMissileSpec.movementComponent;
                    if (!mc) continue;
                    const sp = (typeof mc.mSpeed === 'number' && mc.mSpeed) || (typeof mc.mInitialSpeed === 'number' && mc.mInitialSpeed) || 0;
                    // 300 미만(아칼리 W 장막 150)은 투사체 속도가 아니고, 20000 이상(아칼리 Q 99999)은 "즉시" 더미다
                    if (sp >= 300 && sp < 20000) fam.push({ nm, sp });
                }
                const uniq = [...new Set(fam.map(x => x.sp))];
                let pick = null;
                if (uniq.length === 1) pick = uniq[0];
                else if (uniq.length > 1) {
                    const exact = fam.find(x => /^(.+?)(missile|mis)$/i.test(x.nm) && x.nm.toLowerCase().replace(/(missile|mis)$/, '') === bodyName);
                    if (exact) pick = exact.sp;
                    else missileFamilyMulti.push(`${c.name} ${key}: 본체 ${bodyMs} / 후보 ${fam.map(x => `${x.nm}=${x.sp}`).join(' ')}`);
                }
                if (pick !== null && tidy(pick) !== missileSpeed) {
                    missileFamilyFix.push(`${c.name} ${key}: ${missileSpeed} → ${tidy(pick)}`);
                    missileSpeed = tidy(pick);
                }
            }

            // ★★★ **자기 대상 스킬에는 투사체가 없다** (2026-08-14, 44자리).
            //   `mTargetingTypeData` 가 `Self` / `SelfAoe` 인데 본체에 `missileSpeed` 가
            //   박혀 있는 자리가 무더기로 있었다 — 가렌 E(회전 공격) 700,
            //   다이애나 W(보호막) 1800, 카타리나 W(단검 설치) 2500, 티모 E(평타 강화) 1500…
            //   ★ **타겟팅 타입만 보면 안 된다.** 카이사 Q 도 `Self` 인데 진짜 미사일을 쏜다
            //     (자기 중심에서 부채꼴로 발사). 그래서 "진짜 미사일이 있는가" 를 같이 본다:
            //       · 스펠에 `mMissileEffectName` 이 있다
            //       · 계열에 이름이 `...Missile...` 인 스펠 객체가 있다
            //         (카이사 Q 는 `KaisaQLeftMissile1~6` 이 있고 가렌 E 는 없다)
            //   ★ 검증: **롤위키는 투사체 스킬에만 `SPEED:` 를 적는다.** 다이애나는
            //     Q 에 `SPEED: 1900 • 2100` 이 있고 W 에는 없다. 카타리나도 Q 1600 / W 없음.
            //     우리 판별 결과와 정확히 갈린다
            const ttype = spell && spell.mTargetingTypeData && spell.mTargetingTypeData.__type;
            const selfOnly = (ttype === 'Self' || ttype === 'SelfAoe');
            let hasMissile = false;
            if (spell && selfOnly) {
                const needle = (alias + key).toLowerCase();
                hasMissile = !!(spell.mMissileEffectName) || Object.keys(bin).some(k => {
                    const low = k.toLowerCase();
                    return low.includes(needle) && /missile/.test(low.split('/').pop());
                });
            }
            if (missileSpeed && selfOnly && !hasMissile) {
                missileSpeed = null;
                missileSelfDropped.push(`${c.name} ${key} (${ttype})`);
            }
            // ★ 스킬 폭도 같다 (2026-08-14, 21자리). `mLineWidth` 는 **직선 스킬의 폭**이라
            //   자기 대상 스킬에는 뜻이 없다. 롤위키로 대조해 확인했다 —
            //     가렌 E: `EFFECT RADIUS 325` 만 있고 WIDTH 없음 (우리 폭 160 은 근거가 없다)
            //     오리아나 W·R: `EFFECT RADIUS 225 / 415`, WIDTH 없음 (우리 80)
            //     퀸 R: 폭도 반경도 없다 (채널링 스킬인데 우리는 200 이었다)
            //   초가스 E 만 위키에 `WIDTH 340 - 500 (크기 비례)` 이 있는데 우리 170 과 다르고
            //   크기 비례라 고정값으로 못 적는다. 그래서 이 자리도 같이 뺀다
            // ★★ `mLineWidth` 는 **반지름(절반 폭)** 이다 — 화면에는 2배(전체 폭)로 낸다 (2026-08-23).
            //   롤위키 `WIDTH` 전수 대조(127자리)에서 63자리가 **정확히 우리 값의 2배**였고
            //   (드레이븐 E 130↔260, 문도 Q 60↔120, 드레이븐 R 160↔320, 아리 Q 100↔200…),
            //   나머지도 거기에 대상 히트박스(65)를 더한 값이라 계열이 같다.
            //   "폭" 이라 적고 반지름을 내보내면 읽는 사람이 절반으로 이해한다.
            //   되돌리려면 아래 `* 2` 하나만 지우면 된다.
            let lineWidth = (spell && typeof spell.mLineWidth === 'number' && spell.mLineWidth > 0)
                ? tidy(spell.mLineWidth * 2) : null;
            // ★★ 예외: 롤위키 WIDTH 가 bin mLineWidth 와 **그대로** 같으면 그 자리는 이미 전체 폭이다 —
            //   2배를 하면 이중 적용이 된다 (2026-08-24 인게임 확인: 요네 R 은 위키 225 가 맞고 우리 450 은
            //   Q 조준 길이의 절반이었다). 같은 모양 4자리(요네 R·빅토르 E·로크 Q·크산테 Q).
            //   63자리는 위키가 정확히 2배라 기본 규칙(x2)은 그대로다 — 둘은 구성상 겹치지 않는다.
            {
                const wikiW = parseFloat((((WIKI_STATS[DD_ID[alias] || alias] || {})[key] || {})['스킬 폭']));
                if (lineWidth && isFinite(wikiW) && Math.abs(spell.mLineWidth - wikiW) < 0.5) {
                    widthNoDouble.push(`${c.name} ${key}: mLineWidth ${tidy(spell.mLineWidth)} = 롤위키 → 2배 안 함 (x2 였으면 ${lineWidth})`);
                    lineWidth = tidy(spell.mLineWidth);
                }
            }
            if (lineWidth && selfOnly && !hasMissile) {
                lineWidth = null;
                widthSelfDropped.push(`${c.name} ${key} (${ttype})`);
            }

            // ★★ 돌진 속도 (2026-08-14). bin `DataValues` 에 들어 있다 — 58슬롯.
            //   `missileSpeed` 처럼 최상위 필드가 아니라 **이름으로 찾는 값**이라
            //   본체(`spell`)만 본다 (계열까지 훑으면 투사체 속도 때 겪은 오염이 재현된다).
            //   ★ `DataValues` 는 **0번이 쓰레기라 1번부터** 읽는다 — 0번을 보면
            //     이렐리아 Q 가 `0` 이라 통째로 빠진다 (실제 1400).
            //   ★ 이름에 `Ratio|Divisor|Scaling|Growth|Mod` 가 들어가면 **속도가 아니라 배율**이다.
            //     5자리가 그렇다 (코르키 W · 칼리스타 P · 킨드레드 Q · 람머스 R · 렉사이 E)
            //   ★ 값이 둘 이상인 7자리는 **일부러 건너뛴다.** 범위형(브라이어 R 2500/5000,
            //     람머스 R 900/2000, 키아나 E 600/1200)과 조건부(벨베스 Q 기본/균열,
            //     크산테 W 기본/각성, 라칸 E 자야 여부)가 섞여 있어 하나로 못 고른다
            const DASH_NOT_SPEED = /ratio|divisor|scaling|growth|mod$|modifier/i;
            let dashSpeed = null;
            if (spell) {
                const hits = [];
                for (const d of (spell.DataValues || [])) {
                    const nm = String(d.name || '');
                    if (!/dash|leap/i.test(nm) || !/speed/i.test(nm)) continue;
                    if (DASH_NOT_SPEED.test(nm)) continue;
                    const vals = (d.values || []).slice(1, maxRank + 1);
                    if (!vals.length || typeof vals[0] !== 'number' || vals[0] < 100) continue;
                    hits.push(vals.every(x => x === vals[0])
                        ? tidy(vals[0]) : vals.map(tidy).join(' / '));
                }
                if (hits.length === 1) dashSpeed = hits[0];
                else if (hits.length > 1) dashMulti.push(`${c.name} ${key} = ${hits.join('  |  ')}`);
            }

            const lv = lvTop;
            let cd = currentCooldown;

            // ---- ★ 액티브 스킬의 쿨타임 문장 (2026-08-23) ----
            //   패시브처럼 QWER 에도 `keyCooldown` 이 붙은 스킬이 36자리 있다. 클라이언트는
            //   그 문장을 쿨타임 칸에 그대로 쓴다 — 라칸 E `%i:cooldown% @Cooldown@초`,
            //   갱플랭크 E `재충전 대기시간 @AmmoRechargeTime@초 (최대: @MaxAmmo@개)`,
            //   칼리스타 E `재사용 대기시간 @FakedCooldown@초`, 렝가 Q/W/E `@AmmoRechargeTime@초`.
            //   예전엔 이걸 무시하고 CD 의 cooldownCoefficients 만 찍어서 라칸 E·칼리스타 E·
            //   카서스 Q·탈론 E 가 **쿨타임 0**, 렝가가 **0.25** 로 나갔다 (DD 도 같은 값이라
            //   audit_skill_meta 에는 안 걸렸다 — 롤위키 전수 대조에서 드러남).
            //   ★ `@Cooldown@` 은 스펠 필드가 아니라 "이 스킬의 쿨타임" 별명이다.
            //     값이 0 이 아니면 CD 쿨타임, 0 이면 탄약 재충전 시간(라칸 E)으로 푼다.
            //   ★ 화면은 `쿨타임 ${cd}` 로 찍으므로 앞의 "재사용 대기시간" 은 떼고,
            //     순수 숫자만 남으면 "초" 도 뗀다 (다른 슬롯과 같은 꼴). 문장이 남으면 그대로 둔다.
            {
                const lk = spell && spell.mClientData && spell.mClientData.mTooltipData
                    && spell.mClientData.mTooltipData.mLocKeys;
                const raw = lk && lk.keyCooldown ? strings[String(lk.keyCooldown).toLowerCase()] : null;
                const plain = raw ? String(raw).replace(/<[^>]+>/g, '').replace(/&nbsp;/g, ' ').trim() : '';
                if (plain) {
                    const ammoRecharge = (() => {
                        const r = resolveFromPool('AmmoRechargeTime', pool, maxRank);
                        const v = r && r.val !== null ? String(r.val) : null;
                        return (v && !/^0( \/ 0)*$/.test(v)) ? v : null;
                    })();
                    const cdNonZero = cd && cd !== '-' && !/^0( \/ 0)*$/.test(cd);
                    let ok = true;
                    ctx = `${c.name} ${key} / keyCooldown`;
                    let filled = plain
                        .replace(/%i:cooldown%/g, '재사용 대기시간')
                        .replace(/%i:[a-zA-Z0-9_]+%/g, '')
                        .replace(/@([A-Za-z0-9_.*+\-/():]+?)@/g, (m0, nm) => {
                            const want = nm.trim();
                            if (/^cooldown$/i.test(want)) {
                                if (cdNonZero) return cd;
                                if (ammoRecharge) return ammoRecharge;
                                ok = false; return m0;
                            }
                            const r = resolveFromPool(want, pool, maxRank);
                            if (!r || r.val === null) { ok = false; return m0; }
                            return r.val;
                        })
                        .replace(/\s+/g, ' ').trim();
                    if (ok && filled && !/[@?{]/.test(filled)) {
                        filled = filled.replace(/^재사용 대기시간\s*/, '').replace(/\(\s*재사용 대기시간\s*/g, '(재사용 ');
                        if (/^[\d.\s/~()레벨에 따라]+초$/.test(filled)) filled = filled.replace(/초$/, '').trim();
                        if (filled === '재사용 대기시간 없음' || filled === '없음') filled = '없음';
                        if (filled) {
                            if (filled !== cd) activeCdMade.push(`${c.name} ${key}: ${cd} → ${filled}`);
                            cd = filled;
                        }
                    } else if (ammoRecharge && !cdNonZero) {
                        // 문장은 못 풀었지만 탄약 스킬이면 재충전 시간이 곧 쿨타임이다 (바드 W `@f7@`)
                        activeCdMade.push(`${c.name} ${key}: ${cd} → ${ammoRecharge} (재충전 시간으로 대체)`);
                        cd = ammoRecharge;
                    } else {
                        const man = CD_MANUAL[`${c.name} ${key}`];
                        if (man !== undefined) { activeCdMade.push(`${c.name} ${key}: ${cd} → ${man} (CD_MANUAL)`); cd = man; }
                        else activeCdFail.push(`${c.name} ${key} (${lk.keyCooldown}) = ${filled || plain}`);
                    }
                }
            }
            // CD_MANUAL 은 문장을 풀었든 못 풀었든 최우선이다 (야스오 Q 처럼 풀리긴 하는데 수치가 부족한 자리)
            if (CD_MANUAL[`${c.name} ${key}`] !== undefined && cd !== CD_MANUAL[`${c.name} ${key}`]) {
                activeCdMade.push(`${c.name} ${key}: ${cd} → ${CD_MANUAL[`${c.name} ${key}`]} (CD_MANUAL)`);
                cd = CD_MANUAL[`${c.name} ${key}`];
            }
            const costJ = lv(s.costCoefficients, maxRank);
            let cost = (costJ && !/^0( \/ 0)*$/.test(costJ)) ? costJ : '';
            // ★ 마나 스킬이어도 소모값 문장에 **숫자 말고 다른 말**이 붙어 있으면 문장을 쓴다 (2026-08-23).
            //   클라이언트가 그 문장을 소모값 칸에 그대로 찍는다 — 소라카 W `최대 체력의 10%, 마나 40`,
            //   로크 W `초당 마나 50, 현재 체력 N%`, 애니비아 R `60+초당 마나 N`, 하이머딩거 Q
            //   `포탑 부품 1 + 마나 20`, 징크스 Q `로켓 1발당 마나 20`, 아무무 W `초당 …`.
            //   예전엔 costCoefficients 가 0 이 아니면 숫자만 찍어서 체력 소모·"초당" 이 통째로 빠졌다.
            //   ★ 범용 문장 `@AbilityResourceName@ @Cost@` 와 `마나 @Cost@` 는 숫자와 같은 말이라 건너뛴다.
            //   ★ `@AbilityResourceName@` 은 DD `partype`(마나/기력…) 으로 푼다 (아무무 W `초당 마나 8`).
            if (cost && s.cost) {
                const txt0 = String(s.cost).replace(/<[^>]+>/g, '').replace(/\s+/g, ' ').trim();
                const generic = /^(@AbilityResourceName@|마나)\s*@[A-Za-z0-9_.]+@$/i.test(txt0) || !/@/.test(txt0);
                if (!generic) {
                    const resName = (ddPartype[String(c.id)] || ddPartype[c.alias] || "").trim();
                    let ok = true;
                    const out = txt0
                        .replace(/@AbilityResourceName@/gi, () => { if (!resName) ok = false; return resName; })
                        .replace(/@([A-Za-z0-9_.*+\-/():]+?)@/g, (m0, nm) => {
                            const want = nm.trim();
                            if (/^cost$/i.test(want)) return cost;
                            ctx = `${c.name} ${key} / cost:${want}`;
                            const r = resolveFromPool(want, pool, maxRank);
                            if (!r || r.val === null) { ok = false; return m0; }
                            return r.val;
                        })
                        .replace(/\s+/g, ' ').trim();
                    if (ok && !/[@?]/.test(out)) {
                        // 괄호 안 "현재값" 은 본문 쪽 규칙과 같이 버린다 (아래 dropRuntimeParen 주석 참고)
                        const cleaned = out.replace(/\s*\(([^()]+)\)/g, (m0, inner) => {
                            const t = inner.trim();
                            return (t.startsWith('+') || /^[\d\s./%~-]+$/.test(t)) ? m0 : '';
                        }).replace(/\s+/g, ' ').trim();
                        if (cleaned !== cost) { costSentence.push(`${c.name} ${key}: ${cost} → ${cleaned}`); cost = cleaned; }
                    } else {
                        costTextFail.push(`${c.name} ${key} = ${txt0}`);
                    }
                }
            }
            if (!cost) {
                if (binMana && !/^0( \/ 0)*$/.test(binMana)) cost = binMana;
                else if (/없음/.test(s.cost || '')) cost = '-';
            }
            // ★ 마나가 아닌 자원(기력·체력·열기·야성 등)은 costCoefficients 가 전부 0이다.
            //   대신 CD 의 cost 문구에 값이 들어 있고, 그 문구도 설명문과 똑같은
            //   @플레이스홀더@ 문법을 쓴다. 그래서 같은 방식으로 풀 수 있다.
            //   (예: 문도 Q "체력 @HealthCost@", 럼블 W "@HeatCost@ 열기")
            if (!cost && s.cost) {
                const txt = String(s.cost).replace(/<[^>]+>/g, '').replace(/\s+/g, ' ').trim();
                const fillOne = (t) => {
                    let ok = true;
                    const out = t.replace(/@([A-Za-z0-9_.*+\-/():]+?)@/g, (m0, nm) => {
                        const want = nm.trim();
                        ctx = `${c.name} ${key} / cost:${want}`;
                        const r = resolveFromPool(want, pool, maxRank);
                        if (!r || r.val === null) { ok = false; return m0; }
                        return r.val;
                    });
                    return { ok, out };
                };

                if (/기본 지속 효과/.test(txt)) {
                    cost = '-';                     // 지속 효과라 소모가 없다
                } else if (txt) {
                    // ★ 괄호 안은 "절대값 참고" 라서 없어도 뜻이 통한다.
                    //   못 풀면 괄호째 버리고 본문만 살린다. 예전엔 하나라도 실패하면
                    //   문구 전체를 버려서 자크 Q/W/E 와 블라디미르 W 가 통째로 빈칸이었다.
                    //   (자크는 HPCost 8%/4%/4% 가 멀쩡히 있는데 옆의
                    //    @HealthCostTooltip@ 하나 때문에 다 날아갔다)
                    //   블라디미르 E 처럼 괄호가 본문과 같은 말을 반복하는 경우도 같이 정리된다.
                    const stripped = txt.replace(/\s*\([^()]*@[^()]*\)/g, (paren) =>
                        fillOne(paren).ok ? paren : '');
                    const { ok, out } = fillOne(stripped.replace(/\s+/g, ' ').trim());
                    // ★ 소모값 괄호는 인게임에서 **"지금 이 순간의 절대값"** 을 보여주는 자리다.
                    //   자크 Q 가 인게임에선 "현재 체력의 8% (56) 소모" 로 뜬다 — 56 은 그때의
                    //   현재 체력 695 의 8% 다. 우리는 고정값이 없어서 수식이 그대로 튀어나온다
                    //   ("0.08 x 최대 체력의 100% x 현재 체력의 100%"). 그건 버리는 게 맞다.
                    //   ★ 단 계수 괄호는 진짜 정보라 살려야 한다 — 올라프 E "(+ 총 공격력의 50%)".
                    //   그래서 **`+` 로 시작하거나(계수) 순수 숫자면(절대값) 살리고 나머지는 버린다.**
                    //   (`STAT_NAMES` 에 14=현재 체력을 넣기 전에는 수식이 안 풀려서 괄호가
                    //    통째로 버려졌고, 그래서 우연히 결과가 맞았다. 이름을 알려주니 오히려
                    //    나빠져서 이 규칙을 넣었다 — 2026-08-10)
                    const dropRuntimeParen = (t) => t.replace(/\s*\(([^()]+)\)/g, (m0, inner) => {
                        const s = inner.trim();
                        return (s.startsWith('+') || /^[\d\s./%~-]+$/.test(s)) ? m0 : '';
                    });
                    // 괄호가 본문과 같은 말을 반복하면 지운다.
                    //   블라디미르 E 가 "최대 체력의 8% (최대 체력의 8%) 소모" 로 나왔다.
                    const dedup = (t) => {
                        let r = t;
                        for (const m of [...t.matchAll(/\s*\(([^()]+)\)/g)]) {
                            const inner = m[1].trim();
                            const without = r.replace(m[0], '');
                            if (inner && without.includes(inner)) r = without;
                        }
                        return r.replace(/\s+/g, ' ').trim();
                    };
                    if (ok) { cost = dedup(dropRuntimeParen(out)); costFromText.push(`${c.name} ${key} = ${cost}`); }
                    else costTextFail.push(`${c.name} ${key} = ${txt}`);
                }
                // 문구로도 못 채웠으면 manaUiOverride 를 쓴다 (킨드레드 E = 50).
                if (!cost && binManaUi && !/^0( \/ 0)*$/.test(binManaUi)) cost = binManaUi;
                // ★ CD 의 범용 문구 "@AbilityResourceName@ @Cost@" 는 자원 이름 자체가
                //   플레이스홀더인데 CD 에 그 값이 없다. 소모값도 0 이면 실제로 안 쓰는
                //   스킬이다 (사미라 R·시비르 E·탐 켄치 E 인게임 확인). 그래서 "-" 로 본다.
                if (!cost && /@AbilityResourceName@/.test(String(s.cost))) cost = '-';
            }
            // 사거리는 클라이언트 표시값(castRangeDisplayOverride)을 우선한다
            const v1Range = lv(s.range, maxRank);
            const rng = okRange(binRange) ? binRange : v1Range;
            const hasRange = okRange(rng);

            const fnCtr = { n: 0 };   // ★ 각주 번호는 스킬 단위로 이어진다
            lines.push(`        "${key}": {`);
            if (pLines.length) lines.push(pLines.join('\n'));
            // 손으로 쓴 게 있으면 원문 그대로, 없으면 빈칸.
            const vv = carried[key] || {};
            lines.push(vv.v1 !== undefined ? vv.v1 : `            "v1": "", // 구분선 아래 피해량 줄 (직접 작성)`);
            lines.push(vv.v2 !== undefined ? vv.v2 : `            "v2": "",`);
            lines.push(`            "cooldown": ${q(foldParenNote(cd, fnCtr))},`);
            if (COST_MANUAL[`${c.name} ${key}`] !== undefined) {
                costSentence.push(`${c.name} ${key}: ${cost} → ${COST_MANUAL[`${c.name} ${key}`]} (COST_MANUAL)`);
                cost = COST_MANUAL[`${c.name} ${key}`];
            }
            lines.push(`            "cost": ${q(foldParenNote(cost, fnCtr))},`);
            // 같은 스킬에 아이콘이 여러 개 달리는 자리 (재시전·취소·진화·1·2·3타).
            // 첫 번째 폼 라벨. 폼이 두 개인 챔피언의 기본 스킬에만 붙는다.
            //   ★ 폼2 로 대체되는 슬롯에만 붙인다 — 클레드는 Q 만 바뀌므로 W·E·R 엔 안 붙는다
            const f1 = FORM2[alias];
            if (f1 && f1.label1 && f1[key]) lines.push(`            "form1": ${q(f1.label1)},`);
            // 기본 아이콘 교체 (벨베스 Q). app.js 가 `img` 가 있으면 DD 것 대신 쓴다.
            const ov = (ICON_OVERRIDE[alias] || {})[key];
            if (ov) lines.push(`            "img": ${q(`${ICON_BASE}assets/characters/${alias.toLowerCase()}/hud/icons2d/${ov}.png`)},`);
            // 배열 템플릿 0번 파트 전용 아이콘 (유나라 Q). app.js 가 img 대신 이걸 쓴다.
            const bi = (BODY_ICON[alias] || {})[key];
            if (bi) lines.push(`            "imgBody": ${q(`${ICON_BASE}assets/characters/${alias.toLowerCase()}/hud/icons2d/${bi}.png`)},`);
            const ei = extraIcons[key];
            if (ei && ei.length) {
                lines.push(`            "icons": [${ei.map(x => q(x.url)).join(', ')}],`);
            }
            // ★ 손 표를 마지막에 얹는다 (위 STAT_MANUAL / STAT_DROP 주석 참고).
            const statKey = `${c.name} ${key}`;
            const manualStat = STAT_MANUAL[statKey] || {};
            const dropStat = STAT_DROP[statKey] || [];
            if (STAT_MANUAL[statKey] || STAT_DROP[statKey]) statManualUsed.add(statKey);
            const wikiStat = (WIKI_STATS[DD_ID[alias] || alias] || {})[key] || {};   // alias 가 DD id 와 다른 건 FiddleSticks 뿐
            // ★ 자기 중심 스킬의 사거리 중복 제거 (2026-08-24, 렝가 R 지적에서 일반화).
            //   위키에 target range 가 없고(= 조준하는 스킬이 아니다) 효과 범위가 bin 사거리와 같은 값이면
            //   그 숫자의 정체는 "자기 주변 반경" 이다 — 사거리 칸을 빼고 효과 범위로만 보여 준다.
            //   가렌 E(325·325)·다이애나 R(475·475)·워윅 W(4000·4000) 등 55자리가 이 모양이었다.
            const selfRadiusDup = (name, val) =>
                name === '사거리' && val && wikiStat['사거리'] === undefined
                && wikiStat['효과 범위'] !== undefined && String(wikiStat['효과 범위']) === String(val)
                && manualStat['사거리'] === undefined;
            const put = (rows, name, val) => {
                if (dropStat.includes(name)) return;
                if (selfRadiusDup(name, val)) return;
                let v = manualStat[name] !== undefined ? manualStat[name] : val;
                // ★ bin 에 없으면 위키. 속도는 둘 중 하나라도 bin 에 있으면 위키를 안 본다 (같은 칸이다)
                if (!v && wikiStat[name] !== undefined) {
                    const speedPair = (name === '투사체 속도' || name === '돌진 속도') && (missileSpeed || dashSpeed);
                    // ★ 위키가 0.05 미만 시전시간을 그대로 적는 자리가 있다 (말자하 R 0.005 · 미포 R 0.001).
                    //   bin 쪽 더미 컷(CAST_TIME_MIN)과 같은 이유로 화면에 안 싣는다 (2026-08-24)
                    const tinyCast = name === '시전시간' && parseFloat(wikiStat[name]) < CAST_TIME_MIN;
                    if (!speedPair && !tinyCast) { v = wikiStat[name]; wikiStatUsed.push(`${c.name} ${key} ${name} = ${v}`); }
                }
                if (v) rows.push(`                ${q(name)}: ${q(foldParenNote(v, fnCtr))}`);
            };

            const statRows = [];
            // 순서는 STAT_ORDER 와 같아야 한다 (공간 → 시간)
            put(statRows, '사거리', hasRange ? rng : null);
            put(statRows, '효과 범위', null);
            put(statRows, '스킬 폭', lineWidth);
            put(statRows, '시전시간', castTime);
            put(statRows, '투사체 속도', missileSpeed);
            put(statRows, '돌진 속도', dashSpeed);
            if (statRows.length) lines.push(`            "stats": {\n${statRows.join(',\n')}\n            }`);
            // 마지막 항목 뒤 쉼표 제거
            const last = lines[lines.length - 1];
            if (last.endsWith(',') && !last.endsWith('},')) lines[lines.length - 1] = last.slice(0, -1);
            lines.push(`        },`);
        }

        // ---- 두 번째 폼 (니달리 쿠거 등) ----
        //   CD v1 에는 없는 스킬이라 문장은 build 가 stringtable 에서 만들어 뒀다.
        //   여기서는 그 문장의 @이름@ 을 읽어 값을 채운다.
        const f2 = FORM2[alias];
        if (f2 && bin) {
            for (const slot of ['Q', 'W', 'E', 'R']) {
                const objName = f2[slot];
                if (!objName) continue;
                const obj = findSpellObj(bin, alias, objName);
                const lk = obj && obj.mSpell.mClientData && obj.mSpell.mClientData.mTooltipData
                    && obj.mSpell.mClientData.mTooltipData.mLocKeys;
                const raw = lk && lk.keyTooltip ? strings[String(lk.keyTooltip).toLowerCase()] : null;
                if (!raw) continue;

                const f2Names = [...new Set([...String(raw + (textOfSpellObj(obj, strings) || ''))
                    .replace(/@SpellModifierDescriptionAppend@/gi, '')
                    .matchAll(/@([A-Za-z0-9_.*+\-/():]+?)@/g)].map(x => x[1].trim()))];

                // 이 폼의 본체를 pool 맨 앞에 둔다. 자기 값을 먼저 찾게 하려는 것.
                const f2Rank = rankOf(alias, slot);
                const f2Pool = [{ spell: obj.mSpell, tier: '본체', path: objName },
                                ...(binSpells[slot] || [])];

                lines.push(`        "${slot}2": {`);
                f2Names.forEach((name, i) => {
                    total++;
                    ctx = `${c.name} ${slot}2 / ${name}`;
                    const { val, tier } = resolveFromPool(name, f2Pool, f2Rank);
                    if (val === null) stillUnknown.push(ctx);
                    else if (String(val).includes('(?)')) partial.push(`${ctx} = ${val}`);
                    else {
                        filled++;
                        if (tier !== '본체') rescued.push(`${ctx} = ${val}  [${tier}]`);
                    }
                    lines.push(`            "p${i + 1}": ${q(val === null ? '?' : val)}, // ${name}`);
                });
                const vv2 = carried[slot + '2'] || {};
                lines.push(vv2.v1 !== undefined ? vv2.v1 : `            "v1": "", // 구분선 아래 피해량 줄 (직접 작성)`);
                lines.push(vv2.v2 !== undefined ? vv2.v2 : `            "v2": "",`);
                // ★ 두 번째 폼 쿨타임은 bin Cooldown 인데, 이 배열도 0번이 "미습득" 더미다
                //   (엘리스 EliseSpiderE = [23,22,21,20,19,18,17], 롤위키 22~18).
                //   rank1=false 로 읽어서 엘리스 E2·제이스 W2·나르 Q2/E2·렉사이 E2 가
                //   전부 한 칸 높은 값으로 나가고 있었다 (2026-08-24 수정, 위키 5자리 전부 일치 확인).
                //   ★ 2차 폼은 본체의 keyCooldown 문장 처리(위 36자리 블록)를 안 타므로
                //     문장이 있는 자리는 CD_MANUAL 로 준다 — 지금은 클레드 Q2 하나다.
                const f2CdMan = CD_MANUAL[`${c.name} ${slot}2`];
                lines.push(`            "cooldown": ${q(f2CdMan !== undefined ? f2CdMan
                    : (fieldToText(findField(obj.mSpell, 'Cooldown'), f2Rank, 1, true) || '-'))},`);
                lines.push(`            "cost": ${q(fieldToText(findField(obj.mSpell, 'mana'), f2Rank, 1, false) || '-')},`);
                // 이름이 "두번째폼 / 첫번째폼" 형태인 경우가 있다 (니달리·엘리스·나르).
                const f2NameRaw = lk.keyName ? strings[String(lk.keyName).toLowerCase()] : '';
                // 못 찾으면 비워 둔다. app.js 가 Data Dragon 합친 이름 뒷조각으로 채운다.
                lines.push(`            "name": ${q(String(f2NameRaw || '').split('/')[0].trim())},`);
                lines.push(`            "form": ${q(f2.label)},`);
                lines.push(`            "icon": ${q(iconUrlOf(obj) || '')}`);
                lines.push(`        },`);
            }
        }

        entries.push(`    "${ddId(alias)}": { // ${c.name}\n${lines.join('\n')}\n    },`);

        if (PREVIEW.includes(alias)) {
            console.log(`--- ${c.name} (${alias}) ---`);
            previewLines.forEach(l => console.log(l));
            console.log('');
        } else if (n % 20 === 0) {
            console.log(`  (${n + 1}/${champions.length})`);
        }

        // ★ 캐시에서 나왔으면 CD 를 안 때린 것이라 기다릴 필요가 없다.
        //   173명 x 120~150ms = 20~26초를 통째로 아낀다.
        if (cacheStats().fromCache === 0) await sleep(DELAY);
    }

    console.log('\n' + '='.repeat(60));
    console.log(`완전히 채워짐: ${filled}/${total} (${total ? Math.round(filled / total * 100) : 0}%)`);
    console.log(`  그중 딸린 객체에서 건진 것: ${rescued.length}개`);
    console.log(`계수 일부 미해결: ${partial.length}개`);
    console.log(`아예 못 채움: ${stillUnknown.length}개`);

    if (rescued.length) {
        console.log(`\n[딸린 객체에서 건진 값] ${rescued.length}개 (앞 25개) — 수치가 맞는지 눈으로 확인할 것:`);
        rescued.slice(0, 25).forEach(x => console.log(`  ${x}`));
    }

    console.log(`손으로 확인한 값 적용: ${manualUsed.size}/${Object.keys(MANUAL).length}개`);
    const manualUnused = Object.keys(MANUAL).filter(k => !manualUsed.has(k));
    if (manualUnused.length) {
        console.log(`\n[안 쓰인 수동 확인값] 키 오타일 수 있습니다:`);
        manualUnused.forEach(k => console.log(`  ${k}`));
    }

    if (guessedList.length) {
        console.log(`\n[모양으로 추측해서 푼 값] ${guessedList.length}개 — 인게임 툴팁과 대조할 것:`);
        guessedList.forEach(x => console.log(`  ${x}`));
    }

    // ? 가드에 걸려 Data Dragon 툴팁으로 폴백될 스킬 목록.
    //   못 채운 값이든 (?) 가 남은 값이든, 그 문장은 통째로 버려진다.
    const fallback = new Map();
    const noteFallback = (line) => {
        const skill = String(line).split(' / ')[0];
        fallback.set(skill, (fallback.get(skill) || 0) + 1);
    };
    stillUnknown.forEach(noteFallback);
    partial.forEach(noteFallback);
    if (fallback.size) {
        console.log(`\n[DD 폴백 예정 스킬] ${fallback.size}개 — 이 스킬들은 문장이 통째로 버려지고 Data Dragon 툴팁이 뜬다:`);
        [...fallback.entries()].sort().forEach(([k, n]) => console.log(`  ${k}   (빈칸 ${n}개)`));
    }

    if (zeroDrop.length) {
        console.log(`\n[0으로 보고 버린 항] ${zeroDrop.length}개 — 참조 대상이 bin 에 없는 자리:`);
        zeroDrop.forEach(x => console.log(`  ${x}`));
    }

    if (caseList.length) {
        console.log(`\n[대소문자가 어긋나서 찾아낸 이름] ${caseList.length}종 — 툴팁 철자와 bin 철자가 다른 자리:`);
        caseList.forEach(x => console.log(`  ${x}`));
    }

    if (costFromText.length || costTextFail.length) {
        console.log(`\n[소모값 문구에서 채운 값] ${costFromText.length}개 — 마나가 아닌 자원이다. 눈으로 확인할 것:`);
        costFromText.forEach(x => console.log(`  ${x}`));
        if (costTextFail.length) {
            console.log(`  -- 못 푼 것 ${costTextFail.length}개 (직접 채워야 함):`);
            costTextFail.forEach(x => console.log(`     ${x}`));
        }
    }

    if (crossHits.size || crossMiss.length) {
        console.log(`\n[@spell.X:Y@ 교차 참조] 풀어냄 ${crossHits.size}자리 / 실패 ${crossMiss.length}자리`);
        crossMiss.forEach(x => console.log(`  실패: ${x}`));
    }

    if (hashHits.size) {
        console.log(`\n[FNV 해시로 찾아낸 이름] ${hashHits.size}자리 — CD 가 이름을 못 푼 {해시} 자리다:`);
        [...hashHits].forEach(x => console.log(`  ${x}`));
    }

    if (viaField.length) {
        console.log(`\n[최상위 / mAmmo 필드에서 건진 값] ${viaField.length}개 — 새 경로다. 인게임 툴팁과 대조할 것:`);
        viaField.forEach(x => console.log(`  ${x}`));
    }

    if (dotList.length) {
        console.log(`\n[".숫자" 꼬리를 처리한 값] ${dotList.length}개 — 자릿수 해석이 확정이 아니다. 대조할 것:`);
        console.log(`  (틀렸으면 파일 위쪽 DOT_AS_PRECISION 을 false 로)`);
        dotList.forEach(x => console.log(`  ${x}`));
    }

    if (unknownStats.size) {
        console.log('\n[모르는 스탯 번호] 아래 스킬을 인게임에서 열어 보고 STAT_NAMES 에 추가:');
        [...unknownStats.entries()].sort((a, b) => b[1].count - a[1].count)
            .forEach(([id, e]) => {
                console.log(`  mStat ${id}  (${e.count}회)`);
                e.where.forEach(w => console.log(`      ${w}`));
            });
    }
    if (unknownParts.size) {
        console.log('\n[모르는 계산식 종류] 필드 이름을 보고 정체를 판단할 것:');
        [...unknownParts.entries()].sort((a, b) => b[1].count - a[1].count)
            .forEach(([t, e]) => {
                console.log(`  ${t}  (${e.count}회, 그중 추측 성공 ${e.solved}회)`);
                console.log(`      필드: ${e.keys}`);
                console.log(`      내용: ${e.sample}`);
                e.where.forEach(w => console.log(`      ${w}`));
            });
    }
    if (binFails.length) {
        console.log(`\n[bin 로드 실패] ${binFails.length}건: ${binFails.slice(0, 15).join(', ')}`);
    }
    if (partial.length) {
        console.log(`\n[계수가 일부 빠진 값] ${partial.length}개 (앞 15개):`);
        partial.slice(0, 15).forEach(x => console.log(`  ${x}`));
    }
    if (stillUnknown.length) {
        console.log(`\n[못 채운 값] ${stillUnknown.length}개 (앞 20개):`);
        stillUnknown.slice(0, 20).forEach(x => console.log(`  ${x}`));
    }
    if (ieNotes.length) {
        console.log(`\n[무한의 대검 각주] ${ieNotes.length}자리 — 치명타 피해량 200% -> 230% 로 값이 달라지는 곳:`);
        ieNotes.forEach(x => console.log(`  ${x}`));
    }

    {
        const unusedStat = [...Object.keys(STAT_MANUAL), ...Object.keys(STAT_DROP)]
            .filter(k => !statManualUsed.has(k));
        if (unusedStat.length) {
            console.log(`\n[주의] STAT_MANUAL / STAT_DROP 에 안 쓰인 키 ${unusedStat.length}개 — 오타이거나 슬롯이 사라진 것:`);
            unusedStat.forEach(x => console.log(`  ${x}`));
        }
    }

    if (passiveCdNoteMade.length) {
        console.log(`\n[패시브 쿨타임 각주] ${passiveCdNoteMade.length}자리 — 인게임 툴팁 값과 실제가 다른 곳:`);
        passiveCdNoteMade.forEach(x => console.log(`  ${x}`));
    }

    if (missileSelfDropped.length) {
        console.log(`
[자기 대상 스킬의 투사체 속도 제거] ${missileSelfDropped.length}자리 — 타겟팅이 Self/SelfAoe 이고 미사일 객체가 없다:`);
        console.log('  ' + missileSelfDropped.join(', '));
    }

    if (widthSelfDropped.length) {
        console.log(`\n[자기 대상 스킬의 스킬 폭 제거] ${widthSelfDropped.length}자리:`);
        console.log('  ' + widthSelfDropped.join(', '));
    }
    if (widthNoDouble.length) {
        console.log(`
[스킬 폭 2배 안 함] ${widthNoDouble.length}자리 — 롤위키 WIDTH 가 mLineWidth 와 같은 자리 (이중 적용 방지):`);
        widthNoDouble.forEach(x => console.log('  ' + x));
    }

    if (dashMulti.length) {
        console.log(`\n[돌진 속도 후보가 둘 이상] ${dashMulti.length}자리 — 건너뛰었다. 어느 값을 쓸지 정해야 한다:`);
        dashMulti.forEach(x => console.log(`  ${x}`));
    }

    if (passiveCdMade.length || passiveCdFail.length) {
        {
            const cnt = {};
            wikiStatUsed.forEach(x => { const k = x.split(' ').slice(2, -2).join(' '); cnt[k] = (cnt[k] || 0) + 1; });
            console.log(`\n[위키 우상단 값] ${wikiStatUsed.length}자리 — bin 에 없어서 wiki_stats.json 으로 채웠다:`, JSON.stringify(cnt));
        }
        console.log(`\n[투사체 속도 계열 보정] ${missileFamilyFix.length}자리 — 본체 틀값을 계열 미사일 객체 값으로 (2026-08-23):`);
        missileFamilyFix.forEach(x => console.log(`  ${x}`));
        if (missileFamilyMulti.length) {
            console.log(`  -- 후보가 여럿이라 본체 값을 그대로 둔 것 ${missileFamilyMulti.length}자리:`);
            missileFamilyMulti.forEach(x => console.log(`     ${x}`));
        }
        console.log(`\n[액티브 쿨타임 문장] ${activeCdMade.length}자리 — bin keyCooldown 문장으로 바꿨다 (2026-08-23):`);
        activeCdMade.forEach(x => console.log(`  ${x}`));
        if (activeCdFail.length) {
            console.log(`  -- keyCooldown 은 있는데 못 푼 것 ${activeCdFail.length}자리 (CD 쿨타임을 그대로 둔다):`);
            activeCdFail.forEach(x => console.log(`     ${x}`));
        }
        console.log(`\n[소모값 문장] ${costSentence.length}자리 — 마나 스킬인데 문장에 다른 말이 있어 문장을 썼다:`);
        costSentence.forEach(x => console.log(`  ${x}`));
        console.log(`\n[패시브 쿨타임] ${passiveCdMade.length}명 — bin 의 keyCooldown 에서 채웠다. 인게임 툴팁과 대조할 것:`);
        passiveCdMade.forEach(x => console.log(`  ${x}`));
        if (passiveCdFail.length) {
            console.log(`  -- keyCooldown 은 있는데 못 푼 것 ${passiveCdFail.length}명:`);
            passiveCdFail.forEach(x => console.log(`     ${x}`));
        }
    }

    if (WRITE) {
        fs.writeFileSync(OUT_VALUES,
            valuesPrelude + `const customValues = {\n${entries.join('\n')}\n};\n`, 'utf8');
        console.log(`\n생성됨: ${OUT_VALUES}`);
        console.log('확인한 뒤 custom_values.js 로 이름을 바꾸세요.');
    } else {
        console.log('\n미리보기였습니다. 결과가 괜찮으면 --write 를 붙여 다시 실행하세요.');
    }
}

main().catch(e => { console.error('실패:', e); process.exit(1); });