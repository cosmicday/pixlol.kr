# 방송 탭 — 롤 생방송 목록 (2026-09-17)

`/broadcast`. SOOP · 치지직 · 유튜브에서 **리그 오브 레전드 생방송**을 모아 시청자 순으로 보여준다.
카드를 누르면 그 플랫폼 방송이 새 탭으로 열린다.

| 자리 | 무엇 |
|---|---|
| `server.js` 「방송」 절 | 수집 3개 + `GET /api/broadcast` |
| `server.js` `ytChannelSchema` | 유튜브 자동 명단 (`ytchannels`) |
| `broadcast_channels.js` (루트, git 에 있음) | 유튜브 손 명단 — `include` / `exclude` |
| `public/app.js` `showBroadcast()` · `renderBroadcast()` | 화면 |
| `public/style.css` 「방송」 절 | `.bc-*` 카드 격자. 칩·안내 줄·더 보기는 e스포츠 것(`.es-*`)을 그대로 쓴다 |

## 키 (`.env` · Railway Variables 둘 다)

```
SOOP_CLIENT_ID=        # SOOP Developers. 사용 범위: 「방송 리스트」「카테고리 리스트」 둘만 (둘 다 Public)
SOOP_CLIENT_SECRET=    # 지금 코드는 안 쓴다 (목록 조회는 client_id 만 필요)
CHZZK_CLIENT_ID=       # 치지직 개발자센터. scope 는 하나도 안 골랐다 — 라이브 목록은 Client 인증이라 scope 가 필요 없다
CHZZK_CLIENT_SECRET=
YOUTUBE_API_KEY=       # Google Cloud 프로젝트 DOGU-GG. API 제한 = YouTube Data API v3 만. 결제 계정 없음(무료)
```

- **키가 없는 플랫폼은 조용히 빠진다** (칩도 안 그린다). 로컬에서 키 없이 띄워도 나머지는 돈다
- 앱 이름: SOOP 「도구지지」 · 치지직 `dogugg`(한글 불가) · 구글 `DOGU-GG`. **플랫폼 이름(치지직·네이버·SOOP)을 넣으면 거절될 수 있다**
- SOOP 은 문서엔 「제휴 확정 뒤 키 발급(영업일 10일)」이라 적혀 있지만 **실제로는 바로 나왔고 바로 호출됐다**

## 수집 — 세 플랫폼 모두 **매시 00·20·40분** (2026-09-17 사용자 결정)

서버가 방문자와 무관하게 **매시 00·20·40분(+5초)** 에 세 곳을 받는다 (`scheduleBroadcast`, `startJobs` 안 · READONLY_JOBS 뒤).
`/api/broadcast` 는 메모리에 있는 걸 줄 뿐 **바깥 호출을 안 부른다** (부팅 직후 첫 수집이 도는 중이면 그것만 기다린다).

- **이력**: 처음엔 「요청이 올 때만 받고, 받아 둔 게 오래됐으면 옛것을 주고 뒤에서 새로 받는」 방식이었다.
  방문이 뜸하면 첫 방문자가 몇 시간 전 목록을 보고, 유튜브 명단(lastLive·새 채널 찾기)이 안 자라서 정시로 바꿨다.
  유튜브 먼저(`4e1011f`), 이어서 SOOP·치지직도. **대가: 시청자 수가 최대 20분 늦다**
- 받은 결과를 `esportscaches` 의 `key: 'broadcast_<p>'` 에 박제한다 (문서 3개, 합 ~80KB).
  부팅 때 **이번 20분 슬롯에 받은 박제가 있으면** 그걸 쓰고, 없으면 곧바로 한 번 받는다
  → 배포 직후에도 화면이 비지 않고, 같은 슬롯에 두 번 안 받는다 (유튜브 할당량). 실측: 재부팅 때 셋 다 「박제 복원」
- 실패하면 다음 정시까지 기다리지 않고 **1분 뒤 한 번만** 다시 해 본다
- **★ 정시 작업은 `startJobs` 가 아니라 `app.listen` 직후에 띄운다.** `startJobs` 는 명단 조회 등을 수십 초 기다린 뒤라,
  처음엔 거기 뒀더니 (1) 그 사이 들어온 요청이 옛 방식(요청 때 받기)으로 세 곳을 받아 버렸고 (유튜브 64유닛 낭비)
  (2) 늦게 온 박제 복원이 그 더 새 값을 몇 분 전 값으로 덮었다 (배포 실측). 그래서 모드는 부팅 때 상수(`bcScheduled`)로 정하고,
  복원은 `snap.at > 지금 값` 일 때만 하고, 비어 있는 동안 온 요청은 복원·첫 수집을 기다린다
- 한국시간과 UTC 는 분이 같아서 epoch 를 20분으로 나눈 경계가 곧 00·20·40분이다
- **로컬(`READONLY_JOBS=1`)에서는 정시 작업이 안 돈다** — 예전처럼 요청이 올 때만 받는다(20분 지나면 새로).
  같은 유튜브 키로 두 곳이 20분마다 돌면 할당량을 나눠 먹기 때문이다.
  **정시 작업 자체를 로컬에서 시험하려면 `READONLY_JOBS=1 BROADCAST_SCHEDULE=1 node server.js`** (잠깐만 — 할당량을 같이 먹는다)

| 플랫폼 | 주기 | 한 번에 하는 일 |
|---|---|---|
| SOOP | 매시 00·20·40분 | `GET openapi.sooplive.co.kr/broad/list?select_key=cate&select_value=00040019&order_type=view_cnt&page_no=N` — 60개씩, 덜 차면 끝 (실측 2페이지) |
| 치지직 | 매시 00·20·40분 | **비공식** `GET api.chzzk.naver.com/service/v2/categories/GAME/League_of_Legends/lives?size=50` (실측 4페이지 · 0.2초 · 128개). 실패하면 공식 `openapi…/open/v1/lives` 를 시청자 1명 이하까지 (실측 65페이지 · 1.4초) |
| 유튜브 | 매시 00·20·40분 | 명단 채널마다 업로드 목록 앞 3개 → 그 영상들의 라이브 여부·시청자 수 |
| 유튜브 새 채널 찾기 | 2시간 (정시 작업 안에서) | `search` `q=롤` 한 번 (100유닛) |

- 새 채널 찾기는 「마지막 검색 + 2시간 − 1분」 이 지나면 돈다 (20분 틱이 2시간 정각에서 몇 초 모자라 한 틱 밀리지 않게)
- 실패하면 1분 동안 다시 안 부른다. 유튜브 할당량이 바닥나면(`quotaExceeded`) **한 시간** 쉰다
- 받아 둔 게 있는데 실패 중이거나 20분의 3배(1시간)를 넘기면 `platforms.<p>.stale` → 화면에 안내 줄
- **방송 목록은 메모리에만 있다.** DB 에 쓰는 건 유튜브 채널 명단(`ytchannels`)뿐이고 채널당 ~200B 다

### 응답 모양

```js
{ ok: true,
  items: [{ p: 'soop'|'chzzk'|'youtube', id, title, name, viewers /* null = 비공개 */, thumb, avatar, url, start /* ms */ }],
  platforms: { soop: { at, count, stale, error /* null · 'nokey' · 'quota' · 'error' */ }, … },
  ttl: { soop: 90000, … } }
```

실측 크기 ~70KB (180개). 서버는 `(분, 플랫폼별 at·error)` 가 바뀔 때만 JSON 문자열을 다시 만든다.

## 플랫폼별 사실 (2026-09-17 실측)

### SOOP
- **GET 만 된다** — POST 는 405
- 롤 카테고리 = **`00040019`** (`GET /broad/category/list?client_id=…&locale=ko_KR`)
- `total_view_cnt` 는 이름과 달리 **현재 시청자로 보인다** (오전 1위가 698명). ★ 저녁에 SOOP 화면 숫자와 한 번 대조할 것
- 썸네일·프로필 주소가 `//` 로 시작한다 → `https:` 를 붙인다
- 방송 주소 `https://play.sooplive.co.kr/<user_id>/<broad_no>` (301 로 이어진다)
- `is_password === '1'` · `broad_grade >= 19` 는 뺀다 (사용자 결정)

### 치지직
- **★★ 공식 API 에는 카테고리 조건이 없다 (2026-09-17 두 번 확인).** `/open/v1/lives` 는 `size`·`next` 만 받고,
  `categoryId`·`liveCategory`·`category` 를 붙여도 **조용히 무시된다**(20개 중 롤 1개). 공식 카테고리 API(`/open/v1/categories/search`)는 이름 검색뿐이다
- **그래서 치지직 웹이 쓰는 비공식 주소를 먼저 쓴다** — `api.chzzk.naver.com/service/v2/categories/GAME/League_of_Legends/lives`.
  키가 필요 없고 롤만 준다. 다음 페이지 열쇠는 `page.next` 의 `{ concurrentUserCount, liveId }` 를 그대로 쿼리로 붙인다.
  채널 정보가 `channel` 안에 들어 있고 썸네일은 `liveImageUrl`. `blindType` 이 있는 방송은 뺀다.
  (`service/v1/…` 은 빈 목록을 준다 — v2 만 된다)
- **실패하면 공식 API 로 넘어간다** (로그는 한 번만). 공식 경로는 전체를 시청자 순으로 넘기며 `liveCategory === 'League_of_Legends'` 로 거른다 —
  시청자 1명 이하에서 멈추므로 0명 방송은 빠진다 (실측: 비공식 132 · 공식 81). 실패 흉내로 넘어가는 것 확인함
- 둘 다 「에이펙스 레전드」·「레전드 오브 바둑」 같은 게 안 섞여서 검색어보다 정확하다
- 응답은 `content.data` / `content.page.next`. 헤더 `Client-Id` · `Client-Secret`
- 썸네일 주소에 **`{type}` 자리표시**가 들어 있다 → `480` 으로 바꾼다
- `adult: true` 는 뺀다
- 방송 주소 `https://chzzk.naver.com/live/<channelId>`

### 유튜브 — 제일 까다롭다

**API 에 「게임 = 리그 오브 레전드」 조건이 없다.** 그래서 채널 명단을 두고 그 채널들만 확인한다.

- **명단 확인 = `playlistItems`(업로드 목록 `UU` + 채널ID 뒤 22자, 1유닛) → `videos`(50개당 1유닛)**.
  실측: 방송 중인 48채널 중 **47채널이 업로드 목록 앞 5개 안에 라이브가 있었다** (빠진 하나는 몇 주째 켜 둔 24시간 재방송)
- **안 되는 길 둘** — 다시 재 보지 말 것
  - 채널 RSS(`/feeds/videos.xml?channel_id=`) — **50개 중 49개가 404·500**
  - `/channel/<ID>/live` 페이지 — 방송 여부는 나오지만 **한 장 1.2MB**. 비공식이기도 하다
- **`search` 에 `order=viewCount` 를 주면 결과가 15개로 줄어든다** (안 주면 50개 + 다음 페이지). 그래서 안 주고 우리가 정렬한다
- 시청자 수를 숨긴 방송은 `concurrentViewers` 가 없다 → `viewers: null` → 맨 뒤, 화면엔 「비공개」
- 한 채널이 동시에 여러 개 송출하면 하나만 쓴다

**할당량 (하루 10,000 유닛, 결제 없음 — 넘기면 그날 막힐 뿐 청구되지 않는다)**

| 무엇 | 1회 | 하루 |
|---|---|---|
| 명단 확인 (60채널) | 60 + 약 4 | 20분마다 72회 → **~4,600** |
| 새 채널 찾기 | 100 | 2시간마다 12회 → **1,200** |
| 배포 때 부팅 한 번 (그 슬롯 박제가 없을 때만) | 64 | 배포 수만큼 |
| 합 | | **~5,800 + 배포** |

★ 여유가 4,000 쯤 남는다 — 명단을 늘리고 싶으면 `BC_YT_MAX` 를 **최대 ~110** 까지 올릴 수 있다 (72 × 115 + 1,200 ≈ 9,500).

★ **`BC_YT_MAX`(60)나 주기를 바꾸면 이 합을 다시 셀 것.** 로컬 서버도 같은 키라 같이 먹는다(로컬은 요청할 때만).
재부팅(배포)마다 검색이 다시 돌지 않게, 마지막 검색 시각을 `ytchannels.searchAt` 최댓값에서 잇는다.

#### 명단 규칙

- **자동 명단(`ytchannels`)** — 새 채널 찾기에서 **한글 제목 + 롤 신호 + 다른 게임 이름 없음**인 방송의 채널이 들어간다.
  방송이 잡힐 때마다 `lastLive` 가 갱신되고, **14일 동안 안 잡히면 확인 대상에서 빠진다**(문서는 남는다).
  확인 대상은 `lastLive` 최신 순 60개
- **손 명단(`broadcast_channels.js`)** — `include` 는 항상 확인, `exclude` 는 절대 안 보임. 값은 채널 ID(`UC…`)

**★★ 롤 신호를 뺐더니 망가졌다 (실측).** 처음엔 「검색어가 `롤` 이니 제목 조건은 필요 없다」고 봤는데,
게임 카테고리 전체가 딸려 와서 **38채널 중 절반이 리니지·디아2·림버스**였다. 그래서 신호를 되살렸다 —
- `BC_LOL_RE` — 롤 · 리그 오브 레전드 · lol · lck · 솔랭 · 자랭 · 칼바람 · 협곡 · 원딜 · 서폿 · 챌린저 · 그마 · ○딱 · 칼챔 …
- 챔피언 한글 이름 — `champKeyMap`(부팅 때 받는 `champion.json`)에서 **세 글자 이상만**.
  두 글자는 흔한 낱말(아리·진·세트·조이·유미·애니)이라 헷갈릴 일 없는 것만 `BC_CHAMP_2` 에 따로 둔다
- 대가: 롤 방송인이라도 그날 제목에 신호가 없으면(큰코3 「취두부미션」) 그날은 못 잡는다. 다른 날 제목으로 잡힌다

**다른 게임 거르기(`BC_OTHER_GAME_RE`)** — 명단 채널이 다른 게임을 켜도 API 로는 알 수 없어서 **제목에 다른 게임 이름이 있으면 뺀다** (사용자 결정).
명단 확인 때도 같은 규칙을 쓴다. 제목에 게임 이름이 없는 다른 게임 방송은 그대로 보인다 — 그게 이 방식의 한계다.
- ★ 짧은 줄임말(발로·로아·마크)은 넣지 않았다 — 다른 낱말 안에 흔히 들어 있다
- 뒷줄(리니지·아이온·림버스·팰월드·DK온라인·열혈강호·에오스·디아2/4·래더·닌텐도·역전재판·lofi·이클립스 …)은
  실측에서 실제로 딸려 온 것들이다. **새로 섞이는 게임이 보이면 여기에 더하거나 채널을 `exclude` 에 넣는다**

## 화면

- 칩: 전체 / SOOP / 치지직 / 유튜브 (옆에 개수). **주소는 안 바꾼다** — `/broadcast` 하나라 라우터 두 곳은 원래 있던 한 줄 그대로다
- 카드: 16:9 썸네일 + 왼쪽 위 플랫폼 배지 + 오른쪽 위 시청자 수 + 프로필·제목(두 줄)·방송인·방송 시간
- 60개씩, 「더 보기」
- 안내 줄: 「방송 목록은 매시 00·20·40분에 새로 받습니다」
- 페이지에 머무는 동안 2분마다 다시 묻는다 (서버 메모리 응답이라 싸다. 정시에 바뀐 목록을 곧 잡는다) (다른 화면으로 가면 멈춘다)
- 폰(≤768)은 2열
- 실측: 1400 / 820 / 390 넘침 0 · 유튜브 칩 → 11장 전부 유튜브 · 주소 그대로
- ★ **헤드리스 `--window-size=390` 스크린샷은 믿지 말 것** — 새 헤드리스는 창 폭 하한이 있어 390 보다 넓게 그려 놓고 잘라서 **가로로 넘친 것처럼 보인다.**
  CDP `Emulation.setDeviceMetricsOverride` 로 재야 한다 (반복 함정 14번과 같은 부류)

## 남은 것

- SOOP `total_view_cnt` 가 정말 현재 시청자인지 저녁에 화면과 대조
- 유튜브 명단이 쌓인 뒤(며칠) 헛것이 섞였는지 보고 `exclude` 정리
- 할당량이 모자라면 구글에 증액 신청 (무료, 심사)

## ★★★ 2026-09-17 저녁 — 유튜브 수집을 InnerTube 로 바꿨다 · 검색칸·정렬 · SOOP 시청자 확정

### 유튜브 — 왜 바꿨나

명단 방식(공식 `search` 100유닛으로 「롤」 검색 → 제목에 롤 단어가 있는 채널만 명단 → 20분마다 60채널 확인)은
**제목에 롤 단어가 없는 방송을 영영 못 잡는다**(온피 「사일 장인 솔랭 1515점」은 잡히지만 김망치 「균형의 수호자 매튜렁 VS 김망치」는 못 잡는다)
그리고 하루 ~5,800유닛을 썼다. 같은 시각 실측: 옛 방식 10개 → 새 방식 **19개**.

### 새 구조 — 후보는 InnerTube, 확인은 공식

InnerTube = 유튜브 웹이 자기 화면에서 쓰는 비공식 API. 키 없이 부른다 — 처음엔 youtube.com 페이지에 박힌 웹 클라이언트 공개 키를 붙였는데 GitHub secret scanning 이 Google API 키 꼴이라 경고를 내서 뺐다 (browse·search 둘 다 키 없이 200, 2026-09-17 실측). 할당량도 없다.

| 단계 | 무엇 | 비용 |
|---|---|---|
| ① 주제 채널 라이브 탭 | `browse` `browseId=UCZtmNrG53nmbq-Ww2VJrxEQ`(리그 오브 레전드 - 주제) `params=EgRsaXZl` — **방송인이 스튜디오에서 게임을 롤로 설정한 방송 전부**, 시청자 순, 50개씩 | 0 |
| ② 라이브 검색 | `search` `query=롤·리그오브레전드·솔랭` `params=EgJAAQ==`(실시간 필터) — 게임 태그를 안 단 방송 | 0 |
| ③ 거르기 | 제목이나 채널명에 **한글** · `exclude` 아님 · 다른 게임 제목 아님 · 재방송(`BC_RERUN_RE`) 아님 · **검색에서 온 건 롤 신호까지** | 0 |
| ④ 확인 | 공식 `videos.list` 로 방송 중인지·시청자·시작 시각 (50개당 1유닛) + `include` 채널은 늘 확인 | ~1 |

하루 **~150유닛** (72회 × 1~2). 실측 ① 전 세계 92개 · 한국어 15개 · 1.2초.
- **★ 페이지 넘김은 첫 응답의 `responseContext.visitorData` 를 `context.client.visitorData` 로 되돌려줘야 된다** — 안 주면 2페이지가 `responseContext` 만 있는 빈 응답이다. 다음 열쇠는 `continuationItemRenderer.continuationEndpoint.continuationCommand.token`
- 라이브 탭의 카드는 `gridVideoRenderer`, 검색은 `videoRenderer` — 둘 다 긁는다 (`bcYtItVideos`)
- 주제 채널 id 는 InnerTube 채널 검색(`params=EgIQAg==`) 「League of Legends - Topic」 으로 찾았다. 탭 목록: 홈 · **라이브(EgRsaXZl)** · 최근 업로드 순 · 게임 시청 · 공식
- 라이브 탭에 「내 지역 / 전 세계」 정렬이 있는데 결과가 같았다 (`gl=KR`)
- 시청 페이지(`next`)의 `videoAttributeViewModel` 에 게임 이름(「리그 오브 레전드」)이 온다 — 영상 하나씩 판정할 일이 생기면 이걸 쓴다
- **★ 막히면 옛 명단 경로(`bcFetchYoutubeOfficial`)로 넘어간다** — 치지직 비공식→공식과 같은 구조. 새 경로에서 잡힌 채널은 `ytchannels.lastLive` 를 찍으므로 폴백 명단도 같이 자란다.
  검색 발견(`bcYtDiscover`, 100유닛)은 **폴백 경로에서만** 돈다
- 실측 헛것 하나: 「원스 휴먼」 을 켜고 게임 태그를 롤로 둔 채널 → `BC_OTHER_GAME_RE` 에 `원스\s*휴먼|once\s*human` 추가

### SOOP `total_view_cnt` 확정 — 현재 시청자다

웹 API(`live.sooplive.co.kr/api/main_broad_list_api.php`)와 대조: `total_view_cnt 899 = pc_view_cnt 593 + mobile_view_cnt 306`. 이름과 달리 **PC+모바일 현재 시청자 합**이다. 남은 일에서 지운다.

### 화면

- 제목 오른쪽 요약 줄 「방송 N개 · 시청자 N명」 · 칩에 플랫폼 색 점 · **검색칸(방송인·제목)** · 정렬 「시청자 순 / 최근 시작 순」
- 카드 위 테두리 2px 가 플랫폼 색, 썸네일 배지는 반투명 검정 + 색 점, 시청자 배지의 빨간 점이 깜빡인다, hover 에 살짝 뜬다
- **★ 검색칸에 커서가 있을 때 2분 폴링이 오면 격자만 갈아 끼운다** (`bcRenderGrid`) — 통째로 다시 그리면 치던 글자와 커서가 날아간다

### 남은 것

- 유튜브 새 경로가 며칠 돌고 나서 헛것(게임 태그만 롤인 채널)을 `exclude` 로 정리
- InnerTube 가 막히면 로그 `[Broadcast] 유튜브 InnerTube 실패 → 공식 명단 경로로:` 한 줄이 찍힌다 — 그때 응답 모양부터 볼 것

## 플랫폼 아이콘 — 브랜드 가이드 확인 결과 (2026-09-17 저녁, 사용자 요청)

칩·카드 배지에 플랫폼 표식을 붙였다 (`bcPlatMark`). **세 곳의 가이드를 실제로 읽고 허용되는 것만 붙였다.**

| 플랫폼 | 붙였나 | 근거 |
|---|---|---|
| **유튜브** | ✅ **공식 아이콘** `public/youtube_icon.svg` (2026-09-18 — 전엔 직접 그린 인라인 SVG) | [YouTube API Services Branding Guidelines](https://developers.google.com/youtube/terms/branding-guidelines) — 「You do not need special approval to use YouTube APIs or to promote API functionality」, 로고는 **유튜브 콘텐츠로 가는 링크**여야 하고 색·비례 변경 금지. 우리 카드는 전부 유튜브 방송 링크라 조건에 맞는다. ★ brand.youtube 의 다운로드 키트는 스크립트로 못 받지만 **구글이 직접 서빙하는 공식 로고 파일**이 있다 — `https://www.gstatic.com/youtube/img/branding/youtubelogo/svg/youtubelogo.svg` (Illustrator 원본, 아이콘 + 워드마크). 거기서 아이콘 `<g>`(빨강 path + 흰 polygon)만 경로 그대로 잘라 `viewBox 0 0 159.5 110` 으로 뒀다. 같은 아이콘의 PNG 는 `https://www.gstatic.com/images/branding/product/1x/youtube_512dp.png`. 비례 1.45 라 CSS 는 높이만 주고 폭은 `auto`. 직접 그린 SVG(둥근 사각 rx 5.5 + stroke 로 둥글린 삼각)는 버렸다 |
| **치지직** | ✅ 공식 아이콘 PNG (`public/chzzk_icon_40.png`, 20px 표시) | [CHZZK Brand Guides](https://chzzk.gitbook.io/chzzk/resources/brand-guides) 의 「5) 아이콘」 다운로드 파일(`chzzk Icon_02.png`, 둥근 사각)을 크기만 줄였다. 조건: **형태·색 변형 금지 · 제휴/후원 오인 금지 · 상업적 목적 사용 금지.** pixlol 은 광고가 없어(`adsbygoogle` 0) 비상업이다. **★ 사이트에 광고를 붙이는 날엔 이 아이콘부터 뺄 것** |
| **SOOP** | ❌ 색 점 + 이름 그대로 | [SOOP 브랜드 가이드](https://www.sooplive.com/policy/policy9.html) 3-1) 「회사로부터 명시적인 서면 동의를 얻은 경우가 아니라면 **SOOP 플랫폼 외부에서의 브랜드 이용은 허용되지 않습니다**」. 쓰려면 4) 의 양식(목적·매체·기간·방법)을 `soop@sooplive.com` 으로 보내 허락을 받아야 한다 (한 달 안에 회신). 사용자 결정 대기 |

- 사이트 파비콘을 긁어 쓰는 것도 결국 같은 로고라 SOOP 에는 안 된다
- **★ 제휴 부인 문구 (2026-09-18, 사용자 요청)** — 치지직·유튜브 가이드가 둘 다 「제휴·후원으로 오인되게 쓰지 말 것」이라 **모든 페이지 푸터**에 한 줄 넣었다 (`app.js` `mountFooter` 의 `notice` 배열 둘째 줄, 라이엇 고지 바로 아래). 방송 페이지에만 두면 티어 표·홈 등 다른 자리에서 아이콘이 보일 때 빠지고, 약관에 묻으면 아무도 안 읽는다 — 라이엇 고지와 같은 자리가 맞다. 문구: 「pixlol.kr은 SOOP · 치지직(CHZZK) · YouTube 및 그 운영사와 제휴·후원·보증 관계가 없습니다. 각 플랫폼의 이름과 로고는 해당 회사의 상표이며, 방송 탭은 공개 API로 제공되는 생방송 정보를 안내하고 원본 방송 페이지로 연결할 뿐입니다.」
- 치지직 「권장하지 않는 사용」 그림은 워드마크 기준(색 변경·그림자·외곽선·기울기·그라데이션·형태 변경) — 아이콘을 그대로 쓰는 우리 용법은 해당 없음
- ★ 폰(≤768)에서 도감의 `.codex-tab { flex: 1 }` 이 딸려와 아이콘 때문에 둘째 줄로 내려간 칩이 한 줄을 통째로 먹었다 → `.bc-bar .es-leagues .codex-tab { flex: 0 0 auto }`

### 2026-09-17 밤 — 사용자 지적 셋 (아이콘 계단 · 검색칸 돋보기 · 방송 시간)

- **유튜브 삼각형이 계단져 보인다** → 같은 색 stroke(1.4, `stroke-linejoin: round`)를 얹어 꼭짓점을 둥글렸다 + `shape-rendering="geometricPrecision"`. 20px 로 줄이면 맨 path 는 변이 거칠다 (4배 확대로 확인)
- **검색칸 돋보기가 사이트와 안 어울린다** → CSS 로 그린 원+선을 버리고 사이트 검색의 `.pix-search-icon` SVG(같은 stroke 스타일)를 왼쪽에, 랭킹 검색칸의 `.rank-search-clear`(×)를 오른쪽에. 포커스 때 돋보기가 액센트색
- **방송 시간 「n시간째」 → `HH:MM:SS`** — 썸네일 오른쪽 아래(유튜브 재생시간 배지 자리) `.bc-dur`. `bcTick` 이 1초마다 `.bc-dur[data-start]` 의 textContent 만 바꾼다 — 카드 300장이라도 초당 300개 텍스트 갱신이라 부담 없음 (다시 그리지 않는다). 페이지를 떠나면 스스로 멈춘다. 며칠 켜 둔 방송은 시간 자리가 세 자리(`119:09:39`)가 된다 — 그대로 둔다

## ★★ 방송인 롤 티어 (2026-09-17 밤, 사용자 요청 「deeplol 참고해서」)

### 자동으로는 안 된다 — 실측

| 길 | 결과 |
|---|---|
| SOOP 방송 목록·방송국 API | 라이엇 ID 칸 없음. 제목 60개 중 `#태그` 꼴 1개인데 `#미션듀오` 해시태그(오탐) |
| 치지직 목록·채널 정보·live-detail | 칸 없음. 제목 50개 중 0. 소개글은 일정·메일 정도 |
| 유튜브 | 게임 계정 연동 같은 게 없다 |
| 우리 닉네임 사전(`summonercaches` 3.3만)과 이름 대조 | 방송 중 296명 중 이름 일치 10명, 티어 저장 0. 「제영우」는 같은 이름 계정이 셋 |

### 그래서 손 명단 — `broadcast_channels.js` 의 `streamers`

- 열쇠는 **채널**(`p` + `ch`) — 방송 id 는 매번 바뀌지만 채널은 그대로다. 서버 응답 `items[].ch` 에 찍힌다 (SOOP user_id · 치지직 channelId · 유튜브 UC…)
- `riot` 에 라이엇 ID 여러 개 → 서버가 솔로 랭크 **제일 높은 계정**을 골라 `tier: { t, r, lp, w, l, id, n }` 로 붙인다 (`bcAttachTiers`, 수집 파이프라인 안)
- 라이엇 호출: 계정→puuid 는 **한 번만**(메모리 영구), 리그는 **한 시간에 한 번**(`BC_TIER_TTL`). 실패는 15분 뒤 재시도. 호출 사이 0.3초.
  명단 29명 전부 켜져도 시간당 ~30회 — 순회 예산(2분 100회)에 안 걸린다
- 화면: 이름 아래 「문장 + 마스터 · 518 LP」(마스터 이상은 단계 생략, 그 아래는 「다이아 II · 75 LP」). 마우스 올리면 계정 ID·승패·「계정 N개 중 최고」
- 첫 명단 29명: **deeplol 의 스트리머 등록 페이지**(`deeplol.gg/strm/<이름>`, 방송인이 직접 등록한 계정)를 참고해 라이엇 API 로 **하나씩 검증**했다
  (존재 여부 · 솔랭 티어). 언랭·404 는 뺐다. 같은 시각 시청자 상위 45명 중 deeplol 미등록이 절반이라 못 찾은 사람 목록을 파일 끝에 적어 뒀다
  (노페 · 김군 · 앰비션 · 도파 · 크캣 · 테디 …). ★ 나무위키는 옛 소환사명만 있고 태그가 없어 그대로 못 쓴다 (이렐킹 「Aileri」는 `#KR1` 을 붙여 맞았다 — 챌린저 2620 LP)
- **★ deeplol 명단을 통째로 긁지 않는다** — 남의 편집 DB 다. 지금 켜진 상위 방송인만 참고해 검증했고, 이후는 사용자가 한 줄씩 더한다
- 새 방송인 넣는 법: 응답의 `ch` 를 복사 → `riot` 에 ID → `node -e` 로 account-v1 한 번 쳐서 태그가 맞는지 확인 → push

## ★★ 방송인 티어 페이지 · 방송 메뉴 드롭다운 · 닉네임 변경 추적 (2026-09-17 밤, 사용자 요청)

### 헤더
`방송` 이 하위 메뉴만 가진 부모가 됐다 (도감·패치노트와 같은 꼴, href 없음): **생방송** `/broadcast` · **방송인 티어** `/broadcast/tiers`.
라우터는 진입부·popstate 두 곳 다 `pathParts[2] === 'tiers'` 로 가른다. `setActiveNav('nav-broadcast-live' | 'nav-broadcast-tiers')`.
★ 생방송 화면의 폴링·시계·「다른 화면이면 버린다」 판정은 `bcOnLivePage()`(경로가 정확히 `/broadcast`)로 바꿨다 —
`startsWith('/broadcast')` 로 두면 티어 페이지에서도 폴링과 시계가 돈다.

### 닉네임을 바꿔도 따라간다
- 처음 찾은 puuid 를 `esportscaches` 의 `broadcast_puuids` 한 문서(`{ riotId: puuid }`)에 박아 두고 부팅 때 읽는다 → 배포 뒤에도 옛 닉네임으로 다시 조회하지 않는다
- 현재 닉네임은 하루 한 번 `account-v1 by-puuid` 로 다시 받아 카드·표에 그걸 보여 준다 (`tier.id`). 명단 파일의 이름은 메모일 뿐
- 명단에 적은 ID 가 **처음부터** 404 면 로그 `[Broadcast] 라이엇 ID 를 못 찾았다 (닉네임이 바뀌었나?): …` — 이때만 사람이 고친다
- **★ 라이엇 호출은 `bcTierJob` 한 곳에서만** 나간다. 명단 전원(방송인 29 + 프로 50)의 계정을 순서대로 돌되 **2.5초 간격**(분당 24회) —
  순회 예산(2분 100회, 가드 80)과 같은 키라 이보다 빨리 돌리면 안 된다. 리그는 한 시간에 한 번이라 한 바퀴에 실제 호출은 ~80회(3~4분).
  생방송 카드에 붙이는 `bcAttachTiers` 는 **캐시만 읽는다** (호출 0)
- 정시 틱(00·20·40분)마다 한 바퀴 + 부팅 15초 뒤 한 바퀴. 로컬(요청 모드)은 `/api/broadcast/tiers` 첫 요청이 시동을 건다

### `/api/broadcast/tiers`
`{ ok, at, building?, pros: [{ name, team, teamImg, role, tier, accounts, live }], streamers: [{ name, ex, platforms: [{p, ch, live}], tier, accounts, live }] }`
- `building: true` 는 아직 한 바퀴를 못 돈 상태(부팅 직후) — 화면이 「받는 중」 줄을 띄운다
- **★ 같은 사람이 플랫폼마다 한 줄이면(데스티니 = 유튜브·SOOP·치지직) 라이엇 ID 가 겹치는 줄을 한 사람으로 합친다** — 이름은 먼저 적힌 줄, 플랫폼은 전부, 켜진 곳이 `live`
- 프로가 방송도 하면(같은 라이엇 ID 가 `streamers` 에도 있으면) 프로 줄의 `live` 가 그 방송을 가리킨다

### 프로게이머 명단 (`pros`) — 2026 LCK 정규 로스터 10팀 50명
- 로스터는 lolesports `getTeams` 가 아니라 **livestats window API**(`feed.lolesports.com/livestats/v1/window/<게임id>?startingTime=`)로 뽑았다 —
  `getTeams` 의 `players` 는 CL 선수까지 섞여 팀당 11~14명이라 주전을 못 가른다. 최근 경기 1게임의 참가자 10명이 곧 그날 주전이다
  (`startingTime` 은 10초 단위 ISO 여야 하고, 경기 시작 +20분쯤을 주면 된다)
- 계정은 deeplol `/pro/<이름>` 페이지(선수·팀이 등록한 계정)를 참고해 라이엇 API 로 검증했다. 39명은 찾았고
  **못 찾은 11명**(Lucid · Smash · Doran · Zeka · PerfecT · Clear · Raptor · Taeyoon · Frog · DuDu · Peter)은 계정 없이 표에 넣었다 (티어 「-」)
- 팀 로고는 `getTeams` 의 `image` 를 `teamImg` 에 박았다 (http → https 는 서버가 바꾼다)

### 前 프로인 방송인은 어디에 — 「방송인」 탭 (2026-09-17 판단)
지금 하는 일 기준이다. 현역 로스터에 있으면 프로 탭, 방송이 본업이면 방송인 탭에 두고 `ex: 'T1'` 처럼 옛 소속을 적으면 「前 T1」 배지가 붙는다.
테디는 2026 BRO 주전이라 프로 탭이고, 그가 켠 치지직 방송은 프로 줄의 LIVE 로 이어진다.

### 화면 (`/broadcast/tiers`)
칩 「프로게이머 N · 방송인 N」 → 표 `# · 선수/방송인 · 계정(+N) · 티어(문장) · LP · 승패(승률) · 방송(LIVE 링크)`. 티어 높은 순.
폰(≤768)은 계정·승패 칸을 숨긴다. ★ 사이트 공통 폰 규칙이 `.stats-table` 7번째 칸을 숨기므로(통계 표 기준) 방송 칸은 `.bt-table` 에서 도로 켰다.
프로 명단이 비어 있으면 방송인 탭부터 연다.

### 2026-09-17 밤 2차 — 프로 2군·3군 · 방송인 76명 추가 · 생방송 카드 티어 자리

- **프로 2군(LCK CL) 49명** — LCK 와 같은 방식(livestats window 로 최근 CL 경기 주전). CL 팀 코드는 `T1A`·`GGA` 처럼 다르다 → T1·GEN 으로 되돌렸다.
  KRX 의 Frog 는 1군과 같은 사람이라 CL 줄에서 뺐다 (49명인 이유)
- **3군·후보 16명** — lolesports 에 3군 경기 데이터가 없어서 `getTeams` 등록 명단에서 1·2군 주전을 뺀 나머지다.
  **진짜 3군인지 1군 후보인지는 못 가른다** (DNS 의 Pyosik 이 그 예) — 화면에도 「3군 · 후보」로 적는다. `squad: 3`
- 계정은 deeplol `/pro/` 로 65명을 훑어 **41명** 확인 (라이엇 API 검증). 2군 30/49 · 3군 11/16
- **방송인 76명 추가** — 그 시각 켜져 있던 297명(명단 밖)을 deeplol `/strm/` 로 훑어 86명이 나왔고 랭크 있는 KR 계정을 가진 76명을 넣었다.
  `p:ch` 는 `/api/broadcast` 응답에서 그대로 땄다. 검증은 164계정 · 2.5초 간격 · 429 0
- 티어 페이지 프로 탭에 **1군 · 2군 · 3군 칩**(`btSquad`, e스포츠 스테이지 칩 모양). 이름 옆에 「T1 · 2군 · 정글」
- **생방송 카드의 티어는 닉네임 오른쪽**에 티어 이름만 (사용자 요청 — LP·승패는 마우스 올리면 툴팁). `.bc-tier` 가 `.bc-name` 안으로 들어갔다
- 명단이 105 + 115 = 220명(계정 ~345개)이라 `bcTierJob` 한 바퀴가 **첫 부팅엔 20분쯤**(puuid 를 처음 받는 계정이 많아서), 그 뒤엔 리그 호출만이라 ~15분.
  puuid 창고가 차면 배포 뒤에도 15분이다. 2.5초 간격·예산 양보는 그대로

### 여러 플랫폼 동시 송출은 어떻게 세나 (사용자 질문)
- 플랫폼마다 **자기 시청자만** 준다 (SOOP 은 PC+모바일, 치지직·유튜브는 각자 동시 시청자). 합산해 주는 곳은 없다
- 생방송 목록은 **플랫폼별로 한 장씩** 나온다 — 같은 사람이 세 곳에 쏘면 카드 세 장, 각 카드는 그 플랫폼 시청자. 요약 줄의 「시청자 N명」도 그냥 합이라 동시 송출자는 플랫폼 수만큼 들어간다 (중복 제거 안 함)
- 방송인 티어 표는 라이엇 ID 가 겹치는 줄을 **한 사람으로 합치고** 플랫폼 표식을 나란히 단다. LIVE 링크는 켜진 플랫폼 중 먼저 적힌 것

### 2026-09-17 밤 3차 — 동시 송출 합치기 (사용자 결정)

- **명단으로 같은 사람으로 묶인 채널**(라이엇 ID 가 겹치는 `streamers` 줄 — `BC_PERSON_OF`)이 여러 플랫폼에 켜져 있으면 `/api/broadcast` 가
  **시청자 제일 많은 플랫폼 카드 하나**만 남기고 나머지를 `sim` 에 카드째 단다. `viewersAll` = 전 플랫폼 합 → 정렬·요약 줄은 이걸 쓴다
- 카드 이름 아래 「치지직 28 · SOOP 65」 링크 줄(`.bc-sim`). 카드가 `<a>` 라 안에 `<a>` 를 못 두므로 `span[data-url]` + 위임 클릭으로 새 탭
- **플랫폼 칩으로 거르면 그 플랫폼 카드를 대신 꺼낸다** (`bcCardFor`) — 치지직 칩에서 데스티니는 치지직 카드(28명)로 보이고 나머지 둘이 링크가 된다
- 명단에 없는 사람은 여전히 플랫폼마다 한 장 (같은 사람인지 알 길이 없다). 칩의 개수(SOOP 256 …)는 합치기 전 방송 수 그대로
- 실측: 데스티니 유튜브 216 + 치지직 28 + SOOP 65 → 카드 하나 · 합 309

### 나무위키로 방송인 더 찾기 (2026-09-17 밤) — 수확이 적다
- 명단 밖 방송인 215명의 나무위키 문서를 받아(`namu.wiki/w/<이름>`, 동음이의어면 「인터넷 방송인/스트리머/프로게이머」 항목으로 한 번 더)
  `이름#태그` 꼴을 뽑고 라이엇 API 로 검증했다. **문서가 있고 ID 가 적힌 사람이 3명**(앰비션·도파·크캣), 랭크 계정이 있는 건 2명
- 이유: 소규모 방송인은 문서 자체가 없고, 있어도 옛 소환사명(태그 없음)만 적혀 있다. 태그 없는 이름은 `#KR1` 을 붙여 봐야 대개 다른 사람이라 안 썼다
- ★ 문서에 적힌 ID 가 본인 것이 아닐 수 있다 — 앰비션 문서엔 페이커의 `Hide on bush#…` 도 언급돼 있었고(언랭이라 걸러짐), 도파 문서의 계정은 「본인 것이 아니다」라고 적혀 있다.
  **검증을 통과해도 이름·티어가 그 사람답지 않으면 넣지 말 것**

### 2026-09-17 밤 4차 — 티어 창고 · 켜진 방송인 먼저
- **`broadcast_tiers` 창고** — 캐시 항목(puuid·이름·티어·`at`)을 통째로 DB 에 박아 두고 부팅 때 되살린다(`bcLoadPuuids` 가 puuid 창고와 같이 읽는다).
  전엔 재배포마다 한 바퀴(12~25분) 전까지 카드에 티어가 없고 표가 「받는 중」이었다. 되살린 항목은 `at` 이 살아 있어 한 시간 안 지난 건 호출도 안 한다.
  저장은 한 바퀴 끝 + **도중 50계정마다**(재배포로 끊겨도 진행분이 남게) — 그때 생방송 카드에도 붙인다. ~300명에 50KB
- **한 바퀴 순서** — 지금 켜져 있는 방송인 → 나머지 방송인 → 프로. 생방송 카드부터 채워진다. 시작 로그 `[Broadcast] 티어 한 바퀴 시작 — 계정 N (켜진 방송인 M명 먼저)`
- 로컬(요청 모드)은 창고에서 되살린 뒤에도 요청이 오면 30분에 한 번 한 바퀴 돈다 (프로덕션은 정시 틱이 돈다)
- 실측: 재시작 직후 `티어 창고 복원 296개` → 카드 31장에 바로 티어 (그 시점 창고엔 도중 저장분만 있었다)
