# pixlol.kr

롤 전적검색 사이트. 한국어로 대화해줘. 평서형 "~다" 말투 말고 "~야/~어" 쪽으로.

## 스택 / 배포

- Node/Express + MongoDB Atlas(무료 512MB) + Railway + Cloudflare
- 프론트는 바닐라 JS. `public/` 안에 `app.js` / `index.html` / `style.css`
- `server.js`는 `public/` 밖, 프로젝트 루트에 있음
- GitHub `cosmicday/pixlol.kr` — push하면 Railway가 자동배포
- 로컬은 `node server.js` → localhost:3000

## 프론트 함정

- **`app.js`의 `ddragonVersion`을 서버 응답으로 덮어쓰지 말 것.** 전적검색 응답의
  `version`은 서버 부팅 시점 값이라 클라이언트가 이미 맞춰 둔 최신보다 오래될 수 있고
  (`server.js:70` 기본값 `16.1.1`), 그러면 **챔피언 탭이 구버전 `champion.json`을 읽어
  신규 챔피언이 목록에서 통째로 사라진다.** 로크가 이 경우였다. `compareDdragonVersion`으로
  더 새 버전일 때만 받아들인다. **문자열 비교는 안 된다** — `16.5.1 > 16.15.1`이 돼버림

## 절대 건드리지 말 것

- **`riot.txt`** — 라이엇 도메인 소유 확인 파일. 프로덕션 키 심사 중이라 없으면 문제가 생김
- **`.env`** — API 키. git에 올라가면 안 됨
- 라이엇 프로덕션 키가 Pending Review 상태(2026-03-10 신청).
  **라이엇 개발자 포털의 앱 등록 정보에서 `EDIT` 를 누르지 말 것** — 누르면 심사
  대기열 뒤로 밀린다. **코드 수정이나 배포와는 무관하다.** git push 는 마음껏 해도 된다
  (2026-08-09 확인. 그전엔 "앱을 크게 수정하면" 이라고만 적혀 있어서 코드 변경 얘기로
   잘못 읽혔다)

## 작업 규칙

- **파일을 고치기 전에 먼저 읽고 설명해줘.** 내가 납득한 뒤에 수정하는 순서로.
- 문제를 알려줄 때는 "문제되는 코드 → 왜 문제인지 → 해결법" 순서로. 코드부터 던지지 말고.
- `git add .`는 2026-08-08에 `.gitignore`를 손봐서 이제 안전해졌어. 루트 `*.md`(CLAUDE.md 제외)·`*.txt`·`*.PNG`·`*.bak-*`가 다 막혀 있음. 그래도 커밋 전에 `git status`로 한 번 볼 것. `public/` 안은 안 막히니 백업 파일을 거기 두면 웹에서 받아짐
- **인게임 확인이 필요하면 체크리스트 파일을 만들어서 넘길 것.** 스킬 설명을 통째로 적게 하지 말고 문장 어느 자리인지 찍어주면 확인이 훨씬 빠름. 확인 항목은 "몇 자리에 영향 있는지" 순으로 정렬할 것 — 스탯 이름 하나가 수십 자리를 좌우함
- `fill_values.js --write` 돌리기 전에 `public/custom_values.js`를 백업해줘.
- **인게임 툴팁 확인은 내가 해.** 수치가 맞는지 추측하지 말고, 확인이 필요하면 나한테 물어봐.

## 랭킹 탭 (2026-08-13 정비)

`server.js` 의 `/api/ranking` + `app.js` 의 `showRanking`·`renderRankingPage` + `style.css` 16절.
챌린저 300 + 그마 700 + 마스터 1만을 합쳐 **LP 내림차순**으로만 정렬해 통째로 내려준다.

- **★ 티어는 서버가 실명으로 내려준다. 순위 번호로 짐작하지 말 것.**
  예전엔 화면이 `순위 <= 300 이면 챌린저` 식으로 색을 칠했는데 **그게 틀린다** —
  라이엇이 승격을 주기적으로만 처리해서 **마스터가 그마보다 LP 가 높은 순간**이 실제로 있다.
  `updateChallengerList()` 가 `entries.forEach(e => e.tier = tier)` 로 소속을 찍어 두고,
  응답에 `C`/`G`/`M` 한 글자로 실어 보낸다 (1만 명 x 매 항목이라 글자 수가 곧 응답 크기다).
  화면 쪽 표는 `RANK_TIER_INFO` — 이름·짧은 이름·색·메달 파일명이 여기 한 군데 있다
- **★ 원래 순위(`rank`)를 받아올 때 못 박는다.** 검색으로 걸러내면 배열 위치가 순위가
  아니게 되어, 화면에서 세면 걸러진 목록이 "1위" 부터 다시 시작한다
- **★ 닉네임을 `onclick` 문자열 안에 박지 말 것.** 아포스트로피가 든 닉네임(`D'Arcy`)이면
  리터럴이 그 자리에서 닫혀 JS 가 깨지고 **그 사람만 클릭이 통째로 안 먹었다.**
  지금은 `data-name` + tbody 이벤트 위임이다. 표를 매번 새로 그리므로 리스너도 매번 붙인다
- **닉네임 미해결 판별은 `#` 유무로 한다.** 아직 못 받아온 사람은 서버가
  `User-a1b2c3d4` 로 채워 보내는데, 라이엇 ID 에는 **항상 `#태그`가 붙으므로** 이걸로 갈린다.
  `User-` 접두사를 보는 것보다 안전하다 (닉네임에 `-` 가 들어갈 수 있다).
  이 사람들은 눌러도 검색이 헛돌아서 링크로 안 만든다
- **★ 검색어 해석은 한 번만 한다 (`buildRankQuery`).** 1.1만 명을 훑으므로 행마다
  자판 변환을 돌리면 타이핑이 눈에 띄게 밀린다. 후보 문자열을 미리 만들고 비교만 시킨다.
  같은 이유로 소문자·초성(`_lc`/`_cho`)은 **데이터를 받을 때** 한 번 만들어 붙여 둔다
  - 초성·영타 헬퍼는 `getChosung()`·`koCandidates()` 를 **그대로 재사용**한다.
    챔피언 검색과 표를 두 벌 두면 어긋난다
  - 영문 자판 해석은 `/^[A-Za-z]+$/` 일 때만 후보에 더한다. 원문 후보는 항상 들어 있어서
    영문 닉네임(`Faker`)도 같이 걸린다
- **검색 중에는 주소(`pushState`)를 안 건드린다.** 걸러낸 목록의 3페이지는
  `/ranking?page=3` 이 아니라서, 그 주소를 새로고침하면 엉뚱한 자리가 나온다
- **탭에 새로 들어오면 검색어를 푼다** (`showRanking` 첫머리). 안 풀면 메뉴로 다시
  들어왔을 때 걸러진 목록이 그대로 나와 고장처럼 보인다
- **헤더는 `renderRankingHeader()` 하나다.** 예전엔 같은 HTML 이 `showRanking` 과
  `popstate` 두 곳에 복붙돼 있어서 한쪽만 고치면 뒤로가기로 들어왔을 때 다르게 보였다
- **페이저 양 끝 버튼(`«` `‹` `›` `»`)은 못 눌러도 자리를 지킨다**(`disabled`).
  없앴다 만들었다 하면 가운데 정렬이라 페이지를 넘길 때마다 번호가 좌우로 튄다.
  번호는 현재 페이지를 가운데 둔 5칸이고 끝쪽에서도 5칸을 유지한다
- **폰에서는 티어 이름이 `챌`/`그마`/`마` 로 줄어든다**(`.rank-tier-short`).
  390px 기준 순위 36 + 티어 54 + LP 50 + 승률 57 = 197px 라 닉네임 몫이 172px 남는다.
  **가로 넘침 0 을 실측으로 확인했다**

## 스킨 탭 (2026-08-13 신설·공개)

왼쪽 얼굴 중심 썸네일 세로줄 + 오른쪽 원본 일러스트. 골격은 스킬 탭과 같다.
예전엔 DD 스플래시 격자 + 전체화면 상세보기였고, 그때 쓰던 함수 5개
(`openSkinDetail`·`closeSkinDetail`·`prevSkin`·`nextSkin`·`updateSkinDetail`)는 **지웠다**.
DD 폴백도 같은 레이아웃을 타므로 경로가 하나다.

- **★ 데이터는 CommunityDragon 이다.** `ko_kr/v1/champions/<숫자키>.json` 이 스킨마다
  경로를 직접 알려준다. **173명 전원 확보 / 스킨 2111개 / 경로 빠진 자리 0** (전수 확인).
  - `splashPath` 1280x720 얼굴 중심 (78KB) — 왼쪽 썸네일
  - `uncenteredSplashPath` 1215x717 **원본 일러스트** (185KB) — 오른쪽 큰 그림
  - `tilePath` 380 정사각 (39KB) / `loadScreenPath` 308x560 세로 (53KB) — 아직 안 쓴다
  - 한국어 스킨 이름·**설명글**·희귀도·크로마도 같이 온다. **설명은 2111개 중 312개가 빈칸**이라
    없으면 줄을 통째로 접는다
- **★ CD 경로는 전부 소문자로 바꿔야 한다.** 원문이
  `/lol-game-data/assets/ASSETS/Characters/...` 인데 실제 파일은 소문자다 (`cdAssetUrl`)
- **★ 스킨 번호는 연속이 아니다.** 가렌은 86011 다음이 **86013** 이다 (86012 는 없다).
  그래서 번호를 세서 만들지 말고 CD 가 준 목록을 그대로 쓴다.
  DD 로 `Garen_12.jpg` 를 부르면 403 이 나는 게 이 때문이다
- **썸네일은 `loading="lazy"` 다.** 탭이 `display:none` 인 동안엔 **한 장도 안 받는다**
  (실측: 미스 포츈 24개 중 탭 열기 전 0개 / 연 직후 14개 / 끝까지 스크롤 후 24개).
  스킨 최다는 미스 포츈 24개
- **`.skin-item-img` 와 `.skin-view-frame` 에 `aspect-ratio` 를 못 박았다.**
  이미지가 늦게 오면 높이가 0 이었다 늘어나며 목록이 아래로 밀리고 스크롤 위치가 튄다
- **목록은 자기 스크롤을 갖는다.** 바깥 스크롤에 맡기면 스킨 24개짜리에서
  오른쪽 큰 그림이 화면 밖으로 밀려난다

### 스킨 등급 아이콘 · 가격 (2026-08-13)

목록에는 `(등급 보석) 스킨 이름`, 오른쪽에는 `(보석) 이름 - 1820RP` 로 나간다.

- **등급 보석은 CD 에 이름으로 6종만 있다** — `v1/rarity-gem-icons/` 의
  `epic` / `legendary` / `mythic` / `ultimate` / `exalted` / `transcendent`.
  **`rare.png` 는 404 다.** `kRare`(19개)·`kNoRarity`(832개)는 인게임에서도 보석이 없는
  등급이라 빈칸으로 두는 게 맞다. (같은 폴더의 `1.png`~`11_large.png` 는 번호판인데
  스킨 데이터의 `rarityGemPath`·`regionRarityId` 가 **2146개 전부 null/0** 이라 못 쓴다)

**★★ 가격은 CommunityDragon 에 없다. 롤위키에서 받아 온다.**
CD 스킨 객체 23개 필드에 가격이 없고, `v1/` 아래 60여 개 json 에 store/catalog 도 없다
(`rcp-fe-lol-store` 는 css·png 뿐, `loot.json` 은 TFT 맵용). 스토어 API 는 로그인이 필요하다.
→ 롤위키 **`Module:SkinData/data`** (Lua 표 2.08MB) 를 `build_skin_prices.js` 가 받아
`public/skin_prices.js` (19KB) 를 만든다. **한 번 받으면 되고 런타임 의존은 없다.**

- 위키 표는 들여쓰기가 완전히 일정해서(챔피언 2칸 / 필드 4칸 / 스킨 6칸 / 스킨필드 8칸)
  Lua 를 해석할 필요 없이 그 층만 보면 된다
- **조인 키는 숫자다.** 위키의 챔피언 `id` = DD 숫자키, 스킨 `id` = 스킨 번호.
  CD 스킨 id = `챔피언키 x 1000 + 번호` 라 정확히 맞는다. **이름으로 맞추려 하지 말 것** —
  위키는 영문이고 챔피언명 표기도 다르다(`Nunu & Willump` 등)
- 결과: **RP 1869 / 신화 정수 119 / 가격 없음 123** (합 2111 = CD 스킨 수와 일치).
  가격 없음은 배틀패스·랭크 보상·코드 등 **애초에 파는 물건이 아닌 것들**이다.
  위키에 항목 자체가 없는 건 3개뿐이다 (삼바 애니비아, 펄스 건 리븐, 불멸의 영웅 리븐)

**★ 등급으로 가격을 추측하면 안 된다 (전수 확인).**

| 등급 | 가격 |
|---|---|
| kLegendary | 1820 × 123 — 그런데 **975 가 2개** |
| kEpic | 1350 × 976 — 그런데 **975 가 2개** |
| kUltimate | 3250 × 6 — 그런데 **2775 가 1개** (기동총격여신 미포) |
| kNoRarity | 975 / 520 / 750 / 880 / 790 / 585 / 260 / 390 / 460 / 5000 / 150000 … 제각각 |
| kMythic | `cost` 가 전부 `"Special"` — 숫자가 아니다 |

- **★ 신화 정수 가격은 `cost` 가 아니라 `distribution` 에 있다.**
  프레스티지 스킨은 `cost="Special"` + `distribution="150 Mythic Essence"` 다.
  100/125/150/200 네 종류가 있고 119자리다
- **★ 기본 스킨의 `cost` 는 스킨값이 아니라 챔피언 가격이다.** 이렐리아·트런들·스웨인
  등의 `880` 이 그것이다. 그대로 찍으면 **"기본 스킨 - 880RP" 라는 없는 상품**이 된다.
  `isBase` 면 가격을 안 붙인다
- 5000(바다사자 우르프)·150000(우르프 워윅)은 오류가 아니라 **라이엇의 농담 가격**이다
- 표기는 콤마 없이 `1820RP` / `150 (신화 정수 아이콘)` 이다.
  **아이콘 경로는 `v1/lolcurrency.json` 이 알려준다** (`lol_mythic_essence` →
  `assets/currencies/images/mythic-essence-icon.svg`). 15x18 짜리 svg 고 그라데이션이
  파일 안에 있어서 어두운 배경에서 그대로 보인다. RP·블루 에센스 아이콘도 같은 파일에 있다.
  `skinPriceHtml()` 이 **HTML 을 돌려주므로 이스케이프하면 안 된다** — 값은 우리가 만든
  표의 숫자라 위험이 없다

### 크로마 (2026-08-13)

설명 아래에 색점을 한 줄로 깔고, 누르면 큰 칸이 그 크로마 **3D 모델 렌더**로 바뀐다.
다시 누르면 일러스트로 돌아온다.

- **★ 크로마도 같은 `champions/<키>.json` 안에 있다 — 추가 요청이 0이다.**
  **1037개 스킨에 크로마 6972개** (전체 스킨 2111개 중 절반). 한 스킨 최다는
  **2018 월드 챔피언십 카직스 25개**이고 20개를 넘는 건 그 하나뿐이다
- **`colors`(1~2색)가 6972개 전부에 있다 — 누락 0.** 그래서 **색점은 이미지 없이** 그린다.
  두 색이면 `linear-gradient(135deg, …)` 로 반씩 나눈다
- **★ 그림(`chromaPath`)은 270x303 투명 배경 3D 모델 렌더다.** 장당 82KB 라
  카직스면 2MB — **눌렀을 때만 받는다.** 일러스트(가로)와 세로 비율이 달라서
  `object-fit` 을 `cover` → `contain` 으로 바꿔야 한다 (`.is-chroma`). 안 그러면 얼굴이 잘린다
- **★ 뒤에 원래 일러스트를 흐리게 깐다 (`.skin-view-bg`).** 세로 인물이라 칸의 **53%만
  차고 284px 가 비는데**, 그 자리를 메운다. **이미 받아 둔 그림이라 추가 요청이 0이다.**
  - **흐림을 칸에 걸면 안 된다** — 자식인 인물 그림까지 흐려진다. 배경만 따로 깐 층에 준다
  - 칸에 `position: relative` 가 있어야 하고, 배경에 `transform: scale(1.1)` 이 필요하다 —
    `blur` 가 가장자리를 투명하게 갉아 칸 테두리에 띠가 생긴다
  - 스킨을 바꿀 때도 배경을 지워야 한다 (`selectSkin` 과 `selectChroma` 양쪽에서 지운다)
  - **이게 "스킨 인게임 모습" 에 제일 가까운 공식 이미지다.** 위 "프로필 아이콘·모스트" 절에서
    "렌더된 이미지는 없다" 고 적었는데, **크로마가 있는 스킨(1037개)에는 있다**
- 한국어 크로마 이름(`도자기 럭스 (장미석)`)과 설명 1521개가 같이 온다

**★★ 점은 그림 오른쪽 세로줄이다 (`.skin-stage`, 2026-08-13).**
그림 아래 가로줄이던 걸 옮겼다. 세 가지가 한 번에 풀린다 —
**① 세로 예산 40px 회복**(그래서 여유값이 320 → 300 이 됐고 창 900px 에서 그림이
602 → 635 로 커졌다) **② 점이 바꾸는 대상 바로 옆에 온다**(전엔 121px 아래였다)
**③ 그림이 자연히 왼쪽으로 치우쳐** 오른쪽에 점이 놓일 자리가 생긴다.

- **★ `--skin-frame-h` 를 `.skin-stage` 에 두고 그림 `height` 와 점칸 `max-height` 가
  같이 쓴다.** 둘이 어긋나면 접히는 기준이 없어져 **카직스(25개)가 한 칸으로 쭉 뻗어
  113px 넘쳤다** (실제로 그렇게 났다). 지금은 2칸으로 접힌다
- **★ `align-items: stretch` 여야 한다.** `flex-start` 면 점칸이 내용 높이대로 늘어나
  wrap 이 판단할 기준을 못 얻는다
- 점칸이 2칸이 되면 그림이 그만큼 좁아진다 (`.skin-view-frame` 이 flex 항목이라
  알아서 줄어든다). 25개짜리는 카직스 하나뿐이고 그다음이 11개라 나머지 1036개는 1칸이다
- **크로마 이름은 이름 줄 뒤에 ` · 장미석` 으로 붙인다** — 줄을 새로 안 만들어 높이를 안 먹는다.
  **`"… (색이름)"` 꼴이 6972개 중 6971개(100.0%)** 라 괄호 안만 뽑으면 스킨 이름이 안 겹친다
  (유일한 예외는 `2017 월드 챔피언십 애쉬 크로마`)
- **폰에서는 다시 그림 아래 가로줄로 되돌린다.** 세로 화면에서 오른쪽에 세로줄을 세우면
  안 그래도 좁은 그림 폭을 더 뺏긴다

아래는 점이 그림 **아래**에 있던 시절의 기록이다.

**★★ 크로마 줄은 한 줄이어야 한다 — 높이 예산이 빠듯하다.**
처음엔 이름 줄 + 점 줄 2단(59px)으로 만들었는데 설명이 긴 스킨에서 **23px 넘쳐 스크롤이
생겼다** (선지자 야스오 261자+크로마 8개 / 겨울의 축복 헤카림 / 하이 눈 이블린이
**전부 정확히 23px**였다). 한 줄로 합쳐 32px 로 줄여도 **12px 이 모자랐다.**

- **★ 그림 폭과 설명 줄간격은 한 예산을 나눠 쓴다.** 남은 12px 은 그림(600px)을 줄이는
  대신 **설명 줄간격을 1.7 → 1.5** 로 조여서 벌었다 (1.6 은 5px 부족, 1.55 는 2px 밖에
  안 남는다). **하나를 키우면 다른 하나를 조여야 한다**
- **★ 폭 스윕은 단조롭지 않다.** 야스오에서 `580px 은 23px 넘치는데 590px 은 6px` 이었다 —
  폭이 넓어지면 줄 수가 줄어드는 순간이 있어서 뒤집힌다. **한 점만 재고 판단하지 말 것**
- **★ `row.style.display` 에 `block` 을 넣으면 안 된다.** 인라인 style 이 CSS 의
  `display:flex` 를 이겨서 라벨과 점이 위아래로 갈라진다.
  **챔피언 목록에서 겪은 것과 같은 함정이다** (위 "★ 챔피언 목록 항목을 숨길 땐" 항목)
- **라벨은 폭을 못 박는다(150px, 폰 92px).** 안 그러면 크로마를 고를 때마다 이름 길이가
  바뀌어 **점들이 좌우로 밀린다** — 다음 점을 누르려는데 자리가 옮겨진다.
  같은 이유로 줄 자체에도 `width:100%` 를 줘서 내용에 따라 안 흔들리게 했다

**★★★ 오른쪽 일러스트는 폭이 아니라 높이로 정한다 — `min(378px, 75vh - 300px)` (2026-08-13).**
`640px` 고정으로 뒀다가 **"멜은 스크롤 안 생기는데?" 라는 지적에 전제가 드러났다** —
내 측정이 전부 **창 높이 900px** 한 점에서만 이뤄졌던 것이다. 칸 높이가
`75vh - 120px` 로 따라 움직여서(vh 768/800/900/1000 → 456/480/555/630, 차이가 늘 120px),
창이 조금만 낮아도 결과가 뒤집힌다. **고정 폭으로 잰 640px 의 실제 성적:**

| 창 높이 | 500px | 600px | 640px 고정 | **지금(높이 기준)** |
|---|---|---|---|---|
| 768 (노트북) | 36% | 75% | **82%** | **4%** |
| 800 | 13% | 50% | 73% | 1% |
| 860 | 0% | 21% | 39% | 0% |
| 900 | 0% | 0% | 1% | 0% |
| 1000 이상 | 0% | 0% | 0% | 0% |

- **★ 고정 폭으로는 답이 없다.** 노트북(768px)에서는 500px 로 줄여도 36% 가 스크롤한다.
  높이를 창에 맞춰 줄이는 것만이 근본 해결이다
- `300px` = 칸 밖 여백 120 + 글 몫 180(이름 42 + 설명). **크로마 점을 오른쪽으로 옮기면서 320 → 300 이 됐다** (세로 예산 40px 회복).
  여유값 실측(창 768/800/900): `280 → 16%/8%/0%` · **`300 → 4%/1%/0%`** · `320 → 1%/0%/0%`.
  **320 은 창 900px 에서 그림이 602 로 작아진다** — 짧은 창을 더 챙기고 싶으면 320 으로 올린다
- **1000px 이상 창에서는 천장(378px, 곧 641x378)에 걸려 아무 영향이 없다** —
  큰 화면 사용자에게는 640px 고정과 똑같이 보인다
- **★ 폰에서는 이 계산을 되돌려야 한다** (`height:auto; width:100%`).
  폰은 칸이 `75vh` 가 아니라 `auto` 라 `75vh - 300px` 이 칸 높이와 무관하고,
  그 높이로 폭을 역산하면 화면(338px)보다 넓어져 비율이 깨진다
- **★★ 교훈: 높이가 `vh` 로 정해지는 칸 안에서 크기를 맞출 땐 창 높이를 여러 개 재라.**
  한 점(900px)에서만 재고 "12개만 스크롤한다" 고 결론지었는데, 실제로는 그 점이
  **절벽 바로 위**였다 (900 → 1%, 860 → 39%)

아래는 폭만 놓고 재던 시절의 기록이다. **분포로 정한다는 원칙은 그대로 유효하다.**

**★★ 크기는 "최악 한 개" 가 아니라 분포로 정할 것 (2026-08-13).**
처음엔 제일 긴 설명(**멜 293자, 2111개 중 유일한 최장**) 하나를 기준으로 500px 까지 줄였다.
**2110개가 한 개 때문에 손해를 보는 구조**였다. 설명 길이 분포를 보면 답이 다르다 —
중앙값 130자 / 95% 198자 / 99% 222자 / 최장 293자로 **멜만 혼자 튄다.**

측정법: 숨긴 요소에 `.skin-view-desc` 의 폰트·줄간격·폭을 그대로 복사하고 2111개 설명의
높이를 재서 `그림 + 이름 + 설명 + 크로마줄` 이 칸 높이(555px)를 넘는 개수를 센다.
한 챔피언씩 눌러 보는 것보다 훨씬 빠르고 전수다.

| 줄간격 | 600px | 620px | **640px** | 660px | 680px | 694px(꽉) |
|---|---|---|---|---|---|---|
| 1.5 | 0% | 0% | 0.5% | 0.3% | 10.2% | 9.0% |
| **1.7** | 0% | 1.0% | **0.6%** | 12.8% | 10.3% | 35.5% |

- **★ 폭과 넘침은 단조롭지 않다.** `620px 는 22개가 넘치는데 640px 는 12개`다 —
  폭이 넓어지면 줄 수가 줄어드는 지점이 있어 뒤집힌다. **감으로 고르면 틀리니 표를 만들 것**
- 640px 를 골랐다: **12개(0.6%)만 19px(한 줄쯤) 스크롤**하고 2099개는 스크롤이 없다.
  진짜 절벽은 660~680px 사이다
- **★ 줄간격 1.5 는 불필요한 조임이었다.** 크로마 줄이 2단(59px)이던 시절 계산이라,
  한 줄(30px)로 합치면서 이미 여유가 생겼는데 못 알아챘다. **640px 에서는 1.5 든 1.7 이든
  넘치는 수가 12개로 같다** — 이 폭에서 줄간격은 사실상 영향이 없으니 읽기 편한 1.7 을 쓴다
- **`.skin-view-frame` · `.skin-view-name` · `.skin-view-desc` · `.skin-chroma-row` 의
  `max-width` 는 항상 같이 움직인다.** 하나만 바꾸면 그림과 글의 끝이 어긋난다.
  **`.search-box-wrapper`(146줄)도 600px 이라 일괄 치환하면 안 된다**
- 폰에서는 칸이 338px 라 이 상한이 안 걸린다 (그대로 꽉 찬다)

**★★ 폰에서 페이지가 통째로 옆으로 늘어난 원인 — `align-items: flex-start` (2026-08-13).**
`.champ-page-wrap` 은 폰에서 `flex-direction: column` 이 되는데 **기본 규칙의
`align-items: flex-start` 가 그대로 남아 있었다.** 세로 배치에서 그건
**"자식 폭을 내용에 맞춰라"** 라는 뜻이라, 안 줄어드는 칸이 가로로 늘어선 순간
그 내용 폭이 그대로 칸 폭이 된다 — 390px 화면에서 **2202px** 로 벌어졌다.
스킬·스탯 탭은 글이라 줄바꿈돼서 **이 함정이 여태 안 드러났다.** 폰 블록에
`align-items: stretch` 를 넣어 고쳤다.

- **★ `min-width: 0` 은 한 겹만 풀면 소용없다.** `.champ-detail-pane` 엔 이미
  같은 이유로 붙어 있었는데(주석까지 있다), 바로 안쪽 `.champ-detail-inner` 가
  `min-width: auto` 라 **거기서 대신 버텼다.** 둘 다 풀어야 한다
- **★ 진단은 조상 사슬을 재는 것으로 시작할 것.** "어디가 범인인가" 를 추측으로
  좁히다 두 번 헛짚었다. `.skin-list` 부터 `html` 까지 올라가며
  `width / scrollWidth / min-width / overflow-x` 를 찍으니 **`.champ-page-wrap` 370px 인데
  자식 `.champ-detail-pane` 2234px** 라는 게 한 줄로 드러났다

## 배경 탭 (공개) · 대사 탭 (비공개) — 2026-08-13 신설

**대사 탭은 만들어 두고 가려 놨다** (`app.js` 탭 버튼의 `▼▼ 비공개 처리 ▼▼` 주석 한 줄).
채울 게 대표 대사 한 줄뿐이라서다 — 아래 "인게임 대사" 절이 이유다.
내용(`quotesHtml`)·`renderChampQuotes`·`champion_quotes.js` 는 그대로 남겨 뒀으니
주석만 풀면 살아난다. **`champion_quotes.js`(18KB)는 지금 안 쓰이지만 계속 받는다** —
되살릴 때 손댈 곳을 줄이려고 남긴 것이다.


`build_champion_lore.js` 가 **라이엇 Universe** 에서 받아 두 파일을 만든다.

```
universe-meeps.leagueoflegends.com/v1/ko_kr/champions/<슬러그>/index.json
```

- **★ Data Dragon 의 `lore` 는 배경 이야기가 **아니다**.** `blurb` 와 **글자까지 같은**
  짧은 소개(212자)다. 전문은 Universe `biography.full` 에만 있다 (중앙값 2226자, 최대 6779)
- **★ 슬러그는 DD id 소문자인데 레나타 글라스크만 예외**(`renata` 가 아니라 `renataglasc`).
  173명 전수로 확인했고 그 하나뿐이다. **173/173 성공 / 빈 글 0명**
- **★ CORS 가 열려 있다**(`Access-Control-Allow-Origin: *`) — 브라우저가 직접 받을 수도 있다.
  그런데 **한 챔피언에 332KB 인데 쓰는 건 2KB** 라 빌드 때 뽑아 둔다. 런타임 의존도 없앤다
- 결과물이 **두 갈래**다:
  - `public/lore/<챔피언id>.json` **173개** — 배경 전문. 파일 하나에 **문자열 하나**라
    화면에서 `await res.json()` 이 곧 본문이다. 중앙값 5.4KB / 최대 16.3KB(제이스)
  - `public/champion_quotes.js` (18KB) — 대표 대사. 챔피언 데이터와 같이 받아 즉시 뜬다
- **★ 배경을 챔피언별로 쪼갠 이유는 손익분기가 140명이기 때문이다 (2026-08-13).**
  통짜 하나로 뒀을 때 gzip **335KB**(brotli 257KB)인데 챔피언당은 gzip **2.4KB** 다.
  `335 ÷ 2.4 ≒ 140` — **한 사람이 140명의 배경을 열어봐야 통짜가 이득**이라 사실상 없는 일이다.
  서너 명만 보는 실제 사용에서는 **48배 적게 받는다**.
  (전부 합치면 gzip 431KB 로 통짜보다 크다 — 압축 사전을 공유 못 해서인데, 다 볼 일이 없어 무의미하다)
  - 요청 수는 어차피 같다. 챔피언 하나 볼 때 1번인 건 통짜나 쪼갠 거나 똑같고,
    그 1번이 335KB 냐 2.4KB 냐의 차이다
  - 실측: 아리 페이지 → `Ahri.json` 하나만 받음. 다른 챔피언으로 가야 그 파일이 추가되고,
    되돌아오면 재요청 0. **손으로 쓴 `customLore` 가 있는 챔피언(가렌)은 아예 안 받는다**
- **`custom_lore.js`(손으로 쓴 글)가 있으면 그게 우선이다.** 지금 가렌 하나뿐인데
  본문에 럭스 링크가 들어 있어서다. **나머지 171개는 빈 문자열**이라 자동으로 Universe 를 탄다
- 배경 본문은 **`textContent` 로 넣는다.** 생성할 때 태그를 벗겨 순수 텍스트로 만들어 두었고
  CSS 가 `white-space: pre-wrap` 이라 빈 줄이 그대로 문단이 된다. 남의 마크업을
  `innerHTML` 로 믿지 않기 위해서다 (손으로 쓴 `customLore` 만 HTML 로 넣는다)

**★★ 인게임 대사 — 한국어는 공개 데이터에 없다. 영문은 롤위키에 다 있다 (2026-08-13 전수 조사).**

| | 한국어 | 영문 |
|---|---|---|
| **픽·밴 음성** | **있다** — CD `ko_kr/v1/champion-choose-vo/<키>.ogg` (이미 쓰고 있다) | 있다 |
| **전체 대사 음성** | **없다** | **있다** — 롤위키가 `.ogg` 를 호스팅하고 **핫링크된다** |
| **전체 대사 대본** | **없다**(나무위키를 긁는 수밖에) | **있다** — 롤위키 `<챔피언>/Audio` |

- **★ CD 의 픽/밴 음성은 로케일별로 진짜 다른 파일이다.** 가렌 기준
  `ko_kr 43,695B / default 38,675B / ja_jp 38,012B` — 크기가 달라 한국어 더빙이 맞다.
  `baseUrl` 에 `ko_kr` 이 들어 있어서 지금도 한국어로 나간다
- **★ CD 에 전체 음성은 없다.** 다음을 전부 확인했다 — `game/assets/sounds/**` 404,
  `game/ko_kr/assets/sounds/**` 404, `game/characters/<champ>/` 404,
  `game/data/characters/garen/` 안에 오디오 폴더 없음.
  CD 가 주는 챔피언 오디오는 `chooseVoPath`·`banVoPath`·`stingerSfxPath` **3개뿐**이다
- **★ Universe 의 `quote` 도 챔피언당 한 줄**이다 (JSON 전체를 훑어 확인).
  나머지 따옴표 문자열은 관련/추천 챔피언 것이다
- **롤위키 `<챔피언>/Audio` 는 파싱 가능한 형태다** — `{{sm2|파일.ogg}} ''"대사"''` 꼴이고
  **가렌만 664줄**이다. 음성 파일 주소는 API 로 얻는다
  (`action=query&titles=File:Garen_Select.ogg&prop=imageinfo&iiprop=url`).
  `Referer: pixlol.kr` 로도 200 이 온다. **다만 영문 성우 음성·영문 대본이다**
- `stingerSfxPath`(효과음)는 아직 안 쓴다
- 헤더 초상화를 누르면 픽 음성이 난다 (`.champ-header-portrait`)

### 프로필 아이콘 · 모스트 — 보류 (2026-08-13 조사)

**프로덕션 키가 나올 때까지 안 붙인다.** 조사는 끝났으니 다시 하지 말 것.

- **아이콘은 사실상 공짜다.** `SummonerCache` 스키마에 **`iconId` 가 이미 있고**
  전적검색 때마다 `summoner-v4` 응답에서 채워진다. `/api/suggest`(자동완성)는
  **이미 이걸 꺼내 쓰고 있다.** 랭킹에 붙여도 라이엇 호출은 한 번도 안 는다
  - 붙일 때 고칠 곳 2군데: **`loadResolvedNames()` 가 `displayName`·`updatedAt` 만
    메모리에 올린다** — `iconId` 도 같이 올려야 랭킹 응답에서 쓸 수 있다.
    그리고 `/api/ranking` 의 `processedPlayers` 에 필드 추가
  - **커버리지가 "전적검색된 적 있는 사람" 뿐인 게 한계다.** 백그라운드 닉네임 변환
    잡은 `account-v1` 만 부르는데 **그 응답엔 아이콘이 없다**
- **모스트는 캐시가 아예 없다.** `/api/mastery/:puuid` 는 라이엇 직행 프록시라
  저장도 `myCache` 도 안 탄다. 1명당 1회 호출이고 재사용이 안 된다
- **★ 막는 건 개발 키의 2분당 100회다.** 랭킹 한 페이지가 50명이라 모스트만 채워도
  한도의 절반이고, 페이지 두 번 넘기면 429 다
  - **백그라운드로 미리 채우는 길도 막혀 있다.** `resolveNamesInBackground` 실효 속도가
    **분당 20명**(1.2초 x 20명 / 60초 사이클)이라 1.1만 명 한 바퀴가 **약 9시간**인데,
    **랭킹 명단은 10분마다 갈린다** — 다 채우기 전에 새 사람이 들어와 끝이 안 난다
  - **잡에 `summoner-v4` 를 얹는 것도 손해다.** 호출이 2배가 되니 **닉네임 채우는 속도가
    절반**이 된다. 닉네임조차 아직 다 못 채운 상태면 우선순위가 뒤바뀐다
- **★ 키가 나오면 이 순서로 한다**: ① `loadResolvedNames` 에 `iconId` 추가(공짜, 즉시)
  ② 전적검색이 **이미 받아오는** 마스터리(`app.js:654`)를 DB 에 저장만 하기(공짜)
  ③ 그 다음에야 백그라운드 전원 채우기. **①②는 키와 무관하게 언제든 된다** —
  보류한 건 "전원 채우기" 지 "붙이는 것" 자체가 아니다

**★★ 헤드리스 크롬은 창 폭 최소가 500px 다 — `--window-size=390` 이 안 먹는다 (2026-08-13).**
390 으로 찍은 스크린샷이 **오른쪽이 잘려 나와 "표가 넘친다" 고 오진했다.** 실제로는
페이지가 500px 로 잡히고 이미지만 390 으로 잘린 것이었다. 진짜 폰 폭으로 재려면
**CDP 의 `Emulation.setDeviceMetricsOverride`** 를 써야 한다.
puppeteer 없이도 된다 — **node 22+ 에 `WebSocket` 이 내장**이라
`--remote-debugging-port` 로 띄우고 `/json/list` 로 타깃을 잡아 붙이면 그만이다.
덤으로 `Runtime.consoleAPICalled`·`Runtime.exceptionThrown` 으로 **콘솔 에러도 같이 걷힌다**
(`--dump-dom` 은 DOM 만 주므로 에러를 보려면 페이지에 수집 스크립트를 심어야 했다).

## 경기 링크 — `/summoner/<라이엇 ID>/<경기 번호>` (2026-08-14 신설)

친구에게 특정 경기를 통째로 보낼 수 있는 주소. 경기 칸 오른쪽 위 🔗 를 누르면 복사된다.

- **★ 서버는 손댈 게 없다.** `server.js` 맨 끝 `app.get(/.*/, sendIndexHtml)` 가 모든 주소에
  index.html 을 주고, 라우팅은 `app.js` 진입부가 `location.pathname` 을 잘라서 한다.
  **새 주소를 만들 땐 `app.js` 의 `pathParts` 분기와 `popstate` 두 곳을 같이 고칠 것** —
  한쪽만 고치면 뒤로가기로 들어왔을 때 다르게 동작한다 (랭킹 헤더에서 겪은 것과 같다)
- **★ `executeSearch` 의 `pushState` 가 경기 번호를 지워 버린다.** 검색이 끝나면
  `/summoner/<이름>` 을 밀어 넣는데, 링크로 들어온 경우 **펼치기도 전에** 주소가 바뀌어
  새로고침하면 링크가 죽는다. `window.pendingMatchId` 가 있으면 주소에 같이 붙인다
- **검색은 최근 20판만 받아온다.** 그보다 오래된 경기는 `openPendingMatch()` 가
  "더 보기"를 **최대 3번**(총 50판) 눌러 가며 찾고, 못 찾으면 안내를 띄운다.
  더 늘리면 라이엇 호출만 늘어난다
- **★ 복사 버튼은 `.toggle-btn` 위에 겹쳐 있다.** 토글은 오른쪽 35px 을 **세로로 다 쓰는데**
  화살표는 맨 아래에만 있어서(`align-items: flex-end`) 위쪽이 비어 있다. 그 자리를 쓴다.
  그래서 두 가지가 필수다 — **`z-index: 2`**(없으면 클릭이 토글에 먹힌다)와
  **`stopPropagation`**(없으면 주소만 복사하려다 상세가 같이 열린다). 둘 다 실측 확인했다
- **주소에 넣는 이름은 라이엇이 준 정본(`currentProfileName`, `이름#태그`)이다.**
  검색창에 친 글자를 쓰면 대소문자가 제각각인 주소가 돌아다닌다
- **★★ `navigator.clipboard` 는 문서에 포커스가 없으면 실패한다 — 헤드리스에서는 항상 실패다.**
  `Emulation.setFocusEmulationEnabled` 를 안 켜고 재고는 **"복사 기능이 고장났다" 고 오진했다.**
  켜니까 바로 성공했다. `execCommand` 폴백도 포커스가 필요해서 같이 실패하니
  **폴백까지 실패한다고 코드 탓으로 몰지 말 것**
- 링크 미리보기 카드(`og:title`)는 아직 없다. 하려면 `sendIndexHtml` 이 그 주소일 때만
  DB 를 한 번 더 읽어 메타 태그를 끼워야 한다

## 전적 요약 통계 박스 (2026-08-14 정비)

`renderSummaryStats` 가 모드별로 패널 셋 중 하나를 그린다 —
협곡(`renderSummaryStats` 본체) / 칼바람(`renderAramSummaryHtml`) / 아레나(`renderArenaSummaryHtml`).
**셋이 같은 골격을 복붙해 쓰므로 한쪽만 고치면 필터를 누를 때 박스가 흔들린다.**

- **★ 오른쪽 칸 폭은 세 패널 모두 176px 이어야 한다.** 칼바람만 181px 이던 때
  구분선이 협곡·아레나보다 **1px·4px 왼쪽**에 찍혔다 (가로 238/508 → 237/504, 실측).
  박스가 `justify-content: space-between` 이라 **칸이 5px 넓어지면 남는 폭을 다시 나눠서
  구분선 네 자리가 전부 밀린다.** 구분선 자체(1x90)는 셋이 원래 같았다 —
  "구분선이 다르다" 는 증상의 원인이 구분선이 아니었다
- **★ 가운데 칸의 줄 높이(28px + margin 6)를 바꾸면 안 된다.** 가운데 칸(129px)이
  박스 높이(181px)를 정하는 칸이라, 줄 높이가 달라지면 **모드마다 박스 높이가 달라져
  필터를 누를 때 아래 목록이 위아래로 튄다.** 항목이 3개가 안 되면 빈 줄로 채우는 이유다
- **칼바람·아레나는 "플레이한 챔피언" 대신 "플레이한 역할군" 이다** (`renderRoleRowsHtml`).
  두 모드는 라인이 없어 챔피언을 나열해도 성향이 안 보인다
  - **★ 역할군은 `fetchChampionMap()` 이 채운다 — 추가 요청이 0이다.** 전적 페이지가
    이미 `champion.json` 을 받고 있었다(`championIdMap`·`korChampMap` 용). 거기서 `tags` 를
    같이 담는다. **`showChampions()` 와 같은 표(`champRoleMap`)를 쓰고 `isClassicChamp`
    가드도 똑같이 걸어야 한다** — 클래식 챔피언이 섞이면 스탯 탭 역할군 평균의 모집단이 흔들린다
  - **챔피언 하나가 역할군 2개에 들어가므로 역할군별 게임 수의 합 > 총 게임 수다.** 정상이다
  - **★ 아레나만 문법이 다르다 — `N게임 우승 N회`.** 아레나는 승패가 아니라 등수이고
    (경기 칸도 `placementText` 로 등수를 찍는다), 같은 패널 왼쪽에 "평균 등수" 가 있는데
    옆에서 승률을 말하면 서로 어긋난다. 칼바람은 `(N승 N패 N%)` 다

## 아레나 밴 (2026-08-14 조사)

**★★ 아레나는 밴이 있다. 코드 주석이 "칼바람·아레나는 밴이 없다" 고 적고 있었는데 틀렸다.**
MongoDB `matchcaches` 의 아레나 153판 전수로 확인했다.

| | 값 |
|---|---|
| 참가자 수 | 18명 149판(6조 x 3명) / 16명 4판(구버전 8조 x 2명) |
| 밴 개수 | **참가자 수와 정확히 일치 — 1인 1밴** |
| 실린 곳 | 153판 전부 `teams[0]`(teamId 100) 한 곳. `teams[1]` 은 **`teamId: 0`** 에 빈 배열 |
| `pickTurn` | 153판 전부 1..N 연속 |
| `-1`(시간 초과) | 2746개 중 497개(18%) |

- **★ 그래서 `renderTeamSummaryRow` 가 아레나 밴을 통째로 버리고 있었다.** 이 함수는
  `teamId === 200` 을 찾아 없으면 `return ''` 하는데 아레나엔 **100 과 0** 뿐이다.
  게다가 아레나 상세는 `renderArenaDetail()` 이라는 별도 경로라 그 함수를 부르지도 않는다.
  지금은 아레나 상세 표 맨 아래에 `.arena-ban-summary` 줄로 18개를 한 줄에 깐다
- **★ 아레나의 `teamId` 는 진영이 아니라 "상위 절반 / 하위 절반" 이다** (153/153 확인).
  1~3등 조가 100, 4~6등 조가 200 이다. 그래서 **밴 출처를 가르는 데 못 쓴다**

**★★ 밴이 누구 것인지 라이엇은 안 알려준다. 그런데 순서에 답이 있다 (통계로 확인).**
`bans` 항목엔 `championId` 와 `pickTurn` 뿐이고 puuid 도 조 번호도 없다.

- **① `pickTurn` 은 "밴 넣은 순서" 가 아니라 고정 자리다.** `-1`(시간 초과) 비율을
  자리별로 세니 1~18번이 **13~26% 로 고르다.** 넣은 순서 기록이었으면 시간 초과가
  뒷번호에 몰려야 한다
- **② 그 자리는 조 번호 순으로 3개씩 묶여 있다** — `pickTurn 1~3 = 조1, 4~6 = 조2, …
  16~18 = 조6`. 검증법: **사람은 밴 습관이 있다.** 같은 puuid 가 여러 판에 나올 때
  가설대로 배정한 밴이 판마다 반복되는지를 센다. **두 판에 겹치는 사람이 본인뿐인
  매치쌍만** 남겨 같은 파티가 연달아 한 경우를 걷어냈다

  | 배정 가설 | 같은 사람의 밴이 반복될 확률 |
  |---|---|
  | 무작위 (대조군) | 1.5 ~ 2.4% |
  | 참가자 배열 순서 그대로 | 2.9% — **대조군과 구분 안 됨, 틀렸다** |
  | **조 번호 순** | **9.2%** (z ≈ 10) |

  조 단위(3개 묶음) 겹침은 **40.5% vs 무작위 14.1%**. 조↔블록 대응을 한 칸씩 밀어 보면
  `r=0` 40.5% / 나머지 다섯은 12.6~19.7% 라 **어긋난 배치는 전부 떨어진다**
- **블록 안 3자리 중 누구인지는 약하다** — 참가자 배열 등장 순 9.2% / 뒤집기 6.6% /
  한 칸 회전 4.1%. **조 단위는 확실하고 개인 단위는 유력** 정도다
- **★ 아직 인게임 확인을 안 했다.** 아레나 한 판에서 내 조 3명의 밴만 적어 두면 끝난다 —
  그 3개가 `pickTurn (조번호-1)*3+1 ~ +3` 에 들어 있는지 보면 된다.
  조가 6개라 우연히 맞을 확률이 낮아 **한 판으로 갈린다**
- 서버는 `bans` 를 **`pickTurn` 순서로 못 박아** 내려보낸다 (`buildHistoryEntry`).
  이 순서가 곧 조 순서라서, 확인이 끝나면 3개씩 잘라 쓰기만 하면 된다

**★ 아레나 밴 관련 사실을 "라이엇 데이터끼리 대조" 로는 못 얻는다.** `teams[0]` 에
전부 몰려 있다는 것도, `teamId 0` 이라는 것도 스키마상 이상하지 않아서 기계 검사에
안 걸린다. **실제 데이터를 세어 봐야 드러난다** — 위 "라이엇 데이터끼리만 대조하면"
항목과 같은 종류의 함정이다.

## CD 스킬 데이터 작업 (현재 주 작업)

### 구조

- `public/custom_templates.js` = 문장, `public/custom_values.js` = 수치. `index.html`이 직접 로드
- 문장 안 빈칸은 `{p1}` `{p2}` 형식
- `v1`·`v2`는 구분선 아래 피해량 줄 (p랑 역할이 다름). 지금 전부 비어 있고 직접 작성 대상
- `stats`는 객체 형식 `{ "사거리": "300" }`

### 스크립트 (전부 프로젝트 루트)

| 파일 | 역할 |
|---|---|
| `build_champion_data.js` | 문장·쿨타임·소모값·사거리 생성 |
| `fill_values.js` | 수치 채움. `--write` 붙일 때만 파일 생성 |
| `check_data_sources.js` | 확인 전용 |
| `probe_spell_fields.js` | 확인 전용 |
| `audit_skill_meta.js` | 확인 전용. **스킬 칸 우상단 값 전수조사** (아래) |
| `bincache.js` | 챔피언 bin 로컬 캐시 (아래) |
| `build_level_curves.js` | 레벨 1~18 곡선 생성 → `level_curves.json`, `champion_stats_by_level.json` |
| `add_level_graphs.js` | 곡선을 각주로 → `public/custom_graphs.js` |
| `build_stats_page.js` | 챔피언 스탯 압축 → `public/champion_stats.js` (스탯 탭이 쓴다) |
| `build_skin_prices.js` | 스킨 가격 → `public/skin_prices.js` (스킨 탭이 쓴다). **롤위키가 출처** — 위 "스킨 탭" 절 참고 |
| `build_champion_lore.js` | 배경·대사 → `public/champion_lore.json` + `champion_quotes.js`. **라이엇 Universe 가 출처** |
| `belowline.js` | 구분선 아래 회색 글씨 원문. **위 두 생성 스크립트가 같이 쓰는 모듈** |
| `survey_belowline.js` | 확인 전용. 회색 글씨 전수조사 보고서를 찍는다 |

**★ 패치가 오면 이 순서로 다시 돌린다** (앞이 뒤의 입력이라 순서가 중요하다):

```
node build_champion_data.js --refresh     # 문장
node fill_values.js --refresh --write     # 수치 (custom_values.new.js → 확인 후 이름 변경)
node build_level_curves.js --write        # 레벨 곡선
node add_level_graphs.js --write          # 각주 (custom_values.js 를 읽으므로 위가 먼저)
node build_stats_page.js --write          # 스탯 탭 (champion_stats_by_level.json 이 입력)
```

**두 번째 줄의 `--refresh` 는 첫 줄이 캐시를 이미 새로 채웠으면 빼도 된다** (같은 bin 을
두 번 받을 뿐이다). 첫 줄만 `--refresh` 로 돌리는 게 실제로 쓴 순서다.

**★ 언제 돌려야 하나 — DD 와 CD 는 약 12시간 차이가 난다 (2026-08-13 실측).**

| 소스 | 갱신 시각 |
|---|---|
| **DD** (`championFull.json`) | 매 패치 **화요일 19:0x UTC** = **수요일 04:0x KST** |
| **CD** (`latest` 경로 전부) | 그로부터 **약 12시간 뒤** = **수요일 16:0x KST** |

DD 는 시계처럼 규칙적이다 — 16.13.1 부터 16.16.1 까지 넷 다 화요일 19:06~19:13 UTC 였다.
CD 는 16.16 실측이 `bin 06:58 / stringtable 07:08 / champion-summary 07:13 UTC` 였다.

- **패치 당일에는 한국시간 오후 4시 이후에 돌릴 것.** 그 전에 돌리면 **DD 는 새것, CD 는
  헌것**이라 값 이름이 어긋나고 `?` 가 무더기로 생긴다 (2026-08-12에 실제로 겪었다)
- 돌리기 전 확인:
  `curl -sI https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/ko_kr/v1/champion-summary.json | grep -i last-modified`
  날짜가 패치 당일이면 반영된 것이다
- **CD 의 버전별 경로(`/16.15/`, `/16.14/`)의 Last-Modified 는 믿지 말 것.**
  16.13 과 16.14 가 `6/26 22:37` 로 **같은 시각**이다 — 아카이브되면서 일괄 갱신된 값이라
  "그 버전이 처음 올라온 시각" 이 아니다. 진짜 반영 시각은 `latest` 경로에만 남는다

**★ 패치를 받은 뒤 반드시 확인할 것 — "값이 통째로 평평해진 스탯" (2026-08-12 사고).**
16.16 에서 **173명 전원의 마법 저항력 성장이 0** 이 됐다. 화면에는 수평선으로 조용히
나가서 눈으로는 안 걸린다. 원인은 **패치가 아니라 우리 코드**였다:

- `build_level_curves.js` 의 `mf()` 가 **키를 하나만** 봤고, 호출부가 `mf(root, hk('mrPerLevel'))`
  처럼 **해시를 직접 만들어** 넘기고 있었다
- **CD 는 이번 패치부터 이 필드를 실명(`mrPerLevel`)으로 풀어 준다.** 해시 키가 사라지니
  빈손이 됐고, 기본값(`baseMR`)은 실명이라 살아남아 **"기본값만 있고 성장은 0"** 이 됐다
- `mf()` 를 `fld()` 기반으로 바꿔 **해시·실명 둘 다** 보게 했고 호출부의 `hk()` 를 벗겼다.
  자원(`arBase`/`arPerLevel`/`arBaseStaticRegen`/`arRegenPerLevel`) 4자리도 같은 지뢰였다
- **검산법**: `champion_stats_by_level.json` 에서 스탯별로 "18칸이 전부 같은 값인 챔피언 수"를
  세어 이전 판과 비교한다. 갑자기 173/173 이 되면 그 스탯이 죽은 것이다.
  **압축본(`champion_stats.js`)은 4줄만 바뀌어서 diff 로는 안 보인다** — 원본 json 을 볼 것
- 이 버그에 가려 **원거리 딜러 28명의 진짜 MR 상향(30 → 33)** 이 안 보이고 있었다

`DD_VER` 기본값도 `build_level_curves.js` 위쪽에 하드코딩돼 있다. 패치 때 같이 올릴 것.

**★★ 오류가 숨는 구조는 하나다 — "값을 못 읽으면 조용히 0 이 된다" (2026-08-12).**
같은 날 나온 세 건이 전부 이 모양이었다:

| 오류 | 표면 증상 |
|---|---|
| `mrPerLevel` 을 해시 키로만 조회 | 173명 마법 저항력 성장 0 (수평선) |
| `baseStaticHPRegen` 필드 생략을 0 으로 처리 | 8명 체력 재생 0 |
| `ClampSubParts` 의 `mCeiling: null` | 카이사 E 충전시간 `0.6 ~ 0` |

셋 다 **예외로 안 터지고 그럴듯한 숫자로 화면에 나가서** 눈으로 안 걸렸다.

**더 뼈아픈 건 교차검증 코드가 이미 있었는데 못 잡았다는 것이다.**
`mrPerLevel` 은 `CHECK` 목록에 **있었는데** 당시 bin 이 해시 키라 `undefined` 가 되고,
`if (bv === undefined) continue` 에서 조용히 빠졌다 — **검사 대상인데 검사가 안 됐다.**

그래서 고쳤다: **① `mf()` 로 조회(해시·실명 둘 다) ② 못 읽은 필드를 건너뛰지 말고
경고로 출력 ③ `CHECK` 에 체력 재생 추가.** 지금 파이프라인이 이렇게 찍는다:

```
★ bin 에서 못 읽은 필드 4종 — 0 으로 나가면 사고다. DD 값을 확인할 것:
  baseStaticHPRegenModifiable  8명   Kayle, Maokai, ...
```

**이 줄이 늘어나면 반드시 DD 와 대조할 것.** 지금 4종은 전부 정당하다 —
세나 공격력·쓰레쉬 방어력은 영혼으로 크고(DD 도 0), 브라이어는 체젠이 없다.
**"필드 없음 = 0" 이 아니라 "= 라이엇 기본값" 일 수 있다는 게 핵심이다.**

**`bincache.js` — 재실행이 166초에서 9초가 된다 (2026-08-10 추가).**
두 생성 스크립트가 챔피언 173명 bin 을 매번 새로 받던 걸 `.cache/bin/` 에 7일 캐시한다.
표 한 줄 고치고 결과를 보는 짧은 반복이 이것 때문에 통째로 느렸다.
캐시로 만든 결과가 **기존 파일과 바이트 단위로 동일**한 것까지 확인했다.

- **패치 직후에는 `--refresh` 를 붙일 것.** 안 그러면 최대 7일 묵은 bin 을 본다
- 캐시 적중이면 챔피언 사이 대기시간(120~150ms)도 건너뛴다 — 그게 20~26초였다
- 받기에 실패하면 오래된 캐시라도 쓴다 (CD 가 잠깐 죽어도 작업이 안 멈춤)
- `.cache/` 는 gitignore 대상. 지워도 다시 받으면 그만이다

`fill_values.js` 안의 표 두 개가 중요해:

- **`MANUAL`** — `"미스 포츈 W / LoveTapRefund"` 형식 키. 손으로 확인한 값을 적으면 자동 추출보다 우선하고 재실행해도 안 날아감. 영문 alias도 허용. 안 쓰인 키는 오타 경고로 잡힘. 현재 9건
- **`PRESERVE`** — **2026-08-08에 비웠다(`[]`).** 가렌·갈리오가 유일한 항목이었는데 `{pN}` 체계 이전의 옛 형식(pN이 아예 없음)이라 구조가 달랐어. 이제 전부 CD에서 받아옴
- **`extractVV()`** — 손으로 쓴 `v1`/`v2`를 기존 파일에서 **원문 그대로** 물려줘. 여러 줄짜리(`drawGraph` 호출 등)도 줄 단위로 옮기니까 안 깨져. **PRESERVE 없이도 손작업이 안 날아가는 이유가 이거야.** 지금은 `v1`/`v2`가 전부 비어 있어서 아무것도 안 물려주지만, 앞으로 작성할 때를 위해 남겨둔 장치야

`--write`는 `public/custom_values.new.js`를 만들어. 확인한 뒤에 `custom_values.js`로 이름을 바꿔야 반영돼.

### 스킬 칸 우상단 값 전수조사 — `audit_skill_meta.js` (2026-08-14 신설)

화면 우상단에 나가는 건 네 종류다 (`app.js` 의 `champ-skill-cooldown-header` ·
`champ-skill-cost-header` · `champ-skill-stats-header`):
**쿨타임 / 소모값 / `stats` 전체(사거리 · 시전시간 · 투사체 속도 · 스킬 폭)**.

```
node audit_skill_meta.js           # 지금 화면에 나가는 custom_values.js 를 본다
node audit_skill_meta.js --new     # fill_values.js 가 갓 만든 .new.js 를 본다
node audit_skill_meta.js --full    # 예시를 안 자르고 전부 찍는다
```

**★ 대조 상대가 셋이라 교차검증이 된다** — 우리 값 / **DD `championFull`**(독립 출처,
`cooldownBurn`·`costBurn`·`rangeBurn`·`maxrank`) / **bin**(원본. 값이 **어느 스펠 객체에서
왔는지**까지 본다). 14가지를 센다: 패시브 쿨 누락, 쿨타임·소모값·사거리의 DD 불일치,
칸 수 불일치, 빈칸, 투사체 속도 오염 등.

- **★ 첫 실행에서 410자리가 걸렸고 지금은 2자리다.** 고친 것 —
  패시브 쿨타임 20 + 사거리 밀림 17 + 투사체 속도 오염 310 + 사거리 더미 37.
  **남은 2자리는 둘 다 정당하다** — 암베사 P(라이엇이 stringtable 문장을 비워 뒀다) /
  잔나 W(우리 `550` 이 DD 의 `4294967295` 보다 낫다)
- **★★ 쿨타임과 소모값은 DD 와 100% 일치한다.** 865슬롯 전수에서 불일치 0,
  칸 수 불일치 0, 빈칸 0. **`MAX_RANK` 표(우디르 6랭크 등)도 이걸로 검증된다** —
  칸 수가 DD `maxrank` 와 어긋나면 ③번에 걸린다
- **★ "값이 있다" 와 "값이 맞다" 는 다르다.** 투사체 속도는 634자리가 **꽉 차 있어서**
  겉보기엔 완벽했는데 절반이 남의 값이었다. **빈칸을 세는 것만으로는 못 잡는다** —
  값의 **출처**를 bin 에서 되짚어야 드러난다

**★★★ 라이엇 데이터끼리만 대조하면 "일관되게 쓸모없는 값" 은 절대 안 걸린다 (2026-08-14).**
DD 와 bin 은 **같은 라이엇 소스**라, 둘이 일치한다는 건 "라이엇이 그렇게 적었다" 는 뜻이지
**"그게 화면에 나갈 값이다" 는 뜻이 아니다.** 사거리 `25000` 이 정확히 이 함정이었다 —
DD `rangeBurn` 도 25000, bin `castRange` 도 25000 이라 **기계 대조에서 불일치 0** 이 나왔고,
그래서 나는 이걸 **정당한 값으로 한참 착각했다.** 깨준 건 "아트록스 Q·유미 Q 는 전역이
아닌데?" 라는 지적이었고, 확인해 보니 롤위키는 유미 W 를 **700**, 아트록스 Q 를
**`RANGE: Varied`** 라고 적고 있었다.

| 출처 | 잘 잡는 것 | 못 잡는 것 |
|---|---|---|
| **DD `championFull`** | 대량 기계 대조 (865슬롯 즉시). 쿨타임·소모값이 여기서 100% 검증됐다 | bin 과 같은 소스라 **둘 다 틀린 값**은 못 잡는다 |
| **bin** | **값의 출처 추적** — "이 숫자가 어느 객체에서 왔나". 투사체 속도 310자리는 이것만이 잡았다 | 〃 |
| **위키** | **라이엇 데이터에 없는 값과 "표시 의미"** — 25000 의 진짜 값, `Varied` 같은 개념 | 대량 대조에 부적합. **틀리거나 뒤처지는 경우가 많다**(아래 항목) |

- **★★ 위키에서 우상단 값을 찾는 법 (2026-08-14 실전 정리).**
  - **롤위키 스킬 상자에 `SPEED:` 가 있다** — 투사체 속도를 여기서 얻는다.
    덕분에 본체에 기본 공격 속도가 박힌 자리를 **"빼기" 가 아니라 "고치기"** 로 처리했다
    (카직스 W 1700 · 미스 포츈 R 2000). `TARGET RANGE:` 는 사거리다
  - **나무위키는 "투사체 속도" · "돌진 속도" 를 그대로 적는다.** 롤위키보다 나은 경우가 있다
    (브라이어 R `돌진 속도: 2500 ~ 5000`, 진 R `투사체 속도: 3000`, 코르키 R `2000`)
  - **★ 나무위키 URL 이 챔피언마다 다르다.** `럼블` 은 되는데 `럼블(리그 오브 레전드)` 는 404 고,
    `레오나`·`미스 포츈`·`르블랑`·`키아나`·`니코` 는 동음이의어라 **접미사가 필요**하다.
    `마스터_이` 는 404 고 `마스터 이`(공백)가 맞다. 한 번에 안 되면 양쪽을 다 시도할 것
  - **★ 나무위키가 슬롯을 헷갈리게 적는 경우가 있다** — 라칸 E 를 `1700` 이라 적었는데
    그건 **W** 값이다 (롤위키 `W: 1700` 으로 갈렸다)
  - **★ 나무위키가 "클라이언트가 틀렸다" 고 알려주기도 한다** — 니코 패시브 쿨이
    `"클라이언트 설명에는 이유는 모르겠지만 6초로 나와 있다"` 였다.
    **우리는 클라이언트 툴팁을 재현하는 사이트라 6 을 쓰는 게 맞다** (실제 쿨은 2초)
- **★ 그렇다고 위키를 먼저 믿으면 안 된다.** 위키가 우리보다 틀린 사례가 이미 여럿이고
  (2026-08-11 인게임 확인 6건 중 **4건이 우리가 맞았다**), **이번에도 요약 모델이
  아트록스 E 를 "지옥의 사슬 825" 로 잘못 매칭했다** — 실제 E 는 대시고 bin 의
  DataValue `EMaxRange` 가 300 이다. **위키는 "라이엇 값이 화면에 나갈 만한 것이냐" 를
  판단할 때 쓰고, 수치 자체는 bin 을 믿는다**
- **★ 판단 순서**: ① DD 로 전수 기계 대조(빠르다) ② 안 걸린 자리 중 **값이 수상하게
  크거나 작거나 한 종류로 몰려 있으면** 위키로 뜻을 확인 ③ 출처가 의심되면 bin 으로 되짚기.
  **②를 건너뛰면 이번 같은 착각을 한다**

### 폴백 동작

`app.js`의 `renderScalingTable`에 ? 가드가 있어. p 값이 하나라도 `""`나 `"?"`면 **문장을 통째로 버리고** Data Dragon 툴팁으로 폴백해.

문제는 DD 툴팁도 깨져 있는 경우가 있다는 거야. 말파이트 W가 그 예였어. DD 한국어 툴팁에 `[스탯 비례]`랑 `%i:OnHit%` 같은 미해결 토큰이 그대로 들어 있어서 폴백해도 흉하게 나왔어. (2026-08-08에 MANUAL로 채워서 지금은 폴백 안 함) 그래서 **폴백에 기대지 말고 MANUAL로 채우는 게 근본 해결**이야.

### 알아낸 규칙 (다시 알아내지 말 것)

- **★ 게임의 기본 치명타 피해량은 200% 다. `fill_values.js` 의 `CRIT_DAMAGE` 상수 (2026-08-12).**
  bin 의 치명타 배율은 `1 + k x (치명타 피해량 - 1)` 꼴이라 그대로 찍으면 계산식이 화면에 남는다.
  200% 는 나무위키 **4자리 교차 확인**으로 확정했다 — 가렌 E `x1.3`(k=0.3), 미포 Q `x1.5`(k=0.5),
  미포 R `x1.3`, **갱플랭크 Q `200%`(k=1, 이게 곧 기본 치명타 피해량이다)**.
  C=1.75 면 넷 다 안 맞는다. **라이엇이 바꾸면 이 상수 한 줄만 고치면 된다.**
  `추가 치명타 피해량`(아이템 초과분)은 일부러 안 접는다 — 사람마다 달라서 문장에 남는 게 정보다
- **★ "무한의 대검 보유 시" 각주 16자리 (2026-08-12).** 무한의 대검(아이템 3031)은
  **치명타 피해량 +30%** 라 200% → 230% 가 된다 (라이엇 아이템 데이터에서 직접 확인).
  `fill_values.js` 가 값을 만들 때 **치명타 배율을 접었으면**(`critTouched`)
  `CRIT_DAMAGE + IE_CRIT_DAMAGE` 로 **한 번 더 계산해서** 값이 달라지면 각주를 단다.
  가렌 E 130→139%, 미포 Q 150→165% 로 **나무위키 "무한의 대검 구비 시" 와 정확히 맞는다.**
  - **★ 다시 계산할 땐 검산용 로그 배열을 되돌려야 한다.** `withCritDamage()` 가 길이를
    기억했다 잘라낸다. 안 그러면 미리보기 출력이 두 배가 되고 "어디서 건졌나" 목록이 거짓이 된다
  - **각주 HTML 은 홑따옴표만 쓸 것.** `custom_values.js` 는 값이 큰따옴표 문자열이라
    `"` 가 들어가면 이스케이프되고 `add_level_graphs.js` 의 줄 파싱 정규식이 통째로 깨진다
  - **`add_level_graphs.js` 는 값에 이미 달린 각주 수를 세고 그 뒤부터 번호를 매긴다**(`preNotes`).
    안 그러면 한 스킬에 `[1]` 이 두 개 생긴다. 지금은 겹치는 스킬이 없지만 대비해 둔 것
  - **★ `timesText` 에서 숫자끼리는 반드시 `foldProduct` 로 곱할 것.** `0.3 x 1.3` 이
    `0.3의 130%` 가 되면 뒤쪽 계산이 통째로 어긋난다. 기본값에서는 배율이 딱 1 이라
    안 보였고 **무한의 대검 판본을 만들다가 드러났다**
- **★ 배율 표기는 `timesText()` 하나를 거친다 (2026-08-12).** `A x M` 을 `A의 N%` 로 바꾼다.
  화면에 `x` 가 남던 22자리를 **1자리(블라디미르 P, 원래 깨진 식)** 로 줄였다. 규칙 4가지:
  숫자 배율 → `의 N%` / `스탯의 N% x k` → 계수를 직접 곱함(그웬 Q `주문력의 25%`) /
  `(A x a) x b` → 배율끼리 먼저 접음(자크 R) / 나머지는 최소한 괄호로 묶음.
  - **★ `/` 는 나눗셈이 아니라 랭크 구분자다.** 식을 계산할 땐 `/` 가 하나라도 있으면 손대지 말 것.
    2026-08-12에 람머스 W `1 + 0.3 / 0.375 / ...` 를 나눠서 `664.4%` 라는 엉터리를 냈다
  - **치명타 배율은 기본값에 녹이지 말 것.** `mModifiedGameCalculation` 의 push-down 에 태우면
    가렌 E 가 `5.2 / 9.1 / 13 / 16.9 / 20.8` 로 소수점투성이가 된다. 배율 조각에 `mStat 9` 가
    있으면 push-down 을 건너뛰고 `의 130%` 로 따로 보여 준다 (나무위키 표기와도 같아진다)
  - 덤으로 `%%` 가 찍히던 5자리도 잡혔다 (애쉬 P, 야스오 Q, 요네 Q, 탐 켄치 P·Q)
  - **★ 범위 배율(`1 ~ 2`)은 양 끝을 각각 곱해 펼친다 (2026-08-12).** `A의 100 ~ 200%` 는
    곱셈이 두 겹이라 안 읽힌다. 카이사 E 이동 속도가
    `55 / 60 / 65 / 70 / 75% ~ 110 / 120 / 130 / 140 / 150%` 로 나가고 **롤위키 표기와 같다.**
    **왼쪽이 순수 숫자 리스트일 때만** 한다 — 계수가 섞이면(유미 R `30 / 50 / 70 (+ 주문력의 12%)`)
    양 끝을 못 곱하므로 `의 N%` 로 물러난다. bin 전체에 범위 배율은 2자리뿐이다
  - **★ `A x 스탯의 N%` 는 계수를 스탯 쪽으로 접는다 (2026-08-12).** 규칙 ①의 좌우가
    뒤집힌 꼴이다. 아이번 P 가 유일한 사례 — `15 ~ 0.006 x 기본 최대 체력의 1%`
    → **`기본 최대 체력의 15 ~ 0.006%`**. **배율 자리는 `[가-힣][가-힣 ]*의` 로 좁힐 것** —
    `.+의` 로 두면 `1 + 추가 공격 속도의 100%` 같은 덧셈식까지 삼켜 11자리가 깨진다
  - **화면에 `x` 가 남은 자리는 21곳이고 19곳은 이 규칙 밖이다** (배율이 괄호식이거나
    치명타 피해량이 변수인 자리 — 람머스 W, 볼리베어 W, 카타리나 R, 트페 W 등).
    **성격이 달라 손대면 위험하니 남겨 둔 것이다**
- **★ 한쪽 끝이 `null` 인 "열린 clamp" 가 있다 (2026-08-12).** `ClampSubParts` 의
  `mFloor`/`mCeiling` 중 하나가 `null` 인 경우다. `undefined` 만 걸러내면
  **`null * mult = 0` 이라 천장이 0 으로 찍힌다** — 카이사 E 충전시간이 `0.6 ~ 0` 으로 나가
  "0초까지 줄어든다" 로 읽혔다. 열린 쪽 끝값은 **"스탯이 0일 때의 하위 조각 합"** 이다
  (`sumAtZeroStats`). 카이사 `clamp(1.2 - 0.4 x 공격속도, 0.6, 없음)` → **`1.2 ~ 0.6`**.
  - bin 전체에 `ClampSubParts` 는 **6자리**뿐이다. 카이사 2 / 렐 2 / 우디르 1 / 칼리스타 1
  - **칼리스타 P 는 floor 마저 없어서 손대지 않는다.** 양쪽이 다 없으면 clamp 의 뜻이 안 잡힌다
  - **★ 카이사 E 충전시간은 "총" 공격 속도다** (`mStatFormula` 없음). **나무위키가 "추가"라고
    적었지만 틀렸다** — 1레벨 인게임 툴팁이 `0.94` 고, 이는 `1.2 - 0.4 x 0.644`(총 공속)와
    정확히 맞는다 (2026-08-12 인게임 확인). 같은 스킬의 이동 속도(`p1`)는 `mStatFormula: 2`
    라 **진짜 추가 공격 속도**다. **한 스킬 안에서도 계산식마다 다르니 하나로 뭉뚱그리지 말 것**
- **★ 툴팁 "구분선 아래 작은 회색 글씨" 는 본문과 다른 키에 있다 (2026-08-12).**
  bin `mSpell.mClientData.mTooltipData.mLocKeys.**keyTooltipExtendedBelowLine**` →
  stringtable(소문자). 패시브 툴팁과 같은 경로다. CD 의 `dynamicDescription` 에는
  **아예 없어서** 가렌 Q 의 "강화된 기본 공격은 4.5초 후 끝납니다." 가 통째로 빠져 있었다.
  **354자리 / 155챔피언**에 있다. `belowline.js` 가 담당하고
  `build_champion_data.js`·`fill_values.js` 가 **그 모듈 하나를 같이 쓴다** —
  둘이 같은 글자를 같은 순서로 봐야 `{pN}` 번호가 맞는다.
  문장은 `"<슬롯>_rules"` 키로 나가고 app.js 가 구분선 + 13px 회색 블록으로 그린다.
  자세한 건 `회색글씨_적용보고.md`
- **★ 회색 글씨는 본문 "뒤에" 붙여야 한다.** `convertDescription` 이 이름을 **처음 나온
  순서**로 번호 매기므로, 뒤에 붙이면 본문 번호가 하나도 안 바뀌고 새 자리만 뒤에 생긴다.
  덕분에 손으로 쓴 배열 템플릿(`keepArr`)·`KEEP_TEXT` 의 `{pN}` 이 전부 그대로 유효하다.
  실제 diff 가 **`_rules` 354줄 추가 / 본문 수정 0건**이었다
- **회색 글씨를 손질했으면 `KEEP_TEXT` 에 `"<슬롯>_rules"` 로 적을 것 (2026-08-12).**
  안 적으면 재생성 때 되돌아간다. 지금 3자리 — 에코 R·크산테 Q·유나라 W. 셋 다
  **"현재 내 챔피언의 값 / 최대값"** 형태라 앞쪽 현재값을 지웠다 (안 지우면 `?` 라 회색 글씨가
  통째로 안 나갔다). **나무위키도 셋 다 현재값을 안 적는다** — 표기 기준으로 삼을 만하다
- **`rules` 색은 `#918f86` 이다.** 인게임 실측값은 `#5a5955` 인데 **우리 배경이 인게임 툴팁보다
  어두워서 거의 안 읽혔다.** 밝기만 올렸고 색상·채도는 그대로다 (2026-08-12)
- **회색 글씨 가드는 본문 가드와 분리해야 한다.** 값이 `?` 인 8자리(드레이븐 R·파이크 R
  골드 누적 등 "현재 내 챔피언의 값")가 있는데, 본문 가드에 합치면 부연 설명 하나 때문에
  **멀쩡한 스킬이 통째로 DD 폴백**된다. 지금은 회색 글씨 줄만 버린다
- **`<charge>`·`<release>` 등 라벨 태그는 `display:block` 이라 회색 글씨 문장 중간에서
  줄을 끊는다.** `.skill-rules` 안에서만 `display:inline` 으로 되돌려 뒀다
  (이렐리아 W "…`<charge>`충전`</charge>`이 끝나면…" 이 세 줄로 갈라졌다)
- **★ 역할군은 Data Dragon `champion.json` 의 `tags` 다 (2026-08-12).** 챔피언 페이지가
  이미 받는 파일이라 **추가 요청이 0**이다. 6종 — Fighter 60 / Mage 75 / Assassin 47 /
  Marksman 33 / Tank 46 / Support 43. **173명 전부 1~2개다 (0개도 3개도 없다).**
  CD `champion-summary.json` 의 `roles` 와 173명 전부 일치하므로 DD 만 봐도 된다.
  - 공식 아이콘: `rcp-fe-lol-champion-details/global/default/role-icon-<역할>.png` (금색 문양)
  - **필터는 AND(켠 역할을 전부 가진 챔피언만) 다.** 처음엔 OR 로 만들었는데 2026-08-12에
    AND 로 바꿨다. 전사+탱커 = 26명처럼 교집합이 쓸모 있게 나온다.
    **역할이 최대 2개라 3개 이상 켜면 결과는 항상 0명이다 — 버그가 아니다**
  - `info` 도 같이 들어 있다 (attack/defense/magic/difficulty, 0~10). 아직 안 쓴다
- **초성 검색 헬퍼 `getChosung()` 은 app.js 에 이미 있었다** (챔피언 필터 패널 절).
  새로 만들지 말 것 — 2026-08-12에 모르고 한 벌 더 만들어 `HANGUL_CHO` 중복 선언으로 깨졌다
- **★★ 초성만 이어 치면 IME 가 겹받침으로 묶는다 — 35명이 안 찾아지고 있었다 (2026-08-14).**
  "르블랑" 을 찾으려고 ㄹ·ㅂ·ㄹ 을 누르면 화면엔 **`ㄼㄹ`** 이 뜬다 (ㄹ+ㅂ = ㄼ).
  우리가 비교하는 초성 문자열은 `ㄹㅂㄹ` 이라 **한 글자도 안 맞아 검색이 통째로 헛돌았다.**
  - 173명 중 **35명**이 이 상태였다 — 암베사 `ㅇㅄ` · 초가스 `ㅊㄳ` · 문도 박사 `ㅁㄷㅄ` ·
    리 신/럭스 `ㄽ` · 카르마 `ㅋㄻ` · 올라프 `ㅇㄿ` · 누누와 윌럼프 `ㄴㄴㅇㅇㄿ` 등
  - `splitJong()` 이 겹받침 11개(`ㄳㄵㄶㄺㄻㄼㄽㄾㄿㅀㅄ`)를 초성 두 개로 편다.
    **초성 자리에는 겹받침이 올 수 없으므로 푸는 게 항상 옳다.** 원래 후보도 같이 남긴다
  - **★ 쌍자음(ㄲㄸㅃㅆㅉ)은 건드리면 안 된다.** Shift 조합이라 IME 가 자동으로 안 묶는다.
    풀어 버리면 **쓰레쉬(ㅆㄹㅅ)가 `ㅅㅅㄹㅅ` 이 되어 못 찾는다**
  - 챔피언 검색은 `koCandidates()` 한 곳만 고치면 다 걸린다(목록 필터·vs 상자).
    **랭킹 검색은 `koCandidates` 를 영문일 때만 타므로** `buildRankQuery` 에서 따로 챙겼다
- **★ 챔피언 목록 항목을 숨길 땐 `style.display` 를 건드리지 말 것 (2026-08-12).**
  항목은 인라인 `style` 에 `display: flex` 를 들고 있다. 되돌리려고 `''` 를 넣으면
  **그 flex 까지 같이 지워져서** 아이콘과 이름이 세로로 쌓이며 상자가 통째로 깨진다.
  `.filtered-out` 클래스로 끄고 CSS 에서 `display:none !important` 로 인라인을 이긴다
- **★ 스탯 탭 (2026-08-12).** `public/champion_stats.js` = `build_stats_page.js` 가
  `champion_stats_by_level.json` 을 **압축한 것** (347KB → 68KB). 18칸을 그대로 안 내려주는 건
  모든 스탯이 `base + perLevel x g(N)` 한 식으로 복원되기 때문 —
  **1376자리 전부 오차 0.0015 미만**인 걸 확인하고 줄였다. 공격 속도만 곱셈형(`x`)이다.
  `app.js` 의 `statAtLevel()` 이 화면에서 계산한다.
  - **화면은 왼쪽 표(스탯 / 1레벨 / 18레벨) + 오른쪽 그래프**다. 표 폭은 `width:auto` 로
    내용에 맞춘다(232px). 스탯 이름을 누르면 오른쪽 칸에 그 스탯의 곡선이 나온다.
    막대+슬라이더 → 표+아코디언 → 표+오른쪽 그래프 순으로 바뀌었다 (전부 2026-08-12)
  - **자원을 안 쓰는 챔피언도 마나·마나 재생 줄을 만들고 값은 `-` 로 둔다.**
    챔피언마다 줄 수가 달라지면 옆 그래프 칸 높이가 같이 흔들린다
  - **이동 속도·사거리도 일반 줄이다.** `build_stats_page.js` 가 `_고정` 을 스탯 줄로 바꿔 넣는다
  - **★ 트리스타나는 사거리가 550 → 700 으로 큰다.** 기본 스탯이 아니라 **패시브**(성장하는 화력)
    라서 bin 의 `attackRange` 엔 없다. `RANGE_GROWTH` 표에 손으로 적었고 근거는
    `level_curves.json` 의 `tristana.P.BonusPassiveRange`(0→150).
    **이 성장만 g(N) 곡선이 아니라 레벨당 일정(직선)이라 방식 `l` 로 따로 둔다.**
    **나르(`gnar.P.TotalAttackRange` 0→100)는 넣으면 안 된다** — 기본 사거리 175 가
    메가 나르(근접) 값이라 100 을 더해도 미니 나르 사거리가 안 나온다
  - **★ 그래프는 SVG 로 선만 그리고 점·라벨·눈금은 HTML 절대배치다.**
    전부 SVG 로 그리고 `preserveAspectRatio="none"` 으로 폭을 늘렸더니
    **글자와 점이 가로로 3배 찌그러졌다** (viewBox 320 → 실제 1100px).
    선은 늘어나도 되지만 글자·원은 안 된다. 굵기는 `vector-effect="non-scaling-stroke"` 로 지킨다
  - **★ y 축 = 전 챔피언이 갖는 값의 범위 (`statGlobalRange`, 2026-08-12).**
    **천장 = 18레벨 기준 1위 / 바닥 = 1레벨 기준 꼴찌.** 내 챔피언 값으로 축을 잡으면
    어느 챔피언을 봐도 선이 화면을 꽉 채워 **높은 건지 낮은 건지가 안 보인다.**
    전체 범위를 축으로 두면 선의 높이가 곧 순위 감각이다 (가렌 체력 12% → 83.4%).
    제목줄에 `세로축 410(클레드) ~ 2742(갈리오)` 를 적는다.
    - **바닥이 0 이던 걸 1레벨 최솟값으로 올렸다.** 어느 챔피언도 그 아래로 안 가서
      0~최솟값 구간이 항상 비어 있었다. ("y 축은 0 부터" 규칙은 **다른 챔피언과 비교가
      안 되는 축**일 때 이야기다. 지금은 축이 전 챔피언 공통 틀이라 과장이 안 생긴다)
    - **역할군 평균·vs 챔피언도 이 범위를 절대 못 벗어난다** (평균은 최소~최대 사이).
      축이 누구를 켜든 안 흔들리는 이유다
    - 실측 범위: 체력 410(클레드)~2742(갈리오) / 마나 232(베인)~1879(카사딘) /
      공격력 44(오리아나)~150(일라오이) / 사거리 125(아칼리)~700(트리스타나) /
      이동 속도 315(렐)~355(마스터 이)
  - **★ 그래프 칸은 스탯을 안 골랐어도 자리를 지킨다** (`display:block`, 빈 채로).
    `display:none` 이었을 땐 스탯을 누를 때마다 칸이 생겼다 없어지며 **오른쪽 vs 상자가
    좌우로 튀었다** (2026-08-12). 폰은 위아래로 쌓이므로 그대로 `none` 이다
  - **그래프 높이는 왼쪽 표에 맞춘다** (표 405px / 칸 405px, `.g-plot` 277px).
    **칸 높이가 표와 어긋나면 vs 상자 높이가 스탯을 열고 닫을 때 흔들린다** (418 vs 405 였다)
    가로는 460px 그대로다 — **비율을 맞출 이유가 없다.** 천장이 1위 값이라 대부분의 챔피언은
    선이 아래쪽에 깔리는데, 납작하면 그 아래쪽에서 아무것도 안 보인다. 폰은 220px
  - 값 라벨은 양 끝만 찍고 중간은 hover 로 본다. **커서가 그래프 위에 있으면 고정 끝 라벨을
    숨긴다** — 안 그러면 18레벨 점에서 `Lv.18 · 2356` 과 `2356` 이 겹쳐 찍힌다
  - **★ 6·11·16 세로 눈금은 성장이 꺾이는 자리가 **아니다** (2026-08-12 질문받음).**
    스탯은 g(N) 하나로 1~18레벨이 매끄럽게 이어지고 중간에 분기점이 없다.
    5레벨 간격 눈금일 뿐이고, **궁 찍는 레벨(6·11·16)과 우연히 같다**
  - **그래프 폭에 상한(480px)이 있다.** `flex:1` 만 두면 넓은 화면에서 870px 까지 벌어지는데
    18개 점을 그렇게 늘리면 가로만 길고 변화는 안 보인다 (2026-08-12).
    **★ 상한은 칸(`.stat-graph-panel`)이 아니라 그래프(`.stat-graph-inner`)에 걸 것** —
    칸까지 묶으면 제목·범례가 같이 좁아져 두 줄로 접힌다
  - **안 크는 스탯(이동 속도·사거리)도 누를 수 있다.** 수평선이어도 그게 정보고,
    역할군 평균과 겹치면 "안 크는데 남들보다 높다/낮다" 가 보인다. 못 누르는 건
    자원 없는 챔피언의 `-` 줄뿐이다 (2026-08-12)
  - **★ 역할군 평균 비교 (2026-08-12).** 그래프 위 역할군 버튼 6개를 켜면 그 역할군
    챔피언들의 **레벨별 평균**이 점선으로 겹친다. 모집단은 `champRoleMap`(DD `tags`,
    `showChampions()` 가 채운다) ∩ `championStats` 라 **추가 요청이 0**이다.
    - **자원은 같은 키를 가진 챔피언만 센다.** 마나 평균에 기력을 섞으면 뜻이 없고,
      자원 없는 챔피언을 0 으로 넣으면 평균이 통째로 내려앉는다. 표본이 0 이면 버튼을
      비활성으로 둔다 (`기력` 은 원거리 딜러·서포터가 0명이다)
    - `champRoleMap` 은 **정규 챔피언만** 담는다. 클래식 탭으로 들어와도 평균의 모집단이
      같아야 값이 안 흔들린다 (클래식은 `championStats` 에도 없다)
    - 평균선 색 `ROLE_LINE` 은 **Okabe-Ito 팔레트**다. 여기는 6색을 나란히 놓아
      색이 곧 구분이라 색맹 대비가 필요하다 — 스탯 색(`STAT_COLOR`, 단일 시리즈라 이름표
      역할)과 목적이 다르니 합치지 말 것
    - 평균선은 **먼저** 그려 챔피언 선 아래에 깔고, 점선 + 1.5px 로 얇게 둔다
    - **★ 그래프 위쪽에는 높이가 고정된 것만 둘 것.** 켠 개수에 따라 줄 수가 늘었다
      줄었다 하면 그래프가 위아래로 흔들린다. 지금 구조는 **제목 줄 / 역할군 버튼 줄 /
      범례 자리(34px 여백)** 3단이고 셋 다 높이가 고정이다. 범례는 그 여백 바닥에서
      **위로** 쌓인다(`column-reverse` + `position:absolute`, `top` 은 app.js 가 잰다)
    - **★ 범례가 패널 위쪽으로 넘어가면 잘린다.** 조상 `.champ-tab-scroll` 이
      `overflow:auto` 라서다. "제목 줄 위는 탭 줄 오른쪽이라 비어 있으니 거기로 자라면
      된다" 가 **틀렸다** — 역할군 5개를 켜니 위 두 줄이 통째로 사라졌다.
      그래서 제목 블록에 34px 여백을 만들어 시작점을 내렸고, 6역할군 + vs = **7줄이
      전부 패널 안에 들어간다**. 그래프는 320 → 290px 로 줄었다(칸 높이는 표와 같은 405 유지)
    - **범례 줄이 길면 역할군 버튼 오른쪽과 맞물린다.** 두 가지로 줄였다 —
      표본 수(`N명`)를 버튼 말풍선으로 옮기고, 범례에서만 `원거리 딜러` → `원딜`(`ROLE_SHORT`).
      제목도 `역할군 평균과 비교` → `역할군 평균`. 폰에서는 자리가 없어 범례가 그래프 아래로 간다
      (DOM 상 그래프 뒤에 두고 데스크톱에서만 끌어올리는 이유다)
    - **★ 범례 상자에 `pointer-events: none` 이 반드시 있어야 한다.** 글자는 오른쪽에만
      몰려 있어도 상자는 **가로 460px 을 통째로 덮는다.** 4줄째부터 역할군 버튼 줄
      높이까지 올라와서, 없으면 **버튼이 클릭을 못 받는다** —
      "역할군 4개를 켜면 더 못 누르고 취소도 안 되던" 버그가 이것이었다 (2026-08-12)
    - **★ y 천장에 평균선을 반영하면 안 된다.** 버튼을 켤 때마다 y 축이 다시 스케일되어
      **챔피언 곡선의 개형이 바뀐다** (같은 챔피언인데 납작해졌다 살아난다).
      지금은 천장이 "1위 챔피언 값" 이라 애초에 평균이 못 넘는다.
      천장 밖으로 나가는 평균선을 clip 으로 자르고 범례에 `그래프 밖` 이라고 적는 길은
      **남겨 뒀다** — 천장 규칙을 다시 바꾸면 그때 살아난다
    - **clip 은 평균선에만 걸 것.** SVG 통째로 `overflow:hidden` 을 주면 `x=0`/`x=100` 에서
      챔피언 선의 굵기가 절반 잘려 끝이 얇아 보인다
    - 켠 역할군은 **스탯을 바꿔도 유지된다** (`avgRoles`). 챔피언 목록 필터의
      `activeChampRoles` 와는 **별개 집합**이다 — 합치면 목록이 같이 걸러진다
  - **★ vs 챔피언 (2026-08-12).** 그래프 오른쪽 끝에 챔피언 검색 상자를 두고, 고르면
    그 챔피언 곡선을 **실선**(보라 `VS_LINE`)으로 겹친다. 평균선은 점선이라 구분된다.
    - 목록은 `championStats` 만으로 만든다 — **키가 곧 DD id** 라 아이콘 주소가 나온다.
      초성 검색은 `getChosung()` 을 그대로 쓴다 (표를 두 벌 두면 어긋난다)
    - **역할군 아이콘 줄은 일부러 뺐다.** 바로 위 "역할군 평균" 아이콘과 생김새가 같아서
      한 화면에 두 줄이 나란히 있으면 뜻이 갈리지 않는다
    - **★ 목록은 `position:absolute` 로 띄워야 한다.** 보통 흐름에 두면 172명 높이(6249px)가
      그대로 칸 높이가 되고 **`align-self: stretch` 로는 못 막는다** — 플렉스 줄 높이를
      정하는 게 그 항목 자신이라 자기를 기준으로 늘어난다. 싸개(`.vs-list-wrap`)만 `flex:1`
      로 두고 목록을 그 안에 절대배치하면 줄 높이는 왼쪽 표(405px)가 정한다
    - **그래프가 떠 있을 때만 활성화된다** (`syncVsBox`). 스탯을 안 골랐으면 겹칠 대상이
      없어 눌러도 아무 일이 안 일어난다 — 흐리게 + `disabled` 로 꺼진 걸 보여 준다.
      그래프를 닫으면 고른 챔피언도 같이 푼다
    - 챔피언을 바꾸면 `vsChampId` 를 푼다(자기 자신과 비교 방지). 목록에서 자기 자신은 뺀다
    - 비교 챔피언이 그 스탯을 아예 안 가지면(가렌의 마나) 선을 안 그리고 범례에
      `— 이 스탯 없음` 이라고 적는다. 안 그러면 목록은 켜졌는데 선이 없어 고장으로 보인다
  - 가렌 8개 값이 나무위키 능력치 표와 전부 일치한다. **체력 재생만 단위가 다르다** —
    나무위키·DD 는 5초당, 우리는 초당이라 5배 차이다 (`8` vs `1.6`). 화면에도 `/초` 라고 적는다
  - **★ 자원 종류가 `없음` 인 챔피언은 자원 칸을 통째로 빼야 한다.** 대부분 0 인데
    **비에고만 bin 에 10000 이 박혀 있어서**, 안 빼면 그게 전체 자원 최댓값이 되어
    **다른 챔피언 마나 막대가 전부 뭉개진다** (라이즈 마나 1490 이 13% 로 보였다)
- **`<li>` 에는 CSS 가 없어서 브라우저 기본 불릿(동그라미)이 찍히고 있었다 (2026-08-12).**
  `li { display:block }` + `li::before { content:'- ' }` 로 줄표로 바꿨다.
  DD 폴백 경로(`cleanTooltipText`)는 원래부터 `<li>` 를 `- ` 로 바꾸고 있어서 이제 양쪽이 같다
- **아펠리오스 무기 이름 뒤 `(소총)` `(대포)` 같은 분류는 안 적는다 (2026-08-12).**
  P 5건 + R 1건을 지웠다. R 은 중력포에만 붙어 있었는데 2026-08-09에 손으로 쓰면서
  하나만 빠뜨린 것이었다. **무기 이름은 색으로 이미 구분된다**
- **★ `values.imgBody` = 배열 템플릿 **0번 파트 전용** 아이콘 (2026-08-12).**
  스킬 칸(버튼·이름)은 `values.img` 를 쓰는데 0번 파트도 같은 걸 본다. 둘을 다르게 하고 싶으면
  `fill_values.js` 의 `BODY_ICON` 표에 적는다. 유나라 Q 가 유일한 사례 —
  버튼은 컬러(`yunara_q2`, 활성 상태), "기본 지속 효과" 옆은 흑백(`yunara_q`, 비활성).
  파트 구성은 기본 지속 효과(흑백) / 사용 시(컬러) / 초월 상태(궁 아이콘) 3단이다
- **유나라 W·E 의 초월 아이콘은 `EXTRA_ICONS` 로 넣어야 한다.** `Yunara_RW.dds`/`Yunara_RE.dds`
  가 실재하는데 **스펠 객체가 아니라 버프 쪽에 붙어 있어서** 자동 탐색(스펠 객체만 훑는다)에
  안 걸린다. Q 의 초월 파트는 전용 그림이 아예 없어서 궁 아이콘(`yunara_r`)을 쓴다
- **카이사 Q·W·E 진화 파트의 모양을 통일했다 (2026-08-12).** 진화 아이콘이 붙는 파트에
  `진화 시: <효과><br><rules>진화 조건: <색>스탯</색></rules>` 순서로 넣는다.
  전에는 Q 만 그랬고 W·E 는 "진화 시" 효과가 **본체 파트**에 있고 진화 파트엔 조건만 있었다.
  조건의 스탯 색은 패시브의 진화 조건 목록과 맞춘다 (`physicaldamage`/`scaleap`/`attackspeed`).
  **"진화 조건" 파트를 쓰는 챔피언은 카이사뿐이다** — 카직스 R·스몰더 Q 는 자기들끼리 일관됨
- **스킬 시연 영상은 슬롯당 하나뿐이다 (2026-08-12 CDN 4,325건 전수 확인).**
  두 개가 있는 건 폼이 둘인 5명뿐 — 나르(R 포함)·엘리스·제이스·니달리·렉사이.
  **흐웨이 Q·카르마 R·카직스 R·아펠리오스처럼 하위 스킬이 여러 개여도 영상은 하나다.**
  더 찾지 말 것. `자헨 Q 만 파일명이 `Q2`(`Q1` 없음)라 `app.js` 의 `VIDEO_ID_FIX` 로 돌린다.
  로크(805)·신 짜오(5)는 영상이 아예 없다. 자세한 건 `시연영상_조사.md`
- `mStat` 필드 생략 = 주문력 (오공 E로 검증)
- `mStat` 값: 1 = 방어력, 2 = 공격력, 4 = 공격 속도, **6 = 마법 저항력**, **8 = 치명타 확률**, **9 = 치명타 피해량**, 12 = 최대 체력, 18 = 생명력 흡수, 29 = 물리 관통력
- **9는 이동 속도가 아니다.** 야스오 패시브 위키("critical strikes deal only (180% + 27%) AD")와 탈론 Q 인게임 설명으로 확정. 케이틀린 P·루시안 R·미스 포츈 Q·샤코 Q의 `치명타 확률 x (X - 1)` 형태는 X가 치명타 피해량일 때만 성립함 (2026-08-08 정정)
- **6과 8은 원래 반대로 적혀 있었고 2026-08-08에 바로잡았다.** 닐라 R이 실제로 치명타 확률, 뽀삐 W가 마법 저항력이었음. 정확히 맞교환이라 21자리가 틀린 이름으로 나가고 있었다. bin 필드 이름(`CritArmorPen`, `BonusMR`)과 루시안 R 발사 횟수(치명타 비례)로도 교차 확인됨
- **`STAT_NAMES`에 이름이 적혀 있으면 스크립트가 "안다"고 판단해서 의심 목록에 안 올린다.** 그래서 표에 잘못 적힌 값은 미리보기 출력에 한 줄도 안 뜬다. 표에 있는 값도 근거가 없으면 추정으로 취급할 것
- `mStatFormula`가 총/기본/추가를 가름: 없음·0 = 총, 1 = 기본, 2 = 추가
- `DataValues` 배열은 0번이 쓰레기라 **1번부터** 읽어야 함
- **★★ 최상위 필드는 필드마다 시작 인덱스가 다르다. 한 규칙으로 뭉뚱그리지 말 것 (2026-08-14 정정).**
  예전엔 "`castRange`·`mana` 등 최상위 필드는 0번부터" 라고 한 줄로 적어 뒀는데 **사거리가 틀렸다.**
  랭크별로 값이 변하는 자리를 DD `rangeBurn`·`costBurn` 과 전수 대조한 결과:

  | 필드 | 0번부터 맞음 | 1번부터 맞음 | 결론 |
  |---|---|---|---|
  | `mana` | **267 / 271** | 9 / 271 | **0번부터** |
  | `castRange` · `castRangeDisplayOverride` | 3 / 19 | **19 / 19 (100%)** | **1번부터** |

  - 0번은 "스킬 안 찍은 상태" 라 쓰레기가 들어 있다. **레넥톤 Q 가
    `[20, 325, 325, 325, 325, 325, 20]`** 이라 1랭크 사거리가 화면에 **20** 으로 나갔다
  - **대부분 스킬은 사거리가 랭크 불변(전부 같은 값)이라 여태 안 드러났다.**
    랭크별로 변하는 19자리에서만 표가 갈린다 — 갈리오 R 이 `4000/4000/4750`
    (실제 `4000/4750/5500`) 처럼 **한 칸씩 밀려** 있었다. 17자리를 고쳤다
  - 배열이 **7칸인 이유**가 이걸로 설명된다: 0번(미습득) + 랭크 1~6.
    우디르·제이스가 6랭크라 6칸이 필요하다
  - **`cooldownTime` 도 "한 칸 밀린 듯하다" 고 적어 두고 CD v1 로 우회했는데**
    (아래 `cooldownTime` 항목) 같은 현상이었을 가능성이 높다. 지금은 CD v1 이 DD 와
    100% 일치하므로 굳이 안 건드린다
  - **★ `castRangeDisplayOverride` 가 `-1` 이면 "덮어쓸 값 없음" 표식이다.**
    그대로 쓰면 화면에 `사거리 -1` 이 나간다 (잔나 W 가 그랬다). DD 는 이걸 부호 없는
    정수로 읽어 **`4294967295`** 를 내보내므로 DD 쪽이 더 나쁘다. `castRange` 로 폴백한다
- **★★ `castRange` 의 `20000` 이상은 "표시할 사거리 없음" 관용 더미다 — 화면에서 뺀다
  (2026-08-14, `fill_values.js` 의 `RANGE_GLOBAL`).**
  **소환사의 협곡이 약 15000 유닛**이라 그보다 큰 값은 물리적으로 맵 전체다.
  **37자리**가 여기 걸린다 — 25000 이 34개 + 갱플랭크 R 30000 · 쉔 R 35000 · 헤카림 R 50000.
  - **★ "25000 = 전역기" 가 아니다.** 성격이 셋으로 갈리는데 **어느 쪽이든 숫자를 찍으면 안 된다**:
    ① 진짜 전역기(애쉬 R, 카직스 R) ② **자기 중심 스킬**(아트록스 Q, 요네 E — 롤위키도
    `RANGE: Varied` 라고만 적는다) ③ **사거리가 있는데 딴 데 있는 것**(유미 W, 아트록스 E)
  - **★★ ③을 되찾으려면 손으로 표를 만드는 수밖에 없다. 자동 추출이 안 된다** —
    유미 W 는 계열 객체 `YuumiWCast.castRange` 의 **700**(롤위키와 일치)인데,
    아트록스 E 는 DataValue **`EMaxRange`** 의 300 이다. **이름 규칙이 없다.**
  - **★ 계열 객체를 자동으로 쓰면 오히려 망가진다.** 럭스 R 은 `override 3340` 이 정답인데
    계열에 이펙트 미사일 `LuxRVfxMis=1175` 가 있고, **드레이븐 R(진짜 전역)은 계열이
    `DravenRDoublecast=450`** 이다. 유미 Q 는 계열에 1250·1150 이 있는데 **롤위키는 850** 이라
    셋 다 안 맞는다. **투사체 속도에서 겪은 것과 같은 함정이다** (아래 항목)
  - **★★ 반대쪽 끝에도 더미가 있다 — `20` 이하는 "사거리 없음" 이다 (24자리, 2026-08-14).**
    `1` 8자리(다리우스 Q·마스터 이 R·베인 R·알리스타 R·카이사 E…) + `20` 16자리
    (마스터 이 W 명상 · 럼블 W 고철 방패 · 시비르 E 주문 방어막 · 트위치 Q 매복 …).
    **전부 자기 대상 스킬**이고 **DD `rangeBurn` 도 똑같이 1/20 이라 기계 대조로는 안 걸린다.**
    40(초가스 E) 위로는 진짜 사거리다 — 레오나 Q 100, 신지드 E 125, 케넨 E 170, 렐 R 200
  - **★ override 가 무효일 때 폴백해도 되는 경우와 아닌 경우가 갈린다.**
    음수(`-1`) = "덮어쓸 값 없음" → `castRange` 로 폴백(잔나 W). **`20` 이하 = "자기 자신"
    → 폴백하면 안 된다** — 라이엇이 "사거리 없음" 이라고 **정해 준 것**이다.
    다리우스 Q 가 `override 1 / castRange 270` 인데 폴백하면 `사거리 270` 이 나간다.
    롤위키는 이 스킬에 `EFFECT RADIUS 460` 만 적고 사거리를 안 적는다 — 270 은 아무 값도 아니다
  - **★ 3000~12000 은 건드리지 않는다 (25자리).** 진짜 값이 대부분이다 —
    갈리오 R `4000/4750/5500`, 스웨인 W `5500~7500`, 클레드 R `3500/4000/4500`,
    케이틀린 R `3500`, 제라스 R·직스 R `5000`. **랭크별로 변하면 거의 진짜 값이다**
  - **★ `castRangeDisplayOverride` 가 있으면 그게 진짜 표시값이다** — 럭스 R 이
    `override 3340 / castRange 25000` 이라 3340 으로 나간다. 다만 **override 가 없어도
    castRange 가 진짜인 경우가 있다** (갈리오 R). override 유무로는 못 가른다
- `AmmoRechargeTime` 계열도 0번이 "스킬 안 찍은 상태"라 1번부터. **전수 검증 끝** (티모 R, 케이틀린 W, 바이 E, 벨코즈 W, 자이라 W, 카시오페아 E, 아지르 W 전부 통과)
- **`DOT_AS_PRECISION = true`(`.숫자`를 소수점 자릿수로 보는 해석)가 맞다고 확정.** 럼블 W·크산테 Q·크산테 W·하이머딩거 E로 확인. 배열 인덱스 해석은 폐기 — 다시 뒤집지 말 것
- **롤위키(`wiki.leagueoflegends.com/en-us/<영문명>`)를 WebFetch로 읽을 수 있다.** 수치 검산에
  쓸 것. **단 `(based on level)` 수치는 위키 기본 표시가 20레벨이다** — 야스오 방어막
  `674.46`, 우디르 각성 공속 `75.88%`가 그 예. 우리는 18레벨 기준이라 역산해야 맞다.
  랭크별 값(`5 / 25 / 45`)은 그대로 비교 가능. **"아이콘 몇 개냐"처럼 물으면 답이 들쭉날쭉하고,
  "하위 스킬 이름을 슬롯별로 나열"처럼 게임 규칙을 물으면 일관되게 답한다**
  - **★ 페이지에 없는 값을 물으면 요약 모델이 "직접 계산해서" 답한다 (2026-08-12).**
    가렌 챔피언 페이지에 "레벨 9 체력?" 을 물었더니 표기 `690+98` 만 보고 선형으로
    곱해 **`1474`** 라고 답했다. 실제(비선형)는 **`1350.52`** 다. 위키 챔피언 페이지는
    레벨별 값을 **슬라이더로만** 보여주므로 본문에 그 숫자가 없다.
    **레벨별 중간값이나 성장 공식은 챔피언 페이지가 아니라
    `wiki.leagueoflegends.com/en-us/Champion_statistic` 문서에서 확인할 것**
  - **★ 그 문서가 우리 성장 공식과 글자 그대로 같다** —
    `base + g x (n-1) x (0.7025 + 0.0175 x (n-1))`, "level 18 = 1700%, or 17 times".
    **중간 레벨 각주 값이 임의가 아니라는 근거가 여기다**
- **위키 20레벨 값 → 18레벨 역산은 `(끝값 - 시작값) / 19`가 레벨당 증가분이다.** 18로
  나누면 틀린다. 2026-08-09에 세 번 걸렸고 세 번 다 이 식으로 맞았다 — 자헨 P
  `2.95%`(20렙) → `2.797` ≒ 우리 `2.8`, 타릭 P `25~101` → `25 + 4x17 = 93`,
  우디르 Q 번개 `3.18%` → `3.003%` ≒ `3%`
- **곱셈 조각의 배율은 "맨 숫자이면서 1 미만"인 피연산자에만 싣는다.** 일괄 적용도 일괄
  미적용도 틀리다 — 카타리나 R은 `0.16 → 16`이 맞고(위키 "16% bonus AD"), 다리우스 Q는
  `100~140`을 그대로 둬야 한다(위키 "+100~140% AD"). 1 미만이면 아직 분수, 1 이상이면
  이미 퍼센트 단위다. 2026-08-09 위키 3자리 대조로 확정
- **`(중첩당)` 자리도 같은 함정이다. 일괄로 `x100` 하지 말 것.** `BuffCounterByNamedDataValue`
  조각이 내는 값인데 32자리를 훑어보니 세 종류가 섞여 있다 — 분수(스몰더 `0.25` = 중첩의
  25%), 절대값(드레이븐 P 골드 `1`, 스웨인 P 체력 `15`), 이미 % 단위(`2.5% (+ 0.5 (중첩당))`).
  위키로 확인한 자리만 손댈 것
- **범용 이름(`f1`·`Effect2Amount` 등)의 "본체에서만 찾기" 제한을 풀지 말 것.**
  2026-08-09에 "계열 등급까지" 로 완화하는 걸 시험했다. **2자리가 새로 채워지고 기존
  값은 하나도 안 바뀌어서 완벽히 안전해 보였는데, 그 2자리 중 신 짜오 E 가 틀렸다**
  (`30/35/40/45/50` 을 물어왔지만 실제는 `38/46/54/62/70%`). 계열 등급도 못 믿는다.
  **"기존 값이 안 바뀌니 안전하다" 는 판단 자체가 함정이다** — 새로 채워지는 자리가
  맞는지는 별개 문제다. 규칙을 건드리면 새로 채워지는 자리를 전부 검산할 것
- **레벨 보간 조각은 양 끝이 같으면 범위 표기를 붙이면 안 된다.** `6 ~ 6% (레벨에 따라)`처럼
  나가면 안 크는 값이 크는 것처럼 읽힌다. `fill_values.js`의 `levelRange()` 헬퍼가 처리하고
  보간 조각 5군데가 전부 여기를 거친다. **새 보간 분기를 추가하면 이 헬퍼를 쓸 것**
  (`mFloor ~ mCeiling`은 clamp라 대상이 아니다)
- **CD가 못 푼 `{8자리16진수}` 이름은 원래 이름의 FNV-1a 32비트 해시(소문자 기준)다.**
  툴팁 이름을 해싱해서 찾아가면 됨. `fnv1a('firsttierrangeincreasett') = d34fc902`이고
  킨드레드 패시브 계산식 키가 정확히 `{d34fc902}`. `findCalc`·`findDataValue`에 폴백으로 들어감
- **해시 역추적으로 조각 안 필드 이름 6개를 되찾았다.** 전부 `ByCharLevelBreakpoints`의
  **"값 대신 DataValue 이름을 적는" 변종**이야:
  `{91d404a5}`=`Level1DataValue`, `{bbd778a2}`=`InitialBonusPerLevelDataValue`,
  `{9823b29a}`=`DataValueBreakpoints`, `{ae9b464d}`=`AdditionalBonusAtThisLevelDataValue`,
  `{b0d8b2ac}`=`BonusPerLevelAtAndAfterDataValue`, `{b2cd0eb0}`=`BonusPerLevelDataValue`.
  **여기도 "생략 = 0(성장 정지)" 규칙이 그대로 적용된다.** `fld()`가 해시·원래이름 둘 다 봄
- **문자열 필드 두 개짜리 조각을 무조건 "레벨1 ~ 레벨18"로 읽으면 안 된다.** 두 번째가
  `BonusPerLevelDataValue`(레벨당 증가분)인 변종이 있어서 이렐리아 P가 `10 ~ 3`이라는
  거꾸로 된 범위로 나가고 있었다 (실제 `10 + 3x17 = 61`). 이름을 보고 갈라야 함
- **`mStat 7 = 이동 속도`. 인게임 확인 완료(2026-08-08).** 헤카림 P 툴팁이 실제로
  "추가 이동 속도의 12~24%(레벨에 따라)". 표에 7이 아예 없어서 잔나 P·헤카림 P가
  통째로 죽고 있었다
- **최상위 필드가 `{ values: [...] }`로 한 겹 싸여 있는 경우가 있다.** 직스 P의 `Cooldown`이
  그 모양이라 객체인 채로 실패하고 있었음. `fieldToText`에서 벗겨냄.
  **직스 패시브 쿨 12초 고정은 인게임 확인 완료(2026-08-08)** — 배열이 전부 12인 게 맞다
- **`ByCharLevelFormulaCalculationPart`는 필드가 `mValues`가 아니라 `values`다.** 15자리 전수
  확인. 이걸 몰라서 이 분기는 오랫동안 `0 ~ 0`만 냈다 (2026-08-08 수정)
- **★ 그 배열은 31칸 = 레벨 0~30 이라 "인덱스 = 레벨"이다. 레벨1 = `vals[1]`, 레벨18 = `vals[18]`.**
  2026-08-08에 "31칸 = 레벨 1~31" 로 읽고 `vals[0]`/`vals[17]`을 쓴 게 **2026-08-11에 위키
  대조로 뒤집혔다.** 노틸러스 P 캐시가 `idx0=8 idx1=14 idx17=110 idx18=116 idx30=188`이고
  양 위키가 말하는 값이 `14 ~ 116`이다. **110은 17레벨 값이었다.**
  이 분기가 만드는 **11자리가 전부 한 칸씩 낮게** 나가고 있었다 —
  노틸러스P 럭스P 레오나P 브라움P(2) 카직스P 그레이브즈P 카타리나P 렝가W 세트P(2) 요네W.
  **그레이브즈·카타리나·렝가의 `0 ~ ` 시작값도 별개 버그가 아니라 전부 이것이었다**
  (레벨 0 값이 0이라서). 위키로 확인 가능한 8자리가 수정 후 값과 정확히 일치했다
- **★ `partToText`의 Stat 조각 두 개(`StatByNamedDataValue`·`StatByCoefficient`)는 `mult`를
  받아야 한다.** 2026-08-11 전까지 `100`만 곱하고 `mult`를 통째로 버려서, 기본값엔 배율이
  걸리고 계수엔 안 걸렸다. 증상 두 가지가 같은 뿌리였다:
  1. `mMultiplier` 있는 계산식 — 아칼리 E(x0.3), 룰루 Q(x0.5), 릴리아 W(x3), 마스터 이 Q(x0.25)
  2. `mDisplayAsPercent: true` 인 체력 % 피해 — `주문력의 0.03%`처럼 **100배 작게** 나감
     (렐 E, 판테온 W, 트런들 R, 카이사 P, 세트 Q·W, 우디르, 크산테 W 등 20여 자리)
  **단 `FRACTION_STATS = {4 공속, 8 치명타 확률, 9 치명타 피해량, 18 생흡}`은 빼야 한다.**
  이 넷은 값 자체가 소수(0.5 = 50%)라 배율을 또 먹으면 100배로 튄다
  (진 P `치명타 확률의 35%` → `3500%`). 그래서 바루스 P `추가 공격 속도의 1100%`,
  비에고 P `500%`는 **오류가 아니다** — 위키의 `11% / 5% per 100% bonus AS`와 같은 값이다
- **`mModifiedGameCalculation`의 배율은 안쪽 계산식에 실어 내려보낼 것.** `${inner} x ${m}`으로
  붙이면 화면에 계산식이 그대로 찍히고("30 / 50 / 70 (+ 주문력의 10%) x 3") 배율이 기본값에만
  걸린 것처럼 읽힌다. 내려보내면 그웬 R이 `90 / 150 / 210 (+ 주문력의 30%)`로 lolwiki와 맞는다.
  **랭크별 배열처럼 숫자 하나로 못 접는 배율은 예전처럼 `x`로 남긴다**(모데카이저 Q).
  이걸로 곱셈 표시가 145자리 → 29자리가 됐다 (2026-08-11).
  이어서 `foldProduct`(순수 숫자끼리 곱해서 접기, `x 1` 제거)와
  `foldRankMultiplier`(랭크별 배율을 랭크 i 는 배율 k_i 로 돌린 결과의 **i번째 칸**만 뽑는 대각선
  방식으로 접기)를 넣어 **최종 9자리**까지 줄였다. 남은 9자리는 치명타 피해량이 변수로 들어가거나
  배율 자체가 스탯이라 숫자로 접을 수 없다
- **★ 위키가 우리보다 틀린 경우가 생각보다 많다. bin 을 이겼다고 섣불리 고치지 말 것.**
  2026-08-11 인게임 확인 6건 중 **4건이 우리가 맞고 위키가 틀렸다** —
  나서스 E(`50 (+주문력 60%)`, 양 위키 `100 (+120%)`), 헤카림 W(`80 (+80%)`, lolwiki `100`),
  샤코 P(`35`, lolwiki `31.18`), 카타리나 E(`총 공격력`, lolwiki `bonus AD`).
  **bin 은 게임 원본이라, 우리 코드가 bin 을 제대로 읽었으면 우리 값이 곧 게임 값이다.**
  진짜 버그는 "코드가 bin 을 잘못 읽은 자리" 뿐이다
  - **★ 2026-08-12 전수 대조에서 2건 더 나왔다** (`위키대조_20260812.md`):
    **모데카이저 Q** — 위키 `0–55`, 우리 `0~45`. **아지르 W 가 bin 구조가 똑같은데**
    (`mLevel 10, +N/레벨`) 위키도 `0–72`(= 8x9)라고 적는다. 같은 공식을 모데카이저에만
    11번 증가로 적용한 것이라 **위키가 틀렸다.**
    **벨베스 R** — 위키 `5/15/25%`, 우리 `6/13/20`. **16.16 패치로 바뀐 값**이라
    위키가 아직 반영을 안 했다. lolwiki 는 "as of V26.14" 라고 적혀 있다 —
    **위키가 한두 패치 뒤처지는 것이 정상 상태**라고 보는 편이 안전하다
  - **★ 다를 땐 단위·기준부터 의심할 것.** 20레벨 표기(뽀삐 P 3자리),
    5초당 vs 초당(체력 재생), **틱당 vs 초당**(나서스 R — bin 의 `TickRate 0.5` 라
    초당 2틱이다. 위키 `1.5/2/2.5% per tick` = 우리 `매초 3/4/5%`)
  - **★ 같은 bin 구조를 가진 두 자리를 위키와 교차 대조하면 어느 쪽이 틀렸는지 갈린다.**
    모데카이저 Q 를 이 방법으로 확정했다. 표본을 넓히는 것보다 이쪽이 커버리지가 높다 —
    **곡선 종류는 426자리가 6가지로 수렴하므로 종류별 대표만 검증하면 된다**
- **누누 W 최소 피해는 인게임 툴팁조차 틀리다.** 툴팁은 60(= bin 의 `NoImpactDamageScalar` 0.333)인데
  실제로 때려보면 36 이다. 최소 = 최대의 20% 가 맞다. bin 에 0.2 가 없어서 MANUAL 로 넣었다.
  **"안 맞았을 때(NoImpact)" 값과 "최소 충전" 값은 다른 것**이라는 걸 놓치기 쉽다 (2026-08-11)
- **`@spell.<스펠이름>:<이름>@`은 다른 스펠의 값을 부르는 교차 참조다.** 대소문자가 섞여 있고
  (`spell.`/`Spell.`) 자기 자신을 부르기도 함(소나 P). `buildSpellIndex()`가 스펠 객체
  이름 → `{ spell, maxRank }` 색인을 만든다. **`maxRank`를 같이 들고 다녀야 한다** — 패시브가
  Q 값을 부를 때 P의 1로 읽으면 1랭크만 나오고, R 값을 5로 읽으면 없는 4·5랭크가 지어내진다.
  **플레이스홀더 정규식 3군데(`build_champion_data.js` 1 + `fill_values.js` 2)에 `:`가
  들어가야 하고 셋이 항상 같아야 한다.** 어긋나면 문장 `{pN}`과 값 `pN` 개수가 안 맞는다
- **CD가 스펠 *경로* 이름도 `{해시}`로 남길 때가 있다.** 스몰더 패시브가 `{c72a53d8}`라
  `SmolderP`라는 키가 아예 없었음. 그래서 색인에 `<Alias>P`·`<Alias>Passive`·`<Alias>Q..`
  관례 이름으로도 등록해 둠
- **`{{Spell_XXX_Name}}`은 stringtable에서 소문자 키로 푼다.** 패시브 툴팁을 stringtable에서
  가져오면서 딸려 들어온 문법. `@Placeholder@`와 달리 `{p}` 자리가 아니라서 **못 풀어도
  폴백이 안 걸리고 화면에 그대로 찍힌다.** `build_champion_data.js`의 `convertDescription`에서
  처리. 키 안에 `{pN}`이 또 들어간 중첩형 13개는 못 푼다 (아펠리오스 무기 이름 등, 아래 참고)
- **`mScaleByStatProgressionMultiplier`는 무시해도 된다.** 이 플래그는 중간 레벨의 곡선
  모양만 바꾸고 양 끝값은 안 바꿈. 성장곡선 `g(N)=(N-1)(0.7025+0.0175(N-1))`이
  `g(18)=17`이라 18레벨 배율이 정확히 1.0이 됨. 사이트는 양 끝만 찍으니 표시가 안 달라짐.
  올라프 P(100%)·야스오 P(600) 인게임 확인 완료. 23자리 해당
- **★ stringtable 에는 현재판·구버전·클래식 모드용이 전부 섞여 있다. 키 이름으로 고르지 말 것.**
  2026-08-09에 "stringtable 에 더 상세한 문장이 있네?" 하고 `spell_ahriq_tooltip` 을
  꺼내 우리 문장과 비교했다가 **"우리 데이터에 정보가 누락됐다" 는 틀린 결론**을 냈다.
  그건 옛날 아리(정기 흡수 = 스킬 적중 중첩) 설명이었고, 현재 아리는
  `generatedtip_passive_ahripassive_tooltip`(정기 조각 = 미니언 처치)이며 **우리가 맞았다.**
  **`jade_` 접두사만 거르면 되는 것도 아니다** — `spell_ahriq_tooltip` 은 접두사가 없는데도
  구버전이었다. 그래서 기존 규칙(bin 의 `keyTooltip` 에서 읽기)이 존재하는 것이다.
  stringtable 을 직접 뒤져 문장을 가져오려 하지 말 것
- **`<font color=...>` 는 라이엇이 원문에 박아 둔 색이고 인게임과 정확히 일치한다.**
  바드 P "고대의 종" `#cccc00`, "정령" `#ff9900` 을 스크린샷 픽셀과 대조해 확인.
  **덮어쓰지 말 것.** 예전엔 `app.js` 에서 `!important` 로 보라를 강제해 44곳이 죽어 있었다.
  **원문 문장에 색이 하나만 보여도 나머지가 없다는 뜻이 아니다** — 아펠리오스 무기색이
  W 문장엔 중력포 하나만 있었지만 실제로는 `apheliosgun_lorename_1~5` 값 안에 5개가
  전부 들어 있었다 (`{{ ApheliosGun_LoreName_@f1@ }}` 가 런타임에 끼워 넣는 구조).
  추측으로 채우지 말고 stringtable 에서 **값**을 찾을 것 (문장이 아니라 값은 안전하다)
- **한국어 스킬명·무기명이 필요하면 나무위키(`namu.wiki/w/<한글명>`)를 먼저 볼 것.**
  WebFetch 로 읽힌다. 롤위키는 영문명만 주므로 한국어 표기를 추측하게 되는데,
  2026-08-09 아펠리오스 무기 Q 이름 5개 중 4개를 틀리게 추측했다
  (월광 사격→**달빛탄**, 구속의 월식→**월식**, 황혼의 물결→**황혼파**, 파수병→**파수탑**).
  수치는 두 위키가 일치했으니 **한국어 이름은 나무위키, 수치는 롤위키**로 나눠 쓰면 된다
- **`custom_templates.js` 의 `"X": null` 은 "이 챔피언에겐 이 키가 아예 없다"는 뜻.**
  `app.js` 가 스킬 목록에서 통째로 뺀다. 아펠리오스 E 가 유일한 사례다
  (무기 교체가 탄약 소진으로 자동 진행돼 E 키를 안 쓰는데 Data Dragon 엔 더미가 있다).
  **`""` 는 "아직 문장을 안 썼다"(= DD 폴백) 라서 뜻이 다르다.** 헷갈리면 스킬 칸이 사라진다
- **롤위키 "based on level" 수치는 몇 레벨 기준인지 먼저 볼 것.** 기본 표시가 18레벨이
  아닐 수 있다. 야스오 P 방어막 `674.46`이 **20레벨**(우르프 등) 값이었고, 커서를 올리면
  18레벨은 `600`으로 뜬다. 사이트는 18레벨 기준이므로 bin 끝값이 맞다
- bin의 `cooldownTime`은 한 칸 밀린 듯해서 CD v1의 `cooldownCoefficients`를 씀
- 사거리는 내부값(럭스 Q가 10000으로 나옴) 대신 `castRangeDisplayOverride`를 씀
- **★★ 투사체 속도·스킬 폭은 스펠 **본체에서만** 읽는다 (2026-08-14).**
  `missileSpeed` 는 예전에 `pool` 전체를 훑어 `> 0` 인 첫 값을 잡았는데, `pool` 은
  **본체 → 이름이 겹치는 계열 → 기타(leftovers)** 3등급이고 `getSpellsFromBin` 이
  **leftovers 를 다섯 슬롯 전부에 통째로 복사한다.** 거기엔 `<챔프>BasicAttack`·
  `<챔프>CritAttack` 같은 **기본 공격 객체**가 들어 있다.
  - **`missileSpeed` 는 이름으로 찾는 값이 아니라서, 등급을 내려가는 순간 아무 객체의
    값이나 걸린다.** `p` 값(이름으로 조회)과 성격이 다른데 같은 pool 을 썼던 게 원인이다
  - 전수로 재니 **634자리 중 310자리(48.9%)** 가 남의 값이었다 —
    가렌 W `347.8`(가렌 기본 공격 속도), 갈리오 W·E·R `1000`, 나피리 QWER `347.8`,
    **니코 R `250000`**
  - **본체 값이 `0` 이면 "이 스킬엔 투사체가 없다" 는 뜻이지 "값 없음" 이 아니다.**
    `> 0` 조건이 이걸 "못 찾음" 으로 오해해 폴백을 태웠다
  - **★ 본체 값이 `20` 이하면 근접 판정용 더미다.** 70자리가 그렇고
    (아트록스 E·알리스타 Q·다리우스 R…) 전부 투사체가 없는 스킬이다. 가렌 Q 가
    `GarenQAttack` 의 **20** 을 집어서 "투사체 속도 20" 이라고 찍고 있었다
  - **★★ 본체 값이어도 걸러야 하는 더미가 두 종류 더 있다 (2026-08-14, 7자리).**
    · **`1000000000`** — "즉시 도달". 스웨인 Q 가 유일한데 화면에 10억이 그대로 찍혔다
    · **`347.8`** — **라이엇 엔진의 기본 공격 기본값**이다. 마스터 이 bin 에는
      `BasicAttack` 객체가 **아예 없는데도** 본체 `WujuStyle` 에 이 값이 박혀 있다.
      그래서 "기본 공격 속도와 같은가" 로는 못 걸러내고 **값 자체를 봐야 한다.**
      6자리이고 전부 투사체가 없는 스킬이다 (마스터 이 E · 블리츠크랭크 R · 신 짜오 R ·
      신지드 Q · 초가스 E · 하이머딩거 R)
  - **★ "그 챔피언 기본 공격 속도와 같으면 제외" 규칙은 쓰면 안 된다.** 14자리가 걸리는데
    드레이븐 E(도끼)·케넨 Q(표창)·샤코 E(단검)·엘리스 E(고치)처럼 **진짜 투사체**가 대부분이다.
    원거리 챔피언은 스킬 투사체가 기본 공격과 같은 속도인 게 자연스럽다
  - 결과 **634 → 317자리**. 남는 건 대부분 진짜 투사체 스킬이다 (아리 QWER, 애쉬 WER…)
  - **★ 아직 남은 의심 22자리**: `828.5`(13) · `779.9`(7) · `943.8`(2). 소수점이 붙은 걸
    보면 이것들도 기본 공격 계열 값인데, **자기 대상 스킬에 몰려 있으면서도**
    (럼블 W 고철 방패, 아트록스 R, 니달리 R·엘리스 R 폼 변신) **진짜 투사체도 섞여 있다**
    (진 R 커튼 콜, 코르키 R 미사일 폭격, 미스 포츈 R). **값만 보고 자를 수 없어 남겨 뒀다**
- **★★ 패시브에도 쿨타임이 있다 — 173명 중 21명 (2026-08-14).**
  예전엔 두 스크립트가 `"cooldown": "-"` 로 **하드코딩**했다. "패시브엔 쿨타임이 없다" 는
  전제가 틀렸다. 찾아가는 길은 툴팁·회색 글씨와 같다:
  bin `mSpell.mClientData.mTooltipData.mLocKeys.**keyCooldown**` → stringtable(소문자).
  - 문장이 `"%i:cooldown% @PassiveCooldownModified@초"` 꼴이라 **본문과 똑같은
    `@Placeholder@` 문법**이다. 기존 해석기(`resolveFromPool`)를 그대로 쓴다
  - **★ `CooldownMultiplierCalculationPart` 가 `currentCooldown` 을 참조한다.**
    패시브엔 랭크별 쿨타임이 없어서 그대로 두면 **직전 챔피언의 R 쿨타임**이 섞인다.
    갈리오 P 가 `CooldownMultiplier x PassiveCooldown` 이라 이 자리가 곧 배율이고,
    쿨감 0% 기준 배율은 **1** 이다. 그래서 패시브를 풀기 전에 `currentCooldown = '1'` 로 둔다
  - **채운 건 20명이다. 암베사만 못 채웠는데 그게 맞다** — stringtable 에 키는 있는데
    **값이 빈 문자열**이라 인게임에서도 표시하지 않는다는 뜻이다
  - 화면은 `쿨타임 ${cooldown}` 으로 찍으므로 QWER 과 맞춰 **숫자만** 남긴다
    (아이콘 토큰과 "초" 를 벗긴다). 자크 300 / 애니비아 240 / 갈리오 5 처럼 나간다
  - **★ 롤위키로 16명을 대조했고 전부 우리가 맞았다 (2026-08-14).**
    어긋나 보인 셋(뽀삐 위키 6 / 라칸 12.75 / 자이라 8.51)은 **전부 20레벨 값**이다.
    **bin 의 `mLevel: 19` 브레이크포인트가 물증이다** — 19레벨은 협곡에 없다.
    18레벨 값은 뽀삐 `16-4-4=8`, 라칸 `40-1.5x17=14.5`, 자이라 역산 `9.05` 로 우리와 같다
  - **★★ 레벨에 따라 변하는 10자리에 각주를 단다 (2026-08-14).**
    `12 ~ 6 (레벨에 따라)` 한 줄이면 **매 레벨 줄어드는 것처럼 읽히는데** 실제로는
    계단식이다 — 그라가스 1/6/11/16레벨 `12/10/8/6`, 나피리 1/6/9/12/15 `30/25/20/15/10`.
    곡선은 **`level_curves.json` 에 이미 있었다**(`kind: "breakpoints"`).
    - **조인 열쇠는 줄 뒤 주석의 계산식 이름**이다. `fill_values.js` 가
      `"cooldown": "12 ~ 6 (레벨에 따라)", // HealCooldown` 으로 내보내고
      `add_level_graphs.js` 가 `pN` 과 같은 방식으로 조인한다
    - **★ 한 줄 형식(`"P": { "cooldown": ..., "cost": "-" },`)에는 주석을 못 붙인다** —
      `//` 가 **뒤의 `"cost"` 까지 먹어** 파일이 깨진다. 계산식 이름이 있으면 여러 줄로 낸다
    - 쿨타임은 문장이 아니라 우상단에 늘 찍히므로 **"문장에 쓰이는가" 검사를 건너뛴다**
    - `app.js` 는 `skill.cooldownNote` 로 받아 `쿨타임 ${cooldown}${note}` 로 붙인다
    - **★ `drawSteps` 제목의 "상승/감소" 는 양 끝값을 비교해 정한다.** 쿨타임이 들어오면서
      **줄어드는 계단이 처음 생겼는데**, 그라가스 `12 -> 6` 이 "상승" 으로 나가 정반대였다
- **라이엇이 툴팁 철자랑 bin 철자를 따로 관리해서 대소문자가 자주 어긋남.** 22종 발견 (`HoTDuration`→`HotDuration`, `EnergyRefund`→`energyrefund` 등). 계산식 안쪽 참조에서도 같은 일이 생기고, 조각 하나가 null이면 계산식 전체가 죽음
- **"스킬 쿨타임으로 추론" 휴리스틱은 신뢰도가 낮음.** 4건 중 2건이 오답이었어. 필드 이름이 쿨타임 자체가 아니라 환급·감소·제한 시간을 뜻하면 틀려. 이 목록은 매번 전수 검수할 것

### 현재 상태 (2026-08-09 기준)

- **3211/3228 (99%) 채워짐.** 빈칸 총수가 계속 느는 건 새 자리가 생겨서다
  (패시브 173개 → 가렌·갈리오 → 교차 참조 116자리 → 두 번째 폼 17스킬)
- **★ DD 폴백 0개.** 전 챔피언이 우리 문장으로 나간다
  (2026-08-09 달성이라고 적었는데 **실제로는 피들스틱 1명이 폴백이었다**.
   2026-08-10에 원인을 잡고 진짜 0개가 됐다 — 아래 항목 참고)
- **★ `app.js`는 Data Dragon의 `champ.id`로 `customValues`/`customTemplates`를 찾는다.
  그래서 파일의 챔피언 키는 반드시 DD 철자여야 한다.** CD(CommunityDragon) 철자로
  나가면 조회가 통째로 실패해서 **문장·수치·아이콘을 다 버리고 DD 툴팁으로 폴백**한다.
  233명 중 **피들스틱 하나만** 철자가 다르다 — DD `Fiddlesticks` / CD `FiddleSticks`.
  `fill_values.js`·`build_champion_data.js` **양쪽의 `DD_ID` 표**로 출력 키만 바꾼다
  (bin 경로·스펠 조회는 CD 철자를 그대로 써야 하므로 `alias`는 건드리면 안 된다).
  **`MAX_RANK`처럼 두 파일에 같은 표가 있어야 하고, 어긋나면 템플릿과 값의 키가 갈린다.**
  **"폴백 0개"는 스크립트 출력만 보고 적은 것이었고, 화면을 직접 열어본 게 아니었다.**
  피들스틱을 열어보니 `[스탯 비례]`가 그대로 찍혀 있었다. 새 챔피언이 나오면
  DD/CD 철자를 대조하고, **"다 됐다"고 적기 전에 브라우저로 한 명은 열어볼 것**
- **`검산목록.md`(루트, gitignore됨) A 섹션 완료.** 19자리 중 15자리가 맞았고 4자리를
  고쳤다. **다음은 B·C 섹션 15자리**
- **못 채운 17개는 전부 문장에서 뺀 자리라 화면에 영향이 없다.**
  (카이사 진화 게이지·스몰더 현재 중첩·바드 현재 성소 수·아펠리오스 무기 순서 등
   "지금 내 챔피언의 현재 수치" 라 고정값이 없는 자리들)
- 문장 `{pN}` ↔ 값 `pN` 정합성 865자리 전부 일치 (2026-08-08 검사)
- **`f` 계열이 안 채워지는 원인 3종은 bin 조사로 확인해 뒀다** (추측 아님):
  1. **`mEffectAmount` 슬롯 자체가 없거나 `value` 가 빔** — 바드 P, 아우렐리온 솔 P,
     퀸 P 가 이 경우였다. bin 밖(공용 스펠·버프 객체)에 있다는 뜻이라 **MANUAL 밖에 답이 없다**
  2. **`f` 번호가 배열 범위를 넘음** — 자야 P 가 10칸짜리 배열에 `f12`·`f14`·`f16` 을 불렀다.
     **`f번호 = mEffectAmount 인덱스` 전제가 깨지는 자리가 있다.** 값은 배열에 다 있었지만
     (`f12`→인덱스 0, `f14`→인덱스 5, `f16`→인덱스 7) **번호 규칙은 못 찾았다.**
     재사용 가능한 해법이 아니니 이런 자리는 그냥 MANUAL 로 넘길 것
  3. **이름만 다르고 본체에 값이 있음** — 가렌 E `f1` 이 본체 계산식 `NumberOfStrikes`
     (→ `NamedDataValue` `NumTicks`=7) 였다. **`f` 자리를 만나면 본체의 계산식·DataValue
     이름부터 훑어볼 것.** 제일 싸게 풀리는 경우다
- 인게임 확인 결과 원본은 **`확인결과.md`** (루트, gitignore됨). 항목별 근거가 다 적혀 있으니 작업 재개 전에 먼저 읽을 것
- **`피해량_위키대조.md`** (루트, gitignore됨) — 2026-08-11 위키 전수 대조 보고서.
  173챔피언 / 751스킬 / 피해량·계수 1225자리를 lolwiki·나무위키와 대조한 결과와
  고친 버그 4종의 근거가 다 있다. **"위키가 우리보다 틀린 경우" 도 정리돼 있으니
  수치를 의심하기 전에 먼저 볼 것**
- **`레벨곡선_정리.md`** (루트, gitignore됨) — 레벨 곡선 데이터 설명서.
  성장 공식 g(N), 곡선 5종, DD `attackdamageperlevel` 이 깨진 건, 각주 구조
- **`회색글씨_적용보고.md`** (루트, gitignore됨) — 2026-08-12. 구분선 아래 회색 글씨
  354자리 / 155챔피언을 챔피언·스킬별로 전부 적어 뒀다. 숨긴 8자리 이유도 있다
- **`시연영상_조사.md`** (루트, gitignore됨) — 2026-08-12. 라이엇 CDN 4,325건 전수 확인.
  "왜 흐웨이 Q 영상이 하나뿐인가" 의 답과 자헨 Q 파일명 어긋남 건
- **★ `위키대조_20260812.md`** (루트, gitignore됨) — 2026-08-12 롤위키 전수 대조.
  기본 스탯 2310자리 기계 대조 + 레벨 곡선 426자리 패턴 점검 + 챔피언 12명 스킬값
  직접 대조. **우리 오류 0건.** 위키가 틀리거나 뒤처진 사례와, "오류가 숨는 구조"
  분석이 들어 있다. 수치를 의심하기 전에 `피해량_위키대조.md` 와 같이 볼 것
- **`level_curves.json`** (커밋됨) — 스킬 레벨 곡선 425자리 / 141챔피언.
  키가 `bin alias(소문자) → 슬롯 → 계산식 이름` 인데 계산식 이름이
  `custom_values.js` 줄 뒤 주석과 같아서 **그대로 조인된다**
- **`champion_stats_by_level.json`** (커밋됨) — 챔피언 스탯 173명 x 18레벨.
  **2026-08-12부터 스탯 탭이 쓴다.** `build_stats_page.js` 가 base + perLevel 로 압축해
  `public/champion_stats.js`(68KB) 를 만들고, g(N) 은 화면에서 계산한다

### 폴백을 푸는 두 번째 방법 — "문장에서 빼기" (2026-08-09)

빈칸이 **"지금 내 챔피언의 현재 수치"** 라서 고정값이 아예 없는 자리가 있다
(카이사 진화 게이지, 스몰더 현재 중첩, 바드 현재 성소 수 등).
**MANUAL 로 숫자를 넣으면 거짓말이 된다.** 문장에서 빼는 게 맞다.

**단, 문장에서 `{pN}` 을 지우는 것만으로는 폴백이 안 풀렸다.** 두 가지를 알아야 한다.

1. **값 쪽 `pN` 은 지워도 소용없다.** `fill_values.js` 는 문장이 아니라 **CD 원본 `desc` 의
   `@Name@` 을 훑어서** p1..pN 을 만든다. 손으로 지워도 재실행하면 되살아난다
2. **그래서 `app.js` 의 가드를 고쳤다.** 예전엔 `values` 의 **모든** `pN` 을 검사해서,
   문장이 안 쓰는 자리의 `?` 까지 세고 멀쩡한 문장을 통째로 버렸다.
   지금은 **문장에 실제로 등장하는 `{pN}` 만** 본다 (`tpl.includes('{'+k+'}')`).
   가드의 목적이 "화면에 물음표가 찍히는 걸 막는 것" 이니 이게 맞다

이 방식은 **템플릿만 고치면 되고 `fill_values.js` 재실행에도 안전하다.**
8스킬이 이렇게 풀렸다.

### 남은 일

**★ 지금 살아 있는 항목만 (2026-08-12 기준). 아래 번호 목록엔 끝난 것도 섞여 있다.**

| # | 할 일 | 성격 |
|---|---|---|
| 15 | **아레나 밴을 조별로 쪼개기.** 밴 18개를 조 6개에 3개씩 나눠 각 조 줄 옆에 붙인다. 통계로는 `pickTurn 1~3 = 조1, 4~6 = 조2 …` 가 맞는데 **인게임 확인이 남았다** (아래 "아레나 밴" 절) | 인게임 확인 필요 |

- 14번(사거리 손 표)은 2026-08-14에 끝났다

**14번 결과** — `castRange` 가 25000(표시할 값 없음)인 **38자리** 중 **15자리를 채웠고
나머지 23자리는 채울 게 없는 것으로 확인했다.** 값은 `fill_values.js` 의 `STAT_MANUAL` 에
근거와 함께 적혀 있다.

- 채운 것: 유미 Q 850 · 유미 W 700 · 요네 E 300 · 세라핀 R 1200 · 미스 포츈 R 1400 ·
  아트록스 E 300 · 오른 W 500 · 바드 Q 850 · 헤카림 R 1000 · 진 R 3500 · 세트 W 720 ·
  제리 E 300 · 자헨 Q 200 · 자헨 E 350 · 잔나 W 550
- **채울 게 없는 23자리의 정체**: ① 진짜 전역(소라카 R·애쉬 E 는 위키가 `Global` 이라고
  못 박는다, 갱플랭크 R·드레이븐 R·쉔 R·카직스 R 등) ② 자기 대상·방향 지정이라 사거리
  항목 자체가 없음(모데카이저 W·브라움 E·아트록스 Q·유미 E·카타리나 W) ③ 고정값이 아님
  (워윅 R = 이동 속도 250%, 르블랑 R = 흉내내는 스킬을 따름) ④ 나무위키가 `-` 로 적음(탐 켄치 R)
- **★ `STAT_MANUAL` 은 한 슬롯당 한 줄로 합칠 것.** 키를 두 번 적으면 JS 객체라
  **뒤엣것만 남아 앞엣것이 조용히 사라진다** — 진 R 사거리가 실제로 그렇게 날아갔다

아래는 2026-08-13에 끝난 13개 항목이다.

**★ 2026-08-13 기준 화면 상태: `{pN}` 3535자리 중 값없음 0 / 폴백 0 / 미해결 `{{ }}` 0.**

마지막 세 항목은 **고칠 게 아니라 이미 끝나 있던 것**이었다. 표가 실제보다 부풀려져 있었다:

- **1번(폴백 8스킬)** — 벨베스 E·세트 W·신 짜오 E·바드 W 는 값·문장이 이미 완성돼 있었다.
  실제로 남은 건 회색 글씨 5자리(`<stattracking>`)뿐이었다
- **4번(중첩형 13자리)** — 12자리가 이미 `KEEP_TEXT` 로 처리돼 있었고 아크샨 W 하나만 남았다
- **6번(니달리 W)** — `4 ~ 10 (레벨에 따라)` 로 이미 채워져 있고 각주도
  `[1→4] [6→6] [11→8] [16→10]` 로 정확하다. 롤위키의 레벨 구간과 완전히 일치한다
- **10번(`STAT_NAMES` 3종)** — **확인할 대상이 없다.** 5(스킬 가속)는 2026-08-10 인게임
  확인이 끝났고, **11 은 bin 전체에 0곳**(16.16.1 재확인), 13 은 bin 에 4곳
  (문도 Q·엘리스 Q·킨드레드 W·자헨 R) 있지만 **화면에 나가는 문장엔 0자리**다.
  → **새 챔피언이 `mStat 11`·`13` 을 쓰기 시작하면 그때 인게임 확인할 것**

**★ 교훈: 표를 믿지 말고 실제 데이터를 먼저 셀 것.** 네 항목 모두 "적힌 증상"과
"실제 상태"가 달랐다. 작업 전에 `{pN}` 폴백 수와 `{{ }}` 잔여를 세는 것으로 시작하면 된다.

**최근 작업 흐름 (2026-08-12)**: 회색 글씨 → 무한의 대검 각주 → 스탯 탭 신설 →
챔피언 목록 필터 → 스탯 표/그래프 → **역할군 평균 비교 + vs 챔피언** → 파일 정리 →
**카이사 E 두 자리 + 16.16 패치 대응**.
스탯 탭 관련 규칙은 전부 위 "★ 스탯 탭" 절에 모여 있다.

**16.16.1 패치 대응 결과 (2026-08-12)**: 문장 18줄 / 값 24자리가 바뀌었다.
모데카이저 E 에 **기본 지속 효과(마법 관통력)가 새로 생겨 `{p1}` 이 밀렸고**(기존 → `{p2}`),
뽀삐 Q·W, 카밀 P·W(쿨 15→12), 그웬 P, 나서스 Q, 아지르 Q, 케넨 R, 벨베스 R 이 조정됐다.
원거리 딜러 28명 MR 30→33. 검증은 이렇게 했다:
**문장 `{pN}` ↔ 값 정합성 3544자리 전부 일치 / 본문 DD 폴백 0개 유지 / 스탯 압축 복원
24192자리 최대 오차 0.00092 / 헤드리스 크롬으로 5명 렌더 — 콘솔 에러 0**.
`app.js` 의 `ddragonVersion` 초기값도 16.16.1 로 올렸다(동기화 실패 시 폴백용).

1. ~~남은 8개 폴백 스킬~~ **완료 (2026-08-13).** **적힌 것보다 훨씬 적게 남아 있었다** —
   벨베스 E·세트 W·신 짜오 E·바드 W 는 이미 값과 문장이 완성돼 있었고(신 짜오 E 는
   `38 / 46 / 54 / 62 / 70% (+ 주문력의 10%) (+ 영구적 공격 속도 5%당 1%)` 까지 들어 있다),
   카이사 Q·E 와 아펠리오스 R 도 처리가 끝나 있었다.
   실제로 남은 건 **회색 글씨 5자리**뿐이었고 전부 `<stattracking>`(누적값)이었다 —
   아래 4번과 같이 처리했다
2. ~~`guessPart` 두 건~~ **해결 (2026-08-12).** 둘 다 적혀 있던 것과 증상이 달랐다.
   - **우디르 Q 는 이미 고쳐져 있었다.** 2026-08-11 의 `StatBy*` mult 버그 수정으로 같이
     풀렸고 지금은 `1.5 ~ 3% (+ 주문력의 0.6%)` 로 정확히 나간다 (인게임 툴팁과 일치).
     `MANUAL` 의 `우디르 Q / EmpoweredLightningBonusMax` 는 이제 안전망일 뿐이다
   - **카이사 E 는 `ClampSubParts` 를 무시한 게 아니라 `null` 천장을 못 걸러낸 것이었다.**
     아래 "★ 열린 clamp" 항목 참고
3. ~~우디르 6랭크~~ **해결.** `MAX_RANK` 표를 `fill_values.js`·`build_champion_data.js`
   **양쪽에** 뒀다(어긋나면 쿨타임 칸 수와 수치 칸 수가 안 맞음). 6종:
   우디르 QWER 6 / 제이스 QWE 6·R 1 / 니달리·카르마·엘리스 R 4 / 유미 Q 6·R 3.
   **자동 판별은 불가능** — bin `mSpell`에 최대 레벨 필드가 없고 CD의 계수 배열은
   챔피언과 무관하게 전부 길이 6이다. 새 챔피언이 나오면 표에 직접 추가할 것
4. ~~중첩형 `{{키_{pN}}}` 13개~~ **완료 (2026-08-13).** 12자리는 이미 `KEEP_TEXT` 로
   손 처리돼 있었고 **아크샨 W 하나만 남아 있었다.**
   - **★ "정적 조회로는 못 푼다" 가 항상 맞는 건 아니다.** 아크샨 W 의 `{p1}` 은
     `GameModeInteger` 이고 **값이 1 로 고정**이라 키가 정해진다.
     stringtable 에 `spell_akshanw_tooltip_1` 과 `_2` 가 **둘 다 있다** —
     `_1` 이 협곡(아군 부활 포함), `_2` 는 다른 모드(부활 없음)다.
     bin 의 `GameModeInteger` 가 1 이므로 `_1` 이 맞고, 값 6개를 bin 에서 뽑아
     롤위키와 대조했다(이동 속도 `80 / 90 / 100 / 110 / 120` 등 전부 일치).
   - **이 형태는 폴백이 안 걸린다.** 그래서 화면에 `{{ Spell_AkshanW_Tooltip_1 }}`
     이라는 글자가 그대로 찍히고 **스킬 설명이 통째로 없었다**
   - **★ `<stattracking>` 은 드레이븐 P 두 자리가 전부가 아니었다** (2026-08-10 전수
     조사 기록은 틀렸다). 아크샨 W·드레이븐 R·아우렐리온 솔 R·초가스 R·파이크 R 에도 있고,
     전부 값이 `?` 라 **회색 글씨가 통째로 안 나가고 있었다.**
     최대값이 같이 적힌 자리는 현재값만 지우고 최대값을 살린다 —
     초가스 R `현재 중첩 {p5}/{p4}` → `최대 {p4}까지 쌓입니다`.
     남길 게 없으면 통째로 비운다 (파이크 R 은 두 줄 다 누적 골드였다)
5. ~~카이사 W의 `@spell.KaisaPassive:PDuration@`~~ **해결.** 전수 조사해보니 1건이 아니라
   **119자리 / 58스킬 / 37챔피언**이었고 전부 화면에 원문이 그대로 찍히고 있었다.
   115자리 해석 완료. 남은 22자리는 아펠리오스 P `Hotkey`(무기가 별도 캐릭터 bin이고
   값도 숫자가 아님)
6. ~~니달리 W 덫 최대 개수~~ **이미 돼 있었다 (2026-08-13 확인).**
   값은 `4 ~ 10 (레벨에 따라)` 이고 각주가 `[1→4] [6→6] [11→8] [16→10]` 로 계단을 그린다 —
   **롤위키의 레벨 구간(1-5 / 6-10 / 11-15 / 16-18)과 정확히 일치한다.**
   피해도 맞다. 우리 초당 `10 / 20 / 30 / 40 / 50 (+ 주문력의 5%)` x 4초 =
   위키 총합 `40 / 80 / 120 / 160 / 200 (+ 20% AP)`. 단위만 다르고 값은 같다
7. ~~패시브 173개 자동화 경로 없음~~ **해결.** `stringtable.js` 가 인게임 진짜 툴팁을
   가져온다 (`lol.stringtable.json`, 35MB, `.cache/` 에 7일 캐시. `--refresh` 로 강제 재다운).
   찾아가는 길: bin 의 `mSpell.mClientData.mTooltipData.mLocKeys.keyTooltip` → **소문자로**
   `entries` 조회. 키 규칙이 3종이라 이름으로 추측하지 말고 반드시 bin 에서 읽을 것.
   173/173 확보, 163명 문장에 `{pN}` 들어감. 나머지 10명은 패시브에 수치가 없는 타입
8. ~~레벨별 성장 각주~~ **완료 (2026-08-11).** `public/custom_graphs.js` (`add_level_graphs.js` 생성)
   가 **134챔피언 / 188스킬 / 298자리**에 각주를 단다.
   (2026-08-12에 구분선 아래 회색 글씨 문장도 대상에 넣어 268 → 298 이 됐다)

   **★ 각주는 `v1`/`v2` 가 아니라 문장 안 수치 바로 뒤에 붙는다.**
   `app.js` 의 `renderScalingTable` 이 `{pN}` 을 채울 때 `values[key] + graphs[key]` 로 이어 붙여서
   **색칠된 태그 안쪽 마지막 글자**에 달린다 (`<healing>...10.1% (레벨에 따라)[1]</healing>`).
   처음엔 v1/v2(구분선 아래 줄)에 넣었는데 **같은 수치가 두 번 보여서** 옮겼다.

   **왜 별도 파일인가**: 값(`custom_values.js`)도 문장(`custom_templates.js`)도 스크립트가 매번
   새로 찍는다. 거기 써 넣으면 재생성 때 날아간다. `custom_graphs.js` 는 아무도 안 건드린다.
   `index.html` 에서 **반드시 `custom_values.js` 뒤에** 로드해야 한다 —
   `drawGraph`/`drawSteps` 헬퍼가 거기 있고 이 파일은 로드 시점에 그걸 호출한다
   (`loadChampionData` 가 `async=false` 로 붙여서 문서 순서가 보장된다).

   두 종류로 갈린다:
   - **매 레벨 크는 값 → `drawGraph`** (226자리). 꺾은선 그래프
   - **특정 레벨에서만 바뀌는 값 → `drawSteps`** (72자리). "1 / 7 / 13레벨" 목록.
     나서스 P 생명력 흡수(12/18/24)처럼 `12 ~ 24 (레벨에 따라)` 라고만 적으면
     매 레벨 크는 것처럼 읽히는 자리다. 가르는 기준은 **18레벨 중 값이 바뀌는 횟수 5 이하**
     (bin 전수로 보면 17회(매 레벨)가 177자리로 몰려 있고 계단식은 2~5회라 사이가 비어 있다)

   곡선 원본은 `level_curves.json` (`build_level_curves.js` 가 생성).

   - **★ `drawGraph`/`drawSteps` 헬퍼는 `public/custom_values.js` **맨 앞**에 있고,
     `fill_values.js` 가 만드는 게 **아니다**.** `valuesPrelude` 가
     `const customValues = {` 앞부분을 통째로 물려주는 구조라 **거기서 고치면
     재생성해도 살아남는다.** 헬퍼를 스크립트에서 찾다가 못 찾기 쉬우니 적어 둔다
   - `drawGraph` 의 **4번째 인자는 각주 제목**이다(생략하면 `레벨별 성장 수치`).
     **값과 각주의 단위가 다를 때만** 쓴다 — 지금은 아이번 P 두 자리뿐이다

   - **`custom_values.js` 를 스크립트로 파싱할 땐 줄을 `/\r?\n/` 로 자를 것.** 이 파일은 CRLF 인데
     **JS 정규식의 `.` 은 `\r` 을 안 먹는다**(line terminator 취급). `// 주석.*$` 같은 패턴이
     통째로 안 맞아서 709줄을 놓쳤었다. 하루에 두 번 걸린 함정이다
   - 각주 대상은 "값에 `(레벨에 따라)` 가 있고 **그 `{pN}` 이 문장에 실제로 등장**하는" 자리만.
     안 보이는 값에 각주를 달 이유가 없다
   - **점 위에 정확히 올려야만 수치가 뜨던 문제**는 `drawGraph` 가 각 점 **바로 앞에**
     그 점이 담당하는 x 구간을 덮는 투명 `<rect class="graph-hit">` 를 깔아서 해결했다.
     CSS 인접 선택자 `.graph-hit:hover + .graph-point` 로 켠다 —
     **히트존이 `<g>` 바로 앞에 있어야 `+` 가 성립한다.** 순서를 바꾸면 조용히 안 먹는다
   - **★ `.custom-footnote-content` 는 `position: fixed` 고 좌표를 `app.js` 의
     `positionFootnote()` 가 hover 때 찍는다.** CSS 혼자로는 위치가 안 잡히니 둘을 같이 볼 것.
     `absolute` 였을 땐 `.champ-skill-body` 의 `overflow-y: auto` 에 **가로까지 잘렸다**
     (한 축이 visible 이 아니면 다른 축도 auto 로 계산된다). 화면 좌우 끝 각주가 잘려 보인 원인.
     **조상에 `transform`/`filter`/`contain` 이 생기면 fixed 가 그걸 기준으로 잡혀 다시 잘린다** —
     스킬 영역 조상에는 지금 없는 걸 확인해 뒀다
   - **`.champ-skill-body` 에 `scrollbar-gutter: stable` 이 필요하다.** 스킬 데이터가 늦게 로드되며
     스크롤바가 생기면 본문 폭이 15px쯤 줄어 **설명이 다시 줄바꿈되며 덜컹였다**

   **`v1`/`v2` 는 여전히 비어 있고 그대로 두는 게 맞다.** 수치가 이미 문장 안에 다 들어가 있어서
   채우면 같은 숫자를 두 번 보여줄 뿐이다(각주를 v1 에 넣었다가 그 이유로 문장 안으로 옮겼다).
   비어 있으면 구분선까지 통째로 숨겨지므로 화면에 아무 영향이 없다.

9. ~~스킬 설명 색 일괄 적용~~ **완료 (2026-08-09).** 43종 전부 인게임 스크린샷에서
   픽셀을 뽑아 실측했다. `app.js` 의 `<style>` 블록에 있고, 값 근거는 그 주석 참고
10. ~~`STAT_NAMES` 나머지 추정값 확인~~ **확인할 대상이 없다 (2026-08-13 재확인).**
    5(스킬 가속)는 2026-08-10 인게임 확인이 끝났고, 나머지 둘은 **화면에 영향이 0** 이다:
    **`mStat 11` 은 bin 전체에 0곳**(16.16.1 기준으로 다시 셌다),
    `mStat 13` 은 4곳(문도 Q·엘리스 Q·킨드레드 W·자헨 R)뿐인데 **화면에 나가는 문장엔 0자리**다.
    사이트의 "추가 공격력" 표기는 11번이 아니라 `mStat 2` + `mStatFormula 2` 조합에서 나온다.
    → **새 챔피언이 `mStat 11`·`13` 을 쓰기 시작하면 그때 인게임 확인할 것.**
    **1·6·8·9·12는 확인 끝** (9는 이동 속도가 아니라 **치명타 피해량**. 2026-08-08 정정)
11. ~~곱셈 표시 잔여 9자리~~ **해결 (2026-08-12).** `timesText()` 로 22 -> 1자리.
    남은 1자리는 블라디미르 P 로, 식 자체가 깨진 자리라 아래 12번과 같은 문제다.
    아래는 2026-08-11 당시 기록: `x N` 이라는 날 계산식이 화면에 남는 자리다.
    145 → 9 까지 줄였고 **남은 건 숫자 하나로 못 접는다**:
    치명타 피해량이 변수로 들어가는 식(루시안 R, 미포 Q·R, 제리 W, 탈론 Q),
    배율 자체가 스탯인 식(볼리베어 W), `~` 범위 배율(모데카이저 Q, 유미 R), 중첩식(블라디미르 P).
    `rendered.json` 에서 `/\sx\s[\d.]/` 로 뽑을 수 있다. MANUAL 로 손질하는 수밖에 없다
12. ~~문장이 통째로 깨진 자리~~ **완료 (2026-08-12).** **셋 다 MANUAL 이 아니었다** —
    적혀 있던 증상과 실제가 전부 달랐으니 다음에도 값부터 다시 볼 것.
    - **아이번 P — 계산은 처음부터 맞았고 표기만 깨져 있었다.** `15 ~ 0.006` 은 범위가
      거꾸로인 게 아니라 **진짜로 줄어드는 값**이다(레벨1 15, 레벨당 -0.882 → 레벨18 0.006).
      배율이 스탯(`기본 최대 체력의 1%`)이라 `x` 가 화면에 남은 게 전부였다.
      `timesText` 에 규칙을 더해 **`기본 최대 체력의 15 ~ 0.006%`** 로 접었다.
      롤위키와 양 끝이 맞는다 — 레벨1 `630 x 15% = 94.5`, 레벨18 `2300 x 0.006% = 0.14`
      - **★ 그런데 퍼센트만 보면 "레벨 오를수록 싸진다" 로 읽히는데 그게 아니다.**
        퍼센트는 줄지만 **최대 체력이 늘어서** 실제 소모량은 **6레벨에 정점(108)을 찍고**
        떨어진다 — `94.5 → 108 → 0.1`. 나무위키도 `98 ~ 110 ~ 0` 이라고 적고
        "1~6레벨은 오르다가 7레벨부터 내려간다" 고 설명한다 (마나는 `90 ~ 97 ~ 0` 으로
        우리 계산과 완전히 같다. 체력 시작값만 98 vs 94.5 인데 **나무위키가 옛 기본 체력
        기준**이다 — 최신 bin 은 630 이다).
      - 그래서 **각주만큼은 실제 소모량 곡선**을 보여준다. `add_level_graphs.js` 의
        `scaleByStat()` 이 `"<스탯>의 A ~ B%"` 자리를 만나면 곡선에 그 스탯의 레벨 곡선을
        곱하고 제목을 `레벨별 실제 소모량` 으로 바꾼다. **파이프라인 순서상 안전하다** —
        `champion_stats_by_level.json` 은 `build_level_curves.js` 가 먼저 만든다
    - **블라디미르 P — MANUAL 이 아니라 "문장에서 빼기" 자리였다.** 지운 두 줄
      (`추가 주문력: …` / `추가 체력: …`)은 **지금 내 스탯에 규칙을 적용한 결과**라
      고정값이 없다. 식이 서로를 참조해서(체력→주문력→체력) 날 계산식이 그대로 찍혔다.
      **드레이븐 P 와 같은 성격**이고 `KEEP_TEXT` 의 `Vladimir: ['P']` 로 지켜진다.
      앞의 두 문장이 패시브의 실제 규칙이라 정보 손실이 없다
    - **브라이어 P — 이미 정상이었다.** 롤위키와 전부 일치한다(중첩당 `10~50 (+추가 AD 50%)`,
      최대 `20~100 (+100%)`, 5중첩, 회복 25%, 잃은 체력 보너스 40%)
    - **★ 폴백 직전에 넣는 규칙은 온갖 형태가 다 도달한다.** 배율 자리를 `.+의` 로 느슨하게
      잡았다가 `1 + 추가 공격 속도의 100%` 에서 `1 + …의` 까지 삼켜 **11자리를 망가뜨렸다**
      (악샨 P, 카타리나 R, 켄넨 W, 탈론 Q, 트페 W 등). `[가-힣][가-힣 ]*의` 로 좁혀서
      **스탯 이름 하나일 때만** 접게 했다. 부수 효과로 아펠리오스 R 회색 글씨가
      `100% (+ 30 x 치명타 확률의 100%)` → `100% (+ 치명타 확률의 3000%)` 가 됐는데
      **값은 같고**(치명타 확률 50%면 둘 다 115%) 바루스 P `추가 공격 속도의 1100%` 와 같은 형식이다
13. ~~파일 정리~~ **완료 (2026-08-12).** 백업 44개(19MB)와 `fill_values.old.js`·
    `lulubackground.webp`(참조 0건)를 지웠다. 작업 폴더가 30MB → 11MB
    (`.git`·`.cache`·`node_modules` 제외).
    - **`public/` 안 백업은 한 번도 커밋된 적이 없었다** (`git log --diff-filter=A` 로 확인).
      배포는 git 으로 나가므로 **웹에 노출된 적이 없다.** "public 에 두면 웹에서 받아진다"
      는 경고는 맞지만 실제로 새어 나간 적은 없었다는 뜻이다
    - **`database.sqlite` 는 남겼다.** `server.js` 는 mongoose 만 쓰고 sqlite 드라이버가
      아예 없어서 죽은 파일이 맞지만, 안에 `lp_history`·`summoners` 데이터가 들어 있다
      (옛 버전 전적 데이터로 보인다). 지울 거면 git 히스토리에 남으니 언제든 복구된다
    - 루트 백업은 `.gitignore` 대상이라 **git 에 없었다 = 지우면 못 되살린다.**
      8/8~8/10 스크립트 중간 스냅샷이었고 파이프라인은 `--refresh --write` 로 언제든 다시 만든다
    - **`.cache`(48MB) 는 남긴다.** 지워도 되지만 bin 재다운이 166초, stringtable 이 35MB다
