# pixlol.kr

롤 전적검색 사이트. 한국어로 대화해줘. 평서형 "~다" 말투 말고 "~야/~어" 쪽으로.

## 스택 / 배포

- Node/Express + MongoDB Atlas(무료 512MB) + Railway + Cloudflare
- 프론트는 바닐라 JS. `public/` 안에 `app.js` / `index.html` / `style.css`
- `server.js`는 `public/` 밖, 프로젝트 루트에 있음
- GitHub `cosmicday/pixlol.kr` — push하면 Railway가 자동배포
- 로컬은 `node server.js` → localhost:3000

## 프론트 함정

- **`app.js`의 `ddragonVersion`을 서버 응답으로 덮어쓰지 말 것.** 전적검색 응답의
  `version`은 서버 부팅 시점 값이라 클라이언트가 이미 맞춰 둔 최신보다 오래될 수 있고
  (`server.js:70` 기본값 `16.1.1`), 그러면 **챔피언 탭이 구버전 `champion.json`을 읽어
  신규 챔피언이 목록에서 통째로 사라진다.** 로크가 이 경우였다. `compareDdragonVersion`으로
  더 새 버전일 때만 받아들인다. **문자열 비교는 안 된다** — `16.5.1 > 16.15.1`이 돼버림

## 절대 건드리지 말 것

- **`riot.txt`** — 라이엇 도메인 소유 확인 파일. 프로덕션 키 심사 중이라 없으면 문제가 생김
- **`.env`** — API 키. git에 올라가면 안 됨
- 라이엇 프로덕션 키가 Pending Review 상태(2026-03-10 신청).
  **라이엇 개발자 포털의 앱 등록 정보에서 `EDIT` 를 누르지 말 것** — 누르면 심사
  대기열 뒤로 밀린다. **코드 수정이나 배포와는 무관하다.** git push 는 마음껏 해도 된다
  (2026-08-09 확인. 그전엔 "앱을 크게 수정하면" 이라고만 적혀 있어서 코드 변경 얘기로
   잘못 읽혔다)

## 작업 규칙

- **파일을 고치기 전에 먼저 읽고 설명해줘.** 내가 납득한 뒤에 수정하는 순서로.
- 문제를 알려줄 때는 "문제되는 코드 → 왜 문제인지 → 해결법" 순서로. 코드부터 던지지 말고.
- `git add .`는 2026-08-08에 `.gitignore`를 손봐서 이제 안전해졌어. 루트 `*.md`(CLAUDE.md 제외)·`*.txt`·`*.PNG`·`*.bak-*`가 다 막혀 있음. 그래도 커밋 전에 `git status`로 한 번 볼 것. `public/` 안은 안 막히니 백업 파일을 거기 두면 웹에서 받아짐
- **인게임 확인이 필요하면 체크리스트 파일을 만들어서 넘길 것.** 스킬 설명을 통째로 적게 하지 말고 문장 어느 자리인지 찍어주면 확인이 훨씬 빠름. 확인 항목은 "몇 자리에 영향 있는지" 순으로 정렬할 것 — 스탯 이름 하나가 수십 자리를 좌우함
- `fill_values.js --write` 돌리기 전에 `public/custom_values.js`를 백업해줘.
- **인게임 툴팁 확인은 내가 해.** 수치가 맞는지 추측하지 말고, 확인이 필요하면 나한테 물어봐.

## CD 스킬 데이터 작업 (현재 주 작업)

### 구조

- `public/custom_templates.js` = 문장, `public/custom_values.js` = 수치. `index.html`이 직접 로드
- 문장 안 빈칸은 `{p1}` `{p2}` 형식
- `v1`·`v2`는 구분선 아래 피해량 줄 (p랑 역할이 다름). 지금 전부 비어 있고 직접 작성 대상
- `stats`는 객체 형식 `{ "사거리": "300" }`

### 스크립트 (전부 프로젝트 루트)

| 파일 | 역할 |
|---|---|
| `build_champion_data.js` | 문장·쿨타임·소모값·사거리 생성 |
| `fill_values.js` | 수치 채움. `--write` 붙일 때만 파일 생성 |
| `check_data_sources.js` | 확인 전용 |
| `probe_spell_fields.js` | 확인 전용 |
| `bincache.js` | 챔피언 bin 로컬 캐시 (아래) |

**`bincache.js` — 재실행이 166초에서 9초가 된다 (2026-08-10 추가).**
두 생성 스크립트가 챔피언 173명 bin 을 매번 새로 받던 걸 `.cache/bin/` 에 7일 캐시한다.
표 한 줄 고치고 결과를 보는 짧은 반복이 이것 때문에 통째로 느렸다.
캐시로 만든 결과가 **기존 파일과 바이트 단위로 동일**한 것까지 확인했다.

- **패치 직후에는 `--refresh` 를 붙일 것.** 안 그러면 최대 7일 묵은 bin 을 본다
- 캐시 적중이면 챔피언 사이 대기시간(120~150ms)도 건너뛴다 — 그게 20~26초였다
- 받기에 실패하면 오래된 캐시라도 쓴다 (CD 가 잠깐 죽어도 작업이 안 멈춤)
- `.cache/` 는 gitignore 대상. 지워도 다시 받으면 그만이다

`fill_values.js` 안의 표 두 개가 중요해:

- **`MANUAL`** — `"미스 포츈 W / LoveTapRefund"` 형식 키. 손으로 확인한 값을 적으면 자동 추출보다 우선하고 재실행해도 안 날아감. 영문 alias도 허용. 안 쓰인 키는 오타 경고로 잡힘. 현재 9건
- **`PRESERVE`** — **2026-08-08에 비웠다(`[]`).** 가렌·갈리오가 유일한 항목이었는데 `{pN}` 체계 이전의 옛 형식(pN이 아예 없음)이라 구조가 달랐어. 이제 전부 CD에서 받아옴
- **`extractVV()`** — 손으로 쓴 `v1`/`v2`를 기존 파일에서 **원문 그대로** 물려줘. 여러 줄짜리(`drawGraph` 호출 등)도 줄 단위로 옮기니까 안 깨져. **PRESERVE 없이도 손작업이 안 날아가는 이유가 이거야.** 지금은 `v1`/`v2`가 전부 비어 있어서 아무것도 안 물려주지만, 앞으로 작성할 때를 위해 남겨둔 장치야

`--write`는 `public/custom_values.new.js`를 만들어. 확인한 뒤에 `custom_values.js`로 이름을 바꿔야 반영돼.

### 폴백 동작

`app.js`의 `renderScalingTable`에 ? 가드가 있어. p 값이 하나라도 `""`나 `"?"`면 **문장을 통째로 버리고** Data Dragon 툴팁으로 폴백해.

문제는 DD 툴팁도 깨져 있는 경우가 있다는 거야. 말파이트 W가 그 예였어. DD 한국어 툴팁에 `[스탯 비례]`랑 `%i:OnHit%` 같은 미해결 토큰이 그대로 들어 있어서 폴백해도 흉하게 나왔어. (2026-08-08에 MANUAL로 채워서 지금은 폴백 안 함) 그래서 **폴백에 기대지 말고 MANUAL로 채우는 게 근본 해결**이야.

### 알아낸 규칙 (다시 알아내지 말 것)

- `mStat` 필드 생략 = 주문력 (오공 E로 검증)
- `mStat` 값: 1 = 방어력, 2 = 공격력, 4 = 공격 속도, **6 = 마법 저항력**, **8 = 치명타 확률**, **9 = 치명타 피해량**, 12 = 최대 체력, 18 = 생명력 흡수, 29 = 물리 관통력
- **9는 이동 속도가 아니다.** 야스오 패시브 위키("critical strikes deal only (180% + 27%) AD")와 탈론 Q 인게임 설명으로 확정. 케이틀린 P·루시안 R·미스 포츈 Q·샤코 Q의 `치명타 확률 x (X - 1)` 형태는 X가 치명타 피해량일 때만 성립함 (2026-08-08 정정)
- **6과 8은 원래 반대로 적혀 있었고 2026-08-08에 바로잡았다.** 닐라 R이 실제로 치명타 확률, 뽀삐 W가 마법 저항력이었음. 정확히 맞교환이라 21자리가 틀린 이름으로 나가고 있었다. bin 필드 이름(`CritArmorPen`, `BonusMR`)과 루시안 R 발사 횟수(치명타 비례)로도 교차 확인됨
- **`STAT_NAMES`에 이름이 적혀 있으면 스크립트가 "안다"고 판단해서 의심 목록에 안 올린다.** 그래서 표에 잘못 적힌 값은 미리보기 출력에 한 줄도 안 뜬다. 표에 있는 값도 근거가 없으면 추정으로 취급할 것
- `mStatFormula`가 총/기본/추가를 가름: 없음·0 = 총, 1 = 기본, 2 = 추가
- `DataValues` 배열은 0번이 쓰레기라 **1번부터** 읽어야 함
- `castRange`·`mana` 등 최상위 필드는 **0번부터**가 맞음
- `AmmoRechargeTime` 계열도 0번이 "스킬 안 찍은 상태"라 1번부터. **전수 검증 끝** (티모 R, 케이틀린 W, 바이 E, 벨코즈 W, 자이라 W, 카시오페아 E, 아지르 W 전부 통과)
- **`DOT_AS_PRECISION = true`(`.숫자`를 소수점 자릿수로 보는 해석)가 맞다고 확정.** 럼블 W·크산테 Q·크산테 W·하이머딩거 E로 확인. 배열 인덱스 해석은 폐기 — 다시 뒤집지 말 것
- **롤위키(`wiki.leagueoflegends.com/en-us/<영문명>`)를 WebFetch로 읽을 수 있다.** 수치 검산에
  쓸 것. **단 `(based on level)` 수치는 위키 기본 표시가 20레벨이다** — 야스오 방어막
  `674.46`, 우디르 각성 공속 `75.88%`가 그 예. 우리는 18레벨 기준이라 역산해야 맞다.
  랭크별 값(`5 / 25 / 45`)은 그대로 비교 가능. **"아이콘 몇 개냐"처럼 물으면 답이 들쭉날쭉하고,
  "하위 스킬 이름을 슬롯별로 나열"처럼 게임 규칙을 물으면 일관되게 답한다**
- **위키 20레벨 값 → 18레벨 역산은 `(끝값 - 시작값) / 19`가 레벨당 증가분이다.** 18로
  나누면 틀린다. 2026-08-09에 세 번 걸렸고 세 번 다 이 식으로 맞았다 — 자헨 P
  `2.95%`(20렙) → `2.797` ≒ 우리 `2.8`, 타릭 P `25~101` → `25 + 4x17 = 93`,
  우디르 Q 번개 `3.18%` → `3.003%` ≒ `3%`
- **곱셈 조각의 배율은 "맨 숫자이면서 1 미만"인 피연산자에만 싣는다.** 일괄 적용도 일괄
  미적용도 틀리다 — 카타리나 R은 `0.16 → 16`이 맞고(위키 "16% bonus AD"), 다리우스 Q는
  `100~140`을 그대로 둬야 한다(위키 "+100~140% AD"). 1 미만이면 아직 분수, 1 이상이면
  이미 퍼센트 단위다. 2026-08-09 위키 3자리 대조로 확정
- **`(중첩당)` 자리도 같은 함정이다. 일괄로 `x100` 하지 말 것.** `BuffCounterByNamedDataValue`
  조각이 내는 값인데 32자리를 훑어보니 세 종류가 섞여 있다 — 분수(스몰더 `0.25` = 중첩의
  25%), 절대값(드레이븐 P 골드 `1`, 스웨인 P 체력 `15`), 이미 % 단위(`2.5% (+ 0.5 (중첩당))`).
  위키로 확인한 자리만 손댈 것
- **범용 이름(`f1`·`Effect2Amount` 등)의 "본체에서만 찾기" 제한을 풀지 말 것.**
  2026-08-09에 "계열 등급까지" 로 완화하는 걸 시험했다. **2자리가 새로 채워지고 기존
  값은 하나도 안 바뀌어서 완벽히 안전해 보였는데, 그 2자리 중 신 짜오 E 가 틀렸다**
  (`30/35/40/45/50` 을 물어왔지만 실제는 `38/46/54/62/70%`). 계열 등급도 못 믿는다.
  **"기존 값이 안 바뀌니 안전하다" 는 판단 자체가 함정이다** — 새로 채워지는 자리가
  맞는지는 별개 문제다. 규칙을 건드리면 새로 채워지는 자리를 전부 검산할 것
- **레벨 보간 조각은 양 끝이 같으면 범위 표기를 붙이면 안 된다.** `6 ~ 6% (레벨에 따라)`처럼
  나가면 안 크는 값이 크는 것처럼 읽힌다. `fill_values.js`의 `levelRange()` 헬퍼가 처리하고
  보간 조각 5군데가 전부 여기를 거친다. **새 보간 분기를 추가하면 이 헬퍼를 쓸 것**
  (`mFloor ~ mCeiling`은 clamp라 대상이 아니다)
- **CD가 못 푼 `{8자리16진수}` 이름은 원래 이름의 FNV-1a 32비트 해시(소문자 기준)다.**
  툴팁 이름을 해싱해서 찾아가면 됨. `fnv1a('firsttierrangeincreasett') = d34fc902`이고
  킨드레드 패시브 계산식 키가 정확히 `{d34fc902}`. `findCalc`·`findDataValue`에 폴백으로 들어감
- **해시 역추적으로 조각 안 필드 이름 6개를 되찾았다.** 전부 `ByCharLevelBreakpoints`의
  **"값 대신 DataValue 이름을 적는" 변종**이야:
  `{91d404a5}`=`Level1DataValue`, `{bbd778a2}`=`InitialBonusPerLevelDataValue`,
  `{9823b29a}`=`DataValueBreakpoints`, `{ae9b464d}`=`AdditionalBonusAtThisLevelDataValue`,
  `{b0d8b2ac}`=`BonusPerLevelAtAndAfterDataValue`, `{b2cd0eb0}`=`BonusPerLevelDataValue`.
  **여기도 "생략 = 0(성장 정지)" 규칙이 그대로 적용된다.** `fld()`가 해시·원래이름 둘 다 봄
- **문자열 필드 두 개짜리 조각을 무조건 "레벨1 ~ 레벨18"로 읽으면 안 된다.** 두 번째가
  `BonusPerLevelDataValue`(레벨당 증가분)인 변종이 있어서 이렐리아 P가 `10 ~ 3`이라는
  거꾸로 된 범위로 나가고 있었다 (실제 `10 + 3x17 = 61`). 이름을 보고 갈라야 함
- **`mStat 7 = 이동 속도`. 인게임 확인 완료(2026-08-08).** 헤카림 P 툴팁이 실제로
  "추가 이동 속도의 12~24%(레벨에 따라)". 표에 7이 아예 없어서 잔나 P·헤카림 P가
  통째로 죽고 있었다
- **최상위 필드가 `{ values: [...] }`로 한 겹 싸여 있는 경우가 있다.** 직스 P의 `Cooldown`이
  그 모양이라 객체인 채로 실패하고 있었음. `fieldToText`에서 벗겨냄.
  **직스 패시브 쿨 12초 고정은 인게임 확인 완료(2026-08-08)** — 배열이 전부 12인 게 맞다
- **`ByCharLevelFormulaCalculationPart`는 필드가 `mValues`가 아니라 `values`다.** 15자리 전수
  확인. 이걸 몰라서 이 분기는 오랫동안 `0 ~ 0`만 냈다 (2026-08-08 수정)
- **★ 그 배열은 31칸 = 레벨 0~30 이라 "인덱스 = 레벨"이다. 레벨1 = `vals[1]`, 레벨18 = `vals[18]`.**
  2026-08-08에 "31칸 = 레벨 1~31" 로 읽고 `vals[0]`/`vals[17]`을 쓴 게 **2026-08-11에 위키
  대조로 뒤집혔다.** 노틸러스 P 캐시가 `idx0=8 idx1=14 idx17=110 idx18=116 idx30=188`이고
  양 위키가 말하는 값이 `14 ~ 116`이다. **110은 17레벨 값이었다.**
  이 분기가 만드는 **11자리가 전부 한 칸씩 낮게** 나가고 있었다 —
  노틸러스P 럭스P 레오나P 브라움P(2) 카직스P 그레이브즈P 카타리나P 렝가W 세트P(2) 요네W.
  **그레이브즈·카타리나·렝가의 `0 ~ ` 시작값도 별개 버그가 아니라 전부 이것이었다**
  (레벨 0 값이 0이라서). 위키로 확인 가능한 8자리가 수정 후 값과 정확히 일치했다
- **★ `partToText`의 Stat 조각 두 개(`StatByNamedDataValue`·`StatByCoefficient`)는 `mult`를
  받아야 한다.** 2026-08-11 전까지 `100`만 곱하고 `mult`를 통째로 버려서, 기본값엔 배율이
  걸리고 계수엔 안 걸렸다. 증상 두 가지가 같은 뿌리였다:
  1. `mMultiplier` 있는 계산식 — 아칼리 E(x0.3), 룰루 Q(x0.5), 릴리아 W(x3), 마스터 이 Q(x0.25)
  2. `mDisplayAsPercent: true` 인 체력 % 피해 — `주문력의 0.03%`처럼 **100배 작게** 나감
     (렐 E, 판테온 W, 트런들 R, 카이사 P, 세트 Q·W, 우디르, 크산테 W 등 20여 자리)
  **단 `FRACTION_STATS = {4 공속, 8 치명타 확률, 9 치명타 피해량, 18 생흡}`은 빼야 한다.**
  이 넷은 값 자체가 소수(0.5 = 50%)라 배율을 또 먹으면 100배로 튄다
  (진 P `치명타 확률의 35%` → `3500%`). 그래서 바루스 P `추가 공격 속도의 1100%`,
  비에고 P `500%`는 **오류가 아니다** — 위키의 `11% / 5% per 100% bonus AS`와 같은 값이다
- **`mModifiedGameCalculation`의 배율은 안쪽 계산식에 실어 내려보낼 것.** `${inner} x ${m}`으로
  붙이면 화면에 계산식이 그대로 찍히고("30 / 50 / 70 (+ 주문력의 10%) x 3") 배율이 기본값에만
  걸린 것처럼 읽힌다. 내려보내면 그웬 R이 `90 / 150 / 210 (+ 주문력의 30%)`로 lolwiki와 맞는다.
  **랭크별 배열처럼 숫자 하나로 못 접는 배율은 예전처럼 `x`로 남긴다**(모데카이저 Q).
  이걸로 곱셈 표시가 145자리 → 29자리가 됐다 (2026-08-11).
  이어서 `foldProduct`(순수 숫자끼리 곱해서 접기, `x 1` 제거)와
  `foldRankMultiplier`(랭크별 배율을 랭크 i 는 배율 k_i 로 돌린 결과의 **i번째 칸**만 뽑는 대각선
  방식으로 접기)를 넣어 **최종 9자리**까지 줄였다. 남은 9자리는 치명타 피해량이 변수로 들어가거나
  배율 자체가 스탯이라 숫자로 접을 수 없다
- **★ 위키가 우리보다 틀린 경우가 생각보다 많다. bin 을 이겼다고 섣불리 고치지 말 것.**
  2026-08-11 인게임 확인 6건 중 **4건이 우리가 맞고 위키가 틀렸다** —
  나서스 E(`50 (+주문력 60%)`, 양 위키 `100 (+120%)`), 헤카림 W(`80 (+80%)`, lolwiki `100`),
  샤코 P(`35`, lolwiki `31.18`), 카타리나 E(`총 공격력`, lolwiki `bonus AD`).
  **bin 은 게임 원본이라, 우리 코드가 bin 을 제대로 읽었으면 우리 값이 곧 게임 값이다.**
  진짜 버그는 "코드가 bin 을 잘못 읽은 자리" 뿐이다
- **누누 W 최소 피해는 인게임 툴팁조차 틀리다.** 툴팁은 60(= bin 의 `NoImpactDamageScalar` 0.333)인데
  실제로 때려보면 36 이다. 최소 = 최대의 20% 가 맞다. bin 에 0.2 가 없어서 MANUAL 로 넣었다.
  **"안 맞았을 때(NoImpact)" 값과 "최소 충전" 값은 다른 것**이라는 걸 놓치기 쉽다 (2026-08-11)
- **`@spell.<스펠이름>:<이름>@`은 다른 스펠의 값을 부르는 교차 참조다.** 대소문자가 섞여 있고
  (`spell.`/`Spell.`) 자기 자신을 부르기도 함(소나 P). `buildSpellIndex()`가 스펠 객체
  이름 → `{ spell, maxRank }` 색인을 만든다. **`maxRank`를 같이 들고 다녀야 한다** — 패시브가
  Q 값을 부를 때 P의 1로 읽으면 1랭크만 나오고, R 값을 5로 읽으면 없는 4·5랭크가 지어내진다.
  **플레이스홀더 정규식 3군데(`build_champion_data.js` 1 + `fill_values.js` 2)에 `:`가
  들어가야 하고 셋이 항상 같아야 한다.** 어긋나면 문장 `{pN}`과 값 `pN` 개수가 안 맞는다
- **CD가 스펠 *경로* 이름도 `{해시}`로 남길 때가 있다.** 스몰더 패시브가 `{c72a53d8}`라
  `SmolderP`라는 키가 아예 없었음. 그래서 색인에 `<Alias>P`·`<Alias>Passive`·`<Alias>Q..`
  관례 이름으로도 등록해 둠
- **`{{Spell_XXX_Name}}`은 stringtable에서 소문자 키로 푼다.** 패시브 툴팁을 stringtable에서
  가져오면서 딸려 들어온 문법. `@Placeholder@`와 달리 `{p}` 자리가 아니라서 **못 풀어도
  폴백이 안 걸리고 화면에 그대로 찍힌다.** `build_champion_data.js`의 `convertDescription`에서
  처리. 키 안에 `{pN}`이 또 들어간 중첩형 13개는 못 푼다 (아펠리오스 무기 이름 등, 아래 참고)
- **`mScaleByStatProgressionMultiplier`는 무시해도 된다.** 이 플래그는 중간 레벨의 곡선
  모양만 바꾸고 양 끝값은 안 바꿈. 성장곡선 `g(N)=(N-1)(0.7025+0.0175(N-1))`이
  `g(18)=17`이라 18레벨 배율이 정확히 1.0이 됨. 사이트는 양 끝만 찍으니 표시가 안 달라짐.
  올라프 P(100%)·야스오 P(600) 인게임 확인 완료. 23자리 해당
- **★ stringtable 에는 현재판·구버전·클래식 모드용이 전부 섞여 있다. 키 이름으로 고르지 말 것.**
  2026-08-09에 "stringtable 에 더 상세한 문장이 있네?" 하고 `spell_ahriq_tooltip` 을
  꺼내 우리 문장과 비교했다가 **"우리 데이터에 정보가 누락됐다" 는 틀린 결론**을 냈다.
  그건 옛날 아리(정기 흡수 = 스킬 적중 중첩) 설명이었고, 현재 아리는
  `generatedtip_passive_ahripassive_tooltip`(정기 조각 = 미니언 처치)이며 **우리가 맞았다.**
  **`jade_` 접두사만 거르면 되는 것도 아니다** — `spell_ahriq_tooltip` 은 접두사가 없는데도
  구버전이었다. 그래서 기존 규칙(bin 의 `keyTooltip` 에서 읽기)이 존재하는 것이다.
  stringtable 을 직접 뒤져 문장을 가져오려 하지 말 것
- **`<font color=...>` 는 라이엇이 원문에 박아 둔 색이고 인게임과 정확히 일치한다.**
  바드 P "고대의 종" `#cccc00`, "정령" `#ff9900` 을 스크린샷 픽셀과 대조해 확인.
  **덮어쓰지 말 것.** 예전엔 `app.js` 에서 `!important` 로 보라를 강제해 44곳이 죽어 있었다.
  **원문 문장에 색이 하나만 보여도 나머지가 없다는 뜻이 아니다** — 아펠리오스 무기색이
  W 문장엔 중력포 하나만 있었지만 실제로는 `apheliosgun_lorename_1~5` 값 안에 5개가
  전부 들어 있었다 (`{{ ApheliosGun_LoreName_@f1@ }}` 가 런타임에 끼워 넣는 구조).
  추측으로 채우지 말고 stringtable 에서 **값**을 찾을 것 (문장이 아니라 값은 안전하다)
- **한국어 스킬명·무기명이 필요하면 나무위키(`namu.wiki/w/<한글명>`)를 먼저 볼 것.**
  WebFetch 로 읽힌다. 롤위키는 영문명만 주므로 한국어 표기를 추측하게 되는데,
  2026-08-09 아펠리오스 무기 Q 이름 5개 중 4개를 틀리게 추측했다
  (월광 사격→**달빛탄**, 구속의 월식→**월식**, 황혼의 물결→**황혼파**, 파수병→**파수탑**).
  수치는 두 위키가 일치했으니 **한국어 이름은 나무위키, 수치는 롤위키**로 나눠 쓰면 된다
- **`custom_templates.js` 의 `"X": null` 은 "이 챔피언에겐 이 키가 아예 없다"는 뜻.**
  `app.js` 가 스킬 목록에서 통째로 뺀다. 아펠리오스 E 가 유일한 사례다
  (무기 교체가 탄약 소진으로 자동 진행돼 E 키를 안 쓰는데 Data Dragon 엔 더미가 있다).
  **`""` 는 "아직 문장을 안 썼다"(= DD 폴백) 라서 뜻이 다르다.** 헷갈리면 스킬 칸이 사라진다
- **롤위키 "based on level" 수치는 몇 레벨 기준인지 먼저 볼 것.** 기본 표시가 18레벨이
  아닐 수 있다. 야스오 P 방어막 `674.46`이 **20레벨**(우르프 등) 값이었고, 커서를 올리면
  18레벨은 `600`으로 뜬다. 사이트는 18레벨 기준이므로 bin 끝값이 맞다
- bin의 `cooldownTime`은 한 칸 밀린 듯해서 CD v1의 `cooldownCoefficients`를 씀
- 사거리는 내부값(럭스 Q가 10000으로 나옴) 대신 `castRangeDisplayOverride`를 씀
- **라이엇이 툴팁 철자랑 bin 철자를 따로 관리해서 대소문자가 자주 어긋남.** 22종 발견 (`HoTDuration`→`HotDuration`, `EnergyRefund`→`energyrefund` 등). 계산식 안쪽 참조에서도 같은 일이 생기고, 조각 하나가 null이면 계산식 전체가 죽음
- **"스킬 쿨타임으로 추론" 휴리스틱은 신뢰도가 낮음.** 4건 중 2건이 오답이었어. 필드 이름이 쿨타임 자체가 아니라 환급·감소·제한 시간을 뜻하면 틀려. 이 목록은 매번 전수 검수할 것

### 현재 상태 (2026-08-09 기준)

- **3211/3228 (99%) 채워짐.** 빈칸 총수가 계속 느는 건 새 자리가 생겨서다
  (패시브 173개 → 가렌·갈리오 → 교차 참조 116자리 → 두 번째 폼 17스킬)
- **★ DD 폴백 0개.** 전 챔피언이 우리 문장으로 나간다
  (2026-08-09 달성이라고 적었는데 **실제로는 피들스틱 1명이 폴백이었다**.
   2026-08-10에 원인을 잡고 진짜 0개가 됐다 — 아래 항목 참고)
- **★ `app.js`는 Data Dragon의 `champ.id`로 `customValues`/`customTemplates`를 찾는다.
  그래서 파일의 챔피언 키는 반드시 DD 철자여야 한다.** CD(CommunityDragon) 철자로
  나가면 조회가 통째로 실패해서 **문장·수치·아이콘을 다 버리고 DD 툴팁으로 폴백**한다.
  233명 중 **피들스틱 하나만** 철자가 다르다 — DD `Fiddlesticks` / CD `FiddleSticks`.
  `fill_values.js`·`build_champion_data.js` **양쪽의 `DD_ID` 표**로 출력 키만 바꾼다
  (bin 경로·스펠 조회는 CD 철자를 그대로 써야 하므로 `alias`는 건드리면 안 된다).
  **`MAX_RANK`처럼 두 파일에 같은 표가 있어야 하고, 어긋나면 템플릿과 값의 키가 갈린다.**
  **"폴백 0개"는 스크립트 출력만 보고 적은 것이었고, 화면을 직접 열어본 게 아니었다.**
  피들스틱을 열어보니 `[스탯 비례]`가 그대로 찍혀 있었다. 새 챔피언이 나오면
  DD/CD 철자를 대조하고, **"다 됐다"고 적기 전에 브라우저로 한 명은 열어볼 것**
- **`검산목록.md`(루트, gitignore됨) A 섹션 완료.** 19자리 중 15자리가 맞았고 4자리를
  고쳤다. **다음은 B·C 섹션 15자리**
- **못 채운 17개는 전부 문장에서 뺀 자리라 화면에 영향이 없다.**
  (카이사 진화 게이지·스몰더 현재 중첩·바드 현재 성소 수·아펠리오스 무기 순서 등
   "지금 내 챔피언의 현재 수치" 라 고정값이 없는 자리들)
- 문장 `{pN}` ↔ 값 `pN` 정합성 865자리 전부 일치 (2026-08-08 검사)
- **`f` 계열이 안 채워지는 원인 3종은 bin 조사로 확인해 뒀다** (추측 아님):
  1. **`mEffectAmount` 슬롯 자체가 없거나 `value` 가 빔** — 바드 P, 아우렐리온 솔 P,
     퀸 P 가 이 경우였다. bin 밖(공용 스펠·버프 객체)에 있다는 뜻이라 **MANUAL 밖에 답이 없다**
  2. **`f` 번호가 배열 범위를 넘음** — 자야 P 가 10칸짜리 배열에 `f12`·`f14`·`f16` 을 불렀다.
     **`f번호 = mEffectAmount 인덱스` 전제가 깨지는 자리가 있다.** 값은 배열에 다 있었지만
     (`f12`→인덱스 0, `f14`→인덱스 5, `f16`→인덱스 7) **번호 규칙은 못 찾았다.**
     재사용 가능한 해법이 아니니 이런 자리는 그냥 MANUAL 로 넘길 것
  3. **이름만 다르고 본체에 값이 있음** — 가렌 E `f1` 이 본체 계산식 `NumberOfStrikes`
     (→ `NamedDataValue` `NumTicks`=7) 였다. **`f` 자리를 만나면 본체의 계산식·DataValue
     이름부터 훑어볼 것.** 제일 싸게 풀리는 경우다
- 인게임 확인 결과 원본은 **`확인결과.md`** (루트, gitignore됨). 항목별 근거가 다 적혀 있으니 작업 재개 전에 먼저 읽을 것

### 폴백을 푸는 두 번째 방법 — "문장에서 빼기" (2026-08-09)

빈칸이 **"지금 내 챔피언의 현재 수치"** 라서 고정값이 아예 없는 자리가 있다
(카이사 진화 게이지, 스몰더 현재 중첩, 바드 현재 성소 수 등).
**MANUAL 로 숫자를 넣으면 거짓말이 된다.** 문장에서 빼는 게 맞다.

**단, 문장에서 `{pN}` 을 지우는 것만으로는 폴백이 안 풀렸다.** 두 가지를 알아야 한다.

1. **값 쪽 `pN` 은 지워도 소용없다.** `fill_values.js` 는 문장이 아니라 **CD 원본 `desc` 의
   `@Name@` 을 훑어서** p1..pN 을 만든다. 손으로 지워도 재실행하면 되살아난다
2. **그래서 `app.js` 의 가드를 고쳤다.** 예전엔 `values` 의 **모든** `pN` 을 검사해서,
   문장이 안 쓰는 자리의 `?` 까지 세고 멀쩡한 문장을 통째로 버렸다.
   지금은 **문장에 실제로 등장하는 `{pN}` 만** 본다 (`tpl.includes('{'+k+'}')`).
   가드의 목적이 "화면에 물음표가 찍히는 걸 막는 것" 이니 이게 맞다

이 방식은 **템플릿만 고치면 되고 `fill_values.js` 재실행에도 안전하다.**
8스킬이 이렇게 풀렸다.

### 남은 일

1. 남은 8개 폴백 스킬 처리. **전부 MANUAL 대상이 아니다** — 성격이 갈린다
   - **숫자 + 문장 손질이 같이 필요**: 벨베스 E(공속 비례 공격 횟수), 세트 W(투지 계수), 신 짜오 E(주문력·영구 공속 항). 숫자만 넣으면 계수가 사라짐
   - **문장에서 빼는 게 맞는 자리**: 바드 W(현재 성소 수), 카이사 Q·E(진화 게이지 현재값), 아펠리오스 R(무기 이름 참조). **"지금 내 챔피언의 현재 수치"라 고정값이 없다.** MANUAL로 숫자를 넣으면 거짓말이 됨
2. `guessPart` 두 건 — 우디르 Q는 `%`·`x100`이 안 실려 `0.015 ~ 0.03`으로 나옴 (실제 `1.5 ~ 3%`). 카이사 E는 `ClampSubParts`의 `mFloor`/`mCeiling`을 무시해서 `+ 100`이라는 껍데기가 나옴 (실제는 `clamp(1 + 추가공속, 1, 2)`)
3. ~~우디르 6랭크~~ **해결.** `MAX_RANK` 표를 `fill_values.js`·`build_champion_data.js`
   **양쪽에** 뒀다(어긋나면 쿨타임 칸 수와 수치 칸 수가 안 맞음). 6종:
   우디르 QWER 6 / 제이스 QWE 6·R 1 / 니달리·카르마·엘리스 R 4 / 유미 Q 6·R 3.
   **자동 판별은 불가능** — bin `mSpell`에 최대 레벨 필드가 없고 CD의 계수 배열은
   챔피언과 무관하게 전부 길이 6이다. 새 챔피언이 나오면 표에 직접 추가할 것
4. **중첩형 `{{키_{pN}}}` 13개** — 키 안에 플레이스홀더가 또 들어가서 정적 조회로는 못 푼다.
   아펠리오스 P(무기 이름 5개)·R, 갱플랭크 Q, 오른 P, 아크샨 W, 하이머딩거 등.
   전부 "지금 내 챔피언의 현재 상태"를 가리켜 고정값이 없다. **문장에서 빼는 쪽이 맞는 자리**
5. ~~카이사 W의 `@spell.KaisaPassive:PDuration@`~~ **해결.** 전수 조사해보니 1건이 아니라
   **119자리 / 58스킬 / 37챔피언**이었고 전부 화면에 원문이 그대로 찍히고 있었다.
   115자리 해석 완료. 남은 22자리는 아펠리오스 P `Hotkey`(무기가 별도 캐릭터 bin이고
   값도 숫자가 아님)
6. 니달리 W 덫 최대 개수 — `4 / 6 / 8 / 10`인데 **챔피언 레벨 기준**(스킬 레벨 아님). 지금 `MaxTraps`를 0으로 버리는 중
7. ~~패시브 173개 자동화 경로 없음~~ **해결.** `stringtable.js` 가 인게임 진짜 툴팁을
   가져온다 (`lol.stringtable.json`, 35MB, `.cache/` 에 7일 캐시. `--refresh` 로 강제 재다운).
   찾아가는 길: bin 의 `mSpell.mClientData.mTooltipData.mLocKeys.keyTooltip` → **소문자로**
   `entries` 조회. 키 규칙이 3종이라 이름으로 추측하지 말고 반드시 bin 에서 읽을 것.
   173/173 확보, 163명 문장에 `{pN}` 들어감. 나머지 10명은 패시브에 수치가 없는 타입
8. ~~레벨별 성장 각주~~ **완료 (2026-08-11).** `public/custom_graphs.js` (`add_level_graphs.js` 생성)
   가 **129챔피언 / 170스킬 / 268자리**에 각주를 단다.

   **★ 각주는 `v1`/`v2` 가 아니라 문장 안 수치 바로 뒤에 붙는다.**
   `app.js` 의 `renderScalingTable` 이 `{pN}` 을 채울 때 `values[key] + graphs[key]` 로 이어 붙여서
   **색칠된 태그 안쪽 마지막 글자**에 달린다 (`<healing>...10.1% (레벨에 따라)[1]</healing>`).
   처음엔 v1/v2(구분선 아래 줄)에 넣었는데 **같은 수치가 두 번 보여서** 옮겼다.

   **왜 별도 파일인가**: 값(`custom_values.js`)도 문장(`custom_templates.js`)도 스크립트가 매번
   새로 찍는다. 거기 써 넣으면 재생성 때 날아간다. `custom_graphs.js` 는 아무도 안 건드린다.
   `index.html` 에서 **반드시 `custom_values.js` 뒤에** 로드해야 한다 —
   `drawGraph`/`drawSteps` 헬퍼가 거기 있고 이 파일은 로드 시점에 그걸 호출한다
   (`loadChampionData` 가 `async=false` 로 붙여서 문서 순서가 보장된다).

   두 종류로 갈린다:
   - **매 레벨 크는 값 → `drawGraph`** (202자리). 꺾은선 그래프
   - **특정 레벨에서만 바뀌는 값 → `drawSteps`** (66자리). "1 / 7 / 13레벨" 목록.
     나서스 P 생명력 흡수(12/18/24)처럼 `12 ~ 24 (레벨에 따라)` 라고만 적으면
     매 레벨 크는 것처럼 읽히는 자리다. 가르는 기준은 **18레벨 중 값이 바뀌는 횟수 5 이하**
     (bin 전수로 보면 17회(매 레벨)가 177자리로 몰려 있고 계단식은 2~5회라 사이가 비어 있다)

   곡선 원본은 `level_curves.json` (`build_level_curves.js` 가 생성).

   - **`custom_values.js` 를 스크립트로 파싱할 땐 줄을 `/\r?\n/` 로 자를 것.** 이 파일은 CRLF 인데
     **JS 정규식의 `.` 은 `\r` 을 안 먹는다**(line terminator 취급). `// 주석.*$` 같은 패턴이
     통째로 안 맞아서 709줄을 놓쳤었다. 하루에 두 번 걸린 함정이다
   - 각주 대상은 "값에 `(레벨에 따라)` 가 있고 **그 `{pN}` 이 문장에 실제로 등장**하는" 자리만.
     안 보이는 값에 각주를 달 이유가 없다
   - **점 위에 정확히 올려야만 수치가 뜨던 문제**는 `drawGraph` 가 각 점 **바로 앞에**
     그 점이 담당하는 x 구간을 덮는 투명 `<rect class="graph-hit">` 를 깔아서 해결했다.
     CSS 인접 선택자 `.graph-hit:hover + .graph-point` 로 켠다 —
     **히트존이 `<g>` 바로 앞에 있어야 `+` 가 성립한다.** 순서를 바꾸면 조용히 안 먹는다
   - **★ `.custom-footnote-content` 는 `position: fixed` 고 좌표를 `app.js` 의
     `positionFootnote()` 가 hover 때 찍는다.** CSS 혼자로는 위치가 안 잡히니 둘을 같이 볼 것.
     `absolute` 였을 땐 `.champ-skill-body` 의 `overflow-y: auto` 에 **가로까지 잘렸다**
     (한 축이 visible 이 아니면 다른 축도 auto 로 계산된다). 화면 좌우 끝 각주가 잘려 보인 원인.
     **조상에 `transform`/`filter`/`contain` 이 생기면 fixed 가 그걸 기준으로 잡혀 다시 잘린다** —
     스킬 영역 조상에는 지금 없는 걸 확인해 뒀다
   - **`.champ-skill-body` 에 `scrollbar-gutter: stable` 이 필요하다.** 스킬 데이터가 늦게 로드되며
     스크롤바가 생기면 본문 폭이 15px쯤 줄어 **설명이 다시 줄바꿈되며 덜컹였다**

9. ~~`v1`·`v2` 직접 작성 (옛 항목)~~ **과제에서 뺐다 (2026-08-09).** 수치가 이미 문장 안에
   전부 들어가 있어서(3211/3228) 채워봐야 같은 숫자를 두 번 보여줄 뿐이다.
   709자리를 손으로 쓰는 비용에 비해 얻는 게 없다. **비어 있으면 구분선까지
   통째로 숨겨지므로 지금 화면에 아무 영향이 없다.**
   `custom_values.js` 맨 위의 `drawGraph()` 헬퍼도 아직 한 번도 안 쓰였다.
   구분선·그래프는 별도 계획이 있으니 그때 이 칸을 쓰면 된다
9. ~~스킬 설명 색 일괄 적용~~ **완료 (2026-08-09).** 43종 전부 인게임 스크린샷에서
   픽셀을 뽑아 실측했다. `app.js` 의 `<style>` 블록에 있고, 값 근거는 그 주석 참고
10. `STAT_NAMES` 나머지 추정값 확인 — 5(스킬 가속), 11(추가 공격력), 13(추가 체력).
    **1·6·8·9·12는 확인 끝** (9는 이동 속도가 아니라 **치명타 피해량**. 2026-08-08 정정)
11. 파일 정리 — `public/custom_templates.bak.js`, `public/custom_values.bak.js`(둘 다 배포되면 웹에서 받아짐), `fill_values.old.js`, `custom_values.js.bak-0808`, `public/custom_values.js.bak-*`, `로고였던것.png`, 루트 `lulubackground.webp`. `database.sqlite`는 `server.js`가 참조하는지 확인하고 판단
