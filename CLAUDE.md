# pixlol.kr

롤 전적검색 사이트. 한국어로 대화해줘. 평서형 "~다" 말투 말고 "~야/~어" 쪽으로.

## ★ 갈라 둔 문서 (`docs/`)

**이 파일이 한도를 넘을 때마다 큰 절을 밖으로 뺐다 (2026-08-19 둘 · 08-21 둘 · 09-04 다섯 + 부록 둘). 내용은 안 지웠다 — 전부 이동이다.**
아래 갈래는 여기 요약만 있으니, 그 일을 할 거면 **파일을 먼저 열어 읽을 것.**

| 파일 | 무엇 | 언제 읽나 |
|---|---|---|
| `docs/스킬데이터.md` | 스킬 문장·수치, 빌드 스크립트, 패치 파이프라인, 스탯/레벨 곡선, 남은 일 표 + **부록: 8/23~8/24 전수 정비 세션·16.17 패치 교훈** | **패치가 왔을 때** · 스킬 툴팁·스탯 탭 작업 |
| `docs/통계집계.md` | 통계 수집·집계 3단 잡, 컬렉션 6개, 슬림 문서 28칸, 티어 공식, 룬 빌드, 패치 원본 박제 + **부록: 통계 화면 기능(패치 영향·조합·픽률 추이·LP·채택률·versus·홈 위젯·상세 페이지)·수집 점검·MongoDB 용량 상세** | 통계 탭·집계·`server.js` 잡 · DB 용량 |
| `docs/신화상점.md` | 수집기 규약, 서버 검증·409·인덱스, 구획 4개, 카드 그림 규칙, Cloudflare UA 함정 | 신화상점 탭·수집기 연동 |
| `docs/챔피언탭.md` | 스킨·크로마·가격(롤위키), 배경 이야기(Universe), 대사 탭(비공개) | 챔피언 페이지의 스킬·스탯 **말고** 나머지 |
| `docs/도감.md` | 도감 아이템·룬·주문 화면 전체 — 거르는 버튼(등급·유형·스탯), 룬 트리·레벨 각주, 조합식 트리, 주문 실제 수치·채택률, 검색 규칙 | 도감 탭 작업 |
| `docs/랭킹.md` | 랭킹 탭 화면·검색·칸 폭, 명단 갱신 격자, 컷라인(`rankcutoffs`)·`apexdrifts`·재계산 실측 | 랭킹 탭·컷라인 잡 |
| `docs/전적페이지.md` | 경기 링크 주소, 상세 전적 표 폭, 요약 통계 박스, 아레나 밴 | 전적 페이지 작업 |
| `docs/공통UI.md` | dogu 공통 파일 규칙(원본 위치·sync·md5), 헤더 `DOGU_NAV`·하위 메뉴, 패치노트 수집·PBE 백필 | 헤더·푸터·공통 파일·패치노트 |
| `docs/세션기록.md` | 2026-08-19 ~ 08-31 세션 마무리 원문 (각 세션의 "걸린 함정" 상세 포함) | 그 무렵 결정의 근거·함정의 전말을 찾을 때 |

**★ 2026-09-04 정리로 옮긴 절들의 "위/아래 절" 참조는 옮기기 전 CLAUDE.md 기준이다** — 그 파일 안에 없는 절은 다른 docs 파일이나 여기에 있다. 못 찾으면 `docs/` 를 grep 할 것.

## 스택 / 배포

- Node/Express + MongoDB Atlas(무료 512MB) + Railway + Cloudflare
- 프론트는 바닐라 JS. `public/` 안에 `app.js` / `index.html` / `style.css`
- `server.js`는 `public/` 밖, 프로젝트 루트에 있음
- GitHub `cosmicday/pixlol.kr` — push하면 Railway가 자동배포
- 로컬은 `node server.js` → localhost:3000

## 프론트 함정

- **`app.js`의 `ddragonVersion`을 서버 응답으로 덮어쓰지 말 것.** 전적검색 응답의
  `version`은 서버 부팅 시점 값이라 클라이언트가 이미 맞춰 둔 최신보다 오래될 수 있고
  (`server.js:70` 기본값 `16.1.1`), 그러면 **챔피언 탭이 구버전 `champion.json`을 읽어
  신규 챔피언이 목록에서 통째로 사라진다.** 로크가 이 경우였다. `compareDdragonVersion`으로
  더 새 버전일 때만 받아들인다. **문자열 비교는 안 된다** — `16.5.1 > 16.15.1`이 돼버림

## 절대 건드리지 말 것

- **`riot.txt`** — 라이엇 도메인 소유 확인 파일. 프로덕션 키 심사 중이라 없으면 문제가 생김
- **`.env`** — API 키. git에 올라가면 안 됨
- 라이엇 프로덕션 키가 Pending Review 상태(2026-03-10 신청).
  **라이엇 개발자 포털의 앱 등록 정보에서 `EDIT` 를 누르지 말 것** — 누르면 심사
  대기열 뒤로 밀린다. **코드 수정이나 배포와는 무관하다.** git push 는 마음껏 해도 된다
  (2026-08-09 확인. 그전엔 "앱을 크게 수정하면" 이라고만 적혀 있어서 코드 변경 얘기로
   잘못 읽혔다)

## 작업 규칙

- **★★ 설명은 전부 맨 아래에 한 번에 적을 것 (2026-08-15).** 도구 호출(파일 수정·명령 실행)을
  먼저 쭉 하고, **내가 읽어야 하는 말은 마지막에 몰아서** 쓴다.
  설명 조금 → 코드 수정 우수수 → 다시 "위에서 설명한 대로…" 식으로 번갈아 나오면
  **읽는 사람이 위아래로 오가야 해서 보기가 너무 힘들다.**
  중간에 꼭 필요한 한 줄(예: "이건 확인이 필요해서 먼저 잽니다") 말고는 아래로 내릴 것
- **파일을 고치기 전에 먼저 읽고 설명해줘.** 내가 납득한 뒤에 수정하는 순서로.
  (위 규칙과 충돌하지 않는다 — **무엇을 왜 고칠지는 미리**, **작업 결과 보고는 맨 아래**다)
- 문제를 알려줄 때는 "문제되는 코드 → 왜 문제인지 → 해결법" 순서로. 코드부터 던지지 말고.
- `git add .`는 2026-08-08에 `.gitignore`를 손봐서 이제 안전해졌어. 루트 `*.md`(CLAUDE.md 제외)·`*.txt`·`*.PNG`·`*.bak-*`가 다 막혀 있음. 그래도 커밋 전에 `git status`로 한 번 볼 것. `public/` 안은 안 막히니 백업 파일을 거기 두면 웹에서 받아짐
- **인게임 확인이 필요하면 체크리스트 파일을 만들어서 넘길 것.** 스킬 설명을 통째로 적게 하지 말고 문장 어느 자리인지 찍어주면 확인이 훨씬 빠름. 확인 항목은 "몇 자리에 영향 있는지" 순으로 정렬할 것 — 스탯 이름 하나가 수십 자리를 좌우함
- `fill_values.js --write` 돌리기 전에 `public/custom_values.js`를 백업해줘.
- **인게임 툴팁 확인은 내가 해.** 수치가 맞는지 추측하지 말고, 확인이 필요하면 나한테 물어봐.

## ★★★ 2026-09-12 저녁 — **16.17 마무리 완료.** 다시 훑기 601판이 통계에 들어간 뒤 박제·삭제했다

**하루 종일 여유 5MB 로 버티다 끝냈다. 클러스터 507.8 → 172.0MB (여유 340MB).** 커밋 `e2dc7c0`.

| 한 일 | 결과 |
|---|---|
| 아침 재부팅(11:46, RuntimeBroker)으로 죽은 실행기 둘 되살림 | 다시 훑기 3,493 → 이어서 |
| **09-08 다시 훑기 11,332명 완주** | 실패 0 · 관측 19,539건 · `cnt2` 5,724건 |
| `cnt2` → `cnt` 이동 (아래 mongoose 함정) | **601판이 대기열로** |
| 601판 수집 | 16.17 원본 56,296 → **56,888판** |
| **마무리** (`finalize_patch.js`) | 원본 −293MB · 집계 행 −90MB · 박제 `p_16.17.js` 8.5MB(gzip 1.9) |

**검증된 두 줄** — 이게 오늘의 목적이었다:
```
마무리 시작 — p:16.17 집계 games 56888 / 원본 56888   ← 601판이 전부 들어간 뒤 삭제
대조 통과 (행 수 · games · wins 합계 일치)            ← 박제 파일이 DB 와 완전 일치
```

### ★★★ 이번에 걸린 함정 넷 (다음에도 걸린다)

1. **★★★ mongoose 9 는 업데이트에 배열(집계 파이프라인)을 넘길 때 `{ updatePipeline: true }` 를 요구한다.**
   8 까지는 그냥 됐다. 안 주면 `Cannot pass an array to query updates unless the 'updatePipeline' option is set.`
   가 **던져져서 `startJobs` 가 통째로 죽는다.** 다시 훑기가 11,332명을 다 돌고 **마지막 `cnt2` → `cnt` 한 줄에서** 터졌다
   (훑은 결과가 적용이 안 돼 601판이 대기열에 안 들어갔다). `server.js` 에서 파이프라인 업데이트를 쓰는 자리는 이 한 곳뿐이다
2. **★★★ 용량을 매시 50~05분에 재지 말 것 — 집계가 도는 중이라 최대 60MB 파여 보인다.**
   478.8MB 를 읽고 "19MB 가 줄었다" 고 보고했는데 **champbuilds 세대 교체(25만 줄 삭제 → 재작성) 한가운데**였다.
   같은 순간에 `db.stats()` 와 `collStats` 합이 19MB 어긋난 게 단서였다 (둘은 정상이면 소수점까지 같다).
   **잰 값이 의심되면 `statscopes` 의 `gen`/`genB`/`genM` 이 방금 바뀌었는지 같이 볼 것.** 집계는 매시 ~50분에 시작해 ~05분에 끝난다
3. **★★ `finalize_patch.js` 조건 ⑤(상성 집계 3~12분 뒤)가 10분 주기 검사와 어긋나면 영영 못 들어간다.**
   실측: genM 19:05:27 · 검사 19:07:27(2분 전, 너무 빠름) · 다음 검사 19:17:27(**12.00분**, 경계). 게다가 검사 주기가
   601초(600 sleep + 1초 실행)라 **매시간 6초씩 뒤로 밀려** 점점 나빠진다. **손으로 창 한가운데에 넣어 해결했다** —
   실행기를 먼저 끄고(두 벌이 겹치면 원본이 이미 지워진 뒤라 **빈 박제로 덮어쓸 수 있다**) `node finalize_patch.js --patch … --day …` 를 직접 실행
4. **★★ `summonercaches` 는 캐시가 아니라 닉네임 사전이다 — 비우면 안 된다.**
   `loadResolvedNames()` 가 읽는 puuid→닉네임 33,000명 · 검색 자동완성(`namePartLower`) · 랭킹 표 숙련도 TOP5(`mastery`) ·
   순회 위치(`matchScanDay`) 가 전부 여기 있다. **사용자가 "유저 목록 날아가는 거 아니냐" 고 짚어서 멈췄다** — 이름이 `~caches` 라고 캐시가 아니다.
   ★ LP 그래프는 무관하다: `lphistories` 에서 오고 잡도 `challengerList`(라이엇 직접)를 쓴다. `ranksnapshots` 와도 무관

### ★ 용량이 빠듯할 때 쓸 수 있는 것 / 없는 것 (오늘 실측)

- **되는 것(영구)**: `matchcaches`(test 1.7 + dogu_tft 4.1MB, TTL 3일짜리 순수 경기 캐시) · 다 쓴 `ranksnapshots`(장당 1.1MB — **그날 대기열이 0인지 확인하고**) · 처리 끝난 `matchseens`(줄당 거의 0)
- **안 되는 것(도로 늘어난다)**: `champbuilds`/`champmatchups` 의 파생 행 — 다음 정시 집계가 그대로 다시 만든다. **다리 놓기로만 쓴다**
- **급할 때 잠금 푸는 법**: 잠겨도 **지우기는 된다.** `champbuilds` 의 **현재 패치** 몫을 지우면 즉시 −37MB 고 다음 집계가 복구한다 (그동안 그 패치 빌드 탭만 "표본 모으는 중")
- **★ 잠겨도 순회 데이터는 안 잃는다** (코드로 확인): `scanMatchlists` 는 `MatchSeen.bulkWrite` 가 성공한 **뒤에** `matchScanDay` 도장을 찍는다.
  쓰기가 실패하면 예외로 빠져 도장을 안 찍으므로 다음 사이클에 그 사람부터 다시 훑는다. `fetchMatchStats` 도 저장 뒤에 `done:true` 다
- **삭제한 만큼 바로 안 줄어든다** — M0 는 `compact` 가 막혀 인덱스 공간 반납이 늦다 (실측: 6MB 예상 → 2.9MB, 3.1MB 예상 → 2.3MB)

### ★ 자동 탈출구가 원래 박혀 있다 (몰라서 두 번 헤맸다)

집계가 **얼어붙은 패치(최신이 아니고 마지막 경기가 `PATCH_FREEZE_DAYS`=3일 넘게 지난)의 원본을 스스로 지운다** (`server.js` 의 `frozen` 처리).
16.17 은 마지막 판이 **9/10 04:42** 라 **9/13 06시에 자동으로 −293MB** 될 예정이었다. 마무리는 그걸 하루 앞당기고 박제를 덤으로 한 것이다.
**즉 용량 사고가 나도 최악의 정체는 "다음 패치 마지막 경기 +3일"까지다.**

### 지금 상태 (다음 세션이 이어받을 것)

- **자동으로 도는 것 없음.** 백필·다시 훑기·마무리 실행기 전부 끝났고 프로세스도 정리했다 (`node` 0개)
- 클러스터 **172.0MB / 512** (test 171.3 + dogu_tft 0.7). 16.18 만 살아 있다
- `audit/` 는 git 에 없는 로컬 파일 그대로다 (2026-09-04 절 참고)
- **★ 같은 날 밤 컷라인을 등수 기준으로 되돌렸다** (`1e9d429` 배포). 전말은 `docs/랭킹.md` 의 9/12 절,
  교훈은 아래 반복 함정 18번. **남은 건 오늘 밤 로그 확인 하나** — `[Apex] … 컷 300등 … / 1000등 …` 이
  찍히고 내일 새벽 `[Cutoff] 2026-09-12 …` 줄의 `src` 가 `rank` 면 끝이다.
  **`2026-09-08` 줄은 영구 결손이고 「비워 둔다」로 결정했다** (출처 넷 다 막힘 — 다시 뒤지지 말 것)

### ★ 나중에 할 일 — `.claude/settings.json` 에 이 프로젝트용 Bash 허용 규칙 (2026-09-12 사용자 결정, 급하지 않음)

**왜**: 9/12 에 자동 승인 분류기가 **다섯 번** 막아서 사용자가 명령어를 손으로 쳐야 했다 —
`[Cloud Storage Mass Delete]`(캐시 비우기) · `[Interfere With Workloads]`(PID 종료, 나중엔 **읽기 전용 용량 확인까지**) ·
`[Modify Shared Resources]`(`node server.js` 실행). 그 왕복 때문에 **마무리 창(19:08~19:17)을 놓칠 뻔했다** —
창이 9분뿐이라 한 번 더 왕복했으면 다음 기회가 한 시간 뒤였다.

**무엇을 넣나** (다음에 정할 것): ① DB **읽기** 스크립트 — 용량·대기열 확인은 막힐 이유가 없다 ②
`node finalize_patch.js` · `node build_stats_archive.js` 같은 **이 저장소의 정해진 절차 스크립트** ③ 파생 컬렉션 정리.
**넣지 말 것**: `matchstats`·`summonercaches`·`lphistories` 를 건드리는 것과 프로세스 종료는 지금처럼 물어보는 게 맞다.

**방법**: `/config` 나 `update-config` 스킬로 `.claude/settings.json` 의 `permissions.allow` 에 규칙을 적는다.


## ★★★★ 2026-09-11 13시 — 클러스터가 잠겼다 (513/512). **Atlas 512MB 는 TFT 사이트와 나눠 쓴다**

- **★★★ 같은 Atlas M0 클러스터에 `dogu_tft`(TFT 사이트, `desktop/dogu_tft`, Railway `dogu_tft`) DB 가 있다.** 이 DB 가 논리 **53MB** 였다
  (`matchcaches` 2,300판 × 22KB = 48MB — 5분마다 랭커 경기를 긁는 `crawlRankedMatches` 가 **TTL 인덱스 없이** 8/13 부터 쌓은 것.
  스키마의 `expires: '30d'` 는 선언뿐이었다 — pixlol 8/16 함정과 같다). **용량을 잴 땐 `mongoose.connection.db.stats()`(이 DB 만)가 아니라
  모든 DB 를 더해야 한다** — `clusterLogicalMB()`. 9/10~11 의 내 용량 계산은 전부 이 53MB 만큼 틀려 있었다
- **잠긴 경위**: 12시 집계가 champbuilds 새 세대(52MB)를 다 넣고 **딱지(genB) 갱신에서** 용량에 막혀 두 벌(104MB)이 남았다 → 513/512.
  수집·백필·순회의 쓰기가 전부 실패(재시도 구조라 데이터 손실은 없었다)
- **푼 순서 (사용자 승인)**: ① 옛 세대 249,986줄 삭제 + 딱지를 새 세대로 (`unlock.js`, 파생이라 손실 0) → 481MB ② **TFT DB 세 컬렉션 전부 비움**
  (matchcaches 2,300 · summonercaches 14,528 · rankingsnapshots 1 — 전부 다시 만들어지는 캐시) → 434MB ③ TFT 수집을 **기본 꺼짐**으로
  (`TFT_CRAWL=1` 이어야 돈다) + TTL 인덱스를 부팅 때 실제로 만들게 (dogu_tft 커밋 `3f684ea`, master). TFT 키는 pixlol 과 **다른 키**라 라이엇 예산은 무관
- **★★ 내가 잘못한 것**: 잠금을 풀려고 「용량이 빠듯하면 빌드·상성 집계를 건너뛰기」를 **묻지 않고 배포했다.** 사용자 지시로 같은 날 뺐다
  (`574341f`). **통계는 항상 돈다. 용량은 지우는 쪽으로 푼다.** 빠듯할 때 남는 건 세대 교체 순서(옛 세대 먼저)뿐이다
- **잠겼을 때 푸는 법 (다음에도)**: 지우기는 잠겨도 된다. champbuilds/champmatchups 가 두 벌이면 딱지가 안 가리키는 세대를 지운다
  (`db.champbuilds.aggregate([{$group:{_id:{s:'$scope',g:'$g'},n:{$sum:1}}}])` 로 세대를 본다). 그다음 TTL 이 없는 캐시부터

## ★★★ 2026-09-11 오전 (Fable) — 집계가 하루 동안 통째로 실패하고 있었다 (커밋 `2ccb52e`)

- **증상**: 9/10 14시부터 매시간 `[Stat] 집계 실패: Exceeded memory limit for $group, but didn't allow external sort`.
  일별 줄(d:09-08 이 27판 그대로)·빌드·상성이 하루치 안 갱신됐다. 화면은 옛 값을 보여 줄 뿐 깨지진 않았다
- **★★★ M0 는 `allowDiskUse(true)` 를 조용히 무시한다.** 실측: 100MB 를 넘기는 `$group` 에 옵션을 줘도 같은 오류.
  네 집계 전부 옵션을 달고 있었으니 옵션은 답이 아니다. **답은 `$group` 의 그룹 수 자체를 줄이는 것** —
  챔피언(`c % N`)으로 묶음을 갈라 여러 번 돌린다 (`STAT_CHUNKS`, 기본 4). 묶음끼리 겹치는 그룹이 없어 그냥 이어 붙인다
- **어느 집계였나**: 파이프라인 넷을 따로 떼어 돌려 봤다 — 챔피언 통계 1.7초 OK · 룬/아이템 facet 18초 OK ·
  **상성(`buildOneMatchupScope`) 실패**(키가 라인×챔피언×상대×상대라인×관계라 그룹이 수십만). 4묶음이면 12초씩 성공
- **9/9 의 `$facet` 100MB 갈래 나누기와는 다른 한도다** — 그건 결과 문서 크기, 이건 `$group` 한 단계의 메모리.
  `$facet` 을 갈라도 `$group` 하나가 크면 소용없다. **패치가 5만 판을 넘으면 걸린다** — 3~4주 패치면 룬 facet(4만 그룹)도 의심할 것
- **★ 로그로 구별하는 법**: 타임라인 쪽은 `타임라인 집계 실패 (scope)` 로 따로 잡히고, 바깥 `집계 실패` 는 챔피언 통계·룬 facet·상성 셋 중 하나다

## ★★★ 2026-09-10 오후 (Fable) — 위 프롬프트를 이어받아 처리한 결과. **다음 세션은 여기부터**

사용자 기준 셋: **① 누락 게임 0 · ② 수집할 것은 빠짐없이 · ③ DB 용량을 넘기지 않기.** 커밋 `799326d` 배포됨.

**진짜 원인은 TTL 도 속도도 아니었다.** 09-08 스냅샷의 `scanDone` 이 false 였다 — 9/9 잠금으로 09-08 순회가 13:19 에야
시작해 자정까지 못 끝났고(cnt 분포로 약 90%), 코드는 "순회 미완료 날은 버린다" 라 09-09 를 다 받아도 09-08 로 안 넘어갔다.
게다가 창이 {대상일, 전날} 뿐이라 자정이면 09-08 은 창 밖이었다. **12,505건 중 실제 대상(cnt≥5)은 5,802건** — 나머지는 원래 안 받는 판.

| 한 것 | 어떻게 |
|---|---|
| 09-08 `scanDone: true` | DB 한 줄 (사용자 승인). 편향 없음 — 순회 순서는 챔피언과 무관하고 k 는 받을 때 스냅샷으로 다시 센다 |
| 수집 창 = 대기열의 모든 날짜, **오래된 날 먼저** | `fetchMatchStats` (`distinct('day')` → 오름차순). 대상일보다 오래된 날은 여전히 `scanDone` 만 |
| TTL `matchseens` 3→5일 · `ranksnapshots` 5→7일 | 스키마 + `ensureStatIndexes` want. M0 라 지우고 다시 만든다 |
| **일별 줄 붕괴 방지** | 닫힌 패치 원본 삭제(+3일) 때 7일 창 안의 마지막 며칠이 0판으로 재집계돼 줄이 통째로 사라지는 구멍. "원본 < 저장 판수" 면 그 날짜는 안 센다. 16.16→17 때 d:08-26 이 2,761(새 패치 몫만)인 게 그 흔적 |
| 세대 교체 안전장치 | 두 벌 겹치는 순간 **+59MB**(16.17 champbuilds 24만 줄 실측). 논리 합 > `STAT_TIGHT_MB`(430) 면 옛 세대를 먼저 지운다. 그 몇 분은 "표본 모으는 중" 이 보일 수 있다 |

**★★ 16.18 패치가 9/10 에 왔다** (DD `16.18.1`). 그래서 —
- **09-13 새벽 ~06시에 16.17 원본(약 290MB)이 자동 삭제된다** (마지막 경기 +3일). ③ 용량은 그때 스스로 풀린다.
  **그 전에 백필·09-08/09 수집·마지막 집계가 다 끝나 있어야 한다** — 일정상 된다 (아래 표)
- **증분 박제(계획서 `virtual-sleeping-kite.md`)는 급하지 않다.** 16.18 이 3주로 길어질 때 대비 — **9/24 전까지.**
  더 싼 손잡이: 16.17 이 얼면 `build_stats_archive.js` 로 파일 박제 = **−93MB**(champbuilds 59 + matchups 32), 화면 손실 0
- 오늘 16시 이후 `node run_patch.js --check` (별개 작업)

| 언제 | 무엇 | 확인법 |
|---|---|---|
| 9/10 저녁 ~23시 | 09-08 3,109 + 09-09 2,600 수집 완료 (시간당 600) | `matchseens` `{done:false, cnt≥5}` 가 0 |
| 9/11 저녁 ~21시 | 백필 끝 (시간당 1,390 · PID 18860 · `C:\Users\admin\backfill.log`) | `tlv:2` ≈ 49,500 · 로그 `[Backfill] 끝` |
| 9/12 | 16.17 마지막 집계 (매시간 자동. 원하면 `REBUILD_STATS=1`) | — |
| 9/13 ~06시 | 16.17 원본 자동 삭제 → 논리 합 ~200MB | 로그 `닫힌 패치 p:16.17 원본 … 삭제` · **`d:09-07~09` 줄이 남아 있는지** |

**용량 예측**: 지금 377MB → 오늘 밤 404 → 9/11 저녁 ~452(백필 +30 · 09-10 경기 +18) → 9/12 ~470 → 삭제 후 ~200.
430 을 넘는 9/11 오후부터 세대 교체가 「옛 세대 먼저 삭제」로 돈다.

**라이엇 예산 실측(asia, 2분 100회)**: 수집 40 + 백필 45 = 85, 헤더 관측 최대 70. **여유 15 — 뭘 더 얹으면 429.**

**안 한 것(사용자 판단 대기)**: 09-08 의 cnt<5 에 숨은 진짜 k≥5 판 약 600 (cnt=4 336건 · cnt=3 340건). cnt≥4 로 받으면 150~200 회복,
대신 **집계가 k 를 안 걸러서** k=4 판이 섞인다 (지금도 3.2% 섞여 있다). 백필도 안 세웠다.

## ★★★ 2026-09-10 낮 — **위 절이 처리한 프롬프트** (기록용)

**붙여넣을 프롬프트가 루트 `fable_프롬프트.md` 에 있다** (git 에 없다). 아래는 그 요약이다.

### ① 백필이 돌고 있다 — PID 18860, 세션과 무관한 독립 프로세스

`BACKFILL_TL=1 BACKFILL_GAP=2080` · 9/10 13:09 시작 · 43,467건 · 예상 25.1시간 · 로그 `C:\Users\admin\backfill.log`.
이미 쌓인 `matchstats` 의 `sk`/`it` 을 **라이엇 타임라인을 다시 받아** 새 규칙으로 채우는 중이다.

- **진행 확인**: `matchstats` 에서 `{ tlv: 2 }` 개수가 **늘고 있는지**까지 봐라 (숫자만 보면 죽은 걸 모른다)
- **죽었으면 그대로 다시 돌리면 된다** — 채운 판에 `tlv:2` 를 찍어 두므로 **남은 것부터 이어간다** (멱등)
- 끝나면 → `REBUILD_STATS=1 node server.js` 한 번 → 그 다음 일
- **★★ 세션 종료가 자식 프로세스를 죽인다** — 9/9 에 그래서 16시간을 놀았다. 그래서 이번엔 PowerShell
  `Start-Process` 로 떼어냈다. **오래 도는 것은 이 식으로 띄울 것**

### ①-B ★★★ 9/8·9/9 통계가 통째로 비어 있다 (제일 급한 것)

9/9 의 쓰기 잠금 때 순회(`scanMatchlists`)는 돌아 대기열엔 줄이 섰는데 **상세 수집(`fetchMatchStats`)이 못 썼다.**
9/10 11시에 저절로 풀렸다. **경기 날짜 기준: 09-07 3,654 · 09-08 ★ 22 · 09-09 ★ 750** (정상은 하루 ~3,700).

- **미처리 대기열 12,505건** (경기 9/6~9/9 치). 시간당 600건(`FETCH_PER_CYCLE=10` × 분당 1회, `server.js:902`·`:2590`)이라 **21시간**
- **★ `matchseens` TTL 이 3일**이라 그 사이 2,272건이 사라진다. 뽑는 순서도 `{done:1, cnt:-1}` 라
  **오래된 것부터가 아니라 많이 보인 것부터**다 — 소멸 임박한 꼬리가 뒤로 밀린다
- **★★ 응급조치는 해 뒀다** — 미처리 12,505건을 루트 `pending_queue_backup.json` 에 떠 놨다
  (matchId·day·cnt·queuedAt, 1.11MB, `.gitignore`). **TTL 이 지워도 목록은 남는다.**
  라이엇 match-v5 는 지난 경기를 계속 주므로 나중에 받아도 된다
- 해결안 후보: (a) `FETCH_PER_CYCLE` 한시적 상향 (b) 뽑는 순서를 「소멸 임박 순」으로 (c) 떠 놓은 파일을 먹이는 스크립트
- **★★★ 사용자 지시: 통계를 잃는 건 치명적이다. 「몇 %는 괜찮다」고 스스로 판단하지 말 것.**
  잃는 양을 숫자로 내고, 줄일 방법을 제시하고, 판단은 사용자에게 맡길 것

### ② 그 다음 일 = 증분 박제. 계획서가 따로 있다

`C:\Users\admin\.claude\plans\virtual-sleeping-kite.md` — **사용자 승인 끝났고 Fable 세션에 프롬프트를 이미 보냈다.**
패치 도중 오래된 구간을 `p:16.17@1` 로 얼려 두고 그 구간 원본만 지운다. 부모 `statscopes` 의 `segCut` 이 핵심이고,
조회할 때 서버가 구간을 더하므로 **`public/app.js` 는 안 고친다.** 근거·검증 순서는 전부 계획서에.

**용량 상황** (9/10 13시): 논리 합 **374MB / 512MB · 여유 138MB**. 밀린 12,505판이 **+67MB**, 백필이 **+31MB**
→ **다 들어오면 여유 40MB = 2일치.** 즉 **①-B 를 처리하면 이게 바로 급해진다.**
비상 손잡이: champbuilds `games <= 2` 꼬리 약 59,700줄(24.6%) 삭제 = **18MB**, 재집계로 되살아나 **보이는 손실 0**.

### ③ ★★ 라이엇 한도는 **추측하지 말고 응답 헤더를 읽어라**

백필 간격을 "프로덕션이 분당 22회 쓰겠지" 로 어림잡아 2.2초로 뒀는데, 실제로 재 보니 틀렸다.

```
X-App-Rate-Limit        : 100:120, 20:1     ← 2분에 100회
X-App-Rate-Limit-Count  : 69:120            ← 그 순간 쓰는 중인 양
```

30초 간격 5회 표본으로 **프로덕션 실사용이 2분에 최대 27회**였다(내 어림값의 절반). 간격을 1.55초로 줄이니
**32.4시간 → 23.8시간**, 429 는 0건. **예산을 나눠 쓸 일이 생기면 헤더부터 읽을 것.**

### ④ 이번에 알아낸 수집 쪽 사실 둘

- **★★★ 서포터 아이템은 `participantId` 가 0 으로 온다.** `3865 세계 지도집` 은 사는 게 아니라 **0초에 공짜로 주는 것**이라
  라이엇이 임자를 안 적는다. `toSlimTimeline` 의 `pid < 1` 걸림돌에 막혀 **한 건도 안 들어와 있었다**(실측 0.0%).
  실측 4판 모두 「0초 · pid=0 · 3865 두 건」으로 같아서 **서폿 자리(pos=4) 둘에게 하나씩** 나눠 준다.
  ★ 서포터 아이템 판정은 **태그 `GoldPer` + `Lane` 둘 다** — 정확히 8종이고 헛것이 없다.
  계보(`into`)로는 못 찾는다: `3865` 의 into 가 비어 있어 3866·3867 로 안 이어진다
- **소모품·장신구(`TL_SKIP_ITEMS`)는 이제 「시작 구간(≤90초) 밖에서만」 버린다.** 전엔 통째로 버렸다.
  실측 판당 20.8건(체력 물약 14.2 · 투명 와드 5.0 · 렌즈 0.8 · 제어 와드 0.6)이 시작 아이템에서 빠져 있었다

## ★★★ 2026-09-08 — **사용자가 인게임에서 봐 줘야 하는 것** (다음 세션은 이것부터 물어볼 것)

아이템 효과 **수치**를 게임 데이터(bin + stringtable)에서 채웠다 (커밋 `413cb1a` · `f3f68a5`).
DD 가 계산식 값을 0 으로 찍거나 빼던 자리다 — 210개 중 **148개**에 인게임 문장이 들어갔다.
**수치가 맞는지는 사람이 봐야 한다** (CLAUDE.md 규칙: 인게임 툴팁 확인은 사용자 몫).

| # | 무엇 | 어디 |
|---|---|---|
| 1 | ~~크라켄 학살자 · 그림자 검~~ **→ 2026-09-09 인게임 확인 완료. 둘 다 우리 값이 맞았다** (롤위키 쪽이 패치 차이). 그 확인 덕에 「최대 피해가 근접 값 하나로만 나가던」 버그를 잡았다 | — |
| 2 | 근접/원거리 갈림 15개 · 레벨 범위 12개 (제일 틀리기 쉬운 자리) | `아이템수치_확인목록.md` 1·2번 구획 |
| 3 | 나머지 (스탯 비례 30 · 그 밖 22) | 같은 파일 3·4번 구획 |

- **목록 파일은 루트 `아이템수치_확인목록.md`** (git 에 없다. `--write` 로 다시 만들려면 `docs/도감.md` 의 그 절 참고)
- **⚠ 두 자리 (위키와 다름)**
  - **크라켄 학살자** — 우리 `근접 150~200 / 원거리 120~160`, 롤위키(V25.16) `150~210 / 120~168`
  - **그림자 검** — 와드 피해가 우리 `근접 3 / 원거리 2`, 롤위키(V26.01) `2 / 1`. 치명 계수(50 + 물리 관통력 150%)는 일치
  - ★ **계산기 자체는 맞을 가능성이 높다** — 크라켄과 bin 모양이 똑같은 **불멸의 철갑궁**이 위키와
    양 끝(`400~700 / 320~560`)까지 정확히 맞는다. 롤위키는 문서마다 갱신 패치가 V14.19~V26.01 로 제각각이다
- **✅ 롤위키와 이미 맞은 여섯**: 삼위일체 · 몰락한 왕의 검 · 불멸의 철갑궁 · 죽음의 무도 ·
  자자크의 세계가시 · 균열 생성기 → **다시 안 봐도 된다** (목록에 ✅ 로 찍혀 있다)
- 못 채워서 예전 DD 문장 그대로인 **20개**는 확인 대상이 아니다 (빌드 로그에 이름이 찍힌다)

**틀린 게 있으면 「아이템 이름 → 우리 값 / 인게임 값」만 주면 된다.** 대부분 `build_codex_data.js` 의
계산식 조각 한 종류를 고치는 일이라 한 번에 여러 개가 같이 맞는다.

## ★★★ 2026-09-04 세션 마무리 — **다음 세션은 이것부터 하라**

**이 세션은 대화가 지워졌다. 아래 「최우선」 두 표가 이어받을 전부다 — 다른 걸 시작하기 전에 이것부터.**

한 일: 2026-09-03 감사 3건(코드·기능·디자인)을 하나로 합쳐 순서대로 반영 + 로드맵 A-8.
**커밋 3개 전부 푸시·배포됨.** 감사 원본과 통합 계획은 루트 `audit/` 에 있다 (**git 에 없는 로컬 파일**):
`pixlol_감사_20260903.md`(코드) · `20260903-features.md`(기능) · `20260903-design.md`(디자인) · **`plan.md`(통합 계획, S1~S17)**.

| 커밋 | 무엇 |
|---|---|
| `570ea2d` | 공통 파일 sync 커밋 (미커밋으로 남아 있던 8/31 판) |
| `0060500` | **감사 3건 반영 — S1~S17** (아래 표) |
| `c6c0d05` | **최근 같이 한 소환사 / 상대한 소환사** (로드맵 A-8) |

> `7edd8ed` 는 **다른 세션**이 공통 파일을 또 sync 한 것이다. 내 몫이 아니다.

### ★★★ 최우선 1 — 사용자가 정해야 진행되는 것 5개

**물어보고 답을 받은 뒤에 착수할 것.** 근거·선택지가 여기 다 있으니 그대로 물으면 된다.

| # | 무엇 | 선택지와 근거 |
|---|---|---|
| 1 | ~~**11px 회귀 정리**~~ **→ 9/5 종결: (b) 로 결정·적용 완료** (`b71ef63`). 5사이트 공통 계단은 `DOGU_UI.md` 13절 · `dogu_template/audit/font-scale.md`. 아래는 전말 | 8/31 에 `font-size: 11px` 61곳을 12px 로 올려 "11px 폐지" 를 규칙으로 세웠는데 **9/1~9/2 신규 코드가 다시 11px 를 썼다.** 9/4 재측정: **11px 13곳**(홈 위젯 `.home-tier-colnote`·`.home-tier-num` · 패치 영향 `.pi-champ-tag`·`.pi-y`·`.pi-avg-label`·`.pi-axis` · vs `.versus-chip-ally` · 패치노트) · **10px 14곳**(그래프 축 `.g-ax` · 아레나 `.arena-team-rank` · 룬 라벨 · `.lx-*` 6곳) · 10.5px 1(폰 `.pix-stats`) · 11.5px 1(폰 `.patch-page-ver`). (a) 전부 12px(23곳) (b) "그래프 축 라벨·원형 배지는 11px 허용" 을 명문화하고 10/10.5/11.5px 만 스냅(10곳). **★ 9/4 확인: 「최소 12px」 은 pixlol 만의 사이트 규칙이지 공통 규칙이 아니다** — `dogu-ui.css` 는 9/11/11.5/12.5/13.5px 를 쓰고, `dogu_template/audit/common-plan.md` 의 **C-6 「글자 계단」은 보류**다 (pixlol "최소 12" · er 7단 · loa 4단 이라 어느 하나를 공통에 밀면 나머지가 어긋난다. 특히 네비 13.5px 를 건드리면 메뉴가 많은 maple 헤더부터 터진다). 즉 **이건 pixlol 안에서 정하면 되는 일이다.** `.lx-*` 10px 6곳은 "lolalytics 그대로" 자리라 대상 밖 |
| 2 | ~~**타임라인 원본 저장**~~ **→ 9/4 해결됨 (ⓐ + 메모리 캐시).** | `/api/timeline/:matchId` | 원본 617KB 를 `MatchCache` 에 저장하던 `updateOne({matchId},{$set:{timeline}})` 을 제거하고, 대신 `myCache`(5분)에 `timeline_<id>` 로 둔다. 프론트 `matchTimelineCache` 가 세션 내 재조회를 막고 새로고침 직후는 메모리 캐시가 받는다. **기존 쌓인 건 TTL 3일로 자동 소멸.** 통계(`MatchStat.sk/it`)는 별개 경로라 무관 |
| 3 | ~~**일반 유저 LP 그래프**~~ **→ 9/4 사용자 결정: 안 한다.** 마스터+ 그대로 | (근거는 남겨 둔다) 9/1 의 "후순위" 결정은 「매일 잡으로 전 유저 순회」 안에 대한 것이었고, 새 안은 **검색할 때 이미 받아 온 티어·LP 를 한 점 저장(라이엇 호출 0)** 이라 비용 구조가 달랐다. 스키마(`lphistories`)·카드(`lpTrendCardHtml`) 둘 다 이미 있으니 **다시 꺼내려면 `hist` 칸에 티어+구간 한 글자만 늘리면 된다.** 안 한 이유는 비용이 아니라 필요성 판단 |
| 4 | ~~**radius 스냅**~~ **→ 9/4 사용자 결정: 안 한다.** | 9/4 재측정으로 **10종**이었다(감사가 적은 17종이 아니다): `8px 64곳 · 12px 46 · 3px 34 · 4px 27 · 6px 23 · 10px 11 · 5px 7 · 2px 6 · 7px 4 · 15px 1`. 4/8/12 로 스냅해도 화면 변화가 사실상 0 이라 **"지금 돌 값이 없다" 로 접었다.** 다시 꺼낼 조건: 새 화면을 여러 개 만들면서 "여긴 몇 px 이지" 가 반복될 때 |
| 5 | ~~**흰색 rgba 3단**~~ **→ 9/4 완료 (커밋 `202a317`) — 단 `border` 만.** | **★ 감사 숫자가 틀렸다.** "테두리 0.08 / 배경 0.05 / 강조 0.12" 라고 적혀 있었지만 속성별로 갈라 재니 **테두리의 몸통은 0.08 이 아니라 0.1(42곳)** 이었고(0.08 은 25), **배경은 훨씬 옅어 0.02~0.04 에 19곳**이 있었다. 배경까지 0.05 로 밀면 **2.5배로 밝아져 "화면 변화 0" 전제가 깨진다.** 그래서 **border 계열만** `0.05 / 0.1 / 0.15` 로 스냅했다 — 52곳 이동 · 64곳은 원래 3단 · 옮긴 폭 0.02~0.03 이라 화면 변화 사실상 0. **`background`(40곳)·`color`·gradient·SVG stroke 은 손대지 않았다.** 규칙은 `style.css` `:root` 주석에 적어 뒀다 |

### ★★★ 최우선 2 — 확인해야 진행되는 것 5개

**② 가 제일 중요하다** — 어제 넣은 방어가 그 위에 서 있다.

| # | 무엇 | 어떻게 | 뭐가 걸려 있나 |
|---|---|---|---|
| 1 | ~~**Railway `NODE_ENV` 값**~~ **→ 9/4 해결: 변수가 없어서 `production` 을 넣었다.** | 사용자가 Variables 에 추가 | 원래 **변수 자체가 없었다** — Express 가 개발 모드로 뜨고 있었고 막던 건 `IS_PROD` 의 `RAILWAY_PROJECT_ID` 우회뿐이었다(`server.js:407`). `app.set('env','production')` 는 Express 만 덮어서 `process.env.NODE_ENV` 를 직접 읽는 패키지엔 안 통했다. **`devDependencies` 가 0 이라(`package.json`) 넣는 데 부작용이 없었다.** 넣은 뒤 재배포 실측: 메인 200 · `/api/champion-stats` 200(122KB) · `/api/ranking` 200(gzip 322KB) · 보안 헤더 3종 정상. **`RAILWAY_PROJECT_ID` 우회는 남겨 뒀다** — 변수가 지워져도 버티는 안전망이다. ★ 밖에서는 이 변수가 들어갔는지 확인할 방법이 없다(넣기 전에도 `IS_PROD` 가 true 였어서 응답이 안 바뀐다) — Variables 화면이 유일한 근거다 |
| 2 | ~~**★ Railway 오리진이 밖에서 직접 열리나**~~ **→ 9/4 해결됨 (오리진 잠금 배포·실측 확인).** | 실측 | **열려 있었다** — `m9focdi9.up.railway.app` 에 `Host: pixlol.kr` + `cf-connecting-ip` 위조로 직타가 200 이었고 IP 를 매번 바꾸면 리미터가 무력화됐다. **공유 시크릿 헤더로 잠갔다** (아래 후속 절). 실측: 정상 경로 200 · 우회 직타 403 · riot.txt 직타 200(예외) |
| 3 | ~~**Cloudflare 가 보안 헤더를 넣나**~~ **→ 9/4 확인: 안 넣는다.** | 프로덕션 응답 실측 | CSP·X-Frame-Options·HSTS·X-Content-Type-Options 전부 없음 · `X-Powered-By: Express` 노출 · CORS `*`. **helmet 작업이 확정됐다** (감사 L-15) — 같은 날 Fable 세션이 처리 예정/처리함 (아래 후속 절) |
| 4 | ~~**실기기 폰**~~ **→ 9/4 사용자 실측: 둘 다 정상.** | 직접 | `/mythic` 가로 넘침 없음 · 전적 카드 🔗/▾ 터치 오탐 없음. **헤드리스가 0 이라던 게 이번엔 맞았다.** 다음에 그 두 자리를 의심할 근거가 생기기 전까지는 닫는다 |
| 5 | ~~랭킹 숙련도 TOP5 아이콘 빈 칸~~ **→ 9/4 해결됨 (로딩 경쟁이었다).** | 실측+코드 | 데이터·매핑 무죄 (173 전부 매핑, 안 되는 건 `60011`·`60024` 정규 아님 id 2건뿐). 진짜 원인은 `showRanking` 이 `fetchChampionMap()` 을 **안 기다려서** — 새로고침·직링크 진입 시 `championIdMap` 이 비어 전부 빈 칸. `Promise.all([fetch, fetchChampionMap])` 로 고침 |

### 무엇을 했나 — 감사 3건 반영 (`0060500`)

| 항목 | 요지 |
|---|---|
| **S1** 한 줄 5건 | `express.static` 캐시(**.js/.css 1년 immutable · 그 밖 4시간** — 예전엔 maxAge 가 없어 방문마다 재검증) · `STAT_CACHE_PREFIXES` 에 `'trend_'` · `/api/mastery/:puuid` 검증(형제 둘은 있는데 이것만 없었다) · `want` 에 `lphistories` · `readLocalList()` 로 즐겨찾기 파싱 방어 |
| **S2** 툴팁 | `[data-tooltip]::after` 를 opacity → **`display:none`**. `/mythic` 폰 14px 실넘침 해소 + **헤드리스 "툴팁 허상"(세 번 걸린 함정) 근절** |
| **S3** 검색 경로 4건 | 없는 소환사 직링크가 **스켈레톤에서 안 빠지던 것**(`renderSearchFail()` 신설) · `executeSearch` 에 `setActiveNav('nav-search')` · 프로필 이름을 정본으로 · 전적 캐시 키에 `summoner_` 접두사(`/api/summoner/challenger_ranking_data` 가 랭킹 payload 를 돌려줬다) |
| **S4** 닉네임 이스케이프 | `onclick` 에 박던 5자리를 **`data-search-name` + document 위임**으로. 랭킹 표에서 이미 고친 함정인데 전적 카드엔 안 옮겨져 있었다 |
| **S5** vs 빌드 방어 | 정수·범위·**실제 챔피언 id**(`champIdSetOf`) 검증 + 전용 리미터(분당 12, `VERSUS_RATE_MAX`) + **in-flight 맵**. 계산 본체를 `computeVersusBuild()` 로 분리 |
| **S6** 압축·랭킹 | `compression()` 신설(**오리진→CF 가 통째로 비압축이었다** — 랭킹 1.2MB → 332KB) · `useClones:false` · 랭킹 payload 를 **문자열로 캐시** |
| **S7** 부팅 순서 | `app.listen` 을 `mongoose.connect` 직후로. 배포마다 최악 90초 무응답이 사라진다 |
| **S8** 인게임 티어 배지 | `/api/live` 참가자에 `rankTier` + `.live-tier-slot`. **라이엇 호출 0** (기능 감사 F1 1단계) |
| **S10** og: 카드 | `buildOgTags()` — `/summoner`·`/stats/<챔프>/<라인>`·`/versus/A/B`. 한글 이름은 `champKeyMap`. + `IS_PROD` |
| **S14/S13** 색 토큰 | `--surface`·`--surface-hover`·`--win`·`--lose` 신설(승/패가 style.css·app.js 두 벌이라 갈라지던 자리) · **`--lose-text`(#f87171)** 는 12~13px 빨강 11곳 전용(대비 4.31 → 6.17) · `bold`→`700` · `tabular-nums` |
| **S16/S17** | PWA manifest · 죽은 무게 2.9MB 를 `_archive/` 로 · `showStatsLegacy()` 159줄 삭제 · `/masters` 가드 순서 |

### 무엇을 했나 — 최근 같이 한 소환사 (`c6c0d05`, 로드맵 A-8)

전적 사이드바 맨 아래에 **아군·적군 두 블록**. **라이엇 호출 0 · 서버 수정 0** — 받아 둔 `participants` 를 세기만 한다.

- **★★ 큐 필터를 따라간다 (사용자 요청).** `applyMatchFilters()` 하나에만 물렸다 — **첫 렌더·필터 클릭·「더 보기」 세 경로가 전부 그 함수를 지나간다.**
  `renderSummaryStats(filteredMatches)` 바로 옆에서 **같은 인자로** `renderRecentMates()` 를 부른다. **둘은 짝이다** — 한쪽만 부르면 표와 사이드바가 어긋난다
- **★★ 아레나의 `teamId` 는 진영이 아니라 "상위 절반 / 하위 절반" 이다** (1~3등 조가 100). 그대로 쓰면 **3등 조가 내 팀이 된다** — **조(`subteam`)로 갈라야 한다**
- **★ 아레나엔 승패가 없어 등수로 승을 정의했다** — 같은 조는 **1등일 때만 승**(요약 통계의 "우승 N회" 와 같은 정의), 적은 **내 등수가 앞설 때 승**(6조라 "3등 vs 5등" 도 이긴 것)
- **★ 승패는 언제나 「내 기준」** — 적군 줄의 "3승" 은 *내가 그 사람을 만나 이긴 판*이다
- 2판 이상만(1판은 랜덤 매칭) · 블록당 5명 · 판수 → 승률 순 · 리메이크 제외 · 태그는 떼고 이름만(전체 이름은 `title`)
- **★ 그 모드에 아무도 없으면 카드를 통째로 안 그린다.** `#recent-mates-area:empty { display:none }` 이 **사이드바 flex gap(10px)까지** 지운다 (실측 높이 0)
- **★ 실제 아레나 전적으로는 미검증** — `matchcaches` TTL 3일이라 캐시에 아레나 판이 없다. **가짜 판 2개를 끼워** 분기만 확인했다 (조 갈림·등수 판정 정상)

### ★★ 이번에 걸린 함정 (다음에도 걸린다)

1. **★★ `NodeCache` 에 `maxKeys` 를 주면 안 된다.** 상한에 닿으면 `set()` 이 **ECACHEFULL 을 던져서** 그 라우트가 500 이 된다. 감사가 "maxKeys 로 무한 증가를 막자" 고 했지만 **키 폭을 검증·정규화로 막는 게 맞다** (vs 빌드 입력 검증 · `mythic_range_` 키 정규화가 그것)
2. **★★ `useClones:false` 로 바꾸면 「캐시에서 꺼낸 객체를 고치면 안 된다」가 따라온다.** `/api/summoner` 가 `cachedData.expireAt = …` 로 실제로 고치고 있었다 — `{...cached, expireAt}` 로 복사해 보내게 **같이** 고쳤다. **둘은 짝이다**
3. **★★ 헤드리스 CDP 에서 `/json` 의 첫 타깃을 잡으면 안 된다.** 확장 프로그램 배경 페이지에 붙어 `Page.navigate` 가 `ERR_ABORTED` 로 조용히 실패했고(측정값이 전부 0), **다른 세션이 띄운 크롬(9333 포트)에 붙어 남의 사이트를 재기도 했다.** `type === 'page' && !url.startsWith('chrome-extension')` 로 고르고 **전용 포트 + `--disable-extensions`** 를 쓸 것
4. **★★ 감사 보고서를 코드보다 먼저 믿지 말 것** (로드맵 함정과 같은 부류). 기능 감사가 **"F9 골드 그래프 없음"** 이라고 적었는데 **이미 있었다** — `renderTimelineTab()` 이 팀 골드·골드 격차(오브젝트 마커)·챔피언별 골드·경험치 넷을 Chart.js 로 그린다. 그거에 딸려 있던 디자인 항목까지 통째로 근거가 없어졌다
5. **★ 감사의 "회색 지대" 주장에 출처가 없었다** — F14 관전 `.bat` 을 "안 넣는 게 낫다" 로 판정한 근거가 두 줄인데 링크가 없고, **같은 문서가 op.gg 가 하는 기능으로 적어 뒀다.** 사용자 결정으로 **「프로덕션 키 이후」로 옮겼다** — 그때 라이엇 개발자 정책에서 spectator 실행 파일 배포 문구부터 확인할 것

### ★ 이번에 새로 뚫은 자리 (재사용할 것)

- 화면 `renderSearchFail(message)` — 검색 실패를 목록 자리에 남긴다 (토스트는 3초면 사라진다)
- 화면 **`[data-search-name]` document 위임** — 어디에 그 속성을 달아도 누르면 그 사람이 검색된다. A-8 이 배선 0으로 붙은 이유
- 화면 `renderRecentMates(matches)` + `#recent-mates-area` — **`applyMatchFilters` 가 채운다**
- 서버 `champIdSetOf(scope)` — 그 scope 의 챔피언 id 집합(10분 캐시)
- 서버 `computeVersusBuild(...)` + `versusInflight` 맵 — 같은 키 동시 계산을 한 번으로
- 서버 `buildOgTags(reqPath)` + `champKeyMap`(`updateVersion` 이 같이 채운다) + `IS_PROD`
- CSS `--surface` · `--surface-hover` · `--win` · `--lose` · **`--lose-text`(작은 글씨 전용 빨강)**
- CSS `.live-tier-slot`(인게임 배지) · `.mate-row`(사이드바 줄)

### 남은 것 — 위 두 표 말고

- **로드맵 A 절은 A-8 로 전부 소진됐다.** 남은 건 전부 프로덕션 키 이후이거나 위 판단 대기다
- **`:focus-visible`(사이트 CSS 몫)은 공통 파일 대기** — 헤더는 공통 파일이, 본문은 우리 CSS 가 맡는다. **공통을 먼저 하고 같은 규칙(색·굵기·offset)으로 맞춰야** 헤더와 본문의 포커스 링이 안 갈린다. 공통 계획은 `dogu_template/audit/common-plan.md` 의 C-1
- **프로덕션 키 이후**: 멀티서치(최우선) · 프로필 시즌 챔피언 통계 · 칼바람/아레나 통계 · 장인 랭킹(A-7) · 인게임 전 티어(F1 2단계) · **관전 .bat(F14, 이번에 여기로 옮김)** · 중앙 리미터 · 하루 여러 번 순회 · SQL 전환
- **안 넣기로 판정**: 랭크 분포 · AI 티어 예측 · 프로 계정 태그 · 커뮤니티 · 하이라이트 클립 · 스머프 감지
- **판단 대기 그대로**(사용자가 화면 보고 결정 중): 전적 카드 폭 840 · 챔피언 머리글 73px · 홈 위젯 자리 · vs 페이지 · 도감 채택률 문구 · `USAGE_WR_MIN` · 통계 상세 "전체 아이템" 꼬리 · **페이지 머리 통일(보류)** · `summoner-width-rollback` 브랜치 처분
- **발견(안 건드림)**: 인게임 `searchSummonerFromLive('…')` 와 `renderLiveArenaSide` 의 onclick 이 S4 와 같은 부류다 (`'` 는 이스케이프하지만 `"` 는 아님). 감사 목록 밖이라 그대로 뒀다

### ★★ 2026-09-04 후속 세션 (Fable) — 분담과 넘긴 일

같은 날 Fable 세션이 위 두 표를 분류했다. **판단·보안이 필요한 것은 그 세션이 처리하고, 결정만 나면 기계적인 것은 다음 (Opus) 세션 몫으로 넘겼다.**

**다음 세션이 할 일 (전부 사용자 결정을 먼저 받을 것 — 근거는 최우선 1 표):**

| # | 무엇 | 어떻게 |
|---|---|---|
| 1 | **11px 회귀 정리** (최우선 1-①) | **→ 9/5 완료** — (b) 그래프 축·원형 배지·초상 안 레벨만 11, 나머지 12, 0.5 단위 4곳 폐지, `custom_values.js` 각주 3줄 12/13/12. 남은 건 실기기 확인(`dogu_template/audit/font-scale-phone-checklist.md` 4.5·10번) |
| 2 | ~~LP 그래프 일반 유저 확장~~ **→ 9/4 「안 한다」** | — |
| 3 | ~~인게임 onclick `"` 이스케이프~~ **→ 9/4 완료 (`202a317`)** | `data-search-name` + 기존 위임으로 2곳 전환 · 같은 자리 `title` 2곳도 `escapeHtml` · 안 부르게 된 `window.searchSummonerFromLive` 정의 제거 |
| 4 | ~~radius 스냅 · 흰색 rgba 3단~~ **→ 9/4 결정 완료** | radius 는 **안 한다** · rgba 는 **border 만 3단으로 완료**(`202a317`). 자세한 건 최우선 1 표 ④⑤ |

**Fable 세션이 맡은 것 — 9/4 에 전부 끝났다:** ~~Railway 오리진 노출 확인·잠금~~(`8db5adb`) · ~~보안 헤더~~(`8dbfe74` — helmet 이 아니라 기본 3개 직접. **CSP 는 인라인 스크립트가 많아 넣지 않았다**) · ~~타임라인 원본 저장~~(`bb58897`) · ~~숙련도 빈 칸~~(`e2f3744`).
**사용자 몫:** ~~NODE_ENV 값~~(**9/4 확인 — 변수 없음.** 최우선 2-① 참조) · ~~LpHist 자정 로그~~(**9/4 DB 실측으로 정상 확인** — 아래 절) · ~~실기기 폰~~(**9/4 둘 다 정상**).

#### ★★ LP 추이 점검 (2026-09-04) — **정상이다. 3점이 맞는 값이다**

사용자가 "9월 1일부터 3점밖에 없다" 며 유실을 의심해서 `lphistories` 를 직접 열어 봤다. **안 잃고 있다.**

- **왜 3점인가**: 잡은 **한국시간 00:02 에 「어제」를 기록**한다. 기능을 만든 게 9/1 이므로 9/2·9/3·9/4 새벽 세 번이 돌아
  **260901 · 260902 · 260903 세 점**이 맞다. 9/5 새벽이면 4점이 된다. 최대 180점(`LP_HIST_MAX`, 스플릿 하나)
- **실측 (2026-09-04)**: 문서 11,580명 · 날짜별 `260901 11,001점 / 260902 11,000 / 260903 11,000`
  · 점 개수 분포 `3점 10,439명 / 2점 543 / 1점 598`. **합이 33,001 로 두 셈이 정확히 맞는다** (누락 0)
- **★★ 하루 11,000명이 정확한 전수다** — 라이엇 실측으로 `challengerleagues 300 + grandmasterleagues 700 +
  masterleagues **10,000**` 이다. **마스터 리그가 정확히 1만에서 막혀 있어서** 합이 항상 딱 11,000 이 된다.
  처음엔 "11,000 이라는 반올림 수가 수상하다" 고 의심했는데 그게 진짜 값이었다
- **★ `ranksnapshots` 의 11,43x 명과 다른 건 정상이다** — 그건 **하루치 합집합**(그날 명단에 한 번이라도 있던 사람)이고,
  LP 잡은 **00:02 그 순간의 명단**을 찍는다. 차이 ~430명은 낮 동안 오르내린 사람들이다
- **1점·2점인 1,141명**은 최근에 마스터+ 로 올라온 사람들이다. 그 전날엔 명단에 없었으니 점이 없는 게 맞다
- **점검 방법 (다음에도 이걸로)**: `lphistories` 를 `$unwind: '$hist'` → `$arrayElemAt: ['$hist', 0]` 으로 묶어
  **날짜별 점 수**를 세면 로그 없이도 잡이 도는지 알 수 있다. Railway 로그는 오래 안 남아 이쪽이 낫다

#### ★★ 오리진 잠금 완료 (2026-09-04, 커밋 `8db5adb`) — 감사 M-7

**Cloudflare 우회 직타가 실제로 뚫렸던 걸 실측 확인하고 공유 시크릿 헤더로 막았다.**

- **구조**: `server.js` 의 `trust proxy` 바로 아래 미들웨어 하나. CF Transform Rule 이 모든 요청에 붙이는
  `x-origin-guard` 헤더를 `crypto.timingSafeEqual` 로 검사해 **CF 를 거친 요청만** 통과. 헤더 없거나 틀리면 403
- **★★ `ORIGIN_GUARD` env 가 있을 때만 켜지는 no-op 설계** — 없으면 완전 통과(로컬·미설정 배포 무영향).
  덕분에 코드를 먼저 배포해도 무변화였고, 배포 순서 사고(전 요청 403)를 원천 차단
- **★ 배포 순서 (다음에 비밀값 로테이션할 때도 동일)**: ① 코드 배포 → ② CF 룰 등록 → ③ 사이트 정상 확인 →
  ④ **마지막에** Railway 변수. Railway 변수를 CF 룰보다 먼저 넣으면 **전 요청 403(사이트 다운)**
- **★ riot.txt 는 `req.path === '/riot.txt'` 로 항상 예외** — 라이엇 심사 봇 경로가 불확실하고 파일 자체가 무해
- **★ 비밀값은 Railway `ORIGIN_GUARD` 와 CF Transform Rule 두 곳에만 있다** (코드·git 엔 없음).
  로테이션은 양쪽 값만 새로 맞추면 된다. **이 값이 이번 세션 대화 로그에 노출됐으니, 찜찜하면 한 번 로테이션할 것**
- **★ CF 최신 UI**: Transform Rules 탭이 없어지고 **Rules → Overview → Create rule → "Request Header Transform Rule"**
  안으로 들어갔다. "All incoming requests" 옵션이 안 보이면 필터 `Hostname equals pixlol.kr` 로 대체. "Set static" 선택
- **★ 실측**: 정상 경로(CF) 200 · 우회 직타(IP 위조) 403 · riot.txt 직타 200. `m9focdi9.up.railway.app` 이 오리진 주소

## ★★★ 2026-09-02 세션 마무리 — 다음 세션이 먼저 볼 것

**로드맵(`260828 LOL 로드맵.txt`)의 Opus 항목을 우선순위 순으로 처리한 세션이다.
커밋 8개 전부 배포·프로덕션 실측 확인했다.** 자세한 내용은 `docs/통계집계.md` 부록의 2026-09-01 절에 있고, 여기는 요약과 남은 것.

| 커밋 | 무엇 | 로드맵 |
|---|---|---|
| `6e4ffd4` | 도감 아이템·룬 채택률 + **`ITEM_TOP_N` 컷 폐지** | A-5 |
| `ff43be7` | 스탯 파편을 격자 칸별 항목으로 · 도감 상세 스크롤 초기화 | 사용자 요청 |
| `650adf4` | 홈 위젯 「라인별 추천 챔피언」 · **A-4 는 접음** | A-6 |
| `004ae1e` | 그 위젯을 5열 × 3줄 가로 일자 카드로 | 사용자 요청 |
| `1405cc5` | 티어 배지를 초상화 왼쪽으로 | 사용자 요청 |
| `2148856` | 챔피언 vs 챔피언 비교 `/versus/<A>/<B>` | A-2 |
| `eca23a7` | vs 확장 — 모든 관계(적·아군·라인쌍) + 빌드 비교 | 사용자 요청 |
| `51b969d` | **vs 빌드를 통계 상세와 같은 컴포넌트로 다시 만듦** | 사용자 지적 |

### 로드맵은 어디까지 왔나

- **A 절(지금 DB 로 되는 것) 중 Opus 항목은 A-8 하나만 남았다** — 「전적 페이지: 최근 같이 한 소환사」
  (★ 하나 · 난이도 하 · 최근 20판 캐시 안에서 세면 끝이라 **라이엇 호출 0**). 착수 전이다
- **A-4(통계 표 전일 대비 ▲▼)는 접었다** — 표본이 못 받친다. 근거와 "다시 꺼낼 조건" 은 `docs/통계집계.md` 부록의 그 절에
- **A-7(장인 랭킹)은 프로덕션 키 이후** (2026-09-01 결정 그대로). B 절 5개도 전부 키 이후
- Fable 몫이던 A-3·A-1·A-9 는 2026-09-01 에 끝났다

### 남은 것 · 판단 대기

- **★ vs 페이지의 룬 격자(`lxRuneBody`)만 「평소 대비」 칸이 없다.** 칸마다 픽률·승률을 두 줄로
  쌓는 자체 렌더라 값을 하나 더 넣을 자리가 없다. 나머지 상자 6개는 전부 붙어 있다.
  넣으려면 격자 구조를 손봐야 한다 — **사용자에게 물어보고 할 것**
- **★ 사용자가 화면을 보고 판단 중인 것들**: 홈 위젯 자리·크기 / vs 페이지 / 도감 채택률 안내 문구 /
  아이템 승률 표본 가드 100판(`USAGE_WR_MIN` — 내가 정한 값이라 되돌릴 수 있다)
- **★ 통계 상세의 "전체 아이템" 꼬리가 얇아졌다** — `ITEM_TOP_N` 컷을 없애서 60칸이 다 차는데,
  카밀 2,868판 기준 **60번째가 8판(0.3%)** 이다. 표본 컷을 넣을지는 판단으로 남겼다
- **★ `public/dogu-header.js`·`dogu-ui.css` 미커밋 변경은 이 세션에서도 손대지 않았다** (다른 세션 몫)

### ★★★ 이번에 걸린 함정 (다음에도 걸린다)

1. **★★★ `String.replace` 의 두 번째 인자에서 `$$` 는 리터럴 `$` 로 치환된다.**
   서버에 aggregate 파이프라인을 넣을 때 **`$$i` 가 전부 `$i` 로 먹혀서** 결과가 조용히 0건이었다.
   **에러가 안 난다** — `$i` 는 그냥 없는 필드라 빈 결과가 나올 뿐이다. 스크립트로는 375판이 나오는데
   서버만 0이라 한참 헤맸다. **긴 코드 블록은 `splice` 로 넣거나 replace 에 함수를 넘길 것**
   (`s.replace(a, () => b)`). 이 저장소의 백틱 함정과 같은 부류다
2. **vs 페이지에서 `loadPerkData()` 를 안 부르면 빌드 상자가 통째로 안 그려진다** —
   `perkIcon`/`itemNameOf` 가 그 표를 읽는데 없으면 예외가 나고 catch 가 삼킨다
3. **CSS `nth-of-type` 은 형제를 **태그로** 센다** — 홈 위젯에서 `.home-tier-row:nth-of-type(n+2)` 로
   2·3위를 숨기려 했더니 `colhead` 가 1번 div 라 **줄이 전부 사라졌다.** 형제 선택자(`~`)로 바꿨다
4. **폰 넘침을 잴 때 `[data-tooltip]` 은 허상이다 (이번이 세 번째)** — 도감 룬 탭에서 46px 이 나오는데
   `.codex-view-toggle` 속성을 떼면 0 이다. 헤드리스가 hover 미디어를 안 바꿔서 생기는 값
5. **로드맵의 "집계 추가 0" 을 믿기 전에 데이터를 재 볼 것** — A-5 는 룬만 맞았고 아이템은
   `ITEM_TOP_N` 컷 때문에 값이 통째로 틀렸다. A-4 는 아예 표본이 못 받쳤다

### 이 세션 기록이 어디 있나 — 전부 `docs/통계집계.md` 부록의 2026-09-01 절 안이다

- 도감 채택률·`ITEM_TOP_N` 폐지 → **「같은 날 넷째」**
- vs 비교 페이지 → **「챔피언 vs 챔피언 맞대결 비교」** 와 그 다음 **「같은 날 확장」** 두 절
- 홈 위젯 → **「홈 위젯 「라인별 추천 챔피언」」**
- A-4 를 접은 근거 → **「로드맵 A-4…는 접었다」**

### 이 세션에서 새로 뚫은 자리 (다음에 재사용할 것)

- 서버 `usagePayload(scope, type, field, keyOf)` + `usageRoute` — 도감 채택률 세 라우트가 한 함수를 탄다
- 서버 `buildOneBuildScope(scopeKey, matchCond, opts)` — `opts.pick`(참가자 한 명만) ·
  `returnDocs`(저장 안 함) · `timeline`(강제). **안 넘기면 예전과 똑같이 돈다**
- 화면 `lxBuildData(builds, pos, baseBuilds)` — 세 번째 인자를 주면 `lxVals3`·`lxM3` 이
  **모든 줄에 「평소 대비」 칸을 자동으로 붙인다**
- 화면 `josa(단어, '을', '를')` — 한국어 조사를 받침 보고 고른다
- 화면 `lxRow` 카드의 `c.attr` — 카드에 그대로 붙는 속성 문자열 (상성 카드의 `data-foe`)

## ★★ 반복 함정 모음 — 여러 세션에서 되풀이해 걸린 것 (전말·근거는 `docs/세션기록.md` 등 각 문서)

**코드 / 서버**

1. **라우터는 항상 두 곳이다** — 진입부 `pathParts` 분기와 `popstate`. 한쪽만 고치면 "주소를 치면 되는데 뒤로가기로 오면 안 되는" 상태. 새 주소 분기엔 `setActiveNav` 도 빠뜨리기 쉽다 (`/ranking`·`/summoner` 에서 두 번)
2. **`String.replace` 둘째 인자에서 `$$` 는 리터럴 `$` 로 치환된다** — aggregate 의 `$$i` 가 전부 `$i` 가 되어 **에러 없이 0건**. 긴 코드 블록은 `splice` 로 넣거나 `s.replace(a, () => b)` 로 함수를 넘길 것 (메모리 `js-replace-dollar-trap`)
3. **bash `node -e "…"` 안의 백틱은 명령 치환으로 먹힌다** (세 번 걸림) — 긴 글은 Write 로 파일에 쓰고 스크립트가 읽어 끼울 것
4. **`NodeCache` 에 `maxKeys` 를 주면 안 된다** — 상한에 닿으면 `set()` 이 ECACHEFULL 을 던져 그 라우트가 500. 키 폭은 입력 검증·정규화로 막는다
5. **`useClones:false` ↔ "캐시에서 꺼낸 객체를 고치면 안 된다" 는 짝이다** — 고쳐 보내야 하면 `{...cached, …}` 복사로
6. **JS 객체 손표(`STAT_MANUAL` 등)에 줄을 더할 땐 키 중복 검사** — 뒤엣것만 남아 기존 값이 조용히 사라진다 (다섯 번 잡힘). 한 줄에 여러 항목이 있으니 줄 중간까지 훑을 것 — 검사 코드는 `docs/스킬데이터.md` 부록
7. **배포하면 부팅 ~90초 뒤 전체 재집계가 돈다** — 배포를 몰아 하면 "통계가 안 뜸". 세대 교체로 증상은 근절했지만 유발 자체는 그대로다
8. **★★ Atlas M0 는 `allowDiskUse(true)` 를 조용히 무시한다** — `$group` 이 100MB 를 넘으면 옵션을 줘도 실패한다. 그룹 수를 줄이는 수밖에 없다(챔피언 묶음으로 갈라 여러 번, `STAT_CHUNKS`). `$facet` 결과 100MB 한도와는 별개다 (9/11 상성 집계, 하루 동안 조용히 실패)
9. **★★★ mongoose 9 는 업데이트에 배열(집계 파이프라인)을 넘길 때 `{ updatePipeline: true }` 를 요구한다** — 8 까지는 그냥 됐다. 안 주면 던져져서 그 잡이 통째로 죽는다 (9/12 다시 훑기가 11,332명을 다 돌고 마지막 한 줄에서 터졌다)
10. **★★ 프로세스 유무로 작업 순서를 잇지 말 것** — "`server.js` 를 인자로 가진 node 가 없으면 시작" 트리거가 다른 세션의 로컬 서버 때문에 8시간을 대기했다 (9/12). **DB 상태로 이을 것** (`finalize_patch.js` 가 그렇게 한다). 두 벌 방지는 heartbeat 잠금으로

**검증 / 측정**

11. **★★★ 용량을 매시 50~05분에 재지 말 것** — 집계 세대 교체(25만 줄 삭제 → 재작성) 한가운데라 **최대 60MB 파여 보인다.** `db.stats()` 와 `collStats` 합이 어긋나면 그 순간이다 (정상이면 소수점까지 같다). `statscopes` 의 `gen`/`genB`/`genM` 이 방금 바뀌었는지 같이 볼 것 (9/12, 두 번 속았다)
12. **감사 보고서·로드맵·계획 파일을 코드보다 먼저 믿지 말 것** — "없다" 는 항목은 grep 한 번 하고 믿는다 (틀린 사례 네 번 이상). 계획 파일에 적힌 절차·추천은 사전 승인도 아니다
13. **헤드리스 폰 넘침의 `[data-tooltip]` 은 허상이다** (세 번 걸림) — 속성을 떼고 다시 잴 것. nowrap 말풍선 가상요소는 opacity 가 아니라 **`display:none`** 으로 숨겨야 진짜 넘침도 안 생긴다
14. **헤드리스 CDP 에서 `/json` 의 첫 타깃을 잡지 말 것** — 확장 배경 페이지나 남의 크롬에 붙는다. `type === 'page' && !url.startsWith('chrome-extension')` + **전용 포트 + `--disable-extensions`**
15. **`taskkill /IM chrome.exe` 금지** — 사용자 크롬까지 죽였다(두 번). 내가 띄운 PID 만 (메모리 `never-kill-user-chrome`)
16. **헤드리스 스크린샷은 절대 경로 + 로딩 대기** — 상대 경로면 에러 없이 파일만 안 생기고, 로딩 중에 찍히면 아이콘이 빈 채 나온다
17. **사용자가 전제를 짚어 오면 대개 그가 맞다** — "그 숫자가 나올 수가 없는데" 류의 지적이 데이터 대조보다 빠르다. 데이터끼리 일치한다는 이유로 정당하다고 단정하지 말 것. **9/12 에 두 번 더 맞았다** — ① "`summonercaches` 지우면 유저 목록 날아가는 거 아니냐" (맞다, 캐시가 아니라 닉네임 사전이었다) ② "495판 받았는데 원본은 373판만 늘었다" (다른 시간대 비교였고, 그걸 따지다 리메이크 걸러짐 0.8% 를 확인했다) ③ "9/1 그마컷이 비상식적으로 낮다" (맞다 — 강등 대기자 한 명에 컷이 끌려가고 있었다, 아래 18번)

18. **★★ 한 사고를 고칠 때 두 가지를 같이 바꾸지 말 것 — 필요 없던 쪽이 새 구멍을 만든다.**
    8/19 컷라인 개편이 **표본 시각**(창의 첫 줄 → 재계산 직후)과 **정의**(등수 → 티어 최저)를 같이 바꿨는데,
    진짜 범인은 시각뿐이었고 정의 변경이 「딱지가 밀린 한 명에 컷이 끌려가는」 구멍을 만들었다.
    그게 9/1 그마컷 1183(진짜 1333)로 터졌다 — 열흘 중 다섯 날에 같은 낙오자가 있었다.
    2026-09-12 에 등수 기준으로 되돌리고 9/1~9/11 을 `lphistories` 로 되찍었다 (`docs/랭킹.md` 의 9/12 절).
    **★ 덤으로 알아낸 것: `lphistories` 는 `[YYMMDD, LP, 티어글자]` 라 과거 명단을 LP 순으로 다시 세울 수 있다 —
    TTL 이 없어 `apexdrifts`(7일)보다 소급 창이 훨씬 길다 (단 9/1 부터).**

**CSS**

19. **`nth-of-type` 은 형제를 태그로 센다** — 클래스로 거를 땐 `~` 형제 선택자
20. **CSS 주석 안의 `*/` 는 주석을 거기서 닫는다** — 선택자 이름을 주석에 적을 때 `*` 뒤 `/` 금지 (뒤 규칙이 통째로 죽는다)
21. **`border-left` 는 `border` 축약 뒤에** — 앞에 쓰면 축약이 네 변을 되돌린다
22. **flex 를 폰에서 column 으로 바꿀 땐 `align-items: stretch` 복귀** — flex-start 가 남으면 자식이 내용 폭으로 벌어져 카드를 뚫는다 (두 번)

## 주소 · 뒤로가기 규칙 (2026-08-17 정비)

**★★ 규칙은 한 줄이다 — "메뉴로 들어오는 첫 걸음만 이력을 쌓고, 그 안에서 움직이는 건 전부
덮어쓴다."**

| 무엇 | 방법 | 주소 |
|---|---|---|
| 메뉴 진입 (전적검색 → 챔피언/도감/신화상점/통계) | **`pushState`** | `/champions` `/codex` `/mythic` |
| 챔피언 바꾸기 | `replaceState` | `/champions/Shen` |
| 챔피언 탭 (스킬·스탯·스킨·배경) | `replaceState` | `/champions/Shen/stats` |
| 스킨 고르기 | `replaceState` | `/champions/Shen/skins/49` |
| 도감 탭 | `replaceState` | `/codex/rune` |
| 신화상점 구획 | `replaceState` | `/mythic/weekly` |

- **★ 왜 덮어쓰나**: 탭 네 개를 훑어본 사람이 페이지를 벗어나려고 **뒤로가기를 네 번**
  눌러야 하는 걸 막는다. **주소는 그대로 바뀌므로 복사해서 보내면 그 자리가 열리는 건 똑같다**
- **★★ 메뉴 진입까지 덮어쓰면 안 된다.** 그러면 `전적검색 → 도감` 하고 뒤로가기를 눌렀을 때
  **사이트를 통째로 벗어난다** (이전 항목을 지워 버리므로)
- **★ 기본 탭(스킬)은 주소에 안 붙인다** — `/champions/Shen` 이 예전 그대로다.
  옛 링크가 살아 있어야 하고, `/champions/Shen/skills` 로 들어와도 그 꼴로 정규화된다
- **★ 스킨은 배열 자리가 아니라 스킨 번호다** (`/skins/49`). 가렌은 11 다음이 **13** 이라
  자리를 담으면 목록이 바뀔 때 엉뚱한 스킨이 열린다
- **★★ 새 주소를 만들 땐 `app.js` 진입부 `pathParts` 분기와 `popstate` 두 곳을 같이 고친다.**
  한쪽만 고치면 "직접 주소를 치면 되는데 뒤로가기로 오면 안 되는" 상태가 된다
- 주소로 들어온 요청은 `window.pendingChampView = {tab, skin}` 에 담아 두고,
  챔피언 상세가 다 그려진 뒤 `applyPendingChampView()` 가 적용한다.
  **신화상점에서 넘어오는 `pendingSkin` 과 같은 함수가 처리한다** — 하는 일이 같아서다
- 탭 버튼은 **글자가 아니라 `data-tab`** 으로 찾는다 (`goChampTab`). 이름이 바뀌어도 안 깨진다
- **★ `og:` 미리보기 카드는 별개다.** 주소를 만든다고 카톡 미리보기가 생기지 않는다 —
  `sendIndexHtml` 이 그 주소일 때 메타 태그를 끼워 넣어야 하고, 그건 서버 작업이다
- 실측(2026-08-17): 홈 → 챔피언 진입 → 탭 3번 → 다른 챔피언까지 가도 **이력은 +1 뿐이고
  뒤로가기 한 번에 홈**이다. `/champions/Garen/skins/13` · `/champions/Ahri/lore` ·
  `/codex/rune` · `/mythic/weekly` 로 바로 들어가면 그 자리가 그대로 열린다.
  신화상점 카드 → `/champions/Shen/skins/49` → 뒤로가기 한 번에 `/mythic/biweekly` 로 복귀

## MongoDB 용량 — 요약 (측정법·계산 근거·이력은 `docs/통계집계.md` 부록)

- **★★ Atlas 무료 512MB 는 `dataSize`(압축 전) 기준이다** — `storageSize` 를 보고 "여유 있다" 고 판단하면 틀린다 (같은 시점에 85 vs 342MB 였다)
- 실측 2026-08-27: **213MB** (데이터 164 + 인덱스 49) · **2026-09-09 에 512MB 를 넘겨 쓰기가 막혔다** (아래)

### ★★★ 2026-09-09 — 용량이 터졌다. 여기서 배운 것 (다음에 또 겪는다)

- **★★★ Atlas 는 「파일」이 아니라 「논리(dataSize + indexSize)」로 센다.** 실측: 클러스터 **파일 합 320MB**인데
  Atlas 는 **516/512MB** 라고 했고, 논리 합(474 + 다른 DB)이 그 값과 맞았다. 그래서 **문서를 지우면 바로 반영된다** —
  `storageSize` 가 작다고 안심하면 안 된다 (2026-08-16 교훈의 재확인)
- **★★ M0 는 `compact` 가 막혀 있다** (`AtlasError CMD_NOT_ALLOWED`). 파일을 줄일 방법이 없으니 **논리를 줄이는 게 유일한 길**이다
- **★★ 쓰기가 막히면 「세대 교체」의 옛 세대 삭제도 막힌다** — champbuilds 가 **424k 줄**까지 부풀어 있었다
  (정상은 212k · 중복 0). 즉 **용량이 찼다는 증상이 용량을 더 먹는다.** 지우기(delete)는 막혀도 되므로 그걸로 푼다
- **★★ `$facet` 은 결과 문서가 100MB 를 넘으면 통째로 실패한다** — 타임라인 집계가 **한동안 조용히 실패**하고 있었고
  (catch 가 로그만 찍는다) champbuilds 에 코어·시작템·스킬순서가 **통째로 없었다.** 실측 104,858,026 / 한도 104,857,600 —
  **426바이트** 차이였다. 아이템 조합 갈래(시작템·초반템 세트·코어 2/3/4/5개)는 판이 쌓일수록 가짓수가 폭발한다.
  → 무거운 「조합」과 가벼운 「낱개」로 **두 번에 나눠** 돌린다. 또 넘치면 heavy 를 반으로 가르면 된다
- **★★ `matchstats.it` 은 집계가 쓰는 구매만 담는다 (2026-09-09 부터)** — 판당 149건 → **80건**.
  남기는 규칙은 **600초 이내이거나 완성 아이템**이고, 그게 집계가 보는 전부다(시작 ≤90초 · 초반 90~600초 · 코어 순서).
  **1코어·2코어·시작템·신발 통계에 손실이 없다.** 이미 쌓인 문서는 `trim_matchstats_it.js` 가 소급한다
- **★ `REBUILD_STATS=1` 은 `updateVersion()` 을 먼저 부른다** — 안 부르면 `currentVersion` 이 부팅 기본값 `16.1.1` 이라
  완성 아이템 목록을 **옛 패치로**(154개 vs 141개) 받아 **틀린 코어 순서 통계가 조용히 덮인다**
- **★★★ 그 소급 정리가 데이터를 다치게 했다 — 상수 하나가 서버와 달라서다.** `trim_matchstats_it.js` 에
  `TL_COMPLETE_GOLD = 1500` 이라고 적었는데 `server.js` 는 **1000** 이다. 그 사이(1000~1499)에
  **2단계 신발이 11종 전부** 들어간다 → 스크립트가 신발을 "완성 아님" 으로 보고 **10분 뒤에 산 신발을 지웠다.**
  48,723판 전체에 걸쳐서다. 증상은 화면에 바로 나왔다 — 신발 줄이 「없음 97%」.
  **되돌릴 수 없다** (라이엇 타임라인을 다시 받지 않는 한).
  → 보완: **신발 통계를 타임라인이 아니라 최종 아이템 6칸(`p[9]`~`p[14]`)에서 센다.** 그 칸은 손댄 적이 없다.
  → 남은 손상: **16.17 패치의 `core`·`set2/4/5` 가 조금 적게 잡힌다** (10분 뒤 신발이 「완성 아이템 n번째」 세는 데서 빠졌다).
    16.18 부터는 `toSlimTimeline` 이 1000 기준으로 담으므로 저절로 낫는다.
  **★ 교훈: 서버와 같아야 하는 상수를 스크립트에 손으로 옮겨 적지 말 것.** 옮겨 적었으면 돌리기 전에 양쪽을 grep 해 대조한다.
- **★★★ 장화를 `Boots` 태그로 판정하면 안 된다 — 라이엇 자료에 구멍이 있다.** 위 신발 줄을 만들다 찾았다.
  · **`3172 건메탈 군화`** 는 3단계 장화(광전사의 군화에서 올라간다)인데 **Boots 태그가 없고** `NonbootsMovement` 만 달려 있다.
    태그로 세면 그 **8,175명(참가자의 1.68%)** 이 「신발 없음」이 되고, 동시에 완성 아이템이라 **코어 줄에도 끼어든다.**
  · 그리고 태그가 맞아도 **1000골드 하한이 장화를 걸렀다** — `3158 아이오니아`(900g · **12.00%**) · `3171 핏빛 명석함`(900g · **4.41%**).
  · 셋을 합쳐 **참가자의 18.1%(88,126명)가 「신발 없음」으로 잘못 세어지고 있었다.**
  → **`1001 장화` 에서 `into` 로 뻗은 계보 전부**를 장화로 본다 (협곡 15종). 실측으로 옛 규칙 11종을 하나도 안 놓치고 4종을 더 잡는다.
    `complete` 는 153개로 **변화 0** 이라 다른 통계에 파장이 없다. 1단계 `1001 장화` 도 넣었다 — 「장화만 신고 끝난 판」은 없음이 아니다.
  **★ 교훈: 아이템 분류를 태그 하나로 하지 말 것.** 계보(`from`/`into`)가 라이엇 자료에서 더 믿을 만하다.
- **★ 아이템 세트(2·3·4·5개)도 신발을 뺀다 (`comp` → `compnb`, 2026-09-09 사용자 결정).** 원래는 「세트는 그 판에 뭘
  갖췄나라 신발도 장비의 하나」로 남겼는데, 신발 줄이 따로 있어 같은 말을 두 번 하고 **위 소급 정리 사고가 `comp` 만**
  **다쳤기 때문**이다. `compnb` 는 그 사고와 무관해서 이 한 줄로 16.17 세트가 정상이 됐다. `comp` 는 쓰는 곳이 없어져 뺐다.
- **★★★ 「N레벨에 도달한 사람만」 모은 통계는 승률이 통째로 부풀어 있다 (2026-09-09).** 사용자가 「리 신 16레벨
  승률이 너무 높다」고 짚어서 재 봤더니 **버그가 아니라 표본이 스스로 치우친 것**이었다 —
  레벨이 높다는 건 그 판을 이기고 있었다는 뜻이다(이긴 팀이 골드·경험치를 더 먹고, 진 팀은 일찍 항복한다).
  실측(리 신 정글 11,993명): **전체 50.70% · 11레벨 도달 53.12% · 16레벨 도달 66.49%**.
  포인트 수별로는 더 극적이다 — 9개 8.86% · 11개 56.46% · 17개 69.95%.
  → **화면에는 아무것도 안 적는다** (2026-09-09 사용자 결정 — 각주를 넣었다가 뺐다). 숫자 자체는 맞는 값이다.
  **★ 교훈: 새 통계를 만들 때 「어디까지 간 사람만」 이라는 조건이 붙으면 승률은 그 조건과 상관관계가 있다**는 걸
  먼저 의심할 것. 「N판 이상」·「M분 이상」·「끝까지 한」 류가 전부 같은 부류다.
- **★★ 「11·16레벨 탭은 픽률 합이 30% 밖에 안 된다」 — 이건 그대로 두기로 했다** (2026-09-09 사용자 결정).
  그 레벨까지 간 사람이 3,909/11,993 뿐이라 그렇다. 한 번 **「도달자 수」를 분모로 바꿨다가**
  (facet 에 `tlord11`·`tlord16` 를 더해 합 30.0% → 91.9% 로 만들었다) **되돌렸다.**
  **★ 이유: 픽률 분모가 줄마다 달라지면 줄끼리 못 견준다.** 지금은 어느 줄이나 `tlall` 하나라
  「전체 판 중 몇 %가 이렇게 갔나」로 읽히고, 그게 다른 상자(아이템·룬)와도 같은 뜻이다.
  **다시 하려면** 커밋 `ad51c81` 에 전부 들어 있다 (서버 facet 2줄 · TL_TYPES · 박제 TYPE_LIST ·
  화면 ARCHIVE_TYPE·LX_TL_TYPES·`tlAt`·`lxDenom`·vs 의 `bTlAt` — **일곱 자리를 같이 고쳐야 한다**).
  ★ 그때 알아낸 것: 도달자 평균 승률은 **줄을 더해서 내면 안 된다** (칸마다 상위 12개만 저장돼 꼬리가 빠진다).
    `grp([])` 가 games 와 wins 를 둘 다 주므로 그 줄을 그대로 쓴다.
- **★ 코어 순서는 「구매 순서」다 — 판 아이템도 그 자리에 남는다** (2026-09-09 사용자 결정: 그대로 두고 각주만).
  `toSlimTimeline` 이 `ITEM_PURCHASED`(+`ITEM_UNDO`)만 담고 **`ITEM_SOLD` 는 안 본다.**
  실측: 코어로 센 아이템이 최종 6칸에 없는 사람 5.58% — 그중 대부분은 판 게 아니라 **변신**이다
  (마나무네→무라마나 12,661 · 대천사의 지팡이→포옹 6,714 · 혹한의 손길→종말의 겨울 1,540). 진짜 판 건 ~1.25%
  (메자이 1,962 · 수호천사 867). **신발·인기·승률·전체 아이템 줄은 최종 6칸 기준이라 이 줄과 답이 다르다** — 각주로 밝혔다.
- **★ `build_stats_archive.js --verify` 에 자리 번호 버그가 있었다** — champbuilds 줄의 games 는 3번인데 2번(type)을 합산해
  **멀쩡한 박제를 「대조 실패」로 막고 있었다.** 고쳤다 (wins 대조도 추가)

**그날 한 일과 결과** (dataSize 399.8 → **285.0MB** · 논리 합 474 → 372MB)

| 순서 | 무엇 | 절감 |
|---|---|---|
| ① | 16.16 패치 집계를 파일로 박제하고 DB 행 삭제 (`build_stats_archive.js`) | 19MB |
| ② | champbuilds 비우기 — **파생이라 재집계로 복구된다** (쓰기 잠금 풀기용) | 53MB |
| ③ | `matchstats.it` 소급 정리 (구매 738만 → 390만) | 88MB |
| ④ | 재집계로 champbuilds 복구 (212,602줄 · 중복 0) | +28MB |

**앞으로 남은 손잡이** — 하루 증가량이 26.5 → 약 19MB 로 줄었지만 패치가 길어지면 또 빠듯하다.
champbuilds 의 `games ≤ 2` 컷(−28% 줄) · champmatchups 꼬리 컷 · summonercaches TTL · matchstats TTL 21 → 16일.
**단 TTL 은 패치 길이(14일)보다 짧으면 안 된다** — 재집계가 그 패치의 앞부분을 잃는다.
- TTL: `matchcaches` **3일** · `matchstats` **21일** · 얼어붙은 패치의 `matchstats` 원본은 그 자리에서 삭제 · 닫힌 패치는 재집계 안 함(`PATCH_FREEZE_DAYS`)이라 박제 마감 없음
- **★★ `matchcaches.timeline`(라이엇 원본 통째, 한 개 617KB)은 저장하지 않는다 (2026-09-04 부터)** — 예전에 287판이 173MB 를 먹었다. `/api/timeline/:matchId` 가 원본을 DB 대신 `myCache`(5분)에 두고, 프론트 `matchTimelineCache` 가 세션 내 재조회를 막는다. `matchstats` 의 `sk`·`it`(판당 2.06KB 슬림, `toSlimTimeline`)은 별개 경로다
- **★ 인덱스는 스키마 선언만으로 안 만들어진다** — 새 인덱스·TTL 은 `ensureStatIndexes()` 의 `want` 배열에 같이 적을 것. 이미 있는 TTL 은 `expires` 만 고쳐선 안 바뀐다 (M0 은 collMod 권한도 없어 지우고 재생성)
- 다음에 빠듯해지면 남은 손잡이는 `matchstats` 단축(재계산 폭이 줄어드는 대가)이나 끝난 패치 집계의 파일 박제(`build_stats_archive.js`)뿐이다 — `matchcaches` 쪽은 이미 다 줄였다

## 챔피언 페이지 · 스킨/배경/대사 탭 → **`docs/챔피언탭.md`**

**문서를 통째로 옮겼다 (2026-08-21, CLAUDE.md 가 한도를 넘어서).** 들어 있는 것 —

- **스킨 탭** — CD `champions/<키>.json` 이 원본이다(스킨 2111개, 경로 누락 0).
  **CD 경로는 전부 소문자로 바꿔야 한다** · 스킨 번호는 연속이 아니다(가렌 11 다음이 13)
- **가격은 CommunityDragon 에 없다 — 롤위키 `Module:SkinData/data` 에서 받는다**
  (`build_skin_prices.js` → `public/skin_prices.js`). **등급으로 가격을 추측하면 안 된다**
- **크로마** — 같은 json 안에 있어 추가 요청 0. 색점은 이미지 없이 그린다. 그림은 3D 렌더라 `contain`
- **★★ 오른쪽 일러스트는 폭이 아니라 높이로 정한다** (`min(378px, 75vh - 300px)`).
  창 높이를 여러 개 재라 — 한 점(900px)에서만 재고 결론지었다가 뒤집혔다
- **배경 탭** — 라이엇 Universe 전문. 챔피언별로 쪼갠 이유(손익분기 140명)와 슬러그 예외(레나타 글라스크)
- **대사 탭은 만들어 두고 가려 놨다** — 한국어 대본이 공개 데이터에 없다. 영문은 롤위키에 다 있다

## 자체 통계 — 챔피언 티어리스트 → **`docs/통계집계.md`**

마스터+ 솔랭 전수로 승률·픽률·밴률을 내는 파이프라인. **문서를 통째로 옮겼다 (2026-08-19,
CLAUDE.md 가 한도를 넘어서). 통계를 건드릴 거면 그 파일을 먼저 읽을 것.** 들어 있는 것 —

- 순회/수집/집계 3단 잡 (`scanMatchlists` · `fetchMatchStats` · `buildChampStats`)과 호출 예산
- **라인 상성/카운터** (`champmatchups`, 2026-08-21 신설) — 같은 라인에서 마주친 두 챔피언의 승률.
  **수집 추가 0** 이고 지난 원본이 소급된다. 실측으로 **라인 짝이 온전한 판이 100.0%** 라 짝 못 짓는 판은 버린다
- 컬렉션 6개 (`matchseens` `ranksnapshots` `matchstats` `champstats` `statscopes` `champbuilds`)
- 슬림 문서 참가자 **28칸 자리표** — 순서를 바꾸면 이미 저장된 문서가 통째로 어긋난다
- **티어 공식(`TIER_COEF` · `TIER_PRIOR` · `TIER_CUTS`, 2026-08-28 부터 OP·1~5)** · 룬 빌드(라인별) · 화면(통계 탭)
- **★★ 패치 원본 박제** — 2026-08-27 부터 닫힌 패치는 재집계를 안 해(`PATCH_FREEZE_DAYS`) **마감이 없어졌다.** 용량용 선택 작업 (`build_stats_archive.js`)
- k 밴드 · 밴 세는 방법 두 가지(`bans` / `banGames`) · 명단 날짜별 스냅샷(`ranksnapshots`)
- **부록 (2026-09-04 이동)**: 통계 상세 페이지·타임라인 수집(08-26) · 수집 방법 점검 7건(08-27) · 티어 개편(08-28) ·
  **통계 화면 기능 전부 — 패치 영향·조합·픽률 추이·LP 추이·도감 채택률·versus·홈 위젯·A-4 접은 근거(09-01)** · MongoDB 용량 상세

자주 쓰는 것만 여기 남긴다:

```
READONLY_JOBS=1 node server.js   # 화면만 볼 때 (순회·수집·집계 잡을 안 띄운다)
REBUILD_STATS=1 node server.js   # 집계만 한 번. ★ 반드시 배포 뒤에 돌릴 것
```

## 신화급 상점 → **`docs/신화상점.md`**

윈도우 로컬 수집기가 롤 클라 화면을 읽어 서버로 보내고, 홈 위젯과 신화상점 탭이 그걸 보여준다.
**문서를 통째로 옮겼다 (2026-08-21, CLAUDE.md 가 한도를 넘어서). 건드릴 거면 그 파일을 먼저 읽을 것.**
들어 있는 것 —

- 서버(`mythicShopSchema` · `POST /api/mythic-shop` · GET 4종)와 화면(`showMythicShop`) 배선
- **검증 규칙** — 구획마다 다르다(일일만 `icon|emote` + 가격 5/25 고정) · 401 `detail` 3종 · 409 중복 처리
- **`items: null` 을 반드시 구분해서 보여줄 것** — 수집이 반자동이라 안 누른 날은 데이터가 아예 없다
- 구획 4개(`featured/biweekly/weekly/daily`)와 `date + section` unique 인덱스
- 카드 그림 규칙(스킨은 가로 판본으로 갈아끼움 · 넥서스 효과는 webm) · 추천 구획 격자
- **Cloudflare 가 기본 User-Agent 를 막는다 (`403 error code: 1010`)** — 새 클라이언트를 붙일 때마다 걸린다

## CD 스킬 데이터 작업 (현재 주 작업) → **`docs/스킬데이터.md`**

스킬 문장·수치 작업 전체. **문서를 통째로 옮겼다 (2026-08-19, CLAUDE.md 가 한도를 넘어서).
패치가 왔거나 스킬 데이터를 건드릴 거면 그 파일을 먼저 읽을 것.** 들어 있는 것 —

- `custom_templates.js`(문장) / `custom_values.js`(수치) 구조와 `MANUAL` · `KEEP_TEXT` 표
- 빌드 스크립트 15개 표와 패치 대응 순서 (아래 요약)
- **다시 알아내지 말 것 목록** — `mStat` 번호표, 배열 시작 인덱스, 사거리 더미(25000 / 20 이하),
  투사체 속도 오염, 위키가 우리보다 틀린 사례, 회색 글씨(`keyTooltipExtendedBelowLine`)
- 스탯 탭 · 레벨 곡선 · 각주(`custom_graphs.js`) · 역할군 평균 · vs 챔피언
- **남은 일 표** (25번 `DD_VER` 하드코딩은 2026-08-24에 끝났고, 20번 박제는 2026-08-27 에 마감이 없어졌다)
- **부록 (2026-09-04 이동)**: 8/23~8/24 전수 정비 세션 넷 (규칙 우선순위·판단 규칙 6개·키 중복 검사 코드) · **16.17 패치 반영 기록**

**★ 패치가 오면 실전은 `node run_patch.js` 한 줄이다** (2026-08-25 신설 — 11단계 + 검증 3종, `--check` 로 CD 가 따라왔는지 먼저 볼 것. 커밋은 사람이 한다).
**★★ 반영 뒤 반드시 공식 패치노트와 대조할 것** — 검증 3종은 "기준선과 같은가" 만 보고 "패치 내용이 제대로 들어왔는가" 는 안 본다.
16.17 때 대조에서 세 자리가 나왔다 (방법·사례는 `docs/스킬데이터.md` 부록의 16.17 절). 아래는 낱개 스크립트 순서 —
**패치 당일에는 한국시간 오후 4시 이후에** — DD 는 수 04시 KST, CD 는 그로부터 12시간 뒤다:

**★★ 버전은 이제 손으로 안 올린다 (2026-08-24).** `dd_version.js` 한 곳이 `versions.json` 의
최신을 받아 오고 넷이 그걸 쓴다 (`build_level_curves` · `build_codex_data` · `build_perk_data` ·
`build_champion_lore`). **옛 버전 주소가 안 죽는 게 문제였다** — `.../cdn/16.16.1/...` 은 패치가
지나도 404 가 아니라 **지난 패치 데이터를 정상 응답으로 준다.** 그래서 안 올리면 스크립트가
멀쩡히 돌고 결과 파일도 만들어지는데 **내용만 옛 패치**였고, 도감·룬은 몇 줄만 바뀌어 diff 로도
안 드러났다.

**★★ 그리고 "오후 4시" 규칙을 코드가 지킨다.** DD·CD 를 둘 다 쓰는 셋(레벨곡선·도감·룬빌드)은
CD 의 `content-metadata.json` 으로 패치를 대조해서 **CD 가 아직 옛 패치면 아예 멈춘다.**
자동으로 최신을 따라가면 그 12시간 창에 돌렸을 때 **DD 만 새 패치인 짝짝이**가 나오는데,
손으로 올리던 시절엔 "아직 안 올렸다" 가 그걸 자연히 막고 있었다. `DD_VER=16.16.1` 처럼
직접 주면 그 값이 이기고 대조도 건너뛴다 (일부러 옛 버전으로 빌드할 때).

```
node build_champion_data.js --refresh     # 문장
node fill_values.js --refresh --write     # 수치 (custom_values.new.js → 확인 후 이름 변경)
node build_level_curves.js --write        # 레벨 곡선
node add_level_graphs.js --write          # 각주 (custom_values.js 를 읽으므로 위가 먼저)
node build_stats_page.js --write          # 스탯 탭
node build_codex_data.js --write          # 도감 탭
node build_perk_data.js --write           # 통계 탭 룬 빌드 아이콘
node build_rune_graphs.js --write        # 도감 룬 레벨 각주 (★ 도감 다음에 — codex_data.js 를 읽는다)
node build_wiki_stats.js --refresh       # 롤위키 우상단 값 → wiki_stats.json (★ fill_values 보다 먼저 — 그게 이걸 읽는다)
```

**★ 받은 뒤 반드시 확인할 것 — "값이 통째로 평평해진 스탯".**
`champion_stats_by_level.json` 에서 스탯별로 "18칸이 전부 같은 값인 챔피언 수" 를 세어
이전 판과 비교한다. 갑자기 173/173 이 되면 그 스탯이 죽은 것이다 —
**압축본(`champion_stats.js`)은 4줄만 바뀌어서 diff 로는 안 보인다.** 자세한 건 위 문서에.
