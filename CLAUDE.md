# pixlol.kr

롤 전적검색 사이트. 한국어로 대화해줘. 평서형 "~다" 말투 말고 "~야/~어" 쪽으로.

## ★ 갈라 둔 문서 (`docs/`)

**이 파일이 한도를 넘어서 큰 절들을 밖으로 뺐다 (2026-08-19 둘 · 2026-08-21 둘). 내용은 안 지웠다.**
아래 네 갈래는 여기 요약만 있으니, 그 일을 할 거면 **파일을 먼저 열어 읽을 것.**

| 파일 | 무엇 | 언제 읽나 |
|---|---|---|
| `docs/스킬데이터.md` | 스킬 문장·수치, 빌드 스크립트, 패치 파이프라인, 스탯/레벨 곡선, 남은 일 표 | **패치가 왔을 때** · 스킬 툴팁·스탯 탭 작업 |
| `docs/통계집계.md` | 통계 수집·집계 3단 잡, 컬렉션 6개, 슬림 문서 28칸, 티어 공식, 룬 빌드, **패치 원본 박제** | 통계 탭·집계·`server.js` 잡 작업 |
| `docs/신화상점.md` | 수집기 규약, 서버 검증·409·인덱스, 구획 4개, 카드 그림 규칙, Cloudflare UA 함정 | 신화상점 탭·수집기 연동 |
| `docs/챔피언탭.md` | 스킨·크로마·가격(롤위키), 배경 이야기(Universe), 대사 탭(비공개) | 챔피언 페이지의 스킬·스탯 **말고** 나머지 |

## 스택 / 배포

- Node/Express + MongoDB Atlas(무료 512MB) + Railway + Cloudflare
- 프론트는 바닐라 JS. `public/` 안에 `app.js` / `index.html` / `style.css`
- `server.js`는 `public/` 밖, 프로젝트 루트에 있음
- GitHub `cosmicday/pixlol.kr` — push하면 Railway가 자동배포
- 로컬은 `node server.js` → localhost:3000

## 프론트 함정

- **`app.js`의 `ddragonVersion`을 서버 응답으로 덮어쓰지 말 것.** 전적검색 응답의
  `version`은 서버 부팅 시점 값이라 클라이언트가 이미 맞춰 둔 최신보다 오래될 수 있고
  (`server.js:70` 기본값 `16.1.1`), 그러면 **챔피언 탭이 구버전 `champion.json`을 읽어
  신규 챔피언이 목록에서 통째로 사라진다.** 로크가 이 경우였다. `compareDdragonVersion`으로
  더 새 버전일 때만 받아들인다. **문자열 비교는 안 된다** — `16.5.1 > 16.15.1`이 돼버림

## 절대 건드리지 말 것

- **`riot.txt`** — 라이엇 도메인 소유 확인 파일. 프로덕션 키 심사 중이라 없으면 문제가 생김
- **`.env`** — API 키. git에 올라가면 안 됨
- 라이엇 프로덕션 키가 Pending Review 상태(2026-03-10 신청).
  **라이엇 개발자 포털의 앱 등록 정보에서 `EDIT` 를 누르지 말 것** — 누르면 심사
  대기열 뒤로 밀린다. **코드 수정이나 배포와는 무관하다.** git push 는 마음껏 해도 된다
  (2026-08-09 확인. 그전엔 "앱을 크게 수정하면" 이라고만 적혀 있어서 코드 변경 얘기로
   잘못 읽혔다)

## 작업 규칙

- **★★ 설명은 전부 맨 아래에 한 번에 적을 것 (2026-08-15).** 도구 호출(파일 수정·명령 실행)을
  먼저 쭉 하고, **내가 읽어야 하는 말은 마지막에 몰아서** 쓴다.
  설명 조금 → 코드 수정 우수수 → 다시 "위에서 설명한 대로…" 식으로 번갈아 나오면
  **읽는 사람이 위아래로 오가야 해서 보기가 너무 힘들다.**
  중간에 꼭 필요한 한 줄(예: "이건 확인이 필요해서 먼저 잽니다") 말고는 아래로 내릴 것
- **파일을 고치기 전에 먼저 읽고 설명해줘.** 내가 납득한 뒤에 수정하는 순서로.
  (위 규칙과 충돌하지 않는다 — **무엇을 왜 고칠지는 미리**, **작업 결과 보고는 맨 아래**다)
- 문제를 알려줄 때는 "문제되는 코드 → 왜 문제인지 → 해결법" 순서로. 코드부터 던지지 말고.
- `git add .`는 2026-08-08에 `.gitignore`를 손봐서 이제 안전해졌어. 루트 `*.md`(CLAUDE.md 제외)·`*.txt`·`*.PNG`·`*.bak-*`가 다 막혀 있음. 그래도 커밋 전에 `git status`로 한 번 볼 것. `public/` 안은 안 막히니 백업 파일을 거기 두면 웹에서 받아짐
- **인게임 확인이 필요하면 체크리스트 파일을 만들어서 넘길 것.** 스킬 설명을 통째로 적게 하지 말고 문장 어느 자리인지 찍어주면 확인이 훨씬 빠름. 확인 항목은 "몇 자리에 영향 있는지" 순으로 정렬할 것 — 스탯 이름 하나가 수십 자리를 좌우함
- `fill_values.js --write` 돌리기 전에 `public/custom_values.js`를 백업해줘.
- **인게임 툴팁 확인은 내가 해.** 수치가 맞는지 추측하지 말고, 확인이 필요하면 나한테 물어봐.

## ★★ 2026-08-28 — 본문 폭을 1200 으로 통일 (디자인 점검 1순위, 사용자 확인 대기)

디자인 점검에서 **페이지마다 통 폭이 960~1400 으로 달라 메뉴를 옮길 때 본문이 좌우로 최대 220px 튀던 것**을 1200 하나로 맞췄다.

| 자리 | 전 → 후 |
|---|---|
| 헤더·푸터·준비중 상자 (`--dogu-wrap`, pixlol `:root` 에서만 덮음) | 1120(내용 1088) → **1232(내용 1200)** |
| 홈 위젯 `.mythic-shop-container` `.patch-note-container` | 1000(내용 960) → 1240(내용 1200) |
| 전적 `#result-container` · 통계 `#stats-container` · 통계 상세 `.lx-page` · 패치노트 · 신화상점 · 장인랭킹 | 1089 / 1000 / 1040 / 1000 / 1100 / 1500 → 1200 |
| 도감 `#codex-container` · 챔피언(index.html 인라인) | 1400 → 1200. 도감 왼쪽 `.codex-left` 740 → 680 (룬 592 · 주문 444 는 그대로) |

- **★ `--dogu-wrap` 은 공통 토큰이지만 pixlol `:root` 에서 덮었다** — 다른 dogu 사이트는 1120 그대로. 브랜드 전체를 맞추려면 원본 기본값을 바꾸고 5곳 sync
- 실측(1600): 11개 페이지 전부 본문 x=193 · 폭 1200, 헤더 내용도 193 시작 — 축이 같다. 도감 아이템 이름 잘림 0/211(칸 230px) · 폰 390 넘침 0 · 전적 카드 729 → 840
- 랭킹은 원래 1200 이라 안 바뀌었다. 도감·챔피언 상세는 200px 좁아졌다 (상세 아이템 500 / 룬 588 / 주문 736, 챔피언 상세 885)

**같은 날 이어서 — 챔피언 페이지 (사용자 요청 넷):** ① 왼쪽 목록 280 → **250** (「트위스티드 페이트」 한 줄 유지 최소, 실측 이름 117px + 여유 8)
② 스킬 P/Q/W/E/R 버튼 96×60 → 84×50 ③ 스탯 그래프 제목 두 줄(`.sgh-main` / `.sgh-title`) ④ **머리글(`#champ-page-header`)을 오른쪽 칸
`.champ-detail-col` 안으로** 옮겨 왼쪽 검색창이 도감 다른 탭처럼 4칸 줄 바로 아래(탭 +14px)에 온다. **★ 함정 둘**: 상세 상자의 `flex: 1` 이
세로 칸 안에선 높이까지 늘려 75vh 를 넘긴다 → `.champ-detail-col > .champ-detail-pane { flex: 0 0 auto }` / 폰은 `.champ-detail-col { display: contents }`
+ 머리글 `order: -1` 로 예전 순서(머리글 → 목록 → 상세)를 유지. 왼쪽 칸 높이는 `calc(75vh + 100px)`(머리글 80 + margin 20)로 아래 끝을 맞췄다.
**전적검색 폭(1089) 되돌리기는 브랜치 `summoner-width-rollback`(ce82209)에 준비만** — 사용자 판단 대기. 합치려면 `git merge summoner-width-rollback`.
- **★ bash `node -e` 안의 백틱 함정에 또 걸렸다** — 이 절을 처음 쓸 때 코드 표기가 전부 빈칸이 돼서 파일로 써서 끼웠다. CLAUDE.md 에 긴 글을 넣을 땐 Write 로 파일부터

## ★★ 2026-08-28 — 챔피언 티어를 lol.ps · 딥롤 기준으로 (S~D → OP · 1~5)

사용자 요청으로 통계 탭 티어 공식을 통째로 바꿨다. **상세는 `docs/통계집계.md` 「티어 공식」 절** — 여기는 요지만.

- `점수 = 1.5 + 0.86·보정승률 + 3.1·ln(1+픽률) + 0.6·ln(1+밴률)` · 컷 `OP 56 / 1 53.5 / 2 50.5 / 3 48.5 / 4 45.5`.
  계수는 **lol.ps 「PS 점수」를 26.16 마스터+ 로 회귀한 것**(R² 0.90). 우리 16.16 에 넣으면 lol.ps 와 티어 일치 59% · ±1 99%
- **★ 핵심 차이는 픽률 비중** — 승률 55% 라도 픽률 0.6% 면 5티어 (두 사이트 다 그렇다). 옛 공식은 S 였다
- **★ 두 사이트 API 주소·티어 코드는 문서에 적어 뒀다** (lol.ps `tier=3` 이 마스터+, `version` 은 versionId). 다음 패치에 다시 대조할 때 그대로 쓰면 된다
- CSS 는 `.tier-op` `.tier-1`~`.tier-5` (옛 `.tier-s`~`e` 는 지웠다 — `showStatsLegacy` 만 참조했다). 상세 페이지 문장·배지도 같이 바뀌었다
- 안 옮긴 것: lol.ps 꿀챔 점수(공식 안 나옴) · OP 의 정확한 판정(점수 56 하나로)

**같은 날 — 공통 헤더 드롭다운이 하위 항목 클릭 뒤 안 닫히던 것 (원본 수정 + 5곳 sync·커밋·푸시).** 열림이 CSS `:hover`/`:focus-within` 뿐이라
SPA 에선 누른 `<a>` 의 포커스가 남아 메뉴가 열린 채였다. 클릭 시 `blur()` + `.dogu-sub-hold`(mouseleave 에 해제). 상세는 `DOGU_UI.md`.
**★ 짧은 페이지(e스포츠·방송)에서 헤더·제목이 7~8px 오른쪽으로 가던 건 세로 스크롤바(15px)가 사라져서였다** — 공통 CSS 에 `html { scrollbar-gutter: stable }` 을 넣어 5곳 다 고쳤다 (같은 날, 사용자 요청). 짧은 페이지 오른쪽에 15px 빈 띠가 남는 게 대가다.

## ★★★ 2026-08-27 저녁 세션 마무리 — 지금 상태와 남은 것

**이 날 저녁에 헤더를 통째로 갈아엎었다. 아래 절들이 그 기록이고, 여기는 "다음 세션이 먼저 알아야 할 것" 만.**
커밋 8개(`43b124e` ~ `839f9fe`) 전부 배포됐고 작업 트리는 깨끗하다.

| 무엇 | 어디 |
|---|---|
| 헤더 = `DOGU_NAV` 한 표 (하위 메뉴 `sub` · 헤더에서만 빼는 `hidden`) | `app.js:540` 근처 |
| 도감 4칸 · 패치노트 2칸 (페이지 안 줄) | `codexTabsHtml()` · `patchTabsHtml()` |
| 패치노트 페이지 | `showPatchNotes()` · 서버 `/api/patch-notes?limit=` |
| PBE 창고 | `pbenotes` + `backfill_pbe_notes.js` |
| 준비 중 페이지 둘 | `showComingPage()` — e스포츠 · 방송 |

**★ 남은 것 (급하지 않다):**

- **e스포츠 · 방송은 빈 페이지다** (「준비 중」). 사용자가 "페이지는 비워놔도 괜찮아" 라고 했다 —
  내용을 채울 땐 `showComingPage()` 대신 각자 `show*` 를 만들면 된다
- **PBE 못 찾은 패치 6개** (25.1·25.2·25.3·25.15·25.20·26.1) — 아카이브 자체가 없다.
  나중에 archive.org 에 스냅샷이 늘면 `node backfill_pbe_notes.js --write` 를 다시 돌리면 된다 (중복은 알아서 걸러진다)
- **다른 4개 사이트(er·maple·loa·tft)에 공통 파일 미커밋 변경이 있다** — `dogu-ui.css` 의 폰 메뉴 스크롤
  (`max-height`/`overflow-y`)인데 **우리가 만든 게 아니라 다른 세션이 원본에 넣은 것**이다. pixlol 은 이미 커밋했고,
  그쪽 레포는 그 세션 몫이다. **손대기 전에 md5 부터 맞춰 볼 것**
- 수집 개선 3건·SQL 전환은 **프로덕션 키 이후** (아래 2026-08-27 낮 절 그대로)

**★ 오늘 되풀이해서 걸린 함정 셋 (다음에도 걸린다):**

1. **라우터는 항상 두 곳이다** — 진입부 `pathParts` 분기와 `popstate`. 오늘 새 주소를 넷 만들면서
   매번 두 곳을 같이 고쳤다. 한쪽만 고치면 "주소를 치면 되는데 뒤로가기로 오면 안 되는" 상태가 된다
2. **`DOGU_NAV` 에서 줄을 지우면 라우팅이 깨진다** — 헤더에 안 보이게 하는 건 `hidden: true` 다.
   `doguRoute` 의 폴백이 `|| DOGU_NAV[0]` 이라 로고(`/`)가 엉뚱한 탭으로 간다
3. **bash `node -e "…"` 안의 백틱은 명령 치환으로 먹힌다.** 오늘 CLAUDE.md 두 줄이 빈칸이 돼서 되살렸다.
   **긴 글은 파일에 써 두고 스크립트가 읽어 끼울 것** (이 저장소가 예전에 겪은 그 함정 그대로다)

## ★★ 헤더 메뉴 개편 (2026-08-27) — 도감 하위 메뉴 · e스포츠 · 방송

**지금 헤더는 이렇다** (같은 날 저녁에 전적검색이 빠지고 패치노트가 들어와 일곱 칸이다):

```
랭킹 · 통계 · 패치노트▾ · 도감▾ · 신화상점 · e스포츠 · 방송
              └ 본서버   └ 챔피언 · 아이템 · 룬 · 소환사 주문
                PBE
```

옛 「챔피언」 탭과 도감 안 탭 3개가 **도감 하위 4개**로 합쳐졌다 (사용자 요청).

- **★★ 하위 메뉴는 공통 파일 기능이다** — `nav[].sub = [{key,label,href}]`. 원본(`dogu_template/dogu-ui/`)을 고쳐 5곳에 sync 했다.
  **`sub` 를 안 넘기는 사이트는 마크업이 한 글자도 안 바뀐다** (er·maple·loa·tft 무영향, 실측 확인)
- **★ 도감 부모는 `href` 가 없다 → 링크가 아니라 `<span>` 이라 눌러도 아무 데도 안 간다** (사용자 결정).
  공통 JS 가 `span.dogu-nav-parent` 의 클릭을 삼킨다 — 안 그러면 **폰에서 라벨을 누르는 것만으로 메뉴가 닫힌다**
- **★★ `.dogu-nav` 가 `overflow-x: auto` 라 드롭다운이 그 안에서 잘린다.** 하위 메뉴가 있을 때만
  `.dogu-nav.has-sub { overflow: visible }` 로 푼다 (안 쓰는 사이트는 네비 가로 스크롤 그대로).
  pixlol 네비는 일곱 칸이어도 **457px** 라 769px 폭에서도 안 넘친다 (실측)
- **★ `DOGU_NAV_FLAT` 을 같이 본다** — `doguRoute()`·`setActiveNav()` 가 `DOGU_NAV` 만 훑으면 하위 4개를 못 찾는다.
  활성 표시는 공통 `setActiveNav` 가 **하위가 켜지면 부모도 같이** 켠다
- **★ 페이지 안 4칸 줄은 `codexTabsHtml()` 하나다** (도감·챔피언 두 페이지가 같은 줄을 쓴다, 사용자 결정).
  클릭은 **위임 리스너 하나**(`goCodexTab`)가 받고 **이력은 덮어쓴다**(replaceState) — 주소 규칙 그대로다.
  헤더 드롭다운으로 들어오는 건 메뉴 진입이라 각 `show*` 가 pushState 한다
- **★ 라우터를 네 곳 고쳤다** — 진입부·popstate 각각의 `/codex`(탭별 navId = `codexNavId()`)와 `/esports`·`/broadcast`.
  **한쪽만 고치면 "주소를 치면 되는데 뒤로가기로 오면 안 되는" 상태**가 되는 그 함정
- **★ 도감 주소가 이제 항상 탭까지 담는다** (`/codex` 로 들어와도 `/codex/item` 으로). 옛 `/codex` 링크는 그대로 열린다
- **★ `codexLastPick` 을 모듈 자리로 뺐다** — 탭이 페이지가 되면서 `showCodex` 안의 지역 표는 매번 비어,
  "탭으로 돌아오면 보던 걸 다시 연다" 가 죽는다
- **★★ 폰 햄버거 메뉴가 원래 반투명이라 뒤 페이지 글자와 겹쳐 읽혔다** (개편 전부터 그랬다 — 옛 판 스크린샷으로 확인).
  항목이 6 → 11개로 늘며 심해져서, 공통 CSS 에 **`--dogu-gnb-panel`** 변수를 뚫고 pixlol 이 `#16112a` 로 덮는다
  (기본값은 예전 반투명이라 다른 사이트는 그대로). **`--dogu-bg-pos` 처럼 사이트가 덮으라고 연 변수다**
- **★ e스포츠·방송은 준비 중 페이지다** — `showComingPage()` 하나가 제목 + 공통 `comingSoonHtml()` 을 그린다.
  상자는 `index.html` 의 `#esports-container`·`#broadcast-container`(`.page-container.dogu-doc`).
  진짜 내용을 채울 땐 그 함수 대신 각자 `show*` 를 쓰면 된다

**같은 날 또 이어서 — 전적검색 탭 제거 · 패치노트 메뉴 신설:**

헤더가 `랭킹 · 통계 · **패치노트▾**(본서버/PBE) · 도감▾ · 신화상점 · e스포츠 · 방송` 이 됐다.

- **★★ 전적검색 탭은 `hidden: true` 로 헤더에서만 뺐다 — `DOGU_NAV` 에서 지우면 안 된다.**
  로고·⌂ 의 `href '/'` 와 `/summoner` 의 `setActiveNav('nav-search')` 가 그 줄을 찾는다.
  지우면 `doguRoute` 의 폴백(`|| DOGU_NAV[0]`)이 걸려 **로고를 눌렀을 때 랭킹으로 간다.**
  `mountHeader` 로 넘길 때만 `filter(n => !n.hidden)` 로 거른다 (홈은 로고·⌂ 로 간다 — 실측 확인)
- **패치노트는 도감과 같은 꼴의 부모다** (href 없음 → 눌러도 안 움직이고 하위만 연다).
  `/patch/live` · `/patch/pbe` 이고 `/patch` 는 본서버로 간다. 페이지 안 2칸 줄은 `patchTabsHtml()` —
  **도감과 같은 `.codex-tabs` 클래스**를 쓴다 (모양이 같아야 메뉴를 옮겨도 안 튄다)
- **★★ 본문은 안 들여온다 — 목록만이다.** 제목을 누르면 공식 페이지 / x.com 으로 나간다.
  공식 패치노트에는 상향·하향 마커가 없어서 옮겨 오면 `10 → 8` 의 방향을 우리가 추측하게 된다
  (`dogu_er` 의 패치 페이지가 같은 이유로 목록만 싣는다 — 그쪽 주석에 근거가 있다)
- **서버 `/api/patch-notes` 를 넓혔다** (수집 방식은 그대로다):
  · `official` 5건 → **목록 전부(172건)** + `thumb` · `desc` · `version` 필드 (`__NEXT_DATA__` 에 다 있다 — 요청은 그대로 한 번이다)
  · **`PATCH_LIST_MAX` 200 — 공식 목록이 주는 172건이 전부 담긴다** (9.1 / 2019-01 까지. 2026-08-27 실측).
    그 페이지의 「더 보기」 버튼은 `pager.more.action.url` 이 **`#`** 이라 **서버에 더 물어볼 곳이 없다** — 172가 원본의 끝이다
  · **PBE 는 `pbenotes` 창고에서 나간다 (2026-08-27 신설).** RSS 는 최근 20글만 줘서 그것만 쓰면 8건이 한계다 —
    30분마다 읽는 김에 `savePbeNotes()` 가 upsert 하고, 화면에는 `loadPbeNotes()` 가 창고를 날짜순으로 준다.
    **창고가 비었거나 조회가 실패하면 RSS 값을 그대로 쓴다** (한 줄이라도 나가게)

#### ★★ PBE 옛 글 백필 — 웨이백 + `tweet-result` (2026-08-27, 59건 넣었다)

`backfill_pbe_notes.js` 하나다. **한 번만 도는 작업이고 서버는 이 길을 안 쓴다.**

① **웨이백 CDX** 로 `RiotPhroxzon` 의 아카이브된 트윗 주소를 모은다 (실측 **783개**, 2017~2026)
② **`cdn.syndication.twimg.com/tweet-result?id=…` 가 인증 없이 본문을 준다** (774/783 성공. `publish.twitter.com/oembed` 도 된다)
③ 첫 줄이 `Patch NN.NN … Preview` 인 것만 골라 upsert → **59건 · 패치 37종 · 2024-10-01(14.20) ~ 2026-08-19(26.17)**

- **못 찾은 패치 6개**: 25.1 · 25.2 · 25.3 · 25.15 · 25.20 · 26.1 — **아무도 그 트윗을 아카이브 안 한 것**이라 방법이 없다
- **★ 번호 표기가 섞여 있다** — 2024~2025 초는 옛 표기(`14.20` · `15.17`), 그 뒤가 `25.x` · `26.x`.
  그래서 **목록 정렬은 패치 번호가 아니라 날짜 기준**이다 (문자열로 정렬하면 15.17 이 25.x 사이에 끼어든다)
- **★ 용량은 무시해도 된다** — 59행에 데이터 9KB · 인덱스 60KB (실측). TTL 없이 계속 쌓는다
- **★★ 안 되는 길 (다시 시도하지 말 것)**:
  니터·xcancel **프로필 HTML / 검색**(빈 페이지·403, poast·privacydev 는 도메인 자체가 없어졌다) ·
  `syndication.twitter.com/srv/timeline-profile`(**2025-11-17 에 얼어붙음** — 100글 중 Preview 14건) ·
  웨이백에 저장된 **트윗 페이지 자체**("JavaScript is not available" 벽이라 본문이 없다) ·
  레딧 검색 API(인증 없이는 결과 0)
- **★ `tweet-result` 는 문서에 없는 뒷문이다.** 백필 한 번에만 쓰므로 막혀도 화면·서버는 그대로다
  · **★ `limit` 파라미터** — 홈 위젯은 기본 5, 패치노트 탭만 `?limit=200`(=전부).
    **캐시에는 항상 통째로 담고 응답에서만 자른다** — 안 그러면 홈이 먼저 열렸을 때 탭이 5건짜리 캐시를 물려받는다
  · **★ 썸네일은 sanity CDN 의 `&w=320` 을 붙여서 준다** — 원본이 1920x1080 **313KB** 라 그냥 걸면 목록 한 장에 30MB 다 (320px = 14KB, 실측)
- 실측: `/patch/live` 100줄 · `/patch/pbe` 8줄 · 헤더 활성이 `패치노트 + 하위` 둘 다 · 홈 위젯은 그대로 5+5 ·
  폰 390 넘침 0 · ⌂·로고 클릭 → 홈 · 콘솔 오류 0

**같은 날 이어서 (사용자 요청 3건):**

- **홈 「오늘의 신화 상점」 제목이 신화상점 탭 링크다** — `<a href="/mythic" data-link>` 하나다.
  공통 헤더와 같은 `data-link` 규약이라 `mountDoguUI` 의 위임 리스너가 `doguRoute()` 로 보낸다 (새 배선 0)
- **도감 상단 제목 줄(`도감` + `소환사의 협곡 기준 · 아이템 211개`)을 없앴다.** 4칸 줄이 곧 제목이다.
  `.codex-header`·`.codex-sub` CSS 와 `codex-count` 갱신 코드도 같이 지웠다 (**개수 표시가 사라졌다** —
  아이템 탭은 왼쪽 스탯 목록에 칸별 개수가 그대로 있다)
- **★★ 도감 통 폭을 1400 으로 고정했다** — 탭마다 통을 줄이던 것(1400 / 1164 / 1026)을 없앴다.
  가운데 정렬이라 **탭을 옮길 때마다 탭 줄·검색창·거르는 버튼이 좌우로 최대 187px 씩 움직였다.**
  남는 폭은 오른쪽 상세가 받는다(`flex: 1`): 아이템 640 / 룬 788 / 주문 936. 왼쪽 칸(740/592/444)은 그대로
- **★ 같이 맞춘 것 둘 — 안 맞추면 챔피언 탭에서만 줄이 튄다**:
  ① 챔피언 통을 **1500 → 1400** (index.html 인라인. 챔피언 상세가 1185 → 1085 로 100px 좁아진다)
  ② 챔피언 페이지의 4칸 줄을 **`#champ-page-header` 위로** 올렸다 —
     그 머리글은 챔피언을 고르면 초상화·이름으로 **통째로 바뀌는 자리**라 지울 수 없다
- 실측: 네 메뉴(챔피언·아이템·룬·주문) 모두 탭 줄이 **x=0 · y=149 · 폭 1385** 로 같다 (1400 창).
  폰 390 도 셋 다 `x=10 · y=73 · 370` · 넘침 0. 홈 제목 클릭 → `/mythic` · 신화상점 활성 · 콘솔 오류 0
- 실측(헤드리스): 데스크톱 1400/1024/800/769 넘침 0 · 드롭다운 148x146 이 네비 아래(y=108)에 정확히 · 폰 390 넘침 0 ·
  4칸 줄이 폰에서도 한 줄(370px) · 도감·챔피언·아이템·룬·주문·e스포츠·방송 일곱 주소 모두 활성 표시가 맞고 콘솔 오류 0

## ★★ dogu.gg 공통 UI 적용 (2026-08-22 · 커밋·배포됨)

`DOGU_UI_PLAN.md` 2단계의 pixlol 몫. **`public/dogu-ui.css` · `public/dogu-header.js` 는 복사본이라
여기서 고치지 말 것** (원본 `dogu_template/dogu-ui/`, 손대기 전 5곳 md5 확인 — `DOGU_UI.md` 11-0). 사양은 `dogu_template/dogu-ui/DOGU_UI.md`. 데이터 로직은 안 건드렸다.

- **`#hero` 는 `main` 밖(헤더와 `main` 사이)에 있다.** `.main-search-section` 안에 두면 모바일 좌우 28px 패딩 때문에
  검색창이 552px 로 좁아져 다른 사이트와 어긋났다 (실측 후 옮김). 홈이 아닐 땐 공통 CSS 가 `body.dogu-home` 기준으로 숨기므로
  `hideAllContainers()`/`goLobby()` 의 `setHome()` 호출이 곧 히어로 표시 조건이다

- **헤더·히어로 검색창·푸터는 전부 `app.js` 의 `mountDoguUI()` 가 그린다** (`index.html` 에 마크업 없음).
  탭은 `DOGU_NAV` 한 표다 (2026-08-27 부터 하위 메뉴 `sub` 와 `hidden` 이 붙었다 — 위 "헤더 메뉴 개편" 절).
  비공개 탭(장인랭킹·클래식)을 되살릴 땐 여기 줄을 더한다
- **★★ 홈에서는 ⌂ 에 밑줄이 켜진다 (2026-08-27).** 예전엔 홈에서 `전적검색` 탭에 밑줄이 남아 **다른 4개 사이트(홈에서 아무 데도 안 켜짐)와 어긋났다.**
  공통 CSS 가 `body.dogu-home .dogu-nav-home` 으로 켜므로 **5개 사이트 전부 자동**이고 사이트 JS 는 손댈 게 없다 (원본 수정 + sync).
  pixlol 쪽은 탭을 끄는 일만 한다 — `mountHeader` 의 `active: false` · `goLobby()` 의 `setActiveNav(null)` ·
  `doguRoute()` 에서 **전적검색 탭만 setActiveNav 를 건너뛴다**(그 탭의 go() 가 goLobby 라 홈으로 간다).
  **★ 그 대신 라우터 `/summoner` 분기에 `setActiveNav('nav-search')` 를 새로 넣어야 했다** — 예전엔 mountHeader 의 기본 active 가 켜 주던 자리다 (/ranking 과 같은 함정).
  폰(≤768px)은 ⌂ 를 숨기므로 홈에서 밑줄이 아무 데도 없다 (실측 390px: 넘침 0)
- **★ 라우터 연결 두 곳**: `hideAllContainers()` 가 `DoguUI.setHome(false)`, `goLobby()`/진입부 `/` 가
  `setHome(true)`. `setActiveNav('nav-xxx')` 가 `DoguUI.setActiveNav('xxx')` + `document.title`
  (고정 `PIXLOL.KR: 리그오브레전드` — index.html `<title>` 과 같은 값) 을 맡는다. 옛 `.bg-solid` · 햄버거(`toggleMobileMenu`) 는 지웠다
- **★ 검색 입력 id 는 `dogu-search-input`** (옛 `summoner-input` 21곳 치환). `search-btn.click()` 9곳은
  `executeSearch()` 로. 1단 헤더 검색창도 값을 히어로 칸에 넣고 `executeSearch()` 를 탄다 —
  **executeSearch 가 그 칸을 읽는 규약**이라 헤더 검색을 따로 만들지 않았다
- 즐겨찾기/최근은 `getFavorites/removeFavorite/getRecents/removeRecentSearch` 를 공통 드롭다운에
  그대로 꽂았다. `renderDropdownList()` 는 `DoguUI.refreshDropdown()` 한 줄이다
- 자동완성(`#autocomplete-dropdown`)은 pixlol 고유라 남기고 마운트 뒤 `.dogu-search-wrapper` 안으로
  옮긴다 (`.ac-dropdown` 이 옛 `.search-dropdown` 의 자리 속성을 물려받았다)
- **★★ CSS 주석에 `.nav-*/` 처럼 `*/` 가 들어가면 주석이 거기서 닫혀 뒤 규칙이 통째로 죽는다.**
  실제로 한 번 그랬다 (홈 위젯이 가로로 눕는 증상) — 선택자 이름을 주석에 적을 땐 `*` 뒤에 `/` 금지
- **★★ 폰 배경 초점은 `--dogu-bg-pos: 75% center` 다 (2026-08-25).** 룰루 스플래시(1215x717)는
  **인물이 가로 62~90%** 에 있는데, 폰은 세로가 길어 `cover` 가 세로에 맞추면서 가로를 크게 잘라낸다 —
  기본값 `center` 로는 **원본의 36~64% 구간만** 보여 룰루가 화면 밖이었다 (배경이 흐릿한 보라 덩어리로만 보였다).
  75% 면 폰 세 폭(360·390·414) 모두 **55~82%** 가 보여 얼굴이 들어온다. 768px 은 42~86%.
  **데스크톱은 안 건드린다** — 가로가 넓어 4~96% 가 보이므로 인물이 이미 나온다.
  **★ 이건 공통 CSS 가 사이트더러 덮어쓰라고 열어 둔 변수라 규칙 위반이 아니다**
  (dogu-ui.css 주석: "일러스트마다 인물 위치가 달라 사이트가 덮어쓴다"). `.dogu-*` 를 건드리는 게 아니다
- **일부러 안 맞춘 것**: 로고·⌂·푸터 홈 링크는 `https://pixlol.kr` 가 아니라 `/` 다 (같은 곳인데
  절대 주소면 로컬에서 프로덕션으로 튄다). `body` 글자색은 공통 `#e6eefb` 가 이긴다 (옛 `#fff`).
  오버레이 색은 옛 값 `11,11,20` 을 변수로 넘겼다 (er 남색 `6,11,26` 아님)
- **게임 스위처 아이콘은 공통 파일이 그린다** — `mountHeader` 에 `iconBase: '/'` 만 넘긴다 (`public/header_<key>.png` 5장).
  한때 `app.js` 의 `decorateSwitcherIcons()` 가 DOM 을 덧칠했는데 아이콘 크기·간격이 다른 사이트와 어긋나서 뺐다 (2026-08-22).
  **`.dogu-*` 안쪽을 사이트 JS 로 덧칠하지 말 것**
- 실측(헤드리스 1400/390): 홈↔랭킹 오버레이 0.52↔0.8 전환 · 폰 넘침 0 · 전적 갱신 버튼은
  `.refresh-btn` 으로 분리(옛 `.search-btn` 그라데이션을 빌려 쓰고 있었다)

## ★★ 2026-08-24 세션 — 위키 값 재점검·표시 다듬기 (커밋 9개, 전부 배포 확인)

어제 채운 위키 값을 **사용자가 세 건 지적**(렝가 R 중복 · 누누 W 130 · 룰루 P 900~2600)했는데, 셋이 전부
**"위키 칸에 값이 여러 개인데 첫 숫자만 잘라 옴 / 사거리와 같은 값 / 툴팁에 뜻이 붙은 값"** 세 부류였다.
그래서 그 부류 전체(①223+②58+③278자리)를 위키 툴팁 보존판으로 다시 뽑아 전수 훑고 63자리를 고쳤다.
**같은 지적이 또 오면 개별 수정으로 끝내지 말고 부류를 찾아 전수로 돌 것** — 이번 세션의 교훈이다.

- **★★ 자기 중심 스킬의 "사거리 = 효과 범위" 중복 제거 규칙** (`selfRadiusDup`, fill_values.js):
  위키에 target range 가 없고 효과 범위가 bin 사거리와 같으면 그 숫자는 자기 주변 반경이다 —
  사거리 칸을 빼고 효과 범위만 남긴다 (가렌 E·다이애나 R·워윅 W 등 55자리)
- **★ 위키 값의 함정 셋**: 시야 반경이 효과 범위 칸에 온다(아크샨 Q·나피리 R·스몰더 R·야스오 W — 뺐다) ·
  돌진/밀치기 속도가 speed 칸에 온다(크산테 W/R·람머스 R·트리스타나 W — 뺐거나 돌진으로 옮김) ·
  여러 값 중 첫 숫자가 대표값이 아닐 수 있다(탈론 W 3000은 돌아오는 단검 → 2571, 사미라 P 2050은 돌진 → 2800)
- **★★ 우상단 값의 긴 괄호는 각주로 접는다** (`foldParenNote`, 9자리 — 야스오/요네 공속 공식 등).
  남기는 괄호 셋: `+`로 시작(계수) · `…에 따라`로 끝(값의 정체) · 7자 미만 + `재사용 N초`.
  **각주 HTML 은 홑따옴표만** (기존 규칙 그대로)
- **쿨타임·소모값이 `-` 면 줄을 숨긴다** (app.js `metaLine`). **`없음` 은 유지** — 클라가 일부러 적는 문장
  (바이 W 등 5자리). 값이 `재충전 대기시간…` 문장이면 **"쿨타임" 라벨도 뺀다** (겹말)
- **우상단 순서 = 공간 → 시간**: 사거리 · 효과 범위 · 스킬 폭 · 시전시간 · 투사체 · 돌진 (`STAT_ORDER` 와
  `put` 호출 순서 두 곳 — 같이 고칠 것)
- **★★ 시연 영상 호스트를 `lol.dyn.riotcdn.net` 으로** (라이엇 공식 페이지가 쓰는 곳). 옛 cloudfront 는
  갱신이 멈춘 사본 — 쉬바나 2026-03 리워크판·로크/신 짜오 영상 10개가 새 호스트에만 있다 (ETag 865건 대조).
  케넨 등 ~50명 영상이 2016년판인 건 **라이엇에도 더 새 게 없는 것** — VFX 업데이트가 오면 재업로드된다
- 개별 확인: 그웬 R 폭 `240 ~ 30 (거리에 따라)` 는 날아가며 좁아지는 바늘(오타 아님) · 갱플 E 재사용 0.5초를
  롤위키에서 보충 · 야스오/요네 공속 비례 쿨타임·시전시간은 CD_MANUAL+STAT_MANUAL (클라는 수치를 안 준다)


### 인게임 확인 결과 (2026-08-24 저녁) — **사거리 3자리 정정, 부류 하나 발견**

사용자가 연습 모드에서 사거리 표시원을 **같은 챔피언의 다른 스킬과 비교**해 8자리를 확인했다.
**밀리오 W·초가스 W·오른 E·탈론 W·볼리베어 R 은 우리 값이 맞았고**(8/23 에 위키로 갈아 끼운 게 옳았다),
셋이 틀렸다:

- **★★ 사이온 Q `300 ~ 600` → `500 ~ 850` (나무위키 → 롤위키로 뒤집혔다).**
  8/23 에 "롤위키 500~850 은 폭을 더한 값으로 보인다" 며 나무위키를 골랐는데 **그 추측이 틀렸다.**
  인게임에서 최대 충전 표시가 **사이온 E(800) 와 거의 같았고**, 롤위키 원문에도 충전 시간별 표가
  그대로 있다 (`{{pp|채널 시간|500;500;675;762.5;850|0~1초}}`). **"위키가 폭을 더했겠거니" 하고
  추측으로 값을 고르지 말 것** — 원문을 열면 근거가 적혀 있다
- **★★ 칼리스타 R 은 사거리가 둘이다** → `1000 (던지는 거리 1200)`. 본체 `KalistaRx` 의
  castRange·override 가 **둘 다 1000**(R1, 아군을 끌어당김)이고, `KalistaRAllyStun` 이 **1200**
  (R2, 그 아군을 던짐)이다. **위키가 틀린 게 아니라 다른 걸 재고 있었다** — 사용자가
  "R1은 E(1000)랑 같고 던지는 건 1200 같은데" 라고 정확히 짚었다
- **★★ 사이온 E → `800 (밀려난 유닛은 최대 1350까지)`.** 롤위키 `range` 칸에 값이 **둘**인데
  (`800 Roar missile range` · `1350 Maximum minion projectile range`) 우리가 **첫 값만** 가져오고 있었다

**★★★ 그리고 이게 부류였다 — `range` 칸에 값이 둘 이상인 자리가 26곳이다.**
`.cache/lolwiki` 의 원본 wikitext 에서 `{{tt|숫자|설명}}` 을 세어 전수로 뽑았다. 성격이 갈린다:

| 종류 | 예 |
|---|---|
| **둘째 값이 진짜 별개 사거리** (적을 가치 큼) | **야스오 Q 475 / 1150(Q3 회오리)** · **요네 Q 450 / 1050(Q3)** · **크산테 Q 465 / 930(강화)** · 사이온 E · 나르 Q 1100 / 3000(돌아오는 부메랑) |
| 재사용·2단이 따로 | 레나타 Q 900 / 275(재사용 던지기) · 리산드라 Q 825 / 950(강화) |
| 지형 넘을 때 늘어남 | 야스오 E 475 / 625 · 오로라 W 300 / 450 |
| 미미하거나 같은 값 | 탈론 W 900 / 850(양옆 단검) · 조이 Q 800 / 800 · 신 짜오 W 1000 / 940 |

- **★ 아직 사이온 E 하나만 반영했다.** 나머지 25자리는 **인게임 확인 없이 손대지 않았다** —
  둘째 값이 "사거리" 인지 "돌진 거리" 인지 "이펙트 길이" 인지가 자리마다 다르다.
  **다음에 이 얘기가 나오면 야스오/요네 Q3 부터 볼 것** (회오리는 체감이 확실히 다르다)
- 뽑는 법: `.cache/lolwiki/<챔피언>.json` 의 `|range =` 줄에서 `{{tt|값|설명}}` 을 전부 모은다

**★★ 피오라 W 는 우리 값(750)이 맞다 — 롤위키 900 이 틀렸다.** 눈으로 애매해서 사용자가
스크린샷 두 장(`fioraW.png`·`fioraR.png`, 루트·gitignore)을 줬고 **픽셀로 쟀다:**

- R 사거리 표시원을 **타원 피팅**해 축척을 얻는다 (R 사거리 500 = 가로 반경 336.9px → **1유닛 0.674px**).
  **롤 카메라는 세로가 0.83 배로 눌린다** — 원이 타원으로 그려지므로 세로를 따로 환산해야 한다
- W 검 궤적의 최원점 → **746 유닛** (상위 1% 평균 740). 750 과 사실상 일치하고 900 과는 20% 벌어진다
- **★ 전제 검증까지 했다** — 두 장에서 피오라 체력바 픽셀이 **673개·중심 (882.2, 354.1) 로 완전히 동일**해
  같은 자리에서 찍은 게 확인됐다. 안 그러면 축척을 옮겨 쓸 수 없다
- **★ 이 방법은 재사용할 수 있다.** 사거리 원이 나오는 스킬 한 장 + 재고 싶은 스킬 한 장이면 된다.
  PNG 디코더는 `zlib` 만으로 60줄이라 저장소에 안 넣었다 (파비콘 때와 같은 판단)
- **★ 자기 자신에게 쓰는 스킬은 사거리 원이 안 나온다** (피오라 W 가 그랬다) — 그럴 땐
  시전 이펙트의 최원점을 재는 수밖에 없고, 그건 실제 판정 거리와 다를 수 있다

- **★★ 트위스티드 페이트 Q 투사체 속도 `1450.4` → `1000` (롤위키).** **본체 `missileSpeed` 가 틀값인
  자리인데 틀값 목록(`1200 / 902 / 828.5 / 779.9 / 8700`)에 없었다** — 그 목록으로 거르는 방식의
  한계를 보여주는 첫 사례다. TF 기본 공격은 1500 이라 "기본공격 값이 새어든 것" 도 아니었다.
  **★ 검산법이 좋았다: 스탑워치 실측에 시전시간을 더해서 맞춘다.** 사거리 1450 · 시전시간 0.25 일 때
  `1000 → 1.70초` / `1300(나무위키) → 1.37초` / `1450.4 → 1.25초` 인데 **실측이 1.7초**였다.
  스탑워치는 오차가 크지만 **1.25 와 1.70 은 안 헷갈린다** — 후보 간격이 벌어져 있으면 이 방법이 통한다

- **★ 야스오 Q `475 (회오리 1150)` · 요네 Q `450 (회오리 1050)`** — 위 26자리 부류 중 둘을 반영했다.
  Q3(회오리)은 평타 Q 와 사거리가 완전히 다른데 첫 값만 쓰고 있었다. **나머지 22자리는 그대로다**
  (둘째 값의 뜻이 자리마다 달라 인게임 확인 없이 못 고친다)
- **★★ 그 김에 각주 번호를 스킬 단위로 바꿨다 (`foldParenNote(val, ctr)`).**
  예전엔 **값 하나 안에서만** 세어서, 한 스킬에 각주가 둘 이상이면 화면에 `[1]` 이 여러 번 떴다 —
  야스오 Q 에 사거리를 넣은 순간 **쿨타임·사거리·시전시간 셋이 전부 `[1]`** 이 됐다.
  지금은 `fnCtr` 이 스킬 하나를 따라다니며 `[1] [2] [3]` 으로 이어진다.
  **★ 번호 순서 = 화면 순서다** — `index.html` 의 `champ-skill-meta` 가
  `쿨타임 → 소모값 → 우상단 값` 순으로 그리고, `custom_values.js` 의 키 순서도 같다.
  **호출부가 셋이라 (`cooldown`·`cost`·stats 의 `put`) 새 각주 자리를 만들면 `fnCtr` 을 같이 넘길 것**
- 각주가 둘 이상인 자리는 지금 **3개다** (야스오 Q 3개 · 요네 Q 3개 · 요네 W 2개).
  각주 1개인 6자리는 그대로 `[1]` 이다 (회귀 확인)

### ★ 아직 안 푼 것 — 사거리가 롤위키와 어긋난 **112자리**

`wiki_stats.json` 과 전수 대조한 결과다. **인게임으로 하나씩 풀 문제가 아니라 따로 다뤄야 한다:**

- **차이가 양방향이라 단일 규칙이 아니다** — `+50` 이 25자리인데 `-50` 도 10자리다
  (`+25` 16 · `+75` 6 · `+100` 6 · `-25` 4 …, 서로 다른 차이값이 40종). "위키가 히트박스를 더한다"
  같은 한 가지 설명으로는 안 덮인다
- **차이 대부분이 25~50 이라 사거리 원 비교로 못 갈린다** (5~10% 차이). 인게임 확인 목록에
  올려도 답이 안 나온다
- **★ 롤위키가 다른 걸 재는 경우가 실제로 있다.** 아칼리 Q 가 그 예 —
  `|target range = {{dv|500|{{tt|120|Range offset for applying the slow}}}}` 라 **둔화 적용
  오프셋**을 같이 적어 뒀고, bin 은 `castRange` 와 `castRangeDisplayOverride` 가 **둘 다 550** 이다
  (둘이 일치하면 신뢰도가 높은 편이다 — 자야 Q 처럼 어긋난 자리가 문제였다)
- **차이 151 이상인 11자리**(오른 R·라이즈 R·사일러스 Q·사미라 Q·올라프 Q·진 W·오로라 W·크산테 E·
  나미 R·루시안 R·코그모 E)와 **위키가 `전역` 이라 적은 2자리**(릴리아 R·워윅 W)가
  **"위키 칸이 다른 뜻" 일 가능성이 제일 높다.** 여기부터 원문을 열어 볼 것

**★ 화면 문구 하나**: 스탯 탭 비교 챔피언이 그 스탯을 안 가질 때 `가렌 — 이 스탯 없음` 이던 것을
**`가렌: 해당 스탯 없음`** 으로 (`app.js` 의 `vsNote`). 같은 꼴을 전수로 찾았는데 **화면에 나가는
`이름 — 설명` 은 두 곳뿐**이고 나머지 둘은 일부러 뒀다 — 대사 인용의 `— 저자`(인용 관례)와
크로마 점 title 의 `이름 — 설명`(부연이라 em dash 가 자연스럽다)

### 2026-08-24 (이어서) — 위키 다중값 부류 전수 + 112자리 결론

**★★ 부류를 `target range` 칸까지 넓히니 26 → 52자리였다.** 처음엔 `range` 칸만 봤는데
롤위키는 스킬에 따라 둘 중 하나를 쓴다 (`build_wiki_stats.js:140` 도 `target range` 를 우선한다).

- **최소~최대 5자리**: 애니비아 W(100~1000) · 올라프 Q(425~1000) · 라이즈 R(1000~3000) ·
  사일러스 Q(50~775) · 우르곳 E(450~475). **같은 날 다 넣었다** (아래 "최소~최대 사거리 5자리" 절)
- **별개 값 47자리** 중 **문장이 "다른 시전 모드의 사거리" 라고 뒷받침하는 8자리만 반영했다**:
  케인 W/R(그림자 암살자) · 신 짜오 E(도전 대상) · 나르 Q(돌아올 때) · 클레드 E(재사용) ·
  크산테 Q(강화) · 코르키 R(세 번째 미사일) · 밀리오 W(재사용)
- **★ 안 넣은 것과 이유**: 비에고 E 8000(벽 따라 퍼지는 안개 — 사거리가 아니다) ·
  그레이브즈 R 1690(폭발 원뿔 포함 — 효과 범위 성격) · 리산드라 Q·라칸 E(**우리 기준값이
  위키 첫 값과 달라** 괄호를 붙이면 되레 헷갈린다) · 신드라 E(뜻이 불분명)
- **★★ 판단 기준을 "위키 설명" 이 아니라 "우리 스킬 문장" 으로 삼았다.** 신 짜오 E 는 문장이
  아예 "도전 받은 적을 대상으로는 **이 스킬의 사거리가 증가합니다**" 라고 적고, 케인 W/R 도
  마찬가지다. 문장에 근거가 없으면 안 넣었다

**★★ 릴리아 R 은 `전역` 이다** (2026-08-24 인게임 확인). bin 본체 `LilliaR` castRange 1600 은
시전 이펙트 반경이고, 진짜 적용은 `LilliaRExpungeMissile` 의 **19500**(협곡 대각선보다 크다) 이다.
문장도 "꿈가루가 묻은 적 챔피언을 **전부**" 라고 적는다.
**★ 19500 은 더미 컷(25000·20000)을 안 넘어서 걸러지지 않았다** — 더미 판정만으로는 못 잡는 자리다.

**★★ 워윅 W 는 전역이 아니다 (내가 한 번 잘못 짚었다).** 롤위키 `range = Global` 에는
`Passive detection of wounded enemies` 라는 설명이 붙어 있다 — **패시브 감지가 전역**이라는
뜻이지 액티브 사거리가 아니다. **위키의 `Global` 을 볼 땐 괄호 설명까지 읽을 것.**
사거리·효과 범위에 4000 이 두 번 뜨던 것도 **같은 날 정리했다** — 자세한 건 바로 아래 절.

### ★★ 2026-08-24 밤 — "정확히 모르는 것" 4자리를 사용자가 전부 풀었다

내가 "게임에서 보면 갈린다" 고 넘긴 넷인데, **답을 듣고 데이터를 다시 보니 근거가 다 있었다.**

| 스킬 | 전 | 후 | 무엇이었나 |
|---|---|---|---|
| **초가스 E** | 사거리 없음 | **650** | 가시가 앞으로 뻗는 거리가 실제로 있다. bin `castRange 40` 은 기본 공격 강화 쪽 값이라 못 썼던 것뿐인데, **투사체 속도 1475 는 적어 놓고 사거리만 없어** 앞뒤가 안 맞았다 |
| **트위치 R** | 1200 | **`1100 (+ 추가 공격 사거리)`** | **1200 은 E(`TwitchExpunge`) 와 같은 값이었다.** R 은 자기 강화라 시전 사거리가 없고(위키 `target range = Twitch's attack range`), 1100 은 **강화된 평타 화살이 관통하며 날아가는 거리**다 |
| **징크스 Q** | 시전시간 0.25 | **칸 제거** | 0.25 는 `JinxQAttack`(로켓으로 **공격할 때**) 값이고 폼 전환 자체는 즉발이다. 위키도 `cast time = none` · `static = 0.9` 인데 **그 0.9 는 폼 전환 최소 쿨**이라 이미 쿨타임 칸에 있다 (bin `JinxQ` cooldown 0.9) |
| **에코 R** | 시전시간 0.25 | **0.5** | 인게임에 시전시간이 실제로 있다. 우리 0.25 는 CD v1 값이었다 |

**★★ 위 표의 초가스 E·트위치 R 은 같은 날 되돌렸다 — 둘 다 사거리 칸이 성립하지 않는다.**
"가시가 앞으로 뻗는 거리가 실제로 있다" 는 말을 듣고 위키 650 을 넣었는데 **그 650 의 정체가 달랐다.**

| | bin 이 말하는 것 |
|---|---|
| **초가스 E** | 본체 `VorpalSpikes` 는 castRange **40** · DataValue **`AttackRangeIncrease` 50**(공격 사거리를 50 늘린다). **650 은 `VorpalSpikesMissle` 이 날아가는 거리**로 투사체 속도 1475 와 짝이지 사거리가 아니다 |
| **트위치 R** | 문장이 이미 **"공격 사거리가 {p2}, 공격력이 {p3} 증가"** 라고 적는다(p2 = 300). 위키 `1100 (+100% bonus attack range)` 도 **관통 화살이 날아가는 거리**다 |

- **★★★ 둘 다 "기본 공격을 강화하는 스킬" 이라 시전 사거리 자체가 없다.** 그런 스킬에
  사거리 칸을 채우려 하면 **"미사일이 날아가는 거리" 나 "공격 사거리 증가량" 을 끌어다 쓰게 된다**
- **★ 내가 두 번 틀렸다** — 처음엔 우리 1200(= 트위치 E 값)을 못 잡았고, 지적을 듣고는
  **위키 값을 그대로 넣어 또 틀렸다.** 사용자가 이미 "공격 사거리에 300을 더하는 것" 이라고
  말해 줬는데 위키 숫자를 우선해 버렸다. **인게임 동작 설명이 위키 칸보다 먼저다**


- **★★ 넷 다 "우리 값이 다른 무언가였다" 는 같은 모양이다** — 트위치 R 은 E 값, 징크스 Q 는 공격 시전시간,
  초가스 E 는 기본 공격 강화 쪽 castRange. **한 스킬에 객체가 여럿이면 어느 것을 집었는지 확인할 것**
- **★ "그 숫자가 나올 수가 없는데" 라는 지적이 제일 빨랐다.** 트위치 R 은 공격 사거리를 300 늘려 주는
  버프 스킬이라 1200 이라는 사거리 자체가 성립하지 않는데, **데이터만 보면 안 걸린다**
  (bin 에 실제로 1200 이 있으니까)


### ★★ 공속 비례 시전시간 — **bin 으로 계산하면 안 된다** (2026-08-24)

3번 조사에서 나온 6자리 중 **롤위키가 공식을 주는 2자리만** 반영했다.

```
시비르 Q   0.25 ~ 0.1 (추가 공격 속도 100%당 50% 감소, 최대 60%)
자야 Q     0.25 ~ 0.1 (추가 공격 속도 100%당 0.07초 감소, 최대 60%)
```

**★★ bin 계산법이 검증에 실패했다.** `mAttackCastTime ÷ mAttackTotalTime ÷ as_base` 로
"기본 공격 준비 동작" 을 재면 자야 Q 가 **0.269** 가 나오는데, **롤위키 공식은 0.25 에서 시작한다**
(`0.25-(0.07 per 100% bonus AS)`). 스킬 고유 값이지 기본 공격 준비 동작 그 자체가 아니라는 뜻이다.

- **★ 그래서 케일 Q·미스 포츈 Q·비에고 Q·스몰더 Q 는 안 넣었다** — 위키가
  `Attack Windup Time`·`Basic Attack Timer` 라고만 하고 숫자를 안 준다. bin 계산으로는
  케일 0.310 · 비에고 0.250 · 스몰더 0.250 이 나오지만 **자야에서 틀린 계산법이라 못 믿는다**
- **★ 참고 — bin 에 정본 필드가 있다**: `mAttackCastTime` · `mAttackTotalTime`
  (자야 0.283 / 1.600). 롤위키 `Module:ChampionData/data` 의 `attack_cast_time` ·
  `attack_total_time` 과 **정확히 같은 값**이다 (위키가 bin 을 옮긴 것). 175명 중 51명만 갖고 있고
  나머지는 라이엇 기본값을 쓴다
- **★ 제리 Q `1.52` 를 선례로 삼으면 안 된다** — 그건 `1 ÷ 기본공속` 으로 **공격 주기 전체**를
  잰 값이라 준비 동작과 계산이 다르다

**★★ 이번에도 사용자 지적이 잡아냈다.** "자야 1렙 Q 선딜이 0.43초나 됨?" 이라는 물음에
다시 재보니 bin 필드 뜻을 잘못 읽고 있었다 (`attackDelayCastOffsetPercent` 를 비율로 봤는데
실제로는 시간(초)이었다). **숫자가 상식과 어긋나면 계산법부터 의심할 것.**


### 2026-08-24 — 8/23 부터 남아 있던 "아직 안 본 것" 둘을 확인했다

**① 자기 중심 스킬의 위키 투사체 속도 4자리 — 전부 정상이었다. 안 고쳤다.**

| 스킬 | 우리 | 근거 |
|---|---|---|
| 카타리나 R | 2400 | **bin `KatarinaRMis` mSpeed 2400 과 정확히 일치** (위키 `Dagger missiles travel speed`) |
| 트위치 R | 5000 | 위키 `speed = 5000` 과 일치 |
| 초가스 E | 1475 | 위키 `speed = 1475` 와 일치 |
| 자야 W | 3000 | 위키 `Spline missile speed` (bin 본체는 1800 — 본체 값을 안 쓴 게 맞다) |

- **곁가지 둘**: 트위치 R 은 위키가 사거리를 `1100 (+ 추가 공격 사거리)` 로 적는데 우리는 1200 이고,
  초가스 E 는 우리가 사거리를 `STAT_DROP` 으로 뺐는데 위키엔 `650` 이 있다
  (**투사체 속도는 있는데 사거리가 없는 모양**이라 한 번 볼 만하다)

**② 시전시간 0.25 가 271자리인 게 과한가 → 과하지 않다. 82%가 롤위키와 일치한다.**

```
우리 시전시간 = 0.25 인 자리 271
  ✔ 롤위키도 0.25          221 (82%)
  ✗ 롤위키가 none           26
  ✗ 롤위키가 다른 숫자        10
  – 위키에 cast time 칸 없음  14
```

- **★★ 위키 `none` 26자리는 그대로 두는 게 맞아 보인다.** 아리 W·카밀 Q·카이사 Q 의
  bin `spellCastTime` 을 열어 보니 **실제로 0.25 가 들어 있다.** 롤위키의 `none` 은
  "시전 중 멈추지 않는다" 는 뜻으로 쓰는 것 같고, **우리 0.25 와 재는 대상이 다르다**
  (오늘 사거리에서 반복해서 나온 패턴이다)
- **★ 공속 비례 6자리 — 야스오·요네와 같은 부류다.** 롤위키가 전부 "기본 공격 준비 동작
  (windup time)에 묶인다" 고 적는다: **비에고 Q**(`% of Viego's windup time`) ·
  **스몰더 Q**(`100% of windup time`) · **시비르 Q**·**자야 Q**(공속 비례 표 `0.25 → 0.1`) ·
  **케일 Q**(`Attack Windup Time`) · **미스 포츈 Q**(`Basic Attack Timer`).
  야스오 Q·요네 Q/W 처럼 `STAT_MANUAL` 로 공식을 옮기면 된다 (**아직 안 했다**)
- **★★ 시전시간의 출처는 bin 이 아니라 CD v1 이다** (`fill_values.js` 의 `spell.mCastTime`).
  그래서 bin `spellCastTime` 과 갈리는 자리가 있다:
  **징크스 Q** — CD 0.25 / bin **0.485** / 위키 `none` (셋이 다 다르다) ·
  **에코 R** — CD 0.25 / **bin 에 값 없음** / 위키 0.5.
  **두 자리는 판단이 안 섰다** (로크 R·이블린 Q·징크스 W 는 bin 도 0.25 라 우리와 일치한다)


### ★★ "전역" 표기 25자리 전수 (2026-08-24) — 워윅 W 함정이 부류였다

`전역` 이라고 나가던 자리를 전수로 훑고 롤위키 원문을 다 열었다. **셋으로 갈렸다.**

**① 사거리 = 전역 (10자리) — 전부 맞다.** 갱플랭크 R · 드레이븐 R · 릴리아 R · 멜 R · 세나 R ·
쉔 R · 애쉬 E · 애쉬 R · 이즈리얼 R · 징크스 R. 위키가 `target range = Global` 이고
**괄호 설명이 없다** — 실제로 맵 어디든 시전하는 스킬들이다.

**② 효과 범위 = 전역인데 사실은 "재사용·회수 사거리가 전역" (9자리) — 사거리 괄호로 옮겼다.**

| 스킬 | 바뀐 값 | 롤위키 괄호 설명 |
|---|---|---|
| 아칼리 E | `825 (재사용은 전역)` | `Recast range` |
| 제드 R | `625 (재사용은 전역)` | `Recast and ability mimick range` |
| 오로라 Q | `900 (표식 대상 재사용은 전역)` | `Recast range for marked targets` |
| **우르곳 R** | `2500 (재사용은 전역)` + **효과 범위 600** | `Recast range` + `600 Fear radius` |
| 쉔 Q | `400 (영혼검 회수는 전역)` | `Spirit Blade recall range` |
| 이렐리아 E | `850 (검이 모이는 거리는 전역)` | `Blade convergence range` |
| 요네 E | `300 (귀환 돌진은 전역)` | `Return dash and detonation damage radius` |
| 쓰레쉬 Q | `1075 (기절·끌어당김은 전역)` | `For the stun and pulling` (+3000 Deathly Leap) |
| 브라이어 R | `12000 (2차 시전 돌진은 전역)` | `Second cast dash range` |

- **★★ "효과 범위: 전역" 은 "스킬이 맵 전체를 때린다" 로 읽힌다.** 실제 뜻은 "재사용할 때
  거리 제한이 없다" 라 완전히 다르다. **라벨과 뜻을 맞추려고 사거리 괄호로 옮겼다**
  (사이온 E·칼리스타 R·케인 W 와 같은 처리)
- **★ 우르곳 R 은 손해가 더 컸다** — 효과 범위 칸을 '전역' 이 차지해서 **진짜 값
  `600`(공포 반경)이 밀려나 있었다.** 이번에 자리를 찾았다

**③ 그대로 둔 6자리** — 진짜 전역인 셋(카서스 R·소라카 R·트페 R, 위키가 설명 없이 `Global`)과
같은 날 설명을 붙인 셋(**녹턴 R** 어둠 · **자야 E** 깃털 회수 ·
**조이 W** 아래 참고).

**★★ 판단 보류였던 셋도 정리했다 (2026-08-24) — "일부만 전역" 인 자리는 무엇이 전역인지 밝힌다.**

| 스킬 | 바뀐 값 | 문장 근거 |
|---|---|---|
| 녹턴 R | `전역 (어둠)` | **"전장을 어둠으로 뒤덮어"** — 돌진은 사거리 `2500/3250/4000` 로 따로 있다 |
| 자야 E | `전역 (깃털 회수)` | **"모든 깃털을 불러들여"** |
| 조이 W | `전역 (파편 떨어뜨리기)` | **"적이 소환사 주문이나 아이템을 사용하면 주문 파편을 떨어뜨립니다"** |

- **★ 위 셋(카서스 R·소라카 R·트페 R)과 갈리는 기준은 "스킬 전체가 전역이냐"** 다.
  카서스 R 은 스킬 자체가 전역 피해라 설명이 필요 없지만, 녹턴 R 은 **어둠만 전역이고
  돌진은 사거리가 있다.** 워윅 W 를 `4000 (패시브 감지는 전역)` 으로 적은 것과 같은 정신이다
- **★ 롤위키가 이 셋에는 괄호 설명을 안 붙였다** — `effect radius = Global` 뿐이라
  **위키만 봐서는 안 갈리고 우리 문장을 읽어야 했다**


### ★★ 조이 W 는 사거리 개념이 없다 (2026-08-24) — 사거리 칸 제거

우리 화면이 `3000 / 4500 / 6000 / 3000 / 3000` 을 **랭크별로** 보여주고 있었는데
**랭크 3에서 6000이었다가 4에서 3000으로 되돌아오는** 모양이라 스케일일 수 없다
(같은 스킬 `TotalBaseDamage` 는 `5~65` 로 정상 증가한다).

```
bin ZoeW override : [3000, 3000, 4500, 6000, 3000, 3000, 3000]   ← 7칸
DD  range         : [3000, 4500, 6000, 3000, 3000]                ← bin 의 1~5번 칸
```

- **★ 롤위키에는 `range` 칸 자체가 없다.** 주문 파편은 **밟아서 줍는** 것이라 사거리가 없는 게 맞다
- **★ 주운 주문을 쓸 때는 그 주문의 사거리를 따른다** — bin 에 `ZoeW_SummonerFlash` 425 ·
  `SummonerHeal` 850 · `SummonerBarrier` 1200 처럼 주문별 객체가 따로 있다
- **★ audit 이 못 잡던 종류다** — DD 가 같은 값을 주므로 "⑧ 사거리가 DD 와 다르다" 에 안 걸린다.
  **DD 와 일치한다고 맞는 값인 건 아니다**

### ★ 중복 검사 정규식이 불완전했다 (2026-08-24)

`^\s*'([^']+)':\s*\{` 로 **줄 시작만** 잡고 있어서 `'녹턴 R': {…}, '쉔 Q': {…}, '아칼리 E': {…}`
처럼 **한 줄에 여러 항목이 있으면 뒤엣것을 놓쳤다.** 그대로 갔으면 쉔 Q 의 투사체 속도와
브라이어 R 의 돌진 속도가 조용히 사라질 뻔했다. 지금은 주석을 지우고 `'키':\s*[\{\[]` 를
**줄 중간까지** 훑는다. 전수 검사는 이렇게:

```js
const clean = blk.split('\n').map(l => l.replace(/\/\/.*$/, '')).join('\n');
const keys = [...clean.matchAll(/'([^']+)'\s*:\s*[\{\[]/g)].map(m => m[1]);
```


### ★★ 워윅 W 결론 (2026-08-24) — 패시브는 전역, 액티브는 4000

**사용자 가설이 맞았다.** bin·stringtable·롤위키를 다 대조한 결과:

| | 값 | 정체 |
|---|---|---|
| `WarwickW` | castRange 3000 · **override 4000** · castRadius 2200 | 액티브. **셋이 다 다르다** |
| `BloodScent` | castRange 1600 | 패시브 쪽 객체. 이 숫자의 정체는 확인 못 했다 |
| 롤위키 | `range = Global` **(Passive detection of wounded enemies)** | 전역인 건 **패시브 감지** |
| 롤위키 | `effect radius = 4000` **(Search radius for target)** | 액티브 검색 반경 |

- **★★ `Global` 만 보고 액티브 사거리로 읽으면 안 된다.** 괄호 설명이 "패시브 감지" 라고
  못 박고 있다. 한 번 잘못 짚었던 자리다
- **★ 사거리 칸을 뺐다 (`STAT_DROP`).** 액티브는 대상을 지정하지 않고 "가장 가까이에 있는
  적 챔피언" 을 자기 주변에서 찾으므로 4000 은 사거리가 아니라 검색 반경이다.
  **사거리·효과 범위에 4000 이 두 번 뜨던 것**이 정리됐다
- 효과 범위는 `4000 (패시브 감지는 전역)` — 한 슬롯에 패시브·액티브가 같이 있는 스킬이라
  둘 다 담았다
- **★ `selfRadiusDup` 이 이 자리를 못 잡은 이유**: 규칙 조건이 `wikiStat['사거리'] === undefined`
  인데 워윅 W 는 위키에 `range = Global` 이 있어서 '전역' 으로 채워졌다. **위키에 사거리가
  "있지만 다른 뜻" 인 경우는 그 규칙이 안 걸린다**

### 최소~최대 사거리 5자리 반영 (2026-08-24)

롤위키 `range` 칸이 `Minimum`/`Maximum` 두 값인 자리다. 우리는 **최대값만** 적고 있어서
틀린 건 아니었지만, 조준 지점에 따라 달라지는 스킬이라 범위로 적는 게 맞다.

`애니비아 W 100 ~ 1000` · `올라프 Q 425 ~ 1000` · `라이즈 R 1000 ~ 3000` ·
`사일러스 Q 50 ~ 775` · `우르곳 E 450 ~ 475`

- **★ 괄호 설명은 안 붙인다** — 암베사 P `175 ~ 350`·사미라 P `650 ~ 950` 과 같은 꼴이다

### ★★ "판단 보류 5자리" 결론 — 넷은 우리가 맞다 (2026-08-24)

**위키가 미사일 객체의 실제 비행 거리를 적고, 우리는 클라이언트 표시값(`castRangeDisplayOverride`)을
쓰기 때문에 생긴 차이다.** bin 을 열어 보면 **위키 값이 미사일 객체의 castRange 와 정확히 일치**한다:

| | 우리 (override) | 위키 | 위키 값의 정체 |
|---|---|---|---|
| 나미 R | 2550 | 2750 | `NamiRMissile` castRange **2750** |
| 코그모 E | 1200 | 1360 | `KogMawVoidOozeMissile` castRange **1360** |
| 루시안 R | 1400 | 1200 | `LucianRMissile` castRange **1200** (탄환 하나의 사거리) |
| 오른 R | 2500 | 300 | 위키가 `target range` 칸에 **재시전 돌진**을 적었다 (bin `OrnnRCharge` override 450) |

- **★ 방향이 일정하지 않다** — 나미·코그모는 위키가 크고 루시안은 위키가 작다.
  "위키가 히트박스를 더한다" 같은 한 방향 규칙으로 설명되지 않는 이유다
- **★ 우리 원칙(클라 표시값 우선)이 맞다.** 인게임 툴팁에 뜨는 값이 그것이고,
  미사일 실거리는 툴팁에 안 나온다. **넷 다 안 고쳤다**
- **오로라 W 는 위키가 맞았다** — 같은 날 인게임 확인으로 `300 (지형 넘으면 450)` 으로 고쳤다 (바로 아래 절)

**★★ 오로라 W 도 끝났다 (2026-08-24) — `700` → `300 (지형 넘으면 450)`.**
"E(825)의 절반쯤" 이라는 관찰을 듣고 bin DataValue 를 열어 보니 답이 그대로 있었다:

```
AuroraW   override 700 · castRadius 1000
   DV JumpDistance       300   ← 롤위키 "Regular dash distance"
   DV WallCheatDistance  450   ← 롤위키 "Increased dash distance across terrain"
   DV DashBonusSpeed     350   ← 우리가 이미 "돌진 속도" 로 쓰던 값
```

- **★ 롤위키 300/450 이 bin DataValue 와 정확히 일치한다.** 이 자리는 위키가 맞았다
- **★ `castRangeDisplayOverride` 가 돌진 거리와 무관한 자리다.** override 700 · castRadius 1000 의
  정체는 확인 못 했지만 **돌진 거리는 아니라는 것만 확실하다.** 문장도
  "지정한 방향으로 뛰어오릅니다" 라 대상 지정이 없다
- **★ 돌진 거리를 사거리 칸에 적는 건 이 저장소 관례다** — 자헨 E 350(`DashDistance`) ·
  헤카림 R 1000(`MaxDashRange`) · 볼리베어 R 700(도약 거리)
- **★★ 교훈: "얼마쯤으로 보이더라" 는 관찰이 bin 의 어느 필드를 볼지 정해 준다.**
  700 이 아니라는 것만 알면 `JumpDistance`·`WallCheatDistance` 를 찾아볼 이유가 생긴다.
  숫자를 정확히 못 재도 **자릿수만 좁혀 주면 데이터에서 답을 찾을 수 있다**

### ★★ "사거리가 롤위키와 다른 112자리" 는 대체로 문제가 아니다 (결론)

**`wiki_stats.json` 은 bin 에 값이 **없는** 자리만 채우는 보조 자료**라 (우선순위
`STAT_MANUAL > bin > wiki`), 위키와 다르다는 것 자체는 오류가 아니다. 차이가 큰 13자리의
원문을 전부 열어 확인한 결과:

- **우리가 맞음**: 사미라 Q(위키 340 은 `target range` = 칼 판정 길이, 진짜 사거리 `range` 950 은 우리와 일치) ·
  진 W(위키가 `Estimated` 라고 명시) · 크산테 E(위키에 `Needs to be confirmed` 주석)
- **위키가 맞음**: 릴리아 R(전역) — 위에서 고쳤다
- **표기 개선 여지** → **같은 날 넣었다** (라이즈 R·사일러스 Q·올라프 Q·애니비아 W·우르곳 E 5자리)
- **판단 보류였던 5자리도 같은 날 결론 났다** — 오른 R·나미 R·루시안 R·코그모 E 는 **우리가 맞고**
  (위키가 미사일 객체의 실거리를 적는다), 오로라 W 만 위키가 맞아 고쳤다 (아래 절)
- 나머지 99자리는 **차이 60 이하가 74곳**이라 히트박스·측정 관례 차이로 보인다

**★ `build_wiki_stats.js` 의 `target range` 우선순위는 그대로 두는 게 맞다** — 두 칸이 다 있는
20자리에서 **`target range` 가 우리 값과 맞는 게 7 · `range` 가 맞는 게 1**(사미라 Q)이다.
그 하나도 bin 이 이겨서 화면은 이미 정상이다

**★★ STAT_MANUAL 에 줄을 더할 땐 키 중복을 반드시 검사할 것.** 이번에 코르키 R·밀리오 W 를
새 줄로 넣었다가 잡았다 — **JS 객체라 뒤엣것만 남아 기존 투사체 속도·사거리가 조용히 사라진다.**
검사법: `STAT_MANUAL` 블록에서 `^\s*'([^']+)':\s*\{` 를 뽑아 중복을 센다 (지금 75개, 중복 0)

## ★★ 2026-08-23 세션 — 스킬 우상단 값 전수 정비 (커밋 `78263cf`, 배포 확인)

롤위키 861 템플릿 + 나무위키 173 문서를 **기계로 전수 대조**했다. **설명 문장·계수는 우리 오류 0** (어긋난 97슬롯이
전부 나무위키 구버전). 틀린 건 **우상단 값**이었고 규칙으로 고쳤다 — 자세한 건 `docs/스킬데이터.md` 의
"롤위키·나무위키 전수 대조 (2026-08-23)" 절. 여기는 **다음 세션이 알아야 할 것만**:

- **패치 파이프라인에 한 줄 늘었다**: `node build_wiki_stats.js --refresh` (→ `wiki_stats.json`). **`fill_values.js` 보다 먼저**
  돌려야 한다 — fill 이 그 파일을 읽어 **bin 에 없는 자리만** 채운다 (우선순위 `STAT_MANUAL > bin > wiki`). 861 템플릿이라 10분쯤
- **`fill_values.js` 에 손표가 넷이다**: `STAT_MANUAL`(사거리·속도) · `STAT_DROP`(칸 제거) · **`CD_MANUAL`**(쿨타임 문장을 못 푼
  자리 — 탈론 E·베이가 W·제리 Q·탐 켄치 R) · **`COST_MANUAL`**(우디르 W/E/R `40 ~ 23`). 값을 고칠 땐 이 넷 중 어디인지부터 볼 것
- **★ `castRangeDisplayOverride` 도 틀릴 수 있다** (자야 Q override 400, 실제 1100). 판단 규칙: **롤위키·나무위키가 같은 값이면 위키**,
  둘이 다르면 bin. 그래서 사거리 8자리를 STAT_MANUAL 로 올렸다
- **★ 투사체 속도의 진짜 원인**: 본체 `missileSpeed` 가 `1200 / 902 / 828.5 / 779.9 / 8700` 이면 **틀값**이고 진짜는
  `<본체이름>Missile` 객체의 `mMissileSpec.movementComponent.mSpeed` 다. 본체가 0 인 자리는 일부러 안 채운다 (돌진기 오탐)
- **★ 스킬 폭은 `mLineWidth x 2`** (반지름 → 폭). 인게임 체감으로 확인했다 (드레이븐 E 260·이즈 Q 120)
- **★ 클라 쿨타임 칸에 "문장"을 쓰는 액티브가 36자리** (`keyCooldown`). DD 도 같은 값(0 / 0.25)이라 **`audit_skill_meta` 로는
  못 잡던 종류**였다. 패시브용 코드를 QWER 로 넓혔다. `@Cooldown@` 은 별명 — CD 쿨이 0 이면 `AmmoRechargeTime`
- **인게임 확인 결과 (스크린샷 루트 `zeriQ/kenchR/melW.PNG`)**: 녹턴 P 32·샤코 P 35·멜 W 33 은 **bin 이 맞고 위키가 틀림**.
  탐 켄치 R 은 클라 문장 자체가 깨져 `113 (0초)` 로 뜨고, 회색 글씨 "보호막은 200초에 걸쳐" 는 **라이엇 오타**(초당 감소량) —
  우리는 2.5 로 손질(`MANUAL`)
- **아직 안 본 것** → **2026-08-24에 전부 확인했다** (위 절들). `전역` 표기는 11 → 25자리였고 13자리를 고쳤다 ·
  시전시간 0.25 는 271자리인데 **82%가 위키와 일치해 과하지 않았다** · 자기 중심 스킬의 위키 투사체 속도
  4자리(자야 W·카타리나 R·트위치 R·초가스 E)는 **전부 정상이라 안 고쳤다** · 사이온 Q 는 `500 ~ 850` 으로 정정했다
- **`audit_full.js` 가 08-21 `drawGraph` 이동 뒤 죽어 있었다** — 고쳤다. 두 audit 모두 합계 0 이 기준선이다 (audit_full 의
  "③ 각주 없음 11자리" 는 예전부터 있던 것 + 유미 W 쿨 `10 ~ 0`)
- 스크래치 대조 스크립트는 저장소에 안 넣었다. 다시 대조하려면 `build_wiki_stats.js` 의 파서를 재사용하면 된다 (같은 코드)

## ★★ 2026-08-24 심야 — 전 챔피언·전 스킬 전수 재점검 (배경 세션, 커밋 안 함)

"구멍이 자꾸 보인다" 는 요청으로 **881슬롯 전부**를 다시 돌았다. 순서: ① 파이프라인 재생성 diff
(**0바이트 차이** — 화면 데이터 = 클라 원본 + 손표, 드리프트 없음) ② 문장 숫자 9,200여 개를 롤위키
861 템플릿과 기계 대조 ③ 우상단 값 전 종목 대조 ④ bin 되짚기. **고친 자리 ~90, 새 규칙 4개.**
대조 스크립트는 잡 tmp 라 세션과 함께 사라진다 — 다시 하려면 build_wiki_stats 파서 재사용.

**★★★ 이번에 잡힌 "부류" 넷 — 전부 낱개가 아니라 규칙 결함이었다:**

1. **2차 폼 쿨타임이 한 칸 밀려 있었다 (5자리).** FORM2 경로가 bin `Cooldown` 을 0번부터 읽었는데
   **그 배열도 0번이 미습득 더미다** (엘리스 EliseSpiderE `[23,22,21,20,19,18,17]`, 롤위키 22~18).
   엘리스 E2·제이스 W2·나르 Q2/E2·렉사이 E2 정정 (`fieldToText` rank1=true). **본체는 CD v1 을 써서
   안 걸렸고, 2차 폼은 DD 에도 없어서 audit 의 DD 대조가 애초에 못 보던 사각지대였다**
2. **★★ 본체 안에서 legacy `missileSpeed` 와 `mMissileSpec…mSpeed` 가 갈리면 spec 이 진짜다 (20자리).**
   갈리는 본체 전수에서 **롤위키가 spec 과 일치한 게 20/20, legacy 0** (문도 Q 2000·이즈 W 1700·
   릴리아 E 1400·세나 R 20000·탐 켄치 Q 2800·조이 E 1850…). legacy 는 패치 갱신이 안 되는 옛 필드로
   보인다. fill_values 에 spec-우선 규칙, audit ⑪에도 가르쳤다
3. **TF Q 부류가 12자리 더 있었다** — 본체 legacy 가 틀값 목록 밖의 틀값이고 `<본체>Missile` 객체와
   롤위키가 **같은 값으로 일치**하는 자리 (쓰레쉬 E 2000·제드 W 2500·케이틀린 R 3200·브랜드 E 900·
   브라움 Q 1700·바루스 Q 1900·그레이브즈 R 2100·트위치 W 1400·다이애나 Q 1900·오리아나 Q 1400/E 1850·
   브라움 R 1400·애니비아 Q 950). STAT_MANUAL 로 정정 (근거 둘 일치라 인게임 없이 반영).
   진 R 도 3000(나무위키) → **5000** (bin spec = 롤위키)
4. **돌진기인데 투사체 칸에 legacy 쓰레기가 실린 10자리** — 말파이트 R 700·바이 R 1400·자헨 R 8700·
   브라이어 Q·마오카이 W·코르키 W·우르곳 E·오로라 W·자크 E·탈론 Q. 투사체 칸을 빼고 돌진 속도로
   정리 (말파이트 R `1500 (+ 이동 속도의 100%)`·자크 E 1350·탈론 Q 2000 은 손표)

**그 밖의 정정:** 유미 Q 투사체 `100`(스크립트 유도탄 자리값) → `950 (부착 강화 시 1650)` ·
아우렐리온 솔 Q `3.25/…/9999초` → `3.25 (5랭크는 무제한)` · 트린다미어 Q 최대 회복이
`최대 마나의 100% x (…)` 식 그대로 → 접어서 `80~300 (+주문력의 150%)` (분노 100, 롤위키와 일치) ·
야스오 Q 투사체 1500(찌르기 wrapper 틀값) → `1200 (회오리)` · 벨코즈 Q/W·아리 E (본체 1200 틀값,
위키로) · **float 꼬리 스냅** (그브 P `100.005`·브랜드 P `39.983`·쉔 P `7.995`·신 짜오 W `100.002` →
tidy 에 |차이|<0.018 & ≥1 스냅, 현재 파일에서 이 넷만 걸리는 것 확인) · 말자하 R 0.005·미포 R 0.001·
닐라 W 0.013 시전시간 — 위키가 그대로 적지만 CAST_TIME_MIN 원칙대로 위키 폴백에도 컷 · 카이사 E
시전시간 1.5(정체 불명) → `1.2 ~ 0.6 (공격 속도에 따라)` · 클레드 Q2 쿨 "3" → 클라 문장
`재장전 대기시간 18/16/14/12/10초 (재사용 3초)` (2차 폼은 keyCooldown 문장 처리를 안 타서 CD_MANUAL) ·
럼블 E 에 `(재사용 0.5초)` 보충 (갱플 E 와 같은 처리)

- **★★ `custom_graphs.js` 가 8/14 이후 재생성이 안 돼 있었다** — 8/24 낮에 넣은 괄호 각주와 번호가
  충돌해 **갱플 E·그브 P·쉔 Q 등에서 `[1]` 이 두 번** 뜨는 상태였다. 재생성으로 정리.
  **★ custom_values 에 각주가 생기는 수정을 하면 add_level_graphs.js 를 같이 돌릴 것**
- **사거리검토 65자리 중 49자리 반영** — 기존 두 정책(자기 강화 부류·selfRadiusDup)과 같은 부류만.
  원뿔·방향형 등 16자리는 정책 판단이라 남겼다 → `사거리검토_20260824.md` 에 권고 표
- **시전시간 "위키 none" 26자리 전수 확인 끝** — 전부 bin `mCastTime` 과 일치 (표본 3 → 전수). 유지가 맞다
- **못 가른 것 → `인게임확인_20260824_전수점검.md`** (영향 순 정렬): 스킬 폭 ×2 이중 적용 의심 4자리
  (요네 R·빅토르 E·로크 Q·크산테 Q — 우리 값이 위키의 정확히 2배) · 폭 큰 격차 9 · 투사체 20 ·
  시전시간 11 · 낱개(닐라 E 소모 40 vs 위키 30 등)
- **설명 문장·계수는 이번에도 우리 오류 0** — 걸린 1,100여 숫자를 전부 훑었고 남는 차이는 전부
  "위키가 다르게 적는 방식"(양끝 표기·접기 곱셈·18/20레벨 표시)이었다. 문장 쪽에서 고친 건
  위 트린다미어 Q·아솔 Q·float 꼬리 넷뿐이다

**같은 밤, 사용자 검토로 확정·추가된 것들:**

- **★★ 원뿔·방향형 정책 확정: 원뿔 길이는 사거리 하나로만 적는다.** 위키의 effect radius 는
  원뿔에서 "부채꼴의 반지름" = 닿는 거리라(코르키 E `690 Cone radius` 등 괄호 명시) 사거리와
  중복이었다 — 워윅 W "4000 두 번" 꼴. 카밀 W 는 위키가 `325(안쪽)/650(바깥쪽)` 인데 첫 숫자만
  잘려 오는 함정까지. 7자리 효과 범위 제거, **카사딘 E 만 예외**(사거리 400 vs 위키 원뿔 600 이
  크게 어긋나 인게임 확인 전까지 둘 다 표시).
  **★ 숙제: "최대 사거리 지점의 폭"** — 각도(위키 angle 칸)에서 `2·r·sin(각도/2)` 로 파생 가능.
  각도를 스킬 폭 대신/보완으로 실을지 나중에 판단
- **자이라 W** `없음 (씨앗 재충전 18~10초)` 각주 보충 · **신지드 Q** `0` → `없음` (토글 —
  징크스 Q 0.9 는 데이터에 있어서 적는 것, 신지드는 데이터에 없으니 안 적는 게 일관) ·
  **트린다미어 Q** `(최대: …)` 를 각주로 접음 (문장이 길다는 지적 — **KEEP_TEXT Tryndamere Q +
  MANUAL 각주 HTML 한 쌍**이다, 한쪽만 지우면 안 된다)
- **★★ 스킬 폭 ×2 이중 적용 4자리 — 요네 R 인게임 확인으로 규칙화.** 요네 R 폭이 Q 조준 길이(450)의
  절반이라 위키 225 가 맞았다. 넷 다 **bin `mLineWidth` 가 롤위키 WIDTH 와 그대로 같은** 자리 —
  그 자리는 이미 전체 폭이라 2배를 안 한다 (`widthNoDouble`, audit ⑫). 63자리(위키 = 정확히 2배)의
  기본 규칙은 그대로다. **★ 기본 공격 사거리는 가장자리 기준(A 원 = 사거리 + 내 반경 65)이라 스킬
  축척의 기준 원으로 쓰면 안 된다** — 스킬 사거리 원(중심 기준)을 쓸 것. 위키와 ±50~65 어긋난
  사거리 74곳도 이 기준 차이일 가능성이 있다 (다음에 그 부류 볼 때 "차이가 65 근처냐" 로 거를 것)
- **★★ 스킬 폭 큰 격차 9자리도 인게임 확인 — 9/9 롤위키가 맞았다** (바루스 R 240·럭스 R 200·사미라 E 300·
  세주아니 W 150·제라스 Q 145·암베사 R 130·제리 Q 80·요네 Q 80(회오리 160)·유미 R 450, STAT_MANUAL).
  ×2 이중 적용 4자리까지 **폭은 13/13 위키 승.** bin `mLineWidth` 는 이펙트 폭이지 판정 폭이 아닌 자리가
  많다 (15 같은 자리값도 있다). **★ 스킬 폭에 한해 "위키 우선(bin 은 위키 없을 때)" 로 뒤집을지가
  다음 판단** — 위키와 다른 나머지 ~56자리는 ±20~60 차이라 인게임으로 못 가른다
- **★★ 스킬 폭 규칙을 "위키 우선" 으로 뒤집었다 (2026-08-24 밤, 사용자 확정).** 우선순위가 다른 stat 과
  달리 **STAT_MANUAL > 롤위키 WIDTH > bin mLineWidth×2** 다 (`put` 안 분기). 49자리가 위키 값으로
  바뀌었다. **예외 `WIDTH_KEEP_BIN` 5자리** — 위키 칸이 부분값인 자리(그웬 Q 사다리꼴·루시안 R·미포 R
  탄줄 40·비에고 E/아칼리 Q "estimated"). 헤카림 R 은 위키 `80 기수 하나 | 480 전체` 라 첫 숫자를
  집는 규칙 대신 손표 480, 야스오 Q 는 `80 (회오리 180)` 손표. audit ⑫ 는 wiki_stats 와 같으면 통과
- **★ 투사체 속도 7자리 — 게임 없이 "롤위키 = 나무위키" 교차로 정정** (베인 E 2200·아리 Q 1550·직스 R 2250·
  시비르 Q 1450(돌아올 때 1200)·갈리오 Q 1400·럭스 W 2400·쉬바나 E 1600(용 1575)). **★ 롤위키 챔피언 페이지의
  Patch History 가 결정타였다** — 시비르 Q "12.13 1350→1450" 이 우리 bin 값 1350 을 **패치 전 legacy** 로 못 박았고
  직스 R 11.12·쉬바나 E 26.06 리워크도 같았다. bin legacy 가 의심되면 패치 히스토리부터 볼 것 (WebFetch 로 읽힌다).
  나무위키 URL 은 `namu.wiki/w/<이름>` 본문이다 — `/스킬` 하위 문서는 없다(404)
- **각주 상자 폭 240px 고정 → max-content** (상한 360px·화면 폭, 상한에서만 줄바꿈) —
  내용 길이 무관하게 상자가 일정하다는 지적
- **폰 전용 4건**: ① 스킬 메타(쿨·소모·스탯)를 항상 이름 아래 줄로 (`flex-basis:100%`)
  ② 랭킹·챔피언·도감 검색칸 16px (iOS 포커스 확대 방지) ③④ 는 아래 공통 헤더
- **★★ dogu 공통 헤더를 원본(`dogu_template/dogu-ui/`)에서 고쳐 복사했다** (규칙대로 md5 동기 확인):
  ① 모바일 스위처 목록에 게임명 표시 (버튼만 아이콘) ② **`gamesOrigin` 옵션 신설** —
  GAMES 내부 주소(`/er`)가 dogu.gg 기준 상대 경로라 pixlol 에서 "다른 게임을 눌러도
  pixlol.kr/er 로 가서 제자리" 였다. pixlol `mountHeader` 에 `gamesOrigin: 'https://dogu.gg'`
  ③ **모바일 이중 헤더 정리** — 768px 이하에서 2단 네비를 접고 1단 오른쪽 햄버거가 세로 메뉴를
  펼친다 (`dogu-menu-open`, 같은 마크업이라 setActiveNav 그대로). 상세는 `DOGU_UI.md` 에 적음.
  **★ er·maple·loa·tft 복사본은 아직 옛 판이다** — 그쪽 레포들에 다시 복사해야 하고,
  dogu_er 원본 변경도 그 레포에서 커밋 안 된 상태다

## ★★★ 16.17 패치 반영 완료 (2026-08-26) — **검증 3종만으로는 부족하다**

`run_patch.js` 로 11단계 + 검증 3종을 돌렸고 **전부 기준선(0 / 11 / ✔)** 이었다.
그런데 **사용자가 "패치노트랑 비교 안 해도 괜찮겠냐" 고 물어서 대조했더니 세 자리가 나왔다.**

- **★★★ 검증 3종은 "기준선과 같은가" 만 본다. "패치 내용이 제대로 들어왔는가" 는 안 본다.**
  다음 패치부터는 **반드시 공식 패치노트와 대조할 것.** 방법:
  ① `git diff public/custom_values.js` 로 바뀐 챔피언 목록을 뽑고
  ② `https://www.leagueoflegends.com/ko-kr/news/tags/patch-notes/` 에서 그 패치 글 주소를 찾아
     (**URL 은 `league-of-legends-patch-26-17-notes` 꼴 — 공식 번호는 26.x 다**, 아래 참고)
  ③ 우리가 바꾼 챔피언이 패치노트에 있는지 · 수치가 맞는지 · **패치노트에 있는데 우리가 안 바꾼 게 있는지**
- 16.17 실측: 우리가 바꾼 21명 중 **15명은 패치노트와 정확히 일치**(녹턴 방어력 38→36, 쓰레쉬 E,
  이렐리아 Q, 키아나 Q …), **5명은 아이콘 에셋만**(빅토르·쉬바나·스카너·아펠리오스·오로라), 나머지가 아래 셋

**★★ ① 자야 W — 16.17 에서 본체에 `1800` 이 새로 생겨 위키 값(3000)을 밀어냈다.**
`STAT_MANUAL` 로 3000 을 못 박았다 (롤위키 + bin `XayahWMissile1/2` 둘 다 3000).
**"1800 은 틀값" 같은 규칙으로 막으면 안 된다** — 블리츠크랭크 로켓 그랩은 본체·미사일이 둘 다 1800 이라
그게 진짜다. 본체가 1800 인 자리가 bin 전체에 73개인데 **화면에 나가는 건 이 둘뿐**이다.

**★ ② 아리 W — 같은 날 같은 모양으로 나타났는데 근거가 없어 칸을 뺐다.**
패치 전엔 칸이 아예 없었고, 후보가 셋이다 (`본체 1800` · `AhriWDamageMissile 1400` · `AhriWMissile 900`).
**롤위키에도 값이 없다.** → **남은 일: 롤위키 아리 문서를 다시 볼 것.** 인게임 리플레이 0.25배속으로
재도 900 과 1400 은 갈린다 (트페 Q 를 그렇게 갈랐다)

**★★ ③ 야스오·요네 패시브 문장 — 패치와 무관한 기존 오류였다. 같은 날 고쳤다.**
클라 원문이 `치명타 피해량이 @CurrentCritDamage@**까지** 감소합니다` 인데 그 변수가
**`mStat: 9` × 계수 1, 즉 "지금 이 유닛의 치명타 피해량" 을 읽는 동적 참조**다.
인게임에선 `175%` 같은 실시간 값이 채워지지만 **정적 데이터로는 못 채운다** — 그래서 화면에
`치명타 피해량이 치명타 피해량의 100%까지 감소합니다` 가 나가고 있었다 (패치 전에도 똑같았다).
진짜 값은 bin **`CritDamageMod = 0.95`**(5% 감소)이고 패치노트 `-10% ⇒ -5%` 와 정확히 맞는다.
`MANUAL`(값 `5%`) + `KEEP_TEXT`(문장에서 "까지" 제거) **한 쌍이라 한쪽만 되돌리면 다시 깨진다.**

**★ 남은 판단: 화면의 패치 번호를 `26.17` 로 바꿀지.** DD/CD 내부 버전은 `16.17` 인데
**라이엇 공식 표기는 `26.17`** 이다 (패치노트 URL·본문 모두). 지금 헤더·통계 드롭다운은 `16.16` 꼴이라
유저가 보는 번호와 다르다.

## 2026-08-27 — 수집 방법 점검 (7건) 과 첫 반영

사용량 정리(바탕화면 `pixlol_사용량정리_20260827.md`) 뒤에 수집 방식의 문제 7건을 뽑아 하나씩 보는 중이다.
**결정된 것**: ① "마스터+ 통계인데 10명 전원을 센다" 는 **그대로 두고 나중에 화면에 "마스터+ 5명 이상
게임" 이라고 고지**하기로 (슬림 문서에 명단 포함 비트가 없어 되돌려 거를 수도 없다 — 정의를 바꾸는 쪽).
② **잔량 경고 + 전날 따라잡기는 반영했다** — `docs/통계집계.md` 첫 절의 2026-08-27 항목. 요약:
수집 창이 `{어제, 그저께(순회 완료한 날만)}` 로 넓어졌고, 10분 로그에 `남은 판 N`, 자정에
`수집 종료: 처리/전체 — 놓침` 결산 한 줄. `ranksnapshots.scanDone` 필드 신설.
④ **용량 — 얼어붙은 패치의 `matchstats` 원본은 그 자리에서 지운다 + 집계 3종 `__v` 끔 — 반영.**
"인덱스 다이어트 25MB" 는 **틀린 진단이었다** (열어 보니 `_id` + 조회용 복합 인덱스뿐, 중복 없음).
진짜 손잡이는 원본이다 — 창고에 "현재 패치 + 3일" 만 남아 정착 380 → **최고 310 · 평균 180MB**.
집계 행이 없는 패치(`statscopes` 줄 없음)는 안 지운다. 끝난 패치 집계 행(패치당 33MB)은
`build_stats_archive.js` 로 **아무 때나** 파일로 빼면 된다 (마감 없음 · 안 해도 서너 패치는 버틴다).
③ **닫힌 패치 재집계 중단 (`PATCH_FREEZE_DAYS`) — 반영.** 박제 마감(9/6)이 사라졌다. "일별 롤업" 은
빌드·상성 행이 3~5배로 불어 용량 이득이 작다는 걸 코드를 보고 알아서 **접었다** (SQL 갈 때 같이).
**★ 남은 3건은 "프로덕션 키가 나오면" 다시 꺼낸다 (사용자 결정 2026-08-27 — 지금은 안 본다).**
셋 다 개발 키 한도(50회/분) 아래서는 손대도 얻는 게 없다:

| # | 무엇 | 지금 코드 | 왜 문제 | 그때 할 일 |
|---|---|---|---|---|
| 5 | **중앙 리미터** | 잡 넷이 각자 `sleep(1200)`, 검색은 상세 20개를 150ms 간격, 429 는 사이클만 끊고 `Retry-After` 안 읽음. 속도는 상수(`SCAN_PER_CYCLE 16`·`FETCH_PER_CYCLE 10`) | 오후엔 잡이 2분에 ~48회라 **유저 검색 2건 겹치면 429** — 유저가 잡에 밀린다. 키가 바뀌어도 상수라 **하루 21,000회(프로덕션 한도의 0.5%)만 쓴다** | `riotApi` 앞에 토큰 버킷 하나(앱+메서드 한도, `X-App-Rate-Limit-Count` 로 자기 교정), **유저 요청 우선·잡은 남는 만큼**, 429 는 `Retry-After` 만큼 전체 정지, 속도 상수는 env 로 |
| 6 | **하루 여러 번 순회** (신선도) | 순회 창이 "어제 하루" 고정, 하루 1바퀴 → 패치 당일 통계가 **이틀째 저녁**에 뜬다 | "1바퀴로 충분" 은 개발 키 기준 판단이었다 | 5번 뒤에 순회를 하루 3~4번, 창 "오늘 0시~지금". **단 `matchseens` 가 `$inc cnt` 라 같은 사람을 다시 훑으면 두 번 센다** — `(matchId, puuid)` 쌍을 저장하고 cnt = 쌍 개수로 |
| 7 | **재시작 때 cnt 부풀림** (작음) | 훑은 사람 표시가 `matchScanDay` 메모리 + `SummonerCache.matchScanDay` 인데 **닉네임 없는 사람은 DB 줄이 없어 메모리에만** 남는다 → 재시작하면 다시 훑어 `$inc` 두 번. bulkWrite 뒤·표시 전에 죽어도 같다 | cnt 는 "5명 이상" 을 가르는 값이라 k=4 판이 수집 대상에 들어온다 (저장은 안 되지만 호출 낭비) | 6번의 쌍 저장이 같이 고친다 (멱등). 급하면 진행 표시만 별도 `scanprogress` 컬렉션에 puuid 기준 upsert |

SQL 전환도 같은 시점 — 통계 잡만 Railway Postgres 로, 검색·닉네임·신화상점은 Mongo 유지 (2026-08-27 결론).
바탕화면 `pixlol_사용량정리_20260827.md` 에 세 서비스(라이엇·Mongo·Railway) 사용량·한도 정리가 있다.

## ★★ 2026-08-26 세션 — 통계 상세 페이지 · 타임라인 전수 수집 · 도감 주문

패치(위 절) 말고 오늘 한 나머지다. **커밋 전부 배포 확인.**

### ★★★ 재집계 중에 읽은 부분 결과가 응답 캐시에 굳는다 (사고 · 사용자 지적)

**"서폿 카밀에 표본이 없다" 는 지적으로 드러났다.** DB 는 272줄 · `all` 6줄이 다 멀쩡했고
**curl 도 272줄을 돌려줬는데** 브라우저가 받은 응답만 **36줄**(`rune` 까지만 들어간 순간)이었다.

- **원인**: 집계가 `deleteMany → insertMany` 라 **도는 동안 컬렉션이 반쯤 빈다.** 그 순간
  조회가 들어오면 부분 결과가 **응답 캐시(10~30분)에 굳어서**, 재집계가 몇십 초 만에 끝나도
  그 챔피언만 오래 표본이 없는 것처럼 보인다. **매시간 집계마다 걸릴 수 있었다**
- **고침 둘**: ① `buildChampStats()` 끝에 통계 응답 캐시를 비운다 (`flushStatCaches`) —
  실패해도 비운다 ② `/api/champion-builds` 는 부분 결과를 캐시하지 않는다
  (판별 = **줄은 있는데 `all` 이 없다**). 그때 화면은 `통계를 다시 계산하는 중입니다`
- **★★ 진단이 갈린 지점: curl 은 되는데 브라우저만 안 됐다.** 브라우저 캐시를 의심했지만
  `cache: 'no-store'` 로도 같았고 결국 서버 메모리 캐시였다. **DB 를 직접 연 게 결정타다** —
  API 만 보고 있었으면 데이터가 깨진 줄 알고 재집계부터 돌렸을 것이다
- 자세한 건 `docs/통계집계.md` 의 그 절. **캐시를 새로 붙일 땐 같은 가드를 같이 붙일 것**

### 챔피언 통계 상세 페이지 — `/stats/<영문키>/<라인>`

표에서 줄을 펼치던 것을 페이지로 뗐다. 룬을 **인게임 격자**로 그리고(고른 것만 밝게),
소환사 주문·상성까지 한 화면이다. **사양은 `docs/통계집계.md` 의 그 절** — 여기는 함정만:

- **★ 라인이 주소에 들어간다** — 없으면 **탑 카밀을 눌러도 서폿 카밀 통계가 떴다**
- **★★ 파편을 `key.includes(id)` 로 켜면 안 된다** — 같은 것이 두 줄에 나온다
  (적응형 = 1·2줄, 체력 증가 = 2·3줄). **자리로 켠다** (`shards[i]` 가 i번째 줄)
- ~~폰 미디어쿼리에서 `.cs-body` 를 같이 적을 것~~ — 밤에 통째로 다시 그리면서 그 규칙 자체가 없어졌다 (아래)

### ★★ 같은 날 밤 — 상세 페이지를 lolalytics 그대로 옮김 + 집계 20종 (사용자 요청, 두 번 만에)

**"lolalytics.com/ko/lol/camille/build 과 최대한 같게, 우측 하단 상자들(Secure/Yield·사분면·Best on·Leaderboard)은 빼고."**
첫 판은 골격만 옮기고 색·순서를 우리 식으로 바꿨다가 **"내 맘대로 바꿨다" 는 지적**을 받았다 — 나란히 찍어 보니 실제로 다른 사이트였다.
두 번째 판은 그쪽 DOM 에서 색·크기를 읽어(청록 탭 바 `rgb(58,126,147)` · 글자 12px · 아이콘 37px · 초록/빨강) **순서·밀도까지 그대로** 옮겼다.
**사양·실측은 `docs/통계집계.md` 의 그 절.** 함정만:

- **★★★ "최대한 같게" 는 색·순서·밀도까지다.** "내 사이트에 맞춰서" 를 색 바꿔도 된다는 뜻으로 읽으면 안 된다.
  **그리고 없는 데이터 때문에 뺀 칸은 반드시 보고에 적을 것** — 첫 판에서 9라인 상성·룬별 표·추이 그래프를 빼고 말을 안 해서 두 번째 지적이 나왔다
- **★★ 그쪽 핵심 칸의 자료가 우리에게 없어서 집계를 같이 만들었다** — `champbuilds` type 20종
  (타임라인 18: 스킬 순서 6/10/15·선마·시작·초반·세트 2~5·신발·1~6번째 + 룬 낱개 `perk` + 분모 `tlall`),
  `champmatchups` 를 **한 판의 나머지 9명 전부**로 (fpos·rel 칸), `/api/champion-trend`(일별 7일). 수집은 안 바꿨다 — 한 줄만 빼고:
- **★★ 타임라인은 16.17 부터만 받고·센다** (`TL_MIN_PATCH`, 사용자 지시 "16.16 은 아이템·스킬 로그 없이"). 낮에 켤 때 버전을 안 가려
  16.16 판 1,032개에 로그가 들어갔는데 집계가 건너뛰므로 화면엔 안 나온다 (안 지웠다). **화면은 `tlall` 줄이 없으면 로그 칸을 통째로 안 그린다**
- **★ 패치 선택은 상세 페이지 툴바 `#lx-scope`** — 사용자가 "26.17" 이라 부르는 게 우리 `16.17` 이다. 표기는 사이트 전체가 16.x 라 그대로 뒀다
  (바꾸려면 헤더·통계 표 드롭다운·여기 셋을 한 번에)
- **★ "완성 아이템" 은 DD `into` 를 그대로 보면 안 된다** — 2단계 장화가 상위 장화로 조합돼 `into` 가 있다. 협곡에서 살 수 있는 `into` 만 세고 장화는 예외
- **★ 픽률 분모가 둘** — 타임라인 type 은 `tlall`, 나머지는 `all`. 섞으면 "도란검 1.2%"
- **★ `.lx-badge` 는 두 단 선택자** — 공용 `[data-tooltip] { position: relative }` 가 뒤에 있어 한 단이면 배지가 초상화 아래로 떨어진다
- **★ 박제 `TYPE_LIST`·`ARCHIVE_TYPE` 뒤에 20개, `m` 배열 뒤에 fpos·rel** — 앞에 끼우면 안 되는 그 규칙
- **★★ bash `node -e "…"` 안에 백틱을 넣지 말 것** — 이 절을 처음 쓸 때 백틱이 전부 명령 치환으로 먹혀 문서에 빈칸만 남고 루트에 `=` 파일이 생겼다.
  긴 글은 Write 로 파일에 쓰고 스크립트가 파일을 읽어 끼운다
- 안 넣은 것: 우측 하단 상자들(지시) · 헤더의 스킨 일러스트(광고성 자리) · "Highest Win Build" 는 칸마다 승률 최고(세트 단위 자료 없음)
- **배포 뒤 첫 매시간 집계가 돌아야 새 줄이 생긴다.** 로컬 확인은 카밀 탑에 가짜 줄을 끼워서 했다 (`inject2.js`, 잡 tmp)

### ★ 「PC 버전」 스위치 (2026-08-26 밤, 사용자 요청 — 폰 브라우저의 "데스크톱 사이트" 가 안 먹었다)

푸터 「버그제보 및 피드백」 오른쪽에 **`PC 버전` / `모바일 버전`** 링크. 누르면 `localStorage.pixlol_pc = '1'` 을 두고 새로고침한다.

- **★ 실제 전환은 `index.html` 머리의 인라인 스크립트가 한다** — 플래그가 있으면 viewport 메타를 `width=1240` 으로 바꾼다.
  CSS 보다 먼저 돌아야 `768px 이하` 규칙이 처음부터 안 탄다 (app.js 에서 바꾸면 첫 그림이 폰 배치로 나갔다가 튄다).
  실측: 390px 기기에서 켜면 `clientWidth 1240` · 수치 상자 4칸 · 헤더 가로 배치
- **★ 링크는 터치 기기(`pointer: coarse` 또는 `maxTouchPoints > 0`)이거나 이미 켜져 있을 때만 보인다** — PC 에서 "PC 버전" 은 뜻이 없다.
  헤드리스 기기 에뮬레이션은 coarse 가 안 잡혀 링크가 안 뜨는데 실기기는 뜬다
- **★★ 공통 푸터에 `extraLinks` 옵션을 더했다 — 원본 `dogu_template/dogu-ui/dogu-header.js` 를 고치고 `sync-ui.ps1` 로 5곳 동기화** (md5 일치 확인).
  `extraLinks: [{ id, text, href, onClick }]` 이 공통 링크 셋 **뒤에만** 붙고 없으면 아무것도 안 그린다 (다른 사이트는 안 넘기므로 그대로).
  `DOGU_UI.md` 에 한 줄 적었다. 사이트 JS 로 `.dogu-*` 를 덧칠하지 말라는 규칙 때문에 옵션으로 뚫은 것
- 다른 네 사이트(er·maple·loa·tft)는 복사본만 갱신하고 **로컬 커밋만** 했다 (푸시 안 함 — 추가 옵션이라 화면 변화 0)

### ★★ 타임라인을 전수로 받기 시작했다 — TTL 이 바뀌었다

**"판당 617KB라 전수는 못 한다" 는 예전 판단을 뒤집었다.** 필요한 이벤트만 뽑으니 **판당 2.06KB**
(원본의 0.3%)라 전수로 받아도 용량이 남는다. `fetchMatchStats` 가 같은 루프에서 받아
슬림 문서에 `sk`(스킬 순서 문자열) · `it`(구매, 평평한 배열) 로 넣는다.

- **★★ TTL 이 바뀌었다: `matchcaches` 7 → 3일 · `matchstats` 30 → 21일.**
  아래 "MongoDB 용량" 절의 숫자가 이 값이다
- ~~**★★ 그래서 박제 시한이 9/14 → 9/6 으로 당겨졌다** (첫 수집 8/16 + 21일)~~ → **2026-08-27 에 마감 자체가 없어졌다** (닫힌 패치 재집계 중단)
- **★ 집계는 같은 날 밤에 만들었다** (`buildTimelineFacet`, 위 절). **16.17 부터만** 받고 센다 — 16.16 페이지엔 로그 칸이 아예 없고,
  16.17 은 표본이 얇은 동안 "타임라인 표본을 모으는 중" 이 뜨는 게 정상이다

### 도감 소환사 주문 — 수치·채택률·모드

주문 상세가 도감에서 제일 휑해서 채웠다. 자세한 건 도감 절에 있고 요약만:
**실제 수치 9/9**(`shared.cdtb.bin.json`) · **채택률 + 라인별 채택률**(집계 추가 0) ·
**못 쓰는 모드 표기**. **시연 영상은 라이엇이 안 만든다 — 다시 찾지 말 것.**

## ★★ 다음 세션 먼저 볼 것 (2026-08-24 심야 세션 끝 — 할 일 총정리)

**하루에 커밋 30개 가까이, 전부 배포 확인.** 낮엔 우상단 값 + 인게임 확인, 밤엔 881슬롯 전수 재점검 + 사용자
폰 검토로 규칙 넷(2차 폼 쿨·spec 우선·폭 위키 우선·폭 ×2 예외)과 모바일 헤더. **사거리검토 65자리는 끝났다**
(49 반영 + 원뿔 정책 확정). 아래가 **남은 것 전부**다 — 급한 순.

### 시한이 있는 것 둘

| 언제 | 무엇 |
|---|---|
| ~~8월 26일 (수)~~ **끝났다** | ~~16.17 패치~~ — 2026-08-26 반영 완료. **위 절 참고** (패치노트 대조에서 세 자리가 나왔다). 한국시간 **16시 이후**에 **`node run_patch.js`** 한 줄 (2026-08-25 신설 — 11단계 + 검증 3종, 11분쯤. `--check` 로 CD 가 따라왔는지 먼저 볼 것. 커밋은 사람이 한다). 버전은 `dd_version.js` 가 자동. 오전에 돌리면 CD 가 옛 패치라 멈춘다. **★ 패치 뒤 `node audit_skill_meta.js` · `node audit_full.js` 가 0 / 11 인지 볼 것** — 오늘 넣은 규칙(spec 우선·폭 위키 우선·2차 폼 rank1)이 새 bin 에서 새 자리를 잡아낼 수 있다 |
| **8월 27일 (목)** | **16.16 마지막 표본 → 최종 집계 → 박제.** 로그에 `v:16.17` 이 보이면 16.16 수집이 끝난 것이다. `REBUILD_STATS=1` → `build_stats_archive.js --write` → 배포 → `--delete` |
| ~~**9월 6일 (토)까지**~~ **마감 없어짐 (2026-08-27)** | ~~패치 원본 박제 마감~~ — `buildChampStats` 가 **닫힌 패치(최신 아님 + 마지막 경기 3일 경과)를 재집계에서 빼서** 원본이 TTL 로 사라져도 값이 안 줄어든다 (`PATCH_FREEZE_DAYS`). 박제는 **용량용 선택 작업**이 됐다 — 아무 때나 `build_stats_archive.js`. `docs/통계집계.md` "패치 원본 박제" 절 머리의 2026-08-27 메모 |

### 다른 레포에 넘길 것 (오늘 생긴 빚)

> **2026-08-25 해결**: 원본을 `dogu_template/dogu-ui/` 로 옮기고 5개 사이트 전부 `sync-ui.ps1` 로 동기화·커밋했다. 아래 두 항목은 끝난 일.

- **`dogu_template/dogu-ui/` 원본 변경이 그 레포에서 커밋 안 됐다** — 게임명 표시·`gamesOrigin`·모바일 햄버거 (`DOGU_UI.md` 에 적어 둠).
  **er·maple·loa·tft 의 복사본도 옛 판** — 각 레포 `public/` 에 다시 복사해야 폰 헤더가 같아진다 (그쪽은 `gamesOrigin` 안 줘도 됨)

### 인게임으로만 갈리는 것 (PC 앉을 때 · `인게임확인_20260824_전수점검.md`)

- **투사체 속도 3자리** — 소나 Q(우리 1500 / 롤 1300 / 나무 2000, 셋이 다름) · 아리 R 정기(2200 / 1400) · 노틸러스 R(1400 / 가속식 / 나무 2000).
  **리플레이 0.25배속**으로 발사→도달을 재면 스탑워치가 넉넉하다. 근소 10자리(제라스 E·드레이븐 E·아칼리 E·그레이브즈 W·
  누누 E·룰루 Q·아이번 W·탈리야 R 등 ±100)는 안 봐도 된다
- **카사딘 E 사거리 400 vs 롤위키 원뿔 600** — 원뿔 부류 중 유일하게 둘 다 표시 중. 연습 모드에서 E 표시원을 Q(650)와 비교하면 갈린다
- **키아나 W 투사체 1000** — bin 계열 3종(풀·물·바위)이 전부 2000. 위키는 숫자 없음. 칼 던지는 속도를 보면 됨
- **`WIDTH_KEEP_BIN` 5자리** — 위키 폭이 부분값/추정이라 bin 을 남긴 자리(그웬 Q 120 · 루시안 R 120 · 미포 R 200 · 비에고 E 140 · 아칼리 Q 140). 폭이 13/13 위키였으니 이 다섯도 의심은 남는다
- **시전시간 11자리 bin≠위키** (루시안 Q·로크 E·렉사이 R·리븐 W·릴리아 E·신 짜오 W·아펠리오스 R·오른 Q·이블린 Q/W·조이 E) — 우리 = bin 원본이라 유지가 원칙. 굳이 볼 필요 없음
- **audit_full ③ 각주 없음 11자리** — 예전부터 있던 기준선 (그라가스 P 등 패시브 쿨 레벨 각주). 각주를 달지 말지 판단만 남음

### 판단이 애매해서 보류한 것 (데이터로 더 못 감)

- **★ 숙제: 원뿔 스킬 "최대 사거리 지점의 폭"** — 위키 angle 칸에서 `2·r·sin(각도/2)` 로 파생 가능. 각도 자체를 스킬 폭 대신/보완으로 실을지 판단
- **사거리가 롤위키와 ±50~65 어긋난 74곳** — 기본 공격은 가장자리 기준(A 원 = 사거리 + 내 반경 65), 스킬은 중심 기준이라 **이 기준 차이일 가능성이 크다.** "차이가 65 근처냐" 로 걸러 부류인지 확인할 것 (아칼리 Q 550/500 · W 350/250 포함)
- **위키 다중값 37자리** — 52자리 중 문장이 뒷받침하는 15자리만 반영. 나머지는 둘째 값의 뜻(돌진·이펙트·재사용)이 자리마다 달라 보류
- **아펠리오스 Q 사거리 1450** — 만월총 기준. 무기별 사거리는 문장에 있어 칸을 뺄지 판단 / **쓰레쉬 R 450** — 벽 반경이라 효과 범위로 옮길 수도 / **탈리야 E 950** 유지
- **룰루 W 시전시간 0.2419** — 적에게 쓸 때만(아군은 즉시). 위키도 그렇게 적는다. "(적 대상)" 표기를 붙일지
- **자이라 W 재충전·신지드 Q '없음'** — 오늘 결정 끝. 신지드 토글 내부 쿨 1초(위키)는 클라에 없어 안 적음
- **정체를 모르는 bin 숫자 셋** — 워윅 W `BloodScent castRange 1600` · 오로라 W `override 700 · castRadius 1000` · 조이 W `override 3000/4500/6000`. 화면엔 안 나간다
- **시전시간 4자리(케일 Q·미포 Q·비에고 Q·스몰더 Q)** — 위키가 "windup" 이라고만 하고 숫자 없음. bin 계산법은 자야에서 검증 실패

### 오늘 세션 뒤 상태 요약

- audit_skill_meta **0** / audit_full **11**(기준선) · STAT_MANUAL 128 · STAT_DROP 152 · CD_MANUAL 12 · 중복 0
- 규칙 우선순위: 사거리·시전시간·속도 = `STAT_MANUAL > bin > 위키` / **스킬 폭만 `STAT_MANUAL > 위키 > bin×2`** (예외 `WIDTH_KEEP_BIN`)
- 루트 gitignore 파일: `인게임확인_20260824_전수점검.md`(남은 확인) · `사거리검토_20260824.md`(끝, 권고 표만 남음)

### ★★ 오늘 세션에서 얻은 판단 규칙 (반복해서 쓰였다)

1. **"그 숫자가 나올 수가 없는데" 라는 지적이 제일 빠르다.** 트위치 R 은 공격 사거리를 300 늘려 주는
   버프 스킬이라 사거리 1200 이 성립하지 않는데, **bin 에 실제로 1200 이 있어서 데이터로는 안 걸렸다**
2. **한 스킬에 객체가 여럿이면 어느 것을 집었는지 확인할 것.** 오늘 틀린 값이 죄다
   "다른 객체의 값" 이었다 — 트위치 R 은 E 값, 징크스 Q 는 공격 시전시간, 초가스 E 는 미사일 비행 거리
3. **롤위키 `Global` 은 괄호 설명까지 읽을 것.** 워윅 W 의 전역은 **패시브 감지**였고,
   `효과 범위 = 전역` 9자리는 사실 **"재사용 사거리가 전역"** 이었다
4. **인게임 동작 설명이 위키 칸보다 먼저다.** 사용자가 "공격 사거리에 300을 더하는 것" 이라고
   말해 줬는데 위키 숫자(1100)를 우선해서 한 번 더 틀렸다
5. **★ `STAT_MANUAL`·`STAT_DROP` 에 줄을 더할 땐 키 중복 검사를 돌릴 것.** 오늘 **다섯 번** 잡혔다
   (코르키 R 투사체·밀리오 W·자야 Q 사거리·징크스 Q 시전시간·나서스 R 투사체가 사라질 뻔했다).
   **줄 중간까지 훑어야 한다** — `'녹턴 R': {…}, '쉔 Q': {…}` 처럼 한 줄에 여러 항목이 있다:
   ```js
   const clean = blk.split('\n').map(l => l.replace(/\/\/.*$/, '')).join('\n');
   const keys = [...clean.matchAll(/'([^']+)'\s*:\s*[\{\[]/g)].map(m => m[1]);
   ```
6. **새 절을 위에 끼워 넣을 땐 앞 절의 상태 문구를 같이 고칠 것.** 오늘 "아직 안 했다"·"판단 보류"
   가 남아 모순된 게 **세 번** 있었다

### 손 안 댄 변경이 작업 트리에 있다

> **2026-08-25**: 그 변경은 원본과 같은 내용이었고 이미 커밋돼 있다. 끝난 일.

`public/dogu-header.js` · `public/dogu-ui.css` 에 **자동완성 기능이 추가된 변경**이 커밋 안 된 채 있다.
**내가 한 게 아니고** 이 둘은 `dogu_template/dogu-ui/` 의 복사본이라 여기서 고치면 안 되는 파일이다
(위 "dogu.gg 공통 UI" 절). 원본 쪽에서 온 것으로 보이니 그쪽 상태를 먼저 볼 것.

## 2026-08-21 세션 (지난 기록)

**커밋 9개가 전부 배포됐고 프로덕션에서 실측까지 했다.** (시한·할 일은 위 2026-08-24 절이 최신이다)

| 커밋 | 무엇 |
|---|---|
| `5fedf7e` | **도감 룬 트리** — 인게임 룬 화면과 같은 자리. 목록 순서도 자리순 + 핵심 룬 표시 |
| `653118c` | 파편 두 칸짜리(적응형·체력 증가)에서 **누른 칸만** 켜지게 |
| `18b4979` | **룬 설명의 텍스트 강조를 전부 껐다** (요청 — "나중에 다시 보자") |
| `fa0db3c` | **룬 레벨 각주 + 그래프** (롤위키 수치). `drawGraph` 헬퍼를 `app.js` 로 옮겼다 |
| `7e38cd9` | CLAUDE.md 정리 (144 → 94KB). `docs/신화상점.md`·`docs/챔피언탭.md` 분리 |
| `a1911ab` | **라인 상성/카운터** (`champmatchups`) — 통계 탭 펼침 패널 오른쪽 |
| `30ba69e` | **최종 아이템 통계** + **인원 밴드(8~10 / 5~7) 폐지** |
| `61921be` | **파비콘 교체** — 마법사 모자 쓴 시바 |
| `18fa5ab` `ec74d87` | 문서: 과거 경기 **180일 벽** · 장인 랭킹이 왜 지금 방식으론 안 되나 |

### 지금 무엇을 하면 되나

**★★ 시한이 있는 것 둘 — 이것만 날짜가 정해져 있다.**

| 언제 | 무엇 |
|---|---|
| ~~8월 26일 (수)~~ **끝났다** | ~~16.17 패치~~ — 2026-08-26 반영 완료. **위 절 참고** (패치노트 대조에서 세 자리가 나왔다). 그날 **한국시간 16시 이후**에 빌드 스크립트 9개를 순서대로 (`docs/스킬데이터.md` 의 순서표. 앞이 뒤의 입력이라 순서가 중요하다) |
| ~~9월 2~3일~~ ~~9월 6일까지~~ **마감 없어짐** | ~~패치 원본 박제.~~ 2026-08-27 에 닫힌 패치 재집계를 멈춰서 시한이 사라졌다 (위 표가 최신이다) |

**★ 박제를 안 하면 16.16 통계가 조용히 틀어진다** — 원본이 TTL 로 한 건씩 사라지는 동안
매시간 재집계돼서 "916판" 이 "12판" 같은 값으로 굳는다. 그래서 상성·최종 아이템을
**박제 전에** 만들어 둔 것이다 (원본이 살아 있어야 소급이 된다).

**남은 후보 (키 없이 되는 것)**: ① 전적 상세 **타임라인 골드 그래프**(타임라인은 이미 받고
있다 — 다만 **원본을 DB 에 저장하면 안 된다**, 한 개 617KB 로 예전에 173MB 먹었다.
2026-08-26 에 통계용으로 넣은 `sk`·`it` 는 **필요한 이벤트만 뽑은 2.06KB 슬림**이라 다른 것이다) ②
**og: 미리보기 카드**(`sendIndexHtml` 이 주소별 메타 태그를 끼우는 서버 작업) ③ 즐겨찾기 보완.

**여전히 남은 숙제**: ① 패치 원본 박제 ② `MYTHIC_ITEM_END` 손표 갱신
③ 인게임 확인 2건(`인게임확인_20260816.md`) ④ 아레나 표 실화면

**사용자 실기기 확인 대기 (물어보면 이 목록):**
- iOS 주소창 뒤 밝은 띠 — `body::before` -150px 확장으로 고쳤는데 **아이폰 실물 확인 전**
- 폰 전적 카드 — op.gg 따라 재제작했고 피드백이 올 것
- 터치 역할군 말풍선 — 2개 켰을 때 겹쳐 보이면 다른 방식으로 바꾸기로 했다

**감시 포인트:** 홈 PBE 칸이 비면 Railway 로그에서 `[PatchNotes]` 를 볼 것 — 니터 4개 소스가
다 죽은 것이다. 통계 픽률 커트라인 기본값은 `showStats()` 의 `minPick = 1` 하나다.

**★★ 로컬에 옛 서버 프로세스가 남아 있을 수 있다 (2026-08-19 발견 · 08-21 재확인).**
포트 3000 은 **다른 프로젝트**(`npm run dev` → `server/server.js`)가 잡고 있는 날이 있었다.
`netstat -ano | grep :3000` 으로 **어느 PID 가 잡고 있는지부터 볼 것.** 화면만 볼 거면
`READONLY_JOBS=1 PORT=3100 node server.js` 로 다른 포트에 띄운다.

### ★★ 장인 랭킹은 접었다 — 사용자가 방식을 무너뜨렸다 (2026-08-21)

**"프로덕션 키만 나오면 된다" 가 아니었다. 방식 자체가 틀렸다.**

> 스플릿(S16 3번째 = **2026-07-29 ~ 2027-01-06**) 내내 갱플랭크를 잔뜩 한 사람이
> 우리 수집 기간엔 **다이아**로 지내다가 **마지막 날 마스터로 올라오면**,
> 명단에 들어온 순간 그의 스플릿 기록이 **통째로 비어 있다.**

우리 수집은 "그날 명단에 있는 사람" 의 매치리스트만 훑기 때문이다. 앞으로 아무리 쌓아도
나중에 올라온 사람의 과거는 안 채워진다.

- **★ 구멍 실측**: 명단 신규 진입이 **하루 207~370명**(평균 310), 나흘에 10%가 스쳐 간다
  (5일 합집합 12,460 vs 그날 11,291). 스플릿 160일이면 **3~4만 명이 명단을 거쳐 간다**
- **★ 유일하게 맞는 방식**: "명단에 새로 들어온 사람의 **스플릿 전체를 그때 한 번** 받는다".
  스플릿이 180일 창 안이라 가능은 하다 (1/6 기준 180일 전 = 7/10)
- **★ 그 비용이 하루 13만 호출** — 개발 키 한도(7.2만/일)의 1.8배. 프로덕션 키면 3%
- **결론: 프로덕션 키 + 진입 시 개인 백필**이 둘 다 있어야 한다. 그때 다시 꺼낼 것.
  자세한 계산은 `docs/통계집계.md` 의 그 절에 있다

### ★★ 이번 세션에 드러난 것 (2026-08-21)

- **★★ 과거 경기는 180일까지만 받아온다** (실측). match-v5 매치리스트가 6개월 롤링 창이고
  **창은 매일 하루씩 밀린다** — 오늘 안 받은 판은 영영 못 받는다.
  **티어로 경기를 고르는 엔드포인트는 없다** (티어는 사람 목록에만 있다).
  `docs/통계집계.md` 에 근거까지 박아 뒀다
- **★★ 위키가 아니면 못 얻는 값이 있다.** 룬 레벨 스케일 20자리 중 19개는 직선인데
  **`생명 흡수` 하나가 비선형**이다 (`1 · 1.25 · 1.5 · 1.75 · 2 · 3 · 4 · 5 …`).
  게임 데이터엔 **최소·최대 두 숫자뿐**이라 직선으로 그렸으면 **6레벨이 3이 아니라 4.2** 였다.
  "게임 데이터가 정본" 이라는 원칙에도 **곡선 모양은 예외**다
- **★★ 로드맵을 코드보다 먼저 믿지 말 것.** `로드맵_20260820.md` 가 "즐겨찾기·최근 검색"을
  없는 기능으로 적어 놔서 그대로 추천했는데, **이미 `app.js` [6]절에 통째로 있었다**
  (드롭다운 탭 2개 · 각 10개 · × 삭제 · 프로필 즐겨찾기 버튼). 로드맵 첫 판이 코드를 안 보고
  쓰인 항목이 있다는 뜻이다 — **그 파일의 "없다" 는 항목은 grep 한 번 하고 믿을 것.**
  (그 줄은 정정해 뒀다. 남은 건 목록에 티어·아이콘 붙이기 정도)
- **★ 즐겨찾기 목록이 닉네임을 `onclick` 문자열에 박고 있다** — 랭킹 표에서 이미 겪은
  아포스트로피 함정이다. 지금 명단 11,000명엔 해당 닉네임이 0명이라 급하진 않다
- **★★ 배포 중에 옛 `app.js` + 새 데이터 파일이 섞일 걱정은 없다.** `renderIndexHtml()` 이
  모든 `.js` 참조에 `?v=mtime` 을 붙이고 index.html 은 `no-cache` 라 **항상 한 세트**로 받는다
- **★ 도감 클래스 이름이 부딪히기 직전이었다** — `.codex-tree-*` 는 **아이템 조합식 트리**가
  이미 쓰고 있어서 룬 트리는 `.codex-rune-*` 로 뗐다. 도감에 트리가 둘이라는 걸 기억할 것
- **★ 헤드리스에서 폰 넘침 48px 은 또 허상이었다** (`data-tooltip`). 속성을 떼고 재면 0.
  **이 함정은 이번이 두 번째다** — 폰 넘침을 잴 땐 툴팁부터 빼고 재라

### 파비콘 (2026-08-21 교체)

마법사 모자 쓴 시바. 원본은 `public/favicon_lol.png`(1254px, **1.9MB**)이고 화면이 쓰는 건
**`favicon_lol_32/64/180.png`** + 다시 만든 `favicon.ico`(32·64 두 벌을 담았다) 다.

- **★★ 원본에는 "투명" 을 뜻하는 체커보드가 **그림으로 구워져 있다** (24bpp, 알파 없음).**
  귀퉁이가 `rgb(253,253,253)`, 체커 칸이 `(243,243,243)` 인 **불투명한 흰색**이라
  그대로 걸면 탭에 흰 사각형이 뜬다
- **★ 네 귀퉁이에서 flood fill 로 "이어진 밝은 무채색"만 지웠다** (`밝기>=232 && max-min<=8`).
  전역으로 "흰색이면 다 지우기" 를 하면 **모자 은색 띠**(887px)와 **주둥이 흰 털**(549px)이
  뚫린다 — 둘 다 밝고 무채색이다. 실측으로 확인했다
- **★ 1.9MB 를 직접 걸면 안 된다** — 파비콘은 모든 페이지가 받는다. 32px 2.7KB / 180px 60KB
- 만든 스크립트는 일회성이라 저장소에 안 넣었다 (zlib 만으로 PNG 를 읽고 쓰는 100줄짜리).
  다시 만들 일이 잦으면 `build_favicon.js` 로 넣을 것. **원본 1.9MB 는 public/ 에 그대로 있다** —
  아무 데서도 안 쓰는데 배포에 실려 다니므로 지우거나 옮길지 판단이 필요하다

### ★★ 08-19 에 드러난 규칙 둘 (계속 유효하다)

**① 잡을 다른 잡에 얹으면 그 잡의 실패에 인질로 잡힌다.**
`saveRankCutoff` 를 `saveApexDrift` 끝에 붙여 놨더니 **라이엇 조회가 실패한 사이클에서는
저장 시도조차 못 했다.** 그걸 메우려고 "시각 창을 넓히고 6번 재시도" 라는 군더더기가 붙었고,
사용자가 **"굳이 왜 그렇게 함?"** 하고 짚었다. 떼어내니 **재시도 장치가 통째로 필요 없어졌다** —
컷 계산은 원래 라이엇 호출이 0인 일이었다. **"왜 이렇게 복잡하지" 싶으면 얹혀 있는 곳을 볼 것.**

**② 설명할 때 범위를 밝히지 않으면 없는 사고가 된다.**
"라이엇 호출도 0이야" 라고 썼는데 나는 **`saveRankCutoff` 함수 하나**를 말한 거였고
사용자는 **그 시간대 전체**로 읽어서, **"왜 00:00~00:29 호출을 날렸냐"** 는 지적이 나왔다.
코드는 멀쩡했고 문장만 틀렸다. **"무엇이 0인지" 를 주어로 못 박을 것.**

## ★★ 컷라인 개편 · 티어 재계산 1분 실측 (2026-08-19)

### 1분 표본 실측 — "관측 시차" 가 맞았다

8/18 밤 23:30~00:30 을 1분 격자로 61줄 받았다. **핵심 줄:**

```
23:50   c300 292  cMin 1773  gMax 1946  gMin 1175  mMax 1444   챌 8명 어긋남 (재계산 전)
23:51   c300 294  cMin 1823  gMax 1946  gMin 1175              ← 챌린저 쪽만 새 값
23:52   c300 297  cMin 1773  gMax 1822  gMin 1311              ← 그마 쪽만 새 값 (챌린저는 도로 옛 값)
23:53   c300 294  cMin 1823  gMax 1984  gMin 1175              ← 또 뒤집힘
★23:55  c300 300  cMin 1823  gMax 1822  gMin 1311  mMax 1444   ← 어긋남 0 + gMax <= cMin
★23:56  c300 300  g1000 699  cMin 1823  gMax 1822  mMax 1339   ← 마스터 경계까지 완료
```

- **★★ `gMax <= cMin` 이 실제로 잡혔다 → 라이엇 승격 기준은 순수 LP 컷이 맞다.**
  8/17 에 5분 해상도로 1명이 남아 보였던 건 **그 순간을 못 본 것**이었다
- **★ 두 경계는 1분 차이로 처리된다** — 챌린저↔그마 23:55, 그마↔마스터 23:56.
  8/17 에 "5분 차이" 로 보였던 건 격자가 5분이라 그렇게 보인 것이다
- **★ 재계산은 한 점이 아니라 23:51~23:55 의 4분짜리 구간**이고, 그 안에서는
  **한 줄 안의 값끼리도 서로 다른 시점 것이 섞인다** (23:52 는 그마만, 23:53 은 챌린저만 새 값)

### ★★ 라이엇 API 가 분 단위로 다른 스냅샷을 준다 (새로 알아낸 것)

61줄에서 **"두 칸 전 값으로 되돌아감" 이 11건**이고 서로 다른 `cMin/gMax/gMin/mMax` 조합이
**15가지**였다. `23:47 gMax 1946→1984→1946`, `00:04 cMin 1823→1813→1823` 같은 식이다.

- **우리 쪽 문제가 아닌 걸 확인했다** — ① 인스턴스가 둘이면 한 분에 두 줄이 찍히는데
  **62분에 62줄, 중복 0건**이고 초도 3~14초에 몰려 있다(한 프로세스) ② `updateChallengerList` 의
  실패 폴백은 **직전 명단을 유지**하는 코드라 값을 **고정**시키지 옛 값으로 **되돌리지 못한다**
- **★ 그래서 갱신 직후 한 줄만 보고 판단하면 안 된다.** 컷을 "표본 여러 개 중 최선" 으로
  고르게 바꾼 이유가 이것이다

### ★★ 컷라인 규칙 (2026-08-19 개편) — `rankcutoffs`

**`23:45~23:59` 의 1분 표본 15개 중 `c300 + g1000` 이 가장 큰 하나**를 골라
그 표본의 **챌린저 최저 LP / 그마 최저 LP** 를 하루 한 줄 남긴다.

| | 예전 (08-18) | 지금 (08-19) |
|---|---|---|
| 표본 | 23:45~23:55 창의 **첫 성공 한 줄** | 23:45~23:59 **15개 중 최선** |
| 챌컷 | `lp300` = **300등의 LP** | `lpChal` = **챌린저 최저 LP** |
| 그마컷 | `lp1000` = **1000등의 LP** | `lpGm` = **그마 최저 LP** |
| 도는 시각 | 23:45~23:55 | **한국시간 자정 +30초, 하루 한 번** |
| 어디에 붙어 있나 | `saveApexDrift()` 끝 (명단 갱신에 얹힘) | **독립 잡** (`scheduleCutoffJob`) |

- **★★ 왜 바꿨나: 23:45 는 재계산 10분 전이라 하루 중 어긋남이 가장 큰 순간이었다.**
  8/18 실측이 챌린저 8명·그마 20명 어긋난 상태였고, 그래서 **"300등의 LP" 1830 이
  실제 챌린저 컷 1773 과 57 차이**, 1000등 1330 과 그마 최저 1175 는 **155 차이**였다.
  그 300등은 **그마**였고 1000등은 **마스터**였다 — 티어 컷이 아니라 그냥 등수였다
- **★★ 고르는 기준이 `c300 + g1000` 인 이유**: 둘 다 클수록 **LP 순위와 티어 소속이 덜
  어긋난 순간**이고, 그 순간의 티어 최저 LP 가 곧 그날의 진짜 컷이다. 합이 999~1000 이면
  재계산 직후를 집은 것이다. **동점이면 먼저 나온 표본**(`>` 비교) — 가장 이른 시점이다
- **★★ 필드 이름을 같이 바꿨다 (`lp300`→`lpChal`, `lp1000`→`lpGm`).**
  이름을 두고 뜻만 바꾸면 **옛 점과 새 점이 한 그래프에 섞여도 아무도 못 알아챈다**
- **★★ 화면의 "현재 커트라인" 숫자는 여전히 300등·1000등의 LP 다** (`app.js` 의 `lpAt`).
  **일부러 기준이 다르다 — 맞추려 하지 말 것.** 저건 "지금 몇 점이면 300등 안인가" 라는
  실시간 값이고, 그래프 점은 그날의 티어 경계다. **재계산 전 시간대에는 둘이 50~150 LP 벌어진다**
- **★ 표본을 따로 들고 있지 않는다** — `apexdrifts` 가 이미 분마다 `c300·g1000·cMin·gMin`
  을 저장하므로 `pickCutoffSample()` 이 그걸 되읽는다. 재시작해도 안전하고,
  **규칙이 바뀌면 소급해서 다시 뽑을 수 있다** (apexdrifts TTL 7일 안에서)
- **★★ 명단 갱신 잡에 안 묶여 있다 (2026-08-19).** `scheduleCutoffJob()` 이 자정 +30초에
  혼자 돈다. **예전엔 `saveApexDrift()` 끝에 얹혀 있었는데, 그러면 그 사이클의 라이엇 조회가
  실패했을 때 저장 시도조차 못 한다** — 그걸 메우려고 "00:00~00:29 안에 6번 재시도" 같은
  군더더기가 붙었다. 떼어내니 **라이엇 호출과 완전히 무관해져서 한 번만 돌면 된다**
  (표본은 이미 `apexdrifts` 에 다 들어 있다). 표본이 없으면 5분 뒤 한 번 더 본다
- **★ `day` 는 지금 날짜가 아니라 어제다** — 자정 직후에 도니까 안 그러면 8/18 밤 자료가
  8/19 로 들어간다. 검산: 실행 `08-20 00:00:30` → `day = 2026-08-19`
- **★ 00:00~00:29 에도 명단 갱신은 5분마다 그대로 돈다** (`00:00 00:05 … 00:25`, 6사이클).
  **컷 계산에 안 쓸 뿐 호출을 줄인 게 아니다** — 헷갈리기 쉬운 자리다
- **백필 결과 (2026-08-19)**: 8/17 → `1822 / 1327` (표본 3개, 그날은 5분 간격) ·
  8/18 → `1823 / 1311` (표본 15개, 23:56 채택). 옛 8/18 줄(`1830/1330`)은 갈아엎었다

**사용자 실기기 확인 대기 (물어보면 이 목록):**
- iOS 주소창 뒤 밝은 띠 — `body::before` -150px 확장으로 고쳤는데 **아이폰 실물 확인 전**
  (모바일화면1.png 가 증상 사진. 루트, gitignore)
- 폰 전적 카드 — op.gg 따라 재제작했고 "보고 수정사항 말해줄게" 라고 했다. 피드백이 올 것
- 터치 역할군 말풍선 — 2개 켰을 때 겹쳐 보이면 다른 방식으로 바꾸기로 했다

**새로 생긴 감시 포인트:** 홈 PBE 칸이 비면 Railway 로그에서 `[PatchNotes]` 를 볼 것 —
니터 4개 소스가 다 죽은 것이다 (아래 2차 반영 절). 통계 픽률 커트라인 기본값은
`showStats()` 의 `minPick = 1` 하나다.

## 2026-08-19 — 수정사항 0819.txt 24건 반영 (한 커밋)

**루트 `수정사항 0819.txt` 의 24개 항목을 전부 처리했다.** 새로 생긴 규칙만 적는다:

- **★ 패치노트 영역 (홈, 신설).** `server.js` 의 `/api/patch-notes` 가 공식 홈페이지
  패치노트 태그 페이지를 긁는다 — **Next.js 라 `__NEXT_DATA__` JSON 안에 기사 목록이
  통째로 있다** (`blades` 중 `articleCardGrid.items`, 최신순). 30분 캐시 +
  마지막 성공값 폴백(`lastGoodPatchNotes`). **UA 를 브라우저 꼴로 보내야 한다.**
  라이엇이 페이지 구조를 바꾸면 `[PatchNotes] 수집 실패` 경고가 남고 화면은 폴백 안내로 간다
- **★★ PBE(매튜 렁 X) 글은 자동 수집이 안 된다 (2026-08-19 확인).** X 는 로그인 장벽이고,
  우회로인 `syndication.twitter.com/srv/timeline-profile/…` 은 **2025-11 에 얼어붙은
  스냅샷**을 돌려준다 (100건 받았는데 최신이 2025-11-17). 낡은 글을 최신인 척 내보낼 수
  없어서 오른쪽 칸은 프로필 바로가기 안내로 뒀다. **니터 인스턴스도 사실상 전멸이다**
- **헤더 로고는 텍스트 워드마크다** (`logo.webp` 안 씀). 홈과 같은 Chakra Petch 700
- **★ 폰 헤더는 햄버거 메뉴다** (`.nav-toggle` + `.header.menu-open`). 메뉴 항목을 누르면
  `hideAllContainers()` 가 닫는다. **`hideAllContainers()` 가 `scrollTo(0,0)` 도 한다** —
  "메뉴 옮기면 스크롤 맨 위로" 요청이 이 한 줄이다 (랭킹만 하던 걸 전체로)
- **폰 주소창이 밝던 것**: `theme-color` 메타(#16112a) + 폰 헤더 배경을 0.6 → 0.94 로
  (일부 모바일 브라우저가 backdrop-filter 를 무시해서 밝은 일러스트가 그대로 비쳤다)
- **★ op.gg CDN 핫링크를 전부 뗐다.** 라인 아이콘 → CD `svg/position-*.svg`(금색이라
  **invert(1) 필터도 같이 뺐다**), 랭킹 메달 → CD `ranked-mini-crests/`. 남 사이트 정적
  자산은 리퍼러 차단·경로 변경 한 방에 깨지고 프로덕션 키 심사에도 안 좋다
- **★★ 통계 ALL 은 챔피언×라인 줄이다** (미드 빅토르 / 바텀 빅토르가 딴 줄). 30판 이상인
  라인만 줄이 되고, 어느 라인도 못 넘는 챔피언만 주 라인 한 줄을 흐리게 남긴다.
  **라인 필터에서는 30판 미만을 아예 뺀다** (흐림 아님 — 요청). 룬 패널도 그 줄의 라인을 따른다
- **★ 통계 표의 1px 어긋남 원인: `td` 에 `display:flex`.** td 가 flex 가 되면 table-cell
  이 아니게 되어 border-bottom 이 옆 칸과 어긋난다. **flex 는 안쪽 `.stats-champ-flex`** 가 맡는다
- **호버 색 규칙은 `@media (hover: hover)` 로 감싼다** — 폰에서 탭 자국(회색 눌어붙음)이
  남던 원인. 색은 회색 → 보라 톤(rgba(167,139,250,…))
- **폰 통계 표는 가로 스크롤 없이 6칸이다** (표본 칸 숨김, `min-width: 640px` 폐지)
- **랭킹**: 제목 왼쪽 챌린저 메달 제거 / **폰도 닉네임 말줄임** (`table-layout: fixed` 가
  필수 — auto 면 nowrap span 이 칸을 벌려 말줄임이 안 걸린다) / 폰 티어는
  `챌린저·그마·마스터`(`RANK_TIER_INFO.short`) / **폰 표 우상단에 현재 컷라인**
  (`.rank-cutline`, 명단에서 직접 센다 — 데스크톱은 그래프 카드가 있어 숨김)
- **★ 신화상점 낡은 로테이션 규칙**: 수집 날짜가 **지금 판매 기간 시작(`mythicPeriod().start`)
  보다 앞이면** 상품 대신 "OO 로테이션 상품을 수집 중입니다!" 를 띄운다 — 서버 `stale`
  (주기 초과)로는 갓 바뀐 로테이션을 못 잡는다. **타이머가 초기화 시각을 넘기면
  `updateShopTimer` 가 캐시를 비우고 화면을 다시 그린다** (켜 둔 페이지가 어제 상품을
  계속 보여주던 것). 빈 화면 문구도 전부 "수집 중입니다!" 로 통일
- **폰 추천 구획은 2칸 격자** (flex 는 마지막 홀수 카드가 두 칸으로 늘어났다).
  넥서스 효과 카드(`is-finisher`, 영상 있는 카드에만 붙는다)가 `order:-1` + 두 칸 통짜
- **★ 스킨 로딩**: 누르면 **썸네일(캐시에 있음)을 먼저 걸고 원본이 오면 바꿔 끼운다**
  (`skinViewToken` 으로 경주 방지 — selectChroma 도 토큰을 올려야 한다). 스킨 탭을 열면
  `preloadSkinFulls` 가 나머지 원본을 **한 장씩 순차로** 예열한다 (챔피언 바꾸면 중단)
- 폰 챔피언 로딩 문구는 `.champ-loading` (min-height 65vh) — 상자가 쪼그라들다 튀던 것
- 각주 상자는 **폰에서만** 화면 안으로 클램프한다 (`positionFootnote`, 768px 이하)
- 폰 전적 카드: 상단줄을 `[승리 큐이름 …… 시간 · 날짜]` 로 재배열 (HTML 그대로, flex
  order 만 — 클래스 없는 두 칸은 nth-child(2)=날짜, (5)=시간이다), KDA 16px + 데스 빨강
- **★ 헤드리스로 폰 넘침을 잴 때 `[data-tooltip]` +18px 는 허상이다** — CDP 기기 에뮬레이션이
  hover 미디어를 안 바꿔서 `@media (hover:none)` 숨김이 안 탄다. 실기기는 0 이다

### 2차 반영 (같은 날 추가 메모 7건 + 폰 전적 카드 재제작)

- **★★ PBE(매튜 렁 X) 글 링크는 결국 된다 — `nitter.net` RSS 가 살아 있고 최신이다**
  (2026-08-19 실측: 당일 글까지 옴. **syndication 우회로는 여전히 2025-11 스냅샷**이고
  다른 니터 인스턴스는 403/죽음). `fetchPbePreviewNotes()` 가
  `^Patch NN.NN … Preview` 글만 골라 **첫 줄에 Full 있으면 [상세], 없으면 [간단]** 으로
  `PBE 서버 26.17 패치 [상세]` 꼴 + x.com 주소로 내보낸다 (`/api/patch-notes` 의 `pbe`).
  **니터는 언제 죽어도 이상하지 않다** — 죽으면 lastGood 폴백, 그마저 없으면 화면은
  index.html 의 정적 안내(프로필 바로가기)로 남는다. 공식/PBE 두 소스는 따로 try/catch 다.
  **★★ 니터 미러는 데이터센터 IP 를 막는다 (3차 실측)** — 로컬은 nitter.net 이 되는데
  **Railway 에서는 빈손**이었다. 그래서 `PBE_RSS_SOURCES` 사슬로 4개를 차례로 시도한다:
  nitter.net(브라우저 UA/RSS 리더 UA) → **rss.xcancel.com / xcancel.com (RSS 리더 UA 필수** —
  브라우저 UA 면 "RSS 클라이언트에서만 동작" 이라며 거절한다). 어느 소스가 됐는지
  `[PatchNotes] PBE 소스:` 로그로 남고, pbe 가 비면 캐시를 30분이 아니라 5분만 잡는다
- **폰 헤더는 완전 불투명(#16112a) + 블러 없음** — 0.94 반투명으로도 "주소창 투명" 지적이
  또 왔다.
  **★★★ 이때 같이 넣었던 `html { background-color }` 는 사이트 전체를 밝게 만들어 즉시
  롤백했다 (3차).** html 에 배경이 생기는 순간 body 배경(일러스트)이 루트 캔버스로
  승격되지 못하고 **자기 상자에서 음수 z-index 오버레이(body::before)보다 위에** 그려져
  어둡게 눌러 주던 층이 통째로 무력화된다. **html 에 배경을 깔지 말 것** — style.css
  맨 위에 경고 주석이 있다. 주소창 색은 theme-color 메타가 담당한다.
  **★ 진짜 원인은 4차에 사진(모바일화면1.png)으로 확인됐다** — iOS 사파리가 반투명
  하단 주소창 뒤로 **뷰포트 밖 문서 내용**을 비추는데, fixed 오버레이가 bottom:0 에서
  끝나 그 띠에서만 일러스트가 원본 밝기로 샜다. `body::before` 를 위아래 **-150px 씩**
  삐져나가게 늘려 해결 (데스크톱은 뷰포트 밖이라 잘려서 무영향).
  같은 4차: 터치 기기는 켜진 역할군 버튼(.role-btn.on) 아래에 이름 말풍선을 상시로
  띄운다(hover 가 없어 볼 길이 없었다) / "라인" 헤더 칸에 cursor·user-select 를 잡았다
  (정렬 안 되는 유일한 헤더라 I빔 커서가 떴다).
  5차 — 전적 요약 통계 박스: 폰에서 PC 의 세로 구분선(1x90, 인라인 style)을 숨기는 대신
  **`height:1px !important` 로 눕혀 가로 실선**으로 쓴다(`[style*="width: 1px"]` 선택자).
  패널 가로 가운데 정렬은 `align-items:center !important` 가 맡는다 — column 방향이라
  justify-content 로는 안 된다. **여기 남아 있던 op.gg 라인 아이콘 5개도 CD 로 갈았다**
  (renderSummaryStats 의 posIcon — STAT_LANE_ICON 재사용, invert 필터 제거).
  이제 살아 있는 op.gg 참조는 진짜 0 이다 (showStatsLegacy 주석 안에만 남아 있다)
- **★ 스킨 "썸네일 먼저 → 원본 교체" 는 폰 전용이다** (PC 롤백, 2차). PC 는 캐시된
  원본도 한 프레임 얼굴 중심 구도가 번쩍여서 — PC 는 순차 예열(preloadSkinFulls)만 탄다
- **`.role-btn:hover` 도 `@media (hover:hover)` 로 감쌌다** — 폰에서 역할군을 껐는데
  탭 hover 잔상 때문에 불이 남아 있었다. **`:hover` 규칙을 새로 쓸 땐 항상 이걸 의심할 것**
- 통계 정렬 헤더 hover 는 반투명이 아니라 **단색 #3a2470** (사이트 공통 hover 계단)
- **★ 통계 픽률 커트라인** (`minPick`, 기본 1%): 표 우상단 입력칸(데스크톱), 폰은 칸을
  숨겨 1% 고정. 라인별 줄이라 **그 라인 픽률** 기준으로 거른다
- **★★ 폰 전적 카드는 op.gg 모바일을 실물 스크린샷 보고 그대로 따라 지었다** (CSS 재배열):
  헤더 스트립만 승패색(본문은 공통 #191925) / 원형 초상화 + 왼쪽 아래 레벨 배지 /
  KDA 크게 + `5.40:1 평점` 표기 / 아이템 4×2 격자 오른쪽 / 뱃지는 접음 / 버튼 두 개는
  헤더 안 둥근 사각. **평점 표기는 HTML 에 두 벌**(`.kda-ratio-pc`/`.kda-ratio-m`) 이고
  CSS 가 한쪽만 보여준다 — Perfect 처럼 숫자가 아니면 `:1` 을 안 붙인다.
  인라인 크기를 이겨야 하는 자리(보조 룬 22px·와드 22px)는 `!important` 가 필요하다

## 2026-08-18 세션 (커밋 `bfda884`) — 랭킹 탭 정비

**그때의 할 일 목록은 전부 끝나서 덜어냈다** (임시 1분 계측 → 상시 승격 · 재계산 시각 1분
실측 완료 · 컷라인 두 점 확보). 한 것: `c1000` 계측 추가 · 명단 갱신을 **정각 격자**에 고정
(`nextRankDelay`) · `rankcutoffs` 신설 · 랭킹 표 칸 폭 정리 · **룬 빌드를 라인별로 쪼갬**
(`champbuilds.pos`) · `REBUILD_STATS=1` 도구 · 상세 전적 표 6px 넘침 수정.
자세한 건 아래 "랭킹 탭" 절과 `docs/통계집계.md` 에 규칙으로 남아 있다.

### ★★ 사용자 지적으로 드러난 것 셋 — 이번에도 기계 검사에 안 걸리는 종류다

① **"23:52~23:57 아님?"** — 재계산 시각을 그마 기준으로 뭉뚱그렸는데, **챌린저는 5분 뒤에
  움직였다.** 두 경계가 따로 처리된 것을 놓쳤다 (아래 절).
② **"내 사이트 오류 아님?"** — 갱신 직후에도 1명이 어긋난 것을 두고 물었다. 코드·데이터
  양쪽으로 확인하니 오류가 아니었지만, **확인해 보기 전에는 알 수 없는 종류의 의심이었다.**
③ **"숙련도 줄인 만큼 다른 칸이 늘어난 거 아님?"** — 정확히 그거였다. 표에 남는 폭을 받는
  칸이 정해져 있지 않으면 한 칸을 줄일 때 나머지가 비례로 늘어난다.

### 재계산 시각 실측 — **맨 위 "컷라인 개편 · 티어 재계산 1분 실측" 절이 결론이다**

2026-08-17 에 5분 해상도로 재서 "챌린저와 그마가 5분 차이로 처리된다" 고 봤는데,
**08-19 에 1분 해상도로 다시 재니 챌린저↔그마 23:55 · 그마↔마스터 23:56 이었다.**
5분 갭은 **격자가 5분이라 그렇게 보였던 것**이다. 그때의 5분짜리 표는 덜어냈다.

### ★ "챌린저가 왜 300명이 아니냐" 는 질문의 답 (2026-08-18 확인)

**챌린저 인원은 정확히 300명이 맞다.** 라이엇에 직접 물어서 확인했다
(챌 300 / 그마 700 / 마스터 10000). `c300` 이 295 인 것은 인원이 모자라서가 아니라
**LP 로 줄 세우면 상위 300 안에 그마가 5명 끼어 있어서**다.

```
299등  챌  1823
300등  그마 1823   ← 끼어든 그마          300등 컷 = 1823
301등  챌  1822   ← 밀려난 챌린저
```

- **★ 겹치는 구간이 넓다** — 1739~1865 LP 안에 챌린저 41명과 그마 65명이 뒤섞여 있다.
  그런데도 어긋남이 5명뿐인 건 300등 컷이 그 겹침 구간의 **위쪽**에 있어서다
- **★ 닉네임 미해결자를 빼는 로직은 없다** — `/api/ranking` 은 `.map()` 이라 한 명도 안 뺀다
  (못 받아온 사람은 `User-xxxx` 로 **채워서** 보낸다). 게다가 `apexdrifts` 는 그 경로를
  아예 안 타고 `challengerList` 원본에서 센다. **닉네임 해결 여부는 이 값에 영향을 줄 수 없다**

**★ 그 전 세션(2026-08-17, `338b455`):** 신화상점을 통째로 만들었고, 명단을 날짜별로
사진 찍기 시작했고(`ranksnapshots`), 티어 공식을 승률·밴률·픽률로 개편했다.
그 세션의 지적 셋(순회가 통째로 새던 것 · 스킨 가로 판본 · 넥서스 효과가 webm)도
전부 **게임을 아는 사람이 봐야 드러나는** 종류였다.

**바로 할 수 있는 것:**

- **★ 우선순위 높은 건 여전히 20번(패치 원본 박제) 하나다.** 16.17 전에 해야 하고
  안 하면 **옛 패치 통계가 조용히 틀어진다.** 지금도 패치가 16.16 하나뿐이라 급하진 않다
- **`MYTHIC_ITEM_END`(추천 상품 판매 기한)는 손표다.** 로테이션이 바뀌면 갈아야 한다
- **인게임 확인 대기 2건 → `인게임확인_20260816.md`** (루트, gitignore).
  ① 도감 `상위` 칸 이름 ② 아레나 밴 조 순서
- **수집기에 넘긴 부탁 2건** — 넥서스 효과 카탈로그 / 추천 상품 `ends_at`.
  `C:UsersadminDesktopmythic-collectorSERVER_STATUS.md` 에 적어 뒀다
- **아레나 상세 표 폭은 화면 확인을 못 했다** — 지금 `matchcaches` 에 아레나 경기가 없다.
  협곡 표와 구조가 같아 동작은 같을 텐데, 아레나 전적 있는 사람으로 한 번 볼 것

## 주소 · 뒤로가기 규칙 (2026-08-17 정비)

**★★ 규칙은 한 줄이다 — "메뉴로 들어오는 첫 걸음만 이력을 쌓고, 그 안에서 움직이는 건 전부
덮어쓴다."**

| 무엇 | 방법 | 주소 |
|---|---|---|
| 메뉴 진입 (전적검색 → 챔피언/도감/신화상점/통계) | **`pushState`** | `/champions` `/codex` `/mythic` |
| 챔피언 바꾸기 | `replaceState` | `/champions/Shen` |
| 챔피언 탭 (스킬·스탯·스킨·배경) | `replaceState` | `/champions/Shen/stats` |
| 스킨 고르기 | `replaceState` | `/champions/Shen/skins/49` |
| 도감 탭 | `replaceState` | `/codex/rune` |
| 신화상점 구획 | `replaceState` | `/mythic/weekly` |

- **★ 왜 덮어쓰나**: 탭 네 개를 훑어본 사람이 페이지를 벗어나려고 **뒤로가기를 네 번**
  눌러야 하는 걸 막는다. **주소는 그대로 바뀌므로 복사해서 보내면 그 자리가 열리는 건 똑같다**
- **★★ 메뉴 진입까지 덮어쓰면 안 된다.** 그러면 `전적검색 → 도감` 하고 뒤로가기를 눌렀을 때
  **사이트를 통째로 벗어난다** (이전 항목을 지워 버리므로)
- **★ 기본 탭(스킬)은 주소에 안 붙인다** — `/champions/Shen` 이 예전 그대로다.
  옛 링크가 살아 있어야 하고, `/champions/Shen/skills` 로 들어와도 그 꼴로 정규화된다
- **★ 스킨은 배열 자리가 아니라 스킨 번호다** (`/skins/49`). 가렌은 11 다음이 **13** 이라
  자리를 담으면 목록이 바뀔 때 엉뚱한 스킨이 열린다
- **★★ 새 주소를 만들 땐 `app.js` 진입부 `pathParts` 분기와 `popstate` 두 곳을 같이 고친다.**
  한쪽만 고치면 "직접 주소를 치면 되는데 뒤로가기로 오면 안 되는" 상태가 된다
- 주소로 들어온 요청은 `window.pendingChampView = {tab, skin}` 에 담아 두고,
  챔피언 상세가 다 그려진 뒤 `applyPendingChampView()` 가 적용한다.
  **신화상점에서 넘어오는 `pendingSkin` 과 같은 함수가 처리한다** — 하는 일이 같아서다
- 탭 버튼은 **글자가 아니라 `data-tab`** 으로 찾는다 (`goChampTab`). 이름이 바뀌어도 안 깨진다
- **★ `og:` 미리보기 카드는 별개다.** 주소를 만든다고 카톡 미리보기가 생기지 않는다 —
  `sendIndexHtml` 이 그 주소일 때 메타 태그를 끼워 넣어야 하고, 그건 서버 작업이다
- 실측(2026-08-17): 홈 → 챔피언 진입 → 탭 3번 → 다른 챔피언까지 가도 **이력은 +1 뿐이고
  뒤로가기 한 번에 홈**이다. `/champions/Garen/skins/13` · `/champions/Ahri/lore` ·
  `/codex/rune` · `/mythic/weekly` 로 바로 들어가면 그 자리가 그대로 열린다.
  신화상점 카드 → `/champions/Shen/skins/49` → 뒤로가기 한 번에 `/mythic/biweekly` 로 복귀

## 랭킹 탭 (2026-08-13 정비)

`server.js` 의 `/api/ranking` + `app.js` 의 `showRanking`·`renderRankingPage` + `style.css` 16절.
챌린저 300 + 그마 700 + 마스터 1만을 합쳐 **LP 내림차순**으로만 정렬해 통째로 내려준다.

### ★★ 명단 갱신은 정각 격자에 붙는다 (2026-08-18)

예전엔 `끝난 시각 + 주기` 라 조회에 걸린 만큼 매번 밀려서 23분·28분·33분으로 떠내려갔다.
지금은 **한국시간 자정부터 주기 간격으로 놓인 격자**의 다음 칸을 겨냥한다(`nextRankDelay`).

- 주기가 바뀌는 경계도 저절로 맞는다 — 23:25 에 돌면 5분 격자로 23:30 을 잡고, 거기서
  깨면 그때 `rankRefreshMs` 가 1분을 주므로 23:31 · 23:32 로 이어진다
- 조회가 느려 격자를 넘겨도 `%` 계산이 다음 칸을 가리킨다. 남은 시간이 5초 미만이면 한 칸 건너뛴다
- **★ 배포 직후에는 어중간한 시각에 한 줄이 찍힌다.** `startJobs()` 가 기동 때 즉시
  `updateChallengerList()` 를 부르기 때문이다(빈 화면을 막으려고). 그 뒤부터 격자로 복귀한다.
  같은 시각에 두 줄이면 신규·기존 인스턴스가 겹쳐 돈 것이다
- **★ 갱신 주기를 화면에 적을 땐 서버가 주는 `refreshMs` 를 쓴다** (`/api/ranking` 응답).
  시간대마다 10분/5분/1분이라 화면에 규칙을 복제하면 두 벌이 되어 어긋난다.
  화면 문구는 `N분마다 갱신` 하나고, 마우스를 올리면 `최근 갱신 시각: …` 툴팁이 뜬다
  (**`<p>` 가 아니라 안쪽 `<span>` 에 건다** — `<p>` 는 폭이 헤더 전체라 빈 곳에서도 뜬다.
   그리고 이 툴팁만 글자 **아래**로 띄운다. 공용 `[data-tooltip]` 은 위에 떠서 제목을 덮는다)
- **★ `/ranking` 진입부에 `setActiveNav('nav-ranking')` 이 빠져 있었다** (2026-08-18 수정).
  `index.html` 에서 `nav-search` 가 기본 `active` 라, 주소로 직접 들어오거나 새로고침하면
  메뉴 불이 전적검색에 남아 있었다. **새 주소를 만들 때 이 줄을 빠뜨리기 쉽다**

- **★ 티어는 서버가 실명으로 내려준다. 순위 번호로 짐작하지 말 것.**
  예전엔 화면이 `순위 <= 300 이면 챌린저` 식으로 색을 칠했는데 **그게 틀린다** —
  라이엇이 승격을 주기적으로만 처리해서 **마스터가 그마보다 LP 가 높은 순간**이 실제로 있다.
  `updateChallengerList()` 가 `entries.forEach(e => e.tier = tier)` 로 소속을 찍어 두고,
  응답에 `C`/`G`/`M` 한 글자로 실어 보낸다 (1만 명 x 매 항목이라 글자 수가 곧 응답 크기다).
  화면 쪽 표는 `RANK_TIER_INFO` — 이름·짧은 이름·색·메달 파일명이 여기 한 군데 있다
- **★ 표 글자색은 흰색으로 통일했다 (2026-08-15).** 예전엔 LP 가 티어 색을, 승률이
  `55% 이상이면 빨강 아니면 파랑`을, 티어 이름이 티어 색을 썼다. 셋 다 `#fff` 다.
  **그래서 표에 인라인 `style=` 이 하나도 안 남았다** — 색이 마지막 계산값이었다.
  티어 구분은 왼쪽 메달 그림이 하고, 되돌리려면 `RANK_TIER_INFO.color`(값은 남겨 뒀다)를
  다시 실으면 된다. **CSS 는 `.rank-lp` 가 아니라 `td.rank-lp` 로 쓸 것** —
  클래스만 쓰면 `<th>` 까지 걸려서 헤더의 보라(`thead { color: #a78bfa }`)가 죽는다.
  `(324W 205L)`(`.rank-wl`)과 닉네임은 회색이다 — **2026-08-18 색 통일로 `#555`/`#ddd` 가
  `var(--text-faint)`/`var(--text-bright)` 로 바뀌었다.** 옛 색 전체 대응표는 루트
  `색상정리_20260818.md`(gitignore) 참고
- **검색창 안내 문구는 `닉네임/태그 검색` 이다** (2026-08-15). 예전엔
  `닉네임 검색 (초성 · 영문 자판 그대로)` 였다. **기능은 그대로 살아 있고 안내만 뺀 것이다** —
  초성(`ㅎㅇㅇㅂ`)·영타(`glemdhsqnth`) 검색은 `buildRankQuery` 가 계속 처리한다.
  상자는 `.rank-search-box` 가 320px 고정이라 문구 길이가 폭에 영향을 안 준다
- **★ 원래 순위(`rank`)를 받아올 때 못 박는다.** 검색으로 걸러내면 배열 위치가 순위가
  아니게 되어, 화면에서 세면 걸러진 목록이 "1위" 부터 다시 시작한다
- **★ 닉네임을 `onclick` 문자열 안에 박지 말 것.** 아포스트로피가 든 닉네임(`D'Arcy`)이면
  리터럴이 그 자리에서 닫혀 JS 가 깨지고 **그 사람만 클릭이 통째로 안 먹었다.**
  지금은 `data-name` + tbody 이벤트 위임이다. 표를 매번 새로 그리므로 리스너도 매번 붙인다
- **닉네임 미해결 판별은 `#` 유무로 한다.** 아직 못 받아온 사람은 서버가
  `User-a1b2c3d4` 로 채워 보내는데, 라이엇 ID 에는 **항상 `#태그`가 붙으므로** 이걸로 갈린다.
  `User-` 접두사를 보는 것보다 안전하다 (닉네임에 `-` 가 들어갈 수 있다).
  이 사람들은 눌러도 검색이 헛돌아서 링크로 안 만든다
- **★ 검색어 해석은 한 번만 한다 (`buildRankQuery`).** 1.1만 명을 훑으므로 행마다
  자판 변환을 돌리면 타이핑이 눈에 띄게 밀린다. 후보 문자열을 미리 만들고 비교만 시킨다.
  같은 이유로 소문자·초성(`_lc`/`_cho`)은 **데이터를 받을 때** 한 번 만들어 붙여 둔다
  - 초성·영타 헬퍼는 `getChosung()`·`koCandidates()` 를 **그대로 재사용**한다.
    챔피언 검색과 표를 두 벌 두면 어긋난다
  - 영문 자판 해석은 `/^[A-Za-z]+$/` 일 때만 후보에 더한다. 원문 후보는 항상 들어 있어서
    영문 닉네임(`Faker`)도 같이 걸린다
- **검색 중에는 주소(`pushState`)를 안 건드린다.** 걸러낸 목록의 3페이지는
  `/ranking?page=3` 이 아니라서, 그 주소를 새로고침하면 엉뚱한 자리가 나온다
- **탭에 새로 들어오면 검색어를 푼다** (`showRanking` 첫머리). 안 풀면 메뉴로 다시
  들어왔을 때 걸러진 목록이 그대로 나와 고장처럼 보인다
- **헤더는 `renderRankingHeader()` 하나다.** 예전엔 같은 HTML 이 `showRanking` 과
  `popstate` 두 곳에 복붙돼 있어서 한쪽만 고치면 뒤로가기로 들어왔을 때 다르게 보였다
- **페이저 양 끝 버튼(`«` `‹` `›` `»`)은 못 눌러도 자리를 지킨다**(`disabled`).
  없앴다 만들었다 하면 가운데 정렬이라 페이지를 넘길 때마다 번호가 좌우로 튄다.
  번호는 현재 페이지를 가운데 둔 5칸이고 끝쪽에서도 5칸을 유지한다
- **폰에서는 티어 이름이 `챌`/`그마`/`마` 로 줄어든다**(`.rank-tier-short`).
  390px 실측(2026-08-14): 순위 36 + 티어 52 + LP 38 + 승률 43 = 169px 라 닉네임 몫이 199px.
  **가로 넘침 0 을 실측으로 확인했다**
- **칸 순서는 `순위 · 닉네임 · 티어 · LP · 승률 · 숙련도 TOP5` 다** (2026-08-14에 닉네임과
  티어를 맞바꾸고 숙련도 칸을 더했다). 빈 결과 줄의 `colspan` 도 같이 세야 한다(지금 6).
  - **★★ 칸 폭은 2026-08-18에 다시 정리했다. 지금은 여섯 칸이 전부 고정이다:**
    `순위 60 · 닉네임 285 · 티어 110 · LP 100 · 승률 155 · 숙련도 168` = **878px**,
    여기에 그래프 칸 300 + gap 20 을 더해 **통이 1200px** 다(`#result-container.is-ranking`).
    **★ 통을 이 합보다 넓게 두면 남는 폭이 여섯 칸에 비례로 퍼져** 승률·숙련도가 지정값보다
    넓어진다 — 승률이 173 지정인데 195px 이던 게 그 증상이었다. **칸을 고치면 합계도 같이 고칠 것**
  - **★ 남는 폭을 받는 칸을 하나 정해 두는 것이 핵심이다.** 2026-08-14 에는 숙련도 칸만
    폭이 없어 그쪽이 다 받았고(292px), 그걸 168 로 못 박자 이번엔 승률이 받았다.
    **한 칸을 줄이면 나머지가 비례로 늘어난다** — 사용자가 이걸 먼저 짚었다
  - **★ 닉네임은 칸을 넘기면 `...` 으로 자른다.** 1.1만 명 중 **124명(1.1%)** 이 해당하고,
    그 행들이 두 줄로 늘어나던 게 사라져 모든 행이 49px 로 균일해졌다.
    · **`td` 에 `overflow: hidden` 을 걸면 안 먹는다** (`display: table-cell` 이라 무시된다).
      안쪽 `<span>` 을 `block` 으로 만들어 거기에 건다
    · **`width` 만으로는 칸이 285 가 아니라 301px 이 된다** — span 이 `nowrap` 이라 칸의
      최소 내용 폭이 그 텍스트 폭으로 잡힌다. `max-width` 를 같이 걸어야 못 박힌다
    · **폰에서는 말줄임을 푼다** — 칸이 200px 남짓이라 자르면 대부분 못 읽는다
  - **★ 승률 칸에는 `nowrap` 을 건다.** 145px 로 줄였더니 `(1043W 1000L)` 같은 네 자리
    전적이 승률 아래로 **줄바꿈됐다**(1.1만 명 중 최장이 그 13자, 네 자리 판수가 67명).
    nowrap 이면 폭이 모자라도 두 줄로 갈라지는 대신 표가 칸을 넓히고 닉네임이 도로 내준다
  - **폰에서는 닉네임·승률·LP 폭을 `auto` 로 되돌린다**(`max-width: none` 도 같이).
    안 그러면 390px 를 넘긴다
  **표 CSS 에 `nth-child` 같은 자리 기반 선택자가 하나도 없어서**(전부 `.rank-name` 식 클래스)
  `<thead>` 와 `<tbody>` 의 순서만 바꾸면 된다. 순서를 또 바꿀 일이 있으면 이것만 확인할 것

### ★★ 상위 티어 재계산 시각 알아내기 — `apexdrifts` (2026-08-17 신설)

**라이엇은 챌린저/그마 소속을 연속으로 갱신하지 않고 하루 한 번쯤 몰아서 처리한다.**
그래서 갱신 직후에는 LP 순위와 소속이 정확히 맞고, 시간이 갈수록 아래 티어가 LP 로
치고 올라와 어긋난다. **그 어긋남을 세면 갱신 시각이 드러난다.**

```
c300   1~300등 안의 챌린저 수   (300 이면 안 어긋남)
c1000  1~1000등 안의 챌린저 수  (300 이면 안 어긋남)   ← 2026-08-18 추가
g1000  1~1000등 안의 그마 수    (700 이면 안 어긋남)
→ 어긋남이 0 으로 떨어지는 순간이 재계산 시각이다
```

- **★ `c300` 과 `c1000` 은 보는 게 다르다.** `c300` 은 "위쪽 경계가 얼마나 흐렸나",
  `c1000` 은 "챌린저가 얼마나 **멀리** 밀려났나" 다. 정상 상태에서 `c1000` 은 300 이고
  (챌린저 전원이 1000등 안), 300등 기준으로 5명이 어긋나 있어도 여기는 0 이다
- **★ 소급이 안 된다.** `c1000` 은 2026-08-18 이후 줄에만 있다

- **★ 라이엇 호출이 0 이다.** 이미 받고 있는 명단(10분마다)을 세기만 한다
- **★ 밤 22시~02시(KST)는 5분, `23:45~23:59` 는 1분, 나머지는 10분** (`rankRefreshMs`).
  **1분 창은 컷라인이 그 표본에서 뽑히므로 상시다 — 지우면 안 된다** (2026-08-19).
  판정 순서상 **1분 조건을 먼저 보고 빠져나가므로** 5분 규칙보다 우선한다.
  하루 사이클 180회 x 3 = **540회/일**, 순간 최대 3회/분.
  한 사이클이 **3회**(챌/그마/마스터)라 **하루 432 → 504회**로 72회 는다(+17%).
  순간 최대는 3회/5분 = **0.6회/분**이고, 하루 예산(50/분 x 1440 = 72,000회)의 **0.7%** 다
- **★ `setInterval` 이 아니라 끝난 뒤 다시 예약한다**(`scheduleRankUpdate`). 조회가 느릴 때
  겹쳐 도는 걸 막고, 시간대가 바뀌면 다음 예약부터 새 주기가 자연히 적용된다
- **★★ 명단이 덜 찼으면(`< RANK_SET_MIN`) 기록하지 않는다.** 마스터 조회만 실패해도
  1~1000등이 통째로 달라져 **"갑자기 어긋남이 사라진 것" 처럼 보이는데, 그게 바로
  우리가 찾는 신호라 더 위험하다**
- LP 경계도 같이 남긴다(`cMin`/`gMax`/`gMin`/`mMax`) — `gMax > cMin` 인 만큼이 흐른 양이다
- 한 줄 60B · TTL 7일이라 용량은 없다시피 하다
- 실측 첫 값(8/17 16:51): **어긋남 챌린저 4명 · 그마 17명** ·
  챌 최저 1775 / 그마 최고 1840(**65 높다**) / 마스터 최고 1479

### ★★ 컷라인 그래프 — `rankcutoffs` (2026-08-18 신설 · **2026-08-19 규칙 개편**)

**★★ 규칙이 통째로 바뀌었다. 자세한 건 맨 위 "컷라인 개편" 절을 볼 것.** 여기는 요약이다.

**`23:45~23:59` 의 1분 표본 15개 중 `c300+g1000` 이 최대인 하나**를 골라, 그 표본의
**챌린저 최저 LP(`lpChal`) / 그마 최저 LP(`lpGm`)** 를 하루 한 줄 남긴다.
랭킹 탭 오른쪽에 그래프 두 장으로 나간다.

- **★ `apexdrifts` 와 목적이 다르다.** 저쪽은 어긋남을 분 단위로 보려는 것이라 TTL 7일,
  이쪽은 날짜별 추이를 길게 보려는 것이라 **TTL 이 없다** (하루 한 줄 x 100B = 1년 36KB)
- **★★ 표본을 따로 안 들고 `apexdrifts` 를 되읽는다** (`pickCutoffSample`).
  그래서 **규칙이 바뀌면 소급해서 다시 뽑을 수 있다** (TTL 7일 안에서)
- **★ `RANK_SET_MIN` 가드는 `saveApexDrift` 가 이미 한다** — 명단이 덜 찬 사이클은
  apexdrift 줄 자체가 안 생기므로 표본 후보에 들어오지 않는다
- **★ 필드 이름이 `lpChal`/`lpGm` 인 이유**: 예전 `lp300`/`lp1000` 은 **등수 기준**이라
  뜻이 달랐다. **이름을 두고 뜻만 바꾸면 옛 점과 새 점이 한 그래프에 섞여도 못 알아챈다**
- `day` unique 인덱스는 `ensureStatIndexes()` 에 적어 뒀다 (스키마 선언만으론 안 만들어진다)
- 조회는 `GET /api/rank-cutoffs?days=90` (기본 90 · 최대 365 · 캐시 5분)

**화면 (`cutoffChartHtml`)**

- **★★ 카드 오른쪽 숫자는 그래프의 마지막 점이 아니라 "지금 명단의 컷" 이다.**
  제목이 "현재 챌린저 커트라인" 이니 지금 값이어야 맞고, 그래야 **기록이 하나도 없는
  첫날에도 숫자가 뜬다.** 이미 받아 둔 명단에서 세므로 요청이 0 이다
- **★★ 그 숫자와 그래프 점은 기준이 다르다 — 일부러 그렇다 (2026-08-19).**
  숫자는 **300등·1000등의 LP**(`lpAt`), 점은 **챌린저·그마 최저 LP** 다.
  **재계산 전 시간대에는 둘이 50~150 LP 벌어지는데 버그가 아니다** —
  숫자는 "지금 몇 점이면 300등 안인가" 라는 실시간 값이고 점은 그날의 티어 경계다.
  실측(2026-08-19 렌더): 챌린저 카드가 `현재 1,823` / 마지막 점 `1,823` 로 우연히 같고,
  그마 카드는 `현재 1,326` / 마지막 점 `1,311` 로 15 차이다
- **★ SVG 는 선만 그리고 점·글자는 HTML 절대배치다.** 폭을 카드에 맞추려고 viewBox 를
  늘리면(`preserveAspectRatio="none"`) 글자와 원이 가로로 찌그러진다 — 스탯 탭과 같은 함정
- **★ 점이 30개를 넘으면 선만 그린다.** 붙어서 알아볼 수 없다
- **★ 카드 시작 높이를 표와 맞추려고 `.rank-side` 에 `margin-top: 25px` 을 준다.**
  표 위에 "전체 11,000명" 줄이 그만큼 자리를 차지한다. 폰에서는 이 여백을 푼다
- **★ `.rank-main` 에 `min-width: 0` 이 없으면 안 된다** — 표가 `min-width: 600px` 이라
  flex 항목이 안 줄어들고 오른쪽 칸을 밀어내며 페이지가 가로로 넘친다
- **★ 소급은 안 된다.** `apexdrifts` 가 300등·1000등 LP 를 저장하지 않아 과거는 못 되살린다

## 챔피언 페이지 · 스킨/배경/대사 탭 → **`docs/챔피언탭.md`**

**문서를 통째로 옮겼다 (2026-08-21, CLAUDE.md 가 한도를 넘어서).** 들어 있는 것 —

- **스킨 탭** — CD `champions/<키>.json` 이 원본이다(스킨 2111개, 경로 누락 0).
  **CD 경로는 전부 소문자로 바꿔야 한다** · 스킨 번호는 연속이 아니다(가렌 11 다음이 13)
- **가격은 CommunityDragon 에 없다 — 롤위키 `Module:SkinData/data` 에서 받는다**
  (`build_skin_prices.js` → `public/skin_prices.js`). **등급으로 가격을 추측하면 안 된다**
- **크로마** — 같은 json 안에 있어 추가 요청 0. 색점은 이미지 없이 그린다. 그림은 3D 렌더라 `contain`
- **★★ 오른쪽 일러스트는 폭이 아니라 높이로 정한다** (`min(378px, 75vh - 300px)`).
  창 높이를 여러 개 재라 — 한 점(900px)에서만 재고 결론지었다가 뒤집혔다
- **배경 탭** — 라이엇 Universe 전문. 챔피언별로 쪼갠 이유(손익분기 140명)와 슬러그 예외(레나타 글라스크)
- **대사 탭은 만들어 두고 가려 놨다** — 한국어 대본이 공개 데이터에 없다. 영문은 롤위키에 다 있다

## 경기 링크 — `/summoner/<라이엇 ID>/<경기 번호>` (2026-08-14 신설)

친구에게 특정 경기를 통째로 보낼 수 있는 주소. 경기 칸 오른쪽 위 🔗 를 누르면 복사된다.

- **★ 서버는 손댈 게 없다.** `server.js` 맨 끝 `app.get(/.*/, sendIndexHtml)` 가 모든 주소에
  index.html 을 주고, 라우팅은 `app.js` 진입부가 `location.pathname` 을 잘라서 한다.
  **새 주소를 만들 땐 `app.js` 의 `pathParts` 분기와 `popstate` 두 곳을 같이 고칠 것** —
  한쪽만 고치면 뒤로가기로 들어왔을 때 다르게 동작한다 (랭킹 헤더에서 겪은 것과 같다)
- **★ `executeSearch` 의 `pushState` 가 경기 번호를 지워 버린다.** 검색이 끝나면
  `/summoner/<이름>` 을 밀어 넣는데, 링크로 들어온 경우 **펼치기도 전에** 주소가 바뀌어
  새로고침하면 링크가 죽는다. `window.pendingMatchId` 가 있으면 주소에 같이 붙인다
- **검색은 최근 20판만 받아온다.** 그보다 오래된 경기는 `openPendingMatch()` 가
  "더 보기"를 **최대 3번**(총 50판) 눌러 가며 찾고, 못 찾으면 안내를 띄운다.
  더 늘리면 라이엇 호출만 늘어난다
- **★ 복사 버튼은 `.toggle-btn` 위에 겹쳐 있다.** 토글은 오른쪽 35px 을 **세로로 다 쓰는데**
  화살표는 맨 아래에만 있어서(`align-items: flex-end`) 위쪽이 비어 있다. 그 자리를 쓴다.
  그래서 두 가지가 필수다 — **`z-index: 2`**(없으면 클릭이 토글에 먹힌다)와
  **`stopPropagation`**(없으면 주소만 복사하려다 상세가 같이 열린다). 둘 다 실측 확인했다
- **주소에 넣는 이름은 라이엇이 준 정본(`currentProfileName`, `이름#태그`)이다.**
  검색창에 친 글자를 쓰면 대소문자가 제각각인 주소가 돌아다닌다
- **★★ `navigator.clipboard` 는 문서에 포커스가 없으면 실패한다 — 헤드리스에서는 항상 실패다.**
  `Emulation.setFocusEmulationEnabled` 를 안 켜고 재고는 **"복사 기능이 고장났다" 고 오진했다.**
  켜니까 바로 성공했다. `execCommand` 폴백도 포커스가 필요해서 같이 실패하니
  **폴백까지 실패한다고 코드 탓으로 몰지 말 것**
- 링크 미리보기 카드(`og:title`)는 아직 없다. 하려면 `sendIndexHtml` 이 그 주소일 때만
  DB 를 한 번 더 읽어 메타 태그를 끼워야 한다

## 상세 전적 표 폭 (2026-08-18 정비)

**표가 상세 상자보다 6px 넓어서 간단 전적 카드·탭 줄과 오른쪽 끝이 안 맞았다.**
`<colgroup>` 의 열 폭이 **열 칸 모두 고정 px** 이고 그 합이 735px 인데 상자는 729px 이었다.
`table-layout: fixed` 라 지정 합계가 그대로 표 폭이 되어 그만큼 삐져나온다.

- **★ 첫 칸(소환사)만 폭을 안 정한다.** 나머지 아홉 칸 합 585px 은 그대로 두고 남거나
  모자란 폭을 첫 칸이 흡수한다. **협곡·칼바람 표와 아레나 표 둘 다 같은 구조다**
  (`app.js` 에 `<colgroup>` 이 두 군데 있다 — 같이 고칠 것)
- **★★ `min-width` 를 예전 폭 합(735px)으로 두면 안 된다.** 상자가 729px 라
  **min-width 가 `width: 100%` 를 이겨서 늘 6px 넘치는 예전 상태 그대로가 된다.**
  실제로 한 번 그렇게 났다. `585 + 첫 칸 하한 120 = 705px` 로 잡는다
- **★ 폰 블록에 있던 `min-width: 650px` 은 지웠다.** 두 곳에 두면 어긋나고,
  애초에 고정 합이 735 라 그 값은 한 번도 작동한 적이 없었다
- 실측: 창 1400/1200 에서 카드·탭줄·상자·표가 모두 729px 이고 오른쪽 끝이 같다.
  좁은 창(1000/900)은 표가 705px 로 예전 735px 보다 30px 덜 넘친다.
  폰 390px 은 예전처럼 가로 스크롤로 처리되고 넘침 0
- **★ 아레나 표는 화면 확인을 못 했다** — `matchcaches` TTL 이 3일이라 지금 캐시에
  아레나 경기가 없다. 구조가 같아 동작은 같을 텐데 한 번 볼 것

## 전적 요약 통계 박스 (2026-08-14 정비)

`renderSummaryStats` 가 모드별로 패널 셋 중 하나를 그린다 —
협곡(`renderSummaryStats` 본체) / 칼바람(`renderAramSummaryHtml`) / 아레나(`renderArenaSummaryHtml`).
**셋이 같은 골격을 복붙해 쓰므로 한쪽만 고치면 필터를 누를 때 박스가 흔들린다.**

- **★ 오른쪽 칸 폭은 세 패널 모두 176px 이어야 한다.** 칼바람만 181px 이던 때
  구분선이 협곡·아레나보다 **1px·4px 왼쪽**에 찍혔다 (가로 238/508 → 237/504, 실측).
  박스가 `justify-content: space-between` 이라 **칸이 5px 넓어지면 남는 폭을 다시 나눠서
  구분선 네 자리가 전부 밀린다.** 구분선 자체(1x90)는 셋이 원래 같았다 —
  "구분선이 다르다" 는 증상의 원인이 구분선이 아니었다
- **★ 가운데 칸 폭은 258px 이다** (2026-08-14에 244 → 258). 승패가 두 자리(`15승 / 15패`)면
  안쪽 span 62px 을 넘겨 **줄이 넘어갔다.** span 을 76px(숫자 두 자리분)로 넓히고 칸도
  좌우로 한 자리씩 늘렸다. 실측으로 `15승 / 15패` 까지 한 줄이고,
  `100승 / 100패`(세 자리)는 8px 넘치지만 `white-space: nowrap` 이라 줄은 안 넘어간다
- **★ 가운데 칸의 줄 높이(28px + margin 6)를 바꾸면 안 된다.** 가운데 칸(129px)이
  박스 높이(181px)를 정하는 칸이라, 줄 높이가 달라지면 **모드마다 박스 높이가 달라져
  필터를 누를 때 아래 목록이 위아래로 튄다.** 항목이 3개가 안 되면 빈 줄로 채우는 이유다
- **칼바람·아레나는 "플레이한 챔피언" 대신 "플레이한 역할군" 이다** (`renderRoleRowsHtml`).
  두 모드는 라인이 없어 챔피언을 나열해도 성향이 안 보인다
  - **★ 역할군은 `fetchChampionMap()` 이 채운다 — 추가 요청이 0이다.** 전적 페이지가
    이미 `champion.json` 을 받고 있었다(`championIdMap`·`korChampMap` 용). 거기서 `tags` 를
    같이 담는다. **`showChampions()` 와 같은 표(`champRoleMap`)를 쓰고 `isClassicChamp`
    가드도 똑같이 걸어야 한다** — 클래식 챔피언이 섞이면 스탯 탭 역할군 평균의 모집단이 흔들린다
  - **챔피언 하나가 역할군 2개에 들어가므로 역할군별 게임 수의 합 > 총 게임 수다.** 정상이다
  - **★ 아레나만 문법이 다르다 — `N게임 우승 N회`.** 아레나는 승패가 아니라 등수이고
    (경기 칸도 `placementText` 로 등수를 찍는다), 같은 패널 왼쪽에 "평균 등수" 가 있는데
    옆에서 승률을 말하면 서로 어긋난다. 칼바람은 `(N승 N패 N%)` 다

## 아레나 밴 (2026-08-14 조사)

**★★ 아레나는 밴이 있다. 코드 주석이 "칼바람·아레나는 밴이 없다" 고 적고 있었는데 틀렸다.**
MongoDB `matchcaches` 의 아레나 153판 전수로 확인했다.

| | 값 |
|---|---|
| 참가자 수 | 18명 149판(6조 x 3명) / 16명 4판(구버전 8조 x 2명) |
| 밴 개수 | **참가자 수와 정확히 일치 — 1인 1밴** |
| 실린 곳 | 153판 전부 `teams[0]`(teamId 100) 한 곳. `teams[1]` 은 **`teamId: 0`** 에 빈 배열 |
| `pickTurn` | 153판 전부 1..N 연속 |
| `-1`(시간 초과) | 2746개 중 497개(18%) |

- **★ 그래서 `renderTeamSummaryRow` 가 아레나 밴을 통째로 버리고 있었다.** 이 함수는
  `teamId === 200` 을 찾아 없으면 `return ''` 하는데 아레나엔 **100 과 0** 뿐이다.
  게다가 아레나 상세는 `renderArenaDetail()` 이라는 별도 경로라 그 함수를 부르지도 않는다.
  지금은 아레나 상세 표 맨 아래에 `.arena-ban-summary` 줄로 18개를 한 줄에 깐다
- **★ 아레나의 `teamId` 는 진영이 아니라 "상위 절반 / 하위 절반" 이다** (153/153 확인).
  1~3등 조가 100, 4~6등 조가 200 이다. 그래서 **밴 출처를 가르는 데 못 쓴다**

**★★ 밴이 누구 것인지 라이엇은 안 알려준다. 그런데 순서에 답이 있다 (통계로 확인).**
`bans` 항목엔 `championId` 와 `pickTurn` 뿐이고 puuid 도 조 번호도 없다.

- **① `pickTurn` 은 "밴 넣은 순서" 가 아니라 고정 자리다.** `-1`(시간 초과) 비율을
  자리별로 세니 1~18번이 **13~26% 로 고르다.** 넣은 순서 기록이었으면 시간 초과가
  뒷번호에 몰려야 한다
- **② 그 자리는 조 번호 순으로 3개씩 묶여 있다** — `pickTurn 1~3 = 조1, 4~6 = 조2, …
  16~18 = 조6`. 검증법: **사람은 밴 습관이 있다.** 같은 puuid 가 여러 판에 나올 때
  가설대로 배정한 밴이 판마다 반복되는지를 센다. **두 판에 겹치는 사람이 본인뿐인
  매치쌍만** 남겨 같은 파티가 연달아 한 경우를 걷어냈다

  | 배정 가설 | 같은 사람의 밴이 반복될 확률 |
  |---|---|
  | 무작위 (대조군) | 1.5 ~ 2.4% |
  | 참가자 배열 순서 그대로 | 2.9% — **대조군과 구분 안 됨, 틀렸다** |
  | **조 번호 순** | **9.2%** (z ≈ 10) |

  조 단위(3개 묶음) 겹침은 **40.5% vs 무작위 14.1%**. 조↔블록 대응을 한 칸씩 밀어 보면
  `r=0` 40.5% / 나머지 다섯은 12.6~19.7% 라 **어긋난 배치는 전부 떨어진다**
- **블록 안 3자리 중 누구인지는 약하다** — 참가자 배열 등장 순 9.2% / 뒤집기 6.6% /
  한 칸 회전 4.1%. **조 단위는 확실하고 개인 단위는 유력** 정도다
- **★ 아직 인게임 확인을 안 했다.** 아레나 한 판에서 내 조 3명의 밴만 적어 두면 끝난다 —
  그 3개가 `pickTurn (조번호-1)*3+1 ~ +3` 에 들어 있는지 보면 된다.
  조가 6개라 우연히 맞을 확률이 낮아 **한 판으로 갈린다**
- 서버는 `bans` 를 **`pickTurn` 순서로 못 박아** 내려보낸다 (`buildHistoryEntry`).
  이 순서가 곧 조 순서라서, 확인이 끝나면 3개씩 잘라 쓰기만 하면 된다

**★ 아레나 밴 관련 사실을 "라이엇 데이터끼리 대조" 로는 못 얻는다.** `teams[0]` 에
전부 몰려 있다는 것도, `teamId 0` 이라는 것도 스키마상 이상하지 않아서 기계 검사에
안 걸린다. **실제 데이터를 세어 봐야 드러난다** — 위 "라이엇 데이터끼리만 대조하면"
항목과 같은 종류의 함정이다.

## 자체 통계 — 챔피언 티어리스트 → **`docs/통계집계.md`**

마스터+ 솔랭 전수로 승률·픽률·밴률을 내는 파이프라인. **문서를 통째로 옮겼다 (2026-08-19,
CLAUDE.md 가 한도를 넘어서). 통계를 건드릴 거면 그 파일을 먼저 읽을 것.** 들어 있는 것 —

- 순회/수집/집계 3단 잡 (`scanMatchlists` · `fetchMatchStats` · `buildChampStats`)과 호출 예산
- **라인 상성/카운터** (`champmatchups`, 2026-08-21 신설) — 같은 라인에서 마주친 두 챔피언의 승률.
  **수집 추가 0** 이고 지난 원본이 소급된다. 실측으로 **라인 짝이 온전한 판이 100.0%** 라 짝 못 짓는 판은 버린다
- 컬렉션 6개 (`matchseens` `ranksnapshots` `matchstats` `champstats` `statscopes` `champbuilds`)
- 슬림 문서 참가자 **28칸 자리표** — 순서를 바꾸면 이미 저장된 문서가 통째로 어긋난다
- **티어 공식(`TIER_COEF` · `TIER_PRIOR` · `TIER_CUTS`, 2026-08-28 부터 OP·1~5)** · 룬 빌드(라인별) · 화면(통계 탭)
- **★★ 패치 원본 박제** — 안 하면 옛 패치 통계가 조용히 틀어진다. **16.17 전에 할 것**
- k 밴드 · 밴 세는 방법 두 가지(`bans` / `banGames`) · 명단 날짜별 스냅샷(`ranksnapshots`)

자주 쓰는 것만 여기 남긴다:

```
READONLY_JOBS=1 node server.js   # 화면만 볼 때 (순회·수집·집계 잡을 안 띄운다)
REBUILD_STATS=1 node server.js   # 집계만 한 번. ★ 반드시 배포 뒤에 돌릴 것
```

## 도감 탭 — 아이템 · 룬 · 소환사 주문 (2026-08-16 신설·공개)

**2026-08-27 부터 도감은 헤더 하위 메뉴 넷이다** (`챔피언 · 아이템 · 룬 · 소환사 주문`) — 위 "헤더 메뉴 개편" 절.
이 절은 그중 아이템·룬·주문 세 화면 얘기다. 왼쪽 목록 + 오른쪽 상세이고
골격은 스킨 탭과 같다. `app.js` 의 `showCodex()` / `style.css` 의 `.codex-*` /
데이터는 `build_codex_data.js` 가 만든 `public/codex_data.js` 하나다 (런타임 의존 0).

**아이템 211 · 룬 62 · 스탯 파편 7 · 소환사 주문 9.** 162KB(gzip 33KB)라 도감 탭에
들어갈 때 처음 받는다 (`loadCodexData`).

### ★★ 세 소스 모두 "지금 협곡에 없는 것" 이 섞여 있다 — 거르는 규칙이 각각 다르다

| | 출처 | 전체 → 협곡 | 거르는 법 |
|---|---|---|---|
| 아이템 | DD `item.json` | 868 → **211** | `maps['11']` + 구매가능 + **사본 제외** + **칼바람 수호자 제외**(아래) |
| 룬 | CD `perks.json` | 103 → **62 + 파편 7** | `perkstyles.json` 슬롯에 있는 것만 |
| 주문 | DD `summoner.json` | 34 → **9** | `modes` 에 `CLASSIC` |

- **★ 룬 34개는 삭제된 옛 룬이다.** `perks.json` 이 그걸 그대로 들고 있다.
  지금 쓰이는 목록은 `perkstyles.json` 의 슬롯이 정본이다 (계열별 12/12/13/13/12)
- **★ 주문 25개는 칼바람 표식·아레나·우르프용이다**
- **★ CD `items.json` 의 `inStore` 를 아이템 필터로 쓰면 안 된다.** 696개가 나오는데
  흑요석 검(아레나)·수호자의 부적(칼바람)·"퀘스트: 상단" 같은 **다른 모드 것이 442개** 섞인다.
  DD 필터 결과가 CD `inStore` 의 정확한 부분집합인 것까지 확인했다

**★★ `maps['11']` 만으로는 부족하다 — 다른 모드용 **사본** 39개가 딸려온다.**
`322065 슈렐리아의 군가`(2600G)가 협곡 원본 `2065`(2200G)와 **이름이 같은 채로** 같이 나와서
도감에 한 아이템이 두 번 찍혔다. 이름이 겹치는 게 **27종**이었다.

- **★ 가르는 규칙: 사본은 `maps` 에서 `11` 만 true 이고 나머지가 전부 false 다.**
  진짜 협곡 아이템은 `12`·`21`·`35`·`453` 도 함께 true 다. **39개가 정확히 걸리고
  정상 아이템은 하나도 안 걸린다** (대표 20종 전수 확인)
- **★ "CD 아이콘 파일명 앞 숫자 ≠ 자기 id 면 사본" 규칙은 쓰면 안 된다.**
  그럴듯한데 **정상 아이템 7개를 같이 잡는다** — 폭풍갈퀴(3097)·수호자의 망치(3184)·
  추적자의 팔목 보호대(2420)·지배자의 피갑옷(2501) 등은 원래 남의 번호 아이콘을 쓴다
- **★ 짝이 없는 13개(아트마의 심판·가고일 돌갑옷·도박꾼의 칼날 등)도 지우는 게 맞다.**
  원본 id 를 되짚으면 `3193 가고일 돌갑옷` 은 `maps11=false, purchasable=false` 고
  `7101 도박꾼의 칼날` 은 DD 에 아예 없다 — **협곡에서 빠졌고 아레나에만 있는 것들**이다
- 거르고 나면 이름 중복이 **1종만** 남는다 — `칼리스타의 칠흑의 창`(칼리스타용 3599 /
  실라스용 3600). 둘 다 진짜라서 `requiredChampion` 을 뱃지로 찍어 구분한다
  - **★★ 그 값은 영문 키다 — 담을 때 한글로 바꾼다** (`build_codex_data.js` 의 `korRc`, 2026-08-25).
    안 바꾸면 화면에 **`Kalista 전용`** 이 그대로 나갔다. 지금은 `칼리스타·사일러스·피들스틱` 3자리다
  - **★★ `champion.json` 과 대소문자가 어긋나는 자리가 있다** — 아이템 쪽은 **`FiddleSticks`** 인데
    champion.json 은 **`Fiddlesticks`** 다. **소문자로 맞춰 찾아야** 한다. 못 찾으면 영문을 그대로
    남기고 경고를 찍는다 — **빈칸이 되면 칼리스타 창 두 개를 가를 길이 사라진다**

### ★★ 툴팁 색표는 한 벌이다 — `TOOLTIP_STYLE_CSS` (2026-08-16 분리)

아이템·룬 설명이 `<passive>`·`<magicDamage>` 같은 마크업인데 **챔피언 스킬 툴팁과 같은
태그 계열**이다. 도감에 나오는 **36종 중 31종이 이미 실측 색을 갖고 있었다** (2026-08-09에
인게임 스크린샷에서 픽셀을 뽑아 둔 그 표). 그래서 복붙하지 않고 상수로 뺐다.

- `app.js` 의 `skillsHtml` 안에 있던 `<style>` 블록을 `TOOLTIP_STYLE_CSS` 로 옮기고
  챔피언 페이지는 그걸 참조한다. **출력이 바이트 단위로 같은 것을 확인했다** (6398자 일치)
- **★ `style.css` 로 옮기면 안 된다.** `li`·`b`·`i`·`font` 같은 **진짜 HTML 태그**에도
  규칙을 걸고 있어서, 전역이 되면 **약관·개인정보 페이지의 `<li>` 까지 줄표가 붙는다**
  (index.html 5곳 · app.js 4곳에서 `<li>` 를 쓴다). 그래서 쓰는 쪽에 끼워 넣는다
- 새로 정한 5종은 **실측이 아니라 뜻에 맞춰 고른 값**이다 (`raritylegendary` ·
  `raritygeneric` · `scalelethality` · `statgood` · `lol-uikit-tooltipped-keyword`).
  인게임에서 확인하면 그 자리만 고치면 된다

### 거르는 버튼 (2026-08-16 · **2026-08-25 개편**)

**★★ 아이템 거르기를 라이엇 인게임 상점과 같은 축으로 갈아엎었다 (2026-08-25).**
예전 "분류" 7칸(공격·주문·방어·신발·소모품·장신구·기타)은 **묶는 기준도 칸 이름도 우리가
지어낸 것**이라 라이엇에 대응하는 이름이 아예 없었다. 지금은 셋 다 AND 로 걸린다:

| 줄 | 자리 | 버튼 | 실측 개수 |
|---|---|---|---|
| 아이템 · 등급 | 위 | 전체 / 기본 / 시작 / 서사급 / 전설급 / 상위 | 211 / **20 / 19 / 50 / 112 / 10** (합이 정확히 211) |
| 아이템 · **유형**(역할군) | 위 (옛 분류 자리) | 전체 / 전사 / 원거리 딜러 / 암살자 / 탱커 / 마법사 / 서포터 | 211 / 70 / 60 / 44 / 53 / 58 / 48 |
| 아이템 · **스탯** | **왼쪽 세로** | 전체 + **14칸** (공격력 · 치명타 · 공격 속도 · 적중 시 효과 · 방어구 관통력 · 주문력 · 마법 관통력 · 마나 및 재생 · 체력 및 재생 · 방어력 · 마법 저항력 · **스킬 가속 · 이동 속도 · 생명력 흡수 및 모든 피해 흡혈**) | 67 / 17 / 28 / 21 / 18 / 53 / 9 / 36 / 72 / 25 / 22 / 68 / 46 / 15 |
| 룬 · 계열 | 위 | 전체 / 정밀 / 지배 / 마법 / 영감 / 결의 / 파편 | 69 / 13 / 12 / 13 / 12 / 12 / 7 |

- **★★ 역할군은 게임 bin `mItemAttributes` 의 비트 플래그 6개다** — DD 에도 CD `items.json` 에도
  없고 등급(`epicness`)과 같은 자리에서 온다. **어느 비트가 무엇인지는 전설급만 걸러 보면 갈린다**
  (기본 재료는 여러 비트에 겹쳐 있어 안 갈린다):
  `1 전사`(삼위일체·스테락) · `2 원거리 딜러`(무한의 대검·루난) · `4 암살자`(요우무·밤의 끝자락) ·
  `8 탱커`(태양불꽃·란두인) · `16 마법사`(라바돈·존야) · `32 서포터`(구원·미카엘·헬리아)
- **★ 이름은 stringtable 그대로다** — 유형은 `shop_filter_*_tooltip`, 스탯은 `stats_filter_*`.
  **`stats_filter_on_hit = 적중 시 효과`** 처럼 인게임 왼쪽 목록과 글자까지 같다
- **★ 스탯 태그는 DD `tags` 인데 bin `mCategories` 와 전부 같다** (215개 전수 확인, 지금은 211개).
  bin 을 또 읽을 이유가 없어서 이미 담고 있던 `g2` 를 그대로 쓴다
- **★★ 순서와 칸 수는 인게임 화면을 보고 맞췄다 (2026-08-25, 사용자 확인).**
  `스킬 가속` 이 마법 저항력 **뒤**고, 마지막 셋이 `스킬 가속 · 이동 속도 · 생명력 흡수 및 모든 피해 흡혈` 이다.
  열넷이고 **`생명력 흡수 및 주문 흡혈` 칸은 없다** — stringtable 에 `stats_filter_spellvamp` 키가
  **있는데도** 협곡 상점 목록엔 안 쓴다. 그래서 `SpellVamp` 태그는 `생명력 흡수 및 모든 피해 흡혈`
  칸이 같이 받는다 (이름 그대로다).
  **★ 내가 처음에 stringtable 키 개수(15)만 보고 순서까지 지어냈던 자리다** — 목록의 순서·구성은
  **키 목록으로는 알 수 없고 인게임을 봐야 한다**
- **★★ 칼바람 나락 전용 "수호자" 아이템 4종이 딸려 왔다** (2026-08-25, 사용자 지적. 215 → 211).
  **`maps` 로는 못 거른다** — 수호자의 뿔피리도 `maps['11'] = true` 다. 가르는 건 bin 의
  **`mItemGroups` 에 있는 `Items/ItemGroups/GuardianItems`** 다 (뿔피리·보주·검·망치, 전부 950G ·
  상위 아이템 없음). **★ `MythicItems` 그룹은 빼면 안 된다** — 옛 이름일 뿐이고 `사라진 양피지`(3802)가
  거기 속하는데 대천사의 지팡이·루덴의 메아리·악의로 조합되는 **협곡 아이템**이다
- **★ 태그↔스탯칸 연결은 우리가 잇는다** — 열넷 중 아홉은 1:1 이고 다섯만 둘씩 묶는다
  (스킬 가속 `AbilityHaste+CooldownReduction` · 마나 · 체력 · 마법 저항력 `SpellBlock+MagicResist` ·
  이동 속도 `NonbootsMovement+Boots`). **라이엇이 그 대응표를 데이터로 주지는 않는다**
- **★ 남는 태그 열하나는 목록에 없다** (`Lane`·`Active`·`Vision`·`Consumable`·`Tenacity`·`Jungle`·
  `GoldPer`·`Slow`·`Aura`·`Trinket`·`Stealth`) — 스탯이 아니라 성격 태그다. **인게임 상점에도 없다**
- **★ 역할군이 없는 아이템이 13개다** (신발 업그레이드 7 + 탐욕의 군화·천상의 이의 등).
  유형은 '전체' 로만 찾히지만 **스탯(이동 속도)으로는 찾힌다**
- **★ 스탯 칸은 다시 누르면 풀린다** — 세로 목록이라 맨 위 '전체' 로 되돌아가기가 번거롭다.
  칸 옆 숫자는 **등급·유형이 걸린 뒤 기준**이라 필터를 바꾸면 같이 바뀌고, 0 이면 흐려진다
- **★ 실측 (2026-08-25)**: 데스크톱 1400 — `좌측 740`(스탯 168 + gap 12 + 목록 560) + gap 20 +
  `상세 640`, 목록 한 칸 260px, **이름 잘림 0 · 페이지 넘침 0 · 조합식 트리 넘침 0**.
  폰 390 — **스탯 목록을 가로 한 줄로 눕힌다**(자체 스크롤). 세로로 두면 목록이 190px 로
  쪼그라든다. 넘침 0 · 이름 잘림 0. **룬·주문 탭에서는 스탯 칸이 꺼져 목록이 740 을 다 쓴다**

- **★ 등급만 서로 안 겹친다.** 아이템 하나에 등급은 하나뿐이라 합이 211 로 딱 떨어진다.
  **유형·스탯은 겹치는 게 정상**이라 합계가 211 을 넘는다

**★★ 두 줄의 출처가 다르다 — 등급은 라이엇 것, 분류는 우리 것이다 (2026-08-25 정리).**

| | 값 | 이름 | 우리 몫 |
|---|---|---|---|
| **등급** | 게임 bin `epicness` (라이엇) | stringtable `shop_group_*` (라이엇) | **`epicness 7 → 상위` 연결 하나뿐** |
| **유형(역할군)** | 게임 bin `mItemAttributes` (라이엇) | stringtable `shop_filter_*_tooltip` (라이엇) | 없음 |
| **스탯** | DD `tags` (라이엇) | stringtable `stats_filter_*` (라이엇) | **태그↔칸 연결** (15 중 5개만 둘씩 묶는다) |

- **★★ 라이엇 인게임 상점은 아예 다른 체계로 나눈다 — 역할군 기준이다** (`shop_filter_*_tooltip`):
  `모든 아이템 / 전사 / 탱커 / 마법사 / 원거리 딜러 / 암살자 / 서포터`.
  **2026-08-25 에 이 축으로 갈아엎었다** — 옛 `공격/주문/방어/신발/소모품/장신구/기타` 는 우리가 지어낸 이름이었다
- **★ 스탯 이름 자체는 라이엇에 있다** (`shop_stat_attack_damage 공격력` · `shop_stat_spell_damage 주문력` ·
  `shop_stat_armor 방어력` …). 우리 칸 이름은 그걸 줄인 꼴에 가깝지만 **라이엇이 분류 이름으로 쓴 건 아니다**
- **★ 아이템→역할군 매핑은 DD 에 없다 — 게임 bin `mItemAttributes` 에 있다** (위 "거르는 버튼" 절)
- 폰 실측(2026-08-16): 390px 에서 등급 1줄 · 분류 2줄, 360px 에서 둘 다 2줄.
  **두 폭 모두 가로 넘침 0** 이다

**★★ 등급은 DD 의 `depth` 가 아니라 게임 bin 의 `epicness` 다 (2026-08-16 정정).**
예전엔 `depth` 를 등급이라 보고 **`기본 / 중급 / 완성` 이라는 우리가 지어낸 이름**을 썼는데
**이름도 기준도 틀렸다.** `depth` 는 "재료를 몇 겹 쌓았나" 라서 등급과 다르다 —
**무한의 대검(3500G, 전설급)이 `depth` 2 라 거인의 허리띠(900G, 서사급)와 같은 칸**에 들어갔고,
실제로 무한의 대검·라바돈의 죽음모자가 화면에 **"중급"** 으로 찍히고 있었다.

- **★ 라이엇 공식 한국어 이름은 stringtable 의 `shop_group_*` 에 있다** —
  `basic 기본` / `starter 시작` / `epic 서사급` / `legendary 전설급` /
  `mythic 신화급`(아레나) / `superior 상위` / `unique 고유`.
  인게임 상점 필터는 `shop_filter_tier_label 등급:` + 숫자 `1 2 3` 세 칸이다
- **★ 등급 필드는 `raw.communitydragon.org/latest/game/items.cdtb.bin.json`(15MB)의
  `Items/<id>.epicness` 다.** DD 에도 CD `v1/items.json` 에도 없다
  (CD 필드 19개를 전수 확인했다). **빌드 때만 받으므로 런타임 의존은 0 이다**
- 협곡 215개 실측 분포. **합이 정확히 215 라 빠지는 게 없다**:

  | `epicness` | 개수 | 정체 |
  |---|---|---|
  | 없음·0 | 20 | 기본 재료 — 장화·롱소드·B.F. 대검·천 갑옷 … |
  | 1 | 23 | 시작 아이템·소모품·장신구 |
  | 4 | 50 | 서사급 |
  | 5 | 112 | 전설급 |
  | 7 | 10 | **영약 3 + 신발 업그레이드 7**(건메탈 군화·불멸의 길 …) |

- **★ `epicness 7` 만 성격이 섞여 있어 공식 이름이 없다.** 영약(소모품)과 신발
  업그레이드가 한 값이다. 라이엇이 나눈 그대로 두기로 하고 **그 칸 이름만 우리가 붙인다** —
  `상위`(`shop_group_superior`) 로 뒀다. 처음엔 `기타` 였는데 **분류 줄 마지막 칸도 `기타` 라
  위아래로 같은 이름이 나란히 붙어서** 바꿨다.
  **★ 이 이름만 인게임 확인이 남았다 — `인게임확인_20260816.md` 에 뭘 보면 되는지 적어 뒀다.**
  상점에서 신발 업그레이드와 영약이 **각각 어느 묶음 제목 아래** 있는지만 보면 된다.
  둘이 다른 묶음이면 라이엇도 한 칸에 안 넣는다는 뜻이라 화면에서 갈라야 한다
- **★ 값을 합치거나 다시 매기지 않는다.** `build_codex_data.js` 가 `ep` 로 **그대로** 담고
  `app.js` 의 `CODEX_EPICNESS` 가 이름·정렬 순서만 붙인다
- **★ 등급 버튼 마지막 칸은 `ep === 7` 이 아니라 "앞 네 칸에 안 걸리는 전부" 로 잡는다.**
  라이엇이 새 값을 쓰기 시작해도 그 아이템이 목록에서 통째로 사라지지 않는다.
  빌드 스크립트도 처음 보는 `epicness` 가 오면 경고를 찍는다
- **★ CD 아이콘 파일명의 `_T1_`·`_T3_` 를 등급으로 쓰면 안 된다.** 옛 관례라 어긋난다 —
  무한의 대검이 `depth 2` 인데 아이콘은 `T3` 고, `T` 가 아예 없는 게 67개다
- **★★ 신발 판정은 `Boots` 태그만으로 부족하다 — `건메탈 군화`에 태그가 없다.**
  광전사의 군화로 만드는 신발 업그레이드인데 라이엇 데이터에 태그가 빠져 있어서
  **"공격"(AttackSpeed)에만 나오고 신발 분류에서 통째로 빠졌다.** 재료를 타고 올라가
  신발이 섞였는지 보면 잡힌다 (`isBoots`). **전수 확인: 이 하나만 추가되고 오탐 0**
- **★ 소모품과 장신구를 나눴다.** 장신구(투명 와드·렌즈·허수아비 4개)는 소모되는 물건이
  아니라 "소모품" 에 넣으면 말이 안 맞는다. 둘 다 **다른 분류보다 먼저** 걸러낸다 —
  체력 물약이 "방어"(HealthRegen)에, 와드가 "기타" 에 겹쳐 나오면 목록이 지저분해진다
- **★ 룬 계열 순서는 id 순이 곧 인게임 순서다** (정밀 8000 · 지배 8100 · 마법 8200 ·
  영감 8300 · 결의 8400). 스탯 파편은 계열이 없어서 맨 뒤에 따로 붙인다
- **★ 소환사 주문은 목록에 부제를 안 붙인다.** 9개뿐이라 고를 때 쿨타임이 필요 없고
  오른쪽 상세에 뱃지로 이미 크게 나온다. `sub` 가 빈 문자열이면 `<div>` 자체를 안 만든다

### ★★ 소환사 주문 시연 영상은 **없다** — 라이엇이 안 만든다 (2026-08-25 조사, 다시 찾지 말 것)

챔피언 스킬 영상처럼 붙일 수 있나 알아봤는데 **네 군데 다 없다.**

| 확인한 곳 | 결과 |
|---|---|
| 기준선 — 챔피언 영상 `lol.dyn.riotcdn.net/x/videos/champion-abilities/0086/ability_0086_Q1.webm` | **206** (잘 된다) |
| 같은 CDN 의 주문 경로 후보 4가지 (`summoner-spells/` · `summoner-spell-abilities/` · `champion-abilities/SummonerFlash.webm` …) | **전부 404** |
| CD `summoner-spells.json` | 필드가 `id·name·description·summonerLevel·cooldown·gameModes·iconPath` **7개뿐 — 영상 필드 없음** |
| CD `game/data/spells/` · `game/assets/spells/` | `icons2d`·텍스처뿐 |
| 라이엇 공식 **how-to-play** 페이지의 "소환사 주문" 섹션 | **정지 이미지(png)만.** 그 페이지 webm 3개는 "골드 획득" 같은 일반 소개용이라 주문과 무관 |

- **★ 챔피언 스킬 영상이 되는 건 라이엇이 슬롯마다 규칙적인 파일명으로 만들어 두기 때문**이다.
  주문은 그런 규칙 자체가 없어서 **직접 녹화·호스팅 말고는 방법이 없다** (9개를 손으로 관리하는 빚)
- **★ 대신 그 자리를 `modes` 로 채웠다 (2026-08-25).** 주문 상세는 **카드 높이 720px 에 글자 233자**로
  도감에서 제일 휑했다. 남은 채울 거리는 **챔피언별 채택률**
  (`matchstats` 에 소환사 주문을 **이미 담고 있다** — 룬·주문을 넣으며 한 건이 1.31→2.32KB 가 된 그것.
   수집 추가 0 이고 지난 원본이 소급된다)

### 소환사 주문 — "다른 모드에서 못 쓴다" 표기 (2026-08-25)

`build_codex_data.js` 의 `MAIN_MODES` 가 주요 모드 다섯을 보고 **못 쓰는 곳만** `no` 에 담는다.
지금 걸리는 건 **둘뿐**이고 나머지 일곱은 필드 자체가 없어 화면에 아무것도 안 뜬다.

```
강타      칼바람 나락에서는 쓸 수 없습니다
순간이동   칼바람 나락 · U.R.F. · 돌격! 넥서스에서는 쓸 수 없습니다
```

- **★ 추가 요청 0 이다** — **DD `modes` 와 CD `gameModes` 가 9/9 완전히 같은 것을 확인했고**
  DD `summoner.json` 은 이미 받고 있다. CD 를 또 받을 이유가 없었다
- **★ 모드 코드 → 한글은 우리가 이었지만 이름 자체는 라이엇 것**이다 (CD `queues.json` 의 큐 이름:
  3200번대 `칼바람 나락` · 1200 `돌격! 넥서스` · 480 `신속 대전` · 1020 `단일 챔피언` · 1700 `아레나`).
  **`queues.json` 에는 `gameMode` 필드가 없어서** 코드↔이름을 데이터로 직접 잇지는 못한다
- **★ 아레나(CHERRY)는 목록에서 뺐다** — 협곡 주문 9개가 **전부** 못 쓴다(별도 주문 체계라).
  9개 전부에 같은 딱지가 붙으면 정보가 아니라 잡음이다
- **★ 모드 21종 중 대부분이 테스트·튜토리얼이다** (`RUBY_TRIAL_1~3`·`WIPMODEWIP4`·`DOOMBOTSTEEMO`·
  `KIWI`…). 그래서 `modes` 를 그대로 뿌리면 안 되고 주요 모드만 걸러야 한다

**★★ 그 김에 순간이동 사거리 `25000` → `전역` 으로 고쳤다.** 20000 이상은 **"표시할 값 없음" 더미**인데
(`audit_skill_meta.js` 가 "협곡이 약 15000 유닛이라 그보다 크면 맵 전체" 로 경계를 잡는다)
**도감 주문은 그 검사를 안 타서** 화면에 25000 이 그대로 나가고 있었다.
DD `rangeBurn` 도 25000 · bin `castRange` 도 25000 이라 **기계 대조로는 불일치 0** 이 나오는 자리다
(`docs/스킬데이터.md` 의 그 함정). `app.js` 의 `spellRangeText` 한 줄이다.

### ★★ 소환사 주문의 **실제 수치** — `shared.cdtb.bin.json` 에 있다 (2026-08-26 신설)

주문 설명이 `몬스터 또는 미니언에게 고정 피해를 입힙니다` 처럼 수치가 없는 한 줄이었는데,
**게임 데이터에 다 있었다.** 챔피언 스킬과 **구조가 똑같다** — stringtable 에 `@변수@` 꼴 툴팁이 있고
게임 bin 에 그 값이 있다. **9/9 전부 채웠다.**

```
강타     대형 몬스터·미니언에게 600 의 고정 피해 / 소환수에게 40
탈진     3초 동안 40% 둔화 · 가하는 피해량 35% 감소
정화     3초 동안 75% 의 강인함
회복     체력을 80 ~ 318 회복 [각주]        방어막  100 ~ 460 흡수 [각주]
점화     5초에 걸쳐 70 ~ 475 [각주]         유체화  24 ~ 48% 이동 속도 [각주]
```

- **★ DD 로는 못 한다** — `summoner.json` 의 `tooltip` 은 `{{ smitebasedamage }}` 같은 **변수 자리만** 주고
  `datavalues` 가 **9개 전부 비어 있다.** 값은 `game/shared.cdtb.bin.json`(8.6MB)의
  `Shared/Spells/<이름>` 에, 문장은 stringtable 의 `generatedtip_summonerspell_<이름>_tooltip` 에 있다
- **★ 변수 출처가 세 군데다**: `DataValues` 배열 · `mSpell` 직접 필드(`Cooldown`·`AmmoRechargeTime`) ·
  **`mSpellCalculations` 의 계산식**(레벨에 따라 변하는 넷)
- **★★ 계산식 두 종류를 게임이 쓰는 그대로 푼다**:
  `ByCharLevelInterpolation`(시작→끝 선형, 회복 80→318) ·
  `ByCharLevelBreakpoints`(레벨1 값 + 레벨당 증가, 중간에 증가폭이 바뀜 — 점화는 6레벨부터 +20→+25)
- **★★ `mDisplayAsPercent` 를 봐야 한다** — 라이엇이 "이건 퍼센트로 보여라" 고 적어 둔 필드다.
  유체화가 그 자리인데 안 보면 화면에 **`0.24 ~ 0.48`** 이 나간다 (문장에 `%` 기호가 없다)
- **★ 이름 대소문자가 어긋난다** — 툴팁은 `@MovespeedMod@` 인데 bin 은 `MoveSpeedMod` 다.
  **소문자로 맞춰 찾는다** (아이템 `FiddleSticks` 와 같은 함정)
- **★ 레벨 스케일 넷은 각주+그래프다.** 룬 각주와 **같은 `withGraphNotes()`** 를 쓴다
  (같은 날 `withRuneGraphs` 에서 알맹이를 뺐다 — 데이터 모양이 `{a,t,c,v}` 로 같다)
- **★ `{{ ... }}` 는 통째로 지운다** — 다른 툴팁을 끼워 넣는 자리라(`Item_KeywordDefinition_Wounds`)
  문장으로는 뜻이 안 통한다
- **★★ 지우고 나면 그 앞의 `<br>` 이 남는다 — 끝의 줄바꿈을 잘라야 한다** (2026-08-26 지적).
  개수가 주문마다 달라서 **설명과 아래 구분선 사이 여백이 제각각**이었다 (점화 4개 · 유체화 2개 ·
  나머지 0개). 지금은 끝을 자르고 중간에 3개 이상 연속된 것도 둘로 줄인다 — 실측 9개 전부 14px
- **★ 못 채운 변수가 있으면 빌드가 `★★ 주문 변수를 못 채웠다` 로 경고한다.**
  그 자리는 화면에 `@이름@` 이 그대로 나가므로 반드시 볼 것 (지금은 0)
- **★ 새 필드는 `dt`(문장)·`g`(각주)** 이고 **DD 짧은 설명 `d` 는 남겼다** — 못 만들면 화면이 그쪽으로 물러난다
- **★★ 주문의 변형도 담는다 (`up`, 배열)** — **강력 순간이동**(10분 이후 · 이속 50% 4초)과
  **원시의 강타 3종**(정글 펫 새끼 화염발톱·이끼쿵쿵이·바람돌이). 이름이 셋 다 "원시의 강타" 라
  펫 이름으로 갈라 준다 (툴팁 첫 문장도 펫 이름으로 시작한다)
- **★★ `@spell.<객체>:<변수>@` 는 다른 주문의 값을 참조하는 문법이다** (2026-08-26 지원).
  강타 변형들이 자기 DataValues 를 비워 두고 `@spell.SummonerSmite:FirstPVPDamage@` 처럼
  본체 값을 그대로 가져다 쓴다
- **★ 옛 시즌 변형은 안 넣었다** — `S5_SummonerSmitePlayerGanker`(강력 강타)·`S5_SummonerSmiteDuel`
  (붉은 충전 강타). 지금 협곡의 강타 강화는 정글 펫 쪽이다
- **★★ 못 채운 변수가 있는 변형은 통째로 뺀다** (`spellTooltip` 의 `ok`) — 화면에 `@이름@` 이
  나가느니 칸이 없는 게 낫다

### ★★ 소환사 주문 채택률 (2026-08-26 신설) — 도감이 유일하게 API 를 부르는 자리

주문 상세가 도감에서 제일 휑해서(카드 720px 에 글자 233자) 그 자리를 통계로 채웠다.
**시연 영상은 라이엇이 안 만들어서 못 넣는다** (바로 위 절).

```
점멸 97.4% · 순간이동 21.4% · 점화 21.3% · 강타 20.0% · 회복 11.5%
방어막 10.7% · 탈진 8.8% · 유체화 6.4% · 정화 2.4%       (16.16 · 40만 명분)

강타     헤카림 100% · 릴리아 100% · 그레이브즈 99% · 리 신 98%
순간이동  직스 97% · 문도 박사 93% · 오리아나 92% · 아지르 90%
회복     나미 97% · 밀리오 97% · 유미 96% · 룰루 93%
```

- **★★ 수집·집계에 더한 게 없다.** `champbuilds` 의 `type:'spell'` 을 **주문 기준으로 뒤집기만** 한다 —
  통계 탭은 "이 챔피언이 뭘 드나" 인데 도감은 "이 주문을 누가 드나" 다. **지난 원본도 그대로 소급된다**
- **★ 분모는 경기 수가 아니라 "챔피언 픽 합계(명)"** 다. 한 사람이 주문을 2개 드니 **합이 200%** 다.
  **강타가 정확히 20.0%** 인 건 팀당 정글러 1명이라는 뜻이라 값이 맞다는 방증이다
- **★ 챔피언 top 은 "그 챔피언 판수 대비 채택률" 순이다** (판수 순이 아니다). 판수 순이면
  인기 챔피언만 나와 뻔해진다 — 비율 순이라 **그 주문을 반드시 드는 챔피언**이 드러난다.
  표본 30판 미만은 뺀다 (비율이 튄다)
- **★★ 도감의 "런타임 의존 0" 원칙에 낸 유일한 예외다.** 통계는 매시간 바뀌어 파일에 못 굽는다.
  **실패해도 화면은 그대로**고 그 칸만 안 나온다 (`spellUsageHtml` 이 빈 문자열을 낸다)
- **★ scope 고르는 규칙은 `/api/champion-stats` 와 공유한다** (`pickStatScope`, 같은 날 함수로 뺐다).
  "표본 300판을 넘긴 가장 최신 패치" 라 **패치 당일에 화면이 텅 비지 않는다**
- **★ 점멸은 목록이 전부 100% 라 정보가 적다** (97.4% 가 다 든다). 알고 두는 것
- **★ 화면 문구는 `채택률 TOP5` 이고 다섯 개다** (`SPELL_CHAMP_TOP = 5`, 2026-08-26).
  **문구와 개수가 짝이라 한쪽만 고치면 어긋난다** — server.js 상수와 app.js 문구 두 곳이다
- **★★ 라인별 채택률도 같이 낸다** (2026-08-26). `champbuilds` 의 `pos` 를 그대로 쓴 것이라
  **집계에 더한 게 또 없다.** 값이 라인 성격을 그대로 드러낸다:

  ```
  강타      탑 0% · 정글 100% · 미드 0% · 바텀 0% · 서포터 0%
  순간이동   탑 46% · 정글 0% · 미드 50% · 바텀 11% · 서포터 0%
  회복      탑 0% · 정글 0% · 미드 0% · 바텀 0% · 서포터 57%
  방어막     바텀 51%          점화  탑 36% · 미드 34% · 서포터 36%
  ```

  - **★★ 분모가 "그 라인의 픽 합계" 다.** 전체 픽으로 나누면 라인 하나가 5분의 1이라
    **"탑에서 순간이동 46%" 가 9% 로 찌그러진다.** `picksByPos` 가 그 분모다
    (실측: 라인마다 82,63x 명으로 거의 같다 — 라인당 1명씩이니 당연)
  - **★ 0% 줄도 그린다** — "강타는 정글만 100%, 나머지 0%" 라는 게 그 0 들로 드러난다.
    대신 숫자를 흐리게 해서(`is-zero`) 눈이 100% 쪽으로 가게 한다
  - **★ 채택률 숫자의 강조는 뺐다** (20px 700 보라 → 본문 크기). 강조는 라인 막대가 맡는다

**★★ 왼쪽 칸 폭이 탭마다 다르다 (2026-08-26).** 아이템 탭은 스탯 목록(168)까지 껴서 740 이
필요하지만, 룬·주문은 스탯 칸이 꺼져 목록이 740 을 통째로 쓰면서 **오른쪽 상세만 좁아졌다.**

```
아이템   좌측 740 (스탯 168 + 목록 560)  상세 640
룬       좌측 592 (-20%)                상세 788   룬 트리 줄 폭 559 → 들어간다
주문     좌측 444 (-40%)                상세 936   목록 한 칸 202px · 이름 잘림 0
```

**★★ 2026-08-27 정정 — 통은 1400 고정이고 상세가 남는 폭을 받는다.** 아래 두 항목(상세 고정폭 552/562 ·
통을 1164/1026 으로 줄이기)은 **지금 코드에 없다.** 탭이 헤더 메뉴가 되면서 통 폭이 탭마다 다르면
**탭 줄·검색창·거르는 버튼이 좌우로 최대 187px 씩 움직여서** 사용자가 불편하다고 짚었다.
왼쪽 칸 폭(740/592/444)만 그대로다.

- **★ 상세는 `flex: 1` 이라 폭을 주려면 flex 를 꺼야 한다** (`flex: 0 0 552px`).
  안 끄면 남는 폭을 다 먹어서 `width` 가 안 먹힌다
- **★★ 통(`#codex-container`)의 max-width 도 같이 줄여야 한다** — 상세만 좁히면 남는 폭이
  **오른쪽에 그대로 남아 화면이 왼쪽으로 치우친다.** 통을 칸 합계로 줄이면 `margin: 40px auto` 가
  살아나 **검색창·탭 줄까지 한꺼번에** 가운데로 온다 (룬 1164 / 주문 1026).
  실측: 룬 좌우 여백 118/118 · 주문 187/187

- **★ `data-tab` 은 `renderStatBar()` 가 몸통에 적는다** — 탭을 보고 statbar 를 켜고 끄는 그 자리라
  한 군데서 처리한다. CSS 는 `.codex-body[data-tab="rune"] .codex-left` 로 받는다
- **★★ 폰 블록에서 같은 특이도로 덮어야 한다.** `[data-tab]` 선택자가 `.codex-left` 하나보다
  특이도가 높아서, 폰 규칙을 `.codex-left { width: 100% }` 로만 두면 **폰에서도 592/444 가 이긴다.**
  세 선택자를 나란히 적어 뒀다 (실측: 폰 390 에서 세 탭 모두 370px · 넘침 0)

### 아이템 탭 전수 점검 — 남은 5건 (2026-08-25, 판단 대기)

215개를 데이터·화면 양쪽으로 훑고 7건을 찾아 **둘을 고쳤다** (초성 공백 · `Kalista 전용`).
나머지는 "고칠지 말지" 가 판단이라 남겼다:

| 무엇 | 자리 | 사정 |
|---|---|---|
| **영문 별명이 안 걸린다** | 125 | 위 검색 절 참고. `app.js` 한 줄인데 **사용자가 지금은 안 고치기로 했다** |
| `0 G` · `판매 0` 뱃지 | 9 | 라이엇 값 그대로다(장신구는 공짜, 정글 펫은 판매 불가 — 허수아비 설명엔 **"팔 수 없음"** 이라고 적혀 있다). 0 이면 뱃지를 숨기거나 `판매 불가` 로 |
| **세계 지도집** 설명이 통째로 없다 | 1 | **DD·CD 둘 다 `description` 이 빈 문자열**이다. 라이엇이 안 채웠다. 게임 bin 에는 있을 수 있는데 안 봤다 |
| 분류 버튼 어디에도 안 걸린다 | 2 | **빛나는 티끌**(태그 `CooldownReduction` — 우리 분류표에 없다) · **부서진 팔목 보호대**(주문력 40+방어력 25 인데 **라이엇이 태그를 안 넣었다** — 건메탈 군화와 같은 구멍) |
| 목록 맨 위가 칼리스타 창 2개 | 2 | 정렬이 `등급 → 가격` 인데 0G 라 맨 앞이다. 도감을 처음 열면 **살 수도 없는 전용 아이템**이 먼저 보인다 |

- **★ 정상인 것도 재 뒀다** — 페이지 넘침 0(1400·390) · 이름 잘림 0 · 조합식 트리 넘침 0
  (폰 죽음의 무도는 내부 422/상자 340 → **자체 스크롤 82px**) · 빈 `<stats>` 상자 0 ·
  조합식 참조 깨짐 0 · 조합비 음수 0 · **거르는 버튼 개수가 데이터와 정확히 일치**
  (등급 `20/23/50/112/10` 합 215 · 분류 `85/76/94/15/9/4/41`)
- **`.codex-desc stats:empty { display: none }`** — 스탯이 없는 아이템(장신구·와드)은
  `<stats>` 가 빈 채로 와서 **보라색 빈 상자**만 남는다. **도감 안으로 범위를 좁혀 접는다** —
  `TOOLTIP_STYLE_CSS` 를 건드리면 챔피언 스킬 탭까지 같이 바뀐다

### ★★ 룬 트리 — 인게임 룬 화면과 같은 자리 (2026-08-21 신설)

룬 탭 왼쪽이 **기본으로 트리**다. 계열 버튼이 곧 트리 선택이고, 오른쪽 끝 `트리 / 목록`
토글로 예전 목록을 볼 수 있다. `app.js` 의 `runeTreeHtml()` · `runeNodeHtml()` /
`style.css` 의 `.codex-rune-*` / 데이터는 `codex_data.js`.

**★★ 자리를 그리려면 데이터가 두 개 더 필요했다 — 둘 다 `build_codex_data.js` 에 추가했다.**

| 새 필드 | 무엇 | 없으면 |
|---|---|---|
| `styles[id].sl` | 계열별 **줄 4개 x 룬 id 배열** (CD `perkstyles.json` 슬롯 순서 그대로) | 줄 안의 순서를 모른다 |
| `shardRows` | 스탯 파편 **3x3** | 파편 격자를 못 만든다 |

- **★★ 예전 목록은 가나다순이었고 그게 클라와 어긋났다 — 20슬롯 중 17개.**
  정밀 핵심 룬이 `기민한 발놀림 / 정복자 / 집중 공격 / 치명적 속도` 로 나왔다
  (클라는 `집중 공격 / 치명적 속도 / 기민한 발놀림 / 정복자`). 지금은 **목록도 트리와 같은
  인게임 자리 순서**다 (계열 → 줄 → 줄 안의 자리). 두 칸 격자라 **좌→우, 다음 줄**로 읽힌다
- **★★ 파편은 같은 것이 두 칸에 나온다** — 적응형 능력치(5008)가 1·2번 줄, 체력 증가(5001)가
  2·3번 줄이다. **`shards` 만으로는 못 그린다**: `build_codex_data.js` 가 `!shards[pid]` 로
  중복을 버려서 **처음 만난 줄만** 남기기 때문이다. 그래서 격자용 3x3 을 따로 담는다.
  **★ 화면에서 켜지는 건 누른 칸 하나뿐이다** (2026-08-21 수정). 처음엔 id 로만 켜서
  **두 칸이 같이 켜졌는데 고를 때 헷갈린다는 지적**을 받았다. 칸마다 열쇠를 달아
  (`data-cell="5008:0-0"`) 그 칸일 때만 켠다 — 목록에서 골라 트리로 넘어오면 눌린 칸이
  없으므로 **처음 나오는 칸**을 켠다(`activeShardCell()`).
  **상세 뱃지는 있는 줄을 다 적는다**(`2·3번째 줄`) — 그건 파편의 성질이라 그대로 둔다
- **★ 파편 3줄은 5개 계열이 전부 같다** (실측). 그래서 한 벌만 담는다.
  달라지면 빌드가 `★ 파편 줄이 계열마다 다르다` 로 경고한다
- **★ `perk_data.js` 에도 같은 `slots` 가 이미 있다** (통계 탭 룬 빌드용). 도감은
  "파일 하나 · 런타임 의존 0" 원칙이라 `codex_data.js` 에 따로 담았다 — **패치 때 두 스크립트를
  다 돌려야 한다** (원래 순서표에 둘 다 있다). codex_data 는 163.4 → **163.9KB** (+450B)

**★★ 룬 설명은 강조를 전부 껐다 (2026-08-21, 요청).** 색·굵게·기울임·점선 밑줄이 다 빠진
맨 글씨다. `style.css` 의 `.codex-desc.is-plain` 블록 하나이고 **그 블록만 지우면 되살아난다.**

- **★ `TOOLTIP_STYLE_CSS` 는 안 건드렸다** — 같은 표를 **챔피언 스킬 툴팁과 도감 아이템**이
  같이 쓴다. 룬 상세의 두 글 상자에만 `is-plain` 을 붙여 그 안에서 끈다
- **★ 줄바꿈(`<br>` 172자리)·목록(`<li>`)·구분선(`<hr>`)은 남긴다** — 글의 뼈대라 지우면
  문장이 통째로 붙는다. 끈 것은 색 13종 · `<b>` 22 · `<i>` 62 ·
  **점선 밑줄(`lol-uikit-tooltipped-keyword` 96자리)** · 라이엇이 원문에 박은 `<font color>` 24자리다
- **★ 점선 밑줄은 우리가 넣었던 것이다** (실측이 아니라 뜻으로 고른 5종 중 하나).
  인게임에선 그 자리에 hover 용어 설명이 뜨는데 우리는 그 툴팁이 없어서 표시만 했던 것 —
  **다시 볼 때는 "툴팁을 진짜로 붙일지" 부터 정할 것**

### ★★ 룬 레벨 각주 — 롤위키 수치 + 스킬 탭과 같은 그래프 (2026-08-21 신설)

"레벨에 따라 40~160" 같은 수치 뒤에 `[1]` 이 붙고, 올리면 **1~18레벨 꺾은선**이 뜬다.
**챔피언 스킬 각주와 같은 `drawGraph` 를 쓴다.** 15개 룬 **20자리**(파편 포함).
`build_rune_graphs.js` → `public/rune_graphs.js`(3.7KB) / 화면은 `app.js` 의 `withRuneGraphs()`.

- **★★ 값의 출처는 롤위키다. 게임 데이터로는 못 만든다.**
  `perks.cdtb.bin.json` 에는 **최소·최대 두 숫자뿐**이라 그 사이를 어떻게 오르는지 모른다.
  19자리는 직선인데 **`생명 흡수`(9101) 하나가 비선형**이다:
  `1 · 1.25 · 1.5 · 1.75 · 2 · 3 · 4 · 5 · 6 · 7 · 9 · 11 · 13 · 15 · 17 · 19 · 21 · 23`.
  직선으로 그렸으면 **6레벨이 3이 아니라 4.2** 로 틀린다
- **★★ 위키는 지금 20레벨로 렌더한다** (`{{pp}}` 툴팁의 `data-bot-values` 가 20칸,
  집중 공격이 `40 – 174.12` 로 나온다). 우리는 **앞 18칸만** 쓴다 —
  **18번째 값이 한국어 문장의 최대치와 같은지 검산**하고 안 맞으면 그 자리를 통째로 버린다
- **★ 위키 문서에서 첫 `<h2>` 앞(소개·인포박스)만 본다.** 뒤는 Patch History 와 아레나
  증강체라 **옛 수치가 잔뜩 섞여 있다** (집중 공격 문서에만 옛 툴팁이 8개다).
  스탯 파편은 자기 문서가 없어서 룬 총정리 문서(`Rune`)의 표를 보고, 그 자리만 `whole: true` 다
- **★ 어느 툴팁을 쓸지 자리로 세지 않는다** — "18칸으로 자른 (1레벨, 18레벨)이 기대값과 같은 것"
  으로 고른다. 위키가 문단을 옮겨도 안 깨진다
- **★ 문장 속 자리는 `a`(문장에 그대로 있는 문자열)로 찾는다.** 패치로 문장이 바뀌면
  **조용히 안 붙는다** — 엉뚱한 자리에 붙는 것보다 낫고, 빌드가 그때 `[건너뜀]` 을 찍는다
- **★ 번호는 읽는 순서대로 매긴다.** 자리를 **원문 기준으로 다 찾아 둔 뒤 뒤에서부터** 끼운다 —
  앞에서부터 끼우면 각주 HTML 길이만큼 뒤쪽 자리가 밀린다
- **★ 단위가 다른 자리가 둘 있다**: 죽음불꽃 손길은 위키가 **틱당**이고 문장은 **초당**이라
  `scale: 2` / 절대 집중의 공격력(1.8~18)은 **게임 데이터에 없다** — 주문력(3~30)의 60%라
  검산만 주문력 값으로 한다(`bin: [3, 30]`)
- **★★ 각주는 "강조 끄기"의 예외다.** `.codex-desc.is-plain` 의 `background: none` 이
  **각주 상자 배경까지 지워서 툴팁이 투명해진다** — 실제로 그랬다. `style.css` 에서
  `.custom-footnote`·`.custom-footnote-content` 를 되살린다 (번호는 스킬 탭과 같은 보라색)

**★★ `drawGraph`/`drawSteps` 헬퍼를 `custom_values.js` → `app.js` 로 옮겼다 (2026-08-21).**
도감에서 같은 각주를 쓰는데 그 파일이 489KB 라 받게 할 수 없었다.

- **★ `window.drawGraph` 로 심는다.** `const` 로 선언하면 **옛 `custom_values.js` 가 같이
  로드될 때 같은 이름이 두 번 선언돼 그 페이지가 통째로 죽는다.** 프로퍼티면 옛 `const` 가
  가리기만 하고 에러가 안 난다
- **★ 배포 중에 옛 app.js + 새 custom_values.js 가 섞일 걱정은 없다** —
  `server.js` 의 `renderIndexHtml()` 이 모든 `.js` 참조에 `?v=mtime` 을 붙이고
  index.html 은 `no-cache` 라, 한 번 받아오면 **항상 같은 배포의 한 세트**다
  (열어 둔 탭도 옛 세트끼리라 안 깨진다). 정적 파일 자체는 `max-age=14400` 이다
- 헬퍼 위치를 적어 둔 곳이 세 군데 더 있었다 — `index.html` 주석 · `add_level_graphs.js` 가
  찍는 머리글 · `build_champion_data.js` 주석. **셋 다 같이 고쳤다**

**★★ 클래스 이름을 `.codex-rune-*` 로 뗐다 — `.codex-tree-*` 는 아이템 조합식 트리가 쓴다**
(2026-08-20 신설). 같은 이름을 쓰면 두 트리가 서로를 덮어쓴다. 클릭 위임도 갈라져 있다 —
아이템은 `.codex-tree-node[data-id]`, 룬은 `.codex-rune-node` 다.

- **★ 트리는 "룬 탭 · 트리 보기 · 검색어 없음" 셋이 다 맞을 때만이다.** 검색 중에는 목록으로
  되돌린다 — 걸러낸 결과를 자리에 앉힐 방법이 없다. `#codex-list` 에 `is-tree` 를 붙였다 뗀다
- **★ 줄은 격자가 아니라 `flex + justify-content: center` 다.** 핵심 룬이 4개인 계열이
  둘(정밀·마법) 있어서 줄마다 칸 수가 다른데, 격자로 깔면 3개짜리 줄이 왼쪽으로 몰려
  인게임과 자리가 달라진다
- **★ 계열 머리글은 sticky 다.** 그래서 `.codex-list.is-tree` 의 **위쪽 padding 을 0 으로
  두고 머리글이 자기 padding 을 갖는다** — 안 그러면 머리글 위 10px 띠로 항목이 비쳐 지나간다
- **★ 목록에서 핵심 룬은 금테 + `핵심 룬` 딱지 두 가지로 갈린다.** 금테를 `border` 로 주면
  36px 안에서 그림이 줄어 같은 줄의 다른 룬과 크기가 어긋난다 — **`box-shadow` 로 준다**
- 실측(2026-08-21): 데스크톱 1400px — 트리 노드 **71칸**(룬 62 + 파편 9) · 줄 폭 532/534 ·
  라벨 두 줄 나는 것 0개 · 페이지 넘침 0. 목록 보기는 69줄 · 핵심 룬 17개(3+3+4+4+3).
  폰 390px — 노드 폭 78 x 4칸 + gap 6 x 3 = **342px**, 페이지 넘침 0
- **★ 폰에서 트리는 `max-height: 62vh` 다** (목록은 38vh). 38vh 면 한 계열도 다 안 들어온다
- **★ 헤드리스에서 폰 넘침이 48px 로 나오는데 허상이다** — 토글의 `data-tooltip` 때문이고,
  CDP 기기 에뮬레이션이 hover 미디어를 안 바꿔서 `@media (hover:none)` 숨김이 안 탄다.
  `data-tooltip` 속성을 떼고 다시 재면 **0** 이다 (이 함정은 이번이 두 번째다)

### 화면 규칙

**★★ 목록·상세를 각각 따로 선 상자로 떼고 목록을 두 칸으로 깔았다 (2026-08-16).**
예전엔 바깥 한 상자(`.codex-body`) 안에 둘이 들어 있어 경계가 안 보였다.
지금은 **챔피언 탭(`.champ-page-wrap`)과 같은 모양** — 배경·테두리를 `.codex-list` 와
`.codex-detail` 이 각자 갖고 `gap: 20px` 로 띄운다. 검색창·거르는 줄은 상자 **밖**에 뜬다.

- **★ 왼쪽 칸 280 → 560px, 목록은 `grid-template-columns: repeat(2, minmax(0, 1fr))`.**
  **칸 하나의 실폭이 예전 한 줄짜리와 거의 같다(262 vs 259px)** — 폭을 넓힌 게 아니라
  같은 폭을 두 줄로 세운 것이라 **이름이 잘리는 정도가 안 변한다**
  (실측: 아이템 215개·룬 69개 전부 잘림 0)
- **★ `#codex-container` 를 1100 → 1400px 로 같이 올려야 한다.** 안 올리면 오른쪽 상세가
  776 → **520px 로 쪼그라든다.** 지금은 820px 이라 예전보다 오히려 넓다
- **★ `.codex-item` 의 `margin-bottom` 을 지운다.** 격자의 `gap` 과 겹쳐 두 번 벌어진다
- **★ `.codex-none`(결과 없음 줄)에 `grid-column: 1 / -1` 이 필요하다.** 안 주면
  왼쪽 절반에만 쓰여 가운데 정렬이 어긋나 보인다
- **★ `align-content: start` 가 없으면** 항목이 몇 개 안 될 때 세로로 흩어진다
- **폰에서는 `grid-template-columns: 1fr` 로 되돌린다.** 390px 에 두 개를 넣으면 이름이
  통째로 잘린다. 실측(390px): 가로 넘침 0 · 한 줄 1개
- **★ 격자는 줄 단위로 채워지므로 정렬이 "왼→오른, 다음 줄" 로 읽힌다.** 가격 오름차순이
  그대로 유지된다 — 세로로 먼저 채우는 `grid-auto-flow: column` 을 쓰면 안 된다

- **★ 주소는 `/codex` 이고 `/codex/rune` 처럼 탭도 담긴다.** 새 주소를 만들 때와 같이
  **`app.js` 의 `pathParts` 분기와 `popstate` 두 곳을 같이 고쳤다** — 한쪽만 고치면
  뒤로가기로 들어왔을 때 다르게 동작한다
- **★ 목록 스크롤을 탭 바꿀 때 0 으로 되돌린다.** 같은 `<div>` 를 재사용해서
  `scrollTop` 이 남는데, 그러면 룬 탭으로 옮겼을 때 **아이템 탭에서 보던 위치**가 유지되고
  고른 항목(맨 위)이 화면 밖에 있다. `renderList(keepScroll)` 의 인자가 이것이다
- **★ 목록 부제에 등급을 같이 적는다.** 정렬이 `[등급 → 가격]` 이라 등급이 넘어가는
  자리에서 가격이 되돌아가는데(1,300 다음에 400), 등급이 안 보이면 **정렬이 깨진 것처럼 읽힌다**
- **★ 검색은 `getChosung()`·`koCandidates()` 를 그대로 쓴다.** 챔피언·랭킹과 같은 헬퍼다 —
  표를 두 벌 두면 어긋난다. 아이템은 여기에 **DD 의 `colloq`** 를 더한다:
  라이엇이 넣어 둔 별명이라 `무한의 대검 → 인피/무대`, `장화 → 똥신` 이 공짜로 걸린다
- **★★ 검색 대상(`matches` 의 `hay`)에 공백을 지운 판을 같이 담는다 (2026-08-25 수정).**
  안 담으면 **초성 검색이 통째로 죽는다** — `getChosung('무한의 대검')` 이 `ㅁㅎㅇ ㄷㄱ` 라
  공백까지 쳐야 걸렸다. 영향이 **아이템 184/215 · 룬 46/62 · 파편 6/7** 이었다
  (주문 9개는 이름에 공백이 없어 원래 됐다). **챔피언 검색은 원래 `name.replace(/\s+/g,'')` 로
  지우고 있었고 도감만 그 규칙에서 빠져 있었다.**
  **★ 원본도 남긴다** — `무한의 대검` 처럼 공백째로 치는 사람이 있다. 지운 판만 두면 그게 죽는다
- **★★ 영문 별명은 아직 안 걸린다 (알고 두는 것, 2026-08-25).** `koCandidates()` 가
  **영타→한글 변환 후보만** 만들고 원문을 안 넣어서다 — `koCandidates('ie')` 는 `["댜"]` 고
  `'boots'` 는 `["ㅠㅐ샌"]` 이다. **별명에 영문이 든 아이템이 125/215** 라 `ie`·`infinity`·`boots`
  가 전부 헛돈다. 랭킹 검색(`buildRankQuery`)은 원문을 항상 넣고 **영문일 때만** koCandidates 를
  더해서 안 그렇다. 고치려면 도감도 같은 꼴로 원문을 후보에 넣으면 된다
- **★ 이름을 `onclick` 문자열에 박지 않는다.** `data-id` + 위임이다 (랭킹 표와 같은 이유)
- 조합식은 재료·상위 아이템을 아이콘 줄로 깔고 **누르면 그 아이템으로 옮겨간다.**
  이때 분류·검색을 풀어야 한다 — 안 그러면 목록에 없어서 아무 일도 안 일어난다
  - 조합 재료가 **215개 안에서 하나도 안 빠진다**(누락 0건). 트리를 그리려면 그릴 수 있다
  - `from`/`into` 에서 **목록에 없는 id 는 버린다.** 다른 모드 전용 상위 아이템이 딸려온다
- **★ 조합식 트리를 그렸다 (2026-08-20).** 설명과 "재료" 줄 사이, `recipeTreeRow()` +
  `.codex-tree`. `f` 를 재귀로 따라가고(추가 데이터 0 — 위키 필요 없다), 연결선은 li 위
  패딩 안 가상요소다(::before 왼쪽 가로선 / ::after 오른쪽 가로선 + 가운데 세로선.
  첫/끝 자식은 바깥 가로선만 지우면 외자식은 세로선만 남는다). 노드 클릭은 재료 줄과 같은
  위임을 탄다(`.codex-tree-node[data-id]`, 뿌리는 data-id 를 안 붙여 뺀다).
  실측: 최광폭이 죽음의 무도(잎 7개·3단)이고 최대 깊이는 전 아이템이 3단이다.
  - **★★ 폰에서 트리가 카드를 뚫었던 원인은 `.codex-recipe` 의 `align-items: flex-start`.**
    폰 블록이 column 으로 바꾸는데 flex-start 가 남으면 "자식 폭을 내용에 맞춰라" 가 되어
    트리가 422px 로 벌어진다 — **`.champ-page-wrap` 과 같은 함정, column 전환엔 stretch 를
    같이 넣을 것.** stretch 후에야 `.codex-tree` 의 `overflow-x: auto` 가 자기 가로 스크롤로
    처리한다 (죽음의 무도 390px 실측: 페이지 넘침 0, 트리 내부 스크롤 82px)
- **`.codex-recipe-label` 은 `white-space: nowrap`.** "상위 아이템" 이 62px 에서 두 줄로 갈라졌다
- 폰에서는 목록·상세를 위아래로 쌓고 `height: 72vh` 를 푼다. 목록은 `max-height: 38vh`.
  **상자가 둘로 갈리면서 예전의 `border-top` 구분선 꼼수는 지웠다** — 이제 상자 자체가 갈린다

## 신화급 상점 → **`docs/신화상점.md`**

윈도우 로컬 수집기가 롤 클라 화면을 읽어 서버로 보내고, 홈 위젯과 신화상점 탭이 그걸 보여준다.
**문서를 통째로 옮겼다 (2026-08-21, CLAUDE.md 가 한도를 넘어서). 건드릴 거면 그 파일을 먼저 읽을 것.**
들어 있는 것 —

- 서버(`mythicShopSchema` · `POST /api/mythic-shop` · GET 4종)와 화면(`showMythicShop`) 배선
- **검증 규칙** — 구획마다 다르다(일일만 `icon|emote` + 가격 5/25 고정) · 401 `detail` 3종 · 409 중복 처리
- **`items: null` 을 반드시 구분해서 보여줄 것** — 수집이 반자동이라 안 누른 날은 데이터가 아예 없다
- 구획 4개(`featured/biweekly/weekly/daily`)와 `date + section` unique 인덱스
- 카드 그림 규칙(스킨은 가로 판본으로 갈아끼움 · 넥서스 효과는 webm) · 추천 구획 격자
- **Cloudflare 가 기본 User-Agent 를 막는다 (`403 error code: 1010`)** — 새 클라이언트를 붙일 때마다 걸린다

## MongoDB 용량 (2026-08-15 정비)

**★★ Atlas 무료 티어의 512MB 는 `dataSize`(압축 전) 기준이다.**
`storageSize`(압축 후)를 보고 "90MB 밖에 안 쓴다" 고 판단하면 틀린다 — 같은 시점에
`dataSize` 는 **342MB** 였다. **대시보드의 "Data Size" 가 기준이다.**

```
db.stats()  →  dataSize    342.1 MB   ← Atlas 가 재는 값
               storageSize  84.7 MB   ← 압축 후. 이걸 보면 안 된다
```

### ★★ 타임라인이 617KB 다 — 용량의 절반이었다

> **★ 여기서 말하는 건 `matchcaches.timeline`(라이엇 원본 통째)다.** 2026-08-26 에
> 통계용으로 받기 시작한 건 **`matchstats` 의 `sk`·`it` 슬림 필드(판당 2.06KB)** 라
> 다른 것이다. **원본을 통째로 저장하지 말라는 이 규칙은 그대로 유효하다.**

`matchcaches.timeline` 은 **한 개가 617KB** 로 `detail`(76KB)의 **8배**다. 전적 상세를
펼칠 때만 받아서 287판밖에 없었는데도 **173MB(전체의 절반)** 를 먹고 있었다.
2026-08-15 에 전부 지웠다 (342.1 → 126.3MB, **215.7MB 확보**).

- **한 문서 안의 필드에는 TTL 을 못 건다.** 남기려면 별도 컬렉션으로 분리해야 한다
- **다시 저장할 이유가 별로 없다.** 프론트에 이미 캐시가 있어서
  (`window.matchTimelineCache`, `app.js:94` / `ensureTimeline()`)
  **껐다 켰다 100번 해도 라이엇 호출은 1회**다. 실패해도 `loaded: true` 로 찍어 재시도를 안 한다.
  차이가 나는 건 **새로고침한 뒤뿐**이고, 그것도 서버 메모리 캐시로 막을 수 있다 —
  전적검색은 `myCache` 를 쓰는데 **타임라인 엔드포인트만 안 쓰고 있다**

### ★ `createdAt` 이 없으면 TTL 이 안 걸린다

96판이 그랬다. 스키마에 `default: Date.now` 가 붙기 전에 만들어진 문서라 **TTL 인덱스가
그냥 지나쳐 영원히 남는다.** 2026-08-15 에 지웠다 (6.8MB). 새 스키마를 만들 때
`createdAt` 기본값을 빠뜨리면 같은 일이 생긴다.

### ★★ 스키마에 인덱스를 적어도 실제로 안 만들어진다 — `ensureStatIndexes()` (2026-08-15)

**mongoose 가 `.index()` 선언을 DB 에 반영해 주지 않고 있었다.** `unique: true` 로 붙은
`matchId`·`puuid` 만 살아 있었고, 따로 선언한 것은 **하나도 없었다** —
`matchcaches` 의 `detail.metadata.participants`(429 폴백 조회용),
`summonercaches` 의 `displayName`·`namePartLower+tierScore`(자동완성용),
그리고 새로 만든 **TTL 인덱스 두 개(`matchstats` 45일·`matchseens` 3일)까지**.
TTL 이 없으면 그 컬렉션은 **영원히 안 지워진다** — 슬림 통계가 1년에 1.4GB 로 불어난다.

`startJobs()` 첫머리의 `ensureStatIndexes()` 가 서버 시작 때마다 스키마와 실물을 맞춘다.
**새 인덱스를 선언하면 이 함수의 `want` 배열에도 같이 적을 것.**

- **★★ 이미 있는 TTL 은 `expires` 를 고쳐도 안 바뀐다.** 인덱스는 한번 만들어지면 정의가
  고정된다. 스키마만 고치고 끝내면 **"코드에는 7일이라고 적혀 있는데 실제로는 14일"** 인
  상태가 조용히 유지된다
- **★ Atlas 무료 티어(M0)는 `collMod` 권한이 없다.**
  `user is not allowed to do action [collMod]` 가 뜬다. 그래서 `ensureStatIndexes()` 는
  실패하면 **인덱스를 지우고 다시 만드는 쪽으로 넘어간다.** TTL 청소는 60초 주기라
  그 틈에 새는 문서는 없다

### 현재 구성

2026-08-15 정리 후 **dataSize 21MB** 다 (그날 아침 342MB 에서 내려왔다).

**★★ TTL 은 2026-08-26 에 다시 줄였다 — `matchcaches` 3일 · `matchstats` 21일.**
타임라인을 전수로 받기 시작하면서 슬림 문서에 `sk`·`it` 이 붙었기 때문이다 (위 세션 절).

```
matchstats      통계용 슬림 + 타임라인  (TTL 21일 · ★ 얼어붙은 패치 원본은 바로 지움 → 최고 310 · 평균 180MB)
matchcaches     검색용 원본             (TTL  3일 · 실측 3.0MB / 35건)
summonercaches  닉네임 사전             (TTL 없음, 계속 누적 · 15MB, 인덱스 포함)
champstats·champbuilds·champmatchups     집계 결과 (패치당 ~33MB, 인덱스 포함 · 끝난 패치는 박제로 파일화 가능)
나머지 + 인덱스                          약 10MB
─────────────────────────────────────────────────────
정착점 ~~약 368MB~~ → 2026-08-27 재계산: 최고 약 440MB + 안 뺀 옛 패치 33MB/개 (실측 8/27 213MB)
```

- **★ 2026-08-27 실측 기준으로 위 숫자를 다시 잡았다** (데이터 164 + 인덱스 49 = 213MB). 타임라인 붙은 문서가
  저장 시 ~4.5KB 라 예전 368 은 과소였고(505 였다), 얼어붙은 패치 원본 삭제로 다시 내려왔다.
  **집계 3종은 인덱스가 데이터만큼 크지만 낭비가 아니다** — `_id` + 조회용 `(scope,champ)` 뿐이고 행이
  27만 개라 그런 것. 줄이려면 끝난 패치를 `build_stats_archive.js` 로 파일화하는 것뿐이다 (마감 없음)

**★ TTL 계산 근거** (2026-08-15 최초). 원래 `matchcaches` 14일 + `matchstats` 90일이면
`185 + 7 + 351 = 543MB` 로 **512MB 를 넘었다.** 그래서 7일 + 30일로 줄였고,
타임라인이 붙으면서 **3일 + 21일**이 됐다.

- **`matchcaches` 를 줄여도 손해가 거의 없다** — 검색 트래픽이 거의 없어서 실측이
  **3.0MB / 35건**뿐이다. 2주씩 들고 있을 이유가 없었다
- `matchstats` 는 45일이었는데 **룬·소환사 주문을 넣으면서 한 건이 1.31 → 2.32KB 가 되어**
  30일로 줄였고, **타임라인(판당 2.06KB)까지 붙어서 21일**이 됐다.
  21일이면 패치 1.5개분이라 재집계 폭으로는 충분하다
- ~~**★★ TTL 을 줄이면 박제 시한이 같이 당겨진다** — 첫 수집 8/16 + 21일 = **9/6 이 한계**다.~~
  **2026-08-27 부터는 아니다** — 닫힌 패치는 재집계를 안 하므로 TTL 이 통계 값에 영향을 못 준다.
  TTL 은 이제 **"현재 패치를 원본으로 재계산할 수 있는 폭"** 만 뜻한다 (패치 2주 + 여유).
  **TTL 을 만질 땐 위 "시한이 있는 것" 표를 같이 고칠 것**

- **`summonercaches` 는 사실상 "거쳐간 랭커 명단" 이다.** 28,806명 중 현재 랭커는
  10,992명(38%)뿐이고 **전적검색으로 들어온 사람은 57명(0.2%)** 이다. 명단에서 빠지면
  갱신 대상에서도 빠져 화석이 된다 (마지막 갱신이 90일을 넘은 줄이 9,238명)
- **★ `matchcaches` 손잡이는 이미 다 썼다** (14 → 7 → **3일**). 다음에 빠듯해지면
  **`matchstats` 를 줄이는 수밖에 없는데, 그건 박제 시한을 같이 당긴다** —
  줄이기 전에 박제부터 끝낼 것

## CD 스킬 데이터 작업 (현재 주 작업) → **`docs/스킬데이터.md`**

스킬 문장·수치 작업 전체. **문서를 통째로 옮겼다 (2026-08-19, CLAUDE.md 가 한도를 넘어서).
패치가 왔거나 스킬 데이터를 건드릴 거면 그 파일을 먼저 읽을 것.** 들어 있는 것 —

- `custom_templates.js`(문장) / `custom_values.js`(수치) 구조와 `MANUAL` · `KEEP_TEXT` 표
- 빌드 스크립트 15개 표와 패치 대응 순서 (아래 요약)
- **다시 알아내지 말 것 목록** — `mStat` 번호표, 배열 시작 인덱스, 사거리 더미(25000 / 20 이하),
  투사체 속도 오염, 위키가 우리보다 틀린 사례, 회색 글씨(`keyTooltipExtendedBelowLine`)
- 스탯 탭 · 레벨 곡선 · 각주(`custom_graphs.js`) · 역할군 평균 · vs 챔피언
- **남은 일 표** (★ 20번 패치 원본 박제가 우선순위 높음. 25번 `DD_VER` 하드코딩은 2026-08-24에 끝냈다)

**★ 패치가 오면 이 순서로 돌린다** (앞이 뒤의 입력이라 순서가 중요하다).
**패치 당일에는 한국시간 오후 4시 이후에** — DD 는 수 04시 KST, CD 는 그로부터 12시간 뒤다:

**★★ 버전은 이제 손으로 안 올린다 (2026-08-24).** `dd_version.js` 한 곳이 `versions.json` 의
최신을 받아 오고 넷이 그걸 쓴다 (`build_level_curves` · `build_codex_data` · `build_perk_data` ·
`build_champion_lore`). **옛 버전 주소가 안 죽는 게 문제였다** — `.../cdn/16.16.1/...` 은 패치가
지나도 404 가 아니라 **지난 패치 데이터를 정상 응답으로 준다.** 그래서 안 올리면 스크립트가
멀쩡히 돌고 결과 파일도 만들어지는데 **내용만 옛 패치**였고, 도감·룬은 몇 줄만 바뀌어 diff 로도
안 드러났다.

**★★ 그리고 "오후 4시" 규칙을 코드가 지킨다.** DD·CD 를 둘 다 쓰는 셋(레벨곡선·도감·룬빌드)은
CD 의 `content-metadata.json` 으로 패치를 대조해서 **CD 가 아직 옛 패치면 아예 멈춘다.**
자동으로 최신을 따라가면 그 12시간 창에 돌렸을 때 **DD 만 새 패치인 짝짝이**가 나오는데,
손으로 올리던 시절엔 "아직 안 올렸다" 가 그걸 자연히 막고 있었다. `DD_VER=16.16.1` 처럼
직접 주면 그 값이 이기고 대조도 건너뛴다 (일부러 옛 버전으로 빌드할 때).

```
node build_champion_data.js --refresh     # 문장
node fill_values.js --refresh --write     # 수치 (custom_values.new.js → 확인 후 이름 변경)
node build_level_curves.js --write        # 레벨 곡선
node add_level_graphs.js --write          # 각주 (custom_values.js 를 읽으므로 위가 먼저)
node build_stats_page.js --write          # 스탯 탭
node build_codex_data.js --write          # 도감 탭
node build_perk_data.js --write           # 통계 탭 룬 빌드 아이콘
node build_rune_graphs.js --write        # 도감 룬 레벨 각주 (★ 도감 다음에 — codex_data.js 를 읽는다)
node build_wiki_stats.js --refresh       # 롤위키 우상단 값 → wiki_stats.json (★ fill_values 보다 먼저 — 그게 이걸 읽는다)
```

**★ 받은 뒤 반드시 확인할 것 — "값이 통째로 평평해진 스탯".**
`champion_stats_by_level.json` 에서 스탯별로 "18칸이 전부 같은 값인 챔피언 수" 를 세어
이전 판과 비교한다. 갑자기 173/173 이 되면 그 스탯이 죽은 것이다 —
**압축본(`champion_stats.js`)은 4줄만 바뀌어서 diff 로는 안 보인다.** 자세한 건 위 문서에.
